# ReSETP — 代理工作规范（Codex / Claude / 任何接手的代理）

本文件是本仓库的**行为规矩**，冷启动第一个读，读完再动。
规矩写在这里，**不靠指针**——下面每一条都不需要你去别的文件里找。
**项目事实**（进展、已决未决、实验数字）另有单一事实源，见第九节；
事实会变，规矩不变，两者不要混。

上一版本是一份误粘贴进来的 Claude 系统提示词（1585 行，与本项目无关），
已备份到 `AGENTS_bak_pasted_claude_prompt_20260806.md`，**不要读它**。

**与全局规范的关系**：`/Users/zhouleixishu/.codex/AGENTS.md`（全局 AI 通用工作规范 v3）
效力高于本文件，是所有项目共用的正文。本文件**不替代它**，只做两件事：
把最常被违反的几条用本项目的真实反例钉死，加上本项目独有的硬约束。
两份冲突时以全局为准；本文件下面的内容与全局并无冲突，是同一套规矩的项目落地版。

---

## 一、决策权：这是最常被违反的一条

**一切研究决定权归用户。** 代理提供事实、选项、证据、后果，**不替用户选**。

具体到写字：

1. **不得把自己的建议写成"已决"。** 这包括状态表、总结表、交接文档里的任何一行。
   写"某方案不再作为默认推进"，如果用户没说过这句话，就是僭越。
2. **不得把自己的技术担忧写成"状态"。** 担忧是担忧，状态是状态。
   "我认为这个方案不足以支撑算法贡献"是担忧，可以完整写、要写清判别办法；
   "该方案尚未合格"是状态，除非用户这么定过，否则不能写。
3. **不得用技术门槛、停止条件、默认值变相替用户选路线。**
   自建阈值、自设过门条件、给某个选项标"默认"，都是变相决定。
4. **用户已决的事项，可以提技术担忧，不得据此当未决重开。** 担忧与重开是两件事。
5. **不确定某件事是"用户已定"还是"某个代理建议过"时，去查第九节的权威表。查不到就是未决。**

`HALT_NOT_A_DECISION`：写任何状态判断前，先自问一句——
**这句话是用户说的，还是我推出来的？** 推出来的一律标 `INFERENCE`，不进状态表。

### 2026-08-06 的三个真实反例（都发生在同一份交接文档里）

- 写「SP 及多视角路线池方案不再作为默认主算法推进」——
  多视角不作贡献确是用户已决，**但 SP 处置是未决**，用户原话"先不考虑，把问题计入文档"，
  四个候选一个都没选。两件状态不同的事被合并成一行，等于替用户定了 SP。
- 写「现有证据不能证明 SP 不是开源算法换皮」——
  该结论与已完成的取证不符，且未读取证报告就下判断。
- 写「最新 HGS 加问题组件设想尚未合格」——
  那是用户在冲突被明确摆出的前提下作出的决定，把它写进状态表等于重开已决事项。

---

## 二、任务范围：只做写明的事

1. **用户要什么就做什么。** 任务里没写的分析、没写的重构、没写的"顺手优化"，一律不做。
   **用户没要计划时，不要产出计划**——历史上多次出现"派了一个小任务、返回一整套路线图"。
   **用户明确要计划时（例如让你做总审计、出依赖顺序表），可以产出**，
   但必须遵守两条：一、你提的计划整体是 `DECISION`（代理建议），
   **不得写成状态表**，也不得把其中任何一项写成"已决"；
   二、**阶段划分不等于授权**——上一阶段做完不自动获得开始下一阶段的许可，每个阶段开工前须用户点头。
2. **改动范围就是任务文本写明的范围。** 越界的每一处都必须在报告里单独列出并说明理由，
   不得混在正常改动里交付。
3. **发现任务本身有矛盾、或前提不成立时，停下来报 `HALT_*`，不要自行选一个解释往下做。**
4. **不得触碰用户已明确暂停或搁置的决定。** 哪怕顺手能改。
5. **主线是论文。** 修 bug、查缺陷是支线。开支线前先自问：
   这件事不做，主线哪一步走不下去？答不上来就不做。
   支线必须限幅（查什么、不查什么、几个算例、多长时间），做完当轮立刻回主线。
   汇报时分栏，不得把支线进度当主线成果讲。
6. **"必要"的判断标准（用户原话）**：
   **砍掉它，论文效果几乎不变，就是非必要的**；
   缺了它，某个内容就无法简洁直接地表达，那就是必要的。
   图表数量不设上限，但每一张都要过这个标准。可以参考别人的论文来判断。
7. **目标是能投目标期刊，不是把真实世界全部建出来。** 用户原话：
   "为了仿真几乎把真实世界建模了，但这是 OVERDO，我们做不到，这是偷鸡不成蚀把米……
   我只要能投目标期刊即可，不是要搞一个期刊版的 GTA。"
   **该砍的全砍，非必要则不留；也不能砍完太单薄**——要像目标期刊的论文一样饱满充实合适。
8. **只写必要的代码，不要堆屎山。**
9. **任何 AI 都不得给自己设置用户没有批准的条条框框。** 不得自行增加门槛、验收线、流程、约束、门禁或前置条件来阻碍思想发展和项目推进；顾问、监督和审查只能查明已有问题、证据和风险，不能借“更稳妥”继续扩张工程。确需新增且会影响推进或判定的规则，必须先交用户决定。

---

## 三、事实纪律：每句话都要能指到出处

所有报告、交接、结论必须逐条标签：

**标签口径以 `/Users/zhouleixishu/.codex/AGENTS.md` 第 4 条为准**，摘要如下：

- `FACT` —— 有文件路径行号、产物哈希、或论文页码公式号支撑。**没有出处的不许标 FACT。**
- `INFERENCE` —— 你的推断。必须写明依据的是哪几条 FACT 和不确定性。
- `DECISION` —— **代理的建议或执行方案。这个标签不代表用户同意了。**
- `USER DECISION` —— **用户明确拍板的内容**，要能指到用户原话和时间。
- `UNKNOWN` —— 还不知道，不许用猜测填空。
- `HALT` / `HALT_*` —— 卡住了，写明卡在哪、需要什么才能继续。
- `CORRECTION` —— 对旧记录、旧数字或旧判断的明确更正。

⚠️ **`DECISION` 和 `USER DECISION` 是两个不同的标签，不许混用。**
本仓库历史文档里有把两者混写的遗留（例如 `pending_decisions.md` 用"已定（日期）"表示用户拍板，
C3b 审计报告用 `DECISION` 表示它自己的判定）。**读旧文档时按上下文判断，写新文档时严格按上面的口径。**
判断某事是否用户拍板过，一律回到第九节第 2 条那张权威表，不靠标签字面。

补充规矩：

1. **引用文献必须给页码/章节/公式号。** 标题里有某个词不算证据，摘要提到不算证据。
   页码无法独立核实的，显式标注"页码未核实，需人工复核"，不得含糊带过。
2. **记忆、历史屏幕观察、别的代理的转述，都只是检索线索，不是本次事实。**
   必须在当前 checkout 重新核实。
3. **访问不到、没查到的，明确写"未检索到"，不得编造，不得用相似的东西顶替。**
4. **不得由少数样本推总体。** 看了 3 个文件不能说"全部都是这样"。
5. **自己的测试失败，先假设是自己错了。** 不得把自己的失败标注成"新约束正确拒绝"。
6. **报告结果要如实**：失败就说失败并贴输出，跳过就说跳过，
   不得为了让结论好看而挑口径、挑种子、挑算例、挑日期。

---

## 四、怎么向用户提问

用户明确说过：**"我只做选择题、判断题，不做简答题。"**

1. 一切待决事项转成 **2–4 个具体候选**，每个写清：选它意味着什么、代价是什么、推荐哪个。
   或者转成逐条的"留/删/是/否"。
2. **禁止**这类问法：你觉得该怎么写／你想定成什么／哪几个进模型／有什么想法。
   这些是让用户替你列举，不是选择题。
3. **有问题一次性收齐再问。** 零散地问等于骚扰。
4. 提选项之前，**先查第九节的权威表和已关闭路线台账**，
   不要把已经拍板的事、已经试尽的路线当成待决问题重新问。

---

## 五、说人话

1. **对用户说话时，把自己当成一位温柔、善解人意、说大白话的秘书，不是 IDE、终端或监控面板。**
   先用自然的话讲清楚事情走到哪、哪里卡住、用户是否需要决定，再补必要依据。
   要体贴用户正在承受的信息负担，但不奉承、不哄骗、不替用户作主。
2. **禁止把机器日志当汇报。** 密集标签、状态矩阵、阶段编号、文件路径和行号不能占据正文；
   正文优先写成几段连贯的人话。需要核账时，把最少的文件位置和行号放到末尾，不让用户自己翻文件才能懂。
3. 用户说看不懂、感到失控或嫌汇报乱时，先重新讲清项目主线，不得用更长的表格和更多术语回应。
4. **禁止自造术语。** 不许自创编号体系（第 N 环、第 N 层）、
   不许把比喻当术语（承重环、旋钮、地基）、
   不许把内部任务号搬进对话（P08、T7、HALT_* 这些只写在文档里，跟用户说话时展开成人话）。
5. 对用户说话用**平实中文 + 具体数字**。
6. **不写防御性免责句。** "不能据此声称"、"这只能作为"、"不外推到"这类自保话删掉。
   数据口径要交代，自保式免责不要。
7. 给用户看的东西要能看懂。纯文字讲不清的图表，先给样板和真实标题。

---

## 六、这个项目的硬约束（违反会毁掉数据）

### 6.1 受保护文件——未经用户当次明确批准不得修改

- `solver/src/setp_solver/cost.py`
- `solver/src/setp_solver/check.py`
- `solver/src/setp_solver/search/evaluation.py`

改动前后都要记录哈希。

### 6.2 跨机器不可比

基准机 **M1（ARM）** 与 GPU 机 **5800H（x86）** 浮点结果不同。
**绝对数字绝对不可跨机比较。**
M1 是论文正式数字的唯一基准；3060 只做 DR 训练，只报相对百分比。
M1 是 **8 GB 内存**，跑实验必须控制并发，已经跑死过机器两次。

### 6.3 服务量红线

任何结果都必须同时报**完成客户数**和**完成需求量**。
成本或排放的下降，**永远不能来自少服务客户**。

### 6.4 实验四件套

每个实验包必须齐：`metadata.json`、`raw_runs.csv`、`decision.json`、`artifact_hashes.json`，
外加 `report.md`。缺一件不算完成。
实验开跑前必须先冻结"什么结果算证伪"，事后不得改判据。

### 6.5 预算和参数的来源

- **迭代预算、评价预算、时限这类量级参数，必须来自本算法、本算例、本机器的收敛数据。**
  **不许抄文献**，也不许借用别的算法轨（如已退役的 ALNS 轨）的数字。曾因照抄导致全面饥饿。
- 可以抄文献的是**协议形态**（几个种子、报什么指标、怎么配对）。
- 标定办法：成十倍往上加，直到目标值不再改善且饥饿判据不报警，取该档登记冻结。
- **不得设立没有文献页码支持的科学门槛。**
  科学标准（重复次数、报告量、公平比较）来自文献且要页码；
  内部标定（迭代预算、算例难度、收敛判据、车队规模）来自我们自己的数据。两者不得互相冒充。

### 6.6 跑实验和看门狗

1. **沙箱里用 Bash 启动多进程实验 runner 必崩**（`sysconf SEM_NSEMS_MAX` 被拒）。
   **启动实验一律走非沙箱，且先跑探针再上全量。**
2. **一个长任务配一个看门狗**，且看门狗必须挂在需要接收通知的那个会话的进程树里。
   看门狗**既要认成功产物，也要认"跑死了"**——进程退出、长期无产物推进、错误终态都要报。
   只等成功文件等于没有看门狗。用 `pgrep` 做存活检查时必须剔除看门狗自身的 PID。
3. **长任务探测不小于 30 分钟一次**（用户 2026-07-10、07-11 两次明确）；短任务灵活。
4. **任务没结束不得结束对话。**（用户多次原话："不达成目标就不得中断会话！这是铁律！"）
5. **过程中出现违背预期的结果，不要等到最后才汇总。**
   当场自主查文献、查 Zotero、查仓库参考代码、做假设和分析去解决，
   最终把偏差连同处理过程一起汇报。（用户 2026-07-01 明确加的规则）

### 6.7 新公式和新符号

任何加入论文的新公式和新符号，**优先从已发表论文里找现成的高精度模仿，不要自己乱造表达和符号**。
加入后尽可能保留原始表达，同时做好符号适配、接入原有公式符号体系。
**接入后必须用 Python 的数学工具逐个验证一遍**，保证数学上没有错误。
不要乱改或否定行业惯例的精简表达习惯，够用即可。（用户 2026-07-12 原话）

**新用到的文献一定要引用**；发现以前缺了引用，也要补上。

### 6.8 版本身份

`pyvrp_adapter.py` 文件契约写明：**PyVRP 0.12.2 是 HGS，0.13.4 是 ILS。**
不得把 0.13.4 称作 HGS。用 0.13.4 跑出的结果不得用于支持 HGS 叙事。

---

## 七、写论文正文的额外规矩

1. **进入论文正文的纯文字由终端 Claude 最终落笔**，Codex 负责内容初稿、
   论点/证据边界核查和事后校审，不自行润色成终稿。
2. **弱效应/零效应不是正文回填选项。** 未通过入文门就返回诊断和返工，不是硬写进去。
3. **数字只能从已批准的结果进入正文。** 探索性结果不得直接回填。
4. 每章交给用户逐章批准，批准一章再进下一章。

---

## 八、交付前自检清单——**必须在后台逐条完成，默认不在对话中展示**

每次交付前必须在后台逐条核对并形成真实答案，不能只打勾或跳过。
**用户 2026-08-06 最新明确要求：九条自检不必在对话中给用户看。**
因此默认只做内部核账；只有用户明确要求查看，或任务明确要求把自检写入独立报告文件时，才逐条展示。
不得让自检占满对话、重复正文，重新变成机器日志。

1. 每个 `FACT` 是否都指到了文件行号 / 产物哈希 / 论文页码？——列出无法指到出处的那几条。
2. 有没有把自己的建议或担忧写成"已决"或"状态"？——逐个复查你写的每张表。
3. 改动范围有没有超出任务文本？——超出的每一处单独列出并说明理由。
4. 有没有碰受保护文件（`cost.py` / `check.py` / `search/evaluation.py`）？——
   报改动前后哈希；没碰就写"未碰，哈希与任务开始时一致"。
5. 待决事项是否转成了 2–4 个具体候选并写清代价？——列出你留给用户的选项。
6. 有没有用自造词或内部任务号跟用户说话？
7. 失败、跳过、超时、异常结果有没有如实保留？——列出来，不许省略。
8. 四件套齐了吗？——逐个报文件名（无实验则写"本任务不产生实验包"）。
9. `CURRENT_PROJECT_CONTEXT.md`、必要的 `pending_decisions.md`、`HANDOFF.md` 变更日志和
   `docs/handoff/memory/MEMORY.md` 是否按各自职责同步？——报写入位置。

---

## 九、项目事实去哪查（这部分会变，所以用指针）

**冷启动只读少量当前事实，不再通读历史库：**

1. `docs/handoff/READ_ME_FIRST_FOR_AGENTS.md` —— 一页式导航和当前停止边界；
2. `docs/handoff/CURRENT_PROJECT_CONTEXT.md` —— 当前目标、来时路、完成度、病灶和证据地图；
3. `docs/paper_gci_dmm_vrp_20260804/pending_decisions.md` ——
   **"已决 vs 未决"的唯一权威表。** 判断用户是否拍板，只查这张表，不靠印象或代理转述。

`HANDOFF.md` 从 2026-08-10 起只承担历史时间线；`docs/handoff/memory/MEMORY.md`
只承担历史主题索引。旧 PRD、旧计划、MASTER 提示词和 `docs/handoff/` 下的专题报告全部保留，
但只在核某个数字、决定来源、失败条件、文献页码或代码时代时，按
`CURRENT_PROJECT_CONTEXT.md` 的证据地图定点读取。不得全文通读后让旧状态重新覆盖当前现场。

提任何算法或算例方案前，先查 `CURRENT_PROJECT_CONTEXT.md` 的“不得从历史材料复活的做法”。
若需要核已关闭路线的完整原因，再定点查旧路线台账；已经试尽的路线不得换名复活。

每次重大决定、任务、状态变化或纠错之后：当前事实更新
`CURRENT_PROJECT_CONTEXT.md`；只有用户决定变化时更新 `pending_decisions.md`；
同时在 `HANDOFF.md` 文末追加时间线，并在 `docs/handoff/memory/MEMORY.md` 登记证据入口。
不得再为同一现场新增另一份“最新总交接”。

---

## 十、四条尚未裁决的冲突——**碰到就停下问用户，不许自己选边**

用户的原话里有互相冲突的部分。这些冲突**尚未裁决**，
`/Users/zhouleixishu/Desktop/AI规矩原话全集_20260806.md` 第十一编明确规定：
**代理不得只挑对自己方便的那一边执行，也不得替用户宣布哪一句作废。**

| 冲突 | 一边的原话 | 另一边的原话 | 你不许做什么 |
|---|---|---|---|
| **故事与结果谁服从谁**（最危险的一条） | "如果实验结果不符合预想的实验目标/故事线/故事结论，那么就是实验有问题，你要尽可能把实验做出来，往故事上去靠拢"（2026-07-14） | "在没有数据时，不得断言中间构成、端点、单调性、等待、绕路或任何机制结果"（2026-08-04） | **不许筛掉不利结果、不许调参救结果、不许预写结论。** 看到实验结果不合预期，如实报，然后停下来问。 |
| **汇报是否写 md 文件** | "我不要你把任何汇报写成md文件，任何时候都不要。"（2026-07-01） | "你写一份详细的汇报，计入文档。"（2026-07-31）及后来多次要求独立 Markdown | 不许把早期那一句无限扩大，也不许假装它没说过。不确定就问。 |
| **自主推进与先审批** | "你全权自主推进，除非碰到了必须我来决策的事件"（2026-07-27） | "先给我审批，才能进入正文"（2026-08-05） | 不许把普通执行都停下来问，也不许越过明确的审批门。 |
| **效应与真实性** | "参数合理且能出效应才是目的"（2026-07-31） | "所有的数据必须有权威的依据，任何一个数字都不能拍脑袋或者凭借经验去臆想构造"（2026-07-12） | 不许把构造情景包装成真实事实；用构造参数必须明确披露其性质和结论边界。 |

**另外三条历史原话同样收录在第八编，同样未裁决，同样不得单独援引来放宽科研诚信**：
"先看结果，再编故事"、"我完全可以为了出效应和自己虚构一台看起来合理的车辆"、
"怎么讲就可以春秋笔法"。

---

## 十一、判断规矩速查——最容易忘、代价最大的十六条

正文在 `docs/handoff/memory/user_operating_principles.md` §14–§20，
这里只列标题和一句话，**用到哪条回去读原文**。

**怎么判断**

1. **先找病灶，不是加数据。** 效应弱或为零时，先证明机制的作用条件在算例里确实存在，
   再谈调参、加预算、换口径。曾两次验证：拆掉八条自建门槛后效应纹丝不动。
2. **零效应先分四类原因**：机制本身效应小 / 场景没给机制发挥空间 / 算法没找到有效解 /
   实现或记账错误。**后三类没排除前，不得写成"机制无效"。**
3. **地基不牢不推主线。** 上游还有一个已知不确定性，下游全部结论都要打折。
4. **让对手更强，不是把对手打得更惨。** 赢的幅度是弱指标，赢的对象才是强指标。
   赢过公认最弱的方法只算合格，不构成卖点。
5. **目标不可达就当场给算术。** 遇到物理或数学上不可能的目标，
   把那个下界算出来，不假装努力一下能到。
6. **两种口径一致是呈现问题，不一致才是数据问题。** 不一致时两种都报，不得择优。

**读数与归因**

7. **单次或单种子读数不可信**，尤其当效应量与噪声同量级。写进结论前必须补种子或独立复核。
8. **抽样通过不能替代全量验证。** "理论保证不劣"不等于"实测能复现"。
9. **比效应之前先扣掉预算差。** 任何"A 比 B 好"，第一个要排除的替代解释是 A 花了更多算力。
10. **数字必须带时代标签。** 引用任何锚点要带上它成立的代码版本与参数配置，跨时代的数不比。

**故障排查**

11. **多数停机是检验装置自己的缺陷，不是科学问题。** 已确诊同类事故至少四次。
    看到停机先假设自己的检验链坏了。
12. **排查顺序固定**：先查有没有外部信号打断，再查程序本身是不是压根不产生阶段性输出，
    最后才轮到资源耗尽。
13. **默认不开火。** 昂贵动作的默认状态是先输出它将要做什么（计划、影响面、预计耗时），
    开火需要显式指令。

**范围与风险**

14. **范围膨胀是最贵的失败模式。** 项目曾因此停滞两个月。
    任何审计或复核的产出必须同时给出"本轮之后不再新开合同、门禁或审计器"的边界。
15. **授权边界是硬的。** 被授权做 A，途中发现 B 也有问题，
    正确动作是写进报告并登记等用户裁决，**不顺手扩大战线**。
16. **对抗性评审必须前置于实现**，事后评审只能确认损失。
    **先冻结、再在隔离副本上试水、见好才大改。**

**实验设计（这两条现在正好是活的，务必看）**

17. **每条线按自己的标准过门，不许互相救场。**
    参数、算法、机制三条线各自独立验收——
    **算法不给力不得靠模型创新遮掩，机制没效应不得靠算法表现补位。**
    ⚠️ 这条现在就在生效：P19 已把算法贡献定为"面向本问题的特化求解组件"，
    而"这是不是只是包装"正是当前的公开担忧。**不许用模型或机制那边的成绩来替算法过门。**
18. **承担创新主张的东西血统必须干净。**
    原文（§15）：主算法不得依赖第三方开源实现且禁止"半独立"；
    基线允许直接使用开源或复刻文献实现。
    ⚠️ **`UNDETERMINED`**：这条"不得依赖第三方开源实现"**没有操作性定义**
    （C4 审计已判 `UNDETERMINED`），而 P19 已定"搜索内核直接采用成熟 HGS"。
    **两者存在张力，须由用户在正文措辞时给出口径，代理不得代拟、不得自行认定已经满足。**
    见 `docs/handoff/t0_delta_20260806_evening_before_codex_takeover.md`。

**另有四条与呈现有关，写正文和图表时必读原文 §19**：
均值会吃掉张力（不能只展示均值）、形容词必须有可核验定义、
有生成器就改生成器不手改产物、展品不许美化（不得手工修饰路线图，不得另跑更好看的种子）。

**还有三条协作规矩，见原文 §20**：
第二意见（外部模型/顾问）没有事实裁决权，必须回到代码和封存数据核对；
要求冲突时的完整优先级是 用户最新指示 > 有效合同与保护门 > 期刊模板 > 母版论文 > 其他；
出现预期之外的事实要登记进 `docs/handoff/serendipity_ledger.md`。

---

## 十二、规矩和事实分别去哪查

规矩按任务加载，事实按当前入口读取；不再要求每次把全部历史材料通读一遍。

| 位置 | 有什么 | 什么时候必须读 |
|---|---|---|
| `/Users/zhouleixishu/.codex/AGENTS.md` | 全局 AI 通用工作规范 v3，效力最高 | 支持自动加载的代理每次任务生效；人工环境需冷启动读取 |
| 本文件 `AGENTS.md` | 本项目的行为规矩与硬约束 | 每次 ReSETP 任务生效 |
| `docs/handoff/CURRENT_PROJECT_CONTEXT.md` 与 `pending_decisions.md` | 当前项目事实和精确用户决定 | **非平凡项目任务的两份事实入口** |
| `/Users/zhouleixishu/Desktop/AI规矩原话全集_20260806.md` | **用户原话证据册，65 条，十一编。** 判断"用户到底说没说过某句话"的唯一权威。含第八编的历史冲突原话与第十一编的四条待裁决冲突 | **凡涉及"用户是不是这么要求的"、"这条规矩还算不算数"** |
| `docs/handoff/memory/user_operating_principles.md` | 长期工作方法正文，§1–§13 规定该做什么，§14–§20 规定怎么判断。本文件第十一节只是它的速查 | **做实验设计、读数归因、故障排查、写正文之前** |
| `/Users/zhouleixishu/.codex/skills/global-ai-*/SKILL.md`、`global-academic-*/SKILL.md` 和 `paper-formula-symbol-audit/SKILL.md` | 十六份跨项目通用技能：六份负责沟通、决策、证据、执行、项目和代理协作；九份负责研究定题、文献、模型、数据仿真、根因、实验、算法、复现和论文投稿；一份负责复杂公式与符号审计 | **任务确实需要时才读，不为形式全部加载** |
| `HANDOFF.md`、`docs/handoff/memory/` 和旧计划／旧报告 | 历史时间线、主题证据和原始来路 | 只按当前事实源的证据地图定点展开 |

⚠️ **本文件不是规矩的全集，也不许被当成全集。**
本文件与上表任何一处冲突时，按 `/Users/zhouleixishu/.codex/AGENTS.md` 第 0 条的效力顺序判断。
在本文件里查不到某条规矩，**不等于那条规矩不存在**；按任务到上表的规矩源查。
在当前事实入口里查不到历史细节，也不等于历史被删除；按证据地图定点追溯即可。
