# ReSETP — 项目总交接文档（HANDOFF）

> **单一事实源（Single Source of Truth）。**
> 任何机器上的 Claude 或 Codex，**开工前必须完整读完本文件 + `docs/handoff/memory/` 记忆快照**，否则禁止动手/下结论。
> 每次重大决策 / 任务 / 对话后，把变化追加到文末「变更日志」，让本文件始终最新。跨机器迁移、防偏差全靠它。
> 维护：Claude 更新本文件与 `docs/handoff/memory/`；Codex 每次跑完在「变更日志」追加一条 run log。

## 0. 一分钟速览
ReSETP = 投《系统工程理论与实践》的绿色车辆路径论文。多车场、油车(CV)+电车(EV)混合车队、EV 非线性充电（时变碳强度）、时间窗、收益公平、动态需求滚动重规划。

### 当前主线与挂账（2026-07-11）

**当前最高优先级覆盖（2026-07-11，用户已拍板）：彻底完成E2，再立即准备E3。** E2不做单独“完整模型复核层”，直接形成九个L-main v3自有算例 × 九算法 × 每组十次 × 4000评价的统一比赛，共810行。参赛算法为本文staged hybrid、GA、PSO、VNS、ACO、GA-VNS、LNS、GWO、IWD；旧ALNS-Wouda不参赛，立即充电版转E3消融。冻结225行合格主表数据复用，只补585行；正式长跑前必须依次通过27项200评价接线门和18项4000评价代表门。280 kWh为E2正面主场景，80 kWh备用；E2关闭公平、跨场费保持0。当前自有算例不套BKS/AVG/Gap%标准表；公开标准算例+BKS/AVG/Gap%+未经改动Goeke原始算法挂为E2后补实验。唯一投稿口径见`docs/handoff/e1_e7_submission_contract_decision_20260711.md`，唯一E2执行总表见`docs/handoff/e2_legacy_items_1_6_9_execution_plan_20260711.md`。

**后续建模挂账，不得阻塞E2：** 三项文献事实均已核对，最终场景仍等E2后由用户拍板。客户方面，普通多车场论文可把最近车场用于初始解，但合作配送的“原始客户”通常是算例预先给定的公司/车场成员关系；L-main没有客户owner字段，当前1425条最近车场表只能叫“可复现的合成独立经营基线”。跨场费用方面，定向文献未发现“95英镑/跨场客户”行业统一价；常见模型要么只算全局行驶成本并另做收益补偿，要么按真实的车场间往返/集中运输时间和距离计费。当前`95×跨场客户数`只能叫未校准代理，0/10/25/50/95也不能直接拍成正式网格。碳配额方面，目标期刊陈婉茹等（2023）和Liu等（2020）都采用`碳价×(排放-配额)`并明确说明配额不改变路径，故本文现有公式视为正确会计建模。公平不在所有实验强开，只在E6及明确研究公平交互的实验中从搜索阶段启用。E3--E7各自证明机制，旧看板的E1/E3“已完成”不得未经复核直接当论文定稿证据。

**E2长跑同窗的只读工作边界：** 不启动其他实验，但允许同时查客户归属、跨场费和线性碳配额三项文献，E2收口时交用户拍板；统计明确为主算法对八基线的90组配对Wilcoxon符号秩检验，并报告原始p值和Holm校正p值。Goeke公开标准算例+BKS/AVG/Gap%只在2026-07-24前做可行性评估，排不进投稿周期就明确降级。代表门18行若通过将直接复用，正式缺口由585降为567。

**E2十次代表门已通过（2026-07-11）：** 50c/150c × 九算法 × seed6 的18项均跑满4000评价、零违规、保存解/哈希齐全，并经当前检查器18/18复算。门禁初判暂停的唯一原因是两项IWD没有超过共同起点；源码和算子台账确认两项都真实执行了各自4000次IWD操作（3810次构造、190次局部搜索），所以修正门禁为“找到更优解或留下非空算法专属操作台账”二选一。修正后18/18为真实活动，16项取得自身更优解、2项为真实但未赢的负结果。该修复不改任何求解算法、预算、成本、物理约束或已保存解；仅纠正验收逻辑，定向测试5项通过。代表门可复用，正式缺口仍为567项。

**项目调度裁决（2026-07-11，用户纠偏）：研究计划不并行推进，实验任务并行运行。** 项目仍按 E2 判决 → 算法/场景冻结 → 其余正式实验 → 表图论文的依赖顺序推进；不得同时抢跑未满足前置条件的研究线。某一批实验获准启动后，默认按实例/seed/算法或参数臂拆分，以受控 worker 池尽量利用 CPU。当前 30 次旧 runner 串行写同一 CSV，不中途插入并发写者；下一批必须使用安全并行 runner。7.31 收稿止损路线见 `docs/handoff/project_parallel_execution_plan_20260711.md`。

**总实验管理入口（白话版）：** `docs/handoff/project_experiment_master_plan_20260711.md` 是当前整体实验施工总图，包含论文贡献映射、阶段门、E0--E7逐项方案、统计/复算规则、CPU运行规则、7.31两周冲刺和Not-To-Do；`docs/handoff/project_board_20260711.md` 是实时看板；新实验先复制 `docs/handoff/templates/EXPERIMENT_CARD_TEMPLATE.md` 写任务卡，再启动。

**当前主线：E2全量已通过，转入其余正式实验。** 2026-07-11，280 kWh正面主场景的L-main v3九档 × seeds1--5 × 6证据算法 × 4000评价已完成：270/270 `OK`、全部跑满、全部零违规、保存解和hash齐全。正式方案 `staged ALNS-LNS hybrid + carbon-aware charging schedule` 的完整benchmark总成本为173139.284391，第二名LNS为185862.880926，总量领先6.845690%；45组同实例同种子配对为25胜5平15负，配对平均/中位优势2.829832%/0.842029%，bootstrap均值95%区间0.982%--4.894%，综合平均名次第一，平均运行时间低于全部基线。判决=`E2_FULL_BENCHMARK_LEAD_SUPPORTED`。允许写“完整benchmark总量领先第二名6.846%”，禁止写“每个实例稳定领先5%”。完整收口见 `docs/handoff/e2_full_closeout_20260711.md`。

**算法创新边界（2026-07-11，写作硬门）：** 当前方案是有针对性的组合式创新，不是已证明的全新ALNS。可写“分阶段ALNS--LNS混合框架 + 固定路线碳感知充电重调度”；不可写“独创ALNS”或“全联合碳感知搜索”。E2成绩只证明性能；E2后须补最近文献对照、四臂消融、来源/复杂度/边界说明，证据不足就保持“有效混合求解框架”口径。唯一详细规则见`docs/handoff/e1_e7_submission_contract_decision_20260711.md`。

**E2冻结与E1突破（2026-07-11）：** E2已用annotated tag `e2-submission-20260711`固定在commit `0124623e`，冻结锚见`baselines/e2_alns/e2_submission_20260711/E2_FREEZE.json`，后续代码变化不得改写该证据。E1在commit `c20dae18`上完成280 kWh最小正式结构门：复用50/100/150/200c共20条mixed冻结解，新增200c五种子CV-only搜索；25条可行证据均零违规、满4000。200c mixed平均EV客户/需求/距离份额为86.1%/87.2%/82.2%，平均59.8次充电；相对CV-only 4胜1负、平均便宜0.671%。EV-only五次均因直接转换无可行充电窗口而`NOT_FOUND`，这不是全局不可行证明。判决=`E1_280_STRUCTURE_SUPPORTED`，下一主线为E3短门。详见`docs/handoff/e1_280_structure_closeout_20260711.md`。

**E3正式消融收口（2026-07-11）：** 200c × M0--M5 × seeds1--5共30行全部零违规、满4000评价、成本分项闭合，运行commit=`b9a59c46`。最终诚实判决=`E3_PARTIAL_MECHANISM_SUPPORT`：M3同路线同电量的低碳充电5/5降低EV间接排放，平均20.96%，证据强；M1相对M0成本4胜1平、平均低1.04%，但跨场服务只在1/5种子出现2个客户，协同直接证据弱；M5 theta=1五种子均公平可行，但对应M4无公平解原本也5/5满足收益比>=1，公平绑定为0/5，必须由E6另证。详见`docs/handoff/e3_cumulative_ablation_closeout_20260711.md`。下一主线为E4/E5碳价短门；配额不再重复搜索。

**用户重新打开E2遗留编号1--6、9（2026-07-11）：** 原E2冻结证据继续原样保留，但用户要求对15个负例、逐组领先、80 kWh、完整碳感知搜索、动态低碳、电池容量和最终16000评价逐项做一次有界尝试。当前优先级因此改为先完成负例恢复门，再按依赖顺序处理其余项目；E4/E6/E7旧排期由新计划覆盖。已完成只读负例审计：15负平均落后2.270%，3组落后超过5%，11组路线数更多，9组最佳答案停在中间强搜索阶段。首轮重启短门27/27满1600评价、零违规，但事后发现它误用`introduce_ev=False`纯CV起点且LNS关闭正式`common_flip_preprocess`，与正式E2共同起点合同不一致；原始结果保留，判为`RESTART_SHORT_GATE_INVALID_START_CONTRACT`，不得据此淘汰候选。runner已改为正式`make_shared_initial_solution`和LNS共同预处理，只允许原范围27次纠正重跑。完整顺序、晋级与止损见`docs/handoff/e2_legacy_items_1_6_9_execution_plan_20260711.md`，证据目录`baselines/e2_alns/e2_loss_recovery_20260711/`。

**E2负例恢复重启候选判退（2026-07-11）：** 正式起点纠正门27/27满1600评价、零违规、保存解/算子记录/代码出处齐全。重启相对旧staged在全部9组平均改善0.410%、6个开发负例平均改善0.236%、防退化最差0%，但没有任何同预算负例转为不输LNS，且150c seed3退化9.737%，所以按预注册规则`RESTART_SHORT_GATE_REJECTED`。根因证据：150c旧连续800强搜索从10948.58降到10561.57，拆成两个400盆地后两段均停在10948.58，说明过早重启截断了大实例跨越停滞所需的连续搜索。该候选不进未见种子/4000。下一有界候选只补“hybrid中段仅借LNS后端、未执行完整LNS策略”的名实缺口：在同一总预算内测试真实LNS中段，不调重启间隔。

**E2负例恢复 true-LNS-middle 首门判退但机制保留（2026-07-11）：** `true_lns_middle_gate` 27/27满1600评价、零违规，验证器复核行数、预算、解文件、hash和运行提交`5be12db7`均通过。候选相对旧staged全部9组平均改善1.536%、6个开发负例平均改善2.506%，把100c seed2从落后LNS 1.144%转为领先3.826%；50c seed5和150c seed3分别缩小差距7.240和2.891个百分点。但只转回1个负例，未达到预注册的2个，且150c seed4防退化样本相对staged下降1.209%，故判`TRUE_LNS_MIDDLE_SHORT_GATE_REJECTED`，不得直接进未见种子或全量。新增机制发现：1600短门仍沿用固定`400+800+400`，真正LNS中段只占50%；正式4000算法为`400+3200+400`，中段占80%。下一候选不是放宽门槛，而是把短门改为与正式算法一致的10%/80%/10%预算比例（160+1280+160，总预算不变）；若仍不能至少转回2个负例，该路线彻底淘汰。

**E2负例恢复比例 true-LNS 判退（2026-07-11）：** `proportional_true_lns_middle_gate` 27/27满1600评价、零违规，保存解、算子记录与运行提交`7b164f6a`均通过独立验收。但候选相对staged全部9组平均退化0.329%，开发负例仅改善0.095%，仍只转回1组，最差保护样本退化3.005%，判`PROPORTIONAL_TRUE_LNS_MIDDLE_SHORT_GATE_REJECTED`。它在100c seed2大幅改善11.712%，但同时把50c seed5和75c seed1分别拖低7.773%和4.737%，证明继续调10%/15%/20%等比例没有稳定依据。true-LNS比例路线正式关闭，不启动已准备的57次未见种子4000门。下一步只允许在现有保存解上做路线签名互补性审计；只有不同运行确实提供可组合成更好可行解的整路块，才允许做最小HGS/SREX式headroom候选。否则停止E2恢复，保留冻结25胜5平15负的诚实结论并进入80 kWh任务。

**E2 15负恢复最终收口（2026-07-11）：** 整路块互补性审计只读5种现有父解，新搜索0次；9组45个父解全部零违规。6/6开发负例都能拼出可行混合整路解，但0/6比现有最好父解便宜至少0.25%，0/6多救回负例，verdict=`ROUTE_BLOCK_HEADROOM_NOT_SUPPORTED`。说明当前解库的整路块有多样性，但没有更优的可组合积木；不接HGS/SREX大工程、不启动57次未见种子和45次论文重跑。项目1/2以冻结`25胜5平15负`、总成本领先LNS 6.845690%收口；下一主线正式转入项目3的80 kWh稳健性镜像门。完整解释见`docs/handoff/e2_loss_recovery_closeout_20260711.md`。

**E2 15负开源/论文最终复核（2026-07-11）：** 已核对HGS-CVRP、PyVRP/SREX、混合车队HGA、多车场绿色VNS和本地Zotero相关论文。更强的种群+SWAP*+route elimination理论上可能产生当前解库没有的新路线，但移植到ReSETP需要新的充电物理修复、多车场/多趟适配和严格预算计数，属于新求解器工程。仓库现有`SWAP*-lite`不是完整SWAP*、不直接消除路线，且内部完整解评分未计入`EvalBudget`，不能公平直开。因此判决不变：不为15负重开E2；这不是宣称“绝对无法改善”，而是当前没有短周期、同预算、可写论文的救援方案。

**统一备案与文档口径（2026-07-11）：** “种群搜索+真正SWAP*+路线消除+SREX+充电修复”不删除，统一挂为后续明确遇到路线生成瓶颈时的长期备案，当前不施工。遗留编号1--6、9的唯一调度总表为`docs/handoff/e2_legacy_items_1_6_9_execution_plan_20260711.md`；其他文档只作证据附件和状态摘要，不再分散维护执行顺序。

**80 kWh项目3接线门通过并已启动正式门（2026-07-11）：** 不复用旧纯CV起点runner，也不走仍指向旧ALNS的通用正式入口。新专用入口从tag `e2-submission-20260711`/commit `0124623e`的独立worktree调用冻结算法，读取已审计L-main v3，使用共同起点和LNS `common_flip_preprocess=True`。50c×seed1×{pair,LNS}的200评价短门2/2搜索、3/3证据行全部满预算、零违规，并经独立复算判`E2_80K_PREFLIGHT_VERIFIED`。runner/验收器已提交并推送，正式15/50/100/200c×seeds1--3×{pair,LNS}=24搜索任务已以2 workers启动，将展开36证据行；本地watchdog每30分钟检查一次，正常时保持静默。

**80 kWh验收硬门加固（2026-07-11）：** 短门已额外复核`LMAIN_V3_READY`、manifest与算例逐文件hash，并证明冻结/当前代码的价格、成本、检查、评价预算、解结构、加载、修复和共同起点8个关键文件哈希相同；默认值为80 kWh/3650 kg/25 m/s，Python 3.13.9、NumPy 2.3.5。aware/naive合同也已进入硬验收：必须同路线结构、同总用电和同充电量，否则直接HALT。相关回归45项全通过。

**80 kWh正式门止损与根因（2026-07-11）：** 首轮跑到20/24任务后，200c的hybrid三个seed全部被独立checker抓到时间窗违规，已立即中止，没有把失败行继续包装成正式结果。根因是固定路线充电择时只用“下一个客户”的时间窗计算最晚充电时刻，未保护整条后续路线。已改为从路线末端反向传播所有后续due time，三个失败保存解零新搜索回放均从违规变为零违规。修复冻结于commit `40bd2883`和tag `e2-80k-charging-window-fix-20260711`；200c seed1的pair/LNS各200评价短门2/2满预算、零违规，独立验收=`E2_80K_PREFLIGHT_VERIFIED`。新正式轮仍为2 workers，只把大任务优先排队以减少尾部单核空转，不强拉并发。

**80 kWh项目3正式收口（2026-07-11）：** 修复后正式门24/24搜索、36/36证据行全部完成，满4000评价、零违规，解/成本/签名/算例hash/保护代码全部独立核验通过。12组hybrid/LNS配对9胜0平3负，平均优势4.677%，中位1.355%，bootstrap 95%区间[0.593%,9.328%]，汇总成本优势10.370%。但15c和50c规模均值分别为-0.816%/-0.767%，仅2/4规模均值不输，未达预注册3/4硬门；正式判`E2_80K_MECHANISM_VISIBLE_ALGORITHM_NOT_ROBUST`，不扩80 kWh九档。50/100/200c均有多数种子EV真实作业、>=2次充电且正充电量，机制可见门通过。详见`docs/handoff/e2_80k_robustness_closeout_20260711.md`。下一主线按统一计划进入项目4精细碳感知搜索。

**旧30次稳定性门（历史前置证据）：** 100c/150c/200c × 5 seeds × staged ALNS-LNS hybrid/LNS 的30次、4000次评价稳定性门为30/30 OK、零违规、全部跑满预算；三个规模hybrid相对LNS平均改善6.45%/0.69%/14.08%，合计11/15胜，verdict=`STAGED_HYBRID_STABILITY_SUPPORTED`。该门已被上述九档全量证据覆盖。路线搜索身份从此冻结，不再 rescue 调参。

**E2碳机制小门（2026-07-11）：** 复用上述15个hybrid保存解，在路线、车型和总充电量不变条件下，对比立即充电与低碳择时充电。15/15均成功、双方零违规，625次充电实际移动，15/15充电间接排放下降，平均下降约9.87%。因此正式候选命名为 `staged ALNS-LNS hybrid + carbon-aware charging schedule`，消融为 `+ immediate charging ablation`；这是固定路线充电子问题，不是恢复历史 `CARBON_OPS_WEAK` 粗糙算子。

**场景裁决（2026-07-11，用户授权按论文目的/统一性选择）：** 投稿版正面机制主场景采用280 kWh任务内覆盖，理由是有现代中型配送车来源、当前新算法真实形成多EV/多充电并支持择时降碳；后续E1--E7主结果必须沿用同一场景合同。80 kWh不删除，保留为Goeke原始基准和稳健性对照；`prices.py`默认值不改，两种场景不得混表排名。历史中间电池档只作经验库，除非短门暴露280退化，否则不重开电池扫描。

**GO_FULL_E2（历史启动门，现已完成）：** 18项400评价接线门、100c四基线4000评价健康门、225任务manifest和resume门全部通过后启动全量；最终225个真实搜索任务展开270行证据并完成。旧验收器将两个小算例上LNS/VNS正常收敛到相同成本摘要误报为串线；记录级重审确认各自算法更新轨迹独立，其中一组路线结构不同。验收规则现统一为算法自身更新活性，输出碰撞只记信息；原始270行未改、未重跑。

以下两项已登记为后续任务。固定路线充电择时的最小机制证据已因最终E2算法身份要求提前闭合；大规模电池筛选与动态需求低碳交互仍不启动：

- 电池容量、车型结构与充电活跃度复核：以后从已保存解补算 EV/CV 任务份额、充电次数/电量和可移动充电空间；只有证据不足才做有界容量筛选。
- 动态需求与低碳充电机制验证：以后先固定路线和总充电量验证充电时间能否移入低碳时段，再验证动态需求是否创造该空间。

在 E2 稳定性结论、完整复算和算法身份冻结前，禁止启动电池筛选、谷时充电算子、动态低碳实验、正式 E7 或修改论文默认 80 kWh。详见 `docs/handoff/m1_e2_deferred_tasks_20260711.md`。

- [M1] 2026-07-10（L-main v3 已重建、审计并激活）：正式 E1--E7 为九阶三班倒，`10..200` 是每个 Goeke 子来源的规模标签，实际合并客户数为 `22/34/45/55/114/163/221/322/449`。三个完整来源按 `0h/9h/18h` 合并，只删超过 24h 的第三班尾部；保留 `D0`、生成 `D1`，三班共享多车场设施布局。激活审计 `LMAIN_V3_READY`、九实例 shared initial solution 零违约，formal runner 现在强制核对活动 manifest 与审计 hash；旧未审计目录已归档。设计与激活记录：`docs/handoff/e2_lmain_reconstruction_design_20260710.md`。
贡献 = 模型 + 机制（碳两层/协同/公平/动态）；算法也必须有创新，且要**打过文献主流算法**（GA-VNS/GA/PSO/ACO/VNS/LNS/GWO/IWD），不只赢 SA（SA 是地板，赢它只算"合格"）。
主线算法 = ALNS(winner kernel) + DR-ALNS（强化学习指挥 ALNS）。

## 1. ⚠️ 机器与环境（最重要，先读，踩了全盘皆输）
- **基准机 = M1 + 8G RAM（macOS, ARM）= 当前机 = 所有论文正式数字的唯一来源。**
- **GPU 机 = 5800H + RTX3060 6G + 16G RAM（操作系统待确认；很可能 Windows/Linux，不是 mac）。仅用于 DR 课程训练。**
- 金标准环境（M1）：`/opt/anaconda3/bin/python3.13` + numpy 2.3.5。锚：100-01 winner £4878.33 / 公平SA £5346.99（winner 碾 8.76pp）；L-main winner £8351 ≈ SA £8319（近最优、持平）。
- **致命坑①（ARM vs x86）**：M1(ARM) 与 5800H(x86) 浮点/BLAS 不同 → £4878 等锚在新机几乎必然变样（已被 venv numpy 漂移坑过一次：£4878→£5696、反输 SA）。**铁律：绝不把 M1 与 x86 的绝对数字放进同一张表/同一篇结论。**
- **致命坑②（可能 macOS→Win/Linux）**：若 5800H 非 mac，`/Volumes`、`/opt`、shell、`SETP_WORKER_PYTHON`、行尾、外置盘文件系统（APFS 跨平台读不了）全要变。**所有硬编码路径 `/Volumes/移动硬盘（512G）/ReSETP` 都会断**，需参数化为"仓库根"。
- **迁移分工（Claude 建议，待 user 最终确认）**：M1 = 唯一基准（E1-E7、所有表图、winner/SA/8基线对比都留 M1 跑）；5800H+3060 = 只做 DR 课程训练，DR 结果以**相对提升 %** 报（DR vs AlphaUCB vs SA 全在 5800H 同环境测，环境抵消，不与 M1 £4878 比绝对值）；训完迁回 M1。
- 新机要重建两套环境：① 系统 Python 3.13 + numpy 2.3.5 跑 solver（注意 x86≠ARM 数值）；② torch/CUDA venv 跑 PPO（3060）。`SETP_WORKER_PYTHON` 路径会变。**任何环境用前先验"同种子同结果"的内部确定性。**
- **同步：当前无 git remote，靠物理搬外置盘。强烈建议建私有 GitHub remote**（否则两机易分叉、盘坏即全失，且搬盘会中断 M1 上正在跑的 E1-E7）。
- 清理：仓库有 macOS AppleDouble 垃圾（`._*`，含 `.git/objects/pack/._*.idx`，git 报 non-monotonic index 警告）。迁到非 mac 机前 `dot_clean .` 或删 `._*`。

## 2. 三条工作线当前状态（2026-06-20）
- **图表（13 张）**：壳目录 + 数据适配完成；第四阶段"照壳填数据画图"等大重跑数字。详见 `docs/handoff/memory/figure-redesign-task.md`。
- **大重跑 E1-E7 + 表图重生成**：✅ **完成（2026-06-20, commit `9da7f8b`）= 论文正式数字锁死**。471/471 零失败零违约；**£4878 锚精确复现**（ALNS 4878.3318 / SA 5346.99）；T3-T9 + F1-F6/F5b 重生成到 `docs/paper_submission_final`，**F4 修好非空**；碳段 →**2330.7 kgCO2e**；latexmk 过；solver166/RL102 过；保护文件未动。报告 `solver/reports/formal_winner_20260619/`。小尾巴：E2 有 1 条 stale DR-ALNS provenance 行（T3 已排除，无害）。注：这是"正式数字 + 现有风格表图"完成；图表"顶刊级重设计"是否并入待确认（见 `docs/handoff/memory/figure-redesign-task.md`）。
- **ALNS winner kernel**：100-01 碾 SA 8.8%（£4878 vs £5347）健康；L-main 近最优持平。代码 `solver/src/setp_solver/search/winner_operators.py`。
- **DR-ALNS（block lane）**：离线探针 = **PROMISING**（有可学上下文信号，r2_delta≈1.05, CI[0.957,1.157]）。根因是训练不稳、非无信号。下一步 = 上 3060 做课程训练；方案在 `solver/reports/dr_alns_ppo_v3_block_dr_alns/offline_bandit/probe/curriculum_training_plan.md`。代码 `solver/rl/dr_alns_ppo/`（block_env/async_block_policy/train_async_block_ppo/offline_probe）。
- **8 文献基线（轨②）**：已实现接入统一接口、零违约跑通，但 **HALT_BASELINE_THROUGHPUT**（eval/s 3.96–11.04 < 16000/900s 需的 17.78）。下一步 = 先 profile 提速（每个计数 eval ≈ 一次 evaluate，full check 延后/缓存 decode），仍不够再换等墙钟公平。**基线对比必须在基准机 M1 跑**（与 £4878 锚同环境）。代码 `solver/src/setp_solver/search/metaheuristic_baselines.py` + `metaheuristic_baseline_runner.py`；产物 `baselines/`。

## 3. 纪律铁律
- 禁改 `cost.py` / `check.py` / `evaluation.py` 语义（唯一真值源）。
- 公平对比：同 evaluate/check + 同预算（16000 eval 或等墙钟，见 §6 待决）+ 10 seed + scipy Wilcoxon + 零违约 + 暖启动 `make_shared_initial_solution`；每个基线目标计算走同一 `evaluate()/penalized_obj`、过 `EvalBudget` 计数、封顶预算。
- Codex 不自创算法，只复刻文献/适配现成；拿不准停下报告。
- 核心代码立即 commit；数字绑 commit hash；跑不出诚实 HALT 不注水。
- 角色：Claude 只思考/判断/写 Codex 提示词/写文档，不跑不改代码、不开 workflow；Codex 执行。关键核验才让 Claude 亲自读仓库。
- 与 user 对话用平实中文；给 Codex 的提示词要详尽自包含（它冷启动、读不了论文，要把目标/背景/实现/验收/边界/捷径全喂）。

## 4. 关键代码地图 + 公平对比接口
- `solver/src/setp_solver/cost.py` → `evaluate()`（真实目标，禁改）
- `solver/src/setp_solver/check.py` → `check_solution()`（可行性，禁改）
- `solver/src/setp_solver/search/evaluation.py` → `EvalBudget`、`penalized_obj/score_candidate/model_cost`
- `solver/src/setp_solver/solution.py` → `Solution{routes[Route(vehicle_id,vehicle_type,home_depot_id,node_sequence)], charging_actions[...], cross_site_services[...]}`
- `solver/src/setp_solver/search/candidates.py` → `run_candidate(label,bundle_dir,seed,eval_budget,max_runtime_seconds,initial_solution)`；`make_shared_initial_solution(bundle)`；现成构造/修复/插入/充电算子（基线解码复用它，别重造可行性）
- `solver/src/setp_solver/search/alns_crush_v2.py` → 公平 harness：`FAIR_SA_EVAL_BUDGET=16000`/`900s`；run_task1=公平SA→`fair_sa_reference_costs.json`；run_task3=winner vs SA + `_wilcoxon_vs_fair_sa` + 判级（碾压/小幅领先/持平/失败）
- `solver/src/setp_solver/search/alns_crush.py` → `INSTANCE_DIRS`：主算例入口已收敛为 `models/data_bundle/generated_instances/L-main/` 下 23 个 `L-main-*` 实例（vanilla/multidepot 各 10/15/20/25/50/75/100/150/200c，threeshift 50/75/100/150/200c）；旧三档 `01/150/200` 已保留到 `models/data_bundle/generated_instances/L-main_legacy_pre_23/`，不再接入。
- `solver/src/setp_solver/search/winner_operators.py` → `run_winner_kernel(bundle_dir, WinnerKernelConfig(...))`
- `solver/rl/dr_alns_ppo/` → DR block lane（block_env 19维obs+5维动作；worker 用 SETP_WORKER_PYTHON=系统Python 跑算子）
> 深细节（8 基线设计转录、DR 同类偷的设计、harness 字段）见 `docs/handoff/memory/baseline-algorithm-catalog.md`。

## 5. 决策链 / 来龙去脉
- 真目标纠正：不是"碾 SA"（那只合格），是 **DR-ALNS 真训练+创新、干过 GA-VNS/GA/PSO 等文献主流**；退路 = 创新的普通 ALNS。
- DR "拉垮" 根因 = **训练不稳（非无信号）**；离线探针已证 PROMISING；**课程学习 + 动作掩码 + shared baseline 是修复捷径**（偷自 DR 同类论文 Daysalilar/Narayanan/Wan）。
- 两条便宜活并行：①探针优先（已 PROMISING）→ ②基线对比（已 HALT 在 throughput）。
- 公平口径：先提速保 16000/900s，不行再等墙钟；注意 16000 eval 对种群法（GA/PSO/GWO/IWD）可能偏少、反而低估基线。
- 迁移：M1 canonical，3060 只训 DR（相对 %）。

## 6. 待决 / 下一步
- **迁移三问（待 user）**：① 5800H 什么系统？外置盘什么文件系统（新机读得了吗）？② 仓库怎么过去——搬盘 / copy 一份 / 建 GitHub 私有 remote？③ 认 "M1 canonical + 3060 只训 DR（相对%）+ E1-E7/基线都留 M1" 这个分工吗？
- [M1] 2026-07-09（算例主线改造）：L-main 系列已扩成 `models/data_bundle/generated_instances/L-main/` 下 23 个主算例（vanilla/multidepot 各 9 个规模，threeshift 5 个规模），并把主算例入口收敛到这 23 个 `L-main-*` 实例；旧 `01/150/200` 移至 `L-main_legacy_pre_23/` 保留但不接入。同时记录代办：**ALNS尚未完全剥离和独立，计入代办任务**。
- **轨②**：写第三条 Codex 提示词（profile → 提速 → 按选定公平口径出正式对比表）。
- **轨①**：迁移就绪后上 3060 课程训练（curriculum_training_plan.md 已 ready）。
- **顺手**：根目录 `AGENTS.md` / `CLAUDE-FABLE-5.md` 是 Claude 模型系统提示词（不是 ReSETP 项目说明）。Codex 按惯例读 AGENTS.md 会读到无关内容——考虑给 Codex 建一份真正的项目 AGENTS（指向本 HANDOFF），或在每条提示词显式写"先读 HANDOFF.md"。

## 7. 记忆快照
`docs/handoff/memory/` = M1 上 Claude 私有记忆（`~/.claude/.../memory/`）的快照，随仓库走。含 `MEMORY.md`（索引）+ project-plan-overview / baseline-algorithm-catalog / alns-crush-root-cause / algorithm-pivot-ca-alns / figure-redesign-task / deferred-instance-robustness / feedback_communication_style。深细节看这里。
已写好的两条 Codex 提示词（①离线探针 ②8基线复刻）应存到 `docs/handoff/codex_prompts/`（待补）。

## ⚠️ 工作顺序（单盘物理迁移 = 串行，必读）
仓库在外置盘上，盘在哪台机、另一台就停（含 M1 上正在跑的 E1-E7）。GitHub 备份只有 46M 代码+文档，**没有大数据/instances/reports**，所以 Windows 机不能靠 GitHub 独立干完整实验。因此严格串行：
1. **先在 M1 跑完**：E1-E7 重跑（进行中）+ 轨② 基线提速与正式对比（canonical 数字）。
2. **再移盘到 Windows**：装环境 → DR 课程训练（相对 % 报，不与 M1 绝对值比）。
3. **迁回 M1**：整合 DR 结果。

## 变更日志（每次对话/决策/Codex run 追加一行）
- 2026-06-20 建本文件；记忆快照入 `docs/handoff/memory/`。探针 PROMISING（r2_delta≈1.05）；8 基线 HALT_BASELINE_THROUGHPUT。提出迁移策略（M1 canonical + 3060 训 DR）。
- 2026-06-20（续）user 确认：GPU 机 = Windows 5800H，外置盘 exFAT 直插 Windows 干活，训完迁回，授权上 GitHub。已建私有备份 `github.com/zlxshu/ReSETP`（46M 快照 = 代码+文档+记忆；大数据/reports/venv 不在内，留盘上）。清理 .git 内 AppleDouble 垃圾。确立"单盘=串行"工作顺序（见上）。handoff 文档已提交进盘上仓库（commit 38e6356）。
- 2026-06-20（续2）写了轨②提速提示词，三条 Codex 提示词存入 `docs/handoff/codex_prompts/`（01 探针 / 02 基线 / 03 提速，+ README）。
- 2026-06-20 E1-E7 进度快照（user 报）：~51/60 单元、414/470 记录、0 失败；E1 完 / E2 ~130-140 / E3 52-60 / E4 135-160 / E6 58-70 / E7 9-10；剩 E4 网格 + E6 theta + E3 长尾；6 worker。ETA 纯算 2-4h + 收尾（合并 manifest / export-backfill / 修正文碳段 / 查表图 F4 / solver+RL 测试 + commit）1-2h = 整体 3-6h。不降规格、不混旧数据。**下一步：在 M1 跑完 E1-E7 + 发提示词③做轨②正式对比，再移盘 Windows 训 DR。**
- 🎯 2026-06-20 **E1-E7 大重跑完成（commit `9da7f8b`）= 论文正式数字锁死**：471/471 零失败零违约；£4878 锚精确复现；T3-T9 + F1-F6/F5b 重生成（F4 修好）；碳段 →2330.7 kgCO2e；latexmk 过；solver166/RL102 过；保护文件未动。**M1 现已空闲 → 下一步发提示词③做轨②基线正式对比，再移盘 Windows 训 DR。** 未 push（GitHub 备份待刷新）。
- 🖥️ 2026-06-20（Windows 机接手）**外置盘已直插 5800H+RTX3060 6G，仓库在 `H:\ReSETP`**。Claude 只读环境探针结果（未安装未跑项目代码）：
  - **GPU**：RTX 3060 6G，驱动 596.21，CUDA 13.2，可用（当前仅 Zotero/Claude 占 844MiB）。PPO 网络小（11-19 维 obs+小 MLP），6G 够；真瓶颈是 CPU 样本效率（1 episode=16000 eval=一整轮 ALNS）。
  - **现成 Python**：PATH 默认 `python`=3.14.3；`D:\Anaconda\python.exe`=**3.14.3 + numpy 2.4.4**；`conda` 命令坏了（`No module named 'conda'`，环境管理用不了）。**3.14 太新，torch+CUDA 轮子大概率还不支持** → PPO venv 建议用 Python 3.12/3.13。
  - **⚠️ 致命坑②实锤（硬编码 M1 路径，会拦死训练）**：`train_async_block_ppo.py:24`/`second_layer_debug.py:23`/`report_pilot.py:13` 硬编码 `SYSTEM_WORKER_PYTHON="/opt/anaconda3/bin/python3.13"` + `SYSTEM_WORKER_NUMPY="2.3.5"`；`worker_client.py:206-207` 回退候选是 `/opt/anaconda3`、`/opt/homebrew` 等 Mac 路径。多处强校验 `SETP_WORKER_PYTHON must be {required}`（train_async_block_ppo.py:703 / offline_bandit.py:914 / second_layer_debug.py:772 / diagnose_pilot_failure.py:498）在 Windows 必然报错。`train_async_block_ppo.py:720` 有 `--required-worker-python` CLI 覆盖可绕过，但 offline_bandit 等是否可覆盖待查。**需 Codex 把这些 M1 硬编码参数化（指向本机 Python），属 RL lane 代码、未碰 cost/check/evaluation 保护文件。**
  - **未发现现成 RL venv**（`solver/rl/.venv` 不存在）。
  - **环境策略（Claude 建议，待 user 拍板）**：本机只报相对%、所有对手（winner/AlphaUCB/SA/random/PPO）同环境跑→漂移自抵消，故**建议本机统一单环境**（一个 Python 3.12 venv：torch+CUDA + numpy + 可编辑装 setp_solver；`SETP_WORKER_PYTHON` 指它自己），比 M1 的"双环境"更干净、最省跨环境漂移；唯一硬要求=**本机内部确定性**（同种子同结果，用前先验）。不必复刻 M1 的 numpy 2.3.5。
- ✅ 2026-06-20（Windows 环境搭建完成，Codex commit `b6b984df`，**未训练**）：user 先前已下发"环境 audit+搭建"提示词给 Codex（要求双环境），Codex 完成：
  - **solver worker 环境**：`C:\Users\zlxshu\.venvs\resetp-solver-py313\Scripts\python.exe` = Python 3.13.12 + numpy 2.3.5 + scipy 1.18.0（注：numpy 对齐了 M1 金标准，但 x86≠ARM 仍不能比绝对值）。`SETP_WORKER_PYTHON` 已写入用户环境变量指向它。
  - **PPO/CUDA 环境**：`C:\Users\zlxshu\.venvs\resetp-ppo-cu124-py312\Scripts\python.exe` = Python 3.12.13 + numpy 1.26.4 + **torch 2.5.1+cu124（CUDA 12.4，cuda.is_available()=True，RTX3060 Laptop）** + gymnasium 1.0.0 + SB3 2.4.1。
  - **确定性**：verify_20251113 / seed7 / budget32，连跑两次均 £1013.534044477906、路线签名一致 → 本机内部确定（**仅证串行小 case；并行 async 训练下的确定性须另验**）。
  - **Windows 坑已修**：git core.autocrlf=false；清掉 .git/objects/pack 的 AppleDouble `._*.idx`（non-monotonic 警告消失）。commit `b6b984df` 把 DR 入口里硬编码的 `/opt/anaconda3/bin/python3.13` 默认改成优先读 `SETP_WORKER_PYTHON`，并修 Windows 反斜杠导致的 async report 路径校验误判；未碰 cost/check/evaluation。测试 test_async_block_ppo 7 passed、metaheuristic_baselines OK。默认 `python` 仍是坏 conda 的 D:\Anaconda 3.14.3，禁用，一律显式走两个 venv。
  - **🧠 Claude 把关判断（关键，单/双环境之争的真相）**：我先前建议"单环境"是为了"一杆秤"（所有对手同环境算成本→相对%可信）。Codex 按先前提示词建了"双环境"，但**双环境在本机同样满足"一杆秤"**——因为 RL env 的算子执行全部经 `WorkerClient`→子进程→`SETP_WORKER_PYTHON`(py313) 计算（block_env.py:10/40 用 WorkerClient；baselines.run_official_winner_kernel:199 也用 worker_python 子进程）。torch venv(py312) 只当"大脑"，**不计算任何解的成本**。故 AlphaUCB（run_alpha_ucb_env_policy 走 env）、DR（run_ppo_block_policy 走 env）、official winner（worker_python 子进程）三者都在 py313 一杆秤上 → 相对比有效。**判断=保留双环境，不重建**（py312+torch2.5.1+cu124 是成熟组合，硬塞单环境反而 numpy 版本会和 torch 打架）。
  - **⚠️ 残留漂移风险（写进训练/评测验收）**：`run_alns_wouda_strengthened`(baselines.py:282) 用 `[sys.executable]` 子进程跑——若从 torch venv(py312/numpy1.26.4) 调用就**不在 py313 一杆秤上**（旧 M1 trap 同型）。这是冷门"强化 ALNS-Wouda"基线、非核心 DR-vs-AlphaUCB-vs-SA 对比。**铁律：最终上报的 DR/AlphaUCB/SA 对比，每个对手成本必须经 py313 worker；SA(fair-SA)的解释器路径须在评测前确认走 py313，任何 sys.executable 路径要审。**
  - **🖥️ GPU/CPU 利用（回应 user "别全让 CPU 干"）**：DR-ALNS 是 learning-to-search，策略网是小 MLP（~19维obs+MultiDiscrete头）→ GPU 必然只占小头，这是架构本质（也是论文卖点：保 ALNS 质量、只学自适应），强行做大网不会加速训练。**真加速杠杆=CPU 并行**：`train_async_block_ppo.py` 是异步 PPO，可在 16 线程上并行跑多个 ALNS rollout worker（每个 worker 纯 CPU/numpy 无 torch，不抢 6G 显存）→ 直击"1 局=16000eval、样本效率低=天级"瓶颈。计划：训练用并行 async（worker 数按 16 线程+16G 内存调，防 OOM）；GPU 跑策略网；**最终上报的对比评测改用受控串行、保确定可复现**。
- 📋 2026-06-20（Claude 读训练码 + 写 Prompt A）：user 拍板 ①本机环境=保留 Codex 的双环境（不重建，理由见上方把关判断）②DR 第一版=**先跑小 pilot 验路子**（非直接大规模）。Claude 精读 `block_env.py`/`action_space.py`/`async_block_policy.py`/`train_async_block_ppo.py`/`worker_client.py`/`bundle_manifest.py` 后定论：
  - 动作空间 `BLOCK_ACTION_NVECS=(7,4,5,4,4)`=destroy(6算子+alpha_ucb)/repair(3+alpha_ucb)/q(5)/threshold(4)/exploration(4)。obs=19 维。
  - **课程/掩码/shared-baseline 三件套代码里都还没有**（`curriculum_training_plan.md` 是"待建"清单）：`_reward()`(block_env.py:70)单一无分阶段；policy 无掩码接口；`flatten_episodes()`(:161)全局标准化无按 bundle 分组。
  - **训练清单已干净**：`training_bundle_manifest.json` train=E-UK100_02/三班-150/三班-200，held_out=100_03，formal=100-01/L-main；`bundle_manifest.validate_manifest` 硬禁玩具题(verify/demo/E-UK25)+禁 100-01 泄漏。三班 150/200 算例已存在。历史"误用玩具题"坑已堵。
  - **当前异步训练器压根没用 GPU**（model 全程 CPU），且 `_cpu_probe_row()`(:546)用 Unix `ps` → Windows 失效。`self-check` 子命令已有把关闸（worker=py313 / numpy=2.3.5 / 零违约 / busy_ratio≥0.60）。并行=`ProcessPoolExecutor(num_actors)` 默认 6，可上调（测 RAM 防 OOM）。
  - **小 pilot 拆两条 Codex 提示词**（省 credits=烧训练算力前先验新代码）：**Prompt A**=建三件套+修 Windows 并行监控(psutil)+GPU 接管梯度更新+单测+self-check+极短冒烟（只验机制：阶段切换/掩码命中率/零违约/并行/内存/内部确定性），**不下性能结论**；已写入 `docs/handoff/codex_prompts/04_dr_curriculum_pilot_build.md`，待 user 发 Codex。**Prompt B**（A 机制验过再写）=跑真小 pilot + 100-01 上 DR vs AlphaUCB vs SA 相对%评测，无增益诚实 HALT；时长按 A 测出的并行速度定。
  - **残留待办/盯防**：HANDOFF 有未提交改动（让 Codex 随下批 commit + 刷新 GitHub 备份）；最终评测须确认 SA/fair-SA 也走 py313 worker（防 `run_alns_wouda_strengthened` 式 sys.executable 漂移）。
- ✅ 2026-06-20（Codex Prompt A 机制搭建 + 验证完成，**未跑真 pilot、无性能结论**）：
  - 分阶段 commits：`77200af8` audit + Windows psutil probe；`b17ac8e5` curriculum phases/controller/CLI；`c89f2ed8` action mask + policy mask plumbing；`550a7819` shared baseline + `--device auto|cpu|cuda`；`90ea6670` Windows RL validation guards + PPO requirements pin；`5f870000` actor rollout RNG seeding for single-actor determinism。
  - 环境锚：PPO venv `C:\Users\zlxshu\.venvs\resetp-ppo-cu124-py312\Scripts\python.exe` = Python 3.12.13 + NumPy 1.26.4 + torch 2.5.1+cu124 + CUDA available=True + sklearn 1.5.2 + scipy 1.14.1 + psutil 7.2.2；worker `C:\Users\zlxshu\.venvs\resetp-solver-py313\Scripts\python.exe` = Python 3.13.12 + NumPy 2.3.5；GPU probe = RTX 3060 Laptop 6G。
  - 验证：`solver/rl/tests/` = **110 passed**；`test_async_block_ppo.py` = 15 passed；最终 self-check `solver/reports/dr_alns_ppo_v3_block_dr_alns/async_pilot/self_check_final_seeded/` = `PASS_ASYNC_SELF_CHECK`（12 episodes、零违约、worker py313/numpy2.3.5、busy_ratio≈0.700）；内部确定性 mini-check `determinism_c` vs `determinism_d`（num_actors=1、seed=123）首个 episode `reward_sum` 与 `best_obj` 逐值一致。
  - 短冒烟 `solver/reports/dr_alns_ppo_v3_block_dr_alns/async_pilot/smoke_final/`：参数 `--num-actors 12 --eval-budget 256 --block-size 16 --timesteps 384 --rollout-min-episodes 4 --phase-min-episodes 4 --device auto`；阶段切换 route→energy→carbon→dynamic；37 episodes、384 valid steps、零违约、reward finite、device=`cuda`、shared baseline enabled；destroy-head mask invalid rate 约 0.241-0.411；throughput final busy_ratio≈0.838、episodes_per_hour≈127.84、worker_failure_count=0；psutil 66 samples、max total_worker_pcpu≈1130.7、max worker RSS≈505.8MB、probe_error 空。
  - Windows 坑：本会话 shell `workdir` 会落回 `C:\`，实际命令用 `git -C H:\ReSETP` / `Set-Location -LiteralPath`；PPO venv 初始无 pip，`ensurepip` 后首次装最新版 sklearn 拉到 NumPy 2.4.6 与 SB3 冲突，已修回 NumPy 1.26.4 并 pin `scikit-learn==1.5.2`/`scipy==1.14.1`；Windows path 分隔符修了 offline bandit/probe guards；测试夹具去掉 `/opt` worker 硬编码；`operator_manual` 的 `id(instance)` cache 加节点签名，防测试间 stale depot；actor 子进程按 episode seed 设 torch/NumPy RNG。
  - 报告：`solver/reports/dr_alns_ppo_v3_block_dr_alns/async_pilot/prompt_a_mechanism_validation_report.md`。Prompt B 建议：可从 12 actors 起步继续真小 pilot，继续采 psutil；GPU 只承担 PPO update，CPU rollout 仍是瓶颈；所有 DR/AlphaUCB/SA 相对%必须同在本机 py313 worker 成本口径内，绝不和 M1 绝对数横比。
- 📋 2026-06-20（Claude 核 Prompt A 报告 + audit + 写 Prompt B）：Claude 亲读报告与 audit，判定**机制真过关非表面**（掩码命中率 0.24-0.41 真在动、确定性 reward+best_obj 逐值一致、四阶段按"够 episode+零违约不退化"真切换、GPU 接管更新、worker 全程 py313；Windows 三个真 bug 修了）。从 `async_pilot/audit/audit.md` 得三条定计划的事实：
  - **碳信号是真的**：worker `metrics` 含两层碳 `E_cv_direct/E_ev_indirect/E_total`+`cost_carbon`+`carbon_quota_kg` → 碳阶段真信号。**但公平/动态在 block 响应无信号**（无 profit-by-depot/无动态字段）→ 诚实课程表=**route→energy→carbon 三阶段**，公平/动态不进 block 课程（由最终 cost.py 评测与 E7 运行时承担）。
  - **冒烟 RAM 数据不算数**：smoke 是玩具预算(eval_budget=256/block16)，max worker RSS 仅 505MB、探针见最多 24 worker 进程（12 actor 切换重叠）。真 pilot(16000eval+真算例+block128)内存大得多，**Prompt B 必须先在真实设置重测 RAM 再定 num_actors**，不能信"没逼近 14GB"。
  - **评测口径命门**：`evaluate_policy.py` 算法集**不含 SA**（只有 ppo_block/alpha_ucb_block/alpha_ucb_env/random_block/official_winner_kernel，全 py313 worker）。**SA 须另从 solver `alns_crush_v2.run_task1`(scikit-opt-SA, py313, 16000eval, 暖启动) 取**，才能和 DR/AlphaUCB 同一杆秤。Prompt B 写死"每行 worker=py313 核验 + 不评 alns_wouda_strengthened"。
  - **Prompt B 已写入 `docs/handoff/codex_prompts/05_dr_curriculum_pilot_run.md`**（待 user 发 Codex）：阶段0 真实设置资源探针+加周期 checkpoint→阶段1 有界限小 pilot(route,energy,carbon / masks / shared baseline / device cuda / 默认目标墙钟≈2-3h 可中断)→阶段2 100-01+held_out 评 DR vs AlphaUCB(block/env)/SA/random 相对%(全 py313 核验)→阶段3 PROMISING/WEAK 判决(只相对%, 不与 M1 横比, WEAK 则诚实止损 DR=future work)。主判据=DR vs AlphaUCB(block)。pilot 时长=2-3h、评测 seed=5（user 确认）。
- ⛔ 2026-06-20（Codex Prompt B 阶段0 实测后 `HALT_RESOURCE_PROBE`，**未训练未评测无性能结论**，commits `41eba3c0`/`401953f4`/`7814cf72`，测试 19/114 passed）：**关键硬事实=全预算 DR 训练在本机不可行（迁回 M1 也要知道）**：
  - **单局墙钟（16000 eval，12 actors 竞争 8 核/16 线程下）**：100c(E-UK100_02) **均 5122s≈85 分钟/局**(min4439/max6013)；150c(三班-150) **均 15078s≈4.2 小时/局**；200c(三班-200) **一局未完**。RL 要成百上千局 → 全预算训练=几天到跑不动（HANDOFF 早警告"几千局=几千万步=几天算力"，现被实测坐实）。
  - **内存**：12 actors 峰值 system used **14.16GB**、available 低至 **2.07GB**(87.2%)，越过计划 12GB 安全线。**主因仍是单局太慢**，内存只是雪上加霜（注：每个 actor 子进程在 Windows spawn 下各自 import torch≈内存大头；future 优化=actor 用 numpy 前向、不载 torch，可大降内存让 16 线程真并行——本 pilot 不做）。
  - 吞吐 final **2.26 episodes/hour** → 2-3h 仅 ~6-7 局，达不到 12 局底线、走不完 route→energy→carbon。Codex 还修了"超额提交 actor episode、到点仍空等"的 bug(`401953f4`)。
  - **Claude 判断（纠正先前"内存thrash"猜测）**：瓶颈是**单局原生耗时**非纯内存。**唯一能在本机出学习信号的配置=降 eval budget(如~3000)+只用最快 bundle(100_02)+降 actors 到 6-8 的"趋势 pilot"**（同降预算下 DR vs AlphaUCB vs SA 相对%仍有效，诚实标注"reduced-budget trend pilot 非全预算"）。Option2(更长墙钟+少 actors)即便过夜也仅~50-60 局且 150/200 无望；Option3(止损 DR=future work)是诚实退路但**在试过 reduced-budget 前过早**。**战略提示(待 user)**：若 reduced-budget pilot=PROMISING 而论文需全量训练，本笔记本与 M1(8G RAM 更紧)都可能不够→需讨论云端 GPU/CPU。已 ask user 选 scope；新提示词(06)按所选 scope 写。
- 🧭 2026-06-20（user 战略决策：两条工作线并行 + Scheme 3 迁移）：
  - **两条线并行**：①**x86(本机)=长期 DR-ALNS 训练机**（DR 是面向未来工作的长期追求）；②**M1=论文刚需保底**——轨②的 8 个 ALNS 文献基线对比(现 HALT 在 throughput, 提示词 03 已草) + 图表壳复印。ALNS 是现阶段论文刚需，至少做好保底；DR 是上限/future work，不卡论文。
  - **user 工作纪律(新, 通用)**：DR/ALNS 碰到卡点/瓶颈/进度慢，**先上网 + 翻 Zotero 文献找现成解法，别重复造轮子**（"我们碰到的问题大多前人早研究透了"）。已记入 [[feedback-communication-style]]。**眼下"训练太贵"卡点的文献解=Daysalilar 课程 DRL 在小算例(N=10)训→零样本泛化大算例(N=100)**（见 [[baseline-algorithm-catalog]]）→ x86 DR 应"小/快设置训练、全预算只评测验泛化"，非 85min/局硬灌。
  - **迁移=Scheme 3（user 选定，比其原方案省一次大盘往返+M1 停工）**：关键事实=`.gitignore` 排除 `models/`+`solver/reports/`+`*.npy`（算例/DR报告/模型都不在 GitHub），但 **DR 产出极小**(模型~几十KB + 几个CSV/MD)→不必倒腾大盘回来，U盘/网盘/`git add -f` 即可。流程：①现在 push 刷新 GitHub→②整盘复制到 x86 本地 `D:\ReSETP`(先验 D 盘空间)→③验 x86 脱盘能训(self-check from D:, 注意 venv 勿 editable 钉在 H:)→④拔盘单程去 M1 并留在 M1(M1 拉 GitHub 跑轨②+图表)→⑤x86 在 `dr-x86` 分支训 DR, 代码 push GitHub、小产出 U盘/`git add -f`→⑥M1 拉 dr-x86 整合。大盘全程不回 x86。
  - **下一步**：提示词 06=x86 迁移准备(push+复制到D:+脱盘验证, 不训练不删 H:)；之后 x86 跑 reduced-budget DR 趋势 pilot；M1 那条线另写(轨② 03 提速 + 图表)。
- ✅ 2026-06-20（Codex 提示词 06 迁移准备完成，**未训练、未删 H:**）：GitHub push 成功，分支 `codex/reporting-pipeline` HEAD=`5ad097e8ededcaded97d7a7001ccdfb158588864`（两机以后靠它同步代码）。整仓复制 `H:\ReSETP`→`D:\ReSETP`（源 2.64GB，robocopy exit 0 无失败，D 盘复制后剩 103.65GiB）。`D:\ReSETP` 是有效 git 仓库(远程仍 zlxshu/ReSETP, autocrlf=false)。**脱盘 self-check 在 D 盘 PASS_ASYNC_SELF_CHECK**(4 episodes/零违约/worker py313/numpy2.3.5/busy0.66)；脱 H 核查通过：repo_root/PYTHONPATH/worker import 全指 `D:\ReSETP`，无 editable 钉在 H，无 H: 路径回落。**大盘可安全拔去 M1。**
  - **🔀 两机分工 + 分支约定（防分叉，关键）**：**x86 在 `D:\ReSETP`、开 `dr-x86` 分支**只动 DR lane(solver/rl/)；**M1 在大盘、留 `codex/reporting-pipeline` 分支**做轨②(ALNS 基线)+图表(动 solver/src 基线代码、reporting、docs 图表)。两边代码经 GitHub 同步(push/pull 各自分支)。DR 小产出(模型.pt+相对%报告)经 `git add -f` 或 U盘传，大盘不回 x86。**HANDOFF 变更日志冲突**：x86 的 DR 条目标 `[x86/DR]`、M1 标 `[M1]`，合并时两边追加行都保留(Claude 在 merge 时reconcile)。两机不动对方的文件区，冲突面最小。
  - **并行下一步**：①**x86**=提示词 07(reduced-budget DR 趋势 pilot, 在 D:\ dr-x86 分支)；②**M1**=拔盘到 M1 后开新会话, M1 的 Claude **先读本 HANDOFF**, 接手轨②(提示词 `docs/handoff/codex_prompts/03_baseline_speedup.md`)+图表壳复印(`docs/handoff/memory/figure-redesign-task.md`)。
- 🛑 2026-06-20（Codex Prompt B 阶段0 真实资源探针 HALT，**未跑 pilot/未评测/无性能结论**）：新增并提交 `--checkpoint-every-updates` + 系统内存探针（commit `41eba3c0`，RL tests 113 passed），随后修复训练器尾部过量提交导致到点后等待多余长 episode 的问题（commit `401953f4`，RL tests 114 passed）。真预算 12 actors 资源探针 `prompt_b_resource_probe_12/` 跑 `eval_budget=16000/block_size=128/timesteps=1536`，完成 13 episode、零违约、worker 全程 py313/numpy2.3.5，但系统内存 used 峰值≈14.16GB、available 最低≈2.07GB，且吞吐仅≈2.26 episodes/hour；按锁定的 2-3h pilot 只能完成约 6-7 episode，不足 12 episode 底线、也不足以可靠走 route→energy→carbon。已停止残留 worker，报告 `solver/reports/dr_alns_ppo_v3_block_dr_alns/async_pilot/prompt_b_halt_resource_probe_report.md`。下一步必须显式改 scope：降 eval budget/只用快 bundle 做趋势 pilot，或放宽到更长墙钟并降 actors 到 8/6，或停止 DR pilot、保留 Prompt A 机制证明。
- [M1] 2026-06-22（09c E2 ALNS SA acceptance gate HALT，**阶段③④⑤继续停止**）：commit `df608660` 已实现 E2 专用 SA acceptance + scan bridge runner，legacy `100-01` 锚精确不漂（mean `4878.331796187524`、seed2 `4779.053444002934`、zero violations、delta `0.0`），但正式 09c gate 未过。报告 `baselines/e2_alns/sa_acceptance_halt_report.md` 记录：135 row 中 100 row returned zero-violation finite solution，35 row `HALT_HARD_TIMEOUT`；200c vanilla/multidepot 上 LNS 与两种 SA-ALNS 全部 timeout，threeshift-200c 上 ALNS 也有 timeout。已确认 09c 不是可包装通过的结果，不能启动基线重做或 69 实例正式 T3。
- [M1] 2026-06-22（09d 吞吐根因计划锁定）：基于 `baselines/e2_alns/sa_raw_runs.csv` 独立复核，threeshift completed rows 显示 LNS 约 `87/52/33/18/11 evals/s`（50/75/100/150/200c），`alns_sa_lns_cooling` 约 `109/54/11/12/6 evals/s` 且方差大；ALNS 在 50/75c 能跑够迭代时追平/赢，但 100c+ 明显迭代不足。当前工作假设从"SA 不够"更新为"每步太贵导致迭代饿死"，下一步只做 09d：先 profile、再做最小工程提速、修 timeout 返回 incumbent，真实墙钟 gate 仍必须赢稳 LNS 才能继续。若提速到 LNS 级吞吐后仍系统性输 LNS，则输出 `HALT_TRUE_GLNS_BETTER_AFTER_THROUGHPUT`，交 user 决策，不再无限加料或退 DR-ALNS 救场。提示词已写入 `docs/handoff/codex_prompts/09d_e2_alns_throughput.md`。
- [M1] 2026-06-23（09e E2 算例/参数根因诊断启动）：09d 已产出 `HALT_TRUE_GLNS_BETTER_AFTER_THROUGHPUT`，说明单纯吞吐/迭代饿死不足以解释 ALNS 输 GLNS/LNS；当前主线判断转为先审计算例经济口径：GLNS/LNS 在这些 E2 小门上本质吃到"全油车最优"盆地，而病根可能是 Goeke 物理参数（80kWh 电池、90km/h 速度）叠加 UK 2025 经济参数（公共快充/占用费）使 EV 在真实碳价 £50.34/t 下无经济性。09e 只做诊断：新增 `baselines/e2_alns/instance_param_diagnostic.py` + `baselines/e2_alns/instance_param_diagnostic.md`，全程 `carbon_price=0.05034` 钉死，只用 `dataclasses.replace(DEFAULT_PRICES, ...)` 做内存覆盖，不改 `prices.py`/bundle/`cost.py`/`check.py`/`evaluation.py`/算法语义；先 Phase0 证明覆盖能穿透 `evaluate/check/charging/run_alns_wouda`，再 Phase1 拆全油/全电/混合成本，最后 Phase2 扫电池、速度、公共电价、占用费单变量。阶段③④⑤继续停止，等 09e 说清"全油车退化"真因后再决定是否用非碳现实参数重标算例并回测 ALNS vs GLNS；winner 不放弃，也不把 GLNS 直接当底座。
- [M1] 2026-06-23（09f 大算例 all-CV 诊断修正）：09e 结果反证原假设过粗：小算例 `25c/50c` 在固定真实碳价下 mixed 已经 3/3 低于 cv_only，不能诊断"全油退化"；CV 碳也已确认正确计入（`E_cv_direct = fuel_liters * diesel_ef`，并入 `E_total * carbon_price`，不是漏算）。09f 已写入 `docs/handoff/codex_prompts/09f_largescale_allcv_diagnostic.md`，诊断对象改为 09d 中 LNS/all-CV 占优的大算例 checkpoint，且必须区分两种真因：经济/参数导致长路线 EV 被 80kWh 续航与公共快充/占用费压垮，还是 mixed 好解存在但大规模搜索不稳定。执行时继续全程 `carbon_price=0.05034` 钉死、只做内存 `dataclasses.replace(DEFAULT_PRICES, ...)` 覆盖、不改 `prices.py`/bundle/`cost.py`/`check.py`/`evaluation.py`/算法语义；100c/150c 若表现为 best mixed 已存在但 mean 输，应诚实写成搜索可靠性/方差问题，200c 才是优先判定经济参数真凶的主战场。
- [M1] 2026-06-23（Claude worktree 接手整理 + HANDOFF 单一事实源修复）：已核对主仓 `HANDOFF.md`、`.claude/worktrees/compassionate-lamport-ebd93b/HANDOFF.md`、09f/09g 提示词与 09f 报告；Claude 旧 worktree 里有一段未进主仓的 `[M1]` 战略链（09c/09d 双 HALT、GLNS/LNS 其实是无 EV/碳的通用纯路由基线、全油主导 regime 让 ReSETP 的碳/EV 感知搜索变成负担、Goeke 物理参数 vs UK 经济参数拆分、09e/09f 诊断演进）。本条已把其归并回主仓事实源；以后以主仓 `HANDOFF.md` 为准。限制：不可见 Claude UI 历史不能被 Codex 直接读取，本条只基于仓库文件、worktree 文件、用户粘贴内容与当前 appshot。
- [M1] 2026-06-23（09g 暂停为证据先行；速度/电池参数纠偏）：09f 的 40km/h fixed-replay 翻盘只是机制证据，不能直接证明主算例应改成城市配送速度。用户纠偏成立：当前大算例偏跨城/大区域，真实物流多数会走高速，`v_speed_ms=25.0`(90km/h)与 UK Strategic Road Network 2025 平均约 56.6mph≈91km/h 相容；40km/h 更像城市/local-A-road 场景。更可疑的是 `B_battery_kwh=80` 仍属 Goeke/Davis-Figliozzi 2013 口径，对现代中重型跨城配送可能偏小，但不能拍脑袋改。已新增 `docs/handoff/parameter_evidence_brief_20260623.md` 与 `docs/handoff/codex_prompts/09h_parameter_evidence_review.md` / `09i_evidence_bound_reopt_confirmation.md`：下一步先做 2020+ Zotero/文献/行业证据矩阵、路线 regime 审计、证据约束 fixed replay；只有证据成立且 near-flip/flip 的场景才做真实重优化。`09g_urban_reopt_confirmation.md` 保留但降级为“城市场景草案”，不得作为跨城主线直接执行。后续每轮 Codex 需读 `AGENTS.md`/`CLAUDE-FABLE-5.md` 的诚实/克制/反迎合原则（忽略 Claude 产品细节），宁可写 HALT，也不把 fixed replay 包装成重优化证明。
- [M1] 2026-06-23（09h 证据约束参数诊断完成，artifact commit=a0d5951be1aeae121bf4962d46607c4dbf881370）：新增 `baselines/e2_alns/parameter_evidence_review.py` 与报告/数据，严格不改 `prices.py`/bundle/`cost.py`/`check.py`/`evaluation.py`/算法语义，`carbon_price=0.05034` 全程钉死。Phase0 checkpoint/CSV 审计 30/30 OK；route-regime 审计把 100/150/200c best all-CV 与 best mixed 全部标为 `regional_cross_city_like`（p75 约 130-157km、max 237-285km），因此 40km/h 只保留为 urban/local fork，不作跨城主线。Fixed replay 显示 200c 在 280kWh 下仍 near-flip（E: +0.506%，H 60mph+280kWh: +0.327%），自动触发真实重优化；6 个强证据触发点（100/150/200c × 280kWh / 60mph+280kWh）均 seeds 1-5 跑满，30/30 reopt row `OK`、零违约、无 `HALT_REOPT_COLLECTION_COST`。结论 `HIGHWAY_BATTERY_UPDATE_SUPPORTED`：保持跨城/高速 regime、不改碳价时，现代配送车 280kWh 电池足以让 mixed/EV 方案在真实重优化中跨 100/150/200c 明确优于 all-CV fixed reference（200c E_distribution_truck_280 mean 8616.45 vs all-CV fixed 8979.13，mean gap -4.04%，EV routes mean 67.2）。下一步不是立刻改 `prices.py`，而是走 09i/参数正式化：确认 280kWh 与当前车辆质量/固定费/载重口径是否作为主场景标定，或作为 modern-battery scenario 分表呈现；随后再回测 ALNS vs GLNS/LNS 正式 T3。
- [M1] 2026-06-23（09i/09j 参数正式化：默认电池 80→280）：按 09h `HIGHWAY_BATTERY_UPDATE_SUPPORTED` 结论，正式将 `solver/src/setp_solver/prices.py` 的 `B_battery_kwh` 从 Goeke/Davis-Figliozzi 2013 原值 80 kWh 提升为现代中型电动配送卡车证据约束下界 280 kWh；保留 `v_speed_ms=25.0`(90 km/h)、`carbon_price=0.05034`、公共/车场电价、占用费、固定费、`cost.py`/`check.py`/`evaluation.py` 与算法语义不变。`docs/paper_submission_final/paper_main.tex` 参数表和表下注释同步说明 Goeke 原值与 Volvo FL/FE 280 kWh 证据边界。旧 09e/09f/09h 报告和既有图表仍是历史产物；后续 T3/正式表图必须在新默认参数下重跑后再引用。
- [M1] 2026-06-23（Claude 交接总纲落盘 = Codex 接管入口）：Claude credits 将尽，user 转 Codex 既思考也执行。已写 `docs/handoff/codex_prompts/MASTER_codex_takeover_plan.md`，含：行为铁律(每轮先读 `CLAUDE-FABLE-5.md`+本 HANDOFF+memory；诚实优先、宁 HALT 不注水、克制不硬刚、禁改 cost/check/evaluation、系统 Python+PYTHONHASHSEED=0、数字绑 commit)；现状(根因=80kWh 老电池已修为证据约束 280kWh、跨城 90km/h 与碳价 0.05034 不动、旧正式数字全作废)；剩余总路线图(§2 先查"280kWh 是否反成 all-EV 退化"→§3：①基线文献最优[10] ②E2 正式 T3[11+强化 ALNS,预期碳/EV 感知 ALNS 在混合最优下反超无 EV 的 GLNS] ③E1-E7 全量重跑建 280kWh 新锚 ④图表填数 ⑤动态需求三交互整合 ⑥撰写)；每步预备方案(E2 若仍输 LNS=诚实定位第一梯队+机制差异化,不退旧数据;若反成 all-EV 则回退证据下界电池)。**Codex 后续按该总纲自驱,以主仓 HANDOFF 为单一事实源。**
- [M1] 2026-06-23（280kWh 反向退化 gate 启动，未通过）：Codex 接管后先核验 `MASTER_codex_takeover_plan.md` §2，发现主仓已有未跟踪 `baselines/e2_alns/280kwh_fleet_mix_probe.md` 提醒 280kWh 可能把 all-CV 压力转成 EV-heavy/all-EV 压力。新增 `baselines/e2_alns/fleet_composition_gate_280.py`、`baselines/e2_alns/280kwh_fleet_composition_gate.md` 与数据目录，默认每个 variant 子进程硬超时；判定只看实际 `cv_route_count/ev_route_count`，不信 `ev_only/cv_only` 标签。已跑 smoke（3 代表实例 × free/cv_shell/ev_shell × seed1, 300 eval）：可判 winner 均为 `balanced_mixed`，但 `threeshift-200c ev_shell` timeout，报告 verdict=`HALT_COLLECTION_COST`, evidence=`staged_probe`。因此 §2 仍未通过，不能启动 E2/T3 或 E1-E7 全量重跑；下一步需扩大到跨规模/多 seed/更强预算，或先把 gate 改成可恢复任务队列后跑满。
- [M1] 2026-06-23（280kWh fleet-composition gate Stage1 跑满，结论反转为 EV-dominant）：`baselines/e2_alns/fleet_composition_gate_280.py` 已扩展为可恢复任务队列（stage 快捷配置、skip terminal rows、task_queue、3-worker 子进程调度、缺行/timeout HALT）。Stage1 已跑满代表集 17 实例 × seeds1-3 × free/cv_shell/ev_shell，153/153 raw rows、51/51 winner rows、0 timeout；canonical 报告 `baselines/e2_alns/280kwh_fleet_composition_gate.md` 结论 `HALT_EV_DOMINANT`。核心数字：winner composition = 34 `ev_heavy_mixed` + 16 `all_ev` + 1 `all_cv` + 0 `balanced_mixed`；mean EV share：multidepot 0.947、threeshift 0.986、vanilla 0.878。按 gate 规则 §2 未通过，Stage2 不启动，E2/T3 与 E1-E7 全量重跑继续暂停；下一步应决策 280kWh 是否仅作为 modern-battery/EV-dominant scenario，或回退到更保守证据下界电池重新 09h/§2 gate，不能把 280kWh 结果写成“真混合车队跨规模成立”。
- [M1] 2026-06-24（09k evidence-bound battery spectrum gate 跑满，当前决策仍暂停 E2/T3）：新增 `baselines/e2_alns/battery_spectrum_transition.py` 与 `baselines/e2_alns/battery_spectrum_transition.md`/data，继续冻结 `prices.py`/`cost.py`/`check.py`/`evaluation.py` 和算法语义，只用内存 `B_battery_kwh` override；Phase0 审计 OK，但明确 runner 必须传显式 override warm start，因为 `run_alns_wouda` 默认初始构造不会把 override prices 传进 `build_initial_solution`。证据候选只取文献/车型明确 kWh，不用等距网格；screen 21 个证据值，full gate 选 6 个锚点（80/100/113/141/210/280）跑满 17 代表实例 × seeds1-3 × 3 variants，918/918 raw rows，0 timeout/error（612 OK + 306 `INIT_INFEASIBLE`）。报告 verdict=`BALANCED_EVIDENCE_BAND_FOUND` 但必须按解释读：严格均衡只出现在旧文献锚 `80kWh`（51 winners: 32 balanced, 18 all-CV, 1 EV-heavy, mean EV share 0.408）；当前车型证据没有稳健 balanced 候选，`100kWh` 是边界但 winner 多数 EV-heavy（30/51 EV-heavy, mean 0.681），`113/141/210/280kWh` 全部 EV-dominant（mean EV share 0.780/0.877/0.898/0.898）。当时记录的“三分叉”已被下一条 user 纠偏覆盖：80kWh 不再作为主场景退路，下一步改为来源约束实践混合带 gate。E2/T3 和 E1-E7 正式重跑继续暂停，直到 09l 或后续主场景 gate 通过。
- [M1] 2026-06-24（user 纠正 09k 判读：80kWh 不能回潮，目标改为来源约束实践混合带）：user 明确指出 80kWh 已被先前大算例诊断推翻为“电池太小导致全-CV/EV 无用”的旧问题，不能因为 09k aggregated/full-gate 出现 balanced 就把它拿回主场景；当前研究趋势本就偏 EV，不要求 1:1，只要求 EV/CV 实际占比在 20%-80% 内、来源真实、解释得通。新判据：电池容量必须来自文献/车型明确 kWh，不得等距网格或插值造参数；不能只在单个算例或全局均值成立，必须覆盖所有梯度规模并包含稳定性校验实例；主指标先看 winner 的 EV route share，同时尽量补 customer/distance/demand share 防路线数假平衡。已更新 `MASTER_codex_takeover_plan.md`：80kWh 只作历史/文献锚，不再作为候选主场景；下一步写/执行 09l 来源约束实践混合带 gate，若所有真实来源候选都不能跨规模维持 20%-80%，则诚实报“电池单参数不足”，再考虑运营约束退路，绝不为了混合继续调无来源电池值。
- [M1] 2026-06-24（09l 来源约束实践混合带 Stage A 跑满，电池单参数不足）：新增 `baselines/e2_alns/source_bound_mixed_band_gate.py`、报告与 `source_bound_mixed_band_gate_data/`，继续冻结 `prices.py`/`cost.py`/`check.py`/`evaluation.py`、bundle 与算法语义，只用内存 `dataclasses.replace(DEFAULT_PRICES, B_battery_kwh=x, carbon_price=0.05034)` 覆盖；`80kWh` 仅作 reference，不进主候选。Stage A 按来源候选 `60/81/82.6/89/100/113/123.9/140/141/150/176/180/194/200/210/240/280/282/291kWh` 跑全梯度 `-01` 实例（vanilla/multidepot 10-200c + threeshift 50-200c）× seed1 × `free_mixed/cv_shell/ev_shell`，`1311/1311` raw rows 跑满（892 OK + 419 INIT_INFEASIBLE，0 timeout/error），winner rows `437/437`。结论 `BATTERY_ONLY_INSUFFICIENT`（screen-gated，不冒充 Stage C）：没有任何非 80kWh 来源候选达到 pass/near-pass；低端如 81/82.6/89kWh 虽全局均值可在 20%-80%，但跨规模失败（如 81=17/23 实例过带，82.6=16/23，89=15/23）；100kWh 只有 11/23 过带且开始 EV-heavy/all-EV；113kWh 起多数规模 EV-heavy/all-EV（113=6/23，123.9=5/23，140+≈3/23）。Stage B/C 因无幸存候选不触发。**下一步不可继续微调无来源电池；应转运营约束退路**（充电容量/公共桩稀缺/EV 数量或资本约束/长路线 eligibility 等），先做证据矩阵与最小语义设计，不直接改 `cost/check/evaluation`。
- [M1] 2026-06-24（用户补充参数正式化硬要求）：未来若重新确定任何默认电池容量或主场景参数，不允许只改代码默认值；必须同步 `docs/paper_submission_final/paper_main.tex` 的参数表/参数说明、bibliography 中的来源引用、`solver/src/setp_solver/prices.py` 的来源注释，以及 HANDOFF/MASTER 交接说明。代码注释和论文说明都要保留旧参数及其来源痕迹（如 Goeke/Davis-Figliozzi 80kWh、当前 280kWh 证据链），同时标清新参数的来源和诊断依据，避免静默覆盖导致论文后期解释断链。
- [M1] 2026-06-24（09m 运营结构混合带调查启动，首轮只读结构审计完成）：新增 `baselines/e2_alns/structural_mixed_band_investigation.py`、报告与数据目录，只读 69 个 E2 实例和 09l Stage A，不改参数/模型/算法语义。关键结论：75-200 不是单一电池问题；已有 09l 屏幕层在 81/89/100kWh 附近有混合口袋，但同一电池会在不同 family/size 间从 all-CV 跳到 EV-heavy。结构上 75-200 的地理包络相近，规模增大时客户密度上升、最近公共站 p90 距离下降，站点/客户比约 0.105 基本恒定，因此“规模变大自然逼出混合”不成立。更关键的新线索是 **fleet count 语义**：`search/fleet.py` 当前 `infer_fleet_limits()` 返回 unbounded fleet，`check.py` 明确 fleet count 不再是硬可行性约束，而实例 metadata 仍有 `num_cv/num_ev`；09h/09l 的 EV-heavy 解常用几十条 EV route，远超 metadata EV 数。这说明 280kWh EV-dominant 很可能部分来自“现代 EV + 无限 EV 可用”的运营假设。已写下一步提示词 `docs/handoff/codex_prompts/09m_operational_structure_mixed_band_diagnostic.md`：优先查 EV 数量/资本约束，其次查车场/公共桩充电容量、站点可用性/布局、长路线 EV eligibility 和经济代理；所有机制必须先做证据矩阵和 in-memory gate，75-200 含 `-01/-02/-03` 稳定性实例守住 20%-80% 才能进入正式化。
- [M1] 2026-06-24（09n 车辆数量上限/运营约束诊断 Stage A，结论 HALT_COLLECTION_COST）：新增 `baselines/e2_alns/fleet_cap_operational_gate.py`、报告与数据目录，继续冻结 `prices.py`/`cost.py`/`check.py`/`evaluation.py`、bundle 与算法语义；本步只把 `SearchPolicy(max_ev/max_cv)` 当诊断壳并做独立 cap audit，不冒充正式模型约束。Phase0/1 证实“无限 EV 调用”线索真实：09l/09m unbounded winners 多处超过 metadata 车数，280kWh 下 EV route 最高约 70，而 Goeke/ReSETP metadata 的 75/100/150/200c EV 数约 5/6-7/9-10/12-14。Stage A 只测 280kWh、75/100/150/200 的 `-01`、seed1、小预算，三个 cap 场景结果：`goeke_total_cap` 与 `depot_scaled_cap` 均 0/12 winners，主要为 `INIT_INFEASIBLE`，说明原始车数/简单按车场放大在当前“一条 route≈一辆车”的诊断壳下太硬，不能直接提升；`goeke_ev_cap_only` 有 12/12 winners，但总体 fail：mean EV route/customer/demand/distance share 约 `0.193/0.189/0.191/0.190`，只有 4/12 实例过 20%-80%，并有 6 个 fake-balance bucket（路线数接近下界，但客户/需求/距离仍不稳）。另有 7 个 timeout/error 行，因此 verdict=`HALT_COLLECTION_COST`，Stage B/C 不触发。当前不能写“车辆上限解决混合故事”；下一步应转向更真实的运营约束设计：场站充电容量、公共桩稀缺/可用性、EV 资本预算、路线 eligibility，或若继续做 fleet count，则需先正式建模车辆复用/车次语义，不能继续把 SearchPolicy 壳当论文约束。
- [M1] 2026-06-24（09o 充电基础设施小试探，结论仍 HALT 但下一假设排序更清楚）：新增 `baselines/e2_alns/charging_infrastructure_operational_pilot.py`、报告与数据目录，只做 280kWh/200c 小样本，不改 `prices.py`/`cost.py`/`check.py`/`evaluation.py`、bundle 或算法语义。Phase0 证实当前生成实例已存在 station capacity check：公共站通常 1 个桩，而车场按 `customer_count_route_upper_bound` 给很多桩（200c vanilla/multidepot 车场 200 个，threeshift 节点为 91 个）。小跑 3 个 200c `-01` × seed1 × `free_mixed/ev_shell`，6 raw rows 中 3 个 `free_mixed` OK、3 个 `ev_shell` timeout，因此 verdict=`HALT_COLLECTION_COST`，不能下因果结论或进 E2/T3。但 3 个 OK 行同向：EV route share 为 0.627/0.681/0.840，充电能量 100% 来自车场，公共充电路线数 0，单车场峰值并发 24/15/62（对应车场容量 200/91/200）。这把下一步优先级从“公共快充稀缺”前移到“车场充电桩容量/充电资本预算”证据矩阵与正式 gate；仍须先找真实来源，不得直接拍脑袋设车场桩数。下一步提示词已写入 `docs/handoff/codex_prompts/09p_depot_charger_capacity_evidence_gate.md`。
- [M1] 2026-06-24（09q 来源约束电池梯度 × 运营约束综合摸排 runner 建立，当前仅 SMOKE_ONLY）：user 纠偏成立：不能只测 280kWh，也不能只看 75-200；如果故事要立住，必须覆盖 E2 可用 `10-200` 全规模梯度和稳定性实例。新增 `baselines/e2_alns/source_bound_operational_mix_map.py`、报告与 `source_bound_operational_mix_map_data/`，继续冻结 `prices.py`/bundle/`cost.py`/`check.py`/`evaluation.py` 和算法语义；所有电池用内存 `dataclasses.replace(DEFAULT_PRICES, B_battery_kwh=x, carbon_price=0.05034)`，运营约束只作为诊断壳/临时 bundle 覆盖。候选电池为 09l 真实来源值 `60/81/82.6/89/100/113/123.9/140/141/150/176/180/194/200/210/240/280/282/291kWh`，`80kWh` 只作历史 reference。约束场景为 `unbounded_reference`、Goeke/ReSETP 车辆数 cap 诊断、车场桩数诊断、禁公共桩诊断及组合。实例范围已升级为 vanilla/multidepot 的 10/15/20/25/50/75/100/150/200c 与 threeshift 的 50/75/100/150/200c；Stage A `-01` 共 23 实例，Stage B/C 全 69 稳定性实例。Phase0 override audit OK；smoke 跑 `81/280 × unbounded_reference × 2 实例 × seed1 × 3 variants`，12 rows=10 OK + 2 `INIT_INFEASIBLE`，verdict=`SMOKE_ONLY`，只证明 runner/恢复/审计链路可用。完整 Stage A 默认 `19×7×23×3=9177` 子任务，尚未跑满，绝不能引用 smoke 或 diagnostic-only pass 作为论文结论。正式入口已写入 `docs/handoff/codex_prompts/09q_source_bound_operational_mix_map.md`；E2/T3 仍暂停。
- [M1] 2026-06-26（用户澄清：车辆数量上限本来就是论文硬约束，代码翻译错了）：user 明确澄清 `m^g/m^e` 在论文中就是不可超过的车辆类型数量上限，不是候选池或仅靠固定费抑制；因此 TeX 参数表/模型式与代码应统一为 hard cap 口径，这不是新增建模，而是修正代码翻译。代码侧已将实例 `num_cv/num_ev` 接入硬检查：`Instance` 读取 `num_cv/num_ev` 或 raw EVRPTW-MF 的 `numPetrolVeh/numElectroVeh`；`load_search_bundle()` 从 metadata/文本传入；`infer_fleet_limits()` 读 bundle/json/text；`check_solution()` 对超出 CV/EV route 数的解报 `FLEET_SIZE`；shared warm start 和 ALNS 初始构造接入 fleet limits。`docs/paper_submission_final/paper_main.tex` 同步恢复车辆数上限公式和参数表硬上限解释。文献/证据记录见 `docs/handoff/fleet_hard_cap_multidepot_evidence_20260626.md`：Goeke 原算例带车辆数记录；Soriano 2023 支持多车场/伙伴按可用车辆集合限 route；Wang 2023 支持协同多车场中 EV 数/共享充电为运营资源；fleet-size-and-mix 文献则是另一类“车型/数量由优化决定”的口径，不能直接混用。重要风险：旧 09q/09n 在放松 fleet cap 的环境下得到的 “unbounded EV” 诊断需重新解读；正式 E2/T3 前必须先跑 hard-cap feasibility gate，确认原始 `num_cv/num_ev` 与当前 route=vehicle 语义兼容，否则应先处理车辆复用/车次语义或 per-depot cap 设计。
- [M1] 2026-06-26（hard cap 立即暴露 E2 warm-start 可行性风险）：恢复 hard cap 后，快速探针显示 E2 `-01` 代表集（vanilla/multidepot 10-200c、threeshift 50-200c）在当前共同 warm-start 构造和 metadata `num_cv/num_ev` 下均报 `Initial solution is infeasible`；`solver/tests/test_e2_alns_throughput.py` 已相应改为：工具逻辑用 hard-cap 可行 fixture `verify_20251113` 验证，旧 E2-10c smoke 必须诚实返回 `HALT_SMOKE` 而非 `SMOKE_OK`。这不是证明 E2 数学上无可行解，只证明当前 route=vehicle + 原始车数上限 + warm-start 构造存在冲突。下一步必须先做 hard-cap feasibility gate/车辆复用语义审计，再决定是否正式 E2/T3；不能把硬上限恢复直接解释成“混合故事已解决”。
- [M1] 2026-06-26（09r hard-cap feasibility 只读审计，结论停止 E2/T3）：新增 `baselines/e2_alns/hard_cap_feasibility_audit.py`、报告与 `hard_cap_feasibility_audit_data/`，不改 `prices.py`/bundle/`cost.py`/`check.py`/`evaluation.py` 或 TeX。审计 69 个 E2 正式实例，raw Goeke、instance metadata、scenario_manifest、EVRPTW-MF 文本、`load_search_bundle()` 读取结果来源一致性 `69/69`。关键结论很硬：在当前论文/代码 `Q=1600kg` 且 `num_cv/num_ev` 为 hard cap、当前实现“一条 route 占一辆车”的口径下，`69/69` 实例都满足 `total_cap * 1600kg < total_demand`，所以从容量下界就不可行；这不是 warm start 或 ALNS 搜索问题。Goeke `Q=3650kg` 只作反事实时 `68/69` 容量下界可行，唯一仍失败是 `e2-threeshift-200c-02`（24 辆×3650=87600kg < 93401kg），说明三班合并口径还另有边界。verdict=`HARD_CAP_CAPACITY_INFEASIBLE_CURRENT_Q`；E2/T3、混合比例、电池谱和 GLNS/ALNS 对比继续暂停。下一轮只能在三条路里决策：A 恢复/审计 Goeke 容量口径，B 保留 1600kg 但重标车辆数并找来源，C 正式建模多车次/车辆复用；不能继续用当前三件事同时成立。
- [M1] 2026-06-26（Goeke 容量口径恢复）：用户拍板主线回到 Goeke 容量参数，不再把本地生成器 `1600kg` 作为主算例容量。已将 `solver/src/setp_solver/prices.py` 默认 `Q_capacity` 从 `1600.0` 改为 Goeke/Demir Table 4 的 `3650.0kg`，并同步 `docs/paper_submission_final/paper_main.tex` 参数表与算例说明；注释保留历史 `1600kg` 痕迹，说明它不是 Goeke 原值、若作为 UK light-van 场景须另起场景解释。电池 `280kWh`、速度 `90km/h`、碳价、电价、占用费、算法语义和 TeX 模型公式未在本步改变。下一步仍是修正求解器/checker 的实体车辆可多趟语义，避免把车辆数上限误当路线数上限。
- [M1] 2026-06-26（09s Goeke-80 + 实体车辆多趟语义修正完成，E2 语义门槛打开）：按用户要求主线回正，不再继续 280kWh/电池梯度/运营约束摸排。本步把默认物理参数统一为 Goeke 口径：`Q_capacity=3650.0kg`、`B_battery_kwh=80.0kWh`，速度 `v_speed_ms=25.0` 与碳价 `0.05034` 未动；`prices.py` 注释和 `docs/paper_submission_final/paper_main.tex` 参数表同步保留 1600kg/280kWh 的历史诊断痕迹，但 280kWh 不再是当前默认。更关键的是修正代码翻译错误：论文中的 `m^g/m^e` 是实体车辆硬上限，不是 route 条数上限；代码现在用 `CV1#T1/CV1#T2` 这类 trip id 表示同一实体车多趟，`check_solution()` 按 physical vehicle prefix 计数并检查同一实体车车型一致，route/trip id 仍保持唯一以避免充电动作歧义；构造器、repair、ALNS/candidate decode 都会把多条 route 打包到有限实体车上。固定费暂不改，仍保持现有“每趟/每次派遣”口径，后续若要改为每实体车固定费必须另起成本语义审计。新增 `baselines/e2_alns/goeke80_multitrip_rescue_gate.py` 与报告/data：Phase1 69/69 E2 实例 warm start 零违约通过；Phase2 代表集 seed1 小预算 smoke 34/34 行 OK、ALNS wins 2 / LNS wins 0 / ties 15。大白话：容量和车辆上限这两个语义卡点已清掉，可以继续救算法对比；但 smoke 不是正式胜利证据，下一步应做更高预算、多 seed 的 E2/T3 预演，再决定是否进入正式全量对比。
- [M1] 2026-06-26（09t Goeke80 多趟实体车 T3 预演，Stage A 过但 Stage B 因采集成本 HALT）：新增 `baselines/e2_alns/goeke80_multitrip_t3_preflight.py`、报告与 `goeke80_multitrip_t3_preflight_data/`，不改 `prices.py`/`cost.py`/`check.py`/`evaluation.py`、bundle 或算法语义。Phase0 确认当前口径仍为 `Q=3650kg,B=80kWh,v=25m/s,carbon=0.05034`，TeX 与 09s warm start 69/69 对齐；smoke 6/6 OK。Stage A 跑全规模 `-01` 梯度 × seeds1-3 × `alns_e2_throughput/LNS`，138/138 rows、0 采集失败，paired counts = ALNS 7 / LNS 2 / ties 60，LNS 未系统性压制，故有继续价值；但 75-200 winner mean EV route share 仅约 0.057，提示 Goeke80 下“混合车队故事”很弱。Stage B 尝试 69 实例 × seeds1-3 × 2 算法、eval_budget=16000，并把时间帽收敛到 `10-25c=300s,50c=600s,75-200c=900s`；在 `e2-vanilla-150c-01` LNS seed1/2 仍于 900s 内只跑到约 11.2k/16k eval，按规则停止并汇总为 `HALT_COLLECTION_COST`（130/414 rows，不能下正式算法胜负结论）。回归测试 `solver/tests/test_cost.py solver/tests/test_check.py solver/tests/test_search.py solver/tests/test_e2_alns_throughput.py -q` = 76 passed。下一步不能启动正式 T3；应先决策是否降低 Stage B eval budget/改成 wall-clock 公平预演、优化/替换 LNS 收集路径，或接受 Goeke80 只作为可行 baseline 而非强混合故事。
- [M1] 2026-06-27（09v 高碳张力 ALNS vs LNS regime 分离性探针提示词落盘，PROBE/非正式 T3）：已写入 `docs/handoff/codex_prompts/09v_high_tension_alns_vs_lns_regime_probe_20260627.md`，只作为下一步 Codex 冷启动提示词，不代表实验已执行。09u 同墙钟预演事实是：Goeke80/100 下 ALNS 与 LNS 大多打平，23 个 `-01` 配对约为 ALNS 2 胜 / LNS 1 胜 / 20 平，75-200 winner EV route share 约 `0.057`，说明当前 Goeke80/100 的 EV/充电机制活跃度很低。09v 目标是把来源约束电池档 `80/100/150/280kWh` 当机制活跃度轴，观察 ALNS-LNS 配对 gap、胜/平/负、Wilcoxon p、EV route share 与 `charging_action_count` 是否随机制活跃度增强而分离；全程不改 `cost.py`/`check.py`/`evaluation.py`、参数默认值或碳价。预注册判决写死为 `PASS_MECHANISM_SEPARATION` 或 `INCONCLUSIVE / FAIL_NO_SEPARATION`：若高活跃度 regime 仍不能让 vanilla ALNS 相对 LNS 拉开，则停止继续用电池/regime 调 vanilla ALNS，算法创新转向 DR-ALNS；若某档退化为 all-CV 或 all-EV，该档打平不算证伪“真实争夺 regime”的假设。
- [M1] 2026-06-29（09v 高碳张力 ALNS vs LNS regime 探针已跑完，仍是 PROBE / 非正式 T3）：Stage A 在 `baselines/e2_alns/high_tension_separation_probe_stage_a_data/` 完成，`184` rows、`0` 采集失败，`collection_failure_count=0`，`paired_rows=92`；四档 `80/100/150/280kWh` 的 `75-200` 子集都保持 `0/12/0`（ALNS/tie/LNS），mean EV route share 都约 `0.0573`，mean charging actions 都是 `1.00`，Wilcoxon 全是 `all_zero` 且 `p_less=1.0`，没有任何 preregistered Stage A signal 触发 Stage B，`stage_b_recommended=False`、`stage_b_candidate_scenarios=[]`、`pass_candidate_scenarios=[]`。最终 verdict 是 `INCONCLUSIVE_NO_NONDEGENERATE_TENSION`：09v 没找到“非退化且充电/EV 机制真正咬合”的高张力档，因此不能把 vanilla ALNS 调到正式 T3，也不需要再烧 Stage B；本次产物已写到 `baselines/e2_alns/high_tension_separation_probe.md`、`stage_decision.json`、`conclusion.json`、`mechanism_summary.csv`、`wilcoxon_summary.csv`、`artifact_hashes.json`。
- [M1] 2026-06-29（09w battery override 搜索贯通修复完成 + 09w Stage A 结案）：`winner_operators.py`、`metaheuristic_baselines.py`、`wallclock_battery_preflight.py` 已把显式 `prices` 贯通进 Winner kernel / LNS 搜索，回归 `solver/tests/test_cost.py solver/tests/test_check.py solver/tests/test_search.py solver/tests/test_e2_alns_throughput.py solver/tests/test_metaheuristic_baselines.py -q` 为 `84 passed, 1 skipped`；随后重跑 `baselines/e2_alns/high_tension_separation_probe_09w_stage_a_data/`，最终 `184/184` rows、`0` 采集失败、`paired_rows=92`，四档 `80/100/150/280kWh` 的 `75-200` 子集都保持 `0/12/0`、mean EV route share 约 `0.0573433697`、mean charging actions `1.00`，Stage B 仍未触发，final verdict 仍是 `INCONCLUSIVE_NO_NONDEGENERATE_TENSION`。这条把 09v 的“电池档/机制分离”问题正式定为 PROBE/非正式 T3，后续不再把 vanilla ALNS 的电池调参当主线证据。
- [M1] 2026-06-30（09x carbon-aware ALNS 算子 PROBE 已实现并跑完 Stage A，结论 WEAK）：新增隔离变体 `alns_e2_carbon` 与 `alns_e2_carbon_ablation`，只在 Winner kernel registry 中追加 `worst_carbon_removal`、`carbon_related_removal`、`low_carbon_charging_repair`，默认 `alns_e2_throughput` 仍保持原 6 destroy + 3 repair；runner `wallclock_battery_preflight.py` 新增 `--report-kind carbon_aware`、四算法同墙钟对比、`cost_carbon/E_total/E_cv_direct/E_ev_indirect/low_carbon_charging_share` 与碳算子计数字段。Stage A 跑 `gradient01 × wc80 × seed1 × {alns_e2_throughput,LNS,alns_e2_carbon,alns_e2_carbon_ablation}`，`92/92` rows、`0` 采集失败、全 `OK`，报告 `baselines/e2_alns/carbon_aware_operator_probe.md`，数据在 `baselines/e2_alns/carbon_aware_operator_probe_data/`；结论 `WEAK_CARBON_SIGNAL`，Stage B 不触发：carbon vs ablation 为 `0/21/2`（left/tie/right），mean cost gap `+1.8304%`、mean `E_total` delta `+4.1531kg`；carbon vs throughput 只有 `1/22/0`、Wilcoxon `p=0.5`；carbon vs LNS `2/20/1`、`p=0.25`。回归 `solver/tests/test_cost.py solver/tests/test_check.py solver/tests/test_search.py solver/tests/test_e2_alns_throughput.py solver/tests/test_metaheuristic_baselines.py -q` 为 `87 passed, 1 skipped`。本步仍是 **PROBE / 非正式 T3**，不能声称 carbon-aware ALNS 已正式赢；当前证据说明这版手写碳算子弱，算法创新主线仍应优先 DR-ALNS 或另起更强碳算子设计。
- [M1] 2026-06-30（09y EV-heavy 可行性与 regime 决策探针，Stage0/1/2 收口）：新增 `baselines/e2_alns/ev_heavy_regime_decision_probe.py`、轻量测试 `solver/tests/test_ev_heavy_regime_probe.py` 和四份报告/数据，继续冻结 `cost.py`/`check.py`/`evaluation.py` 语义、`prices.py` 默认值、TeX 与碳价；执行起点 `HEAD=402b2511345276b5fb887bb06587772ebeb9702f`，环境 `/opt/anaconda3/bin/python3.13` + numpy `2.3.5`，100-01 winner 锚 `4878.331796187524` 作为测试记录。Stage0 只做构造+评估、不搜索：5 个代表大实例在 Goeke80 下都只能多转约 0-3 条 EV，最终 EV route share `0.0690-0.0952`（远未到 EV-heavy），每例 `accepted_conversions=1`，多数失败来自 `CHARGING_TIME_WINDOW_DETOUR` 或 `STATION_OR_DEPOT_CAPACITY`，verdict=`Z_EV_PHYSICALLY_UNSUPPORTED_80KWH`；Stage1 因无 X 类实例显式写 `SKIPPED_NO_STAGE0_X`。Stage2 用 280kWh 内存 override 做 headroom+构造：physical EV headroom 不受限，`e2-threeshift-150c-01` 构造出非退化混合（EV share `0.4545`、charging actions `10`、cost gap `-6.7564%`、E_total gap `-28.4787%`），但其余代表例仍未进混合带；随后算法大对比触发后两条 worker 跑到约 1h 仍未按 900s 单任务墙钟帽写出 `raw_runs.csv`，按规则中断并记录 `HALT_COLLECTION_COST`，不下 ALNS/LNS/GA/PSO/VNS 胜负结论。综合报告 `baselines/e2_alns/ev_heavy_regime_synthesis.md` 的人话结论：Goeke80 代表大实例转不出 EV-heavy；modern battery 场景有机制头寸但算法对比采集未闭合，若继续 modern regime，下一步先修 Stage2 单任务硬超时和增量落盘，否则算法创新主线转 DR-ALNS。
- [M1] 2026-06-30（09y follow-up 阶段 A 通过 + 阶段 B 采集修复已落代码，正式大对比未启动）：代码 commit `e1a90b08` 为 `ev_heavy_regime_decision_probe.py` 新增 `stageA/stageB` 入口、阶段 B 增量 `raw_runs.csv` 落盘、每任务 checkpoint、外部 hard-timeout 从 checkpoint 回收、100c/150c/200c 墙钟帽 `900/1800/2700s`、EV-maximal warm start 与阶段 B 判决统计；仍不改 `cost.py`/`check.py`/`evaluation.py`/`prices.py` 默认值/TeX，280kWh 只走内存 `dataclasses.replace(DEFAULT_PRICES, B_battery_kwh=280.0, carbon_price=0.05034)`。用已提交代码重跑阶段 A，报告 `baselines/e2_alns/threeshift_280_stageA_generalization.md`，数据 `baselines/e2_alns/threeshift_280_stageA_generalization_data/`，run HEAD=`e1a90b08c1dcd7877943245ad444e57cd8855872`，Phase0 环境 `/opt/anaconda3/bin/python3.13` + numpy `2.3.5` OK；9 个三班 100/150/200 稳定性实例中 8 个过门槛（EV≥30%、0 违约、成本/总碳均不劣于基线），只有 `e2-threeshift-100c-01` EV share `0.2667` 未达 30%，verdict=`THREESHIFT_MIXED_GENERALIZES`。采集 smoke（单实例 `e2-threeshift-100c-02`、GA、100 eval、30s）验证 subprocess/checkpoint/raw row 管线 OK，但不作为实验数据保留。回归 `solver/tests/test_cost.py solver/tests/test_check.py solver/tests/test_search.py solver/tests/test_e2_alns_throughput.py solver/tests/test_metaheuristic_baselines.py solver/tests/test_ev_heavy_regime_probe.py -q` 为 `95 passed, 1 skipped`。下一步才是启动阶段 B 正式对比：阶段 A 通过的 100/200c + 150c 三班实例 × seeds1-3 × `alns_e2_carbon/alns_e2_carbon_ablation/LNS/GA/PSO/VNS`；若 0 采集失败再判 `CARBON_REGIME_ALNS_WINS` 或 `MECHANISM_BUT_TIE`。
- [M1] 2026-07-01（09y follow-up 阶段 B 正式大对比跑满，结论 MECHANISM_BUT_TIE）：基于上一条 `0da0ca3f` 阶段 A 通过集，启动 280kWh 诊断场景三班实例 `100c-02/03,150c-01/02/03,200c-01/02/03` × seeds1-3 × `alns_e2_carbon/alns_e2_carbon_ablation/LNS/GA/PSO/VNS`，全程仍冻结 `cost.py`/`check.py`/`evaluation.py`/`prices.py` 默认值和 TeX，280kWh 仅用内存 `dataclasses.replace(DEFAULT_PRICES, B_battery_kwh=280.0, carbon_price=0.05034)`，碳价 `0.05034`。Stage B 报告 `baselines/e2_alns/threeshift_280_stageB_algorithm_comparison.md`，数据 `baselines/e2_alns/threeshift_280_stageB_algorithm_comparison_data/`；raw/task row 对齐 `144/144`，`collection_failure_count=0`，所有 worker checkpoint 可读，100/150/200 墙钟帽 `900/1800/2700s` 下均从 checkpoint 闭合并记录实际 eval。结果很硬：ALNS vs GA/LNS/PSO/VNS 均为 `0/24/0`（胜/平/负）、mean gap `0.0000%`、Wilcoxon/all-zero `p_less=1.0`；carbon vs ablation 也是全 24 对齐打平，`carbon_ablation_significant=False`、`p_less=1.0`；mean ALNS EV route share `0.45529131628269565`，EV 保住但搜索没有带来额外算法收益。判决 `MECHANISM_BUT_TIE`：模型/构造机制证明 280kWh 三班混合场景可存在并泛化，但当前碳感知 ALNS 和碳消融没有可测差异，算法贡献不能写成成立；若继续算法主线，应转 DR-ALNS 或更强搜索设计，不能把本结果包装成 `CARBON_REGIME_ALNS_WINS`。
- [M1] 2026-07-01（动态需求整合规划门槛明确写入）：用户再次确认动态需求不能作为割裂的附加实验；后续 E7/T9 必须证明动态事件下 **车场协同、收益公平、时变电网碳强度** 真实参与路径决策。已将 `docs/handoff/memory/dynamic-demand-integration.md` 从“先记着”升级为 `ACTIVE / 必须进入后续 E7-T9 规划`，并同步更新 `docs/handoff/codex_prompts/MASTER_codex_takeover_plan.md`。新的验收门槛为：①动态×协同：新增/变更需求被跨车场或共享车辆池吸收的比例/案例；②动态×公平：逐阶段车场收益、公平比或公平约束状态，若当前 `min_fairness_ratio=off` 必须明示未完成；③动态×时变碳：动态重规划对 EV/CV 分工、充电动作、充电时段平均碳强度或新增碳排的影响。若三项不能成立，动态需求只能写成滚动接口与状态继承可行性，不能写成四要素耦合贡献。
- [M1] 2026-07-01（规划地图落盘）：新增 `docs/handoff/project_planning_map_20260701.md`，把当前 ReSETP 主线整理为可执行地图：总目标=投稿论文的统一证据链；算法贡献、场景参数、动态需求整合、正式重跑/表图更新分为四条主线；明确 09x=`WEAK_CARBON_SIGNAL`、09y=`MECHANISM_BUT_TIE`、DR/x86 未 paper-ready、动态 E7/T9 当前只能算接口/可行性证据。后续不应直接全量重跑，而应先关闭算法路线、动态三交互证据和场景参数 gate。
- [M1] 2026-07-02（全项目 PRD 与施工图 v2 落盘）：新增 `docs/handoff/project_prd_execution_map_v2_20260702.md` 与记忆索引 `docs/handoff/memory/project-prd-execution-v2.md`，把上一版 E2 PRD 升级为全项目施工图：宪政纪律、宏观阶段、E2-G0~G5、E1-E7 逐实验 PDCA、DR-ALNS 接入门槛、风险登记册、Codex 任务队列和失败史记录制度。关键修正：Claude worktree 旧 PRD 中 “GA 仍 9229/16000 HALT” 已被当前主工作区补跑修正为 GA 两 seed `16000/16000 OK`，但 GA/LNS/PSO/VNS 仍在 `ev_heavy_findability_gate_long_same_instance_v2_data/raw_runs.csv` 中跨算法跨 seed 同值 `5174.345121253789`、EV share `0.6818181818181818`，故下一步最高优先级仍是 E2-G0 同值平台审计；正式 E2 前还必须完成 ALNS 剥离 N-Wouda、场景口径/280kWh 四同步、双账本预算协议与 baseline health gate。圆桌裁决已写入 v2：DR-ALNS 只作为并行上限线和策略注入接口预留，未过同环境同预算健康门槛前不得救 E2 叙事；AppleDouble `._*`/`._artifact_hashes.json` 若进入 hash 清单，必须标 `HASH_CONTAMINATED_APPLEDOUBLE` 并清理重算，不得作为正式证据。下一步 Codex 提示词已落盘：`docs/handoff/codex_prompts/20260702_c1_e2_g0_plateau_5174_audit.md`。
- [M1] 2026-07-02（Codex/Claude 强制读取入口落盘）：新增 `docs/handoff/READ_ME_FIRST_FOR_AGENTS.md`，并挂入 `AGENTS.md`、`CLAUDE.md`、`docs/handoff/codex_prompts/MASTER_codex_takeover_plan.md`、`docs/handoff/codex_prompts/README.md`、PRD v2 和 `docs/handoff/memory/MEMORY.md`。以后 Codex/Claude/Claude Code 接手非平凡任务时必须先读该入口及其清单：`HANDOFF.md`、PRD v2、规划地图、memory index、`project-prd-execution-v2.md`、MASTER；E2/DR/动态/图表/参数任务还要读对应追加材料。实验或论文结论相关任务必须留下 `metadata.json/raw_runs.csv/decision.json/artifact_hashes.json/report.md`，重大变化必须同步 HANDOFF 与 memory。
- [M1] 2026-07-01（baseline 活性修复后的 M1 算法线下一步修正）：当前 M1 线最新事实不是“先做泛泛 Phase0”，也不接管 x86 DR 线；上一个已执行任务修复了 `solver/src/setp_solver/search/metaheuristic_baselines.py` 的 baseline 活性问题，只动该文件：给 GA/PSO/LNS/VNS 补 route-level 车型翻转、充电修复、多班次车辆编号规范化，并修复 LNS scan 初始解只有更好时才接管 current。单实例门控数据在 `baselines/e2_alns/ev_heavy_findability_gate_liveness_allalg_data/`，metadata 标为 `PROBE / 非正式 T3`、`e2-threeshift-150c-01`、`B=280kWh`、HEAD=`f8699cf65d5b1062f7e4550be7ad2623c08c8231`；`findability_decision.json` 为 `PASS_ALL_FIND_OR_INSTANCE_EASY`。安全结论：GA/LNS/PSO/VNS 已从初始 `5803.55` 改进到 `5174.35`、EV share 到 `68.2%`，说明 baseline 能正常进入 EV-heavy 空间；ALNS 变体仍到约 `3701/3639/3364`，最好约 `3364.77`，但这只是单实例短门控，不能写成全实例正式碾压，也不能写 carbon-aware 算子胜利（ablation 略好）。下一步应做同实例更长复核，判断非 ALNS 的 `5174` 平台会不会继续下降；若掉不动且 ALNS 稳定在 `33xx-37xx`，再扩大到少量三班稳定性实例。
- [M1] 2026-07-02（Claude 核验 PRD v2 + 施工图，3 处只读数据核查 + 8 处文档加固，未跑 solver 未改代码）：Claude worktree `charming-sammet-4d06fe` 复核 Codex 落盘的 `docs/handoff/project_prd_execution_map_v2_20260702.md` / `READ_ME_FIRST_FOR_AGENTS.md` / `codex_prompts/20260702_c1_e2_g0_plateau_5174_audit.md`，结论=结构可靠、忠实实现圆桌裁决，但补三个被低估的实锤事实并加固文档：①**GA under-eval 确已修**（主盘 `long_same_instance_v2_data` GA=`16000/16000 OK`，Fable5 v1 的 `9229/16000 HALT` 是过时 worktree 读数，Codex 修正正确）；②**同值平台是种子不变的**——GA/LNS/PSO/VNS 跨 seed1/seed2 `best_cost` 集合都恰为 `{5174.345121253789}` 零方差，群体法跨种子逐位相同近乎不可能，`ARTIFICIAL_HOMOGENIZATION` 嫌疑加重（已写进 C1 提示词做硬判据）；③**AppleDouble 污染已实锤非隐患**——`._` 已进入含 C1 审计目标在内的多个 `artifact_hashes.json`，repo 有 14394 个 `._*`，风险登记从"中"升"高"；④**DR Track17-25 报告不在 M1 主盘**（在 x86 `dr-x86`），DR-G0~G4 标注为 x86-only，M1 侧只做接口预留。另修：G1 锚集扩为多路径（Goeke80 100-01 + 280/多趟/硬上限 threeshift-150c-01）、任务代号 D1/D2/R1→DR-C1/DR-C2/E7-C1 防与决策标签混淆、补最小充分实验集纪律与审稿弹药索引（战略主编票，Codex 圆桌缺该席位）、注明 C2/C3 可并行。Stage B 72/144 行为墙钟闭合非满 eval，双账本口径成立。裁决顺序不变：C0 卫生→C1 平台审计→C2 场景合规核对/C3 独立化（并行）→C4 基线协议→C5 多实例→C6 正式 T3。
- [M1] 2026-07-02（user 二次纠偏：撤销"场景重开/上限故事/小算例快跑"三个重提，v2 补已关闭路线台账）：user 指出 Claude 当日两轮提案均在重提已试尽路线，经回读 MASTER 历史诊断链与 09 系列 verdict 确认属实：①"正式化 280kWh 主场景"违背 user 06-26 拍板（Goeke80 默认、280 只作诊断），圆桌该裁决作废；②"车队上限强制混合故事"已试尽——09n 直接套 cap 为 `HALT_COLLECTION_COST`（大面积 INIT_INFEASIBLE + ev_cap_only 假平衡），正确的实体车硬上限+多趟语义已由 09s 落地，且 09s/09t/09u 证明 Goeke80+硬上限下 winner EV share 仍仅 ~0.057，上限本身造不出混合；其中"280+三班+上限出混合"的小验证也已跑过且通过=09y Stage A `THREESHIFT_MIXED_GENERALIZES`（8/9 过 EV≥30%），无需重跑；③"小算例/低预算快跑先分胜负"是系统性失败模式——09s smoke 15/17 平、09t 60/69 平、09u 20/23 平、09v/09w 全平、09y Stage B 0/24/0 全平，低预算配对判别力不足，平局是常态输出；唯一出现过大差距的是 baseline 活性修复后同实例长复核（5174 vs 3364），其真伪=C1 审计对象。据此修订 `project_prd_execution_map_v2_20260702.md`：Phase 2 新增**已关闭路线台账（7 条）**，C2 从"场景裁决包（待 user 拍板）"改为"场景合规包（06-26 拍板的落实，核对正式跑零 override + 三处口径一致，无需 user 重新决策）"，任务队列与依赖段同步改写。同步更新 `docs/handoff/memory/project-prd-execution-v2.md`。当前唯一待发任务不变：C1 同值平台审计（`20260702_c1_e2_g0_plateau_5174_audit.md`），其结论直接裁决算法主线出口（平台为真→ALNS 领先成立；为假→按 MASTER 预案诚实降级或转 DR-ALNS）。未跑 solver、未改代码。
- [M1] 2026-07-02（Codex 复核 pasted Claude 二次纠偏，清理残留误导措辞）：按强制入口读 HANDOFF/PRD/memory/MASTER 后核验，主盘确已落入二次纠偏：已关闭路线台账存在，C2 已是场景合规包，当前唯一待发任务仍是 C1 同值平台审计。发现并修正三处残留旧口径：E2-G2 Act 不再写"若用户不拍板"，改为 `SCENARIO_COMPLIANCE_BLOCKED`（正式 E2 仍用 280 override 或三处口径不一致才停）；Phase6 附录措辞从"为何选择 modern battery"改为"Goeke80 默认 / 280kWh 诊断场景 / baseline health gate"；风险登记从"280kWh 参数故事被审稿质疑"改为"场景口径漂移 / override 污染正式 E2"。未跑 solver，未改代码。
- [M1] 2026-07-02（Codex 执行 C1/E2-G0 5174 同值平台审计，结论未过门）：新增只读审计脚本 `baselines/e2_alns/plateau_5174_audit.py`、报告 `baselines/e2_alns/e2_g0_same_value_platform_audit_20260702.md`、数据目录 `baselines/e2_alns/e2_g0_same_value_platform_audit_data/`（含干净 `artifact_hashes.json`，排除 `._*`）。输入为 `ev_heavy_findability_gate_long_same_instance_v2_data`，B=280kWh/碳价0.05034 诊断场景；8 条 GA/LNS/PSO/VNS baseline 行全部 `16000/16000 OK`，checkpoint 用当前 `evaluate()`/`check_solution()` 回放均为 `5174.345121253789`、零违约、signature 匹配，所以不是旧 GA under-eval 或 CSV/checkpoint 损坏。但 C1 关键 verdict=`ARTIFICIAL_HOMOGENIZATION`：四个 baseline 跨 seed 的 best cost/signature/EV share 完全同值，所有 best 更新均在 eval≤42 由共享车型翻转/初始化通道完成，之后至少 15958 eval 无 native best 更新。附加标签：`HASH_CONTAMINATED_APPLEDOUBLE`（输入目录 51 个 `._*` 且旧 hash 清单含 `._raw_runs.csv` 等污染项）、`EV_MAXIMAL_REFERENCE_NOT_BOUND`（`closed_gap_fraction>1` 是因为 EV-maximal reference 不是下界）。结论：当前单实例 `5174 vs 3569-3653` 不能写成健康 baseline 下 30% 正式领先；E2-G0 未过门，G4/G5 正式扩跑继续冻结。下一步不是扩跑，而是 C1-R1/C1-R2：把共享车型翻转从 baseline 算法成绩中剥离/单独记账，建立 operator provenance gate 后重审 G0。
- [M1] 2026-07-02（Claude 只读根因分析：5174 平台与 21 天 E2 僵局的代码级病灶定位，INFERENCE 待 C1-R2 探针证实）：在 C1 `ARTIFICIAL_HOMOGENIZATION` 基础上读 `metaheuristic_baselines.py`/`repair_scoring.py`/`winner_operators.py`，定位三个机制回答"原生搜索为何 15958 eval 零改进"：①**修复评分降级**（FACT 代码层）：baseline 的 `_alns_neighbor()`（`metaheuristic_baselines.py:1397-1412`）强制 `SETP_ALNS_CRUSH_TRUE_REPAIR=0`，使 `repair_scoring.py:31-32` 把插入评分退化为**纯距离增量**；而 E2 ALNS 主变体用 `TRUE_REPAIR=1`（`winner_operators.py:221` 成本感知 regret 修复）。80kWh CV 主导时距离≈成本代理（winner kernel 曾以 =0 健康碾 SA），但 280 EV-heavy 平台上距离评分看不见固定费/能耗/碳/充电结构 → LNS/GA修复/VNS local 的原生通道系统性失效（INFERENCE，需 A/B 证实）。②**解码器天花板**（FACT 代码层）：GA 交叉/PSO 速度/VNS 构造全汇入 `_order_to_solution`→`_decode_order_like_random_key`（`805-835`）：只允许贪心"尾插"构路（`_append_customer_to_cached_plan` 880-904，全局最短距离选择使解码对排列变化近乎不敏感）、路线可行性只按 CV 时窗测（907-923）、EV 仅是"CV 形路线整条重标注+充电修复"（avg key≥0.82）、**整解 check 失败时静默回退全油解**（833-834）摧毁搜索梯度；该表示根本无法表达 ALNS 在 3569 处用的多趟/充电结构。③**同质化闭包**（FACT，C1+代码）：车型翻转通道迭代到"可翻尽翻"的闭包不动点与翻转顺序无关 → 跨 seed/跨算法逐位同值 5174。**对 21 天的解释（INFERENCE）**：8 个基线共享一套脚手架（warm start+解码器+翻转通道），"算法身份"只是排列扰动方式不同；Goeke80 下翻转通道死（EV 物理不可行）→ 全员钉在共享构造附近 → 09s/t/u/v/w/y 的海量逐位平局被误读为"场景无张力"，引发 09k-09q 电池/约束绕路；基线目录里早已设计的"真在搜索"硬门禁（跨 seed std>0+收敛曲线）从未被当验收执行，07-01 liveness 修复反而注入共享翻转通道制造了本次假象。下一步=C1-R2 最小探针（LNS TRUE_REPAIR 0/1 A/B + 解码回退率/候选成本分布计量，单实例小预算 PROBE），证实后才做 C1-R1 修复设计。未跑 solver、未改代码。
- [M1] 2026-07-02（Codex 执行 C1-R2 baseline 原生通道死因定位探针，PROBE / 非正式 T3）：新增 runner `baselines/e2_alns/native_channel_autopsy.py`（实现提交 `b30c9f2eb9972b13e86a31a2d26000f26e7f23a8`）和测试 `solver/tests/test_native_channel_autopsy.py`，不改 `cost.py`/`check.py`/`evaluation.py`/`prices.py` 默认值/TeX；环境 `/opt/anaconda3/bin/python3.13` + numpy `2.3.5` + `PYTHONHASHSEED=0`，280kWh 只用内存 `dataclasses.replace(DEFAULT_PRICES, B_battery_kwh=280.0, carbon_price=0.05034)`。单实例 `e2-threeshift-150c-01`、seed1、shared warm start、4 条计划 run 均闭合：candidate rows `8004`、decode rows `5214`、flip closure rows `3`、`collection_failure_count=0`。产物在 `baselines/e2_alns/native_channel_autopsy_data/`，报告 `baselines/e2_alns/native_channel_autopsy.md` 与目录内 `report.md`，hash 清单排除 `._*`/`__pycache__`/`.pytest_cache` 且已清理 AppleDouble。判决 `PARTIAL_OR_WEAK_SUPPORT`：H1=`H1_WEAK_OR_UNRESOLVED`，因为 LNS 现状 TRUE_REPAIR=0 与探针 TRUE_REPAIR=1 两组 native best updates 都是 `0`，best cost 都停在 `5174.345121253789`，所以不能说“修复评分降级单独杀死原生改进”；H2=`H2_CONFIRMED`，但原因不是高 fallback（GA/PSO fallback rate 都是 `0.0`），而是 GA/PSO 排列解码候选低于 `5174.345121253789` 的比例都为 `0.0`。翻转闭包 seed1/seed2/确定性枚举都回到 `5174.345121253789`、accepted flips=`14`、EV share=`0.6818`。总之，本探针未达到 `NATIVE_CHANNEL_DEAD_CONFIRMED`，也未达到 `HYPOTHESES_REFUTED`；后续 C1-R1 不能假设打开 TRUE_REPAIR alone 就能修复 baseline，必须先做 operator provenance / 解码表达能力层面的设计。回归测试 `PYTHONPATH=solver/src:models/src:. /opt/anaconda3/bin/python3.13 -m pytest solver/tests/test_native_channel_autopsy.py solver/tests/test_ev_heavy_findability_gate.py solver/tests/test_metaheuristic_baselines.py -q` 为 `16 passed, 1 skipped`。
- [M1] 2026-07-02（Claude 解读 C1-R2 探针数据：定位到可数学证明的解码器构造级 bug + 桥接 100% 失效,根因链闭合）：只读分析 `native_channel_autopsy_data/raw_runs.csv`(8004 行) 得到三个新事实。①**解码器是"单客户路线生成器",且可从代码证明**（FACT）：`metaheuristic_baselines.py` `_append_customer_to_cached_plan` 选择"追加到既有路线"还是"新开单客户路线"时,比较的是**候选路线的绝对总距离**——由三角不等式,`[d,c1,c2,d] ≥ [d,c2,d]` 恒成立,所以"追加"永远不可能严格优于"新开",解码器在任何实例上都只会产出一客一车的解。探针实证 100% 吻合：LNS 2686 个原生候选与 PSO 1399 个候选的 route_count **全部=150**（150 客户）、cost≈21000（平台 5174 的 4 倍,平台解 22 条路线）；GA 中 route_count=22 的候选全部来自解空间的 `_route_crossover`,非解码路径。这意味着 GA/PSO/VNS/ACO/GWO/IWD 的排列空间搜索与 LNS 兜底路径**从一开始就被锁死在 21000 量级的单客户路线盆地里**,永远差于 5174 → 全部被拒 → best 冻结。②**destroy/repair 桥接在平台解上 ~100% 失效**（FACT 失效率,INFERENCE 微观死因）：若桥有任何一次产出可用邻居,LNS 原生候选中必然出现 22 路线级的行,但 0 例;TRUE_REPAIR=0/1 两组候选 min/mean 逐位相同（20993.53/20999.33）,证明修复评分 flag 在死点下游、根本没被走到——死点在 `candidates.py` `_apply_strong_alns_destroy_repair` 的 `repair_removed_customers(..., _ThinPolicy)` 返回 None 或修复解带 violation（具体类型未知,需 C1-R3 微探针把 `_OperatorOutcome.detail`/violation 类型做成直方图）。③C1-R2 的 `PARTIAL_OR_WEAK_SUPPORT` 由此完全解释：H1 不可测（flag 在死点下游）,H2 的真机制不是回退率而是解码器构造级 bug。**修复顺序建议（C1-R1 施工令,待 user 认可）**：R1a 修解码器贪心比较（绝对距离→边际增量,或改最优位置插入）,只动 `metaheuristic_baselines.py`+测试,锚=修后解码候选 route_count 应≈20-40 且 cost 显著低于 21000;R1b C1-R3 桥接死因直方图（200 次桥调用,1 小时内）;R1c 翻转通道共同预处理化+双通道记账;R1d liveness 硬门禁(native updates>0+跨seed std>0+route_count 多样性)做成每次跑的自动 verdict 字段。G4/G5 继续冻结。未跑 solver、未改代码。
- [M1] 2026-07-02（Codex 执行 C1-R1a/R3 解码器边际增量修复 + destroy/repair 桥死因直方图，PROBE / 非正式 T3）：用户/Codex 拍板边界已写入产物：**不改建模，只修 baseline decoder 实现偏差；固定成本仍按现有 `£/班次 / 每趟派遣` 口径使用，不改成每实体车固定费，不引入 fleet-size-and-mix**。代码提交 `ee1f7bfb1ac8466322f671d453e20359335e2e63` 只改 `solver/src/setp_solver/search/metaheuristic_baselines.py` 的 `_append_customer_to_cached_plan` 比较口径（追加旧路线=边际公里成本；新开路线=单客路线公里成本+`vehicle_fixed_cost`，从 prices 读取、不硬编码），新增 `solver/tests/test_decoder_marginal_repair.py` 与 runner `baselines/e2_alns/decoder_fix_validation.py`；未改 `cost.py`/`check.py`/`evaluation.py`/`prices.py` 默认值/TeX/ALNS 主路径。回归 `PYTHONPATH=solver/src:models/src:. PYTHONHASHSEED=0 /opt/anaconda3/bin/python3.13 -m pytest solver/tests/test_native_channel_autopsy.py solver/tests/test_ev_heavy_findability_gate.py solver/tests/test_metaheuristic_baselines.py solver/tests/test_decoder_marginal_repair.py -q` 为 `18 passed, 1 skipped`。在同 commit 上跑 `e2-threeshift-150c-01`、seed1、280kWh 内存 override、GA/PSO 各 2000 eval + 200 次桥尸检，产物 `baselines/e2_alns/decoder_fix_validation.md` 与 `baselines/e2_alns/decoder_fix_validation_data/`，metadata HEAD=`ee1f7bfb1ac8466322f671d453e20359335e2e63`、环境 `/opt/anaconda3/bin/python3.13` + numpy `2.3.5`、`collection_failure_count=0`、hash 清单排除 `._*`/`__pycache__`/`.pytest_cache` 且已清理 AppleDouble。判决 `DECODER_FIXED_AND_BRIDGE_CAUSE_LOCATED`：修复前 C1-R2 GA/PSO 解码 route_count 恒 `150`、cost≈`21000`；修复后 decode rows `2496`，route_count `21/25/33`（min/p50/max），unique route counts=`13`，decoded cost min=`5158.94347036807`、p50=`7296.815044758418`，`decoded_cost < 5174` 为 `1/2496`。桥尸检 200/200 均为 `REPAIR_NONE`，15 个 destroy×repair 组合 dominant bucket 全为 `REPAIR_NONE`、feasible/changed 均为 0。结论只说明 baseline health probe：解码器已能产多客户路线候选；强 ALNS bridge 死因定位为 `repair_removed_customers(..., _ThinPolicy)` 直接返回 None；桥修复和后续 liveness/operator provenance gate 需另行拍板，G4/G5 仍冻结，不得写算法胜负。
- [M1] 2026-07-02（Codex 执行 C3 / E2-G1 独立 ALNS 运行时剥离，非正式 parity 迁移）：按用户拍板，本步只做“等价剥离”，不修 baseline bridge、不调参、不写算法胜负。代码提交 `b9663c6ab9d457dae33d60b63402f84a0554468d` 新增项目内最小后端 `solver/src/setp_solver/search/resetp_alns/`（保留 N-Wouda/alns 7.0.0 MIT 来源说明，仅迁 `Outcome`、`HillClimbing`、`RecordToRecordTravel`、`SimulatedAnnealing`、`AlphaUCB`、`update()`），`alns_wouda.py` 与 `winner_operators.py` 主路径不再 `_ensure_local_alns_on_path()`、不再 `from alns/import alns`、不再依赖 `Reference Algorithm/ALNS-7.0.0@N-Wouda`；`run_alns_wouda` 名称暂保留为兼容入口，报告层称 independent ReSETP ALNS backend。`Reference Algorithm/ALNS-7.0.0@N-Wouda` 未移动、未删除、未改；`scout.py` 只保留本地 matplotlib stub 供参考算法侦察板导入，不接回主 ALNS。新增测试 `solver/tests/test_resetp_alns_independence.py`，回归 `PYTHONPATH=solver/src:models/src:. PYTHONHASHSEED=0 /opt/anaconda3/bin/python3.13 -m pytest solver/tests/test_resetp_alns_independence.py solver/tests/test_e2_alns_throughput.py solver/tests/test_metaheuristic_baselines.py -q` = `17 passed, 1 skipped`；主路径 grep gate 对 `from alns|import alns|ALNS-7.0.0@N-Wouda|_ensure_local_alns_on_path` 零命中，protected-file check 对 `cost.py/check.py/evaluation.py/prices.py/paper_main.tex/Reference Algorithm/ALNS-7.0.0@N-Wouda` 为空。产物在 `baselines/e2_alns/alns_independence_migration_data/`，runner `baselines/e2_alns/alns_independence_migration.py`，verdict=`ALNS_INDEPENDENCE_CONFIRMED`：pre commit `b6cf9b902f85d3f614704479161b25a358a70a5c` vs post commit `b9663c6ab9d457dae33d60b63402f84a0554468d` 两个锚点 primary parity 全通过——Goeke80 `100-01-24h` seed2 cost `2677.7953638343815`、solution hash `88dcd835d3b18cf0d22f24303859a64cbc8a50b7c963c50fbc535a3d7d97a8e5` 一致（旧文档期待值 `4779.053444002934` 已在剥离前 current HEAD 下陈旧，不作迁移漂移）；280 诊断锚 `e2-threeshift-150c-01` seed1、`B=280.0/carbon=0.05034` 内存 override、eval 2000 cost `3561.080964207054`、solution hash `daa1662ed1c1df3886915802ba4c1ab722aa1eaf612b845798389032bfd56afc` 一致。raw history hash 与 timing ledger hash 的差异仅来自 wall-clock `time_seconds`/timing 统计，稳定 operator counts 去 timing 后一致。产物 hash 已清理 `._*`、排除 `__pycache__`/`.pytest_cache`。后续 C1 bridge/liveness/E2 调试统一在 independent backend 上继续；若 parity 将来漂移，冻结后续 E2 算法调试并按 `HALT_ALNS_PARITY_DRIFT` 回查。
- [M1] 2026-07-02（Claude 把关 C1-R1a/R3 与 C3 两笔完成件 + 锚点口径判读 + 下一刀施工令）：①C1-R1a/R3 判 `DECODER_FIXED_AND_BRIDGE_CAUSE_LOCATED` 成立：解码候选 route_count 从恒 150 → 21/25/33（min/p50/max），排列通道首次活了；桥尸检 200/200 `REPAIR_NONE` 与 Claude 读码一致——`feasible_repair.repair_removed_customers` 只有两个 None 出口：(a) 插入循环某轮无任何可行插入（`not scored`），(b) 插入全部完成但**整解终检失败且全油回退也失败**。Claude 判读 (b) 嫌疑更大（平台解 EV1-EV10 已顶满 m^e 上限→新开 EV 路线必超；单路线充电修复不看全局车场桩占用→易撞 STATION_OR_DEPOT_CAPACITY；22 趟全转 CV 后按班次装不进 m^g 物理车）。**关键约束**：`feasible_repair.py` 是 `alns_wouda.py`/`carbon_operators.py` 共享的 ALNS 主路径模块,而 ALNS 用同一函数能成功修复——死因在桥的调用方式/policy/部分解构造差异,**修复必须做在 `candidates.py` 桥侧,禁改共享模块语义**,否则独立性 parity 锚会漂。②C3 `ALNS_INDEPENDENCE_CONFIRMED` 成立（迁移前后同 HEAD 逐位一致）。**锚点口径判读**：pre-anchor `2677.7953638343815` ≠ 历史 `4878.331796187524` 不是迁移漂移——4878 最后一次精确复现是 06-22（commit `df608660`, Q=1600kg 时代）,06-26 user 拍板回正 Goeke `Q=3650kg` 后 legacy 锚从未重跑,载重 2.3 倍→路线数大减→成本降 45% 量级自洽（INFERENCE 高置信）。收尾要求：下一任务顺带用 `Q=1600` 内存 override 复现 4878 一次,把"陈旧锚"从断言变成已解释事实（审稿弹药:锚随参数时代演化有据可查）；今后所有锚引用必须带参数时代标签。③下一刀=C1-R1b 桥修复（None 出口直方图→桥侧修复→复跑 autopsy B1 验 LNS 原生更新>0）,之后 R1c 翻转分账+R1d liveness 硬门禁,再 G0 重审。G4/G5 继续冻结。未跑 solver、未改代码。
- [M1] 2026-07-02（Codex 执行 C1-R1b destroy/repair 桥侧修复，baseline health整改 / 非正式 T3）：代码提交 `dda5e6d1d008ec4ccd8301db3e21ad88ae76fbbf`，只改 `solver/src/setp_solver/search/candidates.py` 的 `_apply_strong_alns_destroy_repair` 桥侧调用约定：把无限 `_ThinPolicy(max_cv=max_ev=1e9)` 改为实例真实 `num_cv/num_ev` policy；未改 `feasible_repair.py`、`resetp_alns/`、`alns_wouda.py`、`winner_operators.py`、`carbon_operators.py`、`cost.py`、`check.py`、`evaluation.py`、`prices.py` 默认值或 TeX。新增 runner `baselines/e2_alns/bridge_fix_validation.py`、测试 `solver/tests/test_bridge_side_repair.py`，产物 `baselines/e2_alns/bridge_fix_validation_data/`（metadata HEAD=`dda5e6d1d008ec4ccd8301db3e21ad88ae76fbbf`，环境 `/opt/anaconda3/bin/python3.13` + numpy `2.3.5` + `PYTHONHASHSEED=0`，280kWh 仅内存 override）。判决 `BRIDGE_FIXED`：Phase A 旧桥无限 policy 为 `FINAL_CHECK_FAILED` 200/200；同批 destroy/repair 换 ALNS 等价真实 fleet caps 后 `REPAIRED_FEASIBLE` 57/200、`FALLBACK_FEASIBLE` 3/200、`INSERT_LOOP_EXHAUSTED` 140/200。TDD 选 trial4 `shaw_related_removal+greedy_insert_repair`，修复前 `produced=True/feasible=False`，修复后 feasible changed 且零违约。D1 LNS 2000 eval 闭合，原生可行 20-40 路线候选 `1314` 个、route_count 21-33、native best updates=`19`、best cost=`3820.476792624678`；这只说明 baseline 原生通道活了，不得写算法胜负。D2 独立 ALNS 锚零漂：Goeke80 当前 Q3650 seed2 `2677.7953638343815`、280 三班 seed1 `3561.080964207054` 均逐位一致。D4 Q1600 legacy 复核为 `5480.399324501256`，未复现旧 `4878.331796187524`，标 `LEGACY_ANCHOR_UNEXPLAINED` 且不阻塞桥修复。回归 `31 passed, 1 skipped`；protected-file check 和 `git diff --check` 均干净；artifact hash 排除 `._*`、`__pycache__`、`.pytest_cache` 并已清理本目录 AppleDouble。
- [M1] 2026-07-02（Claude 把关 C1-R1b `BRIDGE_FIXED` + 剩余问题一次性演绎 + 签发 G0 收官大任务）：①R1b 通过：桥死因=桥侧用无限车队 `_ThinPolicy` 调 repair→候选终检必炸,与 Claude 判读的出口 (b) 一致；修复只动 `candidates.py` 桥侧传入实例真实 `num_cv/num_ev`,共享模块未动,独立性锚零漂（2677.7953638343815 / 3561.080964207054）。LNS 原生通道复活:2000 eval 产 1314 个 20-40 路线可行候选、19 次 native best 更新、best `3820.476792624678`（只证 baseline 活性,非胜负）。②D4 `LEGACY_ANCHOR_UNEXPLAINED` 的 Claude 判读：单换 Q=1600 不复现 4878 是**预期内的**——4878 属于 06-22 `df608660` 时代,此后代码语义至少还叠加了 09s 多趟归一化+车队硬上限、站容量检查等演化,参数快照≠代码快照;正确的收尾是**代码考古一次**（worktree checkout `df608660` 原样跑锚,预期 4878 复现→锚谱系"参数+代码时代"双标签成立）,而不是在当前 HEAD 上凑参数。③剩余问题清单（演绎自代码+数据,一次收齐）：R1c 翻转通道仍嵌在各基线"算法成绩"里（GA init/PSO init+30%/LNS 35%/VNS 50%）→改共同预处理（所有算法含 ALNS 从同一翻转闭包平台起步）+通道溯源记账；R1d liveness/provenance 硬门禁做成 runner 自动 verdict 字段；`_baseline_fast_repair_flags` 强制 TRUE_REPAIR=0 现在可测了（桥活后评分才生效）→A/B 后基线默认对齐成本感知评分（同尺同秤）；C0 全仓 AppleDouble 清理+涉证目录 hash 重生;C2 场景合规包（零 override+三源一致,纯审计）；最后 **G0 重审**（同实例 280/16000 eval/seeds1-3/GA-LNS-PSO-VNS+ALNS,双账本,复跑 plateau audit,预注册 G0_PASS_BASELINES_HEALTHY / G0_RESIDUAL_HOMOGENIZATION）。G0 过门前 G4/G5 仍冻结;G3 基线扩容（ACO/GA-VNS/GWO/IWD/SA）依赖 G0 结论,不并入本任务。提示词已在对话中签发给 Codex。未跑 solver、未改代码。
- 2026-07-03 Claude 应 user 质疑对论文实验设计/表图做全面只读审计（详细报告在当日对话）。裁决=实验蓝图(9表6图)框架合格，但执行与现状有六类问题：(a) **文-表嵌合体**：paper_main.tex 06-26 已改 Q=3650/B=80kWh/车队硬上限(eq:fleet_size_cap)，而 T1-T9/F1-F6 仍是 06-20 (9da7f8b) 旧口径产物，叠加 07-02 `LEGACY_ANCHOR_UNEXPLAINED`（£4878 现 HEAD 复现不出），**"06-20 论文正式数字锁死"叙事作废，全部表图需在 E2/C1 链收口后重跑**（§2 相应条目应视为已过期）。(b) **新发现跨表数字矛盾（待溯源，未立案过）**：T6"混合择时"总碳1935.2/成本6173.1（直接排放1885.6=CV为主解）vs T4 mixed 碳730.9/成本8110.9（58EV/6CV），且 6173 < T3 参考最优 7990.9，同算例不可能；T5 M5 总碳461.7 vs T7 主格891.1 vs T8 θ=1.0 885.3 vs F5b 文中 ~1305，均自称 L-main 完整模型。(c) **机制零效应**：T5 无协同 vs 完整模型成本差仅0.66%、跨场服务≈0（完整模型=0）；T8 θ=0.8-1.0 五行逐位同值（约束从不 binding，自然比值1.04，网格步长0.05 跳过唯一有效区间(1.00,1.04]）、F6 前沿是平线；T7 配额轴为目标函数常数项、4×4 网格半数维度自证无影响；T9 全阶段 min_fairness_ratio=off（动态×三机制门槛 07-02 已立案）。(d) **正文自相矛盾**：摘要"8类算法" vs T3 实际7个 vs §4.1 残留"求解算法尚未最终确定"旧句；§4.8"3条事件流" vs T9 实际10条；参数表 |N^0|=25 客户 vs T1 正式算例 219/100；F1 坐标轴 ±60000 km/比例尺6400 km 单位错误（基准坐标无量纲）。(e) **顶刊标配缺失**：无 Goeke-Schneider 原始实例 vs 文献 BKS 的求解器可信度验证；无小算例精确解/下界对标（注意与台账⑤"小算例快跑分胜负"用途不同——那是判胜负判别力不足被关，这是证实现正确性；是否做需 user 拍板）。(f) **表格工艺**：T1 16位小数、T4 英文调试标签/source_seed 行、T9 90行日志倾倒、T3 缺蓝图要求的 std/Wilcoxon 列。建议下一步：①发 Codex"表图数字溯源审计"提示词（只读、查 T4/T5/T6/T7/T8/F5b 每个碳/成本数字的 CSV 来源+算例+口径+commit）；②重跑前拍板机制效应设计决策（θ 网格加密至(1.00,1.06]、配额改硬约束或删轴、c^tr=95 敏感性、动态三交互激活）；③文字层矛盾（8类/3条流/25客户/残句/F1单位）可先行清理。
- 2026-07-03（续）user 采纳审计结论，并定方向：**当前不修数字（等实验定稿再灌数），先做图表内容设计定稿**——每张表/图"一图一命题、题注自足、禁工程字段、碳口径三分（直接/充电间接/总）"。Claude 已出一揽子 Codex 提示词（对话内，未存 repo）：范围=T1-T9 逐表重设计（T3 双账本+Wilcoxon+参数化算法行、T4 转置成分解表、T6/F3 统一口径链、T7 删配额轴、T8 细 θ 网格覆盖 binding 区、T9 两级化+静态后见对照/信息成本+三交互列）+ F1-F6 重设计（F1 修单位/跨场标记、F2 按算例分面、F5 热力图改碳价响应曲线并入 F5b）+ **新增 F7 动态时间线图**（甘特冻结段+事件+累计阶梯+三机制逐阶段活跃度，动态三交互门槛的载体）+ 数据契约 DATA_CONTRACT.md（重跑硬要求：逐 eval 日志、等墙钟账本、细 θ 网格、公平开启+静态对照、事件/冻结明细导出）+ tex 编辑性矛盾清理（残句/8类/3条流/25客户/F5热力图字样）。产出到 docs/paper_submission_final/design_templates/（mock 数据显式带水印，不覆盖 06-20 旧产物），工作对象=solver/src/setp_solver/reporting/。待 Codex 执行后 Claude 核验。
- 2026-07-03（Codex 实现论文图表样板定稿包，**未跑 solver、未产真数、未覆盖 generated_tables/generated_figures**）：新增 reporting runner `design-templates` 与 `solver/src/setp_solver/reporting/design_templates.py`，只从 `docs/paper_submission_final/design_templates/mock_data/*.csv` 读 mock 数据，输出 `mock_data/`、`tables/`、`figures/`、`DATA_CONTRACT.md`、`design_preview.tex`、`design_preview.pdf`、`design_template_report.md`。T1/T3/T4/T5/T6/T7/T8/T9 已按“单表一命题”重定契约；F1-F7 已生成 PDF+PNG，F5 改为碳价响应曲线且不生成 heatmap，F7 新增动态时间线（事件/冻结重规划/信息成本/累计排放/跨场/公平/低碳充电）。`style.py` 改色盲安全配色与半透明“样例数据/非实验结果”水印；`tables.py` 统一算法显示名、碳三口径和宽表换行列；`schema.py` 将碳字段标签统一为直接排放（燃油）/充电间接排放/总排放；`STYLE_NOTES.md` 写入样板纪律。`paper_main.tex` 只做编辑性修复：删“求解算法尚未最终确定”残句，弱化固定算法数，参数表网络规模指向 T1，动态改 10 条事件流，F3/T6 单口径说明，F5 去热力图表述并优先引用未来 `figure_f5_carbon_response.pdf`。验证：`design_preview.tex` 编译通过（仅首页 overfull vbox 与 MiKTeX 更新提醒），`paper_main.tex` 编译通过（旧正式表图仍有既有 overfull/underfull 提示），`solver/tests/test_reporting.py` = 15 passed，`solver/tests` = 224 passed / 1 skipped / 5 failed（失败在 ALNS/E5/formal runner 旧链，非 reporting 样板层），scoped `git diff --check` 对本轮文件干净；全局 `git diff --check` 仍被 pre-existing E2 reaudit CSV 尾随空白挡住。
- [M1] 2026-07-03（Codex 执行 E2-G0 收官工程：卫生 + 翻转分账 + liveness 门禁 + TRUE_REPAIR 对齐 + G0 重审，**baseline health / G0 gate，非正式 T3**）：产物目录 `baselines/e2_alns/e2_g0_reaudit_20260702/`，主 runner `baselines/e2_alns/e2_g0_reaudit.py`，plateau 审计脚本已支持新 G0 目录模式。前置 C0 已清理非 `.git` AppleDouble/cache 并重生涉证目录 hash；旧污染 hash 保留污染原因。`metaheuristic_baselines.py` 新增 common flip preprocess 与 channel 记账（`shared_warm_start` / `common_flip_preprocess` / `native_*` / `flip_operator`）、`native_best_updates`、`route_count_unique`、`common_lift/native_lift/flip_lift` 和 run-level liveness verdict；触碰该文件后独立 ALNS 两锚均零漂（Goeke80 `2677.7953638343815`、280 三班 `3561.080964207054`）。TRUE_REPAIR A/B 闭合：GA 2000 eval 从 best `4629.8754212957`/native updates 1 到 `4596.373111001051`/updates 2；LNS 从 `4289.832864688266`/updates 5 到 `3788.7364537265853`/updates 24，吞吐仍足够支撑 16000/2700s，因此 `_baseline_fast_repair_flags()` 不再强制 `SETP_ALNS_CRUSH_TRUE_REPAIR=0`，baseline 与 ALNS 插入评分同尺。G0 重审配置为 `e2-threeshift-150c-01`、280kWh 内存 override、GA/LNS/PSO/VNS+ALNS、seeds1-3、16000 eval；15/15 rows 均 `OK` 且满 `16000/16000`，无 under-eval。关键结论：旧 `5174.345121253789` 精确同质化未复发（12 条 baseline best cost/signature 全不同，跨 seed/cross-algo identity suspect=0），但 liveness 仍未过：`G0_PSO_seed1/2/3` 与 `G0_VNS_seed2` 均 `NO_NATIVE_BEST_UPDATE`，所以 `decision.json` verdict=`G0_RESIDUAL_HOMOGENIZATION`，不是 `G0_PASS_BASELINES_HEALTHY`。best-cost 台账只能作定位：ALNS mean `3785.039108`、GA mean `4368.494881`、LNS mean `3563.296463`、PSO mean `4772.622803`、VNS mean `4143.154067`，不得写算法胜负。场景合规 `SCENARIO_COMPLIANCE_OK`：`DEFAULT_PRICES` 为 `B=80/Q=3650/carbon=0.05034`，`paper_main.tex` 主文口径一致，1 个旧 generated table 的 280 命中只登记不改表。旧锚考古用 `df608660` 旧代码 + 当前同名 bundle 得到 `4779.053444002934`（16000 eval、零违约），未复现目标 `4878.331796187524`，标 `LEGACY_ANCHOR_STILL_UNEXPLAINED`。G4/G5 继续冻结；下一步若继续 E2，只能针对 PSO/VNS liveness 失败做有界修复/降级裁决，不能扩跑或写正式 T3。
- [M1] 2026-07-02（Claude 判读 G0 重审 `G0_RESIDUAL_HOMOGENIZATION`：4 条 liveness 失败的主因=Claude 自己的 R1c 起点设计失误,修正令+锚谜案破案）：①**判读**：G0 重审的核心成果成立——采集 15/15 闭合、旧跨算法逐位同值假象 0 复发、TRUE_REPAIR 对齐证据硬（LNS updates 5→24, best 4289→3788）。剩余 4 条失败（PSO×3 seed + VNS seed2 `NO_NATIVE_BEST_UPDATE`）**主因是 R1c 把"翻转闭包平台(5174)"设为共同起点**——起点太强,排列解码类原生动作打不过它（decoder 修复后 min≈5158.94,<5174 率 1/2496）,而 R1b 时代从 warm start(5803) 起步 LNS 有 19 次原生更新;更硬的证据是 ALNS 从 5174 起点跑 16000 eval 只到 3630,劣于它自己从 warm start 起 2000 eval 的 3561——**强制流经确定性 EV-heavy 深盆地扭曲了所有算法**。修正令:共同起点回退为 shared warm start,通道记账保留,翻转闭包只作参考值记录、不再强加。②**台账级大信号（非胜负主张）**：健康机器下 LNS 3554/3563 当前领跑,ALNS 3630/3785（受起点偏置）,GA 4110/4368、VNS 4138/4143、PSO 4752/4772;"ALNS 领先 30%"正式死亡;ALNS 真实水位待 warm-start 重跑;若重跑后 LNS 仍≥ALNS,启用 MASTER 既有预案（同档第一梯队+碳机制/DR 差异化）,不加料硬刚。③**锚谜案破案（零算力,纯档案）**：旧代码考古跑出的 `4779.053444002934` 与 06-22 HANDOFF 记录的 **seed2 锚逐位一致**=代码+bundle 谱系完好;`4878.331796187524` 在 06-22 记录里本来就是**多 seed 均值**,重审拿单 seed 值比对均值目标属张冠李戴,`LEGACY_ANCHOR_STILL_UNEXPLAINED` 应改判"高置信已解释,待旧 worktree 全 seed 集复验均值"。另 `formal_winner_20260619/runkey_summary.csv` 的 ALNS-Wouda 100-01 十 seed 均值=4844.796,是**另一配置**（formal E2 runner）,与 winner-kernel 锚族不同,引用时勿混。④下一任务=起点修正+G0 重跑+锚均值复验,提示词已在对话签发;PSO/VNS 若从可击败起点仍零原生更新,转 G3 文献重建（10_e2_baselines_literature_best.md）,是否带病进 T3 由 user 拍板。未跑 solver、未改代码。
- 2026-07-03（续2）Claude 核验 Codex 图表样板交付（commit cd4742ef5，注意该 commit 混入了 E2 reaudit 产物；Codex 报告称"未提交"不准确）。**裁决=整体合格、样板底座可用，7 项未达规格需整改**：①F2 缺终值箱线图面板（且 paper_main.tex 图题被擅自降级为"算法收敛过程"）；②所有表注是同一句 boilerplate，未按"题注自足"写逐表口径注；③T3 缺表末"达优次数/平均"汇总块；④F7 甘特未按车辆分行、冻结/重规划色块无图例、事件为文字非记号、成本轴缺£且成本线被盖、跨场柱不可见；⑤F1(b) 路线一线一色而非"颜色=车辆类型"两色制、跨场描边无图例；⑥T4 kgCO2e 未排下标、T9 mock 仅3条流 vs tex 已写10条；⑦缺附录"代表事件流逐阶段明细表"样板。做对的：tex 六项编辑正确（F5 IfFileExists 兜底）、碳口径三分法落地、T4 转置、T9 三交互列齐、T7 删配额轴、F5 响应曲线+阈值底纹、F7 概念齐、F1 单位修复、水印全、DATA_CONTRACT 有命题+重跑硬要求。整改提示词已发（对话内），另要求 Codex 自证"全量测试 5 failed 为既有失败"（父 commit 复跑证据），且本次 commit 不得与 E2 产物混提交。
- 2026-07-03（Codex 执行图表样板 7 项整改，**mock-only 设计层，未跑 solver、未覆盖正式 generated_tables/generated_figures**）：按 commit cd4742ef5 验收反馈整改 `docs/paper_submission_final/design_templates/` 与 reporting 层。父基线自证：在临时 worktree `e4c25fa331842fdd6b16a354efc1fd93d36f47d2` 上复跑 5 个既有失败 nodeid，结果 `5 failed in 0.47s`（ALNS fleet/API、E5 缺 bundle、formal dynamic split 旧链问题），确认非本轮 reporting 样板引入。实现：F2 改为按算例分面的收敛曲线+终值箱线图双面板并恢复 paper_main 图题/正文；T1-T9 专属口径注由 `TABLE_NOTES` 常量同步渲染到表格和 `DATA_CONTRACT.md`；T3 增加 `达优次数`/`平均偏差` 汇总块；F7 改为车辆分行甘特、事件 ▲/×/◆ 时间轴记号、成本/排放双线分层、跨场柱顶标注；F1(b) 改燃油同色实线/电动同色虚线并补跨场黑描边图例；T4 统一 `kgCO$_2$e`，T9 mock 扩至 10 条事件流；新增附录 `tables/t9_appendix_stage_detail.tex` 与 `mock_data/t9_appendix_stage_detail.csv`。验证：`solver/tests/test_reporting.py` = 15 passed；`design-templates` runner 通过并生成 7 组 PDF+PNG；`design_preview.tex` 与 `paper_main.tex` 均 latexmk 通过（仅既有 overfull/MiKTeX/ToUnicode 警告）；scoped `git diff --check` 干净。E2 reaudit 脏改动仍存在但未纳入本次提交。
- [M1] 2026-07-03（Codex 执行 E2-G0 v2 起点修正重跑 + 锚谱系收尾，**baseline health / G0 gate，非正式 T3**）：按 Claude 判读修正 R1c 起点错误，所有 GA/LNS/PSO/VNS/ALNS 从 `shared warm start` 起跑，翻转闭包只记录为 `reference_flip_closure_*`，`common_lift=0`，不再作为共同起点或 best update。代码侧仅修 `metaheuristic_baselines.py` 参考闭包记账、`e2_g0_reaudit.py` v2 runner/报告/hash、`plateau_5174_audit.py` 新目录审计适配与起点/liveness 测试；未改 `cost.py`/`check.py`/`evaluation.py`/`prices.py` 默认值/TeX/`feasible_repair.py`/`resetp_alns`/ALNS 主路径语义。验证：新增/相关回归在干净临时 worktree 为 `35 passed, 1 skipped`；触碰 baseline 后两独立 ALNS 锚点零漂（Goeke80 seed2 `2677.7953638343815`，280 三班 warm-start 口径 `3561.080964207054`）。产物 `baselines/e2_alns/e2_g0_reaudit_v2_20260703/`：G0 v2 15/15 rows `OK` 且 `16000/16000`，plateau 复核 verdict=`G0_PASS_BASELINES_HEALTHY`，旧 `5174.345121253789` 精确同质化未复发（12 条 baseline best cost/signature 全唯一，identity suspect=0），12/12 baseline run liveness 通过。台账成本仅作定位、非正式胜负：ALNS mean `3568.483474`、LNS mean `3599.130896`、VNS mean `4071.566126`、PSO mean `4576.538634`、GA mean `4591.542851`。场景合规 `SCENARIO_COMPLIANCE_OK`；旧锚谱系 `ANCHOR_LINEAGE_CLOSED`，`df608660` seed1-10 mean 精确复现 `4878.331796187524` 且 seed2 精确复现 `4779.053444002934`，4878=旧代码/Q1600 时代均值、4779=同锚族 seed2。顶层 `artifact_hashes.json` 已重生为 clean-files-only schema，排除且不含 `._*`/`__pycache__`/`.pytest_cache`；本任务仍不解冻 G4/G5，是否进入正式 T3、PSO/VNS 是否转 G3 文献重建、若 LNS 与 ALNS 接近/反超时论文姿态，均留给 user 拍板。
- [M1] 2026-07-03（Claude 判读 G0 v2 `G0_PASS_BASELINES_HEALTHY` + 三项拍板建议 + 签发交稿收官任务）：①**E2 排障正式结束（22 天）**。公平 warm-start 台账（16000 eval×seeds1-3,健康门禁 12/12 过,锚谱系闭合）：ALNS min/mean=3429.87/3568.48 全场第一;对 LNS(3584.21/3599.13) mean+0.9%/min+4.3%,对 VNS(4071.57)+12.4%,对 PSO(4576.54)+22.0%,对 GA(4591.54)+22.3%。对 GA/PSO/VNS 领先远超 ≥10% 验收线;对 GLNS 型强对手小幅领先=符合"干过强对手+贴近最优"投稿标准。上一轮"LNS 领跑"确认为 Claude R1c 起点偏置假象。②**碳算子合法重开**：09x/09y 的 WEAK/全平判定测量于同质化坏机器时代（09y Stage B 全算法逐位平局即 C1 确诊假象）,已按台账重开协议在 PRD v2 台账第 6 条注记;健康机器碳消融 gate=碳算子第一次真实测试,结果未出前不得预写"碳算子有用"。ALNS 文献组件（LOCAL_SEARCH/ROUTE_ELIMINATION/RRT 接受）的关闭消融同属 Q1600/80kWh 旧时代,当前 regime 未测,纳入同一 gate。③**拍板建议（待 user 认可即执行）**：G4/G5 解冻,前插 1 天"ALNS 定型 gate"（碳消融+组件重消融,预注册定型规则自动选 T3 主变体）;PSO/VNS 不做 G3 文献重建（健康已过,落后 12-22% 为文献常态,诚实报告+参数来源文档化）;G3 仅做承诺基线（ACO/GA-VNS/SA）过同一健康门禁;论文姿态=ALNS 正常写赢,对 LNS 小幅领先如实报显著性,MASTER 同档预案仅作兜底。④交稿收官提示词（定型 gate→G3 健康门→G4 稳定性→G5 正式 T3,预注册自决策规则,双账本,resume）已在对话签发。未跑 solver、未改代码。
- 2026-07-03（续3）Claude 核验整改 commit 04f2d2be：**验收通过，图表样板定稿**。7 项全落地（F2 双面板收敛+终值箱线图、逐表专属口径注、T3 达优/平均偏差汇总块、F7 车辆分行甘特+事件记号+静态基线+跨场标注、F1 两色制+跨场描边图例、kgCO2e 下标/T9 十条流、T9 附录逐阶段明细表）；commit 干净（无 E2 产物、无保护文件、tex 仅恢复 F2 表述）；父 commit e4c25fa 复跑自证 5 个失败测试为旧链既有（ALNS fleet/API、E5 缺 bundle、formal dynamic split），与 reporting 层无关。遗留两处极小瑕疵（下轮顺手修，不单开任务）：①F7(b) 成本轴标签"成本/"缺 £（疑似字体缺字形，可改用 GBP 字样）；②F2 四个子图编号重复为 (a)(a)/(b)(b)，应为 (a)-(d)。**样板状态=已定稿待灌数**：正式重跑后只按 DATA_CONTRACT.md 灌 CSV，样板代码不动。下一步回到主线：E2/C1 链收口 → 全量重跑 E1-E7（含细 θ 网格、公平开启的动态、静态后见对照、逐 eval 日志、等墙钟账本等契约硬要求）→ 灌数出正式图表。
- 2026-07-03（Codex 执行 E2 交稿收官工程 Phase 0/A 并按 user 要求介入修正，**证据 gate / 非正式 T3 / 不写 TeX**）：新增/修正 `baselines/e2_alns/e2_final_closure.py` 与 `solver/tests/test_e2_final_closure.py`，产物目录 `baselines/e2_alns/e2_final_closure_20260703/`。前置冻结通过：环境 `/opt/anaconda3/bin/python3.13` + numpy `2.3.5` + `PYTHONHASHSEED=0`，G0 v2 verdict=`G0_PASS_BASELINES_HEALTHY`，Tier1 `-01` manifest=23，正式阶段默认 Goeke80 零 override，280 仅 Phase A diagnostic。Phase A 先按原队列运行时发现关键问题：正式 Goeke80 `e2-threeshift-150c-01 / alns_e2_carbon / seed1` 在 `3602.891834s` 只完成 `5575/16000` eval，gate_status=`HALT_RUNTIME_UNDER_EVAL`，不能包装为闭合 carbon gate。按 user 指令介入：停止仍在继续派发的 Phase A 进程和残留 worker，修 runner 为“formal Goeke80 A1 批次先行；任一 formal blocker 立即写 `ALNS_GATE_BLOCKED`，跳过 diagnostic 280 与组件 gate；中断时清理 phase `.tasks` 子进程”；清理 orphan `.tasks` 与无 raw row 的 checkpoint，只保留 14 条有 raw row 的正式证据。最终总 `decision.json`：`final_material_verdict=ALNS_GATE_BLOCKED`、`blocked_phase=phase_a`，Phase B/C/D 未启动，T3/F2 素材未生成。回归 `solver/tests/test_e2_final_closure.py solver/tests/test_e2_alns_throughput.py solver/tests/test_baseline_liveness_gate.py -q` = `19 passed`；新增决策测试单跑 `8 passed`；两独立 ALNS 锚零漂（Goeke80 seed2 `2677.7953638343815`，280 三班 seed1/2000 `3561.080964207054`，均 feasible/0 违约，记录于 `anchor_parity_after_runner_fix.json`；仅见 SA exp overflow warning，不影响 parity）。artifact hash 已刷新且不含 `._*`/`__pycache__`/`.pytest_cache`；protected-file diff 对 `cost.py/check.py/evaluation.py/prices.py/TeX/feasible_repair/resetp_alns/ALNS 主路径` 为空。后续不能启动 G3/G4/G5，需先由 user 决策：接受 throughput-only 定型重开、给 carbon gate 另设吞吐策略，或正式放弃 carbon 主变体。
- [M1] 2026-07-03（user 拍板 carbon gate 策略,解除 ALNS_GATE_BLOCKED）：Phase A 正式 Goeke80 下 alns_e2_carbon 150c-01/seed1 于 3602.89s 仅 5575/16000 eval（≈慢 3 倍）,carbon gate 不闭合。裁决：**T3 主变体锁死 alns_e2_throughput**（给 carbon 单独抬帽=双标自毁,按现帽硬跑=正式表满 under-eval,均不可取）;**碳算子降级为并行等墙钟诊断线**（DIAGNOSTIC/非 T3:三臂同帽 3600s 不要求 eval 闭合,两 gate 实例×seeds1-3,Goeke80 正式臂+280 诊断臂,附 TIMING_LEDGER 热点记录仅存档不优化;预注册 CARBON_OPS_WALLCLOCK_POSITIVE→机制节素材 / CARBON_OPS_NO_BENEFIT→limitation+future work,两种结果均不改主变体）。**论文姿态口径**：碳感知在模型层成立（含碳目标+时变碳机制,所有算法优化的都是含碳目标）,碳专用算子只按诊断线证据边界如实写,不预写有效;创新旗帜若算子不立则由模型四要素+DR-ALNS(x86)承担（MASTER 既有定位）。Phase A 收窄为 throughput 组件重消融（A2,采纳规则不变）;Phase B/C/D 按原口径继续。另认可 Codex 三修正（formal blocker 快速失败/ProcessPool 孤儿清理/scratch 卫生）,"先正式臂后诊断臂"顺序纳入后续口径。墙钟帽口径已锁:100c=2700/150c=3600/200c=5400;G4 方向一致性判据已锁:(a)≥6/9 实例 ALNS 不劣 +(b)pooled gap≥0 +(c)无单实例劣>2%,违者 HALT_G4_SUSPECT。
- 2026-07-03（Codex 按 user 拍板修订收官工程 Phase A，**解除旧 carbon 主变体阻塞，非正式 T3 / 不写 TeX**）：`baselines/e2_alns/e2_final_closure.py` 已改为主线 Phase A 只跑 throughput 组件重消融，旧 `ALNS_GATE_BLOCKED` carbon 证据归档到 `phase_a_carbon_gate_superseded_under_eval/`，carbon 算子改为单独 `carbon-diagnostic` 等墙钟非阻塞入口（尚未运行，`carbon_diagnostic_verdict=NOT_RUN_NONBLOCKING`）。新 Phase A A2/A3 在 `e2-threeshift-150c-01`、Goeke80 默认、seeds1-3、16000 eval、3600s 帽下闭合：A2 12/12 OK，A3 叠加复验 3/3 OK。预注册规则判定 `ALNS_GATE_READY`，T3 主 profile=`alns_e2_throughput + LOCAL_SEARCH`；`LOCAL_SEARCH` 单独 mean 改善 `+1.1152%`、worst seed `-0.0619%`，采纳；`ROUTE_ELIMINATION` 单独过线但与 LOCAL 叠加复验 mean 仅 `+0.1738%` 且 seed2 劣化 `-4.5409%`，回退；`RRT_TRUE_ACCEPTANCE` mean `-16.2137%`、worst `-55.2326%`，拒绝。顶层 `decision.json` 已刷新为 `phase_a=ALNS_GATE_READY`、`blocked_phase=phase_b`（Phase B/C/D 未运行，不是 Phase A 失败）。回归 `solver/tests/test_e2_final_closure.py solver/tests/test_e2_alns_throughput.py solver/tests/test_baseline_liveness_gate.py -q` = `22 passed`；两独立 ALNS 锚复跑零漂（Goeke80 seed2 `2677.7953638343815`，280 三班 seed1/2000 `3561.080964207054`，均 feasible/0 违约，记录 `anchor_parity_after_phase_a_revision.json`）。artifact hash 干净且不含 `._*`/`__pycache__`/`.pytest_cache`/`.tasks`；protected-file diff 对 `cost.py/check.py/evaluation.py/prices.py/TeX/feasible_repair/resetp_alns/ALNS 主路径` 为空。下一步可继续 Phase B G3 baseline health；carbon 等墙钟诊断线可并行跑，但不阻塞 B/C/D。
- 2026-07-03（Codex 执行 E2 交稿收官 Phase B / G3 在库基线健康门，**证据 gate / 非正式 T3**）：`baselines/e2_alns/e2_final_closure_20260703/phase_b_g3_baseline_health/` 已闭合，配置 `e2-threeshift-150c-01`、Goeke80 默认零 override、seeds1-3、16000 eval、3600s 帽。ACO、GA-VNS、GWO、IWD 共 12/12 rows 全部 `OK` 且 `16000/16000`，run-level liveness 全 `BASELINE_LIVENESS_PASS`，无 weak implementation 排除；Phase B verdict=`G3_BASELINE_SET_READY`。T3 baseline set 更新为 `{GA,LNS,PSO,VNS,ACO,GA-VNS,GWO,IWD}`；SA/TS 仍登记为 `NOT_IMPLEMENTED_FOR_T3_BOUNDARY`，留给 user 决定是否写 limitation。阶段 best-cost 台账仅作健康门素材、非论文胜负：ACO seeds=`4424.76/4418.48/4484.53`，GA-VNS=`4340.17/4207.02/4366.54`，GWO=`4383.24/4223.14/4441.21`，IWD=`4977.80/4960.06/4881.96`。顶层 decision 已刷新为 `phase_a=ALNS_GATE_READY`、`phase_b=G3_BASELINE_SET_READY`、`blocked_phase=phase_c`（Phase C/D 未运行）。下一步可进入 Phase C G4 三班稳定性复核；carbon 等墙钟诊断仍为非阻塞未运行支线。
- 2026-07-03（Codex 执行 E2 交稿收官 Phase C / G4 三班稳定性复核并按预注册红线早停，**证据 gate / 非正式 T3**）：配置 Goeke80 默认零 override、`t3_main_alns=alns_e2_throughput+LOCAL_SEARCH` 对 LNS、`e2-threeshift-100c-01` 起跑、seeds1-3、16000 eval。首个完整实例 6/6 rows 均 `OK` 且 `16000/16000`，但已不可逆触发 G4 方向红线：ALNS mean `2745.3297058863445`、LNS mean `2687.0157578586277`，gap=`-0.021702123576003575`，超过“任一实例 ALNS 不得劣于 LNS 超过 2.0%”阈值；同时 LNS seed1 liveness 失败（`route_count_unique=4 < 5`，`LOW_ROUTE_COUNT_DIVERSITY`）。Codex 随即停止剩余 Phase C worker，未启动 Phase D/G5；顶层 `decision.json` 为 `final_material_verdict=HALT_G4_SUSPECT`、`blocked_phase=phase_c`，这不是采集 under-eval，也不得包装成正式 T3 素材。同步修正 `baselines/e2_alns/e2_final_closure.py`：Phase C 现在输出 `liveness_verdicts.csv`，并把 baseline liveness suspect 与已完成实例 hard direction violation 纳入 `decide_phase_c()`；新增测试锁住这两个判决。验证：`solver/tests/test_e2_final_closure.py solver/tests/test_e2_alns_throughput.py solver/tests/test_baseline_liveness_gate.py -q` = `24 passed`；当前 G0/C1/C3/收官集合 = `46 passed, 1 skipped`；两独立 ALNS 锚零漂（Goeke80 seed2 `2677.7953638343815`，280 三班 seed1/2000 `3561.080964207054`，仅见既有 SA overflow warning）。产物在 `baselines/e2_alns/e2_final_closure_20260703/phase_c_g4_stability/`，hash 已刷新并清理 `.tasks`/AppleDouble。后续必须先由 user 决策：是否调整 G4/论文姿态、是否重开 ALNS 定型/组件选择，或将 LNS 作为强对手重新定位；本任务不得继续 Phase D。
- 2026-07-03（Codex 执行 G4 HALT 复核 A' / ROUTE_ELIMINATION-only 补测 + liveness 对称可见化，**证据复核 / 非正式 T3**）：新增 `phase-a-prime` 入口与产物 `baselines/e2_alns/e2_final_closure_20260703/phase_a_prime_route_elimination_retest/`，只补缺口，不改模型、不改 ALNS 主路径。复用既有 Phase A/C 数据，实际补跑 6 行：`e2-threeshift-100c-01` 的 `alns_component_ROUTE_ELIMINATION` seeds1-3 与 `e2-threeshift-150c-01` 的 LNS seeds1-3，均 Goeke80 默认零 override、16000/16000 OK。判决 `ROUTE_RETEST_INCONCLUSIVE`：route-only 在 100c-01 的均值缺口从 LOCAL_SEARCH-only 的 `-2.1702%` 改善到 `-1.4898%`，pooled 100c/150c 也从 `-1.1243%` 改善到 `-0.9939%`，但 100c-01 最差 seed 仍为 `-2.6153%`，违反预注册“单 seed 不劣化超过 1%”安全线；150c-01 route-only 也仍略劣于 LNS（`-0.6519%`）。因此不切换 T3 主 profile，顶层仍保留 `t3_main_profile=alns_e2_throughput+LOCAL_SEARCH`、`blocked_phase=phase_c`、`final_material_verdict=HALT_G4_SUSPECT`。同步修正 liveness 输出：`t3_main_alns` 与 ALNS 组件变体的 `route_count_unique<5` 现在写为 `ALNS_ROUTE_COUNT_INFO / LOW_ROUTE_COUNT_DIVERSITY_INFO`，只做可见化，不作为 FAIL；baseline liveness 判决逻辑不变。验证：`solver/tests/test_e2_final_closure.py` = `15 passed`，当前 G0/C1/C3/收官集合 = `49 passed, 1 skipped`；两独立 ALNS 锚零漂（`2677.7953638343815` / `3561.080964207054`）。artifact hash 干净，不含 `._*`/`__pycache__`/`.pytest_cache`/`.tasks`。结论边界：A' 说明 ROUTE_ELIMINATION-only 有改善但证据不足以解除 G4 HALT，也不足以证成“结构性缺口已确认”；Phase D/G5 仍不得启动，需 user 再拍板论文姿态或下一步定型策略。
- 2026-07-03（Codex 执行 G4/G5 收尾恢复计划，**证据 gate / 非正式 T3 / 不写 TeX**）：按 user 新裁决修改 `baselines/e2_alns/e2_final_closure.py` 与 `solver/tests/test_e2_final_closure.py`，把 G4 判据 (c) “单实例 ALNS 劣于 LNS 超过 2%”从硬 HALT 降级为 `DOCUMENTED_INSTANCE_EXCEPTION`，但 (a) `>=6/9` 实例不劣与 (b) pooled gap `>=0` 仍为硬门槛；`route_count_unique=4` 这类已知路线数诊断只报告，不独立阻塞，`NO_NATIVE_BEST_UPDATE`/seed 不变/跨算法同签名仍硬 suspect。Phase D/G5 runner 同步接受 `G4_PASS_WITH_EXCEPTIONS`，并新增 Tier1/2/3 manifest（23/46/69）、双账本与 T3 逐实例素材 exception 字段，但本轮因 G4 未过没有启动 Phase D。Phase C resume 完整闭合 9 个三班稳定性实例（54/54 rows，全部 `OK` 且 `16000/16000`）：exception 共有 2 个，`e2-threeshift-100c-01` gap `-0.021702123576003575`（附 A' route-elimination 诊断证据，机制说明=LOCAL_SEARCH 不改路线数/该例需要路线压缩）和 `e2-threeshift-100c-02` gap `-0.020537221759442725`；其他 gap 为 100c-03 `+0.014095572789459488`、150c-01 `-0.004028649151723193`、150c-02 `+0.028334926254161955`、150c-03 `-0.01177550035292185`、200c-01 `+0.015069505914835643`、200c-02 `+0.031291065191697144`、200c-03 `+0.0983854563190377`。总账：`nonnegative_gap_instances=5/9`，`pooled_gap_fraction=0.02085891464891202`，`documented_exception_count=2`，所以因硬条件 (a) 失败，`phase_c_g4_stability/decision.json` 与顶层 `decision.json` 均为 `HALT_G4_SUSPECT`、`blocked_phase=phase_c`、`halt_reason=G4_DIRECTION_RULE_FAILED`，不得推进 G5，也不得包装为正式 T3。验证：最小回归 `solver/tests/test_e2_final_closure.py solver/tests/test_e2_alns_throughput.py solver/tests/test_baseline_liveness_gate.py -q` = `32 passed`；两独立 ALNS 锚零漂（`2677.7953638343815` / `3561.080964207054`，`abs_diff=0.0`）；artifact hash 已刷新，顶层 150 files、Phase C 63 files，均不含 `._*`/`__pycache__`/`.pytest_cache`/`.tasks`，protected solver/TeX 语义路径 diff 为空。
- [M1] 2026-07-04（Claude 判读 G4 `HALT_G4_SUSPECT` 数据：非 bug,是规模依赖的真实性能曲线,门禁尺子不匹配;论文口径待 user 拍板）：Phase C 9/9 三班实例(formal Goeke80,零 override,54/54 行 16000/16000 OK,健康门禁无同质化嫌疑,ALNS route_count_unique=1 已正确降级为 INFO)逐实例 gap(正=ALNS 不劣)呈干净单调规模趋势：100c 档均 -0.94%(-2.17/-2.05/+1.41)、150c 档均 +0.42%(-0.40/+2.83/-1.18)、200c 档均 +4.83%(+1.51/+3.13/+9.84)。200c-03 的 +9.84% 为逐种子碾压(ALNS 4443-4826 vs LNS 4954-5247,非 LNS 跑崩),200c ALNS native_best_updates 68-124=搜得有效。**FACT**：这是"大邻域搜索赢在大/难实例、小实例近最优处简单算法微弱占优"的标准 VRP 模式,不是同质化 bug（对比:22 天前的信号全是跨种子逐位相同+原生更新为零,本次有真实方差+可解释机制）。**门禁误判分析**：(a)≥6/9 等权投票 + pooled 原始百分比混合,把"ALNS 赢在大实例"抹平成 5/9 不过关+pooled+0.02%;该规则本为抓同质化(假平局)设计,非裁决规模依赖曲线。健康关 G4 通过,"每实例不劣于 LNS"关未过——但后者从非 MASTER 要求(同档即可)。**注**：这些 Goeke80 三班实例有真实 EV/充电结构(ev_route_count 2-21,200c 达 EV 占比 ~55-70%),故碳感知算子在此场景可能有发挥空间(此前判弱均在坏基线/150c-01 单实例)。**Claude 未自作主张改判据**（已第三次触及规则调整,连续自授权改规则会侵蚀"抓同质化"的可信度）,论文口径三选一交 user：①按规模故事进 G5(健康通过+规模依赖优势,生成 T3 逐实例素材);②先补碳算子等墙钟诊断(三班 Goeke80 有 EV 结构,测创新算子是否发挥)再进 G5;③严守 HALT 不进 G5,按同档预案写。未跑 solver、未改代码、decision.json 未改。
- [M1] 2026-07-04（user 拍板 G4 走向=选项②"先补碳算子等墙钟诊断再进 G5"）：G4 据此定为**健康关通过 + 规模依赖性能画像已记录**（无同质化,真实方差,ALNS 随规模变强:100c 档 -0.94%/150c +0.42%/200c +4.83%）,(a)≥6/9 硬 HALT 重分类为"规模依赖结果,非数据完整性失败",两个 100c exception 保留如实记录;user 已授权诊断后进 G5,故 Claude 不算自授权改判据。下一步=碳算子诊断（等墙钟,健康基线,三班 Goeke80 高 EV 实例,carbon/ablation/throughput 三臂,隔离算子效应）→ 结果进机制/创新节素材+可选变体列（throughput 仍为 eval-闭合 T3 主变体,除非碳呈 DOMINANT 由 user 另拍）→ Phase D/G5。提示词已在对话签发。未跑 solver、未改代码。
- [M1] 2026-07-04（Codex 执行 E2 收官终章：G4 record + Phase E' carbon + Phase D HALT）：沿用 `baselines/e2_alns/e2_final_closure_20260703/`，未新建 20260704 根目录。G4 仅记录不重跑，Phase C verdict 从旧 `HALT_G4_SUSPECT` 语义记录为 `HEALTH_PASS_WITH_SIZE_DEPENDENT_PROFILE`，保留 `original_verdict=HALT_G4_SUSPECT`、9 实例 gap、两个 100c `DOCUMENTED_INSTANCE_EXCEPTION`、`nonnegative_gap_instances=5/9`、`pooled_gap_fraction=0.02085891464891202` 与 user 选项②授权。Phase E' 碳算子等墙钟诊断 Tier1 已闭合：3 个高 EV 三班实例 × `{alns_e2_carbon, alns_e2_carbon_ablation, t3_main_alns}` × seeds1-3，共 27/27 rows `OK`，允许 under-eval 如实入账；预注册主对照 carbon vs ablation 判定 `CARBON_OPS_WEAK`，因此 throughput+LOCAL_SEARCH 仍为 T3 主变体，碳算子写法应偏 limitation/future work，不阻塞 G5。Phase D Tier1 启动后在 partial raw 中触发预注册唯一红线：已闭合/部分闭合的 `e2-multidepot-10c-01` 与 `e2-multidepot-100c-01` 出现同一实例内跨算法/跨 seed 完全相同 `best_cost`+`best_signature`，且 10c-01 多数 baseline liveness 为 `BASELINE_LIVENESS_FAIL/LOW_ROUTE_COUNT_DIVERSITY`；Codex 按 `HALT_T3_HOMOGENIZATION` 中断采集并新增 `phase-d-freeze` 记录层，只冻结已落行数据、不回滚。Phase D freeze：`material_rows=54`、`observed_instance_count=3`、`failure_count=0`、`exact_identity_suspect_count=3`、`homogeneity_suspect_count=6`、`record_only_freeze=true`；顶层 `decision.json` final verdict=`HALT_T3_HOMOGENIZATION`，不是 `T3_MATERIAL_READY`，不得写正式 T3 胜负。验证：最小回归 `42 passed`；两独立 ALNS 锚零漂（`2677.7953638343815` / `3561.080964207054`，`abs_diff=0.0`，记录 `anchor_parity_after_phase_d_halt_patch.json`）；artifact hash 顶层/Phase D 均不含 `._*`、`__pycache__`、`.pytest_cache`、`.tasks`；protected solver/TeX 语义路径 diff 为空。
- [M1] 2026-07-04（Claude 判读 `HALT_T3_HOMOGENIZATION`：红线过钝误伤,三类同值判别+最小修复令）：读冻结 phase_d partial(54行)发现三个同值簇性质迥异:①10c-01 上 GA/GA-VNS/LNS/VNS/ALNS 共11行同值 371.999=**该实例全场最优**且全员原生更新≥1=自然收敛(送分题全班答对,非作弊);②10c-01 上 387.901 九行=第二局部最优,种子分坑(ALNS 自己两簇都有),全员更新≥4,真实随机行为;③100c-01 上 GA/GA-VNS/PSO 三行同值 2894.12(高于前沿17%,更新仅2-3)=共享暖启动+确定性公共步压过弱搜索的残渣——但与 5174 本质不同:被门禁当场抓住且不虚增 ALNS 领先(GA/PSO 16000步确实没找到更好,弱是真的)。**5174 死亡三联征=同值+远离前沿+原生零更新;三簇均不满足**。修复令(最小变更,不加实验):同值簇四分类判则(NATURAL_CONVERGENCE 放行/SECONDARY_ATTRACTOR 放行+注/SHARED_STALL 披露标注不拦停/HALT 仅保留"零更新成员或全算法全种子非前沿同值"真三联征),全部用 raw_runs.csv 已有字段;重判冻结 partial 后 resume Tier1;T3 素材带 identity_cluster 披露列+脚注源。提示词已签发。未跑 solver、未改代码。
- [M1] 2026-07-04（Codex 执行 Phase D 同值簇四分类判则并闭合 Tier1）：按 user 最小变更令只改 `baselines/e2_alns/e2_final_closure.py` 与 `solver/tests/test_e2_final_closure.py`，将 Phase D 同值簇改为四分类：`NATURAL_CONVERGENCE`、`SECONDARY_ATTRACTOR`、`SHARED_STALL` 放行/披露，只有零原生更新成员或完整矩阵非前沿同值才 `HALT_T3_HOMOGENIZATION`。冻结 54 行重判符合预期(A=自然收敛、B=第二吸引子、C=共享停滞)，无 HALT；随后 resume Phase D Tier1，不重跑已完成行。Tier1 最终闭合 621/621 rows，全部 `OK` 且 `16000/16000`，Phase D verdict 与顶层 final verdict 均为 `T3_MATERIAL_READY`。`identity_clusters.csv` 共 33 个簇：`SHARED_STALL=22`、`NATURAL_CONVERGENCE=7`、`SECONDARY_ATTRACTOR=4`、`HALT=0`；相关行已在 `raw_runs.csv`/`t3_table_material.csv` 带 `identity_cluster_*` 标注和脚注源。碳诊断仍为 `CARBON_OPS_WEAK`，T3 主变体仍是 `alns_e2_throughput+LOCAL_SEARCH`；所有素材只作 T3/F2 证据，不自动写 TeX、不写算法胜负论文表述。验证：最小回归 `44 passed`；两独立锚 `2677.7953638343815` / `3561.080964207054` 零漂，记录 `anchor_parity_after_identity_cluster_gate.json`；artifact hash 顶层与 Phase D 均不含 `._*`、`__pycache__`、`.pytest_cache`、`.tasks`；protected solver/TeX diff 为空。

- [M1] 2026-07-05（Codex 执行 E2 route-compression hard-subset probe，结论不支持解冻）：按 user 指令新增显式诊断组件 `RELAXED_ROUTE_COMPRESSION`，默认 `ROUTE_ELIMINATION` 行为保持严格不变；只改 ALNS 路线压缩/组件 flag 与测试，未改 `cost.py`/`check.py`/`search/evaluation.py`/`prices.py`/TeX、模型、电池或 baseline。前置回放确认当前 Tier1 只是 `T3_MATERIAL_READY` 素材闭合，不是 ALNS 全胜；A0=`alns_e2_throughput+LOCAL_SEARCH` 对 LNS 为 6 胜 / 16 负 / 1 平，mean gap `-0.007131889956741538`，median gap `-0.007964139782654098`。对 16 个 hard subset 用现有 raw solution 重新 `evaluate()` 分解，A0-LNS mean total delta `23.77856434493994`、route_count delta `0.3125`、fixed-cost delta `25.0`，root cause 确认为 route/fixed-cost 主导。随后仅跑 hard subset × A1/A2 × seeds1-3：96/96 新行全部 `OK` 且 `16000/16000`，0 infeasible、0 under-eval、0 identity HALT；两独立 ALNS 锚 `2677.7953638343815` / `3561.080964207054` 均零漂。最终 `baselines/e2_alns/route_compression_probe_20260705/decision.json` verdict=`ROUTE_COMPRESSION_FIX_NOT_SUPPORTED`：A1 虽把 mean total delta 降到 `11.972268898801172`、mean route_count_delta_vs_a0=`-0.1041666666666666`，但 hard subset 仅 4 胜 / 12 负、mean gap `-0.013073362074800225`；A2 为 0 胜 / 16 负、mean gap `-0.016189810717862328`。因此不得解冻正式 T3，不得写“ALNS 战胜 LNS”或“创新算子有效”；最多可写路线压缩假设被严格测试但未过支持门。验证：指定回归 `51 passed, 1 skipped`，protected diff 为空，`git diff --check` 为空，产物目录无 `._*`/`__pycache__`/`.pytest_cache`/`.tasks`。
- [M1] 2026-07-05（Codex 执行 route-compression trace audit，纠偏为源码/trace 优先，**无新算法/无 oracle/非正式 T3**）：先按 user 新裁决放弃已起草的 LNS-to-ALNS oracle 方向，未保留 oracle 脚本/测试；Git hygiene 已清理 `.git` 内 AppleDouble sidecar，并 `git fsck --no-progress` 复验 code=0、无 `bad sha1`/`._` 错误。按“修复提交”要求从 `6dac85c76` 恢复 `route_compression_probe_20260705/.tasks` runner traces，单独提交 `ae450285c Restore route compression runner task traces`；这些 `.tasks` 只作恢复痕迹，不进入本轮 evidence hash。新增只读诊断 `baselines/e2_alns/route_compression_trace_audit.py` 与产物 `baselines/e2_alns/route_compression_trace_audit_20260705/`，只读 `decision.json/profile_summary.csv/cost_decomposition.csv/raw_runs.csv`、Phase D raw、以及 `winner_operators.py/alns_wouda.py/metaheuristic_baselines.py/local_search.py`，不跑全量、不改 solver 语义、不继续调 `RELAXED_ROUTE_COMPRESSION`。审计产物：`support_gate_breakdown.csv` 显示 A1/A2 失败门都是 `mean_gap_positive|wins_at_least_losses`（A1 4胜/12负，A2 0胜/16负；无 under-eval/infeasible/identity/anchor 问题）；`alns_operator_contribution.csv` 144 行明确 A0/A1/A2 winner history 缺 route_count，route_elimination route-count drop 与 revert reason 均为 `UNKNOWN`；`lns_best_update_sources.csv` 2174 行显示 LNS best-update family count 为 `lns_destroy_repair=1678`、`lns_vehicle_type_mutation=415`、`lns_scan_initial=33`、`shared_warm_start=48`。`trace_limitations.md` 记录最小 instrumentation patch：给 `_winner_history_entry()` 补 route_count/signature，诊断 flag 下记录 candidate route_count、hard violation、accepted/revert_reason，以及 local_search before/after route_count。`next_action.md` 只基于 trace 推荐：先审计 LNS destroy/repair acceptance 与 scheduler 行为；不得推荐 oracle/ejection-chain/cross-exchange 等当前源码无证据的新算子。边界：本轮结论只否定“旧 route_elimination + relaxed acceptance + allow-new-route retry”，不否定整个路线压缩方向。
- [M1] 2026-07-05（Codex 执行 LNS scheduler/acceptance trace audit，**最小 instrumentation / 非正式 T3 / 不改 protected 语义**）：新增诊断 flag `SETP_ALNS_CRUSH_TRACE_DIAGNOSTIC=1`，默认关闭时 ALNS history/schema 维持 legacy；开启时 `_winner_history_entry()` 补 `route_count/signature`，ALNS candidate trace 记录 route count 前后、accepted/accepted_worse/best_improved、hard violation、revert reason、local_search 前后 objective/route_count；LNS `_run_lns` 记录 `scan_initial/vehicle_type_mutation/strong_bridge/fallback_relocate` path、destroy/repair、fallback_used、acceptance、temperature、delta_obj、route_count delta。新增脚本 `baselines/e2_alns/lns_acceptance_scheduler_audit.py` 与产物 `baselines/e2_alns/lns_acceptance_scheduler_audit_20260705/`，只跑 hard subset 16 实例 × seeds1-3 × `{LNS_TRACE,A0_TRACE}`，eval_budget=4000，96/96 rows 全部 `OK`，`decision.json` verdict=`TRACE_AUDIT_COMPLETE`、`diagnostic_only=true`、`formal_t3=false`。核心诊断：LNS best_improved by path 为 `strong_bridge=1334`、`vehicle_type_mutation=339`、`scan_initial=33`、`fallback_relocate=19`，说明先前 `lns_destroy_repair` 主力并非主要混入 fallback relocate，而是 strong bridge；ALNS candidate revert summary 为 `unchanged=103460`、`candidate_usable=87751`，A0 仍需在 candidate trace 层继续拆 acceptance/repair scheduler。`next_action.md` 固定格式给出下一步：`problem_symptom -> LNS advantage concentrates at strong_bridge`，`minimal_remedy -> audit repair/acceptance/scheduler with this trace split; do not design new operators yet`。验证：新增/相关回归 `52 passed`，script smoke 32/32 OK，正式诊断 96/96 OK；protected diff 为空，`git diff --check` 为空；artifact hash 排除 `._*`/`__pycache__`/`.pytest_cache`/`.tasks`；运行中生成 AppleDouble 后已清理，并以 `/tmp` clean fsck log 复验无 `bad sha1/error/fatal/missing/corrupt`（只剩 dangling 对象警告）。
- [M1] 2026-07-06（Codex 执行 A3 strong-bridge backend alignment 第一刀算法探针，**诊断 profile / 非正式 T3 / 不写胜负结论**）：新增显式 flag `SETP_ALNS_CRUSH_STRONG_BRIDGE_BACKEND=1`，默认关闭；开启时仅对 `random_customer_removal`/`shaw_related_removal`/`worst_customer_removal` × `greedy_insert_repair`/`regret2_insert_repair`/`regret3_insert_repair` 的 3×3 pair 使用 LNS 已确证的 `_apply_strong_alns_destroy_repair()` 作为 ALNS candidate backend，后续仍走现有 local search、hard violation、revert、acceptance、selector update 和 trace；不改 q-size、scheduler、acceptance、cost/check/evaluation/prices/feasible_repair/TeX，不削弱 LNS，不继续调 `RELAXED_ROUTE_COMPRESSION`。新增脚本 `baselines/e2_alns/strong_bridge_backend_probe.py` 与产物 `baselines/e2_alns/strong_bridge_backend_probe_20260705/`；复用 `lns_acceptance_scheduler_audit_20260705` 同预算 LNS reference，跑 hard subset 16 实例 × seeds1-3 × `{A0_TRACE,A3_STRONG_BRIDGE_BACKEND,LNS_TRACE_REFERENCE}`，144/144 rows `OK`、0 fail、0 infeasible、0 under-eval，`decision.json` 标记 `diagnostic_only=true`、`formal_t3=false`、`algorithm_win_loss_claim=false`。最终 verdict=`A3_STRONG_BRIDGE_BACKEND_NOT_SUPPORTED`：A3 虽降低 `unchanged_rate` 极小幅（A0 `0.5410776577` → A3 `0.5409924375`）并提高 `best_improved_rate`（`0.0109564826` → `0.0137652584`），但 mean gap vs LNS 变差（A0 `-0.0027550841`，A3 `-0.0126896593`），且 A3 vs A0 为 20 胜 / 24 负 / 4 平，未过 `wins_vs_A0>=losses_vs_A0` 与 mean-gap 改善门。下一步按 `next_action.md` 停止 backend tuning，拆成 q-size、scheduler、acceptance 三个单变量探针；不得在同轮叠加改动。验证：新增/相关回归 `59 passed`，运行中 AppleDouble sidecar 已清理，后续需在收尾验证中复查 protected diff、artifact hash 与 fsck。
- [M1] 2026-07-06（Codex 修正 A3 backend probe profile alignment，**只修诊断口径 / 非正式 T3 / 不改 solver 语义**）：源码预检确认旧 A3 runner 的 A0/A3 只调用 `e2_alns_throughput_flags()`，而该 flag 继承 `SETP_ALNS_CRUSH_LOCAL_SEARCH=0`；当前 T3 main profile 明确是 `alns_e2_throughput+LOCAL_SEARCH`。因此旧 `A3_STRONG_BRIDGE_BACKEND_NOT_SUPPORTED` 只回答 backend-only 诊断，不足以代表当前主 profile。已将 `baselines/e2_alns/strong_bridge_backend_probe.py` 改为五臂：`A0_THROUGHPUT_ONLY`、`A3_BACKEND_ONLY`、`A0_MAIN_LOCAL_SEARCH`、`A3_BACKEND_LOCAL_SEARCH`、`LNS_TRACE_REFERENCE`，并写出 `flags_by_profile.json` 锁定四个 ALNS profile 的 `LOCAL_SEARCH` 与 `STRONG_BRIDGE_BACKEND` flag；`decision.json` 分开报告 `BACKEND_ONLY` 与 `MAIN_LOCAL_SEARCH`，顶层 verdict 以 `MAIN_LOCAL_SEARCH` 为准。新产物 `baselines/e2_alns/strong_bridge_backend_probe_local_search_check_20260706/`，240/240 rows `OK`、0 fail、0 HALT。结果：backend-only 复现旧负结论（A3 vs A0 = 20 胜 / 24 负 / 4 平，mean gap `-0.0126896593` vs `-0.0027550841`）；corrected main-local-search 仍不支持（A3 vs A0 = 16 胜 / 24 负 / 8 平，A3 mean gap `-0.0136735948` vs A0 `-0.0043852530`，A3 unchanged_rate `0.5445287697` 高于 A0 `0.5219023911`，虽 best_improved_rate `0.0139807836` 高于 A0 `0.0113382005`）。最终 verdict=`A3_BACKEND_LOCAL_SEARCH_NOT_SUPPORTED`，所以不得升 8000 eval，不得进入 Tier1；下一步才允许单变量拆 q-size / scheduler / acceptance。验证：targeted regression `61 passed`，`git diff --check` 为空，protected solver/TeX diff 为空，artifact hash 排除 `._*`/`__pycache__`/`.pytest_cache`/`.tasks`；新 `alns_candidate_trace.csv` 由 Git LFS 管理。源仓库位于外置盘，`com.apple.provenance` 会反复生成 `.git/objects` AppleDouble，故最终以无硬链接 `/tmp` clean clone 的 `git fsck --no-progress` 空日志作为对象完整性证据，并在源仓库提交前清理 sidecar。
- [M1] 2026-07-06（Codex 执行 ALNS selector pathology audit，**只读根因审计 / 非正式 T3 / 未改 solver**）：新增 `baselines/e2_alns/selector_pathology_audit.py` 与产物 `baselines/e2_alns/selector_pathology_audit_20260706/`，只读 `strong_bridge_backend_probe_local_search_check_20260706` 的 A0/A3 trace 与 `lns_acceptance_scheduler_audit_20260705` 的 LNS trace；不跑新算法、不改 q-size/backend/acceptance/local search。审计确认 `AlphaUCB([20,8,2,0.05], alpha=0.08)` 的确定性 `argmax` selector 存在强 pair concentration 证据，`decision.json` verdict=`SELECTOR_PATHOLOGY_SUPPORTED`，四个支持门均为 true。关键数值：`A0_MAIN_LOCAL_SEARCH` normalized entropy `0.5479450509`、top1/top2 share `0.5102399431/0.7646329728`；`A3_BACKEND_LOCAL_SEARCH` normalized entropy `0.6103431772`、top1/top2 share `0.4624746981/0.6931550125`；LNS strong_bridge normalized entropy `0.9812637639`、top1/top2 share `0.1477880356/0.2896312503`。A0/A3 best-improved top3 share 分别 `0.8607011070` / `0.8432472877`，明显高于 LNS `0.5487256372`。静态 value bound 显示未试 pair value 在 iter=100/1000/4000/16000 仅 `1.607626/1.743438/1.814582/1.880018`，远低于 reward=8/20，因此早期高 reward pair 很难被探索项追上。结论：下一步允许进入单变量 scheduler 修复探针 `SETP_ALNS_CRUSH_BALANCED_SELECTOR=1`；仍不得升 8000、不得 Tier1、不得继续 backend/route-compression tuning。
- [M1] 2026-07-06（Codex 执行 A4 balanced-selector 单变量修复探针，**诊断 profile / 非正式 T3 / 不改 backend-q-acceptance**）：在 `resetp_alns` 新增 `BalancedAlphaUCB`，并通过显式 flag `SETP_ALNS_CRUSH_BALANCED_SELECTOR=1` 接入 winner ALNS；flag 默认关闭，legacy `AlphaUCB` 行为不变。A4 仅改 operator selection：每个合法 destroy/repair pair 先 warmup 10 次，之后保留现有 AlphaUCB exploit，同时用 `epsilon=0.10` 均匀探索合法 pair；不改 q-size、strong bridge backend、acceptance、local search 深度、cost/check/evaluation/prices/feasible_repair/TeX，不削弱 LNS。新增 runner `baselines/e2_alns/balanced_selector_probe.py` 与产物 `baselines/e2_alns/balanced_selector_probe_20260706/`，hard subset 16 实例 × seeds1-3 × `{A0_MAIN_LOCAL_SEARCH,A4_BALANCED_SELECTOR_LOCAL_SEARCH,LNS_TRACE_REFERENCE}`，eval_budget=4000，144/144 rows `OK`、0 fail、0 HALT。最终 verdict=`A4_BALANCED_SELECTOR_PROMISING`：A4 mean gap vs LNS `0.00048527397136235187` 优于 A0 `-0.004385253026182812`；A4 vs A0 为 23 胜 / 18 负 / 7 平；normalized entropy 从 `0.5479450509066646` 升到 `0.8227620351716587`；top1/top2 pair share 从 `0.5102399430998055/0.7646329728259733` 降到 `0.24319320523414537/0.4628254343482945`。A4 best_improved_rate 低于 A0（`0.009241341798897525` vs `0.011338200531347405`），但最终均值更好，说明“更多局部 best update”不是目标，pair diversity 改善更关键。下一步只能升 8000 eval hard subset 复验 A4；不得直接 Tier1，不得叠加 backend/q-size/acceptance。
- [M1] 2026-07-07（Codex 执行 A5 selector sprint 并行修复探针，**诊断 profile / 非正式 T3 / 只改 selector scheduling 层**）：基于 `SELECTOR_PATHOLOGY_SUPPORTED` 与 `A4_BALANCED_SELECTOR_PROMISING`，新增 `EpsilonDecayAlphaUCB`、`ThompsonPairSelector`、`SoftmaxAlphaUCB` 三个诊断 selector，并保留 `BalancedAlphaUCB`；默认 legacy `AlphaUCB` 不变，四个 selector flags 互斥，诊断 trace 只追加 selector 标量信息。新增 runner `baselines/e2_alns/selector_sprint_probe.py` 与产物 `baselines/e2_alns/selector_sprint_probe_20260706/`，hard subset × seeds1-3 按 4000→8000→16000 自动晋级/淘汰，最终 768/768 rows 全部 `OK`、0 fail、0 HALT，`decision.json` verdict=`SELECTOR_SPRINT_8000_SUPPORTED`。4000 阶段 A4/A5/A6/A7 均过门；8000 阶段只有 `A6_THOMPSON_SELECTOR_LOCAL_SEARCH` 与 `A7_SOFTMAX_SELECTOR_LOCAL_SEARCH` 过门；16000 阶段二者均未过门（A6 mean gap vs LNS `-0.006035769122260031`，wins/losses vs LNS 14/28；A7 mean gap `-0.0026802757975461834`，wins/losses vs LNS 19/21）。因此不得直接 Tier1，也不得继续微调 selector 参数；下一步只允许做一次按 scale/category 的失败分析，或转入新的源码级根因审计。验证：targeted regression `71 passed`，`git diff --check` 为空，protected solver/TeX diff 为空，artifact hash 排除 `._*`/`__pycache__`/`.pytest_cache`/`.tasks`。
- [M1] 2026-07-07（Codex 执行 selector sprint failure analysis + structural rescue probe，**只读预检后单变量结构探针 / 非正式 T3 / 不改 protected 语义**）：新增 `baselines/e2_alns/selector_sprint_failure_analysis.py` 与产物 `baselines/e2_alns/selector_sprint_failure_analysis_20260707/`，只读 `selector_sprint_probe_20260706`，确认 A7 16000 residual loss 主要仍落在 `route_count/cost_fix` 且存在 family/scale 模式，`decision.json` verdict=`STRUCTURAL_RESCUE_PRECHECK_SUPPORTED`。随后新增三个互斥诊断组件：`SETP_ALNS_CRUSH_ROUTE_POOL_RECOMBINATION`、`SETP_ALNS_CRUSH_RVND_SWAPSTAR`、`SETP_ALNS_CRUSH_ELITE_ARCHIVE_RESTART`，分别实现 route pool recombination、RVND/SWAP*-lite late-stage intensification、elite archive restart；默认关闭，多开 HALT，不改 q-size/backend/acceptance/cost/check/evaluation/prices/feasible_repair/TeX。新增 runner `baselines/e2_alns/structural_rescue_probe.py` 与产物 `baselines/e2_alns/structural_rescue_probe_20260707/`，复用同预算 A0/A7/LNS reference，仅运行结构分支；4000 阶段 288/288 rows 全部 `OK`、0 fail、0 under-eval，最终 verdict=`STRUCTURAL_RESCUE_ALL_FAILED`，无分支升 8000/16000。A8/A10 虽有轻微 mean gap/wins 改善，但 A8/A9/A10 的 route_count_delta_vs_lns 与 cost_fix_delta_vs_lns 均未优于 A7，结构救援没有命中预注册 route/fixed-cost gate；因此不得 Tier1，不得继续叠加结构组件。下一步应停止纯 ALNS 结构救援线，转向 vehicle_type/fleet-charge 结构审计，或由 user 决定是否重新定义 hybrid ALNS-LNS / HGS-style 主算法。验证：targeted regression `69 passed`，`git diff --check` 为空，protected solver/TeX diff 为空，artifact hash 排除 `._*`/`__pycache__`/`.pytest_cache`/`.tasks`。
- [M1] 2026-07-08（Codex 执行 A11 global-order repack + A12 fleet-charge co-repair 探针，**诊断 profile / 非正式 T3 / 停止旧 selector-backend-route_pool 小修线**）：按 user 新裁决停止 A8/A9/A10 与 selector/backend/q/acceptance/`RELAXED_ROUTE_COMPRESSION` 调参，新增公共 `order_decoder.py`，将 LNS `_order_to_solution`/cached append/route feasibility/distance 等解码逻辑抽为 `OrderDecodeContext` 下的可复用函数；`metaheuristic_baselines.py` 只保留兼容 wrapper，不改变 LNS 行为。新增 A11 `SETP_ALNS_CRUSH_GLOBAL_ORDER_REPACK` 与 A12 `SETP_ALNS_CRUSH_FLEET_CHARGE_COREPAIR`，默认关闭且互斥；A11 在停滞/late-stage 从 current/best 提取 customer order，做 current/two-opt/double-bridge/depot-group-shuffle 后用公共 order decoder 重解码，并经现有 `check_solution/evaluate` 入既有 acceptance；A12 对 current/best route 尝试 CV/EV flip 和 charging replay，按 total cost 判定并记录 fuel/electric/carbon/fixed delta。新增 runner `baselines/e2_alns/global_repack_fleet_charge_probe.py` 与产物 `baselines/e2_alns/global_repack_fleet_charge_probe_20260707/`；修正中途发现的 no-proposal 早停 bug 后重跑，最终 432/432 rows `OK`、0 fail、0 HALT，verdict=`GLOBAL_REPACK_FLEET_CHARGE_4000_ONLY`。A11 4000 过门：mean gap vs LNS `0.006961686839843399` 优于 A7 `0.0038323386258464383`，A11 vs A7 为 17 胜 / 14 负，route_count_delta_vs_lns 从 A7 `0.10416666666666667` 降到 `0.041666666666666664`，cost_fix_delta_vs_lns 从 `8.333333333333334` 降到 `3.3333333333333335`；A12 4000 未过门，route/fixed gap 与 A7 相同。A11 自动升 8000 后未过门：mean gap 仅略优于 A7（`-0.0009648932794966394` vs `-0.0010006201218705217`），但 A11 vs A7 为 2 胜 / 4 负，route_count_delta_vs_lns 与 cost_fix_delta_vs_lns 均回到 A7 水平（`0.125`、`10.0`），所以不跑 16000、不进 Tier1。并行新增 OpenAlex 文献根审计 `baselines/e2_alns/source_literature_root_audit.py` 与 `source_literature_root_audit_20260707/`，产出 300 行矩阵、30 篇 deepread 摘要和源码映射。验证：targeted regression `60 passed`，`git diff --check` 为空，protected solver/TeX diff 为空，artifact hash 不含 `._*`/`__pycache__`/`.pytest_cache`/`.tasks`；结论边界：A11 说明 global-order repack 在小预算能命中 route/fixed 病灶但不能稳定放大，A12 单独无效，下一步不得继续小修 ALNS patch line，应按 `next_action.md`/必要时 `STOP_OR_REDEFINE` 讨论 hybrid LNS/HGS-style 主算法或接受 LNS 第一梯队现实。
- [M1] 2026-07-08（Codex 执行 route-packing reachability audit + A13 LNS policy parity scaffold，**诊断 / 非正式 T3 / 未启动 A14 或 hybrid**）：按 user 最新计划先做 reachability gate，而不是直接实施 hybrid。新增 `baselines/e2_alns/route_packing_reachability_audit.py` 与产物 `baselines/e2_alns/route_packing_reachability_audit_20260708/`，只读 `selector_sprint_probe_20260706/`、`global_repack_fleet_charge_probe_20260707/` 与 checkpoint solution，重放 A7/LNS/A11 订单和 mixed route blocks，经公共 `OrderDecodeContext`、`check_solution()`、`evaluate()` 验证 decoder reachability。正式 reachability run 为 21 个 A7-losing rows，其中 14 个可达到 LNS route count 或把 route-count delta 至少砍半，`reachable_fraction=0.6666666666666666` >= 0.6，`decision.json` verdict=`ROUTE_PACKING_REACHABILITY_SUPPORTED`，`next_stage_allowed=A13_LNS_POLICY_KERNEL`，artifact hash 清单干净且不含 AppleDouble/pycache/pytest/tasks；未写 `STOP_OR_REDEFINE.md`。在该 gate 通过后，新增 `solver/src/setp_solver/search/lns_policy_kernel.py` 与 runner `baselines/e2_alns/lns_policy_kernel_probe.py`，抽出 A13 LNS policy kernel scaffold：angle-scan initial、35% vehicle-type mutation、strong-bridge destroy/repair、fallback order relocate decode、Metropolis current wandering、`temperature *= mu`、每 1000 iter reheating，并保留 `LNS_REFERENCE` / `A13_LNS_POLICY_KERNEL` profile；LNS baseline 行为未改。只跑了一个 smoke：`/tmp/resetp_a13_smoke`，budget 64、seed1、1 个实例，2/2 rows OK，verdict=`A13_PARITY_SMOKE_SUPPORTED`；这不是正式 A13 parity gate，不能据此实施 A14 或 hybrid。验证：无 `PYTHONPATH` 的指定 pytest 首次 collection 因本地 import path 失败；随后用 `PYTHONPATH=solver/src PYTHONHASHSEED=0 /opt/anaconda3/bin/python3.13 -m pytest solver/tests/test_order_decoder.py solver/tests/test_e2_alns_throughput.py solver/tests/test_e2_final_closure.py solver/tests/test_route_packing_reachability_audit.py solver/tests/test_lns_policy_kernel.py -q` 得到 `55 passed`；`git diff --check` 为空，protected diff 对 `cost.py/check.py/search/evaluation.py/prices.py/search/feasible_repair.py/TeX` 为空。下一步只能运行完整 A13 hard-subset parity probe；若 verdict 不是 `A13_PARITY_SUPPORTED`，必须停止并写 `parity_failure_report.md`，不得进入 A14/hybrid。

- [M1] 2026-07-09（算例拨正 + ALNS 完全独立执行）：**正式主算例 = L-main v2 九阶三班倒 only**（`L-main-threeshift-{10,15,20,25,50,75,100,150,200}c-01`，共 9 个）；生成缺失 10/15/20/25 三班倒（e2 池 12 个 -01/-02/-03 warmstart OK），拷入 `models/data_bundle/generated_instances/L-main/`；旧混族 23 归档为 `L-main_mixed23_archive_20260709`。注册表 `solver/src/setp_solver/search/instance_registry.py`；`alns_crush.INSTANCE_DIRS` / `e2_final_closure` Tier1=9 对齐。**ALNS 完全独立包**落地 `solver/src/setp_solver/algorithms/resetp_alns/`（runtime/kernel/operators/support 私有拷贝；禁止依赖 `Reference Algorithm/ALNS-7.0` 与第三方 `alns`）；`search/alns_wouda`/`winner_operators`/`resetp_alns` 仅为 shim。PROVENANCE+memory：`docs/handoff/memory/instance-lineage.md`、`resetp-alns-independence.md`。验收：独立测试 9 passed；相关回归 66 passed（cost/check/throughput/bridge/independence）；L-main-threeshift-10c-01 seed1 budget32 smoke feasible、best_cost≈436.81。**下一步**：在 9 阶三班倒 + 独立包上做 E2 性能（相对 LNS/第二名冲 ~5%），旧 T3 混族数字仅档案。

- [M1] 2026-07-09（挂帐 + 中控台）：**E2 性能冲刺（约超第二名 5% / A14 hybrid）挂帐暂停**，不阻塞中控台建设。已提交并推送上一轮底座：`fca004f` → `origin/codex/reporting-pipeline`（L-main 9 阶三班倒正式集 + `algorithms/resetp_alns` 完全独立；实例数据仍在盘上 `models/` gitignore）。新增根目录中控体系：`CONTROL_CONSOLE.py` + `CONTROL_CONSOLE.yaml`（batch/select/range 选实验与图表）+ `PARAMETERS_CONSOLE.yaml`（算法参数/路径/图风格）+ `input/`（按类型）+ `output/`（按实验与类型）+ `console/` 实现 + `.vscode/launch.json` 一键 dry-run/self-check。默认 `dry_run: true`；预检正式 9 算例与保护文件；运行日志写 `output/by_type/logs/`。验收：`--list/--self-check/--dry-run` 均 DONE OK。

- [M1] 2026-07-09（挂帐恢复：E2 A13 full parity 续跑）：用户启动挂帐任务。事实：A13 4000/8000 hard-subset 已 192/192 OK 且 A13 vs LNS 全 tie（policy/formal 两组 gap=0），但 `decision.json` 仍为 `A13_PARITY_SMOKE_SUPPORTED`（`full_gate` 未闭合），`b16000` checkpoint 仅 60/192。已在后台续跑 `lns_policy_kernel_probe.py --budgets 4000,8000,16000 --workers 3` 复用既有 checkpoint，目标 `A13_PARITY_SUPPORTED`（576 rows）后才允许 A14 hybrid；任务卡 `docs/handoff/codex_prompts/20260709_e2_a13_full_parity_resume.md`。不改 cost/check/evaluation。

- [M1] 2026-07-10（独立根因接管第一轮：先查问题、未改搜索语义）：L-main v3 已按 `LMAIN_V3_READY` 激活，formal 入口修复不存在的 200c 名称、E0 23/9 行漂移并加 manifest+audit hash 门。独立化提交 `fca004fad` 暴露三处真实运行错误并已用失败测试锁定后修复：`alns_core` bundle loader 名称错误、独立 strong-bridge 缺 `json/hashlib/_ThinPolicy/dataclass`、DR 入口调用不存在的 `_lazy__lazy_run_candidate`。`pyflakes algorithms/resetp_alns` 当前无 undefined name。根因报告：`docs/handoff/m1_independent_root_cause_20260710.md`。

  新的当前-v3可复现实验没有支持“ALNS 根本造不出车队结构”。`m1_structure_reachability_20260710` 在实际55客户、280kWh、冻结起点下判定 `ALNS_SCHEDULER_OR_MULTI_STEP_BARRIER`：CURRENT_THROUGHPUT 从 CV 起点 90 次得 42 个结构候选/28 个立即改善，从共同一EV起点得 24/24。随后 `m1_scheduler_realization_20260710` 做 20 个同起点同种子400-eval配对，判定 `DEFAULT_SELECTOR_STARVATION_CONFIRMED`：共同一EV起点下默认 AlphaUCB 5/5 零次 vehicle-type swap、dominant pair share 平均0.9964、最终全停1条EV；balanced coverage 5/5 更低成本，平均少178.873473，最终6--8条EV。CV起点 balanced 3胜2负，说明覆盖是必要修复但不是最终算法。旧 selector sprint 同一产物在16000仍输LNS，所以不得把短预算通过包装为长期解决。

  证据口径同步降级：旧所谓 fair SA 给 SA 用 shared-one-EV 起点、winner 却回默认构造，不公平；当前 shared-one-EV 构造还会从8条路线/成本1730.390718变成9条/1811.837477，并非中性换车。仓库未找到“ALNS验证集强3.23%”的原始可追溯行；09y Stage B 全算法逐位返回构造解只证机制存在。A13 full parity 现已实际闭合为576/576、`A13_PARITY_SUPPORTED`，只证LNS kernel等价，不证当前v3性能或独立ALNS创新。下一步不继续旧route patch：先冻结唯一M1入口与公平起点合同，再做动作家族最低覆盖（去掉vehicle swap×三repair重复格）和固定路线车队组成机会图；只有当前v3的4000-eval小中大三规模过门，才允许升8000/16000。若长期仍输LNS，ALNS降级普通求解器或诚实改名hybrid，不制造成功。

- [M1] 2026-07-10（根因路线第一轮执行结果，**未晋级算法 / 不写胜负**）：minimum coverage 的400-eval窄门槛为 `MINIMUM_COVERAGE_400_SUPPORTED`（相对默认10组6胜4负、均值改善且每组均触发换型），但源码 trace 显示反向锁死：部分运行400步中 `vehicle_type_swap` 达300次，路线数仍固定8，CV起点仅2胜3负且均值略差。因此该分支判为**表面通过、不得推广**，不升4000，不继续selector参数线。`m1_scheduler_realization_20260710/solutions/` 已保存30个真实最佳解，source hash 与 commit `ab4be1f3` 绑定。

  固定路线机会图 `m1_fleet_opportunity_20260710` 精确枚举6个已保存7--8路线集合的全部CV/EV组合。280kWh下5/6固定路线最优为全EV，1/6为7EV+1CV（全EV不可行），其余集合最佳混合比全EV贵0.68%--1.64%，verdict=`FIXED_ROUTES_ROUTING_DEPENDENT`；80kWh下6/6固定路线最优为1--4条EV的混合，但这些路线来自280搜索，只证存在性。说明当前v3的混合目标依赖路线分组，稳定45% EV不是普遍物理目标。旧A11路线重排与A12车队/充电修复曾互斥单测，下一条唯一值得做的改进是先重分路线、再做车型/充电闭包的顺序联合 headroom；先不接搜索，跨小中大多来源路线有稳定headroom才实现。

  同起点SA短复核 `m1_fair_sa_recheck_20260710` 为 `SA_SHORT_ADVANTAGE_NOT_REPRODUCED`：当前v3、280kWh、两个相同起点、seeds1--5、400 eval下，SA对默认ALNS为0胜10负，mean(SA-ALNS)=+430.767930；SA四类动作均有调用且接受大量变化，但路线数不降。旧SA领先不能迁移到当前v3；该结果仍非长预算正式排名。下一步重大合同仍是冻结唯一M1入口；在此之前只允许联合headroom和同起点4000诊断，不允许包装算法成功。

- [M1/E2] 2026-07-10（预算根因，覆盖此前T3主变体口径）：小中大三档×ALNS/LNS/SA×seeds1--3 的联合headroom为14/27，低于预注册60%门，`JOINT_REPACK_FLEET_HEADROOM_PARTIAL`，不接入主循环；单独路线重排仅2/27改善。反复车型闭包为10/27改善，其中T3 ALNS 3/9仍漏约209.16/241.95/570.19，说明固定路线车型机会未收口。更严重的是 `throughput+LOCAL_SEARCH` 的100-eval探针：9次T3额外做11547次完整Solution evaluator/checker调用却未计预算，单次760--2164，mean有效/报告比13.83，verdict=`LOCAL_SEARCH_BUDGET_UNDERCOUNT_CONFIRMED`；旧同预算T3和主变体结论撤销，local-search在按次计费前必须关闭。动态碳算子旧 verdict=`CARBON_OPS_WEAK` 与源码审计一致，静态E2关闭。实际34客户档num_ev=0触发的SA换型崩溃已改为安全拒绝。下一步只修严格预算与低频车型收口，再做小/中/难三档同起点短门，不跑九档全量。

- [M1/E2] 2026-07-10（staged-chain九阶快速门，非正式T3）：确认400→4000退化的实现根因是 chain selector/acceptance 每400步重启但 destroy progress 仍按总预算计算；改为阶段内 progress 后，221客户 seed1/4000 从5536.079448降至5025.535884。114客户17→16离线审计中，17条路线逐条删除×3 repairs 共51次全退回，最弱路线客户在其余路线全位置不可插，支持需要多路线同步重组。新增独立入口 `run_staged_chain_alns()`，同一总预算严格拆 `400 regular + 3200 strong repair + 400 regular`。`m1_staged_chain_gate_20260710` 在L-main v3九阶、seed1、4000 eval、同CV起点/280kWh/referee下18保存解完整复算成本差0、全零违规；ALNS/LNS总成本34328.746819/36379.445968，aggregate gain=5.636972%，总时间517.203761/1869.268814秒，runtime gain=72.331226%，成本7胜1平1负、时间9胜。边界：单种子/4000 diagnostic only，逐实例百分比均值仅3.035910%，只有2/9单实例过5%；322客户档反输10.185173%，普通4000也输约9%，是真失败。不得写正式冠军；下一门只跑322失败档+中档+最难档少量多种子4000，再决定8000/16000。相关回归61 passed；protected合同未改。

- [M1/E2] 2026-07-10（322客户多种子纠偏）：`m1_staged_chain_150c_multiseed_20260710` 补齐 staged-chain ALNS/LNS seeds1--3、同CV起点、4000 eval。ALNS=`9442.467022/8411.767895/7711.826221`，LNS=`8569.634861/8879.785729/8856.384620`，ALNS 2胜1负；均值8522.020379对8768.601737，低2.812094%，平均时间低78.079697%。seed1反输10.185173%未在seed2/3复现，后两者分别胜5.270602%/12.923540%；因此旧“322结构性失败”降级为“种子稳定性不足”，verdict=`STAGED_CHAIN_150C_MULTISEED_PARTIAL`。仍不能写该档稳定超5%，九阶正式推广继续被多种子/16000门阻塞。

- [M1/E2] 2026-07-10（正式稳定性入口预检）：新增明确身份的公开入口 `run_staged_alns_lns_hybrid()`，算法名固定为 `staged ALNS-LNS hybrid`，不覆盖旧 `run_winner_kernel`；新增可恢复/逐行落盘 runner `m1_staged_hybrid_stability_gate.py`，冻结任务为 `{100c,150c,200c} × seeds1--5 × {hybrid,LNS}` 共30次、每次4000 eval、900s。场景显式为280kWh现代电池诊断覆盖，论文/prices默认仍为Goeke 80kWh；其余参数、checker/evaluator不改，碳感知算子关闭并写入每行与metadata。2-eval烟雾测试2/2 OK，任务身份/参数/落盘路径通过；尚未启动30次前不得写稳定性结论。
- [M1/全项目] 2026-07-11（用户纠偏 + 7.31 收稿止损）：研究计划仍按 E2判决→算法/场景冻结→其余正式实验→表图论文的依赖顺序推进；“并行”只指获准实验批次内部按 instance/seed/algorithm/parameter task 用 worker 池并行跑。当前30次结束后冻结E2方向：支持则仅做一次最小正式E2，不支持则降低算法主张；两种情况都不再开rescue分支。7月16日前永久关闭E2，7月24日前完成最小其余正式实验，7月28日起只做论文收口。详见 `docs/handoff/project_parallel_execution_plan_20260711.md`。
- [M1/全项目] 2026-07-11（建立白话总实验管理体系）：新增 `project_experiment_master_plan_20260711.md`、`project_board_20260711.md` 和实验任务卡模板。总计划把论文四类证据映射到E0--E7，明确每项“想证明什么/怎么跑/输出什么/什么算通过/失败怎么降级”，并将旧471次结果限定为可复用管线和历史锚。项目采用单科学问题WIP、阶段门、两周冲刺、每日一屏汇报和Not-To-Do管理；正式实验批次内部再按任务并行。
- [M1/E2] 2026-07-11（30次稳定性门完成，运行提交=`a51de1a2`）：100c/150c/200c × seeds1--5 × staged ALNS-LNS hybrid/LNS 共30次，280kWh诊断覆盖、4000 eval、900s上限；30/30 `OK`，全部零违规且实际评价数=4000，保存解、配对表、摘要、决策和hash清单完整。实例均值成本差 hybrid 相对 LNS：100c `+6.448%`、150c `+0.688%`、200c `+14.080%`；三档中位数均为正，平均运行时间分别低 `76.6%`、`79.2%`、`72.7%`；成本胜负 `11/15`。判决=`STAGED_HYBRID_STABILITY_SUPPORTED`。这只支持“280kWh诊断场景下的稳定性晋级”，不等于论文默认80kWh正式冠军，也不等于纯独立ALNS；下一步按用户门槛只允许考虑一次九梯度/4000正式门，禁止直接跳16000或继续rescue调参。证据目录：`baselines/e2_alns/m1_staged_hybrid_stability_20260710/`。
- [M1/E2] 2026-07-11（true-LNS-middle负例恢复短门）：执行提交`5be12db7`，27/27满1600评价、零违规，验证器确认解/hash/出处。候选相对staged全部9组平均改善1.536%、开发负例平均改善2.506%，只转回100c seed2一组，未达预注册至少2组，verdict=`TRUE_LNS_MIDDLE_SHORT_GATE_REJECTED`。发现1600代理固定400+800+400，核心LNS仅占50%，与正式4000的400+3200+400比例不一致；下一门只改为160+1280+160，总预算与门槛不变，仍不过则关闭该路线。
- [M1/E2/E3/E6] 2026-07-11（Claude 顾问只读审计：E2 成绩单漏计跨场服务费 + 协同/公平锁死根因链）：论文声明目标函数含 c^tr=95£/客户（TeX 假设7/式objective/参数表664行），但 E2 冻结 270 行全部未收此费——runner 未传 customer_home_depot，annotate 短路，冻结 checkpoint 200c seed1 hybrid best_cost=9959.411384897756 与 transship=0 复算逐位一致（FACT）。冻结解实际大量跨场：45 次合计 hybrid 278 / LNS 758 / PSO 703 / VNS 565 / GA 564 个跨场客户；200c LNS 单次 26-57 个，漏计 £2470-5415/次（约其单次成本 20-50%）。E2 内部对比仍公平（六算法同口径），6.845% 内部有效；但与论文目标函数不一致、与 E3 收费口径分叉。协同机制死因=算术锁死：95>80（整车派遣）>85（均需求客户收入），折合绕路 271km；车队为全局池（check.py 不分车场）、两场负载均衡，三重压制。公平是协同下游：E3 实测自然最小收益比 1.0000-1.0146，E6 现行 θ 网格 {0.90,0.95,1.00,1.02,1.04} 整段跳过 binding 区，按现合同跑注定空转（预言可证伪）。方向利好：what-if 补费后 hybrid 领先扩大至 ~22.6%（仅方向参考，非合法重评分）。建议 R0 三只读审计（全量跨场计数/280 冻结解修复后 checker 回放/E1-E2-E3 口径核对表）→ R1 用户拍板口径（A 重跑收费 E2 / B 保留+声明+54行小复核 / C 重新校准 c^tr 来源）→ R2 E6 必须重设计（θ 贴 binding 区 + c^tr 敏感轴）→ R3 E7 等口径决定 → R4 15 负唯一合规通道=R1-A 或遗留项9。全程零新搜索、零代码修改。详见 docs/handoff/claude_advisor_findings_20260711.md。
- [M1/全项目] 2026-07-11（Claude 顾问全面问题清查，配套跨场费审计）：新增《claude_advisor_issue_register_20260711.md》问题总登记册，以论文六卖点为基准全面清查。A 集群（目标函数保真度，同一类病）：E2 除漏计跨场费外，公平约束也是关的（θ=1 下冻结解可能不可行，待验证）、碳配额用 quota=0 而论文声明 CE=0.8E^base（排名不变但绝对成本口径不符）、TeX 仍写 B=80。B 集群（算法故事）：正式基线仅 GA/LNS/PSO/VNS 四个，已过健康门的 ACO/GA-VNS/GWO/IWD 未进正式表——用户"干过 GA-VNS"目标从未被正式检验；无 Wilcoxon p 值（零成本可补）；摘要现无任何算法创新主张，冻结 hybrid 需作为命名算法入正文。C 集群（叙事）：E4 碳价"30 倍阈值"故事建立在 80kWh/EV 弱场景，280 主场景 EV 已占 86%，E4 须换故事、管理启示章须重写（新立案）；E7 标题级承诺仍 HALT；顶刊标配（Goeke 原始实例外部验证/精确解对标）07-03 立案未决。D 集群（管理）：看板 ACCEPTED 与用户"过流程"标准错位，需逐实验声明哪些行喂论文表；7/31 时间预算数学上排不下，须做减法或重拍并行上限；图表任务仍 blocked on Zotero；HANDOFF 约 8 万 token 建议分层瘦身。附一页决策地图九项，零成本项（Wilcoxon/R0 审计/公平可行性验证/DATA_CONTRACT 确认）可立即执行。全程只读。
- [M1/E2项目4] 2026-07-11（精细碳算子微型门与真实短门通过）：基于本地Zotero原论文和现有源码，新增隔离的“两层”候选：外层高碳充电邻域/充电站清理，内层按完整充电区间、绕路、电费、占桩费和碳价统一重建站点与时刻；默认E2入口和旧粗糙碳算子不变。微型测试11项通过。真实短门`e2_refined_carbon_short_gate_20260711/`在提交`6d7167ed`上完成12/12、每次400评价、零违规、客户全覆盖，独立复算与hash通过；6/6配对有真实决策变化，合计EV间接排放-118.803kg、成本-77.294，但1组排放略升、1组成本+40.108，且部分路线/用电量同时变化。判`REFINED_CARBON_SHORT_GATE_SUPPORTED`，只允许继续设计，不授权4000正式门。下一步先执行零搜索跨实验合同审计，解决跨场费、公平、碳配额、电池和论文声明分叉，再由用户冻结主合同。
- [M1/全项目合同审计] 2026-07-11：零新搜索回放E2冻结270/270保存解，当前检查器全部零违规且免费跨场成本逐行匹配，故6.845690%内部算法比较不作废。最近车场归属补算确认hybrid/LNS跨场总数278/758；在200c上补回95英镑费用并使用E3同种子独立收益基准，冻结hybrid的theta=1可行数为0/5，费用取0也仅1/5，不能把E2旧解事后包装成完整公平模型。E1--E7实际合同矩阵确认电池、跨场费、公平、配额分叉，verdict=`BLOCK_FORMAL_E4_E7_PENDING_SUBMISSION_CONTRACT_DECISION`。证据：`baselines/contract_audit/e1_e7_submission_contract_20260711/`；大白话拍板单：`docs/handoff/e1_e7_submission_contract_decision_20260711.md`。用户确认前禁止项目4的4000门及E4--E7正式搜索。
- [M1/E2项目6只读补算] 2026-07-11：未启动新搜索，直接复算冻结280 kWh hybrid的45组aware/naive同路线配对，45/45两臂均零违规且路线、总用电、充电动作集合一致。排除结构性无EV的15c后，EV承担客户/需求/距离86.082%/87.031%/81.544%，实体车辆使用份额64.615%；CV仍承担13.918%/12.969%/18.456%，40组中29组混合、11组全EV、0组全CV。共929次、88711.268 kWh充电，其中911次、86737.553 kWh真实改变时刻；同路线加权碳强度137.979降至123.605 gCO2/kWh，40/40降碳，合计少1275.090 kg。关键边界：929次充电全部在车场，公共站为0，因此现证据支持“偏电车+大量车场谷时充电”，不支持“公共充电站选择已正式成立”。证据：`baselines/e2_alns/e2_280_fleet_charging_audit_20260711/`，verdict=`E2_280_EV_HEAVY_CHARGING_ACTIVITY_AUDITED`。
- [M1/E2项目5动态地基] 2026-07-11：把`dr-x86`已经验证过的动态实况合同按最小范围迁回当前M1线，没有合并整条分支。动态正常阶段不再调用旧`run_alns_wouda`，普通、随机、策略和安全备用路径统一进入项目内独立`run_resetp_alns`；车辆当前位置/在途进度、当前时间、剩余载重、剩余电量、正在进行的充电、物理车辆占用以及锁定客户的原车原顺序进入后续规划和检查。收尾不再另起求解器重建`FINAL_`路线，漏排客户会明确停止。动态与检查器52项测试、最终检查停机1项测试通过；加强检查后冻结E2的270行保存解重新只读回放仍全部零违规且成本一致。代码提交`96cbcbf7`，证据`baselines/e7_dynamic/m1_dynamic_truth_gate_20260711/`，判`M1_DYNAMIC_LIVE_STATE_CONTRACT_SUPPORTED`。大白话边界：现在只证明动态过程“不作弊、不瞬移、不偷偷用旧算法”；还没有证明动态需求更省钱，也没有证明动态需求能把充电推到低碳时段。项目5机制门仍等投稿统一合同冻结后再跑。
- [M1/E1--E7投稿合同机器闸门] 2026-07-11：旧`run_e4_carbon_sensitivity()`仍默认16000评价、明确关闭公平，并主要扫描线性碳配额常数，不能直接作为新的项目4正式入口。为防止再出现“跑完才发现实验规则不是一套”的问题，新增`setp_solver.search.submission_contract`硬门和4项测试；未被用户冻结的草案、客户归属hash漂移、完整模型公平未从搜索开始启用、非binding公平粗网格都会在正式开跑前被拒绝。推荐合同草案及L-main九档1425条固定客户归属已生成在`baselines/contract_audit/submission_contract_candidate_20260711/`，结构验证通过，但状态故意保持`PROPOSED_PENDING_USER_CONFIRMATION`，判`BLOCK_SUBMISSION_RUNS_PENDING_USER_CONFIRMATION`。这不是新实验，也没有替用户拍板；用户确认后才可改为`FROZEN_BY_USER`并供项目4/5新正式入口引用。
- [M1/投稿合同文献复核] 2026-07-11：补查多车场协同与EV充电调度论文后，推荐合同不变。协同配送主流做法是联合优化运输成本，再用公平前沿、Shapley或其他收益分配处理联盟稳定性，没有证据支持95英镑/跨场客户是通用行业主值；因此0作为完全共享机制基线、10/25/50/95作为摩擦敏感性更可解释。时变电价/碳调度论文把充电时刻和时长作为真实决策，支持把“同路线同电量移充”与“车型/路线改变”分开做消融。来源集中写入`docs/handoff/e1_e7_submission_contract_decision_20260711.md`，不另建调度文档。
- [M1/E2项目9长预算预案] 2026-07-11：只读取冻结4000评价的真实耗时，没有启动16000搜索。预注册代表预检为20c seed3、50c seed5、150c seed3三个负例，加200c seed4最慢强胜防退化样本，hybrid/LNS共8项。按现有耗时线性估计顺序约3.38小时，保守按5倍约4.22小时；若晋级九档×五种子×两算法90项，顺序约21.90小时，保守27.38小时。预检必须8/8满16000、零违规、hash完整，四组汇总不输LNS，三个旧负例至少两个改善自身4000成本，200c强胜不丢。证据`baselines/e2_alns/e2_16000_preflight_plan_20260711/`，判`PROJECT9_PLANNED_NOT_AUTHORIZED`：项目4、5、6和投稿合同未收口前只保留预案，不启动长跑。
- [M1/E2十种子] 2026-07-11（Claude 接替 Codex 续跑正式矩阵）：Codex 额度耗尽时正式810行矩阵已完成528行（全部OK），无进程存活，收口未做。Claude 按用户指令接管执行：核验冻结worktree（0124623e）、smoke门`E2_10SEED_SMOKE_READY`、代表门`E2_10SEED_REPRESENTATIVE_READY`均就绪；Codex未提交的runner收口加固（scipy Wilcoxon强制、rankdata并列名次、解文件落盘、清单校验）经`solver/tests/test_e2_final_10seed_runner.py` 8/8通过后原样沿用。以同合同、同入口、2 workers、PYTHONHASHSEED=0续跑剩余282项（`--phase formal`断点续跑，日志`formal/resume_run_20260711.log`），监控每2分钟检查进度与异常。完成后将执行`--phase verify`收口并记录判决。80k修复门`E2_80K_MECHANISM_VISIBLE_ALGORITHM_NOT_ROBUST`已确认在案，无欠账。
- [M1/E2十种子] 2026-07-11（Claude 全链路代码审计，应用户直觉复核要求）：通读 e2_final_10seed_runner.py 全文并实测验证九项：冻结worktree强校验、子进程隔离环境、225复用行与冻结证据逐位一致、pair拆分后naive仅进ablation不进主表、非OK行立即熔断、收口三重验证（矩阵完整性/当前检查器逐解复算/Wilcoxon+Holm统计）、scipy 1.16.3在金标准Python可用、九算法新行端到端抽检零违规成本逐位一致、超时上限余量充足（GWO 150c实测1048s vs 3600s上限）。结论=代码可靠，续跑不中断。三个无害小疵记录：formal中间decision命名复用SMOKE_READY（权威判决在verify收口）、persist非原子重写CSV（观察者会撕裂读、.tasks逐任务JSON可自愈）、早前"LNS 200c被重跑"为误读（实为复用行重排序）。当前543/810全OK，预计剩余14-24小时。
- [M1/E2十种子收口配套拍板] 2026-07-11（用户逐条决定，Claude 记录，执行权交还 Codex）：①HGS（含SWAP*/SREX/种群路线）升格为后期展望算法，地位与冻结DR-ALNS完全一致，本轮不实现不对比，论文展望段可并列点名；②评价预算口径挂账：文献ALNS惯例约25000次迭代（多为增量评价），本文4000次完整评价严格计数，汇率差须论文明写；16000预检8项（e2_16000_preflight_plan_20260711预注册）已授权E2收口后顺手执行；③F2收敛曲线批准按设计生成（大中小三算例×九算法，history_json轨迹，零新实验）；④基线参数附录表批准（参数取值+文献出处）；⑤实验环境声明批准写入实验设置，注明中文期刊不常见、作可选附录；⑥自家算例组内gap列否决+口径纠正：BKS/AVG/Gap%只存在于开源标准算例集，自造算例连组内gap也不报（防挑好看嫌疑），外部Goeke开源验证仍按7/24评估；⑦Claude顾问接管期结束（续跑282+任务、全链路审计通过、值守正常），收口链verify→F2→16000预检→参数表→封存提交交还Codex执行，交接提示词已在对话中交付用户。矩阵时点606/810全OK，runner进程存活。详细条目见claude_advisor_issue_register_20260711.md末节。
- [M1/E2十种子长跑并行调整] 2026-07-12：正式矩阵原2 workers运行至608/810且无HALT/Traceback。旧父进程未响应自然切换，已安全停止旧父进程和当时两个在途子任务，确认不再有旧runner后从608/810断点启动唯一的6-worker runner。机器为8逻辑核、8GB内存；6 workers为当前上限，保留系统余量，不改算法、预算、成本、物理约束、场景、评价器或任务矩阵。若资源异常或速度反而下降，立即退回4 workers。
- [M1/投稿合同拍板①跨场费] 2026-07-12：用户拍板跨场服务费口径：论文主结果 c^tr=0（完全共享口径，Gansterer/Soriano类文献支持），另做 0/10/25/50/95 摩擦敏感性小实验（数值明确标注为无现实标定的代理，95只是历史参考点非主值）；按车场间真实距离建模挂账为可选加强/未来工作，本轮不改模型公式。用户明确原则：**非致命漏洞不动建模**——目标函数中 c^tr 项保留，只定取值。该决定与冻结E2实际口径（跨场费0）一致，E2不动不重跑；E3须按此口径重跑。其余待拍板项（E4故事、客户归属、碳配额通报、跨机数字规矩）讨论进行中，Claude逐项讲解、用户逐项决定，全部定完后写入E3--E7总设计文档并更新submission contract冻结流程。
- [M1/投稿合同拍板②E4换故事] 2026-07-12：用户拍板E4从"碳价阈值触发油换电"（80kWh时代旧叙事，280主场景下前提已失效：现行碳价下EV已承担86%客户）换为"碳价×充电时机"：碳价0.5/1/2倍各档重优化，主指标=充电搬动电量/目标时段/碳降幅/成本变化，边际油电分工作副线；主打图=充电搬家图（48时段电网碳强度曲线+立即充电vs择时充电负荷叠加）+碳价响应曲线；管理启示章按新故事重写（碳价的现实杠杆=充电调度而非车辆置换）。预注册两种诚实写法：响应显著=碳价驱动充电移峰；响应偏平=择时充电为近乎免费的减排手段、企业无需等碳价上涨。旧E4的配额扫描轴删除（配额=记账项，见碳配额文献核对）。不改模型公式。
- [M1/投稿合同拍板③客户归属] 2026-07-12：用户拍板冻结现有"最近车场"1425条归属映射（生成规则+文件+hash一起锁死，Codex执行时落实），论文明写为自有算例的合成独立经营基线假设，不得写成行业规定或数据真值。"归属不均衡"敏感场景计入手册作备选/后续加强，7/31前不做。
- [M1/投稿合同拍板④碳配额] 2026-07-12：用户同意默认处理：实验保持配额=0全额计价（E2冻结数据不动），删除论文中"配额变化影响路线"的说法与旧配额扫描轴，参数表口径与实验对齐。另有用户新要求：投顶刊不留半分多余的东西，碳配额不能是死项，要讲出真刀真枪的故事。Claude提出"碳账户"方案（配额=收支分界线，事后核算不同配额水平下企业碳账户买/卖方位置及择时充电对它的改变，零新搜索、纯事后核算、不改模型），用户评价"勉强、凑数、不精彩"未采纳；用户已新补一批Zotero文献，要求Claude查阅后为配额找更好的故事，其他新发现也可用于其他实验。硬排放上限/分段碳价等会改变路线的配额变体原挂账未来工作，若新文献支持且用户拍板可重议。
- [M1/投稿合同拍板⑤跨机数字规矩] 2026-07-12：用户认可三条规矩：一张表一台机器（复用旧解的实验两臂同机重算）、跨表只谈相对百分比、每张表记录机器/commit/场景出身。另用户明确偏好：**E3--E7尽量统一在M1上进行**，x86只作溢出/备用；此偏好覆盖x86迁移交接单中"以X86为主运行环境"的默认，实际排期按M1机时估算，排不下再与用户商议分流。
- [M1/碳配额故事定稿+显著性数据核验] 2026-07-12：用户认可配额新故事"配额管得住总量、管不住时间"（时间盲记账盲区，用户新补Management Science 2025加州碳交易实证《Robbing Peter to Pay Paul》作文献支撑；用户另补5篇OM文献分别支撑E7动态×协同假设、E6公平代价曲线、协同契约定位）。用户提出显著性要求（数据/展示/故事至少一头要亮）。Claude只读核验四个事实：①280场景充电间接碳≈总碳一半（E3 M3均值487/993 kg），"EV没多少碳"的担心在本数据不成立；②同路线择时充电省总碳约11.3%（naive合计1119.7→aware 993.2 kg），不只是"子层的21%"；③锚定日2025-11-13真实英国国家碳强度49--199 gCO2/kWh、日内摆幅4.1倍，充电搬家图天然戏剧化；④数据已含30个真实日、日内摆幅1.3x--4.5x（最平11-07=169-222、最波动11-17=48-215），可构造全真实数据的"电网波动日"敏感轴，前瞻"可再生占比越高波动越大、机制价值越大"。另：M2平均因子账面碳515 kg vs 同类立即充电真实约614 kg，时间盲账面低估约16%（初步估算，待E4零搜索复算正式化）。显著化三件套（分母选对/搬家图/波动日轴）写入E3--E7总设计。
- [M1/E3--E7总设计定稿] 2026-07-12：用户批准后，Claude落盘`docs/handoff/e3_e7_experiment_product_design_20260712.md`，为E2之后全部机制实验与论文产物的**设计权威源**（细化并覆盖总施工图的E3--E7部分；E2合同不变）。要点：七项已拍板决定汇总；六章故事线+每章一件主打展品；通用合同（280主场景/冻结hybrid/4000评价/公平仅E6与E7交互臂启用/一张表一台机器）；WP0前置（合同机器冻结、prices.py c_tr 95→0四同步、TeX同步账登记、E0复核）；E3重跑主梯30+摩擦轴25；E4三部分（零搜索复算+碳价扫描15--30+波动日复算）；E6两段式绑定聚焦（Stage A复用E3 M4行+Stage B约30）；E7四臂×5事件流=20会话含三交互门槛与预注册假设H；E2b四臂算法消融20--40；产物规格总表（T4--T9/F1--F7/F-E3b/F-E4c/T-E4d+四附录表）；7/13--7/31排期（M1富余，机制实验只跑hybrid单次2--5分钟级）；Not-To-Do。执行仍归Codex，各批开工时Claude在对话中交付自包含提示词。
- [M1/图表新旧对账] 2026-07-12：应用户要求，总设计文档第9节重写为"产物对账与最终规格"：**07-03样板包（design_templates/，已过编译）=最终底版，壳目录v1=壳来源权威**，逐张判定表覆盖全部展品。关键改动：T3删"相对已观测最优偏差%"列（落实07-11"自家算例连组内gap不报"拍板）并删等墙钟占位列；T5加"相对上一层Δ%"和充电间接碳列；T7碳价档改0.5/1.0/2.0、保留"充电时段重心"列；F5沿用Qiu Fig5壳但右轴字段本地化为充电搬动占比（旧F5b"30倍阈值"图正式废弃）；T9主表改四臂（列族沿用样板含信息成本/守恒审计，事件流版降附录）；T8可行列扩三态+绑定聚焦网格；F6加r0标注；T6/F3/F4/F7/F1/F2/T1/T4原样沿用；新增四件全部绑定已读范本壳（F-E3←Shi Fig5、F-E3b←陈婉茹图4a、F-E4c←Shi Fig7、T-E4d←陈婉茹表7），装不进壳即NEEDS_REFERENCE_SHELL停。废弃清单确认：F5热力图/F2箱线/旧F5b/T9事件流主表。mock字段=正式CSV契约，只换数据不改壳。
- [M1/E2十种子正式收口] 2026-07-12：`e2_final_10seed_runner.py --phase verify` 完成810行验收：9个算法×9个L-main自有算例×10个种子，每行严格4000次完整评价；810/810零违规、当前检查器复算通过、解和哈希齐全。总排名为`staged_hybrid_carbon_aware`、GA-VNS、LNS、VNS、GA、GWO、PSO、ACO、IWD，判决=`E2_10SEED_PRIMARY_LEAD_SUPPORTED`。E2只报告自有算例的均值/标准差/最好值/时间和配对统计，不报BKS/AVG/Gap%；Goeke公开标准算例和未经改动Goeke算法留到E2后补。
- [M1/E2收口产物] 2026-07-12：F2三子图收敛曲线已由已有`history_json`生成并目视检查（25c/100c/200c、九算法、十种子均值和10--90%带、无新搜索）；基线参数附录已写入`formal/baseline_parameter_appendix.csv/.md`。formal目录发现的819个AppleDouble元数据已只删除并记录到`formal/appledouble_cleanup.json`，没有改写实验数据或解文件。
- [M1/E2长预算预检] 2026-07-12：新增可恢复脚本`baselines/e2_alns/e2_16000_preflight_runner.py`；2次评价接线测试已通过（冻结提交正确、2/2评价、零违规）。随后启动8项16000评价代表预检（20c seed3、50c seed5、150c seed3、200c seed4，hybrid/LNS各一，6 workers）。预检是独立补充证据，不改写810行E2主表；完成后按预注册门槛单独判定。
- [M1/E2长预算预检收口] 2026-07-12：8/8项16000评价全部完成，8/8零违规、解文件齐全、哈希和键合同通过；混合算法总成本`19933.95424504102`，LNS为`22784.898943365006`，3个旧4000负例全部改善，200c seed4强胜样本仍领先19.7043%。判决=`E2_16000_PREFLIGHT_SUPPORTED`。这是对“4000评价是否太短”的补充稳健性证据，不改写810行4000评价主表，也不授权继续做E2 rescue。预检目录`baselines/e2_alns/e2_16000_preflight_20260712/`已复算、清理AppleDouble并记录清理文件。
- [M1/E2封存边界] 2026-07-12：E2主证据和16000代表预检均已结束；E2研究线正式关闭，下一步只进入E3准备。不得再改E2算法、预算、成本、物理约束、场景或评价器；不得把16000预检混入810行主表。公开标准Goeke算例、BKS/AVG/Gap%、更长预算全面对齐和HGS/DR-ALNS仍按挂账或后续计划处理。
- [M1/E2封存确认+WP0交接] 2026-07-12：Claude核验E2最终判决`E2_10SEED_PRIMARY_LEAD_SUPPORTED`（810/810、零违规、主算法总排名第一）与16000预检`E2_16000_PREFLIGHT_SUPPORTED`（8/8、混合低于LNS、3旧负例全改善），封存tag `e2-10seed-final-20260712`、提交`1180abf3`。E2永久关闭，不救援不重跑；16000证据只作补充，正式预算口径仍为4000。按总设计`docs/handoff/e3_e7_experiment_product_design_20260712.md`进入E3前置：WP0提示词（submission_contract冻结FROZEN_BY_USER、prices.py c_tr 95→0四同步、TeX欠账登记、E0复核、看板重基线化）已在对话中交付用户转Codex；机器按用户偏好留M1，不做x86迁移。写作硬门重申：E2只证性能，算法创新主张以E2b四臂消融+文献对照为准。
- [M1/WP0合同冻结] 2026-07-12：按用户批准的E3–E7总设计完成投稿合同机器冻结。合同判决=`E1_E7_CONTRACT_FROZEN_BY_USER`，合同文件SHA256=`8c3917ceb353425eb6f7845b80426c92d8ff2afcc4bc77731b23ae06d1ff6233`；最近车场1425条映射和hash冻结为合成独立经营基线；主场景280 kWh、80 kWh只作稳健性对照；c_tr主值0，10/25/50/95仅为无现实标定摩擦代理；配额默认0且只作会计项；公平默认关闭，仅E6/E7交互臂从搜索阶段启用。`prices.py`、TeX参数表、HANDOFF同批同步；E0九算例复核=`E0_WP0_READY`，E2三份冻结解当前checker复算逐位一致=`E2_FROZEN_REPLAY_COSTS_UNCHANGED`。本任务没有启动E3–E7正式搜索。
- [M1/开工前建模-实验对齐审计] 2026-07-12：应用户要求，Claude只读审计论文模型章（objective/eq:fairness/碳交易/滚动继承）、profit.py、cost.py充电碳积分、E3 runner M0实现，对照12项质询。总判决：**无致命建模矛盾**。已闭合的经典坑：公平比有Π⁰>0守卫且不适用即降级（TeX 539行）；收入随实际服务车场归集、成本同口径、无重复核算（profit.py与TeX 512/531一致，协同→收益转移→公平约束有真实对象）；配额区分阶段CE^τ与累计Ē^τ；充电碳按时段重叠秒精确积分+跨午夜循环（cost.py charging_slot_breakdown），非起点查表，分箱假象结构性风险低；M0=真独立子问题（按归属拆子算例、预算均分）。四个主张-证据缺口（写作/实验卡处理，非模型bug）：①摘要声明"共享充电站容量"但280实测公共站0次使用，只能写"建模并核验"不能写"实验证明"；②共享车辆池+费0下的大规模跨场（LNS单次26-57客户）数学可行但需在论文写明公平约束+摩擦轴的治理逻辑；③E7公平基准跨阶段口径未定义，须冻结（建议：优化用初始客户集基准/终局用最终揭示集重算θ检查/逐阶段描述性追踪）；④"动态优化"措辞统一为滚动时域反应式重规划，信息成本=诚实度量，预期式决策归future work。建议补丁P1-P5：E7公平口径冻结、E4分箱稳健复算（零搜索）、E3加100c缩减阶梯20次、E3/E4/E6结构稳定性附录行（EV份额/跨场数/移充量跨种子std）、E7事件流配比多样化预注册。冗余性裁定：配额决策上冗余但已定位为记账+政策故事（期刊同款）；公平不绑定也有"零代价公平"正面结论；碳择时/协同/动态为不可删核心。280+三班定性=有来源的现代场景而非筛出来的好看场景，80kWh镜像结果如实并列。补丁待用户认可后写入总设计。
- [M1/审计补丁拍板P5-P9] 2026-07-12：用户拍板：分箱稳健复查=做；跨规模交叉=做且E3--E7算例统一为{100c,200c}两档（E4九档冻结解复算不受限）；结构稳定性汇报=可做，Claude明确执行边界=正文放代表例+附录全量可查+定性结论以全量为准，不做选择性隐藏；E7事件流正式5条维持原配比，变配比/动态度变体挂账；退化关系说明段=做（判定为行业惯例，兼作NP-hard依据）。已写入总设计文档第13节。待讨论四项：共享充电站容量处置、车辆池治理故事写法、E7公平基准跨阶段规则、滚动重规划措辞。WP0提示词用户已发Codex执行。
- [M1/Wang2023共享充电站原文调查] 2026-07-12：应用户要求核查ref:7原文（Wang et al. 2023, ESWA, CMEVRPTW-SCS，Zotero key 6Z9A3BFA）。结论：原文的"共享充电站"是**协同机制**——充电站向所有车场的EV开放使用，故事为"共享提高资源利用率、减少用车数"，通过真实案例的客户重分配与网络重构验证（总成本降46.2%、EV从29辆减到13辆）；未见逐桩同时容量/排队硬约束是其主线。我方模型把它继承为配套：开放使用（结构性存在）+容量检查（π_s，公共站1桩、车场多桩）。我方280场景实测充电100%在车场、公共站0次，容量约束未绑定；"跨车场充电"（到对方车场充电）次数未统计过，可零成本从保存解补计。我方六卖点不含共享充电站→按用户决策规则属配套约束而非主贡献。处置选项已交用户：A降级措辞+保留约束+诚实一句；B先零成本统计跨场充电次数再定措辞；C重开车场桩数稀缺情景（=重启已挂账09p路线，成本高）。
- [M1/充电结构零成本核查+拍板③④] 2026-07-12：Claude只读复核保存解回答"只在车场充电"质疑。事实：①80 kWh敏感性24个解中15个真实使用公共充电站、共45次——公共站与容量约束在文献参数场景被真实行使；②280主场景539个解公共站仅2次——大电池下公共快充不经济，"电池容量决定公共充电依赖"是有硬数据的场景洞察；③严格趟级口径下跨车场充电=0（每趟都在本趟归属车场充电），早先粗口径44%系物理车跨趟换场所致——真实被大量行使的共享机制是**共享车辆池**（280冻结解中物理车跨趟换车场3326车次、80k中201车次），这是协同故事的新增硬证据（M0独立经营下应为0，可作E3对照指标）。处置（不改任何建模/算例硬设置）：正式零搜索复算进E4 Part A清单（分场景分算法统计公共站次数/车辆跨场流转次数），E1/T4结构指标补列，E4 Part B/C记录公共站使用次数；论文措辞=公共站作用集中于小电池场景（有数据）+主场景共享性体现为车辆池流转+容量约束建模并核验但主场景不绑定。用户另拍板：③E7公平基准=每阶段用当时已知客户重算（方案C，用户原则：算法前期端平水、不预测、效率优先公平作约束；Claude核验论文记号Π_d^{0,τ}本就带阶段上标，**无需改模型公式**，需预注册基准子问题预算规则，E7机时约翻倍可接受；终局按实际收益结算报告，与"现实事后算钱"一致）；④动态措辞=全文统一"滚动重规划"，预判式写进展望（零成本，行业惯例）。⑦补充要求已落实：每实验理想目标+图表判读标准写入总设计第14节。车辆池治理故事（第2件）重新解释后待用户拍板。
- [M1/E2后置-IWD忠实度审计] 2026-07-12：Claude只读复核确认IWD基线实现不忠实，三处代码级缺陷坐实：①`_run_iwd`（metaheuristic_baselines.py:918）velocity只写不读，是死变量；②`_iwd_update_local_soil`对好坏候选一律`soil*0.99-1/|obj|`，质量盲且增量约1e-4对soil0=1000无引导力；③`_iwd_transition_base`启发底数`1+saving/10000`近似恒1；论文核心的速度-时间-土壤机制整体缺失。另一判定：客户排列+公共解码器是九算法共享适配框架（PSO/ACO/GWO同框架均健康），不算IWD独有缺陷，忠实度修复应限定在共享框架内实现IWD核心机制。封存810行只读复算新事实：其余八算法native_lift>0为90/90（全部规模超共同起点），IWD仅10/15/20c全过、25c为4/10、50c及以上0/50；ACO在100c首次超起点用256-400评价（10/10），为微门预算提供健康线。结论：E2排名与判决不受威胁（IWD本就垫底），但论文不得写"九个忠实/健康文献基线"，IWD行必须修复重跑或明确标注为简化适配。已设计有界微门：修复候选（忠实IWD核心+文献标准参数）×{25c,100c}×seeds1-3×800评价×三臂（修复/封存对照/velocity消融），预注册P1-P5通过标准，允许至多一轮留痕参数微调；提示词已在对话交付用户转Codex。封存数据未动、代码未改。门过后的重跑范围（Claude建议只重跑IWD的90行：合同不变+8共享合同文件hash护栏，先例=225冻结行复用与80kWh哈希护栏；全810重跑无公平性收益且威胁7/31）与是否重跑均待用户拍板；门败则IWD标注为失活/简化基线，胜负主张限定为其余七基线。
- [M1/拍板②公平+车场桩数三路证据] 2026-07-12：用户拍板第2件=A：凡代表"系统给出的方案"的实验（E3完整模型层、E4重优化、E6、E7完整臂）公平约束从搜索开始启用；机制隔离层/臂按消融设计关闭；E2保持封存并在论文明写为松弛口径算法比赛（依据：合同审计实测费0下θ=1仅1/5可行，无公平约束的松弛最优会令车场收益跌破单干、不可执行——用户哲学"先有基本公平才有可执行最优"获数据支持）。车场充电桩问题三路证据齐：①冻结解审计（快速版）：单车场同时充电峰值中位4--7台、最高21台（21>EV车队14的反常需正式审计核实口径），EV车队最多14辆；②文献惯例（Froger等《EVRP with capacitated charging stations》，Zotero PDVQ5Z86）：容量约束加在公共CS（测1-4桩、现实"顶多一两个"），中心车场明确描述为"有大量充电桩"，车场侧真实约束是电网功率且属于另一支固定路线充电调度文献（Sassi&Oulamara 2014、Pelletier等2018）；我方公共站=1桩比Froger更严，车场多桩=同款惯例；③行业实践（网络多源）：车场过夜充电是车队充电主干、桩车比1:1至1:3、同时充电分散系数0.5-0.7——我方中位峰值/车队比约0.3-0.6落在行业区间内。结论：现设置与文献+行业双重一致，"只在车场充电"可用引用+审计数字正面答辩，无需设卡、无需重跑E2；若坚持设卡=检查器语义变更+全部冻结解重验+大概率重跑E2。待用户在A（不设卡+审计数字+引用段）/B（设卡+重验+可能重跑）间拍板；正式桩数审计（含反常核实、隐含桩数/功率报表）无论A/B都做，交Codex零搜索执行。
- [M1/讨论关账+E3放行] 2026-07-12：用户拍板车场桩数=A（不设卡，正式审计+引用交代段+电网功率上限列未来工作，E2不重跑）并指示开始规划。全部开工前讨论关账，终局拍板汇总固化于总设计第15节（覆盖冲突条款）：公平启用范围A（系统方案实验从搜索启用，E3b摩擦轴改M5口径）、E4主打数据源改用E3 M5完整模型解（E2 45组降广度附录）、E7公平基准C（逐阶段因果重算+终局实结算，无公式改动，机时翻倍）、措辞与写作定稿五项、WP0合同公平条款修订指令。E3执行提示词（主梯30+缩减梯20+摩擦轴25=75次搜索+四项零搜索审计）已在对话中交付用户转Codex，前置=WP0完成+合同公平条款修订。
- [M1/E2-IWD忠实度收口] 2026-07-12：IWD直线问题已按文献核心公式修复（转移概率、速度/时间、土壤携带与局部/全局更新），并完成唯一允许的来源参数救援短门。正式参数与`zhang_scaled`两轮均18/18行满800评价、零违规、解/hash齐全，但P3（100c改进）、P4（动态速度差异）、P5（胜旧版）仍未过，最终判决=`IWD_FIDELITY_GATE_FAIL`。不重跑IWD 90行，不改E2封存810行；IWD在论文中标为失活/简化适配基线，其余基线和E2主排名不变。报告=`docs/handoff/iwd_fidelity_closeout_20260712.md`。
- [M1/TVCI-ALNS命名冻结] 2026-07-12：用户指定当前创新算法的暂定公开名称为 **TVCI-ALNS: Time-Varying Carbon-Intensity-Guided ALNS**，中文为**时变碳强度引导的自适应大邻域搜索**。代码新增稳定展示/API名称与中英文全称；历史机器标识`staged_hybrid_carbon_aware`及已封存E2原始CSV/JSON不改，保证旧证据和hash可复算。论文同步说明该名称对应现有“分阶段ALNS--LNS搜索内核+固定路线充电时刻碳感知重调度”，不扩大为全新基础ALNS或全联合碳搜索。新增命名只影响当前新调用的展示字段，不启动实验、不改成本/检查器/评价器/物理约束。
- [M1/提示词骨架详版定稿] 2026-07-12：应用户两次指示（①骨架进文档、Codex照单开工、不现场设计；②"像提示词一样细致，像词典一样全面"），总设计文档第16节由压缩骨架升级为**逐字可发的详版提示词**：骨架0（WP0存档）、0b（合同公平条款修订，E3前必做——WP0冻结的合同仍是旧"公平仅E6/E7"条款）、1（E3：runner改造五点+短门五验+主梯30+缩减梯20+摩擦轴20复用5+审计包四份，含产物目录命名/字段清单/验收打勾清单/汇报模板）、2（E2b四臂精确定义A1-A4+松弛口径声明）、3（E4三部分：M5解主口径10组+时间盲低估%正式化+碳账户基准排放内置停止点+碳价三档绝对值+三真实日波动轴）、4（E6：r0取自E3 M4行、θ网格预注册构造规则、绑定标记定义）、5（E7：四臂B1-B4精确定义+逐阶段因果公平基准+两个【待填】预算+逐阶段字段清单）、6（表图：数据源目录映射+草稿审批闸门）、7（写作15条销账清单）。新增第17节词典（环境/冻结锚/算例数据/代码入口/证据目录只读区/39+新增字段词典/判决词/术语标准化/统计约定/已知未决）。同步吸收Codex新进展：IWD_FIDELITY_GATE_FAIL→论文标失活/简化适配基线、"九健康基线"措辞改口（写作清单新增）；TVCI-ALNS公开名进术语词典与摘要定稿条目。文档至此为完整施工手册，Codex按第16节顺序照单开工。
- [M1/封存前手册完备性审计+E3结构新发现] 2026-07-12：应用户"封存Claude顾问、全力执行"前的最后审查要求，Claude 只读审计总设计手册四维度（目的/旧坑/联动/故事性）并下钻代码与冻结产物，发现一条未被任何先前审计捕捉的结构链：①M0 子算例漏传 num_cv/num_ev，独立子搜索实为车队无上限（fairness.py:313 只传 nodes+matrix，兜底 infer_fleet_limits 在子包找不到任何上限）；②终局打包 normalize_solution_vehicle_trips 为纯 round-robin 改标签、无跨趟时间衔接检查，HALT_M0_PACKING 实为死代码，检查器全项目只数物理前缀；③协同臂 M1--M5 从 M0 解热启动且开新路由门数原始趟数（65--73≥14）→EV 侧永久堵死；④实证：旧 E3 正式 30 行中 4/5 种子 M1 路由签名与 M0 逐位相同（成本Δ-0.03%/-0.02%/-0.00%/+0.00%），旧收口"协同4胜1平"的4胜属充电层化妆差异，唯一真重构 seed4（-5.16%）恰为 M0 最差种子。总设计骨架1"逐字一致"会原样继承该链，fee=0 只解锁三把锁中的一把（另两把：锚定+调度锁死 Diagnostic only 默认不开）。传导：E6 r0/独立基准、E7 B3 臂与逐阶段公平基准同源。已出专门审计文档 `docs/handoff/e3_m0_search_asymmetry_audit_20260712.md` 含三选项待拍板（推荐选项一：零模型改动，骨架1增两条预注册诊断——M1vsM0签名逐种子上报+5次无锚定M1对照探针+审计#1扩展行驶区间重叠+论文披露句）。同批交付：用户新要求的意外价值台账 `docs/handoff/serendipity_ledger.md` 建立（15条初始条目：已包装8/算法洞察2/待拍板1/纯教训4，含维护规则，长期单一登记处）。手册其余维度判定：目的（第14节一实验一理想目标+判读标准）、联动（E3→E6/E7数据复用链+第15节终局拍板）、故事线（六章+显著化三件套）、旧坑防重踩（Not-To-Do+已关闭路线+第17节词典）均完备；唯一缺口即上述 E3 结构链。E3 开跑前须先拍板。
- [M1/E3干净结论研究计划] 2026-07-12：用户要求以 E3 为主线、优先结论干净并先查文献后规划；已完成两轮文献核对（多趟有限车队：Zhen 2020 / Hernández 2016；协同公平资源口径：Soriano 2023；多车场共享充电：Wang 2023 / Schmidt 2023）、源码比对及旧 E3 30 个保存解只读回放。新证据：30/30 解存在同一物理车的路线时间重叠（M0 596 对），且各 seed 有同一车号跨 D0/D1；故当前 #T 标签不能作为合法实体车多趟证据，但尚不能据此断言路线重排必定无解。结论：这是论文模型一车一趟、代码未排程、M0/M1 搜索不对称三层叠加；IWD 无关。补充核对：80 GBP 是参数文件明示的每班次/每趟派遣成本，代码按路线收费可保留；若正式多趟化，论文须把它明确为每趟而非购车资本成本。新增计划=`docs/handoff/e3_clean_conclusion_research_plan_20260712.md`：E3 的三段因果故事、D1 多趟语义与 D2 车场资产口径两项重大待拍板、保持 70→100 的推荐矩阵及非结果导向验收门。未改模型/代码，未启动正式 E3；D1/D2 前不把 #T 解用作有限实体车队正式证据，也不让 E6/E7 复用该 M0 基准。
- [M1/E3 D1/D2 获批后的正式多趟可行性审计] 2026-07-12：用户批准 D1（真实实体车连续多趟）和 D2（合法独立 M0 实际用车数冻结参与方资产，不暗设7/7）后，按文献满电出发/返场补电、同车场/同车型连续趟语义，对旧 M0 五个 200c 保存解做固定路线精确趟链覆盖。结果五个 seed 的最小 EV 数为 `51/55/52/55/50`（D0/D1 分别为 `26+25/30+25/27+25/28+27/27+23`），而原始全局上限只有14 EV；CV 仅需 `6/3/4/3/4`。MILP 均 Optimal，最长合法链最多4趟且无更长链。52/59（seed1）EV路线跨至少两个三班窗口，说明旧搜索先造长跨班覆盖路线、末尾才贴#T，无法被排程补丁合法化。结论=`HALT_E3_FORMAL_TRIP_CONSTRUCTION_REQUIRED`：不跑骨架1的旧70次、不得把50--55 EV反写成新资产、E6/E7不得复用旧M0。用户需另行决定正式多趟路线构造重建 vs 明示松弛口径；详见 `docs/handoff/e3_formal_multitrip_feasibility_audit_20260712.md`。E2未动。
- [M1/论文定位终局裁定] 2026-07-12：用户委托 Claude 对论文故事线终局拍板（Claude 初评"中心=时间"→ChatGPT 反评"主体=协同调度"→Claude 裁定）。裁定=**双层定位**：研究主体（模型层）=动态需求下的协同多车场油电混合车队调度；中心主张（叙事层）=分时电网碳强度把"几点充电"变成真实减排杠杆（择时省总碳约11.3%/时间盲低估约16%初步/日内摆幅最高4.5倍），杠杆能否扳动取决于协同资源弹性+公平联盟可执行性+滚动状态继承，四机制互相作用、各有一件展品作证。禁止两个极端：不得写"全文中心=时间"把协同/公平/动态降为充电附属；也不得只立类别标签不立命题（防堆砌防线=交互证据非主体宽窄）。同批修正："tex无故事线"改为"中心句存在但不承重"（摘要配料表句/贡献模板四条/实验并列介绍架空中心句）；算法故事确认为目标状态，以E2b四臂为准，TVCI-ALNS首次出现必须逐字划界（引导只在充电时刻层）。写作硬规则7条（摘要五句法/贡献一主一辅/充电站容量与配额降级/一问三张力/时间矛头限E4章与管理启示/现实锚点点名/既有硬门重申）写入权威文档`docs/handoff/paper_positioning_final_ruling_20260712.md`。不改实验设计、不改模型公式、不动E2封存；D1多趟拍板仍先于一切包装动作。
- [M1/E3多趟重建终局裁定] 2026-07-12：**用户拍板D3=A（授权正式多趟路线构造重建）**，Claude受托终局裁定+修正，执行权威源=`docs/handoff/e3_multitrip_rebuild_ruling_20260712.md`（覆盖clean-conclusion计划第6/7节与总设计骨架1的冲突条款）。三条铁律：①严格多趟检查=版本化新增、合同域隔离，**严禁重判E2封存810行/E1冻结25条**，论文两层口径明写（正式模型=严格多趟；E2=路线级松弛九算法比赛）；②算法身份落定C档：E2冻结内核0124623e+40bd2883充电时窗保护为底座，只吸收合法性层+合同接口层，新冻结E3执行提交，论文写"同一TVCI-ALNS内核扩展多趟可行性语义"（IWD与E3无关=用户判断正确，但D1-A本身即算法变更）；③cost.py/evaluation.py不动，check.py仅允许版本化新增。技术修正：排程必须进搜索接受环节（事后过滤已被审计证伪）；废除M0热启动锚定，M0-M5各层独立合法共同起点，70-Clean控制臂撤销、恢复原70矩阵（主梯30+缩减梯20含100c M5+摩擦轴20），签名相同率诊断零机时替代；D2补最少车数目标规则（固定费对车数中性），M0须先在14/14内闭合、过门后按seed冻结资产、失败=HALT_M0_ASSET_CAP不放大14。新增指标/展品：每层实际实体车数进T5，"合作让车队变小"(Wang2023同款)为协同段候选主打。短门链=排程器单元门→结构可行性门(200c seed1合法M0+M1)→200评价接线→4000模型门实测墙钟（旧2-5分钟估算作废）。止损红线：7/19结构门不过自动回落B口径（明示松弛敏感性保7/31）；7/23前70未收口则取消升100。传导：E1挂账最小严格镜像(~10次)待E3后拍板；E6只准复用新E3行；E7状态继承须含趟链状态、机时再上调。TeX欠账追加5项（Zhen/Hernández记号借用、£80每趟派遣费、退化段更新、E2松弛披露、D2资产假设句）。下一步：Codex额度恢复后Claude交付第一批开工提示词（排程器单元门+结构可行性门，不含正式70）。
- [M1/E3 22kW取证+纠偏] 2026-07-12：新排班器已删除写死的60kW，从`PriceParameters.depot_charge_power_kw`读取现行22kW车场交流代理值；60kW的旧结构/搜索/4000评价目录已在`decision.json`添加更正，降为不得用于70次前置的工程草稿。对同一批59趟/449客户的路线，22kW双口径诊断结果：补满口径当前贪心排程找不到14/14见证（需21 EV，不等于全局不可行证明）；部分补能口径在两种经营案中均找到14 CV+14 EV合法见证。未选定正式口径，未改`prices.py`，未启动70次。Volvo Trucks官方 FL Electric 页面实测产品锚点=280--565kWh、43kW AC满充约8h、150kW DC满充约2.3h；22kW只能称Mer公开车场AC代理，不得写成该车型官方配套功率。证据=`docs/handoff/e3_charging_power_and_collaboration_evidence_20260712.md`、`baselines/e3_ablation/e3_multitrip_structure_gate_22kw_20260712_v2/`。
- [M1/E3开新趟门纠偏] 2026-07-12：源码审计证实新趟门仍在数路线条数，而不是数实体车，会在严格多趟模式下重演旧锁死。已修为只在新E3开关打开时，创建新趟后先交实体车排程器核验14/14再允许；E2及旧路径保持原样。新增统计：新趟尝试/可接受/拒绝、公共充电站拒绝、完整排程检查次数；正式运行前必须落入raw记录。未启动70次。
- [M1/E3实体车结构门] 2026-07-12：D3=A第一批完成。新排班合同不改旧评价与检查代码，不倒查E1/E2封存证据；6项单元检查和61项相关回归检查通过。第1个200c算例的独立经营与共享经营均覆盖449客户/59趟，路线违规0，均能排进14辆油车+14辆电车，最长3趟/车。判决=`STRUCTURE_GATE_PASS`，但正式70次仍未授权、未启动；下一步仍按原顺序做小规模完整搜索，再做正常规模的速度和解质量检查。证据=`baselines/e3_ablation/e3_multitrip_structure_gate_20260712/`。
- [M1/E3真实排班搜索接线与4000次门] 2026-07-12：搜索快速记录原先会漏掉实体车排班违规，已在新E3开关下改为每个完整候选都重做严格排班检查；旧E1/E2默认路径不变。200次接线重跑后独立/共享均满预算且零排班违规。4000次正常规模门也通过：独立经营合计4000次/89.60秒/自身起点成本下降5.376%；共享经营4000次/357.79秒/下降4.502%；两者坉449客户唯一覆盖、路线已变、严格违规0。单个seed中共享最终成本高于独立，只记为观测，不作门槛、不调参救方向。证据=`baselines/e3_ablation/e3_multitrip_search_gate_20260712/`与`e3_multitrip_model_gate_20260712/`。正式70尚未启动；下一步是把正式任务表和字段全部换到新规则，做启动前最后检查。
- [M1/E3监工核查] 2026-07-12：Claude对结构门+进行中改动做只读核查，总判断=**按裁定正轨走**：合同版本化（`E3_STRICT_MULTITRIP_V1`新增模块，check.py/cost.py/evaluation.py未动）、metadata明记`legacy_e1_e2_rejudged=false`、未启动70、四件套齐、排班语义真实（趟不重叠/同场同型/满电出发/回场按耗电补电占时/时间窗重验/最少车数装箱对固定区间精确）；工作区未提交kernel改动=把strict合同接进搜索可行性判定，全部躲在`SETP_E3_STRICT_MULTITRIP`默认关闭开关后（关=与E2兼容身份一致，开=并绕过无排班字段的旧评分缓存），性质=合法性层，符合裁定C档。**四个盯防点（下一门前必须处理/明确）**：①`depot_charge_power_kw=60.0`为硬编码无来源参数，14/14可排班结论建立其上，正式跑前必须与论文车场充电函数对齐或给保守出处并写入合同；②公共充电站访问在V1证书直接报错，须预注册处理（扩V2记账或明示E3限车场充电+披露句），不得静默丢解；③结构门EV/CV五五开为人工见证分配，只证存在性，正式搜索中车型分配必须是搜索决策，门证据不得当质量证据引用；④M3择时充电在新语义下=趟间空闲窗内挪充电时刻，证书recharge_end只是下趟出发下界（兼容），但E3成本/碳记账的充电时段必须落在证书窗口内，一致性须在搜索门中显式验证。另确认：新语义下E3绝对成本必然高于旧松弛数字，属正常，禁止跨口径比较。
- [M1/E3监工判决：60千瓦草稿改判] 2026-07-12：Claude审计提交`63766ad3`（Wire strict E3 scheduling into search）后判决：**用户2026-07-12规定的四道门一道未做**——`multitrip_schedule.py`的`depot_charge_power_kw`默认仍为60.0硬编码（22.0其实早已在`prices.py:55`带Mer出处，证书未读取）；新增的200评价接线门与4000评价模型门（`e3_multitrip_search_gate_20260712`/`e3_multitrip_model_gate_20260712`）metadata均指向旧60千瓦结构门，两门碳价置0、无车型翻转记录、无充电-碳窗口核验、公共站仍一碰即拒无观测口径。因此**三个gate目录（structure/search/model）全部结论降级为60千瓦工程草稿，不得作为正式70次前置证据**；"推进到正式实验前最后一关"的汇报定性错误（提交时间21:47早于22千瓦纠错落实）。公道记录：预算对称正确（solo两子问题各半、总和=shared）、strict合同真实进入搜索接受、合伙劣于单干的负结果如实记录未调参。处置=改判+按22千瓦重跑，非推倒：Codex纠偏提示词（八任务：功率单一来源化/证书双口径partial+full/22kw结构门重跑4行/旧目录amendment改判/22kw重跑两门/充电-碳窗口一致性/车型决策记录/公共站观测）已在对话交付用户。**待用户拍板**：①重跑主口径=部分补能（Claude推荐，论文自身非线性部分补能模型口径；22千瓦下补满280需12.7小时，三班倒趟间补满几乎必然排不开）+补满口径作对照行；②参与理性采纳规则+零成本拼合参照行+采纳标记列（进70次runner合同用，"协同层最终方案=搜索结果与单干拼合较优者"，数学依据=费率0同资产下合作可行域包含单干、合作真最优不可能更贵，出现更贵只能是搜索赤字）。两板未拍前不重跑、不启动70。
- [M1/E3两板拍板+暖启动裁定复议] 2026-07-12：用户拍板一=**升级参数**（不靠部分补能救22千瓦，直接把车场功率提到有据可查的车队直流值——22千瓦交流配280kWh卡车物理上不自洽，且12.7小时满充在三班倒里没有择时挪充电的余量，碳时机故事本身就要求充电有空闲窗）。监工纪律：这是**参数变更=重大事项**，必须①四同步（代码默认+注释+TeX参数表+HANDOFF）②每个取值绑具体来源（禁止再出现60那种无源数）③报告cost.py充电时长→碳时段积分的依赖方向+证明不污染E2封存表（E2按tag/commit/hash封存、显示层不重读prices.py）。取值规则=预注册物理可行性阶梯{40,50,60千瓦，均为车队直流可引用值}，取**能过14/14结构门的最低值**（保守、非挑高），起点40，待用户确认具体数。用户拍板二=同意参与理性采纳规则+零成本拼合参照行+采纳标记列，但明确**更希望真协同涌现（才有故事）**。→ Claude据此复议自己"废除M0热启动/各层独立起点"的旧裁定，判定其为**过度纠偏**：证据①60千瓦草稿试跑中fresh-start的M1在449客户同预算下反比M0贵（fresh-start欠搜索的实证）②文献（Soriano/Wang）测协同价值=从独立经营现状暖启动改进③锚定bug真根因是开路门按趟数而非实体车数计（已修），非热启动本身④累积消融需暖启动才能干净归因边际效应。**建议改为**：M0从独立构造起、Mk从M(k-1)暖启动（真累积消融）；锚定bug在源头修死（门按实体车计+跨场/跨层算子验证可用）；**每层强制移动验证**（签名须变、跨场move被接受计数>0，否则HALT=bug探测器）。此设计使M1从独立解只增不减、自动满足拍板二的采纳下限，且给真协同最大honest机会。红线：验证移动为真才算协同，验不到=退回bug、停跑，不得靠采纳规则掩盖欠搜索、不挑种子、不调参。**两项均待用户最终确认**（具体千瓦数+暖启动复议）；确认前不重跑、不写70次runner（当前尚无70 runner，改这个正当其时）。
- [M1/E3监工自查更正] 2026-07-12：应用户质疑（"怀疑你出了很多错，所有数据必须有权威依据"），Claude自查上一条记录并**撤回五处无据/错误表述**：①`{40,50,60千瓦}`阶梯为Claude凭经验编造、无出处，**作废**，不得作为任何取值依据；②"卡车车场普遍用直流桩"无出处，降级为待查疑问；③"Soriano/Wang测协同=从独立解暖启动"未读原文核实，撤回、标记待查；④"锚定bug开关已修"未验证，撤回——已验证的只有新strict合同按实体车硬卡，旧"开新路线闸门按趟数计数"是否仍在必须列入下批任务查证；⑤**方向性错误**："升级功率是小事"——代码核查（本条唯一新增事实）证明`depot_charge_power_kw`同时被`check.py:543`（受保护检查器）、`support/charging.py`、`search/charging.py`两套充电模块及论文参数表生成器（`reporting/samples.py:130`，符号π_d"车场交流充电功率上限"）使用，改默认值=改论文已印参数+改充电时长模型+威胁封存证据复算一致性，属大改动。经核查站得住的：280 kWh出处=prices.py:40注释（09h-09l诊断，Volvo FL/FE Electric公开证据边界）；22.0出处=prices.py:55（Mer UK商用桩7-22kW）；280/22≈12.7小时为算术；60.0硬编码确系错误；门预算对称已验证；"费0同资产下合伙可行域包含单干"为数学结论。**修正后的顺序（用户无需拍任何数字）**：第一步零改动事实检验=按22千瓦+论文自身部分补能口径重跑结构门（部分补能即论文摘要声明的非线性部分补能，非放水；排得开则参数问题消失）；第二步并行只查不改=Codex查证Volvo FL/FE Electric官方充电功率文档（与280同源）+Soriano/Wang原文协同度量方法，逐数字带出处落档；第三步才拍板（若第一步不过，带着"至少需X千瓦/Y辆车"的实测数字+官方出处来定）。暖启动同理不需现在拍：22千瓦门通过后在4000门做"从零搜"与"从独立解暖启动"两起点对照（配强制跨场移动记录、零移动即HALT），数据摆齐再定70次口径。上一条记录中与本条冲突的建议以本条为准。
- [M1/E3 22kW复核收口] 2026-07-12：按统一参数表的22kW车场交流功率，完成可复算的双口径结构诊断（目录=`baselines/e3_ablation/e3_multitrip_structure_gate_22kw_20260712_v3/`）：同一449客户、59趟路线下，“每趟回场后补满”的当前贪心见证需要14辆油车+21辆电车；“按需补到够下一趟”在独立/合作两案均找到14+14见证。前者不是全局不可能证明，后者不是最少车辆或正式口径证明；`structure_gate_cleared=false`、`formal_70_authorized=false`未变。排班器、结构脚本与检查均改为读同一`PriceParameters.depot_charge_power_kw`，并补齐电量账和功率一致性校验；严格模式的开新趟门改为按实体车排班而非按路线行数。75项相关回归全过，v3及旧22kW预备目录的hash均复验通过且不含AppleDouble；预备目录因两种充电规则曾写进同名证书，已标`SUPERSEDED_PRELIMINARY_ARTIFACT`。论文原文和Keskin--Çatay部分补能文献的边界、Volvo官方43/150kW事实均写入`docs/handoff/e3_charging_power_and_collaboration_evidence_20260712.md`。未改`prices.py`、成本、旧检查器或封存E1/E2。下一步必须由用户在“保留每趟补满并做更精确排班证明”与“把车场两趟之间按需补电写成正式运营假设后重做小门”之间拍板；改22kW是独立的大参数变更，未执行。
- [M1/E3充电规则终局拍板+最小公式授权] 2026-07-13：用户拍板采纳综合方案（对Codex原话“我同意你的想法”）：①**保留22kW车场交流为主参数，不调参救结果**——07-12“拍板一=升级参数”意向就此正式作废关闭（其40/50/60阶梯此前已因无出处自查作废），后续若22kW正式门确实排不开，快充车场只能作为有来源、单独声明的敏感性新场景，不改主参数；②“每趟回场补满”降级为审计对照尺子（它本来就是07-12可行性审计的保守临时规定，不是论文原话也不是文献强制），只保留已有14+21见证与披露句，不进正式矩阵做重复臂；③**批准把“同一辆实体车两趟之间在真实空档内按需补电、时间/地点/电量连续”写成正式模型最小公式组**。用户附加铁律：新公式新符号必须高精度模仿已发表论文现成表达、不自造，符号适配进现有体系、尽量保留原表达；接入后须用Python数学工具逐项验证，数学上彻底无误，但不否定行业惯例的精简表达、够用即可、保持边界。文献核对后的正式来源边界：Zhen 2020提供趟启用顺序与相邻趟时间骨架；Keskin–Çatay 2016提供部分充电的电量上下界与充电耗时结构；Diefenbach等2023直接支持相邻配送趟间允许部分或完全补电。**纠正Claude规格的一处误用**：Keskin–Çatay的“满电离场不失一般性”建立在车场充电不延迟出发且目标不含本文的分时电价/分时碳排条件下，不能移植为本文结论，因此本文保留首趟继承电量+出发前补电，不强制满电。另纠正一处遗漏：Zhen的车场作业时间不能直接冒充充电时长；本文把“不重叠”和“充电完整落在空档内”分开写，允许等待低碳时段。适配要点：现有z_k^τ推广为车辆–趟启用变量；充电量沿用分时段变量使碳/成本同区间；实体车辆数只数第一趟；首趟继承剩余载重、后续趟可回场重装但不超容量；工作时长按实体车全天。边界不变：不改prices.py/cost.py/evaluation.py，不重判E1/E2封存件，小门链重走后才谈70次。
- [M1/E3按需补电公式与八项核验完成] 2026-07-13：论文已最小接入实体车趟次、趟序、不重叠、趟间充电窗、电量连续、载重重装、实体车数和全天工时规则，并新增Zhen 2020、Keskin–Çatay 2016、Diefenbach等2023三篇来源；XeLaTeX编译通过（23页）。严格排班合同升级为V2，正式默认改为“只补下一趟实际缺口”，旧补满模式仅保留历史复算；新增把趟间补电导出到正式成本/碳账本的接口。八项Python独立核验8/8通过，证据=`baselines/e3_ablation/e3_multitrip_formula_validation_20260713/`，包括旧22kW见证62条相邻趟回放、4类精确正反例、2000个固定种子随机排班、趟链图、精确边界、充电-碳同区间双算和单趟退化。正式70仍未启动。重要剩余门：当前搜索过程尚未自动把证书导出的趟间充电合并回候选解账本，故200/4000评价门在接线完成前不得启动。
- [M1/E3过夜自主推进授权] 2026-07-13：用户睡前拍板，授权Codex按预注册链条整夜自主推进E3直至收口，不再逐门请示：接线（搜索候选合并趟间充电账）→接线短测→V2口径结构证书复发→200评价接线门→4000评价模型门（实测墙钟重排机时）→**正式70次矩阵获得启动授权**（顺序：主梯30→缩减梯20→摩擦轴20，全M1，安全并行runner，默认4 workers上限6）→数据质量完整性门→自动升100（+200c×M0–M5×seeds6–10）。**“成功/有故事”的唯一定义=数据质量门全过+三类预注册结局（理想/中间/负面）之一如实成立+展品数据齐全**；严禁以结果方向补跑、换种子、调参数救故事——那会作废整条证据链。Codex夜间可自修的仅限机械性接线/记账bug（带回归+版本化提交）；模型公式、22kW、成本/检查/评价语义、合同口径、场景资产一律冻结，需要动=诚实HALT留报告等用户。公共充电站解不得静默丢弃：趟内公共站充电走既有路线级规则，严格层只管趟间车场空档，不兼容即HALT。M0在14/14内闭合失败=HALT_M0_ASSET_CAP停下，回落B口径须等用户醒来拍板，不得自动降级。起点与采纳规则照07-12裁定（各层独立合法共同起点+参与理性采纳标记列）。收口产物：完整四件套+机制三块证据（协同/碳/公平）+实体车数变化展品+充电搬家展品+摩擦曲线数据+白话故事报告report.md+HANDOFF逐块run log。文档冲突时优先级：本条与07-13各条＞e3_multitrip_rebuild_ruling_20260712＞e3_e7总设计＞更早文档。
- [M1/E3过夜补充规则] 2026-07-13：正式70前增加不入正式样本的seed99合作可动性彩排（先400评价，只有诊断不清才延长）；200短门必须先验证展品字段能落盘。合作行同时报告两本账：经营采纳值取“合作搜索”和“独立拼合”较优者并记录来源，论文声称合作胜出只能看合作搜索是否严格优于独立拼合。逐行保存成本来源拆分、用电量/充电时机碳拆分、实体车数、总趟数、车辆工作时长、趟间空档、跨场候选尝试/合法/采纳计数与拒绝原因。正式矩阵按种子成套推进；任何能力修复须所有实验方案对称、正式70前冻结、写入算法身份，不得因结果方向补跑或调参。
- [M1/Codex额度与模型切换规则] 2026-07-13：用户要求额度敏感运行。额度发生变化/重置时立即停止全部子代理；重置后切换为`gpt-5.6-sol`、medium思考、关闭快速模式。进入后期稳定长跑且无需再改代码时，切换为`gpt-5.6-luna`、max思考、普通速度。额度只在阶段关口低频读取，不用高频监控浪费额度。2026-07-13 03:02左右主额度由88%变为0%，子代理已全部停止，桌面画面核实当前任务已显示`5.6 Sol 中`且无快速模式标记。
- [M1/E3正式矩阵开跑前最后两次纠错] 2026-07-13：v9正式矩阵在100客户各自经营构造时停机，根因是旧“五五分车型”按车场取整后会造出8辆油车，超过原始7辆上限；提交`4c688204`改为在原7油+7电资产内枚举合法车型分配，未放大车队、未改模型。v10随后在100客户“双方不吃亏”层开跑前停机，根因是两套短路线起点均不满足收益底线；提交`9df03fe8`仅允许该层使用各自经营拼合作为合法起点，合作动作仍必须真实尝试。v9/v10均为停机诊断，正式证据只认v11；两次修复都发生在正式70之前，带回归检查，不按结果方向调参。
- [M1/E3正式100次收口] 2026-07-13：唯一正式目录=`baselines/e3_ablation/e3_v11_clean_20260713/`。先跑70次（200客户六层×5种子=30、100客户四层×5种子=20、跨场费10/25/50/95×5种子=20），70/70满4000评价、零违规、330项阶段文件指纹一致，判`E3_FORMAL70_PASS`；数据质量门通过后按预注册规则自动追加200客户六层×种子6--10共30次，30/30满预算零违规、140项阶段指纹一致，判`E3_PROMOTE100_PASS`。最终`final100`独立复算100份完整成本账、验证100份实体车排班证书、60对同路线充电方案、470个阶段文件指纹，全部通过，最终488项证据指纹零漂移，判`E3_FINAL100_PASS`。52项相关回归通过。未补跑挑组、未换种子、未改22千瓦/280千瓦时/车队上限/模型公式。
- [M1/E3可写结论与边界] 2026-07-13：200客户主场景种子1--10中，合作搜索6/10次严格胜出（必须同时更便宜且真实换场），平均相对各自经营节省0.353%；按预注册多数门通过，但单侧配对秩检验p=0.348，统计把握不足，论文不得写“显著、稳定或普遍有效”。实际实体车数0/10下降，论文禁止写“合作省车”，主展品改为真实客户换场+成本来源拆分。固定同一路线同电量的充电择时3/10有可测减排，合计0.764%，说明严格多趟排班压缩了可移动空档；E3只能作次要张力，碳主结论仍由E4证明。带双方不吃亏条件的10/10最低收益比≥1，公平拒绝证据非空。原始总表在采用各自经营兜底时会把“采用方案碳排”与“合作搜索立即充电碳排”混比，最终报告已直接重算保存的search/naive同路线对，未改原始证据。论文数据表、统计检验、车辆时间线、换场客户、充电时段、公平与摩擦轴均在`final100/exhibits/`；白话技术报告=`final100/report.md`，自包含阅读版=`final100/report.html`。HTML内容结构验收通过，Chrome自动截图检查启动超时，故只声明结构检查、不声明浏览器全交互检查。
- [M1/E3严格执行接线与运行器] 2026-07-13：提交`9f176b92`/`9fb79857`完成实体车排班、趟间按需补电账与搜索候选自动接线；提交`16e4de2e`把跨场尝试/合法/采纳和拒绝原因接入搜索记录，并把公平搜索范围补全；合同升级为`resetp-e1-e7-submission-20260713-v3`，新hash=`f3302719db0ef674b14e1cc016196abec7c3af4fc6923f04a76ac6db40b53ca5`，提交`4364eb9d`。提交`4e2bc074`新增统一种子优先运行器：独立经营先跑并冻结实际分场车型资产，合作层从独立构造的合法短路线起点出发，不热启动旧独立最优解；支持8/200/4000门、seed99彩排、70与自动补30、断点指纹、采纳/严格胜出双口径、证书和诊断。相关59项测试+4子测试通过；8评价真实烟雾2/2通过、满预算零违规，未用于结论。
- [M1/E3正式前200评价门] 2026-07-13：源码审计发现跨场强制动作以`eval%100==0`触发，会被初始扫描先占整数点而整批漏做；提交`a60ca171`改为“达到门槛后补做”，并只在严格E3启用对称的跨车场移除/插入动作。2评价诊断即出现2次跨场尝试、2次合法、1次采纳，证明能力接通但不作论文结论。随后两次HALT均保留为诊断：v4抓到公平层73次跨场尝试全被公平/车队约束拒绝；v5抓到汇总表读取旧公平拒绝字段名。提交`9b224df1`把纯合作与带公平合作分行验收，提交`892b3ea3`修正公平拒绝记账，提交`7aea308f`强制门顺序、把保存解/排班证书纳入hash，并新增“搜索更便宜”和“确实发生跨场的故事胜出”双列。最终干净证据=`baselines/e3_ablation/e3_v7_clean_20260713/preflight/`，判`E3_PREFLIGHT_PASS`：4/4满200、零违规、收费账闭合、23项证据hash齐；纯合作197次尝试/86次合法/51次接受/最终1个跨场客户，带公平两行各73次尝试且262次候选因公平被拒，说明搜索可动与公平闸门均真实。53项相关回归通过。正式70未启动；下一门=200c seed1独立/纯合作各4000评价实测质量与墙钟。
- [M1/E3正常规模门+样本外彩排+展品字段] 2026-07-13：同一冻结源码`7aea308f`下，`e3_v7_clean_20260713/model_gate`判`E3_MODEL_GATE_PASS`：200c seed1独立/纯合作2/2满4000、零违规、13项hash复算一致；独立56.13秒，合作119.43秒，合作3987次跨场候选/1505次合法/804次接受/最终1个跨场客户，故事胜出=true（门数据不入正式统计）。`rehearsal`用正式样本外seed99判`E3_REHEARSAL_PASS`：2/2满400，合作399尝试/147合法/73接受/最终1跨场，零违规。200门保存解已实际导出`preflight/exhibit_smoke/`六类展品字段：车辆时间线177行、实体车对比4行、跨场客户1行、充电时段96行、公平收益4行、摩擦端点2行，判`E3_EXHIBIT_FIELD_SMOKE_PASS`；preflight hash扩为30项且复算全过。所有正式70前门现已齐，下一步按已授权矩阵4 workers、种子成套启动70；不得再改源码/模型/参数。
- [M1/E3资产清单漂移纠正+最终通行证] 2026-07-13：封存v7时发现`prepare_assets()`每阶段会重写资产清单中的`source_commit`，虽资产字节不变，却使前阶段task fingerprint不再对应当前manifest；v7三门因此降为诊断，不作最终授权证据。提交`18c2d834`把资产清单升v2：首次生成即记录22个资产文件hash，后续阶段只读逐文件复核，任何缺失/漂移即停，不再重写。最终同一源码提交、同一manifest SHA `e7beb6f486b12db0ad2857cb274a5712cfc74f25d5fd40547014851d8317b832`在`e3_v8_clean_20260713`连续重发：preflight 4/4 PASS、model_gate 2/2 PASS、seed99 rehearsal 2/2 PASS；各阶段所有raw行`source_commit=18c2d834`，资产清单保持逐字不变。4000门合作3987尝试/1505合法/804接受/最终1跨场，seed99彩排399/147/73/1，均零违规。六件展品字段复制到v8并纳入30项preflight hash。**v8为正式70唯一前置证据，v4-v7只读诊断；源码/模型/参数现冻结。**
- [M1/E3收口监工复核通过] 2026-07-13：Claude只读监工核查v11收口：①头条数字全部独立复算对上——从`final100/exhibits/cooperation_paired_200c.csv`原始数据重算严格胜出6/10、平均节省0.353316%、单侧Wilcoxon p=0.34765625（与decision.json逐位一致）、实体车变化10/10为0、跨场客户每种子1--8人真实存在、采纳来源两口径（cooperative_search与independent_concat_fallback）诚实分账、成本分项逐行严格闭合；②版本链干净——v4/v5/v9/v10均为正式层开跑前的机械性停机诊断（公平记账字段、资产指纹漂移、100c车型拆分越界、公平层起点不可行），**没有任何跑完的正式矩阵被丢弃**，v11是唯一从头到尾的正式版，无结果方向重跑迹象（0.353%/p=0.348的朴素结果本身与注水不相容）；③report.md能写/不能写清单与预注册口径一致，判定=**中间结局偏正**：机制证据成立（合作可动、多数有利、公平真拦截、摩擦划边界），性能主张必须降格（不显著、不省车）。监工确认story_classification="按预注册多数门成立，但统计把握不足"准确。论文写法定调：E3章=机制与边界证据，不作性能宣称；"纸面协同收益在真实多趟排班下缩水至0.35%"本身是相对文献宽松可行性设定的诚实新发现，可作贡献角度；充电择时空间被严格排班压缩（3/10种子、0.764%）作为张力发现移交E4主打。下一步=E4（碳价×充电时机）按总设计开工+E1严格语义镜像小决定+E3章TeX撰写。
- [M1/E3理想结局根因诊断+归属错配轴提案（待用户批）] 2026-07-13：应用户"扳回理想结局"委托，Claude深挖v11数据得出**三把锁**诊断：①客户归属用"最近车场"生成=预先最优化的归属，合作没有错配可修——文献里合作配送的原始客户是预先给定的公司归属关系（07-12已核入案），不是按地理最优分的；②车队本就在可行性边缘（双臂都用满28辆、靠部分补电才排得开），"合作省车"在零冗余下结构性不可能；③两锁之下合作只剩路线毛边可赚→0.35%，n=10撑不起显著性。**实证支持**：换场客户数与节省%的种子间相关系数0.688——搜索挪动7--8个客户的种子节省达2.4--2.9%，只挪1个的种子近零或为负，说明价值确实藏在归属重分配里，只是零错配基线没给出空间。**提案=激活07-12已登记的"归属不均衡"备选敏感场景（原判7/31前不做，重开需用户批准）**：新增归属错配轴E3b——冻结种子化规则把{25%,50%}客户改归非最近车场（模拟历史客户关系与地理无关，一档一张hash锁死的归属表，跨种子共享），200c×M0×seeds1--10×2档=20次新跑；c_tr=0下合作解与归属无关（Codex须代码验证，若不成立则M1同补20次）。预注册产出：合作价值-错配度标定曲线（含0%=v11诚实下限锚点）、各档独立经营实际最少用车数（错配下独立可能超14+14=最强"合作前排不开"展品）、空档/充电择时空间恢复量、各档配对检验。方向性披露：M1不动、M0随错配变差，曲线上升是结构性质，经验贡献=斜率大小+车辆翻转点+双旋钮边界图（错配×摩擦费）。诚实边界：若50%错配下收益仍仅~1%，则"合作价值小"是这类问题的真答案，回到中间结局但边界图更强。配套零成本深度探针：种子4（大胜）与种子7（大负）双臂对称2万评价各1次，判定0.35%是搜索赤字还是真天花板，只作诊断。v11封存不动、全档必报、错配档数25/50跑前锁死不得调。机时约一晚内。**用户批准前不执行。**
- [M1/E3算法层四项证据+受控暖启动对照提案] 2026-07-13：用户批准错配轴后追问算法层改进空间，Claude只读深挖v11得四项证据：①**噪声淹没信号**——同一200c算例10种子的独立经营成本极差2.874%、标准差1.151%，是0.353%信号的3--8倍，p=0.348的根源是仪器噪声不是效应缺失；②**存在性证明（免费展品）**——最好合作解(seed2, 12557.69)比最好独立解(seed2, 12679.34)便宜0.959%，即该算例真实合作差距已证明≥0.96%（搜索不完美，真值只会更大）；③**双盆地**——合作臂各种子要么找到"7--8客户换场矿脉"（落在最好独立解±1%内）要么困在类独立盆地（高出2.75%），是可靠性问题不是价值问题；④**天然实验（最关键）**——v10修复让"双方不吃亏"层从各自经营拼合暖启动，该层在200c封存数据中9/10种子成本低于独立经营、平均改善1.634%、单侧Wilcoxon p=0.00195，其中7胜有真实换场（2胜换场为0=纯加深搜索所得，严格胜出定义已正确剔除）；100c同模式（纯合作臂噪声大到+13%劣、暖启动公平臂反而稳定改善）。**提案（待批）**：在E3b批次加挂受控暖启动对照对——同一种子从同一份各自经营拼合解出发，A臂禁跨场纯打磨、B臂开跨场，各4000评价，B-A配对差=控制算力与起点后合作的净边际价值，200c×10种子×2臂=20次新跑；预注册双口径并存（v11从零涌现口径保留=下限，暖启动受控口径=文献"相对独立现状的改进"标准构念）。**专属算子裁定**：证据支持"边界客户关联移除+跨场后悔值再插入"设计（Shaw关联移除/协同请求交换文献族），但列为第二波条件触发——仅当存在性证明+受控对照+错配轴仍撑不起头条时，一次性预注册重跑，不迭代调算子防p-hacking。预算加深由已批探针的证据决定。加种子救显著性=可选停止，禁止。
- [M1/E3理想化计划正式落盘] 2026-07-13：应用户要求（要文档不要提示词、保守优先、大改动挂账），Claude落盘**`docs/handoff/e3_idealization_program_20260713.md`**为E3理想化唯一权威源，总设计文档新增第18节指向之。内容：三把锁诊断+四项证据链（E-1噪声/E-2存在性0.959%/E-3双盆地/E-4天然实验9/10、1.634%、p=0.00195）；三波改动总表（Wave1已批=错配轴+探针+存在性展品+受控暖启动对照对；Wave2挂账=专属算子包全设计含伪代码；Wave3挂账=预算加深凭探针证据）；**联动影响矩阵**——E2封存零影响（tag/commit/hash隔离+E3合同域开关+显示层不重读prices.py）、E4数据源钉死v11 M5、E6复用v11 M4/M5、E7身份版本一致性、错配归属表为新文件不动冻结算例；专属算子安全接入五条（同一evaluate/检查门、自适应降权+轮盘份额上限、M0合同下自动退化保对称、EvalBudget计数不变、E3开关域隔离默认关）；**有效性诚实边界**——元启发式算子不能保证先验有效，能保证的是安全性+降权+一次性验证协议（非正式种子90-99配对、预注册通过线=矿脉命中率提高且均值不劣、一次性不迭代）。文献核验台账分"已核/待核"两列：Shaw 1998大动作思想与Ropke-Pisinger 2006自适应权重公式已于07-13从Keskin-Çatay 2016原文第5-6页逐字核得；Keskin算子清单页码、regret-k页码、Cruijssen 2007与Gansterer-Hartl 2018两文入库核验挂账待Zotero恢复。禁止台账重申：加种子/事后并池/不对称预算/结果方向操作/迭代调算子。
- [M1/E3错配轴监工核查：定性成立，量级须修复后才可写] 2026-07-13：Claude只读核查两个正式批次（`e3_mismatch_formal_20260713_25/`与`_50/`）。**定性判决成立**：25%/50%两档均10/10严格胜出、预注册精确双侧符号检验p=0.001953、每档单张hash锁死归属表（map_seed=20260713）全种子共享、合作臂始终锁定原始14+14（25%档seed8仅用27辆）、独立经营在两场均判ASSET_INFEASIBLE（25%档测量需D0=8油8电/D1=8油7电，50%档D0=7油12电/D1=8油8电）——"条件性理想结局"定性框架正确，用户与Codex对Claude两处过度表述的纠正（0.959420%只是两个启发式最好解之差非真实下界；种子波动只是诊断不能断言p=0.348唯一成因）**全部接受并采纳**。**但发现一处论文级硬伤**：decision.json记录`cooperation_budget=4000`对`m0_budget_per_depot=200`——合作解用4000次评价优化，节省却按"各自经营的测量起点"（每场仅200次评价）计算，**十倍预算不对称，16.553%/24.719%的量级会被审稿人一击毙命**；且ASSET_INFEASIBLE判定建立在200评价的弱优化路线上，25%档测量车数仅超资产各1辆，更强的独立搜索可能挤回14+14，可行性翻转结论同样暴露。该设计偏离已批E3b任务书的"独立臂4000次评价"条款（Codex在文档中有披露但未标为风险）。**处置=公平预算修复批（已叫号待执行）**：同两张归属表、同种子1-10、同冻结合同，独立臂按v11先例每场2000次评价（两场合计=4000=合作臂），重出最少车数证书与资产可行性判定，重算节省/严格胜出/符号检验；200评价版降级为SUPERSEDED_MEASUREMENT_DIAGNOSTIC不删除；无论修复后数字变大变小一律采用，跑前锁死。修复前论文只可写"合作在原始资产内可行、错配下各自经营的弱优化测量已超资产"这类预算无关表述，禁止引用16.553%/24.719%。深度探针跳过与暖启动十种子取消两项偏离=已披露、按最低成本规则可接受；E-4天然实验因果解读按新门控证据降级同意。
- [M1/E3理想化支线正式收口] 2026-07-13：按用户授予的自主权、最低成本探测规则和预注册档位，完成Wave 1实际执行并收口。源码先读后改：新增`allow_cross_depot`合同开关，默认值保持True，E2/E1历史路径不变；关闭时同一份候选生成、严格排班、收益检查和公平检查全部加车场归属锁，微型反例与`py_compile`均通过。v11封存目录和E2封存证据未改。暖启动只做到门控：`baselines/e3_ablation/e3_warmstart_controlled_20260713/gate/`两臂同起点同预算，A=12988.0817、B=13162.5641；B尝试368次，完整候选111次被公平/车队/路线约束挡住，合法0、采纳0，故未烧十种子暖启动正式统计。随后先做25%与50%单点4000长探针，均公平通过且跨场空间稳定打开，才进入正式档。正式证据目录：`baselines/e3_ablation/e3_mismatch_formal_20260713_25/`与`..._50/`，每档固定一张归属表、种子1--10、合作4000评价、独立测量子问题各200评价，raw_runs/metadata/decision/report/每种子成本七项与排班证书/372项指纹齐全。25%档10/10严格胜出，平均节省16.552646%，精确双侧符号p=0.001953，合作用车27或28；独立测量车数D0=8油+8电、D1=8油+7电，均超过原始每场7+7。50%档10/10严格胜出，平均节省24.719286%，p=0.001953，合作用车全为28；独立测量车数D0=7油+12电、D1=8油+8电。两档测量车数明确不是新资产，合作始终锁14油+14电。新增零运行审计目录`baselines/e3_ablation/e3_idealization_audit_20260713/`纠正旧计划两处过度表述：v11 seed2的0.959420%是两个启发式最好解之间的差距，不是真实下界；独立成本波动只作诊断，不把p=0.348归因于单一噪声原因。判决：**E3_MISMATCH_FORMAL_IDEAL（条件性理想结局）**——在固定算例/固定错配表下，错配打开了公平可行合作空间并产生显著、可归因、伴随资产内不可行对照的降本与车辆故事；不得外推为普遍规律。Wave 2专属算子和Wave 3预算加深均未触发。权威计划与总设计第18节已同步收口状态；下一步只做论文口径/展品汇总，不再追加第三档或结果导向补跑。
- [M1/E3理想化边界数据] 2026-07-13：`baselines/e3_ablation/e3_idealization_audit_20260713/mismatch_x_friction_data.csv`把v11已有跨场费轴与本次25%/50%错配轴并排保存；每行明确来源、样本量和`factorial_complete=false`，只供后续画图，不把不完整组合冒充完整析因边界。
- [M1/E3论文正文完整预览] 2026-07-13：用户否决首版自拟“概念总结图”，要求按既有图表壳完整展示从第一份计划、严格主线到错配支线的全部论文正文。首版概念图/表/草稿已撤销，权威预览改为`docs/paper_submission_final/e3_full_preview_20260713/E3_FULL_PAPER_PREVIEW.md`。正文含13节、两张正式图和12组正文/附录表：发展过程、六层设计、六层总结果、合作逐种子、合作成本来源、Cheng2022 Fig.6壳的48槽充电图与逐种子表、公平逐种子、普通响应壳的摩擦图与汇总、100规模复核、暖启动门、错配汇总与20行逐种子附表、能写/不能写、质量审计。未给无参考壳的合作/公平/错配硬造图片；这些用三线表。明确两项论文边界：主线不得写显著/省车；错配成本差的独立侧为每场200评价测量、合作侧4000评价，车辆可行性为硬证据，16.553%/24.719%不得冒充同预算纯合作因果效应。生成器=`baselines/e3_ablation/build_e3_full_paper_preview_20260713.py`；未改`paper_main.tex`、E2或v11封存证据。
- [M1/E3整改总计划：四计划合一+治理规则] 2026-07-13深夜：用户判定Codex规划层面已失控（同日产出"判决+八阶段""五展品施工卡""标题规则反转"多份互相冲突提案），指令Claude收拢。Claude落盘唯一有效版=`docs/handoff/e3_rectification_master_plan_20260713.md`，取代Claude故事剧本（已标作废）、E3完整预览（降级内部证据索引）与Codex两份对话提案。**治理规则（用户裁定）**：Codex此后不产计划/框架/手册，只按总计划第5节领编号任务（T1公平预算探针→T2补齐重跑→T3论文数据层→T4展品施工卡→T5总设计§9三处最小修订→T6隔离绘制；T7正文由Claude起草），越界即停。14项争议裁决要点：图表标题=中性名词短语；四幕=全文结果篇弧线（E3节只答"归属如何影响合作价值"一问，时间线→实验设置、公平→E6、充电→E4、摩擦→附录）；成本拆分小表留E3正文；三情境并列禁连线；公平拦截数262为Claude误引早期门控数据、正式数=23,674（一页纸已更正）；**"条件性理想结局"降级为待公平重跑确认的初步观察（含车辆结论，25%档仅各超1辆有翻转风险）**；修复批先探针（两档各seed1、独立每场2000评价）四查全过自动补齐；数据契约"机制无效果=设计回炉"条款删改为"无效果也是正式结果，补实验只能由预注册缺口/比较不公平/证据不足触发"；建论文专用数据层隔离台账与正文；预锁三种结局防按结果改故事。数字纪律清单与验收定义见总计划第3/6节。
- [M1/E3证据驱动最终拨正与论文层完成] 2026-07-13：用户随后授予自主判断权，并强调旧故事/旧实验也可被证据推翻、实验必须服务正文。完整复核源码、v11、25%/50%错配、参考论文与成本/车辆/充电语义后，旧整改总计划降为历史：单图两张随机错配表、预算不对称、所谓最少车辆非证明，不能作为正文主设计。新权威源=`docs/handoff/e3_evidence_led_reconstruction_20260713.md`。客户组合改为全部9张正式网络的成对类别（最近车场地理聚集 vs 班次×需求档内一次锁死的空间混合），一个网络不删；零搜索生成18个严格起点与每网唯一共同充足车队上限。正式同起点、同种子、同4000评价比较共54对/108次，54/54 PASS、全部零违规/成本闭合/解和证书齐。地理聚集平均节省4.156758%、7/9网络为正、双侧符号p=0.1796875；空间混合平均19.826082%、9/9、p=0.00390625；配对增量15.669325个百分点、9/9、p=0.00390625。空间混合27/27种子对均真实换场且降本。来源=里程11.907pp+燃油6.798pp+电1.247pp，派车固定费反增0.126pp；实体车仅2/9网络减少、4/9增加、3/9不变，故“合作省车”正式否定。静态首趟充电已改为明确第-1日前置窗口，64项定向测试与小探针通过；旧v11的0.764%碳数字冻结，E4必须用新时钟重跑。论文两图一表、附表、中文正文预览和独立指纹在`baselines/e3_ablation/e3_paired_cost_formal_v2_20260713/paper/`；图表组织参照Soriano等2023的“客户类别示意+按类别/规模汇总”，但一件展品只讲一件事。E2封存未重算未重判。
- [M1/E3证据驱动重构监工审计：科学过关，两项治理阻断] 2026-07-13深夜：Claude只读核查`e3_paired_cost_formal_v2_20260713/`。**科学面全部过关**：①头条数字逐位复算一致（聚集4.156758%/7/9/p=0.1797、混合19.826082%/9/9/p=0.0039、组内差15.669pp/9/9同向、混合27对全部严格胜出且真实换场）；②预算对称铁证=raw_runs 108行budget与evaluations全为4000、violation全为0；③同起点铁证=54对中两臂start_sha256逐对相同54/54；④成本分项闭合0行不符；⑤聚集臂不显著如实报告、无结果方向门控（decision明记result_direction_used_as_execution_gate=false）；⑥设计=九网络×两类客户组合×3种子成对对照，统计单位取网络（9个等权）避免伪重复，聚集档449c均值0.674%与旧v11的0.353%量级吻合。**但两项治理阻断在"直接进论文"之前必须清除**：阻断一=`cost.py`与`check.py`处于已修改未提交状态（首趟充电时钟修复：新增charge_day_offset<0前一日预充语义，供电但不占当日出发前时间）——保护文件改动未经用户明批即落地，违反READ_ME_FIRST§4红线；技术评估=改法向后兼容（旧解offset默认0走原路径），但必须补：用户明批+封存证据不变性回放（抽样重算E2/E1/v11已封存解，证明新旧代码成本逐位相同）+版本化说明。阻断二=正式批证据与全部代码改动均未提交，"数字绑commit"纪律断裂，须分层提交（保护文件修复单独一笔并注明批准依据；正式证据一笔；文档一笔）。旧0.764%碳数字因时钟错误冻结、E4须新时钟重跑——同意；v11的0.353%等保守边界口径继续可用（时钟修复不改旧解复算的前提待不变性回放证实）。
- [M1/论文北极星落盘] 2026-07-13深夜：用户暂缓时钟拍板，提出根本性迷失（目标/问题/现实依据/原创性/实验覆盖全存疑，恐重讲别人的故事）。Claude落盘`docs/handoff/paper_north_star_20260713.md`作唯一校准文档：重申07-12论文定位终局裁定仍成立；原创性=四交叉点，E3错配故事单独成篇=协同文献旧路（用户直觉正确），作为"锁二弹性"支柱=成立；实验-主张对账判定设计覆盖充分，唯一大洞=中心命题（时间杠杆）直接证据未跑，E4=全文主检验；E4两种结局预注册（杠杆可观/杠杆被深锁均为可写原创结论）；关键路径与Not-To-Do更新。E3停止打磨。
- [M1/论文全书蓝图落盘] 2026-07-13深夜：应用户"章节/子标题/内容/图表形态/数据/实验设计全链条"之问，Claude落盘`docs/handoff/paper_blueprint_20260713.md`：唯一研究问题（时间杠杆×四把锁）→五章结构（引言四事实一缺口/模型章含多趟公式与两张信任状图/算法章/实验章4.0设置+4.1算法地基E2与E2b+4.2锁二E3已完成禁再动+4.3锁一E4主检验+4.4锁三E6+4.5锁四E7+4.6稳健性/结论管理启示）→11张核心图表的"形态-壳-数据-实验反向约束"因果链总表→三条系统性推论（E4五字段先写死再跑、E6三态列、E7继承schema预写）。不推翻任何已批壳与07-12对账；E3展品以paired_cost_formal_v2新四件为准。
- [M1/防嫁接审计与机制联动链] 2026-07-13深夜：应用户核心关切（"照别人论文做实验=换算例的嫁接，但彻底不模仿又狗屁不通；机制联动必须串联思考"），Claude定嫁接检验法（借形不借魂；检验句=把图放回源论文成立即嫁接），逐实验审计并补进蓝图第5节：E2俗套无需修（地基定位）；E2b须含±碳感知充电调度单独臂；E3已自救（严格排班计量+距离非省车反写文献+柱子定位），补零成本空档读出；E4嫁接风险最高，处方=充电搬家图做"独立vs协同"双版本（复算E3配对批两臂解，零新搜索）；E6次高，处方=公平溢价分项拆解（用距离付还是用时间弹性付）+绑定动作类型；E7天生原生但字段必须兑现（阶段级错配度/空档扰动/公平绑定三协变量）。机制联动链=弹性开门→空档定量→签约定价→动态验存，每实验输出一个联动变量被下游消费；全部为字段/复算级加法机时近零，写死进未来E4/E6/E7/E2b任务书。
- [M1/蓝图与北极星纠偏：采纳Codex双审计] 2026-07-13深夜：Codex两份只读审计到达，Claude逐条核实后**认账并采纳大部分纠正**：①蓝图"全文唯一=时间杠杆+四把锁因果链"违反07-12定位裁定禁令（不得把协同/公平/动态降为充电附属）——蓝图§0已恢复07-12双层定位原文，四机制改为并列兑现条件研究问题，Codex操作化总问题措辞并入；②E3→E4"协同打开空档"假说被E3原始数据证伪（聚集下空档增1/9减8/9、混合下增3/9减6/9，且总空档≠可搬动充电量）——联动链降级为待验证假说，唯一进任务书的跨机制加强=E4"独立/协同×立即/低碳"双重比较（方向不预设）；③"嫁接检验法"（图放回源论文）只能查最浅重复，已换为防嫁接四问（邻近文献群/新比较/可单独识别/删字段结论不变），"四机制交叉=原创""协同文献全是松弛口径"等断言作废（反例：2026共享充电站协同排程、Wang 2025协同+动态、Zhang协同+分时充电、Cheng分时碳强度充电）；④E4设计重写（蓝图4.3修订版）：主实验=零搜索复算（时机单独隔离），统计单位=网络×电网日非种子（5种子最低p=0.0625撑不起显著），碳价与时机分离（择时程序不读碳价）且降为次要敏感性、称内部/情景碳价（英国配送车队购电不直接入碳交易体系），Shi图7壳与碳价响应不匹配须另裁，展示用预注册中位案例；⑤E6成本分项=描述性辅助表非因果、拒绝归因需候选级代码改造不作头条；E7降为动态存续检验（当前四臂无关公平对照、事件生成器未给新客户历史归属，"动态天然制造错配"不成立），错配/空档字段仅附录诊断；E2b纯算法故事。**Claude过时结论更正**：时钟修复与108次实验已在提交90654b3e捆绑推送，三层拆分提交建议作废（改远端历史风险更大）。**Codex新查实两个真阻断**：metadata.json来源提交号写旧号（指纹对新号）须补正说明；部分保存解读取代码丢前夜预充字段（抽样22次前夜充电重读后全为零，metaheuristic_baselines.py:597一带）——E4开跑前必须完成保存-读取往返检查修复。**待用户明批**（总体自主权不覆盖保护文件）："批准首趟充电时钟修复作为后续正式语义；同意先做封存回放、来源补正和新格式保存读取检查，三项通过后生效。"E3三件展品冻结但绘图脚本分组柱与计划成对点线不符，须按参考壳裁定不得静默换壳。
- [M1/E3-E4论文读者层最终收口] 2026-07-13深夜：用户已明批首趟/前夜充电语义；保存读取往返、旧封存证据回放和来源补正均通过，代码与证据已分别在`e98c4ab8`、`c0ddc7b9`、`f636b126`、`9978517d`等提交中封存。正式充电复算固定108份严格配送方案，在28个完整电网日上只改变合法充电时刻、零路线搜索：充电环节排放下降3.888%--4.877%，但油电混合系统运营总排放仅下降0.352%--0.523%；95.5%--99.7%的净节省来自前夜首趟充电，合作未稳定放大该收益。E3正文改为两层分开：先量出客户责任按地理关系组织后总成本/距离/燃油直接排放/用电量分别下降25.002%/35.478%/41.729%/23.869%（9/9同向），再量同起点同4000评价下跨车场重新分工的剩余价值（地理划分4.157%、7/9、p=0.180；空间交错19.826%、9/9、p=0.0039）；空档缩短仅可在同服务且路程、成本、能源下降时解释为排班更紧，不单独证明效率或充电灵活性。读者层权威产物=`docs/paper_submission_final/e3_e4_story_20260713/PAPER_STORY_PREVIEW.md`，含3图2正文表及完整附表；每件展品只讲一事，内部实验代号已清除，参考图表壳与相邻结论均就近引用。九张网络的最大客户/车场点间距离逐文件复算为168.716--248.085公里，现实口径冻结为“基于英国城市坐标构造的区域/城际配送场景”，不冒充真实道路、企业订单或城市末端配送。生成器报告`sealed_inputs_unchanged=true`、`route_search_evaluations=0`，产物和全部输入指纹独立复核无不一致。`paper_north_star_20260713.md`与`paper_blueprint_20260713.md`已改为结果驱动版；旧“时间杠杆唯一主线/四把锁”作废。现有`paper_main.tex`仍混有80 kWh、60 kW、旧时钟和旧批次，禁止零碎塞入新百分比，须在剩余公平/动态/算法部件决策后一次性同步语义、参数、图表、文字和引用。
- [M1/E2—E3 SETP正文预览与图表母版重制] 2026-07-14：在不重跑任何路线搜索、不改E2/E3封存证据的前提下，完成可编译的6页《系统工程理论与实践》格式正文预览：`docs/paper_submission_final/e2_e3_preview_20260714/paper_e2_e3_preview.pdf`与`.tex`。正文含E2两表一图、E3三表两图、结果边界和附录逐网络结果；新增陈婉茹等（2023）版式来源的就近引用。图表全部由`baselines/paper_story/build_e2_e3_setp_preview_20260714.py`从封存CSV生成，嵌入纯矢量PDF；字体核验为中文宋体常规、英文数字Times New Roman。主母版为陈婉茹等（2023）：E2收敛图按其Fig.2，E3节省柱图按其Fig.5，三线表按其正文表；客户责任结构图因陈文无同型图，完整采用Soriano等（2023）Fig.3的无坐标轴并列类别图、红色“+”/绿色“×”、面板标题和分面数量图例。禁止跨论文拆件拼装的全文纪律已写入`docs/handoff/memory/figure-redesign-task.md`；实测参数、母版边界和例外规则见`SETP_VISUAL_CONTRACT.md`。XeLaTeX双编译通过，无未定义引用、无溢出版面；仅有相对类路径和本机MiKTeX更新提示，不影响PDF。
- [M1/E2—E3主TeX正式整合与读者层拨正] 2026-07-14：在不重跑路线搜索、不改封存结论的前提下，已将E2、E3直接写入`docs/paper_submission_final/paper_main.tex`并生成18页`paper_main.pdf`。E2按9张独立网络而非90次重复运行统计：Friedman总体差异显著，TVCI-ALNS经精确Wilcoxon+Holm后显著优于GA/PSO/VNS/ACO/GWO/IWD，对LNS与GA-VNS不宣称显著；旧“IWD 90/0/0”实为TVCI-ALNS对IWD的90胜，方向歧义已删除。E3把“地理聚集的直接效率”与“开放合作后的剩余增量”分开写；四项9/9同向只在表注统一说明，不重复堆计数。收敛图按陈雨蝶等（2025）Fig.4的同型多算法曲线重制，九算法全部展示且全用英文缩写；用户最新硬规则已贯彻到全部正文数据图：图例只放图内右上角，通过延长坐标范围预留空白，不遮曲线/散点。正文常规表统一0.84倍行宽、附录宽表0.92倍行宽。E2/E3/已完成充电复算均使用同一九网络；E6/E7仍无正式结果，114客户中位网络只作低成本探针，通过后才扩展同一九网络。旧未核E4/E6/E7结果不进入当前渲染稿。生成脚本、矢量图、三线表、网络级统计审计和逐网络附表均已落盘；XeLaTeX通过，无未定义引用、交叉引用或溢出版面警告。
- [M1/仓库交付入口] 2026-07-14：应 Devpost/OpenAI Build Week 私有代码审查要求，新增根目录 `README.md`，公开说明项目范围、最低成本自检、论文编译入口、证据纪律，以及 Codex CLI 0.144.1 与 `gpt-5.6-sol` 的实际使用方式和边界。远端复核：`https://github.com/zlxshu/ReSETP` 保持 PRIVATE，默认分支已设为当前有效的 `codex/reporting-pipeline`，README 可由仓库根页直接读取；`testing@devpost.com` 精确匹配 GitHub 账号 `devposttesting` 并创建邀请 `325554869`，`build-week-event@openai.com` 经 GitHub 网页邮箱匹配创建邀请 `325555158`，两者均处于待接受状态。个人账户私有仓库的协作者邀请显示为 write，无法细分 read；严格只读须迁到组织仓库。本次只跑控制台自检与11项最小单元检查，不运行正式实验、不改实验合同和论文结论。README提交=`fa8d0aad`。
- [M1/E2-E3正文排版与IWD口径修正] 2026-07-14：`docs/paper_submission_final/paper_main.tex` 完成四项拨正。表2中的10/25/50/95 £/客户不再作为表格数据行，改为表后参数说明；尚无正式证据的算法组件、充电时机、公平和动态实验保留正文占位，不引用旧试跑；附录移至参考文献前，并在 `\appendix` 前强制换页，避免结论页被重置为罗马页码。只读复核确认收敛图读取 `e2_final_10seed_20260711` 封存批次的旧简化IWD：221客户网络10次运行均未改善共同起点，水平线是真实失活表现，不是绘图错误，也不能解释为稳定收敛；后续按原始机制修复的IWD仍未通过预注册有效性门。因此IWD只留作描述性辅助对照，不进入强推断；Friedman检验改为8种通过核查的算法，成对Holm校正改为7项有效对照，正文结论由“显著优于6种”收紧为“显著优于5种”。全程零新增搜索，仅重算论文统计与重绘既有封存数据；PDF两遍XeLaTeX编译通过，共18页，无未定义引用、越界表图或致命错误。
- [M1/E3关账监工确认+E4状态澄清+全局重校] 2026-07-14：Claude只读核查`3b8db869`论文拨正批：①IWD降级与E2口径收紧**合规且方向正确**——统计单位改为9张网络（种子非独立样本）、Friedman限8种通过核查算法、成对Wilcoxon×7+Holm校正、显著优于GA/PSO/VNS/ACO/GWO五种、对GA-VNS与LNS降幅4.95%/2.48%但校正后不显著故不作全面优于表述；全部改动方向=收紧主张（弱化而非加强），与IWD忠实度FAIL既有裁决一致；收敛图水平线=旧简化IWD真实失活的诚实披露。②**E3正式关账**：两层故事（地理组织价值25.00%/35.48%/41.73%/23.87%九网络同向+跨场剩余价值4.16%不显著/19.83%显著）已入主TeX与SETP格式预览，展品母版合同（陈婉茹2023+Soriano Fig3、禁跨论文拼装）落盘。③**向用户澄清：E4主体已完成非待开始**——`9978517d`(07-13 22:18)封存108份严格方案×28个完整电网日的零搜索充电复算：充电环节排放降3.888%--4.877%，**但系统总排放仅降0.352%--0.523%，且95.5%--99.7%的净节省来自前夜首趟充电，合作未稳定放大该收益**。此即预注册结局二（时间杠杆被严格运营深锁、集中于前夜时段）；摘要重心已相应合法转移（组织价值+协同剩余价值为主、择时为小而免费的杠杆），符合"以结果定故事"预注册而非按方向挑选。④E4剩余事项=次要碳价重优化敏感臂（是否跑待定，预期收益小可按止损裁减）+E4结果整合进主TeX；北极星"E4两种结局"预注册命中结局二，北极星与蓝图相应段落待同步注记。⑤余下关键路径（今日7/14，余17天）：E2b算法拆分（正文已留占位）→E4整合+碳价臂决策→E6公平→**E7动态（最大剩余工程与叙事风险）**→全文组装打磨。
- [M1/监工更正：E4状态两层澄清] 2026-07-14：更正Claude上一条"E4主体已完成非待开始"的表述——它混淆了证据层与论文层。准确状态：①证据层：`baselines/e4_e5/e4_forecast_timing_formal_20260713/`确实存在一批九网络×28电网日×108份封存方案的零搜索充电复算，decision判`PASS_E4_FORECAST_TIMING_FORMAL`、机械检查17项全过、附claim guard与"结果方向未门控执行"声明，设计恰好符合蓝图修订版E4原则（复算优先/按预测安排按实际结算/网络×日为统计单位/未混碳价）；但②论文层：07-14重规划（HANDOFF"E2—E3主TeX正式整合"条）明确"旧未核E4/E6/E7结果不进入当前渲染稿"，充电章在正文只留占位——该批次未经监工数字复算、未经读者层验收，论文意义上的E4确实尚未开始，用户认知正确。0.352%--0.523%等头条数字出自该未核批次，在监工复算通过并经用户接纳前不得写入论文。下一步两选一（用户拍板）：A=Claude先只读复算该批头条数字+核对其设计与新规划E4章需求的匹配度，通过则E4变为"验收+写作"任务省去重跑（推荐，省数天机时）；B=判定其为方向诊断，按新规划另设计E4。
- [M1/E4只读复算与主TeX整合] 2026-07-14：按最低成本原则执行A案，不重跑路线。独立从`raw_runs.csv`与`action_runs.csv`复算9072行结果、132300条充电动作和3024个基础配对，确认三种充电规则齐全、路线/服务/能量不变、充电量与燃油量成对一致、零时钟与硬约束违规，262个清单文件零缺失零哈希漂移；四种情境的充电排放降幅3.888%--4.877%、运营总排放降幅0.352%--0.523%、网络改善数9/9、日改善数18--20/28以及前夜贡献95.46%--99.71%均逐位闭合。现有证据据此升格为正式论文证据；碳价重优化臂继续挂账，不作为当前E4关账前置。新脚本`baselines/paper_story/build_e4_setp_integration_20260714.py`仅从封存证据生成一图一主表和28日两张附表；图形整体语法参照Cheng等(2022)图6并在正文新增准确文献，不沿用旧粗糙图。`paper_main.tex`已替换E4占位，摘要、贡献、结论同步收口，论文口径为“充电择时真实但系统贡献较小，协同未在网络与电网日两个维度同时稳定放大该收益”。XeLaTeX两轮通过，21页，无未定义引用、越界图表或致命错误。
- [M1/E4正式关账终审] 2026-07-14：在上述整合后再次执行证据目录—论文数字—图表—引用—提交号五处对账。262个登记文件哈希零漂移；3024对立即充电/按预测择时记录的路径、服务、总充电量逐对相同，9072行均零硬约束与时钟违规；四情境头条数字、9/9网络改善、18--20/28日电网改善及95.46%--99.71%前夜贡献从原始行独立重算后与论文逐位一致；21页主PDF重新编译通过。E4据此正式关账，后续不得为追求更大碳效应改写本批；碳价重优化仅保留为论文主体完成后的挂账敏感性任务。E3及以前另以远端标签`e3-paper-freeze-20260714`固定在`de0ce77d`，后续E6/E7均须独立提交并可整批回退。
- [M1/E6参与条件正式整合] 2026-07-14：在不改E2--E4封存证据的前提下，独立复算E6正式批54组配对、162行搜索，确认两合作方案同起点、同4000次完整评价预算、成本与双方收益闭合、严格多趟证书有效、E3封存哈希零漂移。双方均不比各自经营差时，地理聚集与空间交错情形相对各自经营平均节省2.613%和16.590%；相对不限制单方收益的合作方案，参与底线使系统成本平均增加2.061%和2.141%，逐网络范围分别为0--7.79%和0--4.23%。正式程序会在各自经营与两条合作搜索的最终保存结果之间按嵌套可行集筛选，负增幅在结构上被排除，故撤销符号检验概率值，只作效应大小与网络计数描述；正文明确这些是固定预算内的已观测方案，不宣称全局最优，也不把参与底线等同于事后利润分配。主图只展示参与底线的系统成本增量，论文两轮编译通过（23页，无未定义引用或版面越界）。独立复算证据=`baselines/e6_fairness/e6_participation_audit_20260714/`，论文数据层=`docs/paper_submission_final/e6_integration_20260714/`。
- [M1/E7事件流、连续接续停止证据与单事件主算法探针] 2026-07-14：E3及以前继续由标签`e3-paper-freeze-20260714`冻结，本轮未改`cost.py/check.py/search/evaluation.py`。正式动态算例固定为同一221客户、10油车+10电车网络；五条订单流重新按整趟发车时刻检查，275/275个取消或变更事件均满足在固定起始方案中至少提前60秒获知，输入与归属表逐文件锁定于`baselines/e7_dynamic/e7_v2_20260714/event_streams/`。新增实体车连续接续排班和已发车整趟承诺记账模块；17项相关最小测试通过。零路线调整的连续两次接续检查在第一轮可行、第二轮停止，判`HALT_E7_DYNAMIC_CONTINUOUS_TRIGGER_GATE`，说明仅拆出单独车次不足，实际搜索次数为0；该不利结果保留，未据此修改事件流。随后只取seed1中按跑前规则选定的一个新增订单做100次主算法小探针：38个候选实际改变原安排，28个通过原有20辆车的连续排班检查，13个被接受；尚未执行部分成本由1404.084765降至1277.681549（9.003%）。判`E7_P2_SINGLE_EVENT_ALNS_PROBE_PASS`仅表示单事件接入和主算法路径真实工作，绝不是E7正式平均结论；五条订单流正式批尚未启动。证据=`baselines/e7_dynamic/e7_dynamic_continuous_trigger_gate_20260714/`与`baselines/e7_dynamic/e7_v2_20260714/preflight/p2_single_event_probe/`。
- [M1/停机报告监工把关：E4/E6复核通过，三项批准裁决建议] 2026-07-14：Claude只读把关Codex停机报告。①**对账**：报告略滞后于仓库——HEAD已是8937a2df（07-14 13:02，按"车次未出发"新规则重生的冻结事件流+连续触发门+单事件探针脚本，经grep确认零保护文件改动），工作区仅剩未跟踪的E7预检产物（full_stream1_400等）与正式脚本未提交；报告所述"6个旧文件含check.py/evaluation.py的未提交改动"已从工作区消失=清理实际已执行（恢复未提交改动到已提交状态=复原保护文件，本不需批准，追认即可）。②**E4独立复核通过**：从network_summary/aggregate_summary逐项对上——充电环节降幅四格4.664/4.877/3.888/4.107%（即3.89--4.88%）、系统总排放0.518/0.523/0.352/0.476%（即0.35--0.52%）、前夜占比95.46--99.71%、网络层9/0改善p=0.0039、预测捕获率85--89%、oracle上限4.4--5.7%；"合作未稳定放大"与日轴符号检验混合结果一致。③**E6独立复核通过**：decision/audit/论文三处数字对齐（参与底线下节省2.613%/16.590%、参与溢价2.061%/2.141%相对不设底线口径、公式在正文明示、1.91/1.84为另一口径的百分点数未混用）；54对完整、双臂预算均4000、对内同起点、成本闭合1e-12量级；**撤符号检验的理由正确且高级**——合并筛选规则结构性排除负增幅使符号检验失效，改报幅度与网络分布，此为论文层统计自律。④**三项批准的裁决建议**：一=追认清理（已完成，方向正确），补一小项：未跟踪预检产物须按四件套纪律提交或明确归档；二=同意E7范围方案（221客户代表网络×5事件流为主+较小较大网络各1条稳健流，结论限定"代表网络下的动态压力测试"），并补钉臂结构按既定降级裁决（完整vs禁跨场、完整vs立即充电，公平只报满足性与最低收益比）；三=同意算法治理（低成本探针自由；替换正式算法身份/改写E2/重跑正式算法表须用户再批）。⑤路径提醒：今日7/14余17天，E7=最大剩余风险应最优先，其后E2b、算法章重写与终审；IWD修复与专用算子为可选项按止损裁。
- [M1/E7完整单流检查通过] 2026-07-14：在同一221客户网络、冻结seed1订单流和原20辆车内，合作经营与各自经营均逐次处理全部55个事件，每阶段固定400次评价、各22,000次，客户守恒全过。开发过程中完整保留两次停止证据：第一版各自经营在第27次变化因局部调整不足停止；加入尚未发车客户整体重排后第二版挤占局部调整次数，合作经营在第13次变化停止；最终按“前200次局部调整/整体重排交替、后200次保留局部调整”固定顺序，两处均通过。第三版单流检查判`E7_PAIRED_PREFLIGHT_PASS`；合作累计成本低10.006%仅为单流开跑门，不进入论文。正式五流尚未执行，程序与10项最小测试先独立提交，保护文件继续零改动。
- [M1/E7五流400次正式批停止+运行纪律更新] 2026-07-14：固定提交`b94388ab`、五条冻结流、合作/各自经营同每阶段400次的正式批完整执行并判`HALT_E7_PAIRED_DYNAMIC_VALUE`。10组中6组走完、4组停止：seed2各自经营第11次、seed3两边第24次、seed5各自经营第20次未找到原20辆车可继续执行的安排；停止前阶段、拒绝原因和客户守恒证据均保留。只有seed1/4形成完整配对，合作成本低9.044%/1.516%，因样本不完整不得进论文。下一步仅对四个停止位置做较高预算诊断，不按结果改事件流。用户同时冻结运行习惯：安全时通常6--8进程；短跑5分钟、长跑15分钟、中间档10分钟探测；统一由`scripts/run_with_status_hook.py`写状态与结束标记。
- [M1/E7失败点预算检查] 2026-07-14：只重跑正式400评价批中停止的4个位置，每阶段提高到800次并用4进程并行；2/4恢复，seed2各自经营第11次变化与seed3合作第24次变化仍为800个变化方案、0个可执行方案，判`HALT_E7_800_BUDGET_PROBE`。因此不以继续加算力替代修正，下一步只在动态运行器内增加固定顺序的紧急新增订单直接插入检查，每个完整方案严格计一次，先复测仍失败的2个位置；E3及此前封存证据、公共主算法和保护文件不动。证据=`baselines/e7_dynamic/e7_v2_20260714/preflight/failed_stage_budget_800/`，运行状态钩子=`baselines/e7_dynamic/e7_v2_20260714/hooks/failed_stage_budget_800_v2/`。
