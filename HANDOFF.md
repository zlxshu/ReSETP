# ReSETP — 项目总交接文档（HANDOFF）

> **单一事实源（Single Source of Truth）。**
> 任何机器上的 Claude 或 Codex，**开工前必须完整读完本文件 + `docs/handoff/memory/` 记忆快照**，否则禁止动手/下结论。
> 每次重大决策 / 任务 / 对话后，把变化追加到文末「变更日志」，让本文件始终最新。跨机器迁移、防偏差全靠它。
> 维护：Claude 更新本文件与 `docs/handoff/memory/`；Codex 每次跑完在「变更日志」追加一条 run log。

## 0. 一分钟速览
ReSETP = 投《系统工程理论与实践》的绿色车辆路径论文。多车场、油车(CV)+电车(EV)混合车队、EV 非线性充电（时变碳强度）、时间窗、收益公平、动态需求滚动重规划。
贡献 = 模型 + 机制（碳两层/协同/公平/动态）；算法也必须有创新，且要**打过文献主流算法**（GA-VNS/GA/PSO/ACO/VNS/LNS/GWO/IWD），不只赢 SA（SA 是地板，赢它只算"合格"）。
主线算法 = ALNS(winner kernel) + DR-ALNS（强化学习指挥 ALNS）。

### 2026-07-10 当前指令（覆盖下文旧计划）

- 当前唯一任务是修复动态滚动收尾阶段重新安排已锁定客户的问题。DQN、碳算子、完整九档和长训练全部暂停。
- 已删除收尾阶段对全部未完成客户再次调用求解器的路径。锁定时保存原车辆、原服务顺序、当前位置/在途进度、当前时间、剩余载重、剩余电量和进行中的充电；收尾只拼接锁定记录与最后一个正常滚动阶段的未锁定计划。若仍有客户没有计划，程序明确停止，不再从车场偷偷生成新路线。
- 原先故意保留的锁定客户失败测试已改为正式测试。随后动态普通、随机、模型和模型异常备用四条路径全部改走 `setp_solver.algorithms.resetp_alns`；测试会把旧入口替换成“一调用即报错”的钩子，再真实运行四条路径，防止只改函数名。动态入口不再导入旧 `search/alns_wouda.py`。
- 实时状态已进入实际约束：阶段车场出发时间不得早于触发时刻；在途车辆从可用车队扣除；剩余载重不足、剩余电量不足、充电占用冲突会判不可行；锁定客户继续保持原车辆和顺序。已经执行过路线、但没有可证明电量恢复过程的 EV 不得重新按满电车使用；当前采用保守冻结，后续若要返场复用必须先实现逐车部分电量和显式充电恢复。位置/在途进度、时间、载重、电量和充电状态的极小测试连同独立动作链邻近测试共 66 项通过。较重的旧 `test_formal_runner -k dynamic` 超过一分钟无输出后按短测纪律停止，不算通过。
- 强制动作短测确认动作不是空壳：同起点/种子/预算下四组动作产生四个不同候选路线，单种子成本约 642–687，且在一个固定种子上搜索一步前后最优动作发生变化。但 5 个种子的初步比较中，部分动作差距会被随机波动覆盖；再用两个 25 客户算例、3 个种子、搜索前/后两个状态复核时，两题始终是同一个动作组合最好，没有出现稳定的“状态变了，最佳动作也变”。因此只能确认算子会改变答案，尚未确认存在可学习的状态依赖优势；DQN/PPO 继续禁止，不能发布学习成功结论。
- DR 稳定超过同底层独立 ALNS、L-main 九档第一且超过第二名 5%、在线时间也最低，均尚未达到。下文 6 月 PPO 训练、7 月 E2 路线压缩和 Track24 全量重跑安排均为历史记录，不是当前执行许可。

## 1. ⚠️ 机器与环境（最重要，先读，踩了全盘皆输）
- **基准机 = M1 + 8G RAM（macOS, ARM）= 当前机 = 所有论文正式数字的唯一来源。**
- **GPU 机 = 5800H + RTX3060 6G + 16G RAM（操作系统待确认；很可能 Windows/Linux，不是 mac）。仅用于 DR 课程训练。**
- 金标准环境（M1）：`/opt/anaconda3/bin/python3.13` + numpy 2.3.5。锚：100-01 winner £4878.33 / 公平SA £5346.99（winner 碾 8.76pp）；L-main winner £8351 ≈ SA £8319（近最优、持平）。
- **致命坑①（ARM vs x86）**：M1(ARM) 与 5800H(x86) 浮点/BLAS 不同 → £4878 等锚在新机几乎必然变样（已被 venv numpy 漂移坑过一次：£4878→£5696、反输 SA）。**铁律：绝不把 M1 与 x86 的绝对数字放进同一张表/同一篇结论。**
- **致命坑②（可能 macOS→Win/Linux）**：若 5800H 非 mac，`/Volumes`、`/opt`、shell、`SETP_WORKER_PYTHON`、行尾、外置盘文件系统（APFS 跨平台读不了）全要变。**所有硬编码路径 `/Volumes/移动硬盘（512G）/ReSETP` 都会断**，需参数化为"仓库根"。
- **迁移分工（Claude 建议，待 user 最终确认）**：M1 = 唯一基准（E1-E7、所有表图、winner/SA/8基线对比都留 M1 跑）；5800H+3060 = 只做 DR 课程训练，DR 结果以**相对提升 %** 报（DR vs AlphaUCB vs SA 全在 5800H 同环境测，环境抵消，不与 M1 £4878 比绝对值）；训完迁回 M1。
- 新机要重建两套环境：① 系统 Python 3.13 + numpy 2.3.5 跑 solver（注意 x86≠ARM 数值）；② torch/CUDA venv 跑 PPO（3060）。`SETP_WORKER_PYTHON` 路径会变。**任何环境用前先验"同种子同结果"的内部确定性。**
- **同步：当前无 git remote，靠物理搬外置盘。强烈建议建私有 GitHub remote**（否则两机易分叉、盘坏即全失，且搬盘会中断 M1 上正在跑的 E1-E7）。
- 清理：仓库有 macOS AppleDouble 垃圾（`._*`，含 `.git/objects/pack/._*.idx`，git 报 non-monotonic index 警告）。迁到非 mac 机前 `dot_clean .` 或删 `._*`。

## 2. 三条工作线当前状态（2026-06-20）
- **图表（13 张）**：壳目录 + 数据适配完成；第四阶段"照壳填数据画图"等大重跑数字。详见 `docs/handoff/memory/figure-redesign-task.md`。
- **大重跑 E1-E7 + 表图重生成**：✅ **完成（2026-06-20, commit `9da7f8b`）= 论文正式数字锁死**。471/471 零失败零违约；**£4878 锚精确复现**（ALNS 4878.3318 / SA 5346.99）；T3-T9 + F1-F6/F5b 重生成到 `docs/paper_submission_final`，**F4 修好非空**；碳段 →**2330.7 kgCO2e**；latexmk 过；solver166/RL102 过；保护文件未动。报告 `solver/reports/formal_winner_20260619/`。小尾巴：E2 有 1 条 stale DR-ALNS provenance 行（T3 已排除，无害）。注：这是"正式数字 + 现有风格表图"完成；图表"顶刊级重设计"是否并入待确认（见 `docs/handoff/memory/figure-redesign-task.md`）。
- **ALNS winner kernel**：100-01 碾 SA 8.8%（£4878 vs £5347）健康；L-main 近最优持平。代码 `solver/src/setp_solver/search/winner_operators.py`。
- **DR-ALNS（block lane）**：离线探针 = **PROMISING**（有可学上下文信号，r2_delta≈1.05, CI[0.957,1.157]）。根因是训练不稳、非无信号。下一步 = 上 3060 做课程训练；方案在 `solver/reports/dr_alns_ppo_v3_block_dr_alns/offline_bandit/probe/curriculum_training_plan.md`。代码 `solver/rl/dr_alns_ppo/`（block_env/async_block_policy/train_async_block_ppo/offline_probe）。
- **8 文献基线（轨②）**：已实现接入统一接口、零违约跑通，但 **HALT_BASELINE_THROUGHPUT**（eval/s 3.96–11.04 < 16000/900s 需的 17.78）。下一步 = 先 profile 提速（每个计数 eval ≈ 一次 evaluate，full check 延后/缓存 decode），仍不够再换等墙钟公平。**基线对比必须在基准机 M1 跑**（与 £4878 锚同环境）。代码 `solver/src/setp_solver/search/metaheuristic_baselines.py` + `metaheuristic_baseline_runner.py`；产物 `baselines/`。

## 3. 纪律铁律
- 禁改 `cost.py` / `check.py` / `evaluation.py` 语义（唯一真值源）。
- 公平对比：同 evaluate/check + 同预算（16000 eval 或等墙钟，见 §6 待决）+ 10 seed + scipy Wilcoxon + 零违约 + 暖启动 `make_shared_initial_solution`；每个基线目标计算走同一 `evaluate()/penalized_obj`、过 `EvalBudget` 计数、封顶预算。
- Codex 不自创算法，只复刻文献/适配现成；拿不准停下报告。
- 核心代码立即 commit；数字绑 commit hash；跑不出诚实 HALT 不注水。
- 角色：Claude 只思考/判断/写 Codex 提示词/写文档，不跑不改代码、不开 workflow；Codex 执行。关键核验才让 Claude 亲自读仓库。
- 与 user 对话用平实中文；给 Codex 的提示词要详尽自包含（它冷启动、读不了论文，要把目标/背景/实现/验收/边界/捷径全喂）。

## 4. 关键代码地图 + 公平对比接口
- `solver/src/setp_solver/cost.py` → `evaluate()`（真实目标，禁改）
- `solver/src/setp_solver/check.py` → `check_solution()`（可行性，禁改）
- `solver/src/setp_solver/search/evaluation.py` → `EvalBudget`、`penalized_obj/score_candidate/model_cost`
- `solver/src/setp_solver/solution.py` → `Solution{routes[Route(vehicle_id,vehicle_type,home_depot_id,node_sequence)], charging_actions[...], cross_site_services[...]}`
- `solver/src/setp_solver/search/candidates.py` → `run_candidate(label,bundle_dir,seed,eval_budget,max_runtime_seconds,initial_solution)`；`make_shared_initial_solution(bundle)`；现成构造/修复/插入/充电算子（基线解码复用它，别重造可行性）
- `solver/src/setp_solver/search/alns_crush_v2.py` → 公平 harness：`FAIR_SA_EVAL_BUDGET=16000`/`900s`；run_task1=公平SA→`fair_sa_reference_costs.json`；run_task3=winner vs SA + `_wilcoxon_vs_fair_sa` + 判级（碾压/小幅领先/持平/失败）
- `solver/src/setp_solver/search/alns_crush.py` → `INSTANCE_DIRS`：`100-01-24h`=models/data_bundle/generated_instances/E-UK100_01__d2_s3_seed1_24h_20251113；`L-main`=…/E-UK24h-三班-01。150/200 未生成。
- `solver/src/setp_solver/search/winner_operators.py` → `run_winner_kernel(bundle_dir, WinnerKernelConfig(...))`
- `solver/rl/dr_alns_ppo/` → DR block lane（block_env 19维obs+5维动作；worker 用 SETP_WORKER_PYTHON=系统Python 跑算子）
> 深细节（8 基线设计转录、DR 同类偷的设计、harness 字段）见 `docs/handoff/memory/baseline-algorithm-catalog.md`。

## 5. 决策链 / 来龙去脉
- 真目标纠正：不是"碾 SA"（那只合格），是 **DR-ALNS 真训练+创新、干过 GA-VNS/GA/PSO 等文献主流**；退路 = 创新的普通 ALNS。
- DR "拉垮" 根因 = **训练不稳（非无信号）**；离线探针已证 PROMISING；**课程学习 + 动作掩码 + shared baseline 是修复捷径**（偷自 DR 同类论文 Daysalilar/Narayanan/Wan）。
- 两条便宜活并行：①探针优先（已 PROMISING）→ ②基线对比（已 HALT 在 throughput）。
- 公平口径：先提速保 16000/900s，不行再等墙钟；注意 16000 eval 对种群法（GA/PSO/GWO/IWD）可能偏少、反而低估基线。
- 迁移：M1 canonical，3060 只训 DR（相对 %）。

## 6. 待决 / 下一步
- **迁移三问（待 user）**：① 5800H 什么系统？外置盘什么文件系统（新机读得了吗）？② 仓库怎么过去——搬盘 / copy 一份 / 建 GitHub 私有 remote？③ 认 "M1 canonical + 3060 只训 DR（相对%）+ E1-E7/基线都留 M1" 这个分工吗？
- **轨②**：写第三条 Codex 提示词（profile → 提速 → 按选定公平口径出正式对比表）。
- **轨①**：迁移就绪后上 3060 课程训练（curriculum_training_plan.md 已 ready）。
- **顺手**：根目录 `AGENTS.md` / `CLAUDE-FABLE-5.md` 是 Claude 模型系统提示词（不是 ReSETP 项目说明）。Codex 按惯例读 AGENTS.md 会读到无关内容——考虑给 Codex 建一份真正的项目 AGENTS（指向本 HANDOFF），或在每条提示词显式写"先读 HANDOFF.md"。

## 7. 记忆快照
`docs/handoff/memory/` = M1 上 Claude 私有记忆（`~/.claude/.../memory/`）的快照，随仓库走。含 `MEMORY.md`（索引）+ project-plan-overview / baseline-algorithm-catalog / alns-crush-root-cause / algorithm-pivot-ca-alns / figure-redesign-task / deferred-instance-robustness / feedback_communication_style。深细节看这里。
已写好的两条 Codex 提示词（①离线探针 ②8基线复刻）应存到 `docs/handoff/codex_prompts/`（待补）。

## ⚠️ 工作顺序（单盘物理迁移 = 串行，必读）
仓库在外置盘上，盘在哪台机、另一台就停（含 M1 上正在跑的 E1-E7）。GitHub 备份只有 46M 代码+文档，**没有大数据/instances/reports**，所以 Windows 机不能靠 GitHub 独立干完整实验。因此严格串行：
1. **先在 M1 跑完**：E1-E7 重跑（进行中）+ 轨② 基线提速与正式对比（canonical 数字）。
2. **再移盘到 Windows**：装环境 → DR 课程训练（相对 % 报，不与 M1 绝对值比）。
3. **迁回 M1**：整合 DR 结果。

## 变更日志（每次对话/决策/Codex run 追加一行）
- 2026-06-20 建本文件；记忆快照入 `docs/handoff/memory/`。探针 PROMISING（r2_delta≈1.05）；8 基线 HALT_BASELINE_THROUGHPUT。提出迁移策略（M1 canonical + 3060 训 DR）。
- 2026-06-20（续）user 确认：GPU 机 = Windows 5800H，外置盘 exFAT 直插 Windows 干活，训完迁回，授权上 GitHub。已建私有备份 `github.com/zlxshu/ReSETP`（46M 快照 = 代码+文档+记忆；大数据/reports/venv 不在内，留盘上）。清理 .git 内 AppleDouble 垃圾。确立"单盘=串行"工作顺序（见上）。handoff 文档已提交进盘上仓库（commit 38e6356）。
- 2026-06-20（续2）写了轨②提速提示词，三条 Codex 提示词存入 `docs/handoff/codex_prompts/`（01 探针 / 02 基线 / 03 提速，+ README）。
- 2026-06-20 E1-E7 进度快照（user 报）：~51/60 单元、414/470 记录、0 失败；E1 完 / E2 ~130-140 / E3 52-60 / E4 135-160 / E6 58-70 / E7 9-10；剩 E4 网格 + E6 theta + E3 长尾；6 worker。ETA 纯算 2-4h + 收尾（合并 manifest / export-backfill / 修正文碳段 / 查表图 F4 / solver+RL 测试 + commit）1-2h = 整体 3-6h。不降规格、不混旧数据。**下一步：在 M1 跑完 E1-E7 + 发提示词③做轨②正式对比，再移盘 Windows 训 DR。**
- 🎯 2026-06-20 **E1-E7 大重跑完成（commit `9da7f8b`）= 论文正式数字锁死**：471/471 零失败零违约；£4878 锚精确复现；T3-T9 + F1-F6/F5b 重生成（F4 修好）；碳段 →2330.7 kgCO2e；latexmk 过；solver166/RL102 过；保护文件未动。**M1 现已空闲 → 下一步发提示词③做轨②基线正式对比，再移盘 Windows 训 DR。** 未 push（GitHub 备份待刷新）。
- 🖥️ 2026-06-20（Windows 机接手）**外置盘已直插 5800H+RTX3060 6G，仓库在 `H:\ReSETP`**。Claude 只读环境探针结果（未安装未跑项目代码）：
  - **GPU**：RTX 3060 6G，驱动 596.21，CUDA 13.2，可用（当前仅 Zotero/Claude 占 844MiB）。PPO 网络小（11-19 维 obs+小 MLP），6G 够；真瓶颈是 CPU 样本效率（1 episode=16000 eval=一整轮 ALNS）。
  - **现成 Python**：PATH 默认 `python`=3.14.3；`D:\Anaconda\python.exe`=**3.14.3 + numpy 2.4.4**；`conda` 命令坏了（`No module named 'conda'`，环境管理用不了）。**3.14 太新，torch+CUDA 轮子大概率还不支持** → PPO venv 建议用 Python 3.12/3.13。
  - **⚠️ 致命坑②实锤（硬编码 M1 路径，会拦死训练）**：`train_async_block_ppo.py:24`/`second_layer_debug.py:23`/`report_pilot.py:13` 硬编码 `SYSTEM_WORKER_PYTHON="/opt/anaconda3/bin/python3.13"` + `SYSTEM_WORKER_NUMPY="2.3.5"`；`worker_client.py:206-207` 回退候选是 `/opt/anaconda3`、`/opt/homebrew` 等 Mac 路径。多处强校验 `SETP_WORKER_PYTHON must be {required}`（train_async_block_ppo.py:703 / offline_bandit.py:914 / second_layer_debug.py:772 / diagnose_pilot_failure.py:498）在 Windows 必然报错。`train_async_block_ppo.py:720` 有 `--required-worker-python` CLI 覆盖可绕过，但 offline_bandit 等是否可覆盖待查。**需 Codex 把这些 M1 硬编码参数化（指向本机 Python），属 RL lane 代码、未碰 cost/check/evaluation 保护文件。**
  - **未发现现成 RL venv**（`solver/rl/.venv` 不存在）。
  - **环境策略（Claude 建议，待 user 拍板）**：本机只报相对%、所有对手（winner/AlphaUCB/SA/random/PPO）同环境跑→漂移自抵消，故**建议本机统一单环境**（一个 Python 3.12 venv：torch+CUDA + numpy + 可编辑装 setp_solver；`SETP_WORKER_PYTHON` 指它自己），比 M1 的"双环境"更干净、最省跨环境漂移；唯一硬要求=**本机内部确定性**（同种子同结果，用前先验）。不必复刻 M1 的 numpy 2.3.5。
- ✅ 2026-06-20（Windows 环境搭建完成，Codex commit `b6b984df`，**未训练**）：user 先前已下发"环境 audit+搭建"提示词给 Codex（要求双环境），Codex 完成：
  - **solver worker 环境**：`C:\Users\zlxshu\.venvs\resetp-solver-py313\Scripts\python.exe` = Python 3.13.12 + numpy 2.3.5 + scipy 1.18.0（注：numpy 对齐了 M1 金标准，但 x86≠ARM 仍不能比绝对值）。`SETP_WORKER_PYTHON` 已写入用户环境变量指向它。
  - **PPO/CUDA 环境**：`C:\Users\zlxshu\.venvs\resetp-ppo-cu124-py312\Scripts\python.exe` = Python 3.12.13 + numpy 1.26.4 + **torch 2.5.1+cu124（CUDA 12.4，cuda.is_available()=True，RTX3060 Laptop）** + gymnasium 1.0.0 + SB3 2.4.1。
  - **确定性**：verify_20251113 / seed7 / budget32，连跑两次均 £1013.534044477906、路线签名一致 → 本机内部确定（**仅证串行小 case；并行 async 训练下的确定性须另验**）。
  - **Windows 坑已修**：git core.autocrlf=false；清掉 .git/objects/pack 的 AppleDouble `._*.idx`（non-monotonic 警告消失）。commit `b6b984df` 把 DR 入口里硬编码的 `/opt/anaconda3/bin/python3.13` 默认改成优先读 `SETP_WORKER_PYTHON`，并修 Windows 反斜杠导致的 async report 路径校验误判；未碰 cost/check/evaluation。测试 test_async_block_ppo 7 passed、metaheuristic_baselines OK。默认 `python` 仍是坏 conda 的 D:\Anaconda 3.14.3，禁用，一律显式走两个 venv。
  - **🧠 Claude 把关判断（关键，单/双环境之争的真相）**：我先前建议"单环境"是为了"一杆秤"（所有对手同环境算成本→相对%可信）。Codex 按先前提示词建了"双环境"，但**双环境在本机同样满足"一杆秤"**——因为 RL env 的算子执行全部经 `WorkerClient`→子进程→`SETP_WORKER_PYTHON`(py313) 计算（block_env.py:10/40 用 WorkerClient；baselines.run_official_winner_kernel:199 也用 worker_python 子进程）。torch venv(py312) 只当"大脑"，**不计算任何解的成本**。故 AlphaUCB（run_alpha_ucb_env_policy 走 env）、DR（run_ppo_block_policy 走 env）、official winner（worker_python 子进程）三者都在 py313 一杆秤上 → 相对比有效。**判断=保留双环境，不重建**（py312+torch2.5.1+cu124 是成熟组合，硬塞单环境反而 numpy 版本会和 torch 打架）。
  - **⚠️ 残留漂移风险（写进训练/评测验收）**：`run_alns_wouda_strengthened`(baselines.py:282) 用 `[sys.executable]` 子进程跑——若从 torch venv(py312/numpy1.26.4) 调用就**不在 py313 一杆秤上**（旧 M1 trap 同型）。这是冷门"强化 ALNS-Wouda"基线、非核心 DR-vs-AlphaUCB-vs-SA 对比。**铁律：最终上报的 DR/AlphaUCB/SA 对比，每个对手成本必须经 py313 worker；SA(fair-SA)的解释器路径须在评测前确认走 py313，任何 sys.executable 路径要审。**
  - **🖥️ GPU/CPU 利用（回应 user "别全让 CPU 干"）**：DR-ALNS 是 learning-to-search，策略网是小 MLP（~19维obs+MultiDiscrete头）→ GPU 必然只占小头，这是架构本质（也是论文卖点：保 ALNS 质量、只学自适应），强行做大网不会加速训练。**真加速杠杆=CPU 并行**：`train_async_block_ppo.py` 是异步 PPO，可在 16 线程上并行跑多个 ALNS rollout worker（每个 worker 纯 CPU/numpy 无 torch，不抢 6G 显存）→ 直击"1 局=16000eval、样本效率低=天级"瓶颈。计划：训练用并行 async（worker 数按 16 线程+16G 内存调，防 OOM）；GPU 跑策略网；**最终上报的对比评测改用受控串行、保确定可复现**。
- 📋 2026-06-20（Claude 读训练码 + 写 Prompt A）：user 拍板 ①本机环境=保留 Codex 的双环境（不重建，理由见上方把关判断）②DR 第一版=**先跑小 pilot 验路子**（非直接大规模）。Claude 精读 `block_env.py`/`action_space.py`/`async_block_policy.py`/`train_async_block_ppo.py`/`worker_client.py`/`bundle_manifest.py` 后定论：
  - 动作空间 `BLOCK_ACTION_NVECS=(7,4,5,4,4)`=destroy(6算子+alpha_ucb)/repair(3+alpha_ucb)/q(5)/threshold(4)/exploration(4)。obs=19 维。
  - **课程/掩码/shared-baseline 三件套代码里都还没有**（`curriculum_training_plan.md` 是"待建"清单）：`_reward()`(block_env.py:70)单一无分阶段；policy 无掩码接口；`flatten_episodes()`(:161)全局标准化无按 bundle 分组。
  - **训练清单已干净**：`training_bundle_manifest.json` train=E-UK100_02/三班-150/三班-200，held_out=100_03，formal=100-01/L-main；`bundle_manifest.validate_manifest` 硬禁玩具题(verify/demo/E-UK25)+禁 100-01 泄漏。三班 150/200 算例已存在。历史"误用玩具题"坑已堵。
  - **当前异步训练器压根没用 GPU**（model 全程 CPU），且 `_cpu_probe_row()`(:546)用 Unix `ps` → Windows 失效。`self-check` 子命令已有把关闸（worker=py313 / numpy=2.3.5 / 零违约 / busy_ratio≥0.60）。并行=`ProcessPoolExecutor(num_actors)` 默认 6，可上调（测 RAM 防 OOM）。
  - **小 pilot 拆两条 Codex 提示词**（省 credits=烧训练算力前先验新代码）：**Prompt A**=建三件套+修 Windows 并行监控(psutil)+GPU 接管梯度更新+单测+self-check+极短冒烟（只验机制：阶段切换/掩码命中率/零违约/并行/内存/内部确定性），**不下性能结论**；已写入 `docs/handoff/codex_prompts/04_dr_curriculum_pilot_build.md`，待 user 发 Codex。**Prompt B**（A 机制验过再写）=跑真小 pilot + 100-01 上 DR vs AlphaUCB vs SA 相对%评测，无增益诚实 HALT；时长按 A 测出的并行速度定。
  - **残留待办/盯防**：HANDOFF 有未提交改动（让 Codex 随下批 commit + 刷新 GitHub 备份）；最终评测须确认 SA/fair-SA 也走 py313 worker（防 `run_alns_wouda_strengthened` 式 sys.executable 漂移）。
- ✅ 2026-06-20（Codex Prompt A 机制搭建 + 验证完成，**未跑真 pilot、无性能结论**）：
  - 分阶段 commits：`77200af8` audit + Windows psutil probe；`b17ac8e5` curriculum phases/controller/CLI；`c89f2ed8` action mask + policy mask plumbing；`550a7819` shared baseline + `--device auto|cpu|cuda`；`90ea6670` Windows RL validation guards + PPO requirements pin；`5f870000` actor rollout RNG seeding for single-actor determinism。
  - 环境锚：PPO venv `C:\Users\zlxshu\.venvs\resetp-ppo-cu124-py312\Scripts\python.exe` = Python 3.12.13 + NumPy 1.26.4 + torch 2.5.1+cu124 + CUDA available=True + sklearn 1.5.2 + scipy 1.14.1 + psutil 7.2.2；worker `C:\Users\zlxshu\.venvs\resetp-solver-py313\Scripts\python.exe` = Python 3.13.12 + NumPy 2.3.5；GPU probe = RTX 3060 Laptop 6G。
  - 验证：`solver/rl/tests/` = **110 passed**；`test_async_block_ppo.py` = 15 passed；最终 self-check `solver/reports/dr_alns_ppo_v3_block_dr_alns/async_pilot/self_check_final_seeded/` = `PASS_ASYNC_SELF_CHECK`（12 episodes、零违约、worker py313/numpy2.3.5、busy_ratio≈0.700）；内部确定性 mini-check `determinism_c` vs `determinism_d`（num_actors=1、seed=123）首个 episode `reward_sum` 与 `best_obj` 逐值一致。
  - 短冒烟 `solver/reports/dr_alns_ppo_v3_block_dr_alns/async_pilot/smoke_final/`：参数 `--num-actors 12 --eval-budget 256 --block-size 16 --timesteps 384 --rollout-min-episodes 4 --phase-min-episodes 4 --device auto`；阶段切换 route→energy→carbon→dynamic；37 episodes、384 valid steps、零违约、reward finite、device=`cuda`、shared baseline enabled；destroy-head mask invalid rate 约 0.241-0.411；throughput final busy_ratio≈0.838、episodes_per_hour≈127.84、worker_failure_count=0；psutil 66 samples、max total_worker_pcpu≈1130.7、max worker RSS≈505.8MB、probe_error 空。
  - Windows 坑：本会话 shell `workdir` 会落回 `C:\`，实际命令用 `git -C H:\ReSETP` / `Set-Location -LiteralPath`；PPO venv 初始无 pip，`ensurepip` 后首次装最新版 sklearn 拉到 NumPy 2.4.6 与 SB3 冲突，已修回 NumPy 1.26.4 并 pin `scikit-learn==1.5.2`/`scipy==1.14.1`；Windows path 分隔符修了 offline bandit/probe guards；测试夹具去掉 `/opt` worker 硬编码；`operator_manual` 的 `id(instance)` cache 加节点签名，防测试间 stale depot；actor 子进程按 episode seed 设 torch/NumPy RNG。
  - 报告：`solver/reports/dr_alns_ppo_v3_block_dr_alns/async_pilot/prompt_a_mechanism_validation_report.md`。Prompt B 建议：可从 12 actors 起步继续真小 pilot，继续采 psutil；GPU 只承担 PPO update，CPU rollout 仍是瓶颈；所有 DR/AlphaUCB/SA 相对%必须同在本机 py313 worker 成本口径内，绝不和 M1 绝对数横比。
- 📋 2026-06-20（Claude 核 Prompt A 报告 + audit + 写 Prompt B）：Claude 亲读报告与 audit，判定**机制真过关非表面**（掩码命中率 0.24-0.41 真在动、确定性 reward+best_obj 逐值一致、四阶段按"够 episode+零违约不退化"真切换、GPU 接管更新、worker 全程 py313；Windows 三个真 bug 修了）。从 `async_pilot/audit/audit.md` 得三条定计划的事实：
  - **碳信号是真的**：worker `metrics` 含两层碳 `E_cv_direct/E_ev_indirect/E_total`+`cost_carbon`+`carbon_quota_kg` → 碳阶段真信号。**但公平/动态在 block 响应无信号**（无 profit-by-depot/无动态字段）→ 诚实课程表=**route→energy→carbon 三阶段**，公平/动态不进 block 课程（由最终 cost.py 评测与 E7 运行时承担）。
  - **冒烟 RAM 数据不算数**：smoke 是玩具预算(eval_budget=256/block16)，max worker RSS 仅 505MB、探针见最多 24 worker 进程（12 actor 切换重叠）。真 pilot(16000eval+真算例+block128)内存大得多，**Prompt B 必须先在真实设置重测 RAM 再定 num_actors**，不能信"没逼近 14GB"。
  - **评测口径命门**：`evaluate_policy.py` 算法集**不含 SA**（只有 ppo_block/alpha_ucb_block/alpha_ucb_env/random_block/official_winner_kernel，全 py313 worker）。**SA 须另从 solver `alns_crush_v2.run_task1`(scikit-opt-SA, py313, 16000eval, 暖启动) 取**，才能和 DR/AlphaUCB 同一杆秤。Prompt B 写死"每行 worker=py313 核验 + 不评 alns_wouda_strengthened"。
  - **Prompt B 已写入 `docs/handoff/codex_prompts/05_dr_curriculum_pilot_run.md`**（待 user 发 Codex）：阶段0 真实设置资源探针+加周期 checkpoint→阶段1 有界限小 pilot(route,energy,carbon / masks / shared baseline / device cuda / 默认目标墙钟≈2-3h 可中断)→阶段2 100-01+held_out 评 DR vs AlphaUCB(block/env)/SA/random 相对%(全 py313 核验)→阶段3 PROMISING/WEAK 判决(只相对%, 不与 M1 横比, WEAK 则诚实止损 DR=future work)。主判据=DR vs AlphaUCB(block)。pilot 时长=2-3h、评测 seed=5（user 确认）。
- ⛔ 2026-06-20（Codex Prompt B 阶段0 实测后 `HALT_RESOURCE_PROBE`，**未训练未评测无性能结论**，commits `41eba3c0`/`401953f4`/`7814cf72`，测试 19/114 passed）：**关键硬事实=全预算 DR 训练在本机不可行（迁回 M1 也要知道）**：
  - **单局墙钟（16000 eval，12 actors 竞争 8 核/16 线程下）**：100c(E-UK100_02) **均 5122s≈85 分钟/局**(min4439/max6013)；150c(三班-150) **均 15078s≈4.2 小时/局**；200c(三班-200) **一局未完**。RL 要成百上千局 → 全预算训练=几天到跑不动（HANDOFF 早警告"几千局=几千万步=几天算力"，现被实测坐实）。
  - **内存**：12 actors 峰值 system used **14.16GB**、available 低至 **2.07GB**(87.2%)，越过计划 12GB 安全线。**主因仍是单局太慢**，内存只是雪上加霜（注：每个 actor 子进程在 Windows spawn 下各自 import torch≈内存大头；future 优化=actor 用 numpy 前向、不载 torch，可大降内存让 16 线程真并行——本 pilot 不做）。
  - 吞吐 final **2.26 episodes/hour** → 2-3h 仅 ~6-7 局，达不到 12 局底线、走不完 route→energy→carbon。Codex 还修了"超额提交 actor episode、到点仍空等"的 bug(`401953f4`)。
  - **Claude 判断（纠正先前"内存thrash"猜测）**：瓶颈是**单局原生耗时**非纯内存。**唯一能在本机出学习信号的配置=降 eval budget(如~3000)+只用最快 bundle(100_02)+降 actors 到 6-8 的"趋势 pilot"**（同降预算下 DR vs AlphaUCB vs SA 相对%仍有效，诚实标注"reduced-budget trend pilot 非全预算"）。Option2(更长墙钟+少 actors)即便过夜也仅~50-60 局且 150/200 无望；Option3(止损 DR=future work)是诚实退路但**在试过 reduced-budget 前过早**。**战略提示(待 user)**：若 reduced-budget pilot=PROMISING 而论文需全量训练，本笔记本与 M1(8G RAM 更紧)都可能不够→需讨论云端 GPU/CPU。已 ask user 选 scope；新提示词(06)按所选 scope 写。
- 🧭 2026-06-20（user 战略决策：两条工作线并行 + Scheme 3 迁移）：
  - **两条线并行**：①**x86(本机)=长期 DR-ALNS 训练机**（DR 是面向未来工作的长期追求）；②**M1=论文刚需保底**——轨②的 8 个 ALNS 文献基线对比(现 HALT 在 throughput, 提示词 03 已草) + 图表壳复印。ALNS 是现阶段论文刚需，至少做好保底；DR 是上限/future work，不卡论文。
  - **user 工作纪律(新, 通用)**：DR/ALNS 碰到卡点/瓶颈/进度慢，**先上网 + 翻 Zotero 文献找现成解法，别重复造轮子**（"我们碰到的问题大多前人早研究透了"）。已记入 [[feedback-communication-style]]。**眼下"训练太贵"卡点的文献解=Daysalilar 课程 DRL 在小算例(N=10)训→零样本泛化大算例(N=100)**（见 [[baseline-algorithm-catalog]]）→ x86 DR 应"小/快设置训练、全预算只评测验泛化"，非 85min/局硬灌。
  - **迁移=Scheme 3（user 选定，比其原方案省一次大盘往返+M1 停工）**：关键事实=`.gitignore` 排除 `models/`+`solver/reports/`+`*.npy`（算例/DR报告/模型都不在 GitHub），但 **DR 产出极小**(模型~几十KB + 几个CSV/MD)→不必倒腾大盘回来，U盘/网盘/`git add -f` 即可。流程：①现在 push 刷新 GitHub→②整盘复制到 x86 本地 `D:\ReSETP`(先验 D 盘空间)→③验 x86 脱盘能训(self-check from D:, 注意 venv 勿 editable 钉在 H:)→④拔盘单程去 M1 并留在 M1(M1 拉 GitHub 跑轨②+图表)→⑤x86 在 `dr-x86` 分支训 DR, 代码 push GitHub、小产出 U盘/`git add -f`→⑥M1 拉 dr-x86 整合。大盘全程不回 x86。
  - **下一步**：提示词 06=x86 迁移准备(push+复制到D:+脱盘验证, 不训练不删 H:)；之后 x86 跑 reduced-budget DR 趋势 pilot；M1 那条线另写(轨② 03 提速 + 图表)。
- ✅ 2026-06-20（Codex 提示词 06 迁移准备完成，**未训练、未删 H:**）：GitHub push 成功，分支 `codex/reporting-pipeline` HEAD=`5ad097e8ededcaded97d7a7001ccdfb158588864`（两机以后靠它同步代码）。整仓复制 `H:\ReSETP`→`D:\ReSETP`（源 2.64GB，robocopy exit 0 无失败，D 盘复制后剩 103.65GiB）。`D:\ReSETP` 是有效 git 仓库(远程仍 zlxshu/ReSETP, autocrlf=false)。**脱盘 self-check 在 D 盘 PASS_ASYNC_SELF_CHECK**(4 episodes/零违约/worker py313/numpy2.3.5/busy0.66)；脱 H 核查通过：repo_root/PYTHONPATH/worker import 全指 `D:\ReSETP`，无 editable 钉在 H，无 H: 路径回落。**大盘可安全拔去 M1。**
  - **🔀 两机分工 + 分支约定（防分叉，关键）**：**x86 在 `D:\ReSETP`、开 `dr-x86` 分支**只动 DR lane(solver/rl/)；**M1 在大盘、留 `codex/reporting-pipeline` 分支**做轨②(ALNS 基线)+图表(动 solver/src 基线代码、reporting、docs 图表)。两边代码经 GitHub 同步(push/pull 各自分支)。DR 小产出(模型.pt+相对%报告)经 `git add -f` 或 U盘传，大盘不回 x86。**HANDOFF 变更日志冲突**：x86 的 DR 条目标 `[x86/DR]`、M1 标 `[M1]`，合并时两边追加行都保留(Claude 在 merge 时reconcile)。两机不动对方的文件区，冲突面最小。
  - **并行下一步**：①**x86**=提示词 07(reduced-budget DR 趋势 pilot, 在 D:\ dr-x86 分支)；②**M1**=拔盘到 M1 后开新会话, M1 的 Claude **先读本 HANDOFF**, 接手轨②(提示词 `docs/handoff/codex_prompts/03_baseline_speedup.md`)+图表壳复印(`docs/handoff/memory/figure-redesign-task.md`)。
- 🛑 2026-06-20（Codex Prompt B 阶段0 真实资源探针 HALT，**未跑 pilot/未评测/无性能结论**）：新增并提交 `--checkpoint-every-updates` + 系统内存探针（commit `41eba3c0`，RL tests 113 passed），随后修复训练器尾部过量提交导致到点后等待多余长 episode 的问题（commit `401953f4`，RL tests 114 passed）。真预算 12 actors 资源探针 `prompt_b_resource_probe_12/` 跑 `eval_budget=16000/block_size=128/timesteps=1536`，完成 13 episode、零违约、worker 全程 py313/numpy2.3.5，但系统内存 used 峰值≈14.16GB、available 最低≈2.07GB，且吞吐仅≈2.26 episodes/hour；按锁定的 2-3h pilot 只能完成约 6-7 episode，不足 12 episode 底线、也不足以可靠走 route→energy→carbon。已停止残留 worker，报告 `solver/reports/dr_alns_ppo_v3_block_dr_alns/async_pilot/prompt_b_halt_resource_probe_report.md`。下一步必须显式改 scope：降 eval budget/只用快 bundle 做趋势 pilot，或放宽到更长墙钟并降 actors 到 8/6，或停止 DR pilot、保留 Prompt A 机制证明。
- 2026-06-21 [x86/DR] `D:\ReSETP` / `dr-x86` reduced-budget Pilot07 完成：pilot manifest 只训 100_02，held_out=100_03，formal=100-01；校准选 `eval_budget=1500/block_size=64/num_actors=6`（约 45.3 episodes/hour，峰值内存 11.6GB）；正式训练 136 episodes、route→energy→carbon、零违约、worker 全程 py313/numpy2.3.5、device=cuda；评测同降预算同 worker，一切只报 x86 内相对%。结论 **WEAK**：100-01 上 ppo_block 相对 alpha_ucb_block 为 +1.63%，但 100_02 为 -4.77%、100_03 为 -6.06%，且未稳定超过 random_block；DR 暂按 future-work/learnable lane 处理，论文刚需继续押 ALNS winner kernel + 机制线。报告 `solver/reports/dr_alns_ppo_v3_block_dr_alns/async_pilot/pilot07/summary/pilot07_final_report.md`。
- 🔧 2026-06-21 [x86/DR] pilot07 收尾 + push 校正：**实际 `origin/dr-x86` tip = `101a4819`（非报告里的 `e0e6d941`）**——`e0e6d941`(WEAK 结论) 之上多一个清理提交 `101a4819 Remove obsolete generated files and stale content`，本地 dr-x86 与 origin 已同步推送。`101a4819` 把 pilot07 生成产物（`async_block_ppo_model.pt`、eval/summary CSV、`pilot07_final_report.md`、训练日志、checkpoints）+ `baselines/*` + `docs/claudecode_handoff.md`(11462行) 移出 git 跟踪——**文件均仍在 x86 本地硬盘**（落回 `solver/reports/` 的 .gitignore；pilot07 manifest + calibration_summary 仍跟踪）。按双机约定 DR 小产出须随 git 到 M1，**user 决定 `git add -f` 把最小产物集（model.pt + pilot07_final_report.md + paired_relative/algorithm_summary/checkpoint_trend.csv + summary.json，≈120KB）加回 dr-x86 并 push**（笨重 checkpoints/日志/baselines/旧 handoff 保持移除；git 由 Codex 执行）。校准定 `1500/64/6`（校准≈45.3 ep/h、峰值内存≈11.6GB）：拒 `1500/32/8`(内存≈13.76GB)、拒 `3000/64/6`(3h 预计<100 ep)。正式训练 136 ep、route→energy→carbon 停在 carbon、零违约、device=cuda、shared baseline 开、训练末端吞吐≈65.6 ep/h、峰值系统内存≈11.38GB。验证：`test_pilot07_tools.py` 5 passed、`test_async_block_ppo.py` 19 passed、manifest 过 `bundle_manifest.load_manifest` 且 train 内无 `E-UK100_01` 泄漏、summary integrity ok=true（66 行无 worker/NumPy/budget/violation/non-finite 异常）。结论仍 **WEAK**（数字见上条）→ DR 维持 future-work，论文刚需继续押 ALNS winner kernel + 机制线。
- 🚀 2026-06-21 [x86/DR] DR-ALNS 课程线启动（user 指令：x86 唯一任务=训出 DR-ALNS，M1 线不归本会话）。pilot07 WEAK 根因诊断=100c 成本墙→PPO 更新不足→末段塌缩（中段 checkpoint 一度 `+1.37%` 超 AlphaUCB，final 塌到 `-1.44%`/`-4.74%`）。方案=Daysalilar 小→大课程 + 稳定性，900s 等墙钟在 100c 验证（user 拍板）。**第一步完成**（commit `db4de2d`）：用现成生成器 `models/src/setp_instance_lab/` 造 12 个合规小算例（E-UK25_02..09 + E-UK50_01..04，全 validation.passed=True）；课程 manifest `solver/reports/dr_alns_ppo_v3_block_dr_alns/async_pilot/pilot08/training_bundle_manifest_curriculum.json`（train=12 小算例 / held_out=100_03 / formal=100_01，无泄漏）；新增 `validate_curriculum_manifest`（只禁 verify/demo/E-UK100_01，放行合规小算例），**未削弱**严格 `validate_manifest`（仍禁 E-UK25）；13 测试过；小算例在 `models/`（gitignore）下、由脚本确定性可重生成。**关键发现**：训练器 `run_train` 经 `load_manifest`(严格)会拒 E-UK25 课程 manifest → B1 需把课程校验接进训练器（加 `--curriculum` 开关）；而 value/advantage clipping、per-phase LR/entropy/clip、shared baseline 等稳定性手段 CLI 早已内建（Prompt A）。**下一步**：B1=接课程校验+资源校准（不全训）；B2=route→energy→carbon 课程+稳定性长训；C=900s 验证+选最优 held-out checkpoint。
- ✅ 2026-06-21 [x86/DR] 课程 B1+B2 完成（**未评测，C 步待**）。B1（commit `ee342022`，已 push）：`--curriculum` 开关接通课程校验 + 校准——**小算例打破成本墙，吞吐 ~1030 ep/h（pilot07 的 ~25 倍）**；6 actor 吞吐已最优（加 actor 不增反降），校准三组峰值略超 12GB 但实跑（关掉其他 app）仅 11.1GB。B2 正式长训（**未 push**，产物在 gitignore `…/pilot08/train_final/`）：**8422 episodes / 350 PPO 更新**（pilot07 仅 136/10）、route105→energy105→carbon8212、吞吐 1132 ep/h、峰值内存 11.1GB/可用 5.1GB、零违约、worker py313、device cuda；**entropy 7.11→5.90 全程未塌缩**（pilot07 末段塌缩未复现）；shared baseline 12 组全生效；35 个 checkpoint（update_0010..0350）。**这是训练健康信号、非性能结论**——DR 是否真超 AlphaUCB 由 C 步 900s 等墙钟评测 + 选最优 held-out checkpoint 定，诚实判级。
- 🏁 2026-06-21 [x86/DR] DR-ALNS 课程 pilot08 = **WEAK（但比 pilot07 更干净、更有结论性）**。C 步 900s 等墙钟评测（C 工具 commit `f5b2f406`，C 结果 commit `750135d6`，**待 push**；提交链 ee342022→443527c6→f5b2f406→750135d6 干净线性、无分叉）：EVAL_BUDGET_900=13100（校准 runtime 884s）；选最优 held-out checkpoint=`update_0240`（final 精筛仅排第3，确有轻度末段退化但选 0240 规避）；66 行 comparison gate 全过、worker py313/numpy2.3.5/零违约、28 测试过。主对比 ppo_block(0240) vs alpha_ucb_block 同机相对%：100_02 train `+0.69%`(wins3/5,p0.31)、100_03 held `+0.24%`(wins2/5,p0.5)、100_01 formal `+1.28%`(wins5/5,p0.031)。**判 WEAK**：train/held 未稳超 AlphaUCB（≈平手），且未对 random 在 train/held 形成均值优势；formal 虽好但单题不覆盖 train+held gate。**意义（关键）**：pilot07 的 WEAK 是塌缩+负值（训练失败混淆）；pilot08 是 DR **训练成功（无塌缩、8422ep、课程+稳定性齐全、选最优 checkpoint）后收敛到与 AlphaUCB 持平**——印证旧判断"PPO 天花板≈AlphaUCB/winner kernel"，learned 算子选择对好 bandit 的 headroom 边际/为零。runtime caveat：block 算法共享 eval_budget（AlphaUCB 实际更短），真等墙钟反会给 AlphaUCB 更多工作量→DR 的微弱优势只会更小，故 WEAK 稳健。**结论**：DR-ALNS 维持 future-work（诚实负结果，非"训不出来"——课程/稳定性/海量训练都做对了仍只持平）；论文算法线押 ALNS winner kernel + 机制 + 轨②文献基线（M1）。C 产物按 commit-curated 已入 git（B2 final 模型 + update_0240 + eval CSV/报告），待 push。
- 🔬 2026-06-22 [x86/DR] DR rescue 诊断 pilot09（**文献优先 headroom 诊断，未长训、未 push**；工具 `pilot09_rescue_tools.py`+测试当前 untracked，59 测试过）：先查 Zotero(PASS)+web（Daysalilar/DR-ALNS ICAPS+GitHub/Narayanan/Wan，证据 log 带 key/URL），再做低成本诊断。**关键发现=动作空间设计错位**：①stochastic PPO(0240)=WEAK_STOCHASTIC（held_out **-3.65%** 更差）；②deterministic masked PPO ≈平手（+0.56%/+0.04%/+1.97%）→ 印证算子选择对 AlphaUCB 无 headroom；③**但 AlphaUCB-meta 屏（固定 destroy/repair=AlphaUCB，只扫 q/threshold/exploration）发现最优静态参数 q0.40/thr0.0/exp0.05 比默认 AlphaUCB train +4.62% / held +3.12%** → **headroom 在搜索参数、不在算子选择**。gate=`REFINE_ALPHA_UCB_META_NEXT`（非同构 PPO 长训），未触发 HALT_DR_HEADROOM。**🧠 Claude 把关 caveat（关键，别被带歪）**：该 meta 优势来自更优**静态**参数（=AlphaUCB 默认参数欠调），**尚未证明动态/学习能超过"最优静态参数"**——若 block_meta 学出来只等于最优静态，则贡献只是"重调 AlphaUCB 参数"（无学习价值），且"DR 打过默认 AlphaUCB"对 AlphaUCB 是 strawman。且 meta 屏在 budget 600 + best-of-45 选择偏差，需 900s held_out 复核。**下一步建议**：训 block_meta 前先廉价验 ①最优静态 meta 在 900s held_out 是否仍 +3%、②把 block_meta 成功 bar 设成"打过最优静态 meta"（非默认 AlphaUCB）——只有动态超最优静态才算真学习贡献，否则诚实记"重调参=minor"。
- 🧯 2026-06-25 [x86/DR] Pilot10 block_meta rescue 完成并收口，结论 **WEAK_RETUNE**：D1 900s-budget 静态 meta 复核通过，`BEST_STATIC_META=q0.40/threshold0.0025/exploration0.15` 相对默认 AlphaUCB-meta 为 train `+1.07%`(wins3/5)、held_out `+1.91%`(wins4/5)，说明 Pilot09 的“搜索参数 headroom”不是纯 budget600 假象；随后按课程 manifest 训练 `--meta-mode` block_meta，健康 gate 全过（8483 episodes、595 updates、route→energy→carbon、zero violation、worker py313/numpy2.3.5、device cuda、final entropy 3.74、59 checkpoints），held-out 精筛选 `checkpoint_0030`（final rank4/4）。正式 100c comparison 中 block_meta_best vs BEST_STATIC_META：100_02 train `+0.58%` wins2/5 p0.406，100_03 held_out `-1.08%` wins1/5 p0.938，100_01 formal `+0.15%` wins2/5 p0.5；**未达到 train+held positive 且 wins>=4/5 的学习贡献 bar**。runtime caveat：`EVAL_BUDGET_900=13100` 对 block_meta 行实际约 1769-1846s、best_static_meta 约 1100s，SA/official timed 行约 901-903s；本报告仍只作 x86 同机相对，不与 M1 绝对数横比。**判定**：DR 学算子选择已死；DR 学动态 meta 也未超最优静态 meta；剩余价值是 AlphaUCB 参数重调（minor/engineering），非学习增益。DR 长训线止损，论文主线继续押 ALNS winner kernel + 机制 + 轨②文献基线。
- 🧭 2026-06-26 [x86/DR] Pilot11 interface audit 启动并完成 no-training 审计工具：新增项目节奏手册 `docs/handoff/dr_alns_project_rhythm_manual.md` 与执行计划 `docs/superpowers/plans/2026-06-26-dr-alns-project-rhythm.md`，并新增 `pilot11_interface_audit.py`/测试。审计 gate=`NEEDS_INTERFACE_REDESIGN`，确认当前 block PPO 缺 route sequence、customer/time-window、vehicle energy、charging strategy、accept/stop、learned repair/insertion 等关键状态/动作接口；`q_threshold_exploration_control` 仅为已存在静态/动态 meta 头，不能再支撑长训。选择下一小步为 **Path A: acceptance or stop control**（先做接受/停止控制审计，仍不训练不评测），未来任何 DR 学习声明必须打过 best static/tuned meta，而不是 default AlphaUCB。
- [M1→x86] 2026-06-26（09s Goeke-80 + 实体车辆多趟语义同步源）：M1 09s 线提交 `8853d6aa`/`fffb1ae6` 把默认物理参数回正为 Goeke 口径 `Q_capacity=3650.0kg`、`B_battery_kwh=80.0kWh`，速度 `25.0m/s` 与碳价 `0.05034` 不动；核心修正是车辆数上限按实体车辆计数而非 route 数计数，同一实体车可有 `CV1#T1/CV1#T2` 多趟，`check_solution()` 按 physical vehicle prefix 检查 `m^g/m^e`。M1 gate 报告 `baselines/e2_alns/goeke80_multitrip_rescue_gate.md`：69/69 E2 warm start 零违约，代表集 smoke 34/34 OK；这只说明容量/车辆上限语义卡点清掉，可继续算法对比，尚不是 ALNS 正式胜利证据。
- ✅ 2026-06-27 [x86/DR] Pilot16/Pilot17 全规模三班倒 DR-ALNS 长训与验收完成：x86 线先核 M1 09s 及后续 Goeke80/普通 ALNS 提交，未发现必须同步到 DR lane 的新 solver 语义修复，故保留 `dr-x86` 任务边界。Pilot16 用 `50/75/100/150/200c × 3` 三班倒全规模 manifest、`candidate-generator + search-control` 结构、`6 actors / eval_budget 20 / block_size 4 / CUDA` 长训，完成 `11280 episodes / 375 updates / 37 checkpoints`，route→energy→carbon，15/15 bundle 全触达，每个规模各 2256 episodes，零违约，worker py313/NumPy 2.3.5，final throughput≈910 ep/h，峰值内存≈13.83GB，entropy 9.19→8.68 未塌缩，candidate 非默认率≈64.5%，search-control 非 continue≈49.6% → **训练健康 gate 通过**。Pilot17 正式同预算全规模评测新增 `pilot17_allscale_eval_tools.py`（注意修正：按模型 `action_nvec=(7,4,5,4,4,4,3)` 恢复候选生成器/search-control 评测环境；旧 `evaluate_policy.py` 会剪掉新头，不能直接用于 Pilot16 模型）。筛 37 checkpoint 后 best=final/update375；405 行 comparison gate 过、外部基线含 default AlphaUCB、tuned AlphaUCB-meta、random、SA、official winner。结论 **ACCEPTABLE_PASS but NOT TARGET_PASS**：相对最强外部基线各规模均为正但很小，50c `+0.12%`、75c `+0.38%`、100c `+1.01%`、150c `+0.99%`、200c `+0.36%`，全规模平均仅 `+0.57%`，远低于 user 目标 `+10%`；且 PPO 120 行均提前 stop（通常只用 8-9.3/20 eval），说明 search-control 学成了“早停拿初始/浅层收益”的捷径。**把关判断**：全规模结构能稳定跑、能小幅非劣，但没有训练出“强大的 DR-ALNS”；它不应作为论文主算法领跑者，只能作为受限预算下的 learnable controller 负/弱正结果。下一步若仍救 DR，必须先修 stop/reward/acceptance 口径并用独立 held-out 三班倒算例复核；否则应回到 M1 普通 ALNS/基线对比主线。
- 🩺 2026-06-27 [x86/DR] **Pilot18 headroom 体检（只测不训练，commit `8e840309`，18 测试过）= `VERDICT_NO_HEADROOM` → DR「领先第二名 10%」线正式止损。** 设计动机=之前修早停/候选流/接口全在错的高度上打：winner kernel 已近最优、DR 建于其上、Pilot17 实测 DR≈winner kernel(+0.57%)，「DR 比 winner kernel 强 10%」是数学死结。体检办法=最强非 DR 基线(winner kernel + `BEST_STATIC_META` q0.40/thr0.0025/exp0.15 + SA 兜底)在 50/75/100/150/200c 各 1 代表算例上测「放大预算还能榨多少」。**硬结果**：5 规模 `obj@B == obj@4B == obj@16B`，headroom 全 `0.000%`；0 墙钟封顶（200c@16B 实烧 627s<1200s 上限仍 0 增益）；90/90 baseline integrity 过。DR 早停≈8/20(0.40B) 属实，**但 DR 停后基线 `late gain = 0.000%`** → 排除 `VERDICT_DR_INTERFACE_GAP`，证明是题本身搜两下即饱和、非 DR 接口锅。**铁证逻辑**：代表算例上最强搜索已饱和 → 任何建于其上的控制器(含 DR)都不可能领先第二名 10%，且此结论与训练时长/候选流/接口全无关。caveat：headroom=0 是相对 winner+SA 搜索族（非证明全局最优不可更低），但对「谁能领先第二名 10%」无影响。**决策（替代 Pilot17 的「若仍救 DR…」分支）**：① DR 长训/rescue 线**终止**，叙事改为 `DR≈ALNS / learned 控制无需人工调参即追平专家手搓 ALNS`（诚实中性结果，二级贡献 / future-work）；② 论文 +10% 头条**不是 DR 的责任**，由方法族(ALNS winner kernel + 机制) vs 文献元启发式承担 = **轨②(M1)**，那才是真正未完的关键路径；③ x86 DR 这条线可收尾，后续若碰 DR 只作 future-work 深挖、且任何「修」必须先有文献/参考代码依据再动手（实践已证靠自创解决不了）。
- ⛔ 2026-06-27 [x86/DR] **上条 Pilot18「DR 终止」结论被 user 推翻，更正如下（Claude 认错）**：Pilot18 的 headroom=0 是「启发式跟启发式比」——winner kernel/SA/tuned-meta 全卡在**同一局部最优盆地**、16× 预算跳不出去，这**不等于近全局最优**。Claude 之前把「多个启发式互相持平」误当「近最优」，是逻辑漏洞；**「近最优」从来只由启发式互相印证、从未由精确下界证明过**。user 指出：DR-ALNS 是新兴算法，必须和传统算法拉开差距（**10% 是最低线、理想 50%**），被轻易追平不可能，是实现/方法还没找到病根；之所以「像到头了」是因为**还没上精确算法**，缺一把量「离真答案多远」的尺子。**仍成立的部分**：DR 现用 ALNS 的破坏-修复招式 → 与 ALNS 同盆地 → 故 DR≈ALNS；所以调 reward/修早停/再长训这类 RL 旋钮确是死路，真病根=**招式太弱、跳不出盆地**。**更正后的下一步（关键路径）**：先在小算例上拿**精确解/下界**（MILP / LP 松弛 / VRP 列生成下界；或异种范式强搜索取 best-known）量启发式盆地离最优多远——差大→有空间，DR 该装更强招式（exact-guided LNS / 学习型大邻域 / 神经构造，先查文献+参考代码再动手）；差小→才真到头。待查：仓库是否已有问题的精确/MILP 模型，还是需新建（注意 EV 非线性充电+公平会让严格精确很难，可能先做可解松弛的下界）。记忆已记 [[dr-alns-optimality-gap-open]]。
- 🧪 2026-06-27 [x86/DR] **方向定案：先上 ALNS+Set-Partitioning 路线池重组当 headroom 探针（Pilot19），提示词 `docs/handoff/codex_prompts/08_route_pool_sp_headroom.md`（待发 Codex）。** user 否决了"靠想修 DR"，要求一次干成、先做足准备、用现成文献经验。Claude 通读 `cost.py/evaluation.py/check.py/winner_operators.py/action_space.py` + 文献（NLNS/Learning-to-Delegate/DR-ALNS=算子选择/SP matheuristic ~12%）后定的策略：
  - **病根精确定位**：DR（动作空间同 Reijnen 原版 `[3,1,10,100]`=选算子+调破坏量+调温度）与 winner kernel 共用同一族破坏-修复邻域 → 同盆地 → 必然持平；`_run_winner_kernel_loop` 只留 best/current、**把搜索中几千条好路线全扔了**。Pilot18 的"到头"只是单轨迹榨干，非"无更优解"。
  - **关键核实（SP 可直接用的条件）**：①目标函数**完全可加**（`penalized_obj` 成本=`evaluate` total_cost=固定+里程+油+电+占用+转运+碳，碳对总排放线性、配额是常数偏移；**公平默认关**`fairness_enabled=False`/`FAIRNESS_ENABLED=False`，无跨路线非线性耦合）；②跨路线约束仅三类（客户恰好覆盖一次=SP 分割本身、车队物理车上限 num_cv/num_ev 可靠多趟 #Tn 重贴标签满足、公共 f 站时隙容量是唯一要在 SP 防的真冲突）；③每条路线精确成本直接用 cost.py（单路线建 `Solution` 调 `evaluate(carbon_quota_kg=0)`）；④求解器用 worker 自带 `scipy.optimize.milp`(HiGHS)，**绕开 Gurobi**（呼应 user "大规模 Gurobi 不一定适用 + 正在申请"）。
  - **两层战略**：杠杆1=ALNS+SP 路线池重组（快/稳/文献~12%，先做，既冲"拉开 GA-VNS/LNS 大差距 + 拉开 ALNS≥10%"，又是最对路的 headroom 复测，还是 DR 地基）；杠杆2=把 DR 从"选算子"升级成"学破坏/学委派(Learning-to-Delegate, 治 150/200c)+学何时触发 SP 重组"——这才是 learned 真打过手搓 ALNS、DR 当大杀器的地方（user 目标：领先任意第二名≥10%、理想 50%；50% 多半对弱基线且逼近最优才有）。
  - **Pilot19 预写死判级**：任一规模 SP headroom≥10%→`ROOM_CONFIRMED`(推翻 Pilot18 到头, 绿灯杠杆2)；2–10%→`MODEST_ROOM`(ALNS+SP 已是超 GA-VNS/LNS 的贡献)；全<2%→`NO_ROOM_SP`(比 Pilot18 更硬的近最优证据, 等 Gurobi 上精确下界)。绝对边界：不训练/不改 cost·check·evaluation·winner 语义/SP 只后处理(真 check+真 evaluate 当裁判)/worker py313·numpy2.3.5/只 x86 相对。**M1 线在优化 ALNS，记着但本轮不受干扰。**
- ✅ 2026-06-28 [x86/DR] Pilot19 路线池 + Set-Partitioning headroom 探针已实现并跑完：新增纯后处理模块 `route_pool_sp.py`、runner `pilot19_route_pool_sp_probe.py`、单测 `test_route_pool_sp.py`，不训练、不改 `cost.py/check.py/evaluation.py/winner_operators.py` 语义；worker/runner 均用 py313 + NumPy 2.3.5 + SciPy HiGHS。结果 `50/75/100/150/200c-03`、12 seeds、`eval_budget=20/block_size=4` 全部 `NO_ROOM_SP`，`best_single == sp_cost`、headroom 全 `0.000%`、重建后真 `check_solution` 零违约；50/75/100/150 有补充 harvest，200c 因运行时把补充 harvest 降到 `10 eval / 60s`（不影响 best_single 口径）。结论：该 ALNS 路线池 + SP 重组机制没有跳出当前盆地，比 Pilot18 更硬地说明“同族路线重组”没有可榨 headroom，但仍不是精确下界/全局最优证明；若继续证明离最优差距，下一把尺子仍应是 MILP/LP/列生成下界或异种范式强搜索。
- 📚 2026-06-28 [x86/DR] **文献彻底精读 → 决定性结论 + 「救活/领跑 DR-ALNS」策略定案（Claude 主线判断，未训练未改代码）**。user 定调：dr-alns 拉垮是主线（ALNS/VNS/LNS 顺带），文献是参考非天花板（它们的局限=我们超越点）。精读集（决定性）：目标刊《系统工程理论与实践》3 篇（李得成2021 branch-price EVRP、陈婉茹2023 多中心混合车队碳交易 PIVNS、陈雨蝶2025 复杂冷链 MPGA-VNS）；顶刊3 篇（周鲜成2026《中国管理科学》非线性EVRP+改进鲸鱼、师瑞阳2024《中国科学·数学》CapsGAT神经构造、谭云洁2025 本校上理工·倪静组 神经构造MFF）；李英2020《系统管理学报》SS-MACO；张玉玺2025 元启发综述；导师邱莹莹·干宏程2024 PSO；**DR-ALNS 源码**（Cao DRL-ALNS-EVRP 的 DDQN+算子真实代码、Reijnen DR-ALNS、N-Wouda ALNS、wangqianlong ALNS）；网络 NLNS/Learning-to-Delegate/RL4CO。
  - **Pilot19 NO_ROOM 是假阴性（更正上条 line144）**：池子退化——`池大小−客户数=SP选中数=一个解的路线数`（50c:58−50=8、200c:228−200=28），12 seeds 去重后只剩"一个解"那么多条不同路线（winner kernel 太收敛+eval_budget=20 太小），SP 没有不同积木可拼→必然 0。**不能据此说"SP 无用/已近最优"**；真做需"真预算+全轨迹+扰动"的多样池。
  - **🎯 决定性因素**：差距 = 富问题 × 强军火 × 比对象弱。成熟 benchmark 上强算法互相<1%（陈婉茹 Table5）；**富/新模型上通用基线离最优远→差距 10-27% 可达**（周鲜成对 ALNS +9.73%/VNS +16.67%；谭云洁对 GA/ALNS/AM 距离 −27.6%）。→ **user 的"领先第二名≥10%、理想更高"可达，但只在"你那比全库都富的模型上、对手是正常发挥的 GA/PSO/蚁群/水滴(IWD)/VNS/AM"，不是对自家强 winner kernel。**
  - **🔑 DR-ALNS 为何拉垮（主线根因，三方+源码实锤）**：DR 学的是"在固定几个算子里挑/调权重"=Reijnen/Cao/我们同一范式→上限≈调好的自适应 ALNS。**Cao 源码避雷铁证**：`test_evrp_dlns_CEVRP-NL.py` 硬编码 `if dlns比alns差>2%: raise`、结尾 `if dlns不是最好: raise "DLNS should be the best"`→它的"DRL赢ALNS"是筛选运行+2%容忍喂出来的→这一档 DR vs 强 ALNS 真实差距就 ±2%。**我们拉垮=范式天花板，非实现烂。**
  - **两条学习范式+局限（=我们的机会）**：①学习改进/选算子(DR-ALNS: Reijnen/Cao)=只在简单题、对强ALNS≈持平；②神经构造(师瑞阳CapsGAT/谭云洁MFF/Kool AM)=对通用基线能拉大差距(27.6%)但**只在 TSP/CVRP/HCVRP/基础TW、纯距离/弱约束、泛化差**。**两条路都没碰过你这种"两层时变碳+EV非线性充电+公平+动态+混合车队+多车场"的富问题——处女地=新故事+大差距入口。**
  - **🏆 救活/领跑 DR-ALNS 策略（surpass-the-references）**：DR 学习不止选算子，而是 **(a) 学习型破坏/委派**（神经网络选"拆哪些客户/哪块区域重优化"，借 NLNS/Learning-to-Delegate/师瑞阳-谭云洁的神经力进 ALNS 循环=DR 真能超手搓 ALNS 的唯一已验证途径）+ **(b) 控有威力的决策**（碳感知充电/出发时刻=卡电网清洁时段=你独有两层时变碳，周鲜成同思路躲拥堵上顶刊；破坏规模；接受阈值）+ **(c) 跑在全富问题上**。强军火配套（从 Cao/周鲜成/综述搬）：SISR 字符串破坏、regret+2-opt、FRVCP 最优充电插入、公平/跨车场/多趟算子、route-first-charge-second 分层。
  - **验证三段（头条+可信度+诚实增量）**：①主擂台 DR-ALNS vs GA/PSO/蚁群/水滴/VNS/AM（**正常发挥不削弱**）→富问题上目标平均≥10%、每规模≥0；②可信度锚 小规模 5/10/25c vs 精确解 CPLEX/branch-price gap→0（李得成/李英模板，堵"只赢弱鸡"）；③DR 消融 vs 强 ALNS 小幅稳赢+省迭代/跨规模免调参（DR 诚实增量，别指望这项 10%）。
  - **诚实 caveat**：10-27% 是对**通用场**(GA/PSO/AM)；对强 tuned-ALNS/精确解差距小→必须"vs通用场拉差距 + vs精确解证近最优"两条腿，才既能吹又站得住。
  - **下一步（user 暂缓写 Codex 提示词，先研究清楚）**：可选再精读神经构造实现细节（师瑞阳 CapsGAT 解码/掩码、谭云洁 MFF 编解码、Hottung NLNS 代码）夯实"学习型破坏"接口；再写 DR 学(碳感知时刻+学习型破坏)提示词。记忆已记 [[dr-alns-winning-strategy]] / [[dr-alns-optimality-gap-open]]。
- 🌍 2026-06-28 [x86/DR] **国际顶刊/arXiv SOTA 精读补充（user 提醒：国外常出牛方法）→ 权威背书我们的白地**。读 **NCO 综述 2024（arXiv 2406.00415，HTML 正文）** + 国际 EVRP-DRL 一批。
  - **国际牛方法分类（可借的"学习型破坏/委派"在此）**：L2C 构造(POMO/AM/LEHD，TSP/CVRP-100 对 HGS gap 0.03-0.1%、快但**只在简单题**)；**L2I 学习改进/神经局部搜索(NeuRewriter/NeuOpt/N2OPT/GAMA——神经网络选"哪块区域/节点对"破坏再修=我们要的"学习型破坏")**；L2P 神经辅助 OR(NeuroLKH/DIFUSCO + L2P-M 每步指导 LKH=神经-OR 混合)；大规模分解(GLOP AAAI24/L2D/learning-to-segment)。
  - **🎯 综述自陈的 open challenges = 我们的白地（权威背书）**：①泛化差(换分布/规模就崩)；②大规模难(万节点 transformer 内存爆)；③**"多数 NCO 解不能同时处理多种 VRP 变体/附加约束，必须为带约束变体专门设计"——综述全文未提 EV 充电/电池**；④"神经 vs OR 的不公平比较"是公认软肋；⑤**神经-OR 混合(L2P-M)是被点名的有前途方向**。
  - **国际 EVRP-DRL 竞品 + 局限**：eco-routing ELV(DRL+GAT, 能耗/充电站/坡度, 神经构造)、HAMEDA(异构注意力, 省能≥1.64%)、Token-specific DRL EVRP、Learning-to-Dial-a-Ride-electric 等——**都是神经构造、能耗单约束为主、margin 不大(~1.6%)；没有一个做"两层时变碳+公平+动态+混合CV/EV+多车场"的全富问题，也没有一个是 DR-guided ALNS。**
  - **🏆 结论强化**：①我们的 surpass 路线（DR 学"学习型破坏"+碳感知时刻，跑在全富 EVRP 上）正落在**综述点名的两大前沿（神经-OR 混合 + 富约束变体）**且 **EV+两层碳+公平+动态是国际上的真空白地** → 新颖性/故事极强；②"学习型破坏"有成熟国际方法可借（NeuOpt/NeuRewriter/GAMA 的 region-select 思想接进 ALNS 的 destroy）；③大规模 150/200c 可借 GLOP/分解；④诚实 caveat 不变（神经法质量多在简单题追平、赢在速度；我们靠"富问题拉通用场 + 小规模证近最优"两条腿）。记忆 [[dr-alns-winning-strategy]] 已并入国际方法。
- 📐 2026-06-28 [x86/DR] **方案蓝图落盘 `docs/handoff/dr_alns_learned_destroy_plan.md`**（Claude 写，未训练未改代码）。深挖代码细节定论：①NLNS=神经学**修复**+程序化破坏；②NeuOpt=学 k-opt **移动**(S/I/E-move)+**GIRE 可行/不可行都探索**(非硬掩码)；③**我们 DR 的接口改动点**=把 `block_env` 的"选 destroy 算子下标"换成"对每客户打移除分(pointer/attention)"——需 worker 吐**逐客户特征**、policy 加**指针头**、新增 `remove_given_customers` 算子；GIRE 用我们已有的 `penalized_obj` BIG_M 罚天然支持。方案四层(L0 强基座+FRVCP/SISR / L1 学习型破坏 / L2 碳感知时刻控制 / 大规模 GLOP 分解)、验证三段(主擂台 vs 通用场≥10% + 小规模 vs 精确解 gap→0 + 消融)、**分阶段(阶段A 小算力先证"learned-destroy > operator-select" 再大投，防重蹈烧夜训)**。下一步=解除暂缓后写阶段A Codex 提示词。
- ✍️ 2026-06-28 [x86/DR] **阶段A Codex 提示词写定 `docs/handoff/codex_prompts/09_dr_learned_destroy_phaseA.md`（待发 Codex）**。scope：只做"学习型破坏接口 + 小算力(25/50c)证 learned-destroy > operator-select"，**不全训、不上 GA/PSO 擂台、不建精确解**(那是阶段B)。核心改动=worker 吐逐客户特征 + `remove_given_customers` 算子；policy 加指针头(对每客户打移除分、采样 q 个无放回)；env `control_mode=learned_destroy`；GIRE 用现成 `penalized_obj` BIG_M 罚(允许探不可行)；修复保持与基线同口径以隔离变量。内部对比 learned-destroy vs operator-select vs random，同 evaluate/check/预算/seeds。预写死判级：held-out 小算例平均 ≥+2%/每规模≥0/稳超 random/零违约 → `PASS_LEARNED_DESTROY` 进阶段B；0~+2% → `WEAK` 调特征/reward 再判；≤0 → `HALT` 诚实负结果回蓝图重想。铁律：小算力不烧夜训、不改保护文件、worker py313、x86 相对、先证据后单点改再短测。
- ⚠️ 2026-06-28 [x86/DR] **Pilot20 Phase A 跑完 = `UNDERTRAINED_DEFERRED_RUNTIME`（learned-destroy 没赢，但不算真负）**。commits `5541a2ed`(reconcile 分支) + `b1b5c0b7`(run)；dr-x86 ahead origin 4 未 push。结果：learned vs operator-select `formal_120 −22.4% / formal_240 −12.3% / formal_240_b180 −14.5%`，reward early/mid/late 42.728/42.671/42.933（**+0.48%≈平**，未训练充分）；三档满预算/零违约/worker py313。**Claude 读码诊断（careful，回答 user 两问）**：
  - **真根因（读 `learned_destroy_reward`）**：reward_sum≈`120×总best改进`，全程几乎不动 → **策略怎么拆，最终解几乎一样 → 学习信号近零**。（纠正：reward 是按完整目标 best_obj 算的，`curriculum_phase="route"` 只是观测标签、没卡住目标。）
  - **Q2「文献/代码没吃透」=成立、主因**：①reward `max(0,..)` 砍掉所有非改进/恶化步的信号→学不到"拆谁"的梯度；②学习机器太轻=裸 pointer 套 18 手工特征、**无解/图编码器**（NeuOpt 有 RDS、NLNS 有注意力重建）；③我们学**破坏**、NLNS 学**修复**、NeuOpt 学**k-opt+GIRE**，没忠实复刻任何一个；④240 episode vs 文献 10⁴⁻⁶。
  - **Q1「课程/教材不对」=部分成立、待证**：25/50c 小算例+小预算，破坏对最终解的杠杆可能本就低（Pilot18/19 饱和同源）。需实验证。
- 🧪 2026-06-28 [x86/DR] **应对=Pilot21「大实验」提示词 `docs/handoff/codex_prompts/10_dr_learned_destroy_big_experiment.md`（待发 Codex；user 要直接上大实验、自动化先后串行、不盯）**。一份入口脚本自动跑四阶段带闸自停：**Stage0** 破坏杠杆诊断(operator-select vs worst_removal-fixed vs best-of-k 上界探针，同预算)——闸 G0 无杠杆→`HALT_NO_DESTROY_HEADROOM`(换 DR 旋钮如碳感知时刻/或 future-work)；**Stage1** 修学习机器(去 reward `max(0)` 钳制改带符号 delta + 加小自注意力解编码器 + 放量训≥1000ep)——闸 G1 曲线仍平→`HALT_LEARNER_FLAT`(别再加 episode)；**Stage2** held-out 对比 learned vs operator-select vs random vs worst_removal + 判级 PASS(≥+2%每规模≥0稳超 random/worst+零违约→Phase B)/WEAK/HALT(诚实负结果→future-work)。铁律：不改保护文件、worker py313、x86 相对、阶段间闸不过即自停不 blind 烧、周期 checkpoint 可中断。**大局诚实**：论文头条不押 DR(押富问题 vs 弱场+机制)；learned-destroy 即便止损也踩在 NCO 综述"富约束 VRP 对神经法开放难题"上，负结果有家。
- 🧪 2026-06-28 [x86/DR] **Pilot21 大实验跑完 = `HALT_LEARNER_FLAT`（G1 自停，commit `e2fc52e4`，dr-x86 ahead origin 1 未 push）**。Stage0 `G0 PASS`：25c best-of-k 比 operator-select 高 **6.26% → 破坏有杠杆(Q1=是)**。Stage1 训 1000ep/250updates 后 G1 失败，但**唯一失败项是"熵没降"**（`reward_lift=+7.29%、slope=+0.0046、loss 有限、kl ok、entropy 21.35→30.78`）。Stage2 未跑（闸没过，纪律对）。
  - **🔬 Claude 查资料判定（user 要求"查资料修不许拍脑袋"）**：HALT **大概率是判据误杀**，且 reward/基线设计**没对齐已发表方法**。三条有出处的修法：①**G1 判据改对**——删"熵必须降"，改 held-out 验证 cost 判据；依据 POMO(NeurIPS2020)核心"防过早收敛、熵不塌缩才健康"+NCO 看验证 gap 不看熵(RL4CO)。②**上 POMO shared baseline**——同算例多 rollout 组均值作基线，低方差抗局部最优；替我们的单轨迹 GAE。③**reward 换 5/3/1/0 离散结果分**——Cao `alns/ALNS.py:34 reward_list=[5,3,1,0]`、Reijnen 同类(选择型策略验证 reward)；弃我们自创 120×best_gain（注：Pilot21 已去 `max(0)` 钳制成带符号，但仍是自创 scaling）。NLNS 的连续增量+critic 是给学习型**修复**的、不直接套破坏选择。
  - **应对=Pilot22 提示词 `docs/handoff/codex_prompts/11_dr_learned_destroy_pilot22_grounded_fixes.md`（待发 Codex，自动过夜）**：Stage A 先拿现 ckpt 跑 exploratory held-out 量真实差距(看 +7.3%reward 把 Pilot20 −22% 缩没缩)→ Stage B 按三修法改+重训(G1 用验证-cost 判据)→ Stage C 对比判级 PASS/WEAK/HALT。含 **resume guard**(禁 `--resume` 绕过 HALT 类 state 进后续 stage)、沿用 §9 过夜铁律(飞行前检查/checkpoint/≤6h/防OOM/禁过夜push/增量留痕)。**三修法全部带出处，不拍脑袋。**
- 🚀 2026-06-28 [x86/DR] **Pilot22 跑完 = `HALT_POLICY_UNSTABLE`，但实为突破被不稳定盖住（commit `0b364f49`，dr-x86 ahead origin 2 未 push）**。三条文献修法**全做对**(报告逐条记症状/出处/改哪)：held-out 验证从 Pilot21 ckpt 的 **−15.9% 冲到 +11.5% 峰值**(摆动 +27pp)，`zero_violations=True、entropy_floor_ok=True、finite_losses=True`；**唯一坏项**=`kl_ok=False`(KL 爆) + `gain_slope=−0.10`(峰后回落到 final +2.4%)。Stage C 正确未跑(闸没过)。**判读**：learned-destroy **真能学到比 operator-select 强 ~11% 的破坏**(整条线首次见真东西，Q1+Q2 基本=能)；剩下纯属**经典 PPO 后期 KL 失稳 + 未做 checkpoint 选择**，RL 有标准解。caveat：+11.5% 是 validation 峰值、可能含选择偏差/噪声，**必须在独立 test 上复核**(防 validation=test 泄漏)。
  - **应对=Pilot23 提示词 `docs/handoff/codex_prompts/12_dr_learned_destroy_pilot23_stabilize.md`（待发 Codex，自动过夜）**，文献依据解法(公认可引用)：①**选峰值 checkpoint + 独立 test 复核**(POMO/AM/RL4CO 标准；validation 与 test 分离防泄漏)——Stage A 先拿峰值 ckpt 在全新独立 test 上评，**≥+5%/每规模≥0/稳超 random+worst/零违约 → `PASS_LEARNED_DESTROY_CKPT` 直接绿灯 Phase B、不必再训**；<+2%→峰值是噪声→进 Stage B。②**target-KL early stopping**(PPO/Schulman/SB3，直接修 kl_ok=False)。③**LR 退火 + 减 clip/减 ppo_epochs**。保留 POMO baseline+5/3/1/0 reward+验证判据。Stage B 稳定化重训(validation/test 严分、存 best-validation ckpt)→ Stage C 独立 test 判级。resume guard + §9 过夜铁律照旧。**最便宜的胜路=Stage A 峰值 ckpt 在独立 test 上保住 +10%，那基本就赢了(checkpoint-selection 是标准实践，非作弊)。**
- 🧱 2026-06-28 [x86/DR] **Pilot23 跑完 = `HALT_POLICY_UNSTABLE_V2`（commit `bed95aa0`，dr-x86 ahead origin 1 未 push）。真根因终于定位到「数据/泛化」，不是算法。** Stage A：Pilot22 峰值 ckpt 在独立 test 上 **−17.7%**（+11.5% 没保住=过拟合虚高）。Stage B：target-KL 修生效 `max_approx_kl=0.017`(KL 稳了)，但 val 峰 +12.07% 回落到 final +8.69%(>3pp 阈值)→停。**执行中发现数据 bug**：计划的 25c 独立 test `E-UK25_01` 缺 `carbon_profile.csv`(遗留旧 bundle)、其余 25c 与 train 重叠 → 只能用 50c/100c 当独立 test → **等于"25c 训、50-100c 考"，−17.7% 混了过拟合+跨规模**。
  - **🔗 证明逻辑链（Claude，user 要"确定有用+逻辑链+正确文献+充足样本"）**：P1 val+12%/test−17.7%=过拟合+跨规模；P2 文献=NCO 泛化需"大量·每批新生成·同规模"分布(POMO/AM ~1 亿新算例、同规模；scale-match 必要)；P3 我们 ~9 固定算例+跨规模→违反 P2 约 7 个数量级；C 失败是数据所致非算法，修数据移除成因；满足数据仍不泛化=稳健真负→止损。**诚实边界：富问题 eval 贵、到不了 POMO 规模，不保证 PASS，只保证"直击根因+必给定论"。** 文献：POMO NeurIPS2020 / IJCAI2024 generalizable-neural-solvers / LEHD / Rethinking-NCO-constraint-tightness / ACM"Applicability of NCO: A Critical View"(防再吹)。
  - **生成器已核**：`models/src/setp_instance_lab/build_curriculum_instances.py` 写完整 bundle(含 carbon_profile、validation_passed)、按 seed 参数化 → **改 seed 可批量造充足、完整、同规模新算例**；E-UK25_01 是遗留旧 bundle、非此生成器产物。
  - **应对=Pilot24 提示词 `docs/handoff/codex_prompts/13_dr_learned_destroy_pilot24_data_generalization.md`（待发 Codex，最后一次数据修，自动过夜）**：Stage0 按 seed 造数百~上千**同规模、完整、validation_passed**新算例，**train/val/test 三 seed 段不重叠**(排除遗留缺 carbon 的旧 bundle)；Stage1 **POMO fresh-per-batch**(每批取新算例、严禁复用固定9个)、同规模训、保留 target-KL/POMO baseline/5310 reward/LR 退火/best-val ckpt；Stage2 **独立同规模 test** 判级：PASS(≥+2%稳超random/worst零违约→Phase B)/`HALT_NO_GENERALIZATION`(数据做对仍不泛化=稳健真负→**止损 DR 转 future-work**，论文锁回富问题vs弱场+机制)。**user 已拍板：这是最后一次数据修，HALT 即止损。**
- 🔁 2026-06-28 [x86/DR] **user 推翻"止损"，质问前几轮是否真尽力——Claude 认账：前 5 轮(Pilot20-24)有真偷工**：①DR 一直用**弱通用算子(烧火棍)**，从没接仓库已验证的强招式(`charging.py` 充电插入 / `local_search.py` 2-opt / `fairness.py` / SISR / 碳感知)；②只试"挑算子"这一弱变体，**没试文献真正赢的**(NLNS 学修复 / NeuOpt 学 k-opt / 高杠杆碳感知时刻)；③**练严重不够**(Pilot24 仅 250)；④别人源码只读摘要/README、**没逐行吃透**。**故"RL 挑算子≈ALNS(+1~2%)"成立(死)，但"DR 满配(强招式+真变体+练够)"从没真试过——"DR 彻底没希望"是 Claude 武断。** 已核仓库确有强招式(`charging/local_search/fairness/dynamic/winner_operators` + `metaheuristic_baselines.py` 弱场)。
  - **终局方案=Pilot25 提示词 `docs/handoff/codex_prompts/14_dr_alns_pilot25_endgame.md`（满配·吃透·双保险·自动过夜，待发 Codex）**：Stage0 逐行吃透赢家源码(Cao/NeuOpt/NLNS/POMO/GLOP)出实证清单 + 给 DR **接上强招式**；Stage1 真学习变体(**碳感知时刻控制**=独有高杠杆/或学修复) + Daysalilar 小→大课程 + **练够**(≤8h 最大量)；Stage2 **双重判级**——(A)DR vs operator-select/plain-ALNS 增量 +(B)强方法 vs GA/PSO/蚁群/水滴**弱场领先(论文 10% 目标)** +(C)小规模对精确解锚。判级 `PASS_DR_BREAKTHROUGH`(DR 真突破) / **`PARTIAL_WIN_NO_DR`(DR≈ALNS 但强方法碾弱场≥10% → 论文照样成立、DR 转 future-work=不空手而归的保底)** / `HALT_BOTH`。**诚实边界**：不保证 DR 达 10%(算力/数据/富约束泛化难)，但保证满配机会 + 无论成败都给可发表结论。沿用 §9 过夜铁律 + resume guard + 禁过夜 push。
- 🟥 2026-06-29 [x86/DR] **Pilot25 终局跑完=`PARTIAL_WIN_NO_DR`（commit `38e1d80a`，dr-x86 ahead 2 未 push）；但 Claude 第一手核 `pilot25_test_rows.csv` 发现"对弱场 +27.8%"是假的。** 满配 DR 仍败：learned-destroy 独立 test −24.4% vs operator-select、−15.9% vs plain-ALNS，验证 +1.6%(KL 稳 max_kl=0.0144、零违约)→**静态 DR 第 6 次证伪，铁定死**。`pilot25_absorption.md` 诚实标注"未重实现外部系统"(读机制未重写 NeuOpt/NLNS)。**假 27.8% 实锤**：test_rows 里 GA/PSO/ACO/IWD/VNS 的 best_cost 全=warm start(如 823.15)、解哈希相同、各跑 0.2-0.6s——**因为只给 120 次评估，种群法连一代都跑不完、原样交回初始解**(HANDOFF §5 早警告"评估太少假性碾压"成真)。**故"赢弱场 10%"也没真拿到，两个都还没到手。** Claude 差点 celebrate 假结果，第一手取证拦住=证据链此前不充分的教训。
- 🎯 2026-06-29 [x86/DR] **终局方案 `docs/handoff/codex_prompts/15_final_trackA_fair_baselines_trackB_dynamic_dr.md`（待发 Codex）**。**Track A(论文命根，优先)=把"赢弱场"做成真**：强方法(winner kernel+碳感知+FRVCP+SISR+fairness+local_search) vs 8 基线，**同等充足预算(等墙钟 或 16000 eval，不是 120)**、同 warm start、10 seed、Wilcoxon；**焊死基线健康验证**(每基线须 actual_evals/budget≥0.9 且 best_cost<warm_start 且哈希≠warm start，否则 `HALT_BASELINE_NOT_RUNNING` 先修——这是假 27.8% 的直接补丁)；先证"强方法明显胜 plain ALNS"。判级 `WIN_REAL`(每基线健康+领先≥10%+显著→推全规模) / `MARGIN_SMALL`(公平后<10%→诚实调叙事或再强化) / `HALT_BASELINE_NOT_RUNNING`(先修基线)。**诚实边界：公平后差距未知、可能远小于 27.8%。** **Track B(future-work)=DR 挪到动态滚动重规划**(`dynamic.py`，RL 真本事在序贯决策；对照=每阶段重跑 ALNS)：`DR_DYNAMIC_PROMISING`/`DR_DYNAMIC_FLAT`，不保证成。先 A 后 B。**静态 DR 不再碰。**
- ✅ 2026-06-29 [x86/DR→论文线] **Track16 跑出干净进展：7 基线里 6 个健康(GA/VNS/SA/GWO/ACO/IWD)，仅 PSO 不健康；DR 主线已关、拼装 strong_method 已弃。** user 自写的 Track16 方案(健康字段矩阵 + 逐个修基线 + 主方法 ablation 不硬焊 + 公平对比)很专业，已落 runner/健康字段/基线修复(commit 未 push，dr-x86)。`track16_baseline_health_matrix.csv` 等已留。PSO 根因(干净)：生成 3978 个**唯一**候选但 `best_update_count=0`、无一优于 warm start → **搜索方向弱、非编码 bug**；已累计 Track15(3 次自修)+Track16(根因) >3 轮。runner 在 Stage1 把 PSO 当"必须健康"→`HALT_BASELINE_NOT_RUNNING`，Stage2/3 未跑。
- 🎯 2026-06-29 [论文线] **判断：按 user 自己 Track16 Stage4 规则，PSO 根因查清后应"透明排除"、不再 patch、10% 只基于健康基线 → 解锁继续。Track17 提示词 `docs/handoff/codex_prompts/17_track17_unblock_fair_comparison.md`（待发 Codex）**：Stage A 冻结 6 健康基线 + 写死 PSO 排除声明(不当输家)；Stage B 主方法 ablation(plain_alns/winner_kernel/单组件逐个加、只留"加了不掉分"的、必须 ≥plain_alns；Track15 已证硬焊全组件 −3.69% 拖后腿)；Stage C 正式公平对比(主方法 vs 6 健康基线，25/50/100c、≥10 seed、16000 eval/等墙钟、Wilcoxon)→ `WIN_REAL`(每基线≥10%显著)/`MARGIN_REAL`(诚实报真数字)/`HALT_MAIN_NOT_STRONG`。**赢点很近**：普通 ALNS 已 +8.5% vs 健康 GA。论文头条=方法 vs 弱场(健康)；DR=future-work。沿用 §9 过夜铁律。
- 🧭 2026-06-29 [双线并行，user 选"3"] **user 质问"Track16/17 与 DR 效能何干"——Claude 认账**：Track16/17 **不提升 DR 本身效能**，是论文保底(DR-ALNS≈强 ALNS，骑底座领先弱场，RL 只加 1~2%)。两目标须分清：(a)"DR-ALNS 方法名领先 10%"=Track17 可达、DR 是配角；(b)"DR 的 RL 本身提效"=**静态死(6 次+Cao 源码 RL 是摆设)，唯一活路是动态**(RL 本事在序贯/不确定决策)。user 选 **3=两条都要**：Track17 锁论文底 + 动态 DR 搏 DR 真创新。**静态 DR 不再碰。**
- 🧪 2026-06-29 [动态 DR 主线] **Track18 提示词 `docs/handoff/codex_prompts/18_track18_dynamic_dr.md`（待发 Codex）**，第一手读穿 `dynamic.py` 后定：已有 `run_rolling_reoptimization`(事件滚动、**每阶段 ALNS 重解=myopic 强基线**)且已算 `information_cost=动态−全信息静态`。**Stage0 先量 anticipation headroom=information_cost%(廉价不训练)**：≥~5%→预判有空间进 Stage1；<~2%→`HALT_NO_ANTICIPATION_HEADROOM`(myopic 已逼近全信息，动态 DR 也封顶，诚实止损，DR 整体 future-work)。Stage1 anticipatory DR(预留运力/承诺-延后/预定位，接 `_run_stage_plan` 注入口，不改 evaluate/check；足量 fresh 动态场景训、POMO baseline/target-KL/LR 退火)→ Stage2 vs myopic 重优化判 `DR_DYNAMIC_REAL_GAIN`/`DR_DYNAMIC_FLAT`。文献 Ulmer 等 anticipatory dynamic-VRP。**这是 DR 唯一有理论优势、且没被证伪的效能战场；先量 headroom 再训，杜绝静态覆辙。**
- 🧾 2026-06-30 [双线结果 + 修正归因] **Track17=`HALT_BASELINE_NOT_RUNNING`(100c GA 不健康)；Track18=`HALT_DYNAMIC_POLICY_ENTRYPOINT`(有 headroom 无接口)。** Track17 主方法选出 `winner_kernel_true_repair_adaptive_q`(验证集比 plain_alns **+3.23%**，非 10%)；Track18 Stage0 实测 mean `information_cost=43.99%`(25c 37.9/50c 50.1，20/20 健康)。**Claude 第一手复核纠正归因（连 Superpowers 自我纠正都漏了）**：100c 失败**不是 GA 专属 bug**——`track17_rows.csv` 里**主方法在 100c 也只跑 4234/16000(ratio 0.264)、也返回 warm start** → **100c 900s/16000 预算不够、主方法与基线都饿死**(HALT_BASELINE_THROUGHPUT 再现)。且健康门"best<warm"混淆了"没跑起来(无效)"与"跑满但确实弱(合法输)"。诚实结论(可证伪、不为自圆其说)：①**当前不能写 10%**(100c 正式比较未合法完成)，但**未证永不可达**；②主方法是"验证集最强候选(+3.23%)"，**非全规模已证**(它 100c 也饿死)；③动态 43.99% headroom 是真的，但**未证其中多少可被在线策略约减**(可能含全信息不可达/承诺不可逆/预算协议损失)。
- 🎯 2026-06-30 [两条可证伪证据修复步，先证据后结论] **Track19 `docs/handoff/codex_prompts/19_*.md`**=修 100c 预算(给够让主方法达 eval ratio≥0.9 或严格等墙钟)+修健康门三态(INVALID_NOT_RUN 才阻断；VALID_BUT_WEAK 算合法输计入)→重跑正式比较报**真数字**：`WIN_REAL(≥10%)`/`MARGIN_REAL(<10% 诚实写)`/`HALT_100C_STILL_STARVED`(吞吐真硬骨头→先押 25/50c 合法结论)。可证伪：修好预算后≥10% 则"降级叙事"过早；仍<10% 才降级。**Track20 `docs/handoff/codex_prompts/20_*.md`**=动态先加**非学习**动作接口(reserve capacity/commit-defer/vehicle preposition 挂 rolling 回调，不改评分)+**启发式 sanity**在 20 场景看 information_cost 降不降→`HEURISTIC_MOVES_HEADROOM`(能降=DR 值得建)/`HEURISTIC_FLAT`(降不动=现有动作吃不到 headroom、训 DR 白烧)。可证伪：启发式能显著降则"DR 只能 future work"过早；降不动才停。**先证动作面有效再训学习器；先修证据链合法再定叙事。静态 DR 不碰。**
- 🔴 2026-06-30 [根因翻案·可能推翻大量负结论] **Claude 第一手读穿代码，定位 winner-kernel 车队 policy 漏传 bug——这可能是"100c 卡死 / DR 拉垮 / 领先只有 3-7%"一系列负结论的共同污染源。** 证据链(全是实读代码)：`winner_operators.py:656` 的 winner-kernel 主循环 `policy = SearchPolicy(require_charging_signal=config.require_charging_signal)` **漏传 `max_cv/max_ev`**，静默落到类默认 `UNBOUNDED_FLEET`(100万)；而正确写法在 `alns_wouda.py:120`(`max_cv=limits.cv,max_ev=limits.ev`)与 `root_cause.py:244` 都存在——**说明此坑曾被局部修过、但没同步进 winner_operators.py**。传导：`feasible_repair.py:239/246` 用 `policy.max_cv/max_ev` 判可否新开路线(无限→乱开)、`:360-361` 多趟(`#Tn`)归一化也用同一无限值(压缩失效)→候选真实车队超编→`check.py`(按 `instance.num_cv=10`)判违约→`penalized_obj`+BIG_M→默认 HillClimbing 全拒→跑满 16000 eval 仍 `unique_solution_count=1` 卡死(与 Track19 100-01 现象完全吻合)。时间线：`13989832 Restore Goeke battery and physical fleet trips` 把车队收紧为物理车计数(100-01=10CV+10EV)后，此前隐性 bug 变必现卡死；这也解释"当年 winner 碾 SA 8.76%(£4878 锚)、如今同算例卡死"。**唯一未 100% 锁死**：SA 为何没中招(大概率走 `alns_wouda.py:120` 正确默认路径)——留 Track21 Stage0 锁死。**含义：Track17/19 的 5-7%/不到10%、以及 Pilot20-25 的 DR≈ALNS/学不到，都可能是此 bug 压制候选所致，需修复后重跑，不能仍当定论。**
- 🎯 2026-06-30 [下一步·彻底修复非验证] **Track21 `docs/handoff/codex_prompts/21_*.md`**(应 user 要求写成"认定根因→彻底修→全面清算"而非验证式)：Stage0 锁死无第二坑(审计全仓所有 `SearchPolicy(` 构造点是否传车队上限)→Stage1 winner_operators 及所有漏传点统一走 `infer_fleet_limits` 传真实 `max_cv/max_ev`+防回归单测(不改保护文件语义、保留 UNBOUNDED_FLEET 作"未指定"合法默认)→Stage2 确认 winner 复活(100-01/100c `unique_count≫1`、best≪warm、vs SA 重新≥)→Stage3 作废并重跑被污染对比(标 `SUPERSEDED_BY_TRACK21_FLEET_FIX`，复用 Track19 预算/健康门三态/6健康基线，判 WIN_REAL/MARGIN_REAL/HALT_100C_STILL_STARVED)→Stage4 标注 DR 历史负结论污染范围(核 DR worker 侧是否同样漏传、不重训只标注)。**这是一行漏传参的修复，不是又一个通宵大实验；修好才知道我们是在修 bug 还是在给搜不动的算法编故事。**
- ✅ 2026-06-28 [x86/DR] **阶段A learned-destroy 接口实现完成，尚未正式训练/判级**：新增 `learned_destroy.py` 逐客户特征与全局观测、`learned_destroy_policy.py` pointer actor-critic、`pilot20_learned_destroy_phaseA.py` 小算力 runner；worker 新增 `control_mode=learned_destroy` 分支，复用 `_remove_customers` + 现有 repair callable，候选解用 `penalized_obj`/BIG_M 计分以支持 GIRE，但 best 只允许零违约刷新；learned 模式强制 `block_size=1`，因为每步必须基于最新客户特征重新采样；worker 响应同时给 `customer_features/per_customer_features`。环境口径已硬闸：runner 用 `resetp-ppo-cu124-py312`(torch)，worker 必须是 `resetp-solver-py313` + NumPy2.3.5，否则 `HALT_WORKER_INTEGRITY`；正式判级名固定为 `PASS_LEARNED_DESTROY` / `WEAK_LEARNED_DESTROY` / `HALT_LEARNED_DESTROY`。验证：py313 下 `test_learned_destroy_phaseA.py` + `test_worker_contract.py` = 16 passed/2 skipped；py312+py313 worker 下 `test_learned_destroy_phaseA.py` = 7 passed、`test_async_block_ppo.py` = 33 passed；1-episode/2-eval smoke 仅证 runner 可写报告和 worker integrity 字段，smoke 结论不作算法判级且临时产物已删。
- 🧪 2026-06-29 [x86/DR] **Pilot20 Learned-Destroy Phase A 正式跑数完成，最终判级 `UNDERTRAINED_DEFERRED_RUNTIME`（不进 Phase B，也不当真负结果）**：先合并远端同名实现分叉 `a22a18c7`，保留本地 worker integrity 硬闸和欠训防误杀口径，提交 `5541a2ed`；测试复核 py313 `test_learned_destroy_phaseA.py`+`test_worker_contract.py` = 16 passed/2 skipped，py312 learned tests = 7 passed，旧 `test_async_block_ppo.py` = 33 passed。正式跑 `formal_120`、`formal_240`、`formal_240_b180`，均 worker py313/NumPy2.3.5、actual_evals 用满、learned 零违约；runner 原始 verdict 均 `HALT_LEARNED_DESTROY`（最终档 `formal_240_b180`：learned vs operator-select 平均 -14.493%，min-scale -15.453%，vs random -15.439%）。但按 §4.1 审计训练曲线，三档均无明确学习信号：`formal_120` reward early/mid/late=39.508/39.720/39.158，`formal_240`=39.614/39.724/38.812，最终档=42.728/42.671/42.933（late 仅 +0.480%），entropy 最终还升到 32.680 且峰值 38.698。结论：不是 PASS；也不能写“学到了仍不赢”的真负结果；按本轮上限停止，产物在 `solver/reports/dr_alns_ppo_v3_block_dr_alns/async_pilot/pilot20_learned_destroy_phaseA/`，决策摘要 `pilot20_phaseA_decision.md/json`。
- ✅ 2026-07-02 [Track21 执行进展·修复已落但正式重跑未完成] Codex 按 `21_track21_fix_fleet_policy_bug_and_reclaim.md` 完成 Stage0/1 + Stage2 复活探针：新增 `search_policy_for_bundle/search_policy_for_instance`，保留 `SearchPolicy` 默认 UNBOUNDED 语义；修复 `winner_operators.py`、`candidates.py`、`dynamic.py`、`formal_runner.py`、`alns_crush.py`、`fairness.py`、`e5_probe.py`、`solver/reports/formal/run_w2_parallel_chain.py` 的裸 policy 覆盖；修复 DR worker/learned-destroy/best-of-k 复用 `state.policy`。保护文件 `cost.py/check.py/evaluation.py` 未改。py313+NumPy2.3.5 下 targeted tests `18 passed in 5.76s`。100-01 seed901：300 eval quick probe warm 4079.0301，fixed winner 2596.7756（零违约，+36.338% vs warm），同预算 SA 仍 4079.0301；4000 eval 诊断 fixed winner 2509.1028，SA 3518.6016，winner 对 SA +28.690%，两者零违约 → Track19 4000-eval 卡死/弱结论已被 Track21 修复推翻。已写 `solver/reports/dr_alns_ppo_v3/final_track21/`：policy audit、fix report、winner revival、4000eval revival、superseded rows、DR contamination、interim final_report。**未完成**：Stage3 正式 25/50/100c ≥10 seed 公平比较与最终 WIN_REAL/MARGIN_REAL/HALT 判级；DR 未重训，只标注 Pilot20-25 需 fixed-worker 重评估。
- 🧭 2026-07-03 [x86/Claude 决策·跨机情报整合] **「算法拉不起来」定性=测量仪器连环故障，非算法失败；Track21 判级已被 25c 数据锁死为 MARGIN_REAL → 决定 50c 跑完即收口、不进 100c；x86 回归 DR 本职（Pilot26 干净复验 learned-destroy）。**
  - **Track21 当前数**（25c 10 seeds 全合法 / 50c 6 seeds，runner 存活）：主方法 vs SA +13.6/+15.0%；vs 健康基线 25c 仅 6.45(IWD)~8.27(GA)%、50c 4.09(IWD)~25.6(GA)% → WIN_REAL(每基线≥10%)数学上已不可能；剩余 100c 按 4200s/行帽预计 2-3 天、改不了判级=无用功。
  - **跨机铁证（读 origin/codex/reporting-pipeline，M1 线 07-01~03）**：M1 已数学证明并修复基线解码器构造级 bug——`_append_customer_to_cached_plan` 用**绝对路线距离**比较「追加 vs 新开」，由三角不等式追加永不可能赢 → GA/PSO/VNS/ACO/GWO/IWD 排列解码通道恒产出一客一车解、锁死在 ~4 倍成本盆地（探针 2686+1399 候选 route_count 全=150；M1 修复 commit `ee1f7bfb` 改边际成本+固定费）；另定位 destroy/repair 桥 200/200 `REPAIR_NONE` 全死（修复归 candidates.py 桥侧，未完）；「5174 平台」判 ARTIFICIAL_HOMOGENIZATION；4878→2677 锚差 45% 归因 Q=1600→3650 参数演化非漂移；主 ALNS 已剥离 N-Wouda 成独立后端（parity 过）。**已核 dr-x86 同函数仍是旧绝对距离比较=解码器修复未移植** → Track21 的 4-8% 是「对残障基线」的上界，不能当论文数字。且 M1 `79863dde`(07-01) 与 x86 `34c9d429`(07-02) **各自独立修了同一个车队上限 bug**——两机重复劳动，代码已分叉，需尽早 reconcile（M1 canonical）。
  - **仪器故障清单与波及面（半个多月"失败"的总解释）**：①winner 车队 policy 漏传（06-30 发现、07-02 修）→污染 Track17/19 全部、Pilot16/17(+0.57%)、Pilot18 headroom=0、Pilot19 SP NO_ROOM、Pilot20-25 learned-destroy 负结论（Track21 Stage4 官方确认污染）；②基线解码器 bug（M1 数学证明）→污染两机所有含 GA/PSO/GWO/IWD/ACO 的对比；③桥 REPAIR_NONE→基线原生 LNS 通道死。**唯一仍站得住的干净负结论=「选算子范式≈调好的 ALNS」**（Pilot07-10 在 06-26 车队收紧前 + Cao 源码 ±2% 独立佐证）。**DR-ALNS(learned-destroy) 的真实上限至今从未被干净测过**——Pilot22 在坏仪器上尚见 held-out +11.5% 峰值。
  - **决策**：①Track21 等 50c 10 seeds 跑完（预计 07-03 晨 ~05:00）优雅收口，判 `MARGIN_REAL_PROVISIONAL_X86`，100c 标 `NOT_RUN_SUPERSEDED`（正式 100c 公平表归 M1 修完解码器+桥后做）；②x86 下一步=Pilot26：Track21 修复后 worker 上复验 learned-destroy（Pilot22 稳定化配方+Pilot24 数据设计+仪器健康前置闸），给「DR-ALNS 配不配当未来主算法」第一个干净答案；③10% 头条按 06-28 既定战略走「富问题主擂台+各自构造初始解+小规模精确解锚」三条腿，不在「共享 warm start+基线借我们修复机器+松容量静态小算例」的压差擂台上硬凑。提示词 `docs/handoff/codex_prompts/22_track21_wrapup_and_pilot26_clean_learned_destroy.md`（待发 Codex）。（07-03 补：应 user 要求升级为 **Track22 一次性终局版**并覆盖同名文件——收口+仪器闸+破坏杠杆干净重测（旧 6.26% 也是坏仪器量的）+learned-destroy 训练判级+碳感知时刻杠杆探针（默认场景无杠杆则判"场景问题非算法问题"）+总判级 DR_COMPETITIVE_CANDIDATE/DR_PARTIAL/DR_CLEAN_NEGATIVE，全分支预判自动串行；user 纪律：提示词以对话粘贴为准、一次性大工程、有证据即放手不请示。）
- ⚖️ 2026-07-03 [x86/Claude 把关] **Track21 收口认账；Track22 `DR_CLEAN_NEGATIVE` 判级驳回（第一手取证，三个硬伤）→ 发 Track22-R 纠正重跑。** Track21（commit `0bddc7ed`）：25c+50c 140 行合法、六基线 HEALTHY、Wilcoxon 全显著、最小领先 IWD 5.515% → `MARGIN_REAL` 成立、每基线≥10% 不成立；100c seed901 健康(2500.5 vs warm 4079)、seed902 eval_ratio 0.714 触发预设闸 → `HALT_100C_STILL_STARVED` 老实，认。Track22 驳回证据：①Stage2 探针每 arm 仅 **80 eval/2-5 秒**（预算给的是 ≤3h），且等 eval 记账使 best_of_k 只走 10 步 vs operator_select 80 步——**上界探针量出负数(-1.405%)是设计假象非无杠杆**（对照 Pilot21 同类探针 +6.26%）；②Stage3 并非被闸门跳过——`track22_runner.err.log` 05:31 有完整 traceback：learned-destroy 首次真跑时 **worker 静默崩溃无响应**（50c、移除 20 客户的 block_step，stderr 空）=修复后仪器在关键路径仍有未捕获崩溃 bug，随后 05:33 重跑才写成"无杠杆跳过"；③Stage4 仅 3 解（全 25c 同 seed），其碳成本占总成本仅 0.1-1.5%（<0.5% 闸门在这批解上数学不可能过=量的是场景碳份额非机制），且 aware 比 naive 碳排更多/成本更差（候选集没含 naive=实现 bug），EV-heavy 诊断构造报错(HALT_M0 cv=5/0,ev=0/999)没跑。**新纪律（第三次同类翻车后立）：任何判级行必须先过预算完整性闸（最低 eval/墙钟下限硬 assert），不足只能标 UNDERPOWERED 不得计入判级**——同类前科：Pilot25 假+27.8%(120 eval)、Track19 100c 饿死。Track22-R 提示词已粘对话（要点：R1 修 worker 崩溃+回归测试；R2 双口径杠杆探针[等步数=oracle 主判据/等 eval=部署参考]+真预算+Pilot21 同设置锚定复现；R3 原设计训练；R4 碳探针先算占比天花板+aware 候选集强制含 naive+修 EV-heavy 构造+25/50c 各≥5 解；R5 终局报告含与被驳回版差异对照）。
- 🧭 2026-07-03 [x86/Claude 复盘 Track22-R HALT + DR 立身战略重构] Track22-R 判 `HALT_R0_STAGE2_WALL_UNSTABLE_WITHOUT_VERDICT`——**根因是 Claude 的 R0 规格错误**：闸门写成「eval 下限**且**墙钟≥60s」，被卡行实为 4000 eval（下限 2 倍）/墙钟 50.95s，不是饿死是机器快；防饿死语义应为「eval 达标**或**墙钟达标」。R1 成果真实：原崩溃未复现，但修了更根本的「worker 裸死 stderr 空」仪器盲区（crash log+PYTHONFAULTHANDLER，28 测试过）。**Claude 预读 9 行干净 equal-steps 行（不作判级）**：25c-4000 步饱和点上 best-of-k oracle（8×eval）仅 +0.0~0.6% ——饱和点量杠杆机械地≈0（Pilot18 同理），**杠杆探针必须改为预算阶梯/anytime 口径**（杠杆在中短预算与大算例上，不在饱和终值上）；这与 DR-ALNS 文献真实卖点一致（省 eval 到好解=效率，非终值质量）。**应 user「M1 ALNS 不乐观、DR 须救场立得住」重构 DR 立身三支柱**：①质量支柱（已有）=引擎 vs 文献场显著赢（Track21 5.5-20.5%）+ DR 免调参持平专家 ALNS（pilot08/16 曾示，须干净复验）；②效率支柱（本轮主攻）=中短预算杠杆→DR 目标改「更少 eval 到好解」；③**动态支柱（决胜擂台）**=信息成本 43.99% 池子+滚动重规划每阶段预算紧（效率直接变现）+anticipatory 接口（Track20 计划从未执行），这是「领先第二名≥10%」最现实战场且满足 user「动态必须带机制」的既定要求。已发 Track23 立身工程提示词（对话粘贴）：StageA 修 R0 或语义+R2 预算阶梯（25c 250/1000/4000 步、50c 750/3000、100c 1000×2seeds）+Pilot21 锚定；StageB R4 碳探针照 R 版设计；StageC 免调参平价干净复验（pilot08 ckpt 0240 vs default AlphaUCB/BEST_STATIC_META/official winner，3000 eval 降预算标注）；StageD 动态奠基（重测 info_cost on fixed worker+Track20 非学习 anticipatory 钩子+heuristic sanity）；StageE 立身报告=论文可写 claim 清单。
- 🧭 2026-07-04 [x86/Claude 把关 Track23 首跑 → 发 Track23-D2 续跑令] Codex 跑到 Stage C 崩溃后停手（commit `1dbe8674` 建 runner）。**三个干净结论 + 一个低级事故**：①Stage A `NO_DESTROY_LEVERAGE_ANY_BUDGET`——预算阶梯扫全（25c 250/1000/4000、50c 750/3000、100c 1000），0 欠功率，8×eval 的 best-of-k oracle 在**所有预算档**headroom 为负（最好 −0.96%@25c_4000、短档差到 −4.5%）→ **「学破坏」在修好的仪器上、量遍预算轴仍干净否定（第 7 次证实，效率支柱经此路死）**；②Stage B `CARBON_MECHANISM_WEAK`——默认 Goeke80 碳天花板仅 1.93%（挪不出 2%），但 EV-heavy 诊断碳占比跳 14.7%、aware>naive 零违约 → **结论是"默认场景没碳可挪"（场景问题非机制问题），提示论文主场景需高 EV/高碳价**；③Stage C 崩溃 `mat1(1x24) vs mat2(19x128)`——pilot08 ckpt 是 19 维观测、当前 block_env `BLOCK_OBSERVATION_SIZE=24`（候选生成器扩展加 5 维），**纯接口漂移非 DR 判决**；控制器却因此 HALT，把从未跑过的 Stage D 动态一起饿死。**Claude 判断**：别为学破坏难过（干净死了、可写负结果），DR 立身靠**平价泛化（Stage C，零调参追平专家，DR-ALNS 标准贡献）+ 动态预判（Stage D，RL 唯一理论优势战场、正是论文动态+碳机制发光处）**——两者被同一崩溃饿死。10% 头条不押 DR 对强 ALNS（无人做到），仍靠 M1 富问题 vs 弱基线。已发 Track23-D2（对话粘贴）：修复1 控制器解耦（单 Stage 崩溃不 HALT 全链+回归测试）；修复2 Stage C 弃 pilot08、改当前 24 维环境短平价训练≤3h 再评 + pilot16 干净重评旁证；Stage D 全跑（重锚 info_cost/anticipatory 钩子/heuristic sanity）；Stage B 补场景旋钮表；resume 跳过已完成 A/B。
- 🧾 2026-07-03 [x86/Track22 终局完成] **Track21 已停止，Track22 判 `DR_CLEAN_NEGATIVE`，x86 线不再把 learned-destroy/碳时刻 DR 推成主算法候选。** Stage0 收口：Track21 50c 跑满后关闭，25c+50c 共 140 行合法比较，`MARGIN_REAL_PROVISIONAL_X86`；100c 只保留 1 行 partial（winner 2500.518、16000/16000、unique 45、零违约），不作公平结论。Stage1 仪器闸通过：100-01 seed901 300 eval，py313+NumPy2.3.5，winner best 2627.730 vs warm 4079.030，unique 11，updates 10，零违约。Stage2 fresh bundle 破坏杠杆重测 45 行，worker integrity true、zero violations true、non-overlap true；best-of-k destroy 相对 operator-select 的 max-scale headroom **-1.405%**（25c -1.405%、50c -7.563%、overall -4.584%）→ `NO_DESTROY_LEVERAGE_CLEAN`，所以 Stage3 按闸跳过 learned-destroy 训练。Stage4 碳时刻 replay 3 行默认场景零违约，carbon-aware vs naive 平均 **-0.003%** → `NO_CARBON_TIMING_LEVERAGE`；EV-heavy 诊断因车队约束构造失败，只作“该诊断无效”记录，不作算法证据。实现入口 `solver/rl/dr_alns_ppo/track22_endgame.py`；核心证据在 `solver/reports/dr_alns_ppo_v3/final_track22/`，根摘要 `final_report.md`。执行中发现并修正 Track22 runner 自身一个闸门 bug：整数 `0` 被 `or 1` 误判成违约、且 Stage2 非通过状态仍会进入 Stage3；已修成只在 `DESTROY_LEVERAGE_CLEAN/LEVERAGE_MARGINAL` 才训练。
- 🧾 2026-07-03 [x86/Track21 最终补账] **Track21 后续 100c resume 已正式关账为 `HALT_100C_STILL_STARVED`，不再继续跑。** 25c+50c 完整 140 行仍为 `MARGIN_REAL`：六个基线全 `HEALTHY`，主方法 pooled aggregate mean gain 为 GA 20.534%、VNS 6.415%、SA 16.557%、GWO 7.311%、ACO 6.440%、IWD 5.515%；一侧 paired Wilcoxon 全显著，但最小优势只有 IWD 5.515%，所以“每个健康基线 >=10%”不成立。100c seed901 winner 健康（2500.518、16000/16000、unique45、updates44、零违约）且 SA 健康（3054.450），GA/VNS/GWO/ACO/IWD 为 `VALID_BUT_WEAK`；seed902 winner 虽改善到 2561.627、unique71、updates70、零违约，但 4200s 只跑 11430/16000，eval_ratio=0.714375<0.9，按预设闸门 halt。根 `final_report.md` 保持 Track22 终局，Track21 详细收口在 `solver/reports/dr_alns_ppo_v3/final_track21_reclaim/final_report.md`。
- ⛔ 2026-07-03 [x86/Track22-R 纠正重跑停止] **R1 完成，但 R2 被 R0 墙钟闸挡住，最终 `HALT_R0_STAGE2_WALL_UNSTABLE_WITHOUT_VERDICT`，不形成 DR 正负判级。** 仓库卫生已做：根目录 `final_report.md` 移至 `solver/reports/dr_alns_ppo_v3/final_track22/final_report_first_run_REJECTED.md` 并加驳回头。R1：新增 worker faulthandler/per-process crash log/stderr tail，50c q=0.4 移除 20 客户 learned-destroy 回归 + worker 合约 + PhaseA + runner gate 测试 `28 passed in 23.01s`；原 05:31 静默崩溃未在 targeted 50-step 路径稳定复现，因此只确认并修复“stderr 空/无 crash log”的仪器缺陷，不编造 learned-destroy 根因。R2：按 R0 写入预算硬闸后正式重跑，连续三次自修仍未得到稳定合格判级集：①2000-step best_of_k 撞 runner cap `EvalBudget target reached: 12010 >= 12010`；②25c 3000-step 仍有 E-UK25_11 seed1202 operator_select wall 58.829s<60；③25c 4000-step 仍有 E-UK25_12 seed1201 operator_select wall 50.953s<60（actual_evals=4000、eval_ratio=2.0、zero violation、worker py313/NumPy2.3.5，但 wall_ratio=0.849）→ 按“连续 3 次自修失败停写诊断”停止，partial rows 10 行中 9 OK/1 UNDERPOWERED，**不得汇总成 leverage 数字**。R3/R4 未跑，故不能回答 DR-ALNS 配/不配当未来主算法；只能说 Track22-R 被 R0/Stage2 仪器预算闸挡住。证据在 `solver/reports/dr_alns_ppo_v3/final_track22r/final_report.md`、`track22_final_report.json`、`stage2_destroy_leverage_summary.json`、`track22r_destroy_leverage_equal_steps_rows.csv`。
- 2026-07-05 [x86/Track23-D2 resume closeout] Final status `TRACK23_COMPLETE`: DR_PILLAR_QUALITY=站住, DR_PILLAR_EFFICIENCY=没站住, DR_PILLAR_DYNAMIC=没站住
  - Stage status: B `CARBON_MECHANISM_WEAK`; C `NO_TUNING_PARITY_CLEAN`; D `HEURISTIC_FLAT`; E `TRACK23_COMPLETE`.
  - Pillars: QUALITY=站住, EFFICIENCY=没站住, DYNAMIC=没站住.
  - Evidence dir: `solver/reports/dr_alns_ppo_v3/final_track23/`; key files `stage_b_carbon_scenario_knobs.csv`, `stage_c24_no_tuning_parity_rows.csv`, `stage_d_dynamic_summary.json`, `final_report.md`, `track23_final_report.json`.
  - HEAD when appending this run log: `baab206ab085f437c70a872967d424e149415a6c`; no push/rebase/prune.
