# MDA-ILS-VNS 自适应控制门

判定：`STOP_ADAPTIVE_LAYER_KEEP_FOUNDATION`。

行为等价门：`True`；静态检查：
`True`。A组选择的自适应臂为
`adaptive_d20_10`，A组判定：
`False`。

未见确认块结果：0胜、
0平、0负；基础母体与候选
总目标分别为 NA 与
NA；平均BKS gap改善
NA个百分点；吞吐中位比
NA。新BKS数为
0。

本包只判断AILS-II思想改写的自适应控制能否继续增强已通过的强母体。STOP只淘汰该
自适应层，不推翻基础门；PASS也不等于完整28题、China81、阶段二或最终算法完成。
执行错误：`none`。
