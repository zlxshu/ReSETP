# ReMIX HGS 供料桥

判定：`PASS_REMIX_HGS_SUPPLIER_BRIDGE`。

v1 在固定两次迭代时尚未找到可行解。本 v2 不调质量参数，只改为找到第一份可行解
即停，最多五秒，以回答路线桥本身是否成立。

- 0.12.2 与 0.13.4 路线逐条一致：`True`。
- 重建前后整数距离一致：`True`。
- 两端完整可行且独立复算通过：`True`。
- 保护文件未变化：`True`。

这里只证明 HGS 路线能无损进入共同路线库，不证明 HGS 或 ReMIX 的性能。
