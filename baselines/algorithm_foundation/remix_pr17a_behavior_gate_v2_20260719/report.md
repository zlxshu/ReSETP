# ReMIX PR17A 多重混合行为门

判定：`PASS_REMIX_PR17A_BEHAVIOR`。

本包是 v1 的同输入、同种子、同预算审计重放。算法参数与选择规则没有变化；只把
独立复算次数和“三岛进入路线库”的检查改成直接读取真实执行位置。

## 核验结果

- 预算 0 与母算法逐位一致：`True`。
- 预算 0 增强调用为零：`True`。
- 预算 1/2/5 调用数与合同一致：`True`。
- 三个小岛都进入精英库：`True`。
- 上游到路线库的完整谱系存在：`True`。
- 三个正预算的精确路线会审都成功：`True`。
- 至少一次选中两个以上来源的路线：`True`。
- 全部最终解经独立复算完整可行：`True`。
- 三个保护文件运行前后哈希不变：`True`。

## 诚实边界

本门只证明多岛搜索、路线交换、定向拆解重建、邻域精修和精确路线会审能够在
PR17A 上真实传递同一份解，并且预算与可行性账闭合。它不是性能试验，没有刷新
BKS，也不允许声称 ReMIX 胜过任何外部算法。
