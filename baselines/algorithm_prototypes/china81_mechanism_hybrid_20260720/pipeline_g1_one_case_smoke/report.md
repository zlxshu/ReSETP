# China81 三臂 G1：一题最低成本性能冒烟

机器结论：`HOLD_CHINA81_ONE_CASE_NO_STRONG_POSITIVE`。

在 `cn-jjj-25c-01-V2-LOCATIONS`、种子 `1`、每臂同为 `2.0` 秒的
条件下，纯 PyVRP-HGS 母体成本为 `1253.672412633`，项目
ALNS 母体为 `1255.959127584`，分阶段混合体为
`1217.529330672`。含电动车但不看分时电价与碳的
朴素异构母体为 `1217.529330672`。混合体分别低
`2.883%` 和
`3.060%`；相对朴素异构母体
低 `0.000%`。

混合体实际选择了 `1` 条电动车路线并生成
`1` 个非线性补能动作；两个母体在
该题最终均为全燃油车。这表明增益来自“路线生成时同时看见车型容量、
电耗和补能成本”，不是只在最后改标签。朴素异构臂用于区分“只是把
电动车交给 PyVRP”与“分时电价和碳机制进入路线生成”。

边界：这只是一道开发题、一个种子和两秒预算，只够支持继续做预先
选定的三题探针，不是 China81 正式结论。
