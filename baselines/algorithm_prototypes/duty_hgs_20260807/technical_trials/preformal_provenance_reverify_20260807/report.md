# Duty-HGS 正式开跑前出身复验报告

## 结论

三个当前有效技术试跑均在干净提交 `79e8d67e` 上按原条件重新执行。三次运行开始时工作树均为空，记录的原型 Python 源码总哈希均为 `f9ab0833`。

最终单轮试跑的 368 行轨迹与原权威包逐字节相同；多车场、多电动车动作核查的 7854 行动作明细及汇总与原包逐字节相同；真实漏服务修复的轨迹和汇总与原包逐字节相同。由此闭合“当前权威结果由哪份源码产生”的记录缺口。

本轮不比较成本，不决定正式算例、正式预算、正式 Pi0 或真值哨兵用法，也不产生算法优越性证据。

## 如实保留的异常

把四条命令连续启动时，前三条完成，第四条重启点火只创建了空目录，没有留下错误文本、产物或存活进程。当前证据无法判断外部执行为何中断，因此记为未知执行中断。随后使用完全相同的命令单独重跑，重启点火正常完成；该结果另存为独立包。第一次空目录没有冒充实验包，也没有隐去。

## 交付前九条自检

1. 每个 `FACT` 是否都指到了文件行号 / 产物哈希 / 论文页码？——全部比较值在 `comparison.json`，新运行原始包保存在 `replays/`，文件哈希登记在 `artifact_hashes.json`。
2. 有没有把自己的建议或担忧写成“已决”或“状态”？——没有；只记录出身复验判定，不改变用户决定。
3. 改动范围有没有超出任务文本？——没有；只复验三个有效技术包并保存证据。
4. 有没有碰受保护文件？——未碰；三次运行前后哈希一致，值见 `metadata.json` 和各子包。
5. 待决事项是否转成了 2–4 个具体候选并写清代价？——本轮没有新增待用户选择的问题；既有正式实验事项继续未决。
6. 有没有用自造词或内部任务号跟用户说话？——没有。
7. 失败、跳过、超时、异常结果有没有如实保留？——首次组合启动的空目录和无诊断中断已记录；没有将它写成算法失败或删除记录。
8. 四件套齐了吗？——顶层含 `metadata.json`、`raw_runs.csv`、`decision.json`、`artifact_hashes.json`、`report.md`，另含 `comparison.json` 和三个完整重放子包。
9. `HANDOFF.md` 变更日志和 `docs/handoff/memory/` 同步了吗？——将在本任务最终收口、顾问论证完成后同步。
