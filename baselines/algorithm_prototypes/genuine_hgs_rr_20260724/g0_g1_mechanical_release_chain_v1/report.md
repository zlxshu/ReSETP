# Genuine hybrid G0--worker-probe--G1 release chain

Decision: `STOP_G0_REAL_BUNDLE`.

- `protected_e2_v7_release`: PASS / `PASS_E2_V7_MECHANICAL_INTEGRITY_RELEASE` — release passed and protected processes exited
- `g0_real_bundle`: STOP / `HALT_G0_REAL_BUNDLE_WIRING` — returncode=2

Selected G1 workers: `None`.

The chain did not change algorithms, budgets, inputs, evaluators, random seeds, or acceptance gates.
