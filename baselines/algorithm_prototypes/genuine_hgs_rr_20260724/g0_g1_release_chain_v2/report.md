# Genuine hybrid G0--worker-probe--G1 release chain

Decision: `STOP_UPSTREAM_E2_V7_RELEASE`.

- `protected_e2_v7_release`: STOP / `HALT_E2_STAGED_V7_RELEASE_CHAIN` — upstream release did not pass

Selected G1 workers: `None`.

The chain did not change algorithms, budgets, inputs, evaluators, random seeds, or acceptance gates.
