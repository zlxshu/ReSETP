# 已取消：私有性能只认 China81

这两个 donor03 输入在用户追加范围限制之前生成。没有算法在它们上运行，完整搜索评价
次数为 0。用户随后明确要求“公开算例只考虑带 BKS 的原始开源算例，私有算例只考虑
China81”，因此本目录只保留输入生成事实和哈希，不得用于算法性能、调参、论文或
China81 的替代证据。

对应性能运行器已设置为 fail-closed：
`CANCELLED_SCOPE_PRIVATE_PERFORMANCE_CHINA81_ONLY`。
