# 有界双视图档案行为门独立终审

结论：`PASS`。本次终审只读完成，没有修改算法，也没有运行任何强度题。

本结论只允许启动已冻结的旧 20/25/50 客户、seed 1、B=100 一次性 P1 小门；不允许据此启动新鲜 D3、阶段二、正式搜索或宣称算法更强。

## 机制硬门

车型—补能夹具从 `709.5492738220125` 降至 `621.0831141941019`。其中车型—补能处理自身贡献 `-88.25794958105121`，更新 1 次，车型模式由 `[cv, cv, cv]` 变为 `[ev, ev, cv]`。

低碳时刻夹具从 `621.2913242409613` 降至 `621.0831141941019`。车型—补能处理更新为 0，低碳时刻处理自身贡献 `-0.20821004685939926`，实际改动 2 个充电时刻。

跨场责任夹具从 `963.0311701401012` 降至 `828.678306576153`，独立复算同为 `828.678306576153`，误差为 0。非绑定夹具的成本、精确内容哈希、更新次数和动作清单均保持不变。

## 行为、记账与复算

16/16 行行为矩阵闭合。B=0/1/2/5 下，记录开关、档案开关和控制/候选之间的起点、完整候选预算、原始搜索成本、精确解、路线骨架、历史、算子、主随机数状态和选择器随机数状态均按合同一致；机制候选评价始终为 0。

搜索后完整复算账为：B0 控制/候选 `1/1`，B1 `5/9`，B2 `5/13`，B5 `5/16`。父进程复核 8/8 通过；12 个唯一完整方案见证、38 个快照引用和 47 个用途全部按完整内容哈希闭合，没有非有限值。

## 封存与边界

AppleDouble 封存文件先登记瞬时状态 `HASH_CONTAMINATED_APPLEDOUBLE` 和 7 个边车路径，再对唯一正式输出目录执行精确清理。首次清理返回码为 0，清理后余量为 0，原始 CSV 没有重写；最终正式目录及其父目录伴随文件均为 0 个 `._*`。

独立复核确认 7/7 原始展品、17/17 源文件、40/40 输入文件和 3/3 保护文件哈希匹配。正式判定字段为：只允许旧三题 P1；新鲜 D3、强度主张、正式搜索和阶段二全部为禁止。

