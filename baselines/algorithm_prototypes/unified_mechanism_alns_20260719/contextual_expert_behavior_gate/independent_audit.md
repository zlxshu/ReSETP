# 当前病灶驱动精确专家 P0 独立红队审计

- 审计对象提交：`94028699fc3307356b3c08d3ed5cd91e62973acb`
- 审计结论：`PASS_CONTEXT_GATED_EXPERT_BEHAVIOUR`
- 结论边界：只通过行为、职责边界和预算闭合，不构成算法强度结论，不授权阶段二或正式试验。

## 独立复算结果

1. `raw_runs.csv` 的 24 行全部满足合同；6 个 `B=0` 行的起终签名和成本完全相同，没有机制事件或机制收费。
2. 绑定状态共提交 9 个机制完整候选，不绑定状态提交 0 个。每行均满足 `evaluations = candidate_scores = actual_moves = candidate_channel_sum = B = mechanism + generic`。
3. 9 个绑定事件均只增加 1 次完整候选评分，均被接受并改善最好解；专家估算与共同评分误差不超过 `1e-7`。
4. `mechanism_proposal_witnesses.json` 的三份见证均可重建、可行且能独立复算。多车场责任方案成本为 `828.678306576153`，车型补能方案为 `621.2913242409613`，碳时机方案为 `621.0831141941019`；均与专家估算及共同评分一致。
5. 独立差分确认职责边界：多车场专家只改变责任归属和受影响路线次序，车型与充电不变；车型补能专家保持客户路线和责任不变，只改变车型与充电；碳时机专家只改变充电开始时刻。
6. `solution_witnesses.json` 的 7 个唯一签名均可重建、独立成本匹配且可行；24 行的全部起终签名都有见证。
7. 6 个原始展品、13 个来源文件、17 个输入文件和 3 个保护文件的哈希全部匹配；目录无 AppleDouble 旁文件。
8. 源码复核确认 P0 显式关闭终局补全，只调用一次主搜索循环，不重建 AlphaUCB、不重启搜索，也不另开机制随机流。局部计算和参考复算没有混入完整候选预算。

## 对下一门的强制要求

P1 性能短门必须保存完整候选评分频道、参考复算、修复局部账和终局后处理账，并把 `runtime/budgeted_scoring.py`、`search/e3_multitrip_runtime.py` 及终局补全转调列入来源哈希。P1 两臂必须共用起点、预算、价格、终局补全和独立复算器。
