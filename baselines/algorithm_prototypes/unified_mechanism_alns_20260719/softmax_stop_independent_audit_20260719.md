# Softmax 选择器训练停止：独立复核

最终状态：`SCIENTIFIC_STOP_CONFIRMED / EVIDENCE_HYGIENE_REPAIRED`

## 科学复核

独立复算 12 个训练臂，得到：

- 12/12 可行；
- 12/12 满足 `evaluations = candidate_scores = candidate channel = actual moves = selector selections = 100`；
- 12/12 落盘解用 280 kWh 口径重新计算后，成本差为 0，完成解签名一致；
- 线性降温版本 0/4 严格胜，最差倒退 11.877822%，中位改善 -4.897062%；
- 固定温度版本 1/4 严格胜，最差倒退 8.015713%，中位改善 -3.063669%。

因此两个版本都远未达到“至少 3/4 严格胜、任一题倒退不超过 0.25%”的事前门槛。`TRAINING_STOP_SOFTMAX_MECHANISM` 不依赖数值容差或边界判定。

旧的共用随机流临时诊断没有形成独立证据包。它的向好数字没有在本次独立选择器随机流下复现，因此不能把旧信号归功于 Softmax。现有证据只支持“旧信号很可能受到随机轨迹耦合显著影响”，不支持断言“随机流扰动是唯一或主要原因”。

## 治理复核

出分时的冻结提交为 `2890c6eef19068c145f0320075e9ef9338507f29`，评分脚本可由该提交逐位恢复，评分时脚本哈希为：

`72d40ab94ea938aac0497322bba2fb056d806375726195963e07c896dc81f153`

出分后只做了两项治理修改：

1. 打包哈希跳过 macOS `._*` AppleDouble 边车文件；
2. 自动门把实际动作数和选择次数闭合也纳入失败即停检查。

治理后脚本哈希为：

`14841f9e86c41448a1a74e53941018d885d583d6db1cd1422bf379e1d9735176`

上述修改没有改变任何搜索、成本、解、比较或裁决值。结果目录中的 7 个 AppleDouble 边车文件已用 `dot_clean -m` 清理，CSV 换行已统一为 LF；`artifact_hashes.json` 只登记 6 个读者可见证据文件，6/6 重算一致。

## 后续边界

不重跑本门，不继续调 Softmax 温度，不把旧共用随机流数字写成有效收益。下一条算法开发若继续处理算子垄断，必须另立合同、换成熟方法和旧诊断材料，不能把它包装成 Softmax 的补丁续跑。
