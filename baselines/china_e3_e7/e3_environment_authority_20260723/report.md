# E3 formal environment authority

Decision: `PASS_E3_FORMAL_ENVIRONMENT_AUTHORITY`.

The gate requires PyVRP, NumPy and SciPy to resolve inside the formal Python prefix, hashes their installed distribution trees, locks single-thread environment variables, runs `pip check`, and solves a one-variable integer HiGHS smoke model. It performs no ReSETP search.
