# E6-FAIRNESS-V3-20260731 任务卡

目标：只重跑两个指定算例、种子 1--10 的 20 个 JOINT 单元，在不改变搜索语义的
前提下持久化所有通过完整模型评价的候选本体；逐单元与封存 E3 比较最终解哈希、
目标浮点位、完整评价数、分类 trace 和终止原因；全部一致后按 θ=1 精确筛选 F。

输入：`e3_zone_joint_20260731` 的 JOINT 输入、ZONE/JOINT 封存方案与 trace；
`e6_fairness_20260731` 的成员账本定义与 0/20 自然帕累托结论。

允许改的文件：本目录内的新 runner、checker、监控配置和新证据产物。

禁止改的文件：`cost.py`、`check.py`、`search/evaluation.py`、`profit.py`、
`route_pool_sp.py`、`epochal_hgs.py`，以及旧 E3/E6 证据目录中的全部真实文件。

命令：冻结线程环境后，用 pyvrp-hgs-0.12.2 Python 执行
`run_e6_fairness_v3.py all --workers 2`。

PASS：20/20 单元的五项语义指纹全一致；候选事件与本体逐项覆盖；F 是同次 U
候选池内满足参与条件的最低成本唯一解，只有公平候选数为 0 时才回退 I；独立验收
通过；四件套、报告、候选池、选择证书与哈希清单闭合。

停止条件：任一保护哈希漂移，任一单元五项语义指纹不一致，完整候选技术错误，
墙钟安全熔断，候选事件无法与返回本体闭合，成员成本无法与完整目标在 1e-8 元内
闭合，或 F 不满足 θ=1。

完成信号：`done.json`，且必须在独立验收和 `artifact_hashes.json` 之后最后写入。
