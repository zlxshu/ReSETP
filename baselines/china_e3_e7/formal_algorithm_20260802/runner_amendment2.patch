--- run_xa_formal.py.sha60806ca
+++ run_xa_formal.py.shab73f318c
@@
-    with path.open("w", newline="", encoding="utf-8") as handle:
+    temporary = OUT / ".raw_runs.csv.tmp"
+    with temporary.open("w", newline="", encoding="utf-8") as handle:
         writer = csv.DictWriter(handle, fieldnames=RAW_FIELDS, extrasaction="ignore")
         writer.writeheader()
         for row in results:
             writer.writerow({key: row.get(key, "") for key in RAW_FIELDS})
+        handle.flush()
+        os.fsync(handle.fileno())
+    os.replace(temporary, path)
@@
-        if ".experiment-monitor" in relative.parts:
+        if any(part.startswith(".experiment-monitor") for part in relative.parts):
             continue
