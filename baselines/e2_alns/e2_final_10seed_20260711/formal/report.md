# E2十次运行最终收口

判决：`E2_10SEED_PRIMARY_LEAD_SUPPORTED`。

九个自有算例、九个算法、每组十次，共810行；全部4000评价、零违规并完成成本复算。
按九个算例平均成本之和排序，顺序为：staged_hybrid_carbon_aware > GA-VNS > LNS > VNS > GA > GWO > PSO > ACO > IWD。
自有L-main当前报告十次运行的均值、波动、最好值、时间、可行率和配对统计，不套BKS/Gap表。
公开标准算例的BKS、AVG、Gap%补实验留到E2之后，拟使用未经改动的Goeke原始算法。
