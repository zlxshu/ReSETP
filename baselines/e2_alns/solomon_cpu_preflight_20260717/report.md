# Solomon公开基准CPU预检

判定：`FAIL_SOLOMON_CPU_CONTRACT`。Apple M1含4个性能核和4个能效核，定时求解器最多并行4项且每项单线程。

冻结PassMark单线程分数为3674，DIMACS基准为2000，时间换算系数为1.837。原始与标准化墙钟时间均须保留。

本预检不执行路径搜索；正式运行开始时必须重新生成一次新的、不可覆盖的CPU现场记录。
