# Solomon公开基准CPU预检

判定：`PASS_SOLOMON_CPU_CONTRACT`。Apple M1含4个性能核和4个能效核；匹配墙钟时间时并行4项，按评价预算运行或执行非定时审计时可并行8项，每项均为单线程。

冻结PassMark单线程分数为3674，DIMACS基准为2000，时间换算系数为1.837。原始与标准化墙钟时间均须保留。

本预检不执行路径搜索；正式运行开始时必须重新生成一次新的、不可覆盖的CPU现场记录。
