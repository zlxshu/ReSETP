# 机制展示算例十种子运行器事故

首次十种子计算已全部返回到父进程，但父进程在写结果文件时把 Python 布尔值
`False`误写为`false`，触发`NameError`。错误发生在结果汇总字典构造阶段，不在
算法、评价器、算例、种子、预算或胜负计算内。由于该开发运行器没有逐子任务落盘，
十行内存结果未被保存，必须在只修正该拼写后按原预登记设置完整重跑。此次事故不
允许改变任何算法设置或验收门槛。
