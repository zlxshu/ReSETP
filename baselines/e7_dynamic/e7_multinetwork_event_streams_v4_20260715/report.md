# E7 multi-network event-stream freeze

Status: `PASS_E7_MULTINETWORK_STREAM_FREEZE`.

All three networks use the same result-blind event proportions and require existing-order events to remain editable in both frozen responsibility-condition starting plans. No route search was executed while constructing or selecting these streams. Added orders are restricted to donors whose two responsibility labels agree with the depot that can serve the order directly at its trigger.
