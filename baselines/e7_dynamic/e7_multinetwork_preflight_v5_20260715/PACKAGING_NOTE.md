# 预检证据封装说明

运行结束时生成的原始哈希清单包含120个`.tasks/`断点文件，共132项，清单自身的SHA-256为`fea7a392954f315f3664853ce05cdae8108c65a5565bd5d332b7fdde923c8763`，逐项复核为零失败。

`.tasks/`只用于异常中断后的恢复，并受仓库`.gitignore`排除；其完整最终载荷已经无损汇总到`sessions.json`。为使提交后的证据包能在新检出中独立验签，本目录的权威`artifact_hashes.json`在预检完成后仅做封装修正：排除`.tasks/`和AppleDouble文件，保留并哈希`sessions.json`、结果表、判决、元数据、报告、完成标记及本说明。该修正没有改变任何任务载荷、结果行或判决。
