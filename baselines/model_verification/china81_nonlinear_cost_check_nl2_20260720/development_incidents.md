# NL2 开发事件

- 首次扩展回归时误写了一个不存在的动态测试文件名，并用临时容错命令得到“未收集测试”；随后先列出仓库真实测试文件，再明确运行实际动态测试，13 项通过。该空跑不计入验收。
- 静态碳择时迁移时删除了动态模块仍在导入的旧内部函数，导致动态测试收集阶段报 `ImportError`。修复只恢复带明确“仅限 L100、等待 NL3 迁移”说明的兼容函数；实际动态测试 8 项全部通过。非线性动态能力没有因此冒充完成。
- 全量回归得到 835 通过、12 失败、1 跳过；NL1 基线是 830 通过、12 失败、1 跳过，新增 5 项均为 NL2 测试。为排除失败数量偶然对消，用冻结提交 `12940ce4` 的模块和当前同一数据窄跑受影响测试；E5 两项、ALNS 预算一项和 E2 smoke 一项在基线均同样失败。其余失败是保护哈希、环境和旧探针契约漂移。
- 冻结提交临时工作树第一次直接运行时缺少外接盘上未被 Git 物化的大文件，产生若干 `FileNotFoundError`，不作为基线结论。随后改为“冻结模块 + 主仓库同一数据”复跑并删除临时工作树。
- `dot_clean -m .` 在清理本门 AppleDouble 边车时，也删除了仓库历史上被 Git 跟踪的 14 个 `models/**/__pycache__/._*` 文件。状态检查在提交前发现；这些无关文件随即逐路径从冻结提交原样恢复，最终 Git 状态不含其变化。此后不再对仓库根目录运行 `dot_clean`。
- 本门没有运行算法搜索、没有选择 China81 实例、没有改变任何胜负判据。动态多趟仍含旧恒功率公式，明确留给 NL3，不以静态闭合替代动态闭合。
