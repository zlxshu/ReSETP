# NL1 开发事件

- 首次目标测试命令误写了一个不存在的测试文件名，因此没有收集任何测试；更正为仓库内实际文件后重新运行，62 项通过。
- 全量回归在当前工作树得到 830 通过、12 失败、1 跳过。逐项用 NL1 前提交与当前数据复跑控制后，运行时失败均可在基线复现；其余为保护哈希、环境或旧探针契约漂移。该事实不被改写成“全量全绿”，NL1 只按新路径目标回归判门。
- 外接盘生成了 AppleDouble `._*` 边车文件。只删除了可重建元数据边车，没有删除项目数据；证据哈希明确排除这类文件。
- 本门未运行任何搜索、未选择 China81 实例、未改变胜负判据。
- 证据生成器第一次运行时，`git status` 把整个未跟踪证据目录折叠成一个目录名，边界审计因此误报越界并给出 `HALT`；改为逐文件列举且显式允许本门证据目录后重跑。物理指标和判据未改。
