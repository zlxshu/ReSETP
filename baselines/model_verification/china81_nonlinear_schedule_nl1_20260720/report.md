# China81 非线性充电 NL1 接线验证

判定：`PASS_NL1_CURVE_AWARE_MULTITRIP_SCHEDULE`。

同一组两趟 EV 路线在恒功率曲线下需要 1 辆实体车，在 NL90 曲线下需要 2 辆。这证明非线性曲线已经进入排程结果，而不是只留在报告里。

新动作 2 条，起止电量、曲线身份、持续时间和 JSON 往返均闭合；最大非线性持续时间误差 3.97903932026e-13 秒，最大电量误差 0 kWh。恒功率控制最大漂移 3.12638803734e-13 秒。

本门没有运行搜索。成本、碳排、独立检查器和 China81 三套道路矩阵仍未闭合，因此仍不允许宣称 China81 可正式比较算法。
