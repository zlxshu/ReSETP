# 论文 79 个占位逐项台账（2026-08-19 14:40 交接版）

`STATUS_ACTIVE`。本文件是**唯一的占位清单事实源**。
先前按"17 个展品"汇报是**不完整的**——论文正文实际有 **79 个占位**，
其中 17 个是图表展品，7 个是摘要/Abstract 的核心结论句，55 个是正文数字空。

来源：`docs/paper_gci_dmm_vrp_20260804/paper_main_foundation.md` 全文正则抽取。

---

## 一、总览

| 类别 | 总数 | 有数可填 | 部分 | 完全没有 |
|---|---:|---:|---:|---:|
| 甲 展品 | 17 | **8** | 4 | 5 |
| 乙 核心结论句 | 7 | 部分可填 | — | — |
| 丙 正文数字空 | 55 | 约 13 | 约 15 | 约 27 |
| **合计** | **79** | | | |

**三个最大窟窿**：5.3 发车时机整层（10 个占位全空）、
4.3 消融（8 个占位数据存在但设计不可用）、第 7 章结论（12 个占位，依赖前面全部）。

---

## 二、甲类｜17 个展品逐项

| # | 展品 | 章节 | 状态 | 实数／缺口 | 产物路径 |
|---:|---|---|---|---|---|
| 1 | 图1 算法流程 | 3.1 | 不需数据 | 待核框图文字是否仍准确 | `figure_shells_20260813/figure_1_*` |
| 2 | 表1 算例事件流碳日历 | 4.1 | ✅ 完成 | — | `exhibits_20260819/table1_*.csv` |
| 3 | 表2 模型与算法参数 | 4.1 | ⚠️ 部分 | 若干参数标"出处待补"，须回文献补页码 | `exhibits_20260819/table2_*.csv` |
| 4 | 图2 节点分布与路线 | 4.1 | ✅ 完成 | 源解 joint/seed_8 | `exhibits_20260819/figure2_*.csv` |
| 5 | 表3 实体车排班 | 4.1 | ⚠️ 部分 | 8 车 16 趟趟链齐；**缺逐车发车/返回时刻、油耗、直接排放、运营成本** | `exhibits_20260819/table3_*.csv` |
| 6 | 表4 公开算例质量 | 4.2 | ✅ **完成** | **28/28 全接受**，全部满服务、原精度可行 | `solver/reports/public28_20260819/` |
| 7 | 表5 增量式消融 | 4.3 | ⚠️ **有数但设计不可用** | 基准臂 A0/A1 0/3 全失败；时变碳充电属机制不该混入算法链 | `solver/reports/ablation_20260819/table5.csv` |
| 8 | 图3 各臂收敛 | 4.3 | ✅ 数据齐 | 72 行，横轴＝完整评价次数；A0/A1 无曲线如实缺席 | `ablation_20260819/figure3_convergence.csv` |
| 9 | 表6 日均vs时变碳 | 5.1 | ❌ **待重跑** | 60 秒预验已证日历生效（逐时 3183.137 vs 日均 3182.654，差 0.48 元） | 驱动脚本 `/tmp/run_t6.sh` 已验 |
| 10 | 表7 固定排班充电择时 | 5.1 | ✅ **完成** | 可移动 174.73 kWh；排放 −59.08 kg；电费 +26.52 元；**平衡碳价 0.446009 元/kg** | `table7_data_20260819.csv` |
| 11 | 图4 碳强度与充电响应 | 5.1 | ✅ 数据齐 | 24 行逐小时；0 点最脏 0.6433 却是谷电 | `figure4_hourly_data_20260819.csv` |
| 12 | 表8 车队四档 | 5.2 | ⚠️ **2/4 档** | 18/2＝3339.41、13/7＝3372.86，**两档电车动作均为 0**；7/13 与 2/18 因 `unregistered physical vehicles` 失败 | `solver/reports/t8_levels_20260819/` |
| 13 | 表9 动态事件与状态继承 | 5.3 | 🔄 修正中 | — | `solver/reports/dynamic_20260819/` |
| 14 | 图5 触发前后路线 | 5.3 | 🔄 修正中 | — | 同上 |
| 15 | 表10 动态策略比较 | 5.3 | 🔄 修正中 | — | 同上 |
| 16 | 表11 有无分摊 | 6.1 | ✅ **完成** | 省 888.872696（22.663306%）；参与约束 1/2→2/2 | `exhibits_20260819/table11_*.csv` |
| 17 | 表12 分摊规则 | 6.2 | ✅ **完成** | 两方 Shapley＝核仁，极差 0，均在转归集内 | `exhibits_20260819/table12_*.csv` |

---

## 三、乙类｜7 个核心结论句（论文命门）

| # | 占位 | 位置 | 依赖 | 现状 |
|---:|---|---|---|---|
| 18 | 待回填：算法质量结论 | 摘要 | 表4＋表5 | 表4 已齐，表5 待修 |
| 19 | 待回填：场景条件 | 摘要 | 第 5 章三层 | 差动态 |
| 20 | **待回填：充电时刻／车型指派／发车时机** | 摘要 | **三选一** | **前两层已证非零，可填** |
| 21 | to be filled: algorithmic finding | Abstract | 同 18 | 同 |
| 22 | to be filled: operating conditions | Abstract | 同 19 | 同 |
| 23 | to be filled: charging time / vehicle-type / dispatch timing | Abstract | 同 20 | 同 |
| 24 | to be filled: cost and system-wide emission findings | Abstract | 第 5 章 | 差动态 |

`FACT`：第 20/23 号是全文核心，三层全零才失败。
**充电时刻层作用空间 59.471360 kg/日、车型指派层 3 油优/5 电优 —— 两层已证非零，失败条件排除。**

---

## 四、丙类｜55 个正文数字空

| 位置 | 占位编号 | 依赖展品 | 现状 |
|---|---|---|---|
| 4.1 代表解 | 11–15（A–E） | 表3 | 部分 |
| 4.2 公开 | 17–18 | 表4 | **可填** |
| 4.3 消融 | 20–27 | 表5 图3 | **不可用** |
| 5.1 充电时刻 | 30–35 | 表6 表7 图4 | 表7/图4 可填，表6 缺 |
| 5.2 车型指派 | 36–41 | 表8 | 部分 |
| 5.3 发车时机 | 44–53 | 表9 图5 表10 | **全空** |
| 5 章总结 | 54–57 | 三层齐 | 差动态 |
| 6.1 分摊 | 58–62 | 表11 | **可填** |
| 6.2 规则 | 64–67 | 表12 | **可填** |
| 7 结论 | 68–79（12 处） | 全部 | 差最多 |

---

## 五、四个碳价阈值（第 5 章总命题的实数）

| 碳价 元/kg | 事件 | 来源 |
|---:|---|---|
| 0.075 现行 | 两层皆不动，电车一辆不派 | 表8 两成功档充电动作＝0 |
| **0.296271** | 第一个车型翻转 | 逐车 CV/EV 完整可行成本包络 |
| **0.446009** | 充电择时开始划算 | 固定路线精确枚举 |
| 1.705754 / 1.969218 | 第二、三个车型翻转 | 同包络 |

**主导杠杆随碳价升高而转移** —— 这就是「当条件变化时，主要作用层级转移」要填的内容。

---

## 六、今天查出的三处真缺陷（已修或待修）

1. **`time_varying_carbon_charge` 提 5,474 次碳感知动作，实际全执行 ASAP** —— 真接线断裂，**已修**。
2. **`duty_crossover` 接受判定未回写** —— **已修**；但该算子历史上被反复实测为零贡献
   （1461→10→0、1550→1530 被拒→20→0），**不得以"修好了"为名混入机制消融**。
3. **动态"整趟冻结"** —— 当前代码把在途整趟一起冻结，而每触发点有 5–7 辆车在途；
   照原样跑会把表9 要证明的"继承真实执行状态"写成假证据。**已批准修正，施工中。**

## 七、待修的两处

- **表8 两个电车重档失败**：`unregistered physical vehicles: CV_..._3` ——
  油车名额调低后实体车注册表未同步收缩。
- **表8 真端点不可表示**：`endogenous` 参数类硬性要求 Rd≥1 且 Re≥1，
  故"全油车/全电车"跑不了，四档已登记为 18/2、13/7、7/13、2/18（近端点），须在表注写明。

## 八、放弃的

- **冻结 PyVRP 对照臂**：卡在环境守卫 `frozen PyVRP environment must not contain copied kernel`。
  表4 的对比列是"公开最好值及版本"，冻结臂在壳规格里仅为附加参照，**不影响表4 成立**。

## 九、执行纪律（今天两次踩同一个坑后固化）

**启动任何批次前必须先做导入自检，确认源码树稳定。**
2026-08-19 14:24 表6 四单元瞬间失败，原因是动态轮正在改
`dynamic_multitrip_schedule.py`，批次撞上半成品源码
（`ImportError: cannot import name 'instance_with_inherited_virtual_origins'`）。
这与 2026-08-18 夜里毁掉 49 个单元是同一种事故。

**改共享源码的轮次与跑批的轮次不得并行。**

---

## 十、收尾状态（2026-08-19 14:50，用户令"老的尽快收尾，新的暂时别做"）

### 三个后台的最终结局

| 后台 | 结局 | 证据 |
|---|---|---|
| 公开 28 题 | ✅ **完成** 28/28 全接受 | `public28_20260819/driver.log` BATCH END |
| 表8 四档 | ⚠️ **2/4 成功**，数据文件已还原 | `t8_levels_20260819/driver.log` BATCH END restored |
| 动态修正 | ❌ **进程死亡，但代码已改完** | 输出 0 字节；工作区 8 文件 +2311/−157 行 |

### 动态那 2311 行的真实状态（关键，交接必读）

`FACT`：动态轮**死在收尾前**——三臂没跑、报告没写，**但源码改动是完整的**。

**导入自检全过**，且 `instance_with_inherited_virtual_origins` 已存在
——正是它此前缺席导致 2026-08-19 14:24 表6 四单元 `ImportError` 撞挂。
即：**"只冻结已执行前缀、从虚拟起点继承真实车辆状态"这套接线已经做完。**

`FACT`（回归实测，独立解释器 `build/python_envs/setp-independent-hgs`）：
- 全量：**510 passed / 6 failed**（另 6 个文件因环境缺 `pandas`/`bs4` 无法收集，属既有环境问题）。
- **暂存动态改动做基线对照**：同样 3 个文件下 5 failed / 2 passed。
- **故 6 个失败中 5 个是既有红项**（缺 `pyvrp` 模块），
  **动态改动只引入 1 个新失败**：`test_c8_outsourcing_fallback.py`。

`FACT`（该失败的确切原因）：
`AttributeError: 'types.SimpleNamespace' object has no attribute 'source_full_execution_solution'`
（`dynamic_insertion.py:833`）——**测试桩未随新字段更新，不是算法缺陷。**

`DECISION`：**改动保留在工作区未提交**，供用户决定采纳或回滚。
接手者若要继续动态：①补测试桩的 `source_full_execution_solution` 字段；
②跑三臂（每决策点 64.5 秒＝387 秒收敛拐点 ÷ 6 批）；③出表9／图5／表10。

### 受保护文件哈希（收尾核对）

| 文件 | 当前 | 说明 |
|---|---|---|
| `cost.py` | `268147a7ac30` | 2026-08-19 经 Claude 授权做去废码改动，逐位一致已验证 |
| `check.py` | `1cdb6236a662` | **未变** |
| `search/evaluation.py` | `c7215263c39d` | **未变** |

### 本轮未做、留给下一棒的

1. **表6 重跑**（脚本 `/tmp/run_t6.sh` 已验证可用，日历切换实证生效：逐时 3183.137046 vs 日均 3182.654035）。
2. **表8 两个电车重档**（`unregistered physical vehicles`，油车名额调低后实体车注册表未同步收缩）。
3. **表5 消融重设计**（基准臂须自身可行；时变碳充电应移出算法链归第 5 章）。
4. **表3 逐车明细**（时刻/油耗/直接排放/运营成本，靠重新完整评价固定解提取，13 毫秒/次）。
5. **回填用户设计的图表壳**（`figure_shells_20260813/build_figure_shells.py` 是现成脚本，
   四个壳：算法流程、各臂收敛、预算标定、碳强度与充电负荷）。
6. **表4 的 BKS 列**（公开最好值及版本，须回文献补）。
7. **第 7 章结论 12 个占位**（依赖前面全部落地）。

---

## 十一、仓库清理与私有流水线恢复（2026-08-19 15:30）

### 清理结果：75 GB → 34 GB

| 项 | 前 | 后 | 删除 |
|---|---:|---:|---:|
| `baselines/` | 17 G | 103 M | **16.9 G**（E1–E7 全部退役实验线） |
| `solver/reports/` | 5.8 G | 865 M | **5 G**（453 个历史报告目录） |
| `data/ChinaInstances/` | 34 G | 20 G | **14 G**（被 v10/v5 取代的旧矩阵与旧路网图） |
| 编译缓存与元数据 | — | — | **6 G**（2780 个 `__pycache__`） |
| 退役测试 | 16 个文件 | 0 | 测退役件的测试 |

保留：`algorithm_foundation`（公开 28 题算例源）、`china_instances`、
08-18/08-19 的 25 个证据包、全部 `data` 活套件。

### `CORRECTION`：清理误删了不可再生文件

`FACT`：删除 453 个历史报告目录时，连带删掉
`solver/reports/instance_build_only_d996f755bd_20260815/health_witness_routes.csv`
——**当前算例的健康见证文件，私有 runner 启动时必读**
（`run_problem_hgs_private_technical.py:801,1482,1500`）。

`FACT`：`solver/reports/` 被 `.gitignore` 排除，**全部未被 git 跟踪，删除即永久丢失**。

**错误归因（Claude 认）**：用"08-18 之前的报告都是退役产物"一刀切，
**未先检查活代码是否引用**。用户给的判据是"证明不了删掉会垮就删"，
正确做法是**先证明它没被引用再删**，该步被跳过。
与同日上午固化的"跑批前先做导入自检"是同类疏漏。

**该文件删除前，私有流水线是健康的**——当日它跑出：
协同批 28 单元 success=28/failed=0、表8 四档、消融 15 单元、碳价扫描 95 单元。

### 重建路径（用现成代码，未改仓库源码）

原生成链已断两处：`depot_pair_search_28cells_20260815.py` 需要
`PRIOR_REPORT` 下的 `no_eligible_cells.csv` 与 `suite_health_final.csv`（已删）；
产这两个 CSV 的 `rebuild_china81_final_suite_20260815.py` 有守卫
`if OUTPUT.exists() or REPORT.exists(): raise FileExistsError`，而其 OUTPUT（v1 套件）仍在。

`DECISION`（用户授权）：改走**从当日合格解反推见证**。
一次性脚本 `/tmp/rebuild_witness.py`（**未进仓库**），
源解 `synergy_formal_20260819/joint/seed_8/best_solution.json`，
输出 16 行、覆盖 50 个客户、8 辆实体车（与代表解"8 车 16 趟"吻合）。

`FACT`（验证）：私有 runner 30 秒探针 `PROBE_RUN_COMPLETE`、50/50 客户满服务，
**私有流水线已恢复可跑**。

### ⚠️ 边界：恢复后基准已变，两批数字不得混用

`FACT`：重建见证的初始种群指纹为 `3573e4df8c802a86cd0d289c474dae1789d7dd43f19db88711845353154f98c0`，
当日原见证为 `e30592607a1c61dbeca7fb883ff5bf1c67530d204c3f3e6ed45d9f7aeafb8203`，**不一致**。
原因：反推的见证缺逐趟里程（`distance_km` 留空）与 EDF 构造的精确发车/返回时刻
（以班次边界代替）。

**故：当日已产出的私有数字（协同 888.872696、表8 两档、消融、碳价扫描）
与恢复后新跑的任何批次，属于不同起手基准，绝不可混入同一张表。**

`USER DECISION`（2026-08-19）：「今天的值我就没打算放进论文，我只要求跑通，
能正常实现实验目的即可。」——**故指纹不一致不再追究，当日数字仅作口径与结构验证，
不作论文终值。**
