# 醒来后从这里继续 — 2026-08-02 彻查轮接续简报

状态：`DIAGNOSIS_ROUND_1_COMPLETE__AWAITING_USER__NOTHING_CHANGED`
全程只读：未改 `paper_main.tex`、未动任何 `baselines/` 结果目录、未启动任何重跑。
用户在睡觉，醒来后才做决定。**不要替用户决定，也不要开始改稿或重跑。**

## 一、用户当前指令（原话要点）

暂不做任何决定。任务是**无死角彻查**。三层关切：
1. 第五组（非线性充电）、第七组（动态订单）做不出来，不知是业务原因还是顶层设计原因；
2. 其余实验即便效应明显，未必经得起审稿人抽丝剥茧；
3. 论文谋篇布局与实验设计未必准确必要——"看着就是堆砌出来的缝合怪，没有主线故事和主线问题"。
要求：把 Codex 当顾问而非只当手脚，要互相商谈；只查不改。

## 二、已完成的五份诊断（全部在 `docs/handoff/diagnosis_20260802/`）

| 编号 | 主题 | 判定 |
|---|---|---|
| D1 | 主线故事 | 论断"无单一主线"**部分成立** |
| D2 | 同义反复 | 论断**整体不成立**，但两个头条数字降为"情景效应量"而非发现 |
| D3 | 五、七组根因 | **两组均为顶层设计原因**，非业务、非程序缺陷 |
| D4 | 算例来源 | 44 项来源台账，**12 处措辞强于来源** |
| D5 | 敌意审稿人 | 四条拒稿理由，**击败它们所需证据仓库全无，代价全为"需重新搜索"**；结构性风险 `STRUCTURAL_DIRECT_REJECTION_RISK` |

## 三、本代理独立复算证实的三条硬发现（不是转述 Codex）

1. **所谓"历史承包商客户归属"是合成随机标签。** `source_p_sequences.csv` 全文 6 行，
   内容为 `Uniform_Balanced`/`Uniform_Unbalanced` 均匀随机标签；处理臂改派客户
   均值 **121.58/175 = 69.5%**，基准臂恒为 0（按 `cross_contractor_customer_count` 120 行复算）。
   → 36.477% 度量的是"把人为随机打散的归属理顺"，不是历史约束解除的经验效应。
2. **碳主结果 28/30 个配对路线与车型完全没变。** 按 `raw_runs.csv` 自带的
   `path_differs_from_cost_only`/`types_differ_from_cost_only` 复算：
   `COST_PLUS_CARBON` 仅 2/30 变，`PURE_CARBON` 19/30 变。
   → 2.679% 在 93% 配对里就是固定路线上的充电时刻重排，与被降为"补充"的实验测同一件事。
3. **论文第 1519 行自陈**两个头条数字来自同类四车场协同结构。

## 四、D1 发现的一处叙事错置（最值得先看）

引言最可识别的命题是纯充电择时；**唯一直接检验它的是 E4 的"补充"实验，其系统减排为 9.746%；
而结语只回收了联合优化的 2.679%，没有回收 9.746%。**
即：真正回答引言问题的数字被放在补充位置且未进结论，进结论的是不回答该问题的数字。
另两处错配：动态订单与滚动状态继承写进创新点和摘要但**无任何正式实验支撑**；
实体车多趟写进创新点但**从未在任何结果中出现**。

## 五、D2 对本代理的两处纠正（必须吸收，不得忽略）

1. 两臂**是对称的**：同车队上限、同 Solomon I1 初始解、同种子、同预算、同评价器，
   只有 `hard_home_depot_lock` 不同。问题在基准的**来源**（合成），不在两臂不公平。
2. 同类文献**并未**把"放松约束型增益"统一排除出研究发现，效应幅度常写进结论。
   因此不能说"同义反复所以不能报"，准确说法是**降为情景效应量**。
   D2 另指出：E6 的单干成本之和**不是全局可执行方案**（公共充电容量冲突），
   因此不存在严格可行域包含关系；E6 的**分配稳定性仍是独立发现**。

## 六、待办：下一轮该做什么

1. ~~与 D3 对质一处~~ **已完成**：D3B 已把 `third_occurrence` 由 false 改判 **true**。
   1.48% 在 `baselines/china_e3_e7/e3_zone_joint_20260731` 有实据，与池化探针的 −0.038% 是两个不同杠杆
   （前者只解客户硬锁、车队上限不变；后者再撤逐车场车辆数约束）。
   三次同型失误确认：非线性充电、动态订单、客户归属重分。
   D3B 亦独立确认该组"测的是修复一次人为随机打散，不是解除经验意义的历史约束"。
   **对质方法教训**：Codex 读不到 Claude 私有记忆目录，对质须把原文贴进提示词，
   且必须写明"允许维持原判"。
   （原文如下，保留备查）**与 D3 对质一处**：D3 判 `third_occurrence=false`，部分理由是找不到
   `docs/handoff/memory/e3-lever-is-fleet-pooling.md`——该文件在 Claude 个人记忆目录，
   Codex 读不到，是**派活方指错路径**。但 D3 另给了独立反证
   （`e3_pooling_probe_20260731/probe_results.json` 显示 POOLED 相对 JOINT 仅 −0.038%/0%/−0.036%；
   陈雨蝶等 2025 p.16 独立→分区已降 31.18%、分区→联合仅追加 3.44%，与王勇等方向冲突）。
   **须把个人记忆原文贴进后续提示词重新对质，不得因路径错误整条推翻其结论。**
2. **尚未交给用户的三项决定**（上一轮已提交，未获答复，后台通知不算答复）：
   算法三视角分解砍留、算例种子网格是否按文献缩减、动态订单改用哪种替代口径。
3. **本轮新增、待用户裁决的更上位问题**：主线问题到底定成什么。
   D1 给的候选是"历史责任边界解除后，协同价值如何产生并被稳定分配"，
   但它远离现有题名与摘要领衔的时变碳强度与动态需求。

## 七、纪律提醒

- Claude 只思考/判断/写提示词/写文档；Codex 执行。提示词**直接派，不贴进对话**（除非用户要）。
- 与用户对话**不得出现任何方案代号或状态标签**，实验按内容叫名字。
- 一任务一看门狗；看门狗只判进程死活、日志停滞、产物有无，**不塞科学门槛**；
  多任务看门狗启动前必须做交付路径两两互异自检（bash 3.2 无关联数组）。
- 额度看门狗 `tools/claude_quota_watchdog`（pid 47008，绑定本会话）在岗，断线由其在额度恢复后唤醒。
