# ReSETP 施工总清单（2026-08-02）

用途：把散在 HANDOFF 几十条记录里的决定与待办，收成一份可执行清单。
状态标记：`[ ]` 未开始 `[~]` 进行中 `[x]` 已完成 `[!]` 卡住等拍板

---

## A. 已经拍板、不需再问的（共 9 条）

- [x] A1 主线故事＝双碳（时变电网碳强度）× 混合车队 × 动态需求
- [x] A2 协同、公平分账、非线性充电降为第 2 章模型组件，引文献，不做实验
- [x] A3 敏感性立为独立一章，三根柱子各一节
- [x] A4 新跑车队电动化配置
- [x] A5 动态需求必须产出正式结果，不降为展望
- [x] A6 中心论点＝碳信号一直起作用，着力点在「充电时刻→车型指派→发车时机」三层间转移
- [x] A7 目录六章定稿
- [x] A8 车队规模自有推导 + 五档比例（0/25/50/75/100%）
- [x] A9 等待损失人民币系数确认不可得，不做人民币经营实验

## B. 唯一未决（1 条）

- [x] **B1 多趟开关：用户已拍板「打开」（2026-08-02）**。原文如下备查：
  《不能信任就修到能够信任再开始新实验改造；没有清楚就彻底查清楚后修改在新实验改造；打开；执行。》
  连带后果：车队规模与五档设计须在多趟语义下重算（Y2 进行中），
  且 E4 的 90 份、E6 的 900 份旧解均在多趟关闭下产生，去留由 Y2 判定。
  ~~原待决内容~~ **B1 多趟开关怎么办**：`SETP_E3_STRICT_MULTITRIP` 默认 0（关），论文却写成一直开。
  选项一＝打开重跑（多趟描述成立，但车队档位设计要跟着重算）；
  选项二＝论文改口只写单趟（现有推导与五档立刻可用，删掉「实体车多趟」卖点）。
  **此条不定，后续 270 次求解不能开跑。**

## C. 施工前必须先清（阻塞项，共 4 条）

- [ ] C1 关闭 `X2-PD-003`：把多趟语义从环境变量提升为显式模型配置，写入 metadata，主线入口 fail-closed
- [ ] C2 关闭 `X2-PD-001`：取消 China 入口对 `DEFAULT_PRICES`（英国 Goeke 参数）的隐式依赖，改为必须显式传场景价格
- [ ] C3 关闭 `X2-PD-002`：`carbon_slot_index` 的 18 槽默认值改为必须显式传参（当前无调用点漏传，属埋雷不属现错）
- [ ] C4 关闭 `X2-RI-003`：把 `_curve_aware_action` 提到中性共享模块，解开「正式包反向依赖旧目录」

## D. 风险处置（19 项，全部只改文字或从已有数据重算，0 项需重跑）

- [ ] D1 12 处算例来源措辞降到与来源相符
- [ ] D2 服务费 0.40 元/度 → 改措辞为政策锚定情景，去掉「九城统一观测」
- [ ] D3 迎风面积与风阻 → 改措辞为统一气动情景
- [ ] D4 EV 初始电量 0 → 补目标期刊同类中文论文出处，限定为建模初态
- [ ] D5 NL90 充电曲线 → 改措辞为预设情景
- [ ] D6 公开算例参考值标签 → 全文改为「Vidal 等 2013 年发表参考值」
- [ ] D7 公开算例表补两列：逐实例 MV 原始最好目标值、逐题 10 次平均实际 CPU 分钟

## E. 要跑的实验（4 项，约 270 次求解，规模均有目标期刊页码依据）

- [ ] E1 五算法当前车型同协议比较：1 实例 × 5 算法 × 10 种子＝50 次
- [ ] E2 一因素组件消融：完整臂／去多视角臂／去限时MIP路线池臂，3 × 10＝30 次
- [ ] E3 车队电动化五档：1 实例 × 5 档 × 10 种子＝50 次（原估 70，档位由 7 改 5）
- [ ] E4 动态触发正式效果：1 动态实例 × 3 策略 × 10 种子＝30 次；含事件/状态表与代表路线图
- 备注：5.1 时变碳强度所需数据**全部现成，不需重跑**

## F. 写作（按 W2/X3 的五栏对照施工）

- [ ] F1 引言改 8 段，文献按三层组织，末段提 P1/P2/P3 三命题且不预告方向
- [ ] F2 第 2 章四小节；配角在 2.4 引文献落位
- [ ] F3 第 3 章；补「决策没变 ≠ 搜索器没找到」的论证
- [ ] F4 4.1 算例参数与代表解（现有九趟九车表删除重做）
- [ ] F5 4.2 两表（含新增两列、参考值标签更正）
- [ ] F6 4.3 消融两表一图；旧五臂阶梯与性能剖面移附录
- [ ] F7 4→5 承接句（X3-12 已给定稿）
- [ ] F8 5.1 五段两表一图 / 5.2 两段一表 / 5.3 五段两表一图
- [ ] F9 结论按引言三问回收，不罗列数字；局限与展望单独一段
- [ ] F10 文献比较表列重定（现有「实体车多趟」「成员收益条件」两列已非主线）

## G. 历史遗留未竟部分（X2 自陈 PARTIAL，需第二轮）

- [ ] G1 103 个 Python 文件逐行通读 + 全量公开符号调用图
- [ ] G2 `baselines/` 全部二三级实验包的 metadata 引用图
- [ ] G3 81 算例逐文件哈希重算
- [ ] G4 `paper_v2` 87 个图/TeX/PDF 资产与正式产物双向映射
- [ ] G5 `docs/handoff` 约 660 份文件的 superseded_by 覆盖关系图
- [ ] G6 `baselines/china_instances` 59 个新旧脚本按四条 authority 建白名单并归档其余

---

## H. 用户 2026-08-02 追加指令下的执行顺序（已生效）

用户原话四条：不能信任就修到能够信任再开始新实验；没有清楚就彻底查清楚后修改再新实验；打开；执行。
故顺序**改为先查清、再修、再跑**，不得并行抢跑：

- [~] H1 `Y1` 历史遗留彻查第二轮（穷尽五件事：103 个 py 逐行通读+调用图、
  `baselines/` 全量引用图、81 算例哈希重算、论文资产双向映射、660 份文档覆盖 DAG）
- [~] H2 `Y2` 多趟语义下车队规模与五档重认证（含"100% 电动化在多趟下变好还是变差"、
  旧 E4/E6 结果能否作为多趟数=1 的特例）
- [ ] H3 待 Y1/Y2 回来后，一次性修复全部阻塞项与新发现项（含把多趟从环境变量升为显式配置且默认开启）
- [ ] H4 修复后做一次全链回归，证明"修到能够信任"
- [ ] H5 才开跑 E 段四项实验

## 依赖关系

```
B1 已拍板（多趟＝打开）
  └→ H1 Y1 彻查 + H2 Y2 车队重算（并行，只读）
        └→ H3 一次性修复（C1..C4 + Y1 新发现项 + 多趟升为默认开启的显式配置）
              └→ H4 全链回归，证明可信
                    └→ H5 E1..E4 跑实验（约 270 次求解，规模待按多趟重估）
                          └→ F1..F10 写作
D1..D7（风险措辞）可并行，不阻塞
```

---

## I. 原七个实验的逐一去处（2026-08-02 补记，此前散落未成表）

| 原实验 | 原内容 | 新论文去处 | 是否重跑 |
|---|---|---|---|
| E1 | 混合车队结构验证：仅油/仅电/混合三列对照＋路线图（已完成，原定只重生成表图） | **并入 5.2 车队电动化配置**——E1 本就是三档粗版，现扩为 0/25/50/75/100% 五档 | 并入 5.2 一起跑 |
| E2 | 公开算例＋China81 五算法比较 | **4.2 公开算例求解质量**＋**4.3 一因素组件消融** | 公开算例部分现成可用；China81 五算法必须重跑（旧车型＋非等预算），50 次＋消融 30 次 |
| E3 | 历史客户归属重分 36.477% | **撤下**。跨场服务与责任归属作为模型组件写入 2.4，引文献 | 不跑 |
| E4 | 路线—车型—充电联合优化 2.679%＋固定路线择时（11340 对，系统排放 −9.746%） | **5.1 时变碳强度**：固定路线择时为主体（数据现成）；联合优化在新车队档位下重做 | 部分现成，其余随 5.2 一起 |
| E5 | 非线性充电（零效应） | **撤下实验**。非线性充电曲线作为模型组件写入 2.3，引文献 | **不跑** |
| E6 | 四承包商合作分账 33.018% | **撤下**。成员结算参与作为模型组件写入 2.4，引文献 | 不跑 |
| E7 | 动态订单（无正式结果） | **5.3 发车时机层**，必须产出正式结果 | 必须跑，30 次 |

`DECISION——E5 的处置口径（回答用户"失败的第5实验怎么办"）`：
**E5 不是"失败需要救"，是"本来就不该作为独立实验存在"**。
用户已明确非线性充电属于"前人早就研究透、直接拿来用"的模型组件。
故正确处置是写入第 2 章、引文献、不做实验——
**那个零效应结论随之不再需要，因为不再有实验声称要证明它**。
X1 已判定该曲线参数（SOC 断点 0/0.9/1.0、相对功率 1.0/0.5）无厂家标定，
改措辞为预设情景即可，不影响模型完整性。

`DECISION——E7 的处置口径（回答用户"失败的第7实验怎么办"）`：
**E7 是三根柱子之一，必须做**。它此前无结果的根因是选了一条需要
"等待损失人民币系数"的经济发车路线，而该系数经 15 条候选取证确认不可得。
新做法按姜广田等（2024）pp.2374-2377 的结构：
事件/状态表 → 代表路线调整双面板图 → 策略比较表；
报告量为**事件后剩余充电窗口、可用油电运力、服务量、路线/车型/充电改变**，
**不编造任何货币化等待成本**。规模 1 动态实例 × 3 策略 × 10 种子＝30 次。

`INFERENCE`：E3 与 E6 的既有数据不作废，只是不进正文结果段；
是否作为附录或模型组件的实现见证，待写作阶段决定，不影响当前施工顺序。

---

## J. 大审核之后必须回到的主线（用户 2026-08-03："弄完这些，我们再回来继续调试实验，别跑题"）

**审核不是目的，是为了让重跑不白跑。审核一结束立即回到下面五组，不得再开新审核分支。**

### J0 前置（已在跑）
- [~] 选定贯通全文的唯一代表算例（`docs/handoff/representative_instance_selection_20260803/`）。
  用户 08-03 定：**一个算例贯通全文，其余仅作稳定性校验**。

### J1 五组实验，全部在**同一个选定算例**、**新合同**、**项目自有预算**下重跑
新合同：多趟开启／固定成本按实体车计费 c_fix=170／车场充电不设并发上限／fleet authority v3。
预算：**每视角 25000 次迭代**（项目自有标定 `V7_FIXED_25000_ITERATIONS_PER_VIEW`），
**不再套用陈婉茹 p.3328 的 2000 迭代／150 无改善**——该数属对方求解器的内部标定，已证水土不服。
重复次数沿用 10 次（属科学标准，来自文献）。

- [ ] J1-a **4.2 五算法同协议比较**——五臂必须同批、同机、同停止规则、同预算。
      已作废的一轮：`formal_algorithm_20260802/`（`cn-prd-100c-01`，五臂全部 3621.569908）。
- [ ] J1-b **4.3 一因素消融**（完整／去多视角／去限时MIP路线池）。
      已作废两轮：100c 与 `formal_ablation_200c_20260803/`（三臂全部 7820.181472，
      `NO_MEASURABLE_DIFFERENCE_AT_CHINA81_MAX_SCALE`）。
      判别力先验：旧批次 25000 预算下 405 单元 `M→MV 改进 114／退化 0`。
- [ ] J1-c **5.1 时变碳强度**——`carbon_timing_rescore_20260802/` 已判
      `HALT_XD_CURRENT_CONTRACT_REQUIRES_SEARCH`，**零搜索重打分做不出来，必须重新搜索**。
- [ ] J1-d **5.2 车队电动化五档**（0/25/50/75/100%，总量固定）。
      已作废一轮：`formal_fleet_levels_20260802/` 用了 `cn-cy-100c-01/02/03` 三个算例。
- [ ] J1-e **5.3 动态发车**——三段产出。已作废：`formal_dynamic_dispatch_20260802/` 用 `cn-prd-100c-02`。

### J2 稳定性校验
- [ ] 其余 80 个算例的校验方案与执行（规模按本项目自身判别力与算力定，**不拿文献页码卡自己**）。

### J3 写作
- [ ] 按 W2/X3 的五栏对照施工（F1--F10 见 F 段）。

**红线**：J1 五组必须用**同一个算例**；任何一组换算例都要同步换全部五组。
