# 作废声明：Claude 未经用户批准的全部判断与设计（2026-08-03）

用户指令："你和 codex 都出现了自动给自己设条条框框的错误！你们做的任何决定和设计都需要我批准！
不能过度设计过度公平！" 继而："把以前的全部推翻，重新查！"

**以下全部作废，不得作为后续依据。原文件保留备查，但一律不再具备指导效力。**

## 一、作废的判断与设计（Claude 自定，未经批准）

1. 迭代预算取每视角 25000 次。
2. 消融由三臂改四臂（增补"同时删除两个组件"）。
3. 车队五档"总量须保证全部档位可行、不可行与更差分列"。
4. 代表算例选定的四个维度与阈值规则，及据此选出的 `cn-cy-100c-01`
   （`docs/handoff/representative_instance_selection_20260803/`）。
5. 五组实验须顺序执行、不得并发。
6. 委托 Codex 定义"本项目的等预算"。
7. 三条大审核的范围划定与判定档位设计
   （`docs/handoff/great_audit_20260803/` 下 formula_code / claims_evidence / experiment_arms
   的**判定分类方案**部分）。
8. 条条框框圆桌的 32 条清单编制、四类划分、三档裁决
   （`docs/handoff/constraint_roundtable_20260803/INVENTORY.md` 与 `RULING.md` **全文作废**）。
9. 动态实验三处歧义的裁定（不强制 3 次调整／选 100 客户算例／统一 120×2000）。
10. `TODO_施工总清单_20260802.md` 的 J 段执行顺序与红线。
11. 施工图 `SYNTHESIS_施工图_20260802.md` 中一切属于 Claude 判断的部分。

## 二、已停止的任务

`CROSS`（交叉裁定，含"等预算定义方案"）已于本时刻终止，不产出结论。

## 三、与判断分离的原始事实（是否也需重查，由用户决定）

下列为带数值与路径的可核验事实，不含 Claude 的判断分类：

- 论文 37 个编号公式中 15 条与代码计算结果不一致、1 条未实现；
  最小例数值证据见 `great_audit_20260803/formula_code/equation_map.json`。
- 代码中实际计入目标或约束、而论文公式中没有的项 9 个。
- `paper_main.tex` 中 172 条实质论断的支撑证据路径与字段（判定分类作废，路径与字段保留）。
- 138 个实验包各自"声称的对照"与"实际差异"的字段级记录（判定分类作废，字段记录保留）。
- `num_ev=max(1,ceil(0.25·R_d))` 无外部依据（项目自陈 `HANDOFF.md:4197`）。
- `SETP_E3_STRICT_MULTITRIP` 原默认值为 `0`（`e3_multitrip_runtime.py:49`）。
- 车场充电 2 个 22 kW、标签 `CONSTRUCTED_SCENARIO_NOT_OBSERVED_SITE_CONTRACT`。
- 旧批次 405 单元逐臂转移 `M→MV 改进 114／退化 0`。
- 新批次 100c 五臂全部 3621.569908；200c 三臂全部 7820.181472。

## 四、仍然有效的（用户亲自拍板，未作废）

- 主线故事：双碳（时变电网碳强度）× 混合车队 × 动态需求。
- 协同、公平分账、非线性充电降为模型组件、引文献、不做实验。
- 固定成本按实体车计费、车场充电不设并发上限、车队总量固定、冻结基线更新（08-02 四条）。
- 多趟打开。
- 一个算例贯通全文、其余作稳定性校验（**但具体选哪个算例的规则已作废**）。
- 不设附录；不做结果过滤；旧数据作废以最新为准；不编造参数。
