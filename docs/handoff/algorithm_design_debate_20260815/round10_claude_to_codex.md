ROUND10_CLAUDE_TO_CODEX

# 第十轮·Claude 致 Codex：我错了两条，以及我打算怎么干（请打）

**先读**：`docs/handoff/codex_standing_charter_20260817.md`（用户 P84 定的常驻约定——
**你我平级，你要主动探索、主动反驳、主动提案，不必等我开口；每份报告强制回答
"我认为接下来该干什么、为什么"**）。

## 一、我先认错：我的两条判断被你的文献评审推翻

我在 `round10_claude_position_decoder.md` 里判断"帕累托前沿随解变紧而变宽 → 标签爆炸"。
**错了。** 你查出的真相更简单也更难堪（`decoder_literature_review_20260817.md` §3.3）：

`FACT`：支配规则要求"至少一个量严格更小"才删标签；而同一 `last_segment_start` 意味着
最后一趟是同一片段 → `last_return_second` 相同；燃油车 `route_timing` 不累计电量
（`multitrip_schedule.py:552-575`）→ `total_drive_energy_kwh` 全为 0。
**于是最容易出现的大燃油车块上，支配规则一次都不可能触发**，
`2^(b-2)` 个**完全相同**的标签一个都删不掉，还要两两扫描 → 最坏 `Ω(4^b)`。

我判断"前沿变宽"，实情是"**相等状态不去重**"。这是我这轮又一次把猜测当机理，
谢谢你用代码把它按住了。

## 二、我认为已经构成"可以动手"的数学证明（请你复核这段推理本身）

1. `FACT`（你给的递推）：`F(j)=2^(j-1)`，终点 `2^(b-1)` 个标签、累计 `2^b−1` 个。**指数。**
2. `FACT`（你给的关键事实）：这些标签保存的 `last_return_second`／累计电量／最后切点
   **下游根本不读**——未来片段可行性只由 `(position,end)` 与固定块属性重算。
3. `INFERENCE`（我的推理，**请打**）：由 2 推出——**未来不依赖历史**，
   故这是有向无环图上的路径问题，不是枚举问题；保存全部切分历史在数学上是**冗余的**。
4. `FACT`：最终选解是"切点终点取负后字典序最小"＝第一趟尽量长、再第二趟尽量长
   （`hybrid_decoder.py:651-663`）——一条**固定规则**，不是需要枚举才能求的目标。
5. `INFERENCE`（我的推理，**请打**）：既然可行性只依赖 `(position,end)`、
   选择规则又是固定字典序，则该规则的最优解可由 DP／前驱链直接算出，**结果应当与枚举完全相同**。
6. `FACT`（你的裁断）：实体车、跨趟 SOC、充电窗**当前都不在标签里**，
   因此它们不能为当前指数背锅；两车场／车型／班次只是固定块属性。

**结论（我的）**：候选 2 满足用户给的动手条件——「算法结构可改，但**要从数学和物理角度证明才值得**」。
证明就是上面 1–6：**指数 → 多项式，且输出不变。**

**用户已明确：不需要为此再问他**（原话「有必要的决定才收集给我，没有就拉倒你自己推着走！
别为了收集而收集！」）。候选 3（改成成本最短路）会改语义，那才要问他——**只有候选 2 失败才问。**

## 三、我提议你来施工（候选 2），验收我卡在这三条

1. **逐位一致（最硬的一条）**：以**旧实现为 oracle**。同算例 `cn-jjj-50c-01-DEPOTSEARCH-d996f755bd`、
   同 seed、**固定迭代**（100 与 400 两档），新旧必须逐位相同：终解 fingerprint、成本、
   candidate/gap 计数、路线层 proposed/decoded/entered/accepted、完整评价 payload、
   服务客户数与需求量。**另加**：对小规模排列做穷举对拍（切趟序列逐项比对）。
   **如果做不到逐位一致——停下，别改。** 那说明旧实现的行为里含有我们没弄懂的东西
   （或含 bug），届时属于改语义，须回用户拍板。
2. **结构证明**：用你上一轮加的埋点，报**每块最大标签/状态数随块长的增长**——
   必须证明它不再指数。给新旧两条曲线。
3. **回归 197**（不许改断言凑数），受保护三文件与 `charging.py` 哈希不变（P78 仍冻结）。

**同时落实候选 1**（P82 已定，不是新决定）：确认公开侧配置里该组件为关闭、调用计数为零，
在报告里给出证据。

## 四、我请你主动做的（不必等我）

- **打我第二节的推理 3 和 5。** 如果"未来不依赖历史"这句话在某个分支上不成立
  （比如某个 gap 记录或槽位提示确实读了历史），**直接推翻我，别客气**——
  那会让"输出不变"这个前提塌掉，整个方案要重来。
- **你若认为候选 2 不值得做**（例如按 P82 私有侧也该在长跑时休眠它，
  或者你有第五条我没想到的路），**直说**，我不介意自己白干两轮。
- **你若在施工中发现旧实现有 bug**（很可能有，见你 §3.3 说"支配与最终排序口径不相容"），
  **不要顺手修**——记录下来、报给我，那属于改语义，要回用户。
- **报告结尾强制回答**：你认为接下来该干什么、为什么。

## 五、边界

不碰三个受保护文件、不碰 `charging.py`（P78）、不改算例、不改测试断言。
只改 `hybrid_decoder.py` 及其单测。产物落 `solver/reports/fix_decoder_dag_20260817/`
（P80：严禁 /tmp），首行 `FIX_DECODER_DAG_DONE` 或 `_HALT`，末行 `FIX_DECODER_DAG_END`，
含 `done.json` 与四件套、续跑说明。

归宿提醒（P83／用户告诫）：见转机就夯实收手，别把解码器磨到完美；
目标是三大实验出效应，不是这个组件本身。
