# FIX-DECODER-DAG 派工封面（发给 Codex，按序读三份后施工）

按序完整读：
1. `docs/handoff/codex_standing_charter_20260817.md`（P84：你我平级，强制回答"接下来该干什么"）
2. `docs/handoff/algorithm_design_debate_20260815/round10_claude_to_codex.md`（方案与验收主体）
3. `solver/reports/decoder_structure_probe_20260817/report.md`（你自己的实测，本封面只补要点）

## 探针把现场钉死了（对施工的直接含义）

`FACT`：爆炸由 **3 次尖峰**主导（第 407/441/657 次解码：306.9/436.3/96.4 秒＝总解码 97%），
各含一个长度 21/22/20 的块，创建标签 157,503/186,003/86,775 个，**支配只删 0/0/19 个**。
`FACT`：Claude"后期块普遍变长"假设被推翻——平均块长反而 3.218→2.890；
是**偶发超长同质块**（max 14→20–22）触发 2^b 膨胀。
`INFERENCE`（施工含义）：重写后长度 22 的块应当只需 O(b²)=231 次区间探测＋一条前驱链；
尖峰应当消失。**验收时请单独报告这三次对应迭代的解码耗时。**

`FACT`：你加的结构埋点已验证双向逐位一致（默认关＝改动前；开启＝关闭，差异键为空）。
**保留这个开关**，重写版必须兼容它（字段含义照旧：块长/创建/存活/剪枝/探测/命中）。

## 施工操作细则（补 round10 信件之外的硬要求）

1. **先备份 oracle**：`hybrid_decoder.py` 是 untracked、无 git 历史。动手前把当前文件
   原样复制为 `solver/reports/fix_decoder_dag_20260817/oracle_hybrid_decoder_pre_rewrite.py`
   并记 SHA-256。穷举对拍从这份副本导入旧实现。
2. **逐位 oracle 用已封存运行**（不必重跑"修前"）：
   - 100 迭代：指纹 `01a44c3bd89eb743baea7e5a970fa1fccf799c35d67e8f6218d89bfdc4c5cfdb`、
     成本 3527.2207739172113、路线层 100/100/70/23（`fix_d2_decoder_cache_20260817/`＋
     `decoder_structure_probe_20260817/iter_100_off`）；
   - 800 迭代：指纹 `fc541b…d5970a3`（全串见 `decoder_structure_probe_20260817/report.md`）、
     成本 2896.722268412695（`decode_growth_probe_20260817/iter_800`）。
   重写后同命令重跑 100 与 800 两档，**除 wall 字段外全部记账逐位相同**（探针报告的比对口径）。
3. **穷举对拍**：随机小排列（含构造出的长同质块，块长覆盖到 ≥12）新旧切趟序列逐项相同；
   旧实现从 oracle 副本导入。**任何不一致＝停手回滚，报告差异样例，不得自行裁决语义。**
4. **结构证明**：开埋点报重写后"创建标签/存活标签"随块长的增长表（对照旧版指数），
   并单独报第 407/441/657 次的耗时前后对比。
5. 回归 197 不许动断言；四件套＋done.json＋续跑说明；产物落
   `solver/reports/fix_decoder_dag_20260817/`；首行 `FIX_DECODER_DAG_DONE`／`_HALT`，
   末行 `FIX_DECODER_DAG_END`。
6. 边界照旧：只改 `hybrid_decoder.py` 与其单测；不碰三保护文件与 `charging.py`（P78 冻结）；
   不改算例。发现旧实现 bug（支配与选解口径不相容处）→ **记录不修**，那是语义问题。

## 授权与档位

P81/P85：用户已授权（数学证明在 `decoder_literature_review_20260817.md` §3.2/3.3——
标签历史下游不读＝冗余；指数→多项式且输出不变）。你按 P86 以 ultra 档执行。
按章程：报告结尾**必须**给"我认为接下来该干什么、为什么"；
你若中途判断本方案不该做或有更优路线，**直说并停手**，平级不是客套。
