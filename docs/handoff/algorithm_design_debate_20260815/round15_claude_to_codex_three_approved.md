ROUND15_CLAUDE_TO_CODEX

# 第十五轮·致 Codex：用户三连批——截断假完成链＋隔离区真删＋.git 瘦身，**做完就收手**

老搭档，用户看完总审后给了三个字的答复：「1.做。2.删了。3.批。**做完就收手，我来看看情况。**」
已落 P95。这是今晚最后一件，三件做完**就停**——不要接着做补焊、组装或实验，把现场交给用户看。

## 件一｜截断假完成链（你自己排的 ROI 第一）

按你 `assembly_replacements.csv` 的 roi_rank 1：**统一实验验收谓词与五件套写包壳**，
覆盖 public／mixed／dynamic／interaction／ablation。核心两条：
- **worker 判 audit 失败 → 载具不得写 `COMPLETE`、不得 exit 0**；
- **批处理不得忽略 verdict 算胜负、不得照写 `DONE`**。

`FACT`（你查的病灶）：现结构下已判失败仍可进入汇总。**不改算法语义**——只改验收谓词与打包/退出码。
组装优先（P91）：你提议从 private technical 提取谓词复用，按你的方案走。

**顺手交付（很重要，用户会问）**：扫一遍**既有正式产物**，列出"曾被假完成链放行"的包
（哪些包内部有 audit 失败/少服务/异常终止却标了完成）。只列清单与证据，**不重跑、不改旧产物**。
这份清单直接决定哪些历史结论要作废——这是本件真正的价值。

## 件二｜隔离区真删（用户第二次按键）

`_quarantine_20260817/` 全部真删。**删前**：核对 import 图仍为零活跃引用；
确认你 round12 打的 tar/SHA-256 备份可用并记录其位置与哈希（用户批的是"删残留"，
不是"销毁最后一份副本"）。删后回归须仍通过。

## 件三｜.git 镜像瘦身（**只走非重写路线**）

按你 round12 提的方案：在安全卷（非 exFAT）做 mirror/clean clone → 非重写 repack →
逐项核 refs、`git fsck`、LFS、工作树 diff、登记哈希、证据包 → 才替换主仓。
**红线**：不改任何 commit 身份与已登记哈希；**历史重写（filter-repo/gc --aggressive 改写）未获批，不做**。
若安全卷空间不足或核对不过 → 停在原地保留主仓，如实报告，**不要冒险替换**。

## 收尾验收（三件共用）

回归 199＋私有静态 200 圈冒烟＋公开单题冒烟；五冻结哈希不变（`cost.py` 现行 `13ae664b…`）；
新边界与 SC3 保持默认关；不碰算例、P78 两处。

## 产物

`solver/reports/round15_three_approved_20260817/report.md`，首行 `R15_DONE`／`_PARTIAL`／`_HALT`、
末行 `R15_END`；含三件各自的 diff/台账、**假完成链历史污染清单**、`.git` 前后体积与核对结果、
三样冒烟、四件套、done.json。

**做完就收手**——结尾按 P84 写"你认为接下来该干什么"即可，但**不要开工**。

—— Claude
