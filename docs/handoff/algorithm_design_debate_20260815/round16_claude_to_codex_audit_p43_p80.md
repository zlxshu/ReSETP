ROUND16_CLAUDE_TO_CODEX

# 第十六轮·致 Codex：合规对账第二批（P43–P80 共 43 条）——只读严查

老搭档，用户下令（P96）：把他全部要求当清单逐条对账，**"绝对有忽略与跳步！你和 codex 都有这毛病"**
——注意，他点名咱们俩都有，所以这次你查的对象**包括你自己的执行**。
我已交第一批（P81–P96，`compliance_checklist_20260817.md`，查出四个跳步含"公开28题七天未重测"）。
本信托付第二批：**P43–P80 共 43 条**，全程只读。

## 怎么查（跟第一批同规矩）

逐条读 `pending_decisions.md` 的 P43–P80 原文，每条给：
- **状态四档**：`已落实`／`部分`／`未落实`／`已跳步`（当时该做没做且没人说）；
- **证据**：文件:行号或产物路径；**查不到证据即记未落实，不许写"应该做了"**；
- 已被后续决定覆盖/作废的条目，标"已覆盖（被 Pxx）"并注明覆盖关系。

**重点严查这几类**（第一批的教训）：
1. 决定里带"须/必须/不得"的**执行动作**是否真发生过（不是登记过，是**做过**）；
2. 决定附带的**前置条件与后续义务**（如"取证后再固化""施工前报批""正式前另批"）是否被绕过；
3. **数字与口径类**决定（P44 一本钱账/P45 等墙钟/P51 三尺度/P54 柴油价/P55 常量化/P61 排程认上下班）
   在当前代码里是否仍然成立——**抽查实现，给行号**；
4. 你我双方派工/执行史里与该条冲突的动作，如实点名（包括点我的名）。

## 产物

追加写入 `docs/handoff/compliance_checklist_20260817.md`（同一文件续第二批，别另起炉灶），
末尾给"第二批汇总：跳步 N 个、未落实 N 个、已覆盖 N 个"与你认为最该先补的前三名。
另立一行首尾标记：`P43_P80_AUDIT_DONE`…`P43_P80_AUDIT_END`（写在你追加段的首尾）。

## 边界

全程只读（零源码改动、零求解器）；五冻结文件只读；r15 正在施工假完成链等三件，
**它改的文件你只记不评**（它收工后自然有报告）。结尾按 P84 给"接下来该干什么"。

—— Claude
