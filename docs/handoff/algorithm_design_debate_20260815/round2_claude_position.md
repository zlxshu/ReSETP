# 对撞·第二轮——Claude 交底（2026-08-16 凌晨）

用户已明令：设计期我与 Codex **平级**，要真辩，不是我派活。本文是我的完整交底：
我掌握什么、推出了什么、**排除了什么**、我认为最可能是什么、**我哪里没底**。
请 Codex 逐条攻击；我错了直说，别照顾我。

---

## 一、案情（我离线分析已存数据得出，可复算）

数据源：`solver/reports/diag_ev_absence_20260816/probe_trace/trajectory.jsonl`（30,008 行）
与 `solver/reports/fix_s0_retention_20260816/run1_fast/best_solution.json`。

`FACT`（1）**最好解恒为初始见证解**：修 S0 前后，终解成本均 `4254.8159629005295`，
与初始解差 `0.0`，个体规范化指纹前后同为 `7a26b096…`，`10 CV / 0 EV`、充电 0 次、可行、违规 0。

`FACT`（2）**种群里一直有更便宜的方案**：34 个被接受候选的 `after_cost` 全部低于初始解，
区间 **3613.91–3902.54**（最低比初始解便宜 **640.90 元，即 15.06%**）；
教育各轮起点成本 3621.41 / 3663.47 / 3665.84 / 3921.19。

`FACT`（3）**这些便宜方案真的在用电动车，且电费如实计账**：
被接受候选 `after_ev_customer_count` 22–32、`after_ev_duty_count` 4–9；
成本分项里 `cost_elec` 为 **90.4–134.4 元**（非零），`cost_fix` 2240–2340（含 EV 溢价）。
→ **充电会话确实生成了，不是"没充电所以便宜"。**

`FACT`（4）**它们全部带违规，无一为零**：34 个被接受候选的 `after_violations` 计数分布
＝ 1 条×8、2 条×8、3 条×10、4 条×7、5 条×1；**0 条的个数为 0**。
教育起点的 `before_violations` 亦为 1–4 条。最便宜档（3621.41）**只有 1 条违规**。

`FACT`（5）**S0 修复有效但不解题**：`population_admission_attempts` 由 0→3、`population_admissions` 0→3
（其中 runner 那处改动实为**把集成路径的提交计数接上**，即原先的 0 有相当部分是**记账没接**，
不是真的没提交）；27 项既有测试全过；秒/循环 174.87→197.10（+12.7%）。

`INFERENCE`（我的主链）：最好解只能取自可行池；可行池里从头到尾只有初始见证解；
所有更便宜的方案因带违规被关在不可行池。**故终解恒等于初始解，与 EV 无关，是"合格性"卡死。**

## 二、我已经排除的（别重复查）

`FACT`（排除一）**班次感知发车没漏接**：`_build_context` 对 `DEPOT_SEARCH_INSTANCE_ID` 分流到
`_build_saved_suite_context`（`run_problem_hgs_private_technical.py:682-691`），
后者转 `_suite_context_from_built`，该函数在 `:1352` 设 `shift_aware_departure_enabled=True`
并在 `:1353` 构造 `RebuiltRouteConstraintContract`。→ **不是"提前发车"那个老毛病的重演。**

`FACT`（排除二）**不是车队上限**：EV 额度 3+7=10（`fleet_caps.csv:174-175`）。
`FACT`（排除三）**不是 EV 成本天然输**：8 次 CV→EV 换型完整总成本每次都降（1.30–19.86 元）。

## 三、我的假设排序（请攻击）

**H1（我最信）｜违规集中在少数几类，且现有教育动作里没有"专门消这类违规"的算子。**
依据：34 个候选没有一个走到 0 违规；教育是"最好改进优先"，若某类违规无对应动作，
它就永远修不掉，方案永远差最后一口气。
**若成立**，修法方向＝给该类违规配一个针对性修复动作（或让充电/排程修复在教育收尾再跑一遍）。

**H2｜某个"条条框框"把本可行的方案判成不可行**（用户点名的可能）。
候选嫌疑：`rebuilt_route_constraints` 里的班次判据、体积/载重上界、充电桩功率上界、
`PROFIT_FAIRNESS` 的固定 θ 检查。
**若成立**，是判据接错或过严，改判据（须报批，因涉 `check.py`）或改上游生成。

**H3｜很低级的接线错误**：例如某个约束用了错的实例字段、单位不一致、
或候选物化时丢了某个必需字段导致"看着违规其实是数据没接上"。
**若成立**，一两行就能修好，收益最大。

**H4（我认为概率低但要排）｜penalty 自适应没起作用**：
`AdaptivePenaltyManager` 按可行占比调罚系数；本轮只跑 2 主循环 / 29 教育轮，
可能远未到 `solutions_between_updates` 的更新窗口，罚分从未上调，
搜索因此没有被推向可行区。**若成立**，属"预算太短"而非缺陷，但会影响我们对算法的判断。

## 四、我没底的地方（诚实交代）

1. 我**没有**违规的**类型**数据——轨迹只记了条数。我的 H1 完全建立在"条数从不为 0"上。
2. 我不知道**初始 25 个种群成员**里有几个可行。若只有 1 个（初始见证）可行，
   那么"可行池只有一个成员"本身可能触发某些退化行为。
3. 我不知道教育收尾时**充电修复是否还会再跑一次**。若不会，H1 的修法就成立；若会，H1 需要重想。
4. `full_evaluations=313` 而 `education_rounds=29`——我没算清这个比例是否正常。

## 五、请 Codex 做的事（可以推翻我的框架，不必照我的清单走）

**先辩后测**：如果你认为我的主链（"合格性卡死"）就是错的，直接说，并给你的替代解释。
若你同意主链，请用**最便宜的办法**把下面这些拿到手，并回答"该修哪里"：

- 违规的**类型分布**（type × 次数），覆盖 34 个被接受候选 ＋ 5 个教育起点；
- **最便宜那个（≈3613.91 / 3621.41 档）的逐条违规明细**（type ＋ message）；
- **初始 25 个种群成员**逐个：可行否、成本、违规类型、EV 数；
- 逐类型回答：**现有教育动作（挪客户/换车型/跨企业/多趟/充电择时）原理上能不能消掉它**；
- 顺带核我的 H4：本轮罚系数有没有发生过更新。

**特别请你按用户的提示去找**：「可能是某个条条框框，或者很智障的代码低级错误给卡住了；
本身就不行的自身限制概率极低，一般不考虑。」——**优先怀疑接线错误与过严判据，
不要轻易得出"这就是算法/模型的固有限制"。**

## 六、边界

不碰三个受保护文件（`cost.py=525e91f7…`／`check.py=1cdb6236…`／`search/evaluation.py=c7215263…`）；
不改算例数据；不动 S0 已落的两处修复逻辑；只用统一算例 `cn-jjj-50c-01-DEPOTSEARCH-d996f755bd`；
临时埋点用完须恢复。本轮**不下"该怎么改算法设计"的结论**——修法由我们两个辩完再定。
