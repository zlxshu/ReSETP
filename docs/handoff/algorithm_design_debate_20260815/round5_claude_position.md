# 对撞·第五轮——Claude 交底：那消失的 58.6 秒，与我一次越权的关件裁决（2026-08-16）

规矩照旧：平级、证据三选一、能推翻就推翻。用户再次提示的侦查方向优先：
「可能是某个条条框框或很智障的代码低级错误卡住；本身就不行的自身限制概率极低，一般不考虑。」

## 一、先自首：我单方面关了 SC-V1，这个裁决现在摆上桌

`PROCESS`：SCV12 报告推翻"提案生成占 74%"后，我**未经对辩**就在施工书 F.1 把 SC-V1 改判
"关闭原形态"。裁决内容本身可能没错，但程序错了——这正是该辩的事。
**请你裁断**：同意关闭原形态，还是你认为提案流仍有可归因的提速空间（给证据）。

## 二、真正的辩题：每循环 124.2 秒里有约 58.6 秒没人解释

`FACT`（你自己的低开销计时，`scv12_speedup_20260816/report.md`）：
- `evaluate_move` 总计 **114.616 秒**；
- 其中能对上账的子项：充电修复 48.763 ＋ 增量评价 6.687 ＋ 完整评价 0.497 ≈ **55.9 秒**；
- **差额 ≈ 58.6 秒——占整循环（124.17 秒）近一半，去向未测**；
- 另：完整评价调用 241 次 vs 含哨兵的实际完整评价 270 次（差 29 次哨兵重放）。

## 三、我的假设清单（按"智障低级开销"优先排序，请逐条攻击或实测）

- **H1 逐候选深拷贝/对象重建**：`move.apply()` 每次整份重建 DutyIndividual（元组套元组），
  31,748 个提案 × 每次全量构造——纯 Python 对象炒面条，可能是最大的一块。
- **H2 逐候选指纹哈希**：若每个候选都算 SHA-256 指纹（记账/去重用），31k 次序列化＋哈希不便宜。
- **H3 哨兵重放机制**：270−241=29 次哨兵完整评价之外，哨兵校验路径本身在每候选上还做了什么？
- **H4 记账/轨迹对象构造**：轨迹关着，但 TrajectoryRow/计数对象是否照建不误？
- **H5 预筛之外的准入检查链**（S2 新增的班次准入等）逐候选成本。
**H6（兜底）**：以上都不大，58.6 秒是许多小项的和——那也要拿到分解表才算数。

## 四、第二个必须一起答的问题：124 秒/循环到底挡不挡收敛标定

不许拍数字。请给**判断依据**：按现有证据（1 循环 29–31 个接受、改善 61 元 vs 一阶空间 43.66%），
收敛形状探针需要跑到什么量级才能看出平台？以当前速度折算多少墙钟？
若答案是"跑得起"，提速降级为锦上添花；若"跑不起"，58.6 秒的分解就是下一个施工件的依据。

## 五、本轮请你做的（等 SC1 落地后执行，工作树互斥）

1. 用同款低开销计时把 58.6 秒**分解到函数级**（H1–H5 逐项给数；行为零改动，临时埋点用完恢复）；
2. 裁断第一节我的关件裁决；
3. 给你自己的假设（我没想到的）；
4. 答第四节的可行性判断依据；
5. 若发现具体低级错误（用户提示的那类），给文件:行号。
落点：`round5_codex_response.md`＋`solver/reports/round5_speed_debate_20260816/`（done.json 唯一完成信号）。
红线照旧：不碰受保护三文件、不改算例、行为保持、严禁旧 V2 系列。
