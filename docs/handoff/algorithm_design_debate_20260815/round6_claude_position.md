# 对撞·第六轮——Claude 交底：发动机调校联合设计（2026-08-16）

平级规矩照旧：这是我的候选清单与理由，**你可以推翻、增删、重排**，并给出你自己的方案；
证据三选一（文献页码/代码行号/探针数字）。用户已驳正方向：**赛道固定、题目有限且公开披露，
发动机就该主动往死里调**——不再"等瓶颈"。

## 一、背景约束（本轮为纯设计，零代码改动）

收敛探针正在跑（convergence_shape_20260816，seed 11×1h＋seed 3/7×20min），
**工作树在它结束前不许动**——本轮只出联合设计文档，实施等探针落地后汇入主路。

## 二、我的私有赛道调校清单（按急迫排序，请逐条攻击）

1. **罚分自适应窗口（我认为最急）**：`solutions_between_updates=50` 是大算例默认；
   本算例每循环注册 3–4 解（round2：注册 3 更新 0；round5 探针：注册 4 更新 0）——
   **发动机的自适应罚分从未启动**。我的倾向：窗口应挂钩"每循环注册数×若干循环"，
   具体值由标定批实测后定。风险：窗口太小会让罚系数震荡——你怎么看？文献里 Vidal 系的取法依据是什么？
2. **邻域粒度**（proximity/nbGranular）：默认为大算例设；50 客户＋两班次下，
   跨班次邻居本来就该被 SC2 的班次邻域过滤掉，剩余有效邻居数是多少、粒度设多少合理——须实测。
3. **种群规模** μ=25/λ=40：每代完整评价贵，小算例下"多代少人 vs 少代多人"的权衡未测。
4. **停滞重启节奏**：现口径与"约 2 分钟一循环"的节奏是否匹配未验。
5. **我不确定的**：这些参数彼此耦合（窗口×种群×粒度），逐个调还是设计一个小型标定阵列一次测完？
   你的意见与文献做法？

## 三、公开赛道（按族调校）

A 族窗紧/B 族窗宽、差距随客户数增大（Spearman 0.41）。我的候选：按族分参数档
（粒度/罚分/重启），整族披露。红线＝不许一题一参数。
请你补：模板论文（金东遥/陈婉茹）参数敏感性的具体做法可否直接套为我们的"按族调校"披露格式？

## 四、请你做的

1. 逐条裁断/增删我的清单，给你自己的调校候选（我没想到的，尤其内核层面你比我熟）；
2. 对"逐个调 vs 标定阵列"给出方案与代价；
3. 产出一份**联合调校设计**（每条：调什么/依据/怎么测/验收对照/风险），
   作为探针落地后的实施依据——落点 `round6_codex_response.md`；
4. 本轮零代码改动（探针在跑）；`solver/reports/round6_engine_tuning_20260816/done.json` 为完成信号。
