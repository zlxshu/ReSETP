# 第九轮·补轮任务书（Q9–Q11，效应优先重排）

你已完成第九轮响应（`round9_codex_response.md`）。本补轮是**用户中途新增告诫**引发的，
不推翻你已冻结的独立立场，只追加三问。规矩不重读（你本轮已读并背诵）。

## 触发：用户 2026-08-17 告诫（原话要点）

「算法跑出来是为了服务各个实验与机制，我希望效应'爆表'或产出**极有政策和企业指导意义的故事**。
**这才是最终归宿，就是为了写论文！千万不要为了调试算法而调试算法，把手段当目的！**
我要灵活，能把死的说成活的，有'游击战'思想，不要这个门那个门的。对我和论文负责，不是对条条框框负责。」

Claude 据此自纠，产物：`round9_claude_addendum_effect_first.md`（**先读它**）。
其核心：算法有**两个不同的"够用"门槛**被混成了一个词——
①服务机制实验（八行中七行）＝能稳定产出可信可行解、对参数有反应、语义冻结＝**已基本达到**；
②服务公开对比表（八行中一行）＝搜索强度要赢＝**未达到**。
原单链把七行压在一行后面排队，故重排为效应线（主）＋强度线（副）双线。

注：你在独立立场 §2.4 已独立撞出同一处（P79 若混用会形成"不跑实验不能证明有效、
不定稿不许跑实验"的死循环，建议拆为结构冻结／效果闭合）。本补轮请把两套说法对齐。

## 三问

**Q9｜对齐并给可判定条件**
你的"结构冻结 vs 效果闭合"与 Claude 的"两个门槛"是不是同一件事？用你的定义统一。
给出**结构冻结的可判定条件**：必须能二值判定、必须是"语义不再变"性质，
**不得夹带任何自设质量线**（不许出现"成本要低于 x""改善要超过 y%"）。给代码或产物依据。

**Q10｜最要害，直接决定施工顺序**
Claude 判"四个效应候选不吃算法强度"（午休充电双降 20.7%/50.8%、碳价临界 1.472、
EV 减排摆动 2.7–27.5 倍、混合车队平段 7/10 可转）。逐个核**是否真的不吃**。

关键约束：**P52 已定"机制实验一律重新优化路线，统一评价器算分"**
（`pending_decisions.md` P52）——即每个扫描点都要**重跑一次搜索**，不能拿一个解换算。

请给**算术**：
- 一次完整情景扫描（例如碳价 k 个点 × 种子 s 颗）在**当前速度**下的机时是多少？
  用已存产物的实测速度推（注明来源包与行号），不要估。
- 若机时不可接受 → **提速就不是"只服务公开表一行"，而是效应线的前置**，
  D0/D1 的先后必须调换。**这条判定请给明确结论，不要两可。**
- 另核：扫描是否真的需要重跑搜索，还是存在合法的"同解重评价"口径
  （若存在，给出它与 P52 的关系与出处）。

**Q11｜协同开/关两臂对照**
Claude 判它是"全项目最便宜的空白"（机制被声称活跃、从未做过对照，且不吃强度）。
给最小设计（两臂怎么切、同批次同预算怎么保证、服务量红线怎么报）与机时估算；
复核或推翻 Claude 的"最便宜"判断。

## 边界（硬）

- 只写一个文件：`docs/handoff/algorithm_design_debate_20260815/round9b_codex_response.md`。
  不改其他任何文件（包括两本双方案与 Claude 的两份立场文件）。
- 零求解器、零实验、零代码改动；只读代码与已存产物。不碰三个受保护文件。
- 逐条标 `FACT`/`INFERENCE`/`DECISION`/`UNKNOWN`；机时数字必须注明推算来源。
- 中文，说人话。首行 `ROUND9B_CODEX_RESPONSE`，末行 `ROUND9B_END`。
