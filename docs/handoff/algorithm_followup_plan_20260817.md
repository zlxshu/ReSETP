# 算法后续设计规划（Claude×Codex 对撞收敛稿，2026-08-17）

状态：`DECISION`（两个代理的共同建议），**不是用户决定**。经用户批准后才生效。
来源：第九轮＋补轮对撞（`algorithm_design_debate_20260815/round9*.md`）、
归属分离台账（`decision_attribution_ledger_20260817.md`）、
底层缺陷审计（`lowlevel_defect_audit_20260817.md`，**待并入**）。

> **归属规矩（用户 2026-08-17 令）**：本文每条约束都标 `[USER]` 或 `[AI]`。
> `[USER]` 必须遵守；`[AI]` 可视情况挑战和突破，引用它拦事时必须说明"为什么这次仍应遵守"。

---

## 一、目的（`[USER]`，一句话）

算法的唯一使命是让三大实验（时变碳、混合车队、动态需求）与支撑机制在统一算例上
**出效应、出能讲的故事**；仅公开 benchmark 加私有消融不算完成（P37 终局句）。
**不为调试而调试**（用户 2026-08-17 告诫）。

---

## 二、把一个词拆成三件事（双方独立收敛的最重要成果）

此前"算法定稿"一个词里混了三件互不相同的事，导致七行图表去等一行强度问题：

| 状态 | 它回答什么 | 判定者 | 当前 |
|---|---|---|---|
| **结构冻结** | 以后还会不会因为算法含义改变而使已出结果作废 | 代理（纯语义二值） | **false** |
| **机制效果闭合** | 各机制臂按 P52 各自重搜后，效应有多大 | **用户看图表** | 未开始 |
| **公开强度闭合** | 与公开对手比搜索质量 | 按已定公开协议 | 平手（14:14 / 13:15） |

**关键**：机制效果闭合与公开强度闭合**互不阻塞**。公开赛道赢不赢，不该拦住七行机制图表。

### 结构冻结的六条二值判据（`[AI]`，纯语义，无任何质量线）

S1 组件清单唯一（每项职责、顺序、默认开关、失败语义唯一）｜S2 开关贯通
（requested→effective→consumer 一致，关闭时消费者真关）｜S3 候选生命周期唯一可追踪｜
S4 身份可重建（源码/内核/依赖/算例/评价器/受保护文件哈希齐全）｜S5 变与不变边界唯一
（碳价、溢价、策略臂、种子＝实验旋钮；组件、顺序、修复、失败语义、评价器＝算法身份）｜
S6 语义分支归零。

**明确不属于结构冻结**：成本低于某数、改善超过某百分比、赢多少公开题、至少接受多少次、
种子差小于多少。这些一律不得塞回结构定义。

`FACT`：当前 `structure_frozen=false`，卡在 **S6**——充电候选的语义分支未定（见第五节 Q1）。

---

## 三、主链（双方最终一致）

```
① 结构冻结（闭合 S1–S6，需用户拍一个语义选择）
    → ② 提速（现为全线前置，不再只服务公开表）
    → ③ 收敛观测（落仓库、可续跑）→ 从曲线读出正式预算
    → ④ 效应扫描（碳价／EV 溢价／充电时刻／协同）→ 八行总表逐行填
  ⑤ 公开强度线并行推进，不阻塞 ①–④
```

### 为什么提速必须在效应扫描之前（`[USER]` P52 ＋ 实测算术）

`FACT`（P52，用户拍板）：每个机制臂必须**从头重新优化路线**，不许冻结路线改参数复算。
`FACT`（实测速度底样，7 次完整运行，M1）：均值 **986.35 秒/次 ＝ 0.274 机时/次**
（来源：`sc11_grid_20260816/cells/*/raw_runs.csv`、`sc10_education_depth_20260816/n3/`、
`sc8_decoder_crossover_20260816/default_off_900/`）。
`FACT`（已写入设计的扫描）：充电时刻 3 臂＋碳价 4 锚点＋EV 溢价 4 点 ＝ 11 点 × 10 种子
＝ **110 次搜索 ＝ 30.14 机时**——而这还是按 900 秒这个**已被 P79 判为未收敛无效**的口径算的。
`UNKNOWN`：正式单次时间 `T_conv > 900 秒`，具体上界无法从三条未收敛曲线推出。

`INFERENCE`：不先提速，效应扫描要么按无效预算跑完作废，要么在慢实现上原价支付全部正式机时。

### `CORRECTION`：Claude"四个候选不吃算法强度"已被推翻

| 候选 | 它实际是什么 | 零搜索能说什么 | 正式做要不要重搜 |
|---|---|---|---|
| 午休充电 20.7%/50.8% | 固定路线上的**复算值**（P44 原文即写"待搜索解复现"） | 可达效应上限／候选故事 | **要**（3 臂×10＝8.22 机时） |
| 碳价临界 1.472 | 固定电量、其余不变的**局部解析式**；另有午休可达窗锚 1.223 | **可零搜索**报解析命题 | 政策扫描**要**（4 点×10＝10.96 机时） |
| EV 减排 2.7–27.5 倍 | 每公里排放系数相除的**纯物理系数** | **可零搜索**画参数图 | 升级成"系统排放随时段摆动"就**要** |
| 混合车队 7/10 可转 | 对一份**全 CV 路线**按临界里程分类；那次终解实际 0 EV | 作用空间 | **要**（4 点×10＝10.96 机时） |

**净结果**：两项可现在就零搜索进论文（标题须写"参数/作用空间"，不得写"优化后效果"）；
另两项只是作用空间，变成机制结果一次搜索都绕不过。

### `CORRECTION`：Claude"协同两臂最便宜"已被推翻

`FACT`：现有 `--mechanism-off cross_depot` 关的是**初始解客户—车场映射**，
不是 P65 冻结的 25/25 企业责任归属，两者哈希不同。它只能做算子消融，
**不能冒充"两家企业各自单干"的政策对照**。
`FACT`：正确的企业协同最小设计是每颗种子 **A 单干＋B 单干＋联合＝3 次**，10 种子共 30 次
（与充电三臂同调用数），不是最便宜；`T_A25`/`T_B25` 尚无实测，正式机时 `UNKNOWN`。

---

## 四、无悔集（不依赖任何拍板，可立即做）

1. 两本方案按 P80 更新状态：无任务在跑、长跑零产物、`speedfix_cache` 从未生成且**不是失败**（被叫停）。
2. 删除 2.3%/4.2%/8.8% 及一切"在噪声内"表述；**不另造替代百分比**。
3. SC1 改判为完成，并写入"语义改变件不要求终解相同"作为验收反例。
4. SC0/SC2/SC8/SC-V2/SC10 的完成层级分标**技术／行为／效果**，不把 DONE 一律写成"算法有效"。
5. SC7 关闭（无趟数上限，已被解码器语义吸收）；SC9 降为公开侧产物 schema，不是算法组件。
6. SC5/SC6 从 SC8 硬依赖中移除，改为"待病灶证据支持的候选偏置"。
7. P37-C 改写为"历史 08-09 等价已逐位证实，源码漂移后当前待极短复验（约 2 分钟）"，
   **不写"从未核过"**。
8. 停止条件写清：live 路径是 `iterations` 或 `max_runtime_seconds` 终止
   （`run_problem_hgs_private_technical.py:3365-3384`），`stagnation_patience` 是**重启**不是终止。
9. 删除旧 C8 第 5 批修复任务身份（MAIN3B 已贯通 5 批），效果仍 `UNKNOWN`。

---

## 五、交用户的选择题（只问真正需要你定的两件）

> 其余原本想问的两件（定稿边界、噪声数据来源）**不问了**——P79 已经定过：
> 算法定稿在前、P70 暂缓、正式 10 种子探索少跑。按 P79 执行即可，不重开已决事项。

### 选择一：充电候选的语义（这一条卡着结构冻结）

差一点可行的候选（能量或时间窗差一点）该怎么办？这是**模型语义**，只能你定。

- **甲｜允许带缺口进搜索（推荐）**：补 SC3，把充电缺口量化成两个数值送进不可行子种群，
  让搜索有机会把它救回来。**好处**：保住设计书原主张，近可行解不被白白丢掉，
  很可能直接缓解"63/63 充电窗全死"。**代价**：一件真施工，且必须证明带缺口的候选不会冒充可行解。
- **乙｜继续硬拒绝**：不补 SC3，**同时把设计书里那条主张明确删掉**（不留"以后必须补"的假状态）。
  **好处**：最快进入提速。**代价**：放弃一条可能救活电动车可行性的路。
- **丙｜SC3–SC6 全做**：最完整。**代价**：SC4 被你保留的单干基准卡住、SC5/SC6 目前没有病灶证据，
  最容易扩大战线。

### 选择二：提速怎么走（含一条 AI 自设铁条要不要松绑）

`FACT`：「纯提速必须终解逐位一致，否则一律回滚」这条**是 AI 自己立的**
（`pending_decisions.md:1187` 在 `PROCESS` 段、`:1224` 在 `DECISION` 段，全册无用户原话）。
它把能快一个数量级的路线定性为"改语义、危险"，只留下能快 2–3 倍的那条叫"安全"。
提速现在是全线前置，**30 机时和 3 机时的差别可能就卡在这条自设规矩上**。

**⚠️ 审计落地后本题已实质改观**：AUDIT-1 找到 **D2**——一个定位到行、有直接代码证据的
**重复计算**（同一区间随存活标签数被反复解码），实测两包相差 **222.7 倍**。
它是**纯提速**：只加缓存，不改标签支配、不改选中规则、不改完整评价、**不碰受保护文件**，
因此**它本身就满足那条逐位一致铁条**。也就是说——**在动那条有争议的规矩之前，
先有一条无争议的路可走。**

- **甲｜先修 D2，再看够不够（`DECISION` 推荐）**：D2 有直接代码证据、修法明确、
  逐位一致可验（修后核 candidate、gap、fingerprint 逐位相同）。
  **好处**：不需要任何妥协，不必现在碰 AI 铁条，且可能拿走最大一块。
  **代价**：222.7 倍里 D2 能解释多少仍 `UNKNOWN`，修完才知道。
- **乙｜D2 ＋ 影子计数一起做**：修 D2 的同时做一轮"影子计数"（只统计有多少完全相同的输入、
  能省多少已观测耗时，不复用结果、不改搜索），把提速甲那个"20–50 秒"的估计一并变成实测。
  **好处**：一次把提速空间探明。**代价**：多约 3 分钟。
- **丙｜同时预先松绑铁条**：上述照做；并**预先授权**——若 D2＋安全缓存仍不够，
  允许走"改语义但更快"的路线，走另一套验收（不要求逐位一致，改验可行性、
  服务量红线全覆盖、同批次等墙钟对照）。**好处**：不被自设规矩挡住数量级提速。
  **代价**：改语义后旧结果不可直接续用；该路线本身仍是 P78，你此前保留未批。

**Claude 建议**：选 **乙**。D2 是白捡的（无争议、不碰保护文件、逐位可验），
影子计数只多 3 分钟却能把剩余空间一次探明；**那条 AI 铁条要不要松，等这两个数出来再决定，
不必现在盲拍**。

---

## 六、底层缺陷审计结果（`lowlevel_defect_audit_20260817.md`，`AUDIT1_DONE`）

11 个反常信号逐条追到代码行，**确认 4 个真缺陷**，其余为真实约束或需探针。

### 6.1 `CORRECTION`：两条我方长期引用的说法被推翻

**① "63/63 充电窗全死"的病根已经修掉了。**
`FACT`：根因就是那条发车时钟断裂（生成端 `timing.earliest_departure_second` vs
检查端 `route_departure_second`）；当前生成端已直接调用检查端同一时钟
（`search/multitrip_schedule.py:718-764`）。修后 SC0 终解有 1 EV、2 次充电、0 违规。
旧 63 条里有多少由它造成仍 `UNKNOWN`（需探针）。
**影响**：本文第五节"选择一"中"补 SC3 很可能缓解 63/63"这条理由**作废**——那个病已经好了。
SC3 是否必要，须只按"近可行候选该不该进不可行子种群"这个纯语义问题来judge。

**② "非线性充电从未启用"是误读，须停止引用。**
`FACT`：FRVCPY **不是**非线性曲线总开关——它只优化固定路线的充电站点与电量
（`run_problem_hgs_private_technical.py:2857-2862`）。默认修复路径本来就在调曲线核
（`charging.py:1883-1895`、`charging_action.py:17-75`、`charging_curve.py:86-106`），
保存解在 FRVCPY=false 时仍带 `M17_FAST_SHAPE_SCALED_60KW_PWL`。
那个"曲线转换计数 0"只属于 schedule oracle，而该 oracle 本次样本数就是 0。
**影响**：`CURRENT_PROJECT_CONTEXT.md` 五之十第 8 条须改写；
"非线性充电无作用条件"仍可能成立（SOC 够不够拐点是另一回事），但**不能再拿这两个数当证据**。

### 6.2 四个真缺陷

| 编号 | 缺陷 | 危害 | 最小修法 | 碰保护文件 |
|---|---|---|---|---|
| **D2** | **路线层解码对同一区间重复计算**：`_split_block` 把只依赖 `(position,end,车型,车场,班次)` 的 `_probe_segment` 放在每个存活 label 的**内层**（`hybrid_decoder.py:398-449`）。实测 441 次解码 760.97 秒 vs 另一包 10 次 0.077 秒＝**相差 222.7 倍** | **唯一同时吃掉三大实验共同搜索预算的缺陷** | 按 `(position,end)` 预计算/缓存；**不改标签支配、选中规则或完整评价** | **否** |
| C2 | 旧 C8 入口仍把未插入客户叫 `outsourced_customer_ids` 并移出活动客户，与 P74-2「顺延且不定价」冲突；生产主入口 MAIN3B 已修 | 复用旧入口会压动态实验并误记服务量 | 字段改 `deferred_customer_ids`；旧入口按 defer 保留客户，或标历史入口删死别名 | 否 |
| D3 | `accepted` 跨层混用：`record_population_admission(inserted=True)` 会调 `record_acceptance("hgs_population")`（`contracts.py:243-249`）；轨迹同一个 `accepted` 在教育阶段＝罚后成本改善、在种群阶段＝是否留在种群 | 不压求解，但**让三大实验的"接受数"被误读，继而误判机制是否生效** | 轨迹增 `acceptance_level=move/population`；报表禁止把 `accepted_actions` 求和当动作接受数；兼容期不删旧字段 | 否 |
| A1 | 发车时钟断裂 | **已修**，见 6.1① | 不再改时钟；不得再复制第二套 | 否 |

### 6.3 判为真实约束、不修的

A2 初始化 3744 分布属旧随机路径（当前 SC0 是 witness 1/1 + perturb 24/121 + random 0，无充电窗拒绝）｜
A3 带罚项接受是 HGS 可行性恢复规则，**无系统性压 EV 的证据**（15 条接受中 8 条 CV→EV、7 条 EV→CV，
EV 客户净增 9）｜B1 罚分是窗口没攒满、不是接线错｜B3 低碳价归因成立｜
D1 测试 199 vs 197 是历史清单缺失，不是真少两个测试（**不许随便补两个测试把数字凑回 199**）。

### 6.4 需探针（读码判不了）

C1 动态批 5 `no feasible clock`——动态 `_route_profile` 自己重算 preferred/latest
（`dynamic_multitrip_schedule.py:1427-1511`），静态 `route_timing` 另有一套几乎相同的递推
（`multitrip_schedule.py:446-550`）。**这是第三套时钟的嫌疑**。探针＝重放该条失败路线、
同时打印两边的 floor/latest，不跑搜索。

---

## 七、失败路（每条都通向下一步，不是死胡同）

| 触发 | 走哪 | 不做什么 |
|---|---|---|
| 影子计数显示重复很少 | 停止提速甲 | 不扩张到删字段或近似 top-k |
| 缓存出现任何轨迹/结果差异 | 完整回滚 | 不用速度收益抵消语义变化 |
| 缓存通过但收敛仍不可承受 | 把改语义的更快路线重提为选择题 | 不自动施工、不用固定 k |
| 收敛曲线仍在下降 | 从同一仓库现场续跑 | 不把硬顶结束点叫平台 |
| 当前内核 100 轮不等价 | 暂停公开线，定位漂移 | 不用私有效果替公开线过门 |
| 组件被可靠激活但 10 种子无价值 | 四类根因分诊后交用户选删除/降级/重定位 | 不调参、挑种子、换口径救故事 |
| 单干基准与 r2 语义仍未冻结 | 公平保持消费者可读、正式选择 `UNKNOWN` | 不用 `Pi0=1` 或路线归属成本冒充单干成本 |
