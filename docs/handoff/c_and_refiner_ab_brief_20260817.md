# FIX-C + REFINER-AB 任务书（发给 Codex，两件独立小任务）

先读 `docs/handoff/codex_standing_charter_20260817.md`。两件都**不依赖**正在等用户拍板的
B（时变碳多趟休眠口径），故先做。**顺序：先做便宜的 AB 对照，再做 C。**

## 件一｜公开侧精修挂件的净收益对照（把 UNKNOWN 变成数字）

`FACT`（你在 `learn_from_rivals_20260817.md` §4 查出）：公开侧自建
`_VidalCompoundRefiner`（分配＋车型槽＋旋转＋客户搬移串行精修）在两个留有分项账的实例中
占总墙钟 **10.5%／10.7%**，做了 **1945 万／1.072 亿**次旋转评价，接受 **18／53** 次。
`UNKNOWN`（你自己标的）：**净收益是否为负没有反事实证据**——没有同一运行去掉它的对照。

**本件就是补这个对照。** 同题、同 seed、同预算、**同批次背靠背**跑：

- 题目：**PR11A 与 PR15B**（就是有分项账的那两题）；
- 两臂：**开精修**（现状）／**关精修**（`refine` 原样返回，其余一律不变）；
- 预算：与刚才 A 段烟雾同口径（每题每臂 ≤120 秒），seed 11；
- 报：两臂**成本、墙钟、旋转评价次数、接受次数、服务量**，以及成本差与省下的时间。

**边界**：只关这一个挂件，不动公开评价尺子、不动题目、不动其他开关、不改内核。
**不要下"该不该砍"的结论**——只给数字，判断留给用户/Claude。

## 件二｜混合车队 EV 溢价显式入口（挡住第 ② 行）

`FACT`（你在全流程普查查出）：`run_mixed_fleet_experiment.py:1222-1247` 无 EV 溢价输入，
实际溢价由 `run_problem_hgs_private_technical.py:1724-1729` 从固定合同读取。

**要的结果**：能显式传入**两个溢价点**各跑 200 圈，产出两点的 CV/EV 台数差异读数。
**边界（硬）**：
- **不改算例、不改合同里的既有默认值**；不传溢价时行为须与现在**逐位一致**（须验证并报）；
- 只加入口与透传，不改成本公式、不碰受保护文件；
- 若发现加入口也需要改语义（例如溢价必须进合同才生效）→ **停下报告**，不自行决定；
- P28 依赖若仍卡正式 MAIN-2，如实记录，不绕过。

## 通用铁条

单进程串行；每次运行 ≤10 分钟，两件合计目标 ≤1 小时；零预算放大。
五个冻结文件不动（`cost.py` 现行 `13ae664b…`）；SC3 默认关。
产物 `solver/reports/c_refiner_ab_20260817/report.md`，首行 `C_AB_DONE`／`_PARTIAL`／`_HALT`、
末行 `C_AB_END`；四件套＋done.json＋resume。结尾按章程答"接下来该干什么"。
