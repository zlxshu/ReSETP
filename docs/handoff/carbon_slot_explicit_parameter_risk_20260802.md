# X2-PD-002：碳时段数隐式 18 槽风险登记

日期：2026-08-02  
任务：Z1 阻塞项代码修复 + 全链回归  
状态：风险已登记；受保护源码未修改；防回归测试已增加

## FACT：风险与当前边界

`solver/src/setp_solver/cost.py:73` 的 `carbon_slot_index` 允许 `n_slots=None`，并在这种情况下使用历史常量 `CARBON_N_SLOTS=18`。该默认值可能让未来新增的 24 小时、48 槽或其他时域调用静默映射到错误时段。Z1 开始前的全仓调用核查确认：生产调用点均已显式传入 `n_slots`；唯一漏传是 `solver/tests/test_carbon_gate.py` 中验证历史 18 槽锚点的测试调用。

`cost.py` 是 Z1 明确指定的受保护文件，因此本轮没有改动函数签名或默认行为。历史包中已有的显式 18 槽语义也没有变化。

## DECISION：本轮处理

历史锚点测试现显式传入 `n_slots=CARBON_N_SLOTS`。新增 `solver/tests/test_carbon_slot_call_contract.py`，以 AST 扫描仓库内全部 Python 调用点；任何未来没有关键字参数 `n_slots` 的 `carbon_slot_index(...)` 调用都会令测试失败。扫描排除 AppleDouble、Git 与测试缓存，不排除冻结源码，因此其调用契约也在覆盖范围内。

## 建议改法（需另行授权）

未来解除 `cost.py` 保护后，建议把 `n_slots` 改为必需的仅关键字整数参数，并删除 `None -> 18` 的静默回退。需要保留历史 18 槽行为的入口应在调用处显式传入 `CARBON_N_SLOTS` 或 `18`；场景入口应传入实际碳曲线长度。实施时应先运行本次新增的调用点契约测试，再运行成本、充电、E4 零搜索重放和全量测试。

## HALT 条件

如果未来发现任何无法确定真实时段数的调用点，不得猜测或沿用 18；应停止该入口并要求场景数据提供明确时段数。
