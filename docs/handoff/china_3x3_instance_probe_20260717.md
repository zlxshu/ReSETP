# C31 中国 3×3 数据可得性探针与 DRAFT 算例构造（2026-07-17）

状态：**DRAFT only；C31 成渝 100 客户已有通过结构门的 v2 候选，但尚未冻结；未运行任何算法或性能搜索。**

完整性记录：构造完成后的外置卷审计发现若干 AppleDouble 旁车文件，已标记为 `HASH_CONTAMINATED_APPLEDOUBLE`；旁车文件不是 OSM 原始响应或算例字段，现已全部清理。最终来源指纹来自实际文件本体，未覆盖或重写任何原始响应。

## 可得性与来源

来源为 OpenStreetMap 官方 Map API 静态小地块快照（ODbL）。每次请求文本、编码 URL、原始 XML、派生要素 JSON 和 SHA-256 位于 `data/ChinaInstances/c31_3x3_osm_map_api_grid_probe_20260717/`。此前 Overpass POST 宽框请求返回 HTTP 406、GET 宽框请求返回 HTTP 504、随后被 HTTP 429 限流；这些失败不作为零数据。没有用随机坐标填补。

| 区域 | POI | 工业/车场候选 | 充电站 | 失败请求 |
|---|---:|---:|---:|---:|
| jjj | 1595 | 26 | 3 | 0 |
| prd | 1413 | 119 | 10 | 0 |
| cy | 571 | 58 | 5 | 0 |

工业用地仅表示 OSM `landuse=industrial` 车场/物流园区候选，不声称每块都已核验为运营物流园。客户 POI 与充电站均保留 OSM 要素 ID；若 OSM 未给 `capacity`，`station_chargers=1` 是明确标注的派生运行参数，不是观察到的桩数。

## 九个 DRAFT 算例与结构门

| 实例 | 客户 | 车场 | 站点 | 覆盖半径（km） | 结构门 |
|---|---:|---:|---:|---:|---:|
| `c31-jjj-50c-01-DRAFT` | 50 | 2 | 2 | 7.1 | PASS |
| `c31-jjj-100c-01-DRAFT` | 100 | 2 | 3 | 107.6 | PASS |
| `c31-jjj-200c-01-DRAFT` | 200 | 2 | 3 | 256.8 | PASS |
| `c31-prd-50c-01-DRAFT` | 50 | 2 | 3 | 4.3 | PASS |
| `c31-prd-100c-01-DRAFT` | 100 | 2 | 3 | 57.2 | PASS |
| `c31-prd-200c-01-DRAFT` | 200 | 2 | 3 | 81.2 | PASS |
| `c31-cy-50c-01-DRAFT` | 50 | 2 | 3 | 3.7 | PASS |
| `c31-cy-100c-01-DRAFT` | 100 | 2 | 3 | 3.7 | PASS |
| `c31-cy-200c-01-DRAFT` | 200 | 2 | 3 | 222.7 | PASS |

每个草稿含 `instance.json`、`nodes.csv`、`distance_matrix.npy`、`carbon_profile.csv`、`source_manifest.json` 和 `structure_gate.json`。坐标来自 OSM；需求、服务时长和时间窗是按已有 `setp_instance_lab` 生成器分布与各实例 seed 派生的模拟订单，绝不冒充真实订单。区域碳列分别为 `Beijing`、`Guangdong`、`Chongqing`，引自仓库已验签的 Li 等（Scientific Data, 2026）S1-2025 投影宽表，不能称为实测或实时数据。

## 失败与决策

- Map API 分块：36/36 快照成功；完整请求台账在 `probe_catalog.json`。
- Overpass POST（北京充电站宽框）：HTTP 406。
- Overpass GET（北京 0.6° 充电站宽框）：HTTP 504，服务端忙。
- Overpass GET（北京 POI 小框，重试后）：HTTP 429，速率限制。

决策：首轮公开 OSM 草稿证明三区域要素可读取，但 `c31-cy-100c-01-DRAFT` 的 3.7 km 足迹不满足成渝中规模预注册足迹，因此首轮不能直接使用。随后保留首轮失败记录，并补充多个成都都市圈真实 OSM 子区形成 v2 候选；工业地块标签不等同于已核验物流园，部分充电站缺少容量字段。v2 仍须经过用户审阅和后续统一正式门，不得把结构门通过写成优化结果。

## C31 成渝 100 客户 DRAFT-v2 尝试（2026-07-17）

为修复旧 `c31-cy-100c-01-DRAFT` 的 3.7 km 足迹，本轮在独立目录 `data/ChinaInstances/C31_DRAFT_20260717_osm_map_api_grid_v2/` 重新使用 OSM 官方 Map API 静态 0.03° 分块。初始成都青羊、德阳绵竹、眉山东坡、资阳雁江和绵竹扩大池的失败尝试全部保留在 `attempt_history.json`；绵竹扩大池 POI 仍为 0，未被当成“无数据”结论。

最终选定 33 个静态小块：成都青羊、德阳旌阳、眉山东坡、资阳雁江、乐山市中。五个片区的 POI/工业候选/充电站分别为 `481/47/4`、`12/13/1`、`17/9/0`、`13/3/0`、`63/36/0`。客户配额在本次最终片区抓取前固定为 `55/10/17/3/15`，均来自真实 OSM POI；需求、服务时间和时间窗仍明确标记为派生订单属性。最终候选 `c31-cy-100c-01-DRAFT-v2` 通过结构门：100 客户、2 车场、3 充电站、105 节点、48 槽重庆 TVCI、覆盖半径 98.410 km、加载器复算 105 节点，优化搜索次数为 0。

这一步是针对前述 HALT 的开发性数据构造修复：乐山市中片区是在绵竹池不足和四片区足迹 76.089 km 失败后才加入的。因此 `DRAFT-v2` 只能作为透明的候选和下一轮冻结输入，不能把 98.410 km 解释为事前确认的结果，也不能直接支撑任何算法或机制主张；正式搜索前必须重新确认片区、配额和冻结合同。

因此当前判决为 `PASS_DRAFT_STRUCTURE_GATE / NOT_READY_FOR_V2_FREEZE`。这是数据和结构候选，不是正式算例，更不是算法性能证据；后续仍需用户审阅、统一非线性物理口径、正式合同冻结和搜索前验收。OSM 原始 XML、请求文本、派生要素、哈希和失败历史均保留；未使用随机坐标、城市中心点或坐标拉伸。
