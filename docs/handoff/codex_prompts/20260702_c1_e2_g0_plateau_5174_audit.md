# Codex Prompt — C1 / E2-G0 5174 平台审计

日期：2026-07-02
目标执行者：Codex
模式：先只读审计，必要时最小脚本；不启动正式扩跑

## 0. 先读

进入仓库 `/Volumes/移动硬盘（512G）/ReSETP` 后，先读：

1. `HANDOFF.md`
2. `docs/handoff/project_prd_execution_map_v2_20260702.md`
3. `docs/handoff/memory/project-prd-execution-v2.md`
4. `docs/handoff/project_planning_map_20260701.md`
5. `baselines/e2_alns/ev_heavy_findability_gate_long_same_instance_v2_data/metadata.json`
6. `baselines/e2_alns/ev_heavy_findability_gate_long_same_instance_v2_data/raw_runs.csv`

Fable5/Claude 的合理规划可以继承，但最终事实以 `HANDOFF.md` 和原始 CSV/JSON/checkpoint 为准。若旧 PRD 与当前数据冲突，必须显式标 `PRD_SYNC_REQUIRED`。

## 1. 任务目标

解释当前 E2 最大风险：在 `e2-threeshift-150c-01`、280kWh、长复核数据中，GA/LNS/PSO/VNS 两个 seed 全部停在同一个 cost：

```text
5174.345121253789
EV share = 0.6818181818181818
```

本任务要回答：这是健康算法在同一合法邻域内收敛到共同局部最优，还是 baseline 活性修复引入了共同 deterministic 通道，导致四个基线被同质化。

## 2. 禁止事项

- 不改 `solver/src/setp_solver/cost.py`。
- 不改 `solver/src/setp_solver/check.py`。
- 不改 `solver/src/setp_solver/search/evaluation.py`。
- 不启动三班多实例扩跑。
- 不把单实例 ALNS 领先写成正式胜利。
- 不清理或重写旧数据目录；若发现 hash 污染，只标记，不覆盖。
- 不把 DR-ALNS 接入 E2；DR 只保留为并行线。

## 3. 输入与重点文件

主要输入：

- `baselines/e2_alns/ev_heavy_findability_gate_long_same_instance_v2_data/raw_runs.csv`
- `baselines/e2_alns/ev_heavy_findability_gate_long_same_instance_v2_data/metadata.json`
- `baselines/e2_alns/ev_heavy_findability_gate_long_same_instance_v2_data/checkpoints/`
- `baselines/e2_alns/ev_heavy_findability_gate_long_same_instance_v2_data/.tasks/`
- `solver/src/setp_solver/search/metaheuristic_baselines.py`
- `solver/src/setp_solver/search/winner_operators.py`
- `baselines/e2_alns/ev_heavy_regime_decision_probe.py`

重点字段：

- `status`
- `actual_evals`
- `best_cost`
- `ev_route_share`
- `charging_action_count`
- `history_json`
- `operator_counts`
- `best_signature`
- `returned_initial_signature`
- `best_improvement_count`
- `first_improvement_eval`

## 4. Plan

先做只读事实表：

1. 列出 14 行长复核数据，按 algorithm/seed 排序。
2. 确认 GA under-eval 是否已经在当前数据中变成 `16000/16000 OK`。
3. 列出所有 `5174.345121253789` 行的 eval、EV share、充电动作、best signature。
4. 列出 ALNS 变体行的 cost、eval、EV share、充电动作。
5. 检查 `artifact_hashes.json` 是否包含 `._*` 或 `._artifact_hashes.json`。若有，只标 `HASH_CONTAMINATED_APPLEDOUBLE`。

再做机制审计：

1. 阅读 `metaheuristic_baselines.py` 中 GA/LNS/PSO/VNS 的搜索入口。
2. 定位 `_vehicle_type_mutation`、`_order_to_solution`、charging repair、vehicle id normalization、LNS current 接管逻辑。
3. 判断四算法是否共享同一个强确定性 repair channel。
4. 从 checkpoint 或 task row 中抽取平台解，比较 route/vehicle/charging action 是否逐字段相同。
5. 用冻结 `evaluate()` / `check_solution()` 做只读 replay；若写脚本，脚本只能输出审计报告，不能改原始数据。

## 5. Do

建议产物路径：

```text
baselines/e2_alns/e2_g0_same_value_platform_audit_20260702.md
baselines/e2_alns/e2_g0_same_value_platform_audit_data/
```

若需要脚本，建议：

```text
baselines/e2_alns/plateau_5174_audit.py
```

脚本要求：

- 只读输入文件。
- 输出 `summary.json`、`platform_rows.csv`、`solution_signature_diff.csv`。
- 不覆盖已有 raw data。
- 不吞失败行。

## 6. Check

报告必须包含：

1. 当前事实表：GA under-eval 是否已修正。
2. 同值平台表：哪些算法/seed 到达 `5174.345121253789`。
3. 路径审计：各算法首次改进 eval、改进次数、是否返回初始解。
4. 解结构审计：route、vehicle type、charging action 是否完全同构。
5. 代码路径审计：共享 repair/type mutation 是否足以解释平台。
6. hash 卫生审计：是否有 AppleDouble 污染。
7. 与旧 Stage B `MECHANISM_BUT_TIE` 的关系：新审计不能自动推翻旧 144/144 结果。

## 7. Act / Verdict

只能给下面三类 verdict 之一：

```text
HEALTHY_SHARED_LOCAL_OPTIMUM
```

含义：GA/LNS/PSO/VNS 是不同搜索路径，合法进入同一局部最优；同值可解释。允许进入 G1/G2，但仍不能把单实例 30% 写成正式结论。

```text
ARTIFICIAL_HOMOGENIZATION
```

含义：共享 deterministic 通道主导四个 baseline，导致算法壳失去差异。必须先整改 baseline 邻域或显式共同 repair layer，再重跑 G0。

```text
BASELINE_HEALTH_UNRESOLVED
```

含义：证据不足，不能说明 baseline 健康；停止 G4/G5。

附加标签：

```text
HASH_CONTAMINATED_APPLEDOUBLE
PRD_SYNC_REQUIRED
```

## 8. 验收标准

任务通过需要同时满足：

- 报告和 JSON 产物落盘。
- 所有引用数字都能追到 CSV/JSON/checkpoint。
- 明确写出 verdict。
- 不改 solver 目标/约束/评估语义。
- 不启动正式扩跑。
- `HANDOFF.md` 追加一条审计记录。

若审计显示 `5174` 平台解释不清，必须停在 C1，不进入 C5/C6。
