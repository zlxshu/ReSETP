# E2 S2-REV-V2 execution record

This record freezes the corrective S2 closeout under approval-register entry `S2-INFEASIBLE-UNIT-001`.

- Preserve the original `full_gate/decision.json` and `report.md` HALT evidence without rewriting `raw_runs.csv`.
- Create separate v2 decision/report/metadata/done files: 810/810 rows complete, 809 OK, one deterministic HGS-E/`naive_ev` infeasible raw row (`cn-jjj-200c-01-V2-LOCATIONS`, seed 2, C099 late by 0.388 seconds). HGS-F is 405/405 feasible, HGS-E is 404/405, and P3 read-only reuse supplies HGS-M and MV-HGS-SP at 405/405 each.
- For that instance only, compute HGS-E Best/avg from its four feasible seeds and disclose the registered table note. Retain the failed row as evidence for the necessity of complete-model referee checking.
- Do not rerun, replace the seed, relax the time window, modify the completioner/evaluator/prices/cost/check/search code, modify P3 raw data, or edit main TeX.
- Remove AppleDouble sidecars before refreshing hashes, preserve the v1 manifest, and mark `HASH_CONTAMINATED_APPLEDOUBLE`.
- S3 and later may retain the same explicitly registered `naive_ev` complete-model time-window infeasibility only while each single-arm failure rate is at most 5%; an unregistered error or a rate above 5% is a HALT.

The S3/S5 runners and chain controller must consume the v2 decision while retaining v1 provenance. The fresh chain monitor is `.e2-s2-to-s5-chain-v3.monitor`.
