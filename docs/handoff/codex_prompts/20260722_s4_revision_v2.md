# S4-REV-V2 execution record

This record freezes the 2026-07-22 S4 correction. The v1 `table4_gate/decision.json` and `report.md` HALT evidence are preserved unchanged. The only permitted code change is the S4 acceptance runner: whole-solution `check_solution`, exact scoring, and one-time customer coverage remain whole-solution checks; route fragments use arithmetic recomputation and additive closure under the same evaluator semantics. The sealed solution and witness are not changed, and the protected evaluator, cost, price, search, and main TeX files are outside scope.

The corrected run is `PASS_S4_ROUTE_DETAIL`. The chain then consumes `table4_gate/decision_v2.json` and releases S5.
