# S5-REV-V2 execution record

The current S5 contract uses `algorithm_naming_map_20260722.md`: `cv_only`→HGS-F, `naive_ev`→HGS-E, `mechanism_ev`→HGS-M, and MV-HGS-SP unchanged. The old S5 A1/engineering-label output is preserved as v1 provenance but is not current paper output. S5 v2 must generate no A1 81-row appendix; China81 is represented by a正文 summary with three city groups × three contiguous sealed-tier bands plus Overall, and the S2 registered HGS-E infeasible unit uses four feasible seeds for that instance's Best/avg without rerun or filtering.

S5 v2 is deterministic materialization from sealed inputs, reports global paired Wilcoxon/Holm auxiliary statistics, preserves the收敛式 CPU boundary, writes v2 four-piece evidence and hashes, and never edits TeX or the final closure marker.
