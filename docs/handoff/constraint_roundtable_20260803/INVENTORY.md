# 条条框框完整清单（圆桌会议共同底稿，2026-08-03）

由 Claude（裁判）编制。正反双方**必须逐条对同一清单表态**，不得只挑好打的。
每条格式：编号 / 内容 / 来源 / 目前在哪些任务里生效 / 已知代价或已知避免的失误。

## A 类：用户明确交代的

- **A1 不设附录，正文只留必要产物。** 来源：用户 08-02。生效：全部写作任务。
- **A2 允许事先设计放大器，禁止事后结果过滤。** 来源：用户长期原则。生效：全部实验任务。
- **A3 旧数据与旧设计几乎全部作废，以最新为准。** 来源：用户 08-02。生效：作废登记、重跑。
- **A4 不得编造参数（尤其等待损失系数）。** 来源：用户 08-02。生效：动态发车。
- **A5 一个算例贯通全文，其余仅作稳定性校验。** 来源：用户 08-03。生效：算例选定与五组重跑。
- **A6 与用户对话不得用代号与黑话。** 来源：用户多次。生效：汇报。
- **A7 Claude 只思考/判断/写提示词/写文档，Codex 执行。** 来源：CLAUDE.md。生效：全局。

## B 类：项目铁律

- **B1 `cost.py`/`check.py`/`search/evaluation.py` 未经批准禁改。**（`cost.py` 已于 08-02 获批改过一次）
- **B2 影响论文结论的任务必须留四件套＋report.md。**
- **B3 `artifact_hashes.json` 排除 `._*`、`__pycache__`、`.pytest_cache`。**
- **B4 M1 机器为论文正式数字唯一基准，x86 绝对数不得跨机比。**
- **B5 一任务一看门狗，须能检测跑死而非只等成功产物。**
- **B6 不得把 `HALT`／弱结果包装成胜利。**
- **B7 不设无目标期刊原文页码支持的科学门槛。**

## C 类：来自文献、可能水土不服

- **C1 停止条件＝最大 2000 迭代或连续 150 次无改善。** 出处：陈婉茹等（2023）p.3328。
  **已证水土不服**：本项目 100c 与 200c 上五臂与三消融臂全部收敛到同一值，判别力为零；
  项目自有标定为**每视角 25000 次**（`V7_FIXED_25000_ITERATIONS_PER_VIEW`），
  该预算下旧批次 405 单元有 `M→MV 改进 114／退化 0`。
- **C2 单算例 × 10 次重复。** 出处：陈婉茹 p.3328、陈雨蝶 pp.13-14 表6、李得成 pp.1006-1007。
- **C3 代表实例规模取 51--149 客户（对应其 71 客户仿真实例）。** 出处：陈婉茹 p.3331 表8。
  **已证水土不服**：该规模对本求解器过易。
- **C4 三臂删除式消融（完整／去 A／去 B）。** 出处：陈婉茹 p.3331 表8。
- **C5 固定车队总量、只扫油电构成。** 出处：陈婉茹 p.3331 表9、李得成 pp.1006-1007；
  另 7 篇合格多趟文献 7/7 总量固定。
- **C6 动态实验三段产出：事件/状态表 → 代表路线双面板图 → 策略比较表。** 出处：姜广田 pp.2374-2377。
- **C7 车场充电不设并发上限。** 出处：真正建模充电的 7 篇中 5 篇不设。
- **C8 固定成本按实体车计费、第二趟不再收。** 出处：Zhao 2024 p.923、Wang 2024 p.11；9 篇零篇按趟。
- **C9 预优化与动态重优化统一种群 120、最大 2000 代。** 出处：姜广田 p.2372；
  该文未给动态重优化独立停止规则，本项目据此决定不补造第二套。

## D 类：Claude 自己发明的

- **D1 「若仍全平，不得再换算例继续试」。** 08-03 写入消融重跑。
  **已知代价：该规则若生效会挡住查明真因（真因是预算不是算例）。**
- **D2 「预注册什么结果会否定本实验，跑完不得修改」。** 写入全部四项正式实验。
- **D3 把 `docs/paper_v2/paper_main.tex` 列入禁改文件。** 项目受保护清单本无此项。
- **D4 「不得跑完多个实例再挑好看的」。** A2 的实现写法。
- **D5 每个提示词堆十余条「不得……」。**
- **D6 「不得为通过而放宽断言／不得删断言跳测试转绿」。** 写入修复与重锚任务。
- **D7 「严禁靠加大预算、换种子、换算例制造差异」。** 写入消融重跑。
- **D8 零搜索与正式实验的硬分界（零搜索任务一律禁止启动搜索）。**
- **D9 「本轮不得再以澄清问题结束」。** 写入动态实验重派。
