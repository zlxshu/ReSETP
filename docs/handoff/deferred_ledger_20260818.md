# 挂账台账（2026-08-18 建，用户令"先修到能跑通，别的先登记挂账"）

`LEDGER_ACTIVE`。本文件只登记**已确诊、但本轮不处理**的事项。用户 2026-08-18 原话：
「**先修到能跑通。先学会走路再学会跑步！别的事先登记挂账，说不定修到能跑后能自然解决。**」

规矩：登记即冻结现状与证据，**不在本表里推进、不在本表里下结论**。
每条给：是什么／证据（A 级）／为什么现在不做／解冻条件（什么时候该重新拿出来）。
L1–L10 的证据出自 `solver/reports/probe_all_mechanisms_20260818/`（主包 `accepted=false`），
数字均经终端 Claude 回原件逐位复核；L11 的证据入口在该条内单列。

---

## 一、机制作用条件类（造得再好也出不了效应，属设计问题不是施工问题）

### L1　碳价过低使"碳感知臂"必然反向

`FACT`：`cost_plus_carbon` 臂成本 2982.891573 **低于** `asap` 的 3014.853523，
但 `E_total` 233.738297 kg **高于** asap 的 151.781379 kg（**+54.0%**）；
车队由 2 CV/7 EV 变为 4 CV/4 EV，`fuel_liters` 由 15.077629 升至 66.816042。

`INFERENCE`（算术必然，非实现缺陷）：现行碳价 0.075 元/kg 下 `cost_carbon` 仅 11.386639–17.535047 元，
占总成本 **0.4%–0.6%**。多排 82 kg 只需多付约 6.2 元，却可省下约 38 元。
搜索层面等于"碳不存在"。此与"完整问题清单"第 7 条一致：κ*=1.472 元/kg vs 现行 0.075，**差约 20 倍**。

**为什么现在不做**：这是碳价参数与实验设计问题，改不动"能不能跑"。
**解冻条件**：进入正式实验设计、要出时变碳那张图时；届时效应只能来自**碳价扫描**，
不得靠调算法或改实现制造。

### L2　非线性充电的作用条件不存在

`FACT`：13 次充电会话全部带 `M17_FAST_SHAPE_SCALED_60KW_PWL`，曲线核确在运行；
但最高会话结束电量仅 35.186382 kWh／77.28 kWh = **45.531% SOC**，
M17 曲线第一个拐点在 **85% SOC（65.688 kWh）**。**本轮没有任何一次充电穿过降功率段。**

`BOUNDARY`：本条只能由"SOC 够不够 85% 拐点"论证。
历史上引用的"`frvcpy_enabled=false`"与"曲线转换计数 0"两个数**已于 2026-08-17 判为误读，禁止再引用**。

**为什么现在不做**：零件是好的、已接通，属算例作用空间问题。
**解冻条件**：若非线性充电要在论文里承担任何主张时；届时须先证明本算例能否让 EV 充到 85% 以上，
不能就如实报告"本算例不触发降功率段"。

### L3　动态滚动臂的每决策点预算处于饥饿态

`FACT`：滚动臂 5 个决策点**每点只完成 1 个 Problem-HGS 循环**；两臂总墙钟 8.126780 秒与 3.183944 秒。
滚动 4919.599974 vs 机械在线 4941.443977，滚动低 21.844003；两臂均 60/60 客户、16111/16111 kg，无 defer。

`INFERENCE`：这一颗种子只证明动态链能配对跑完，**不能区分"动态机制有效"与"重优化预算太小"**。

**为什么现在不做**：它已经跑得通，属预算标定不属贯通。
**解冻条件**：动态那张表要出正式数字时。

---

## 二、实验语义与用户决定类（代理不得代选）

### L4　三臂语义二选一（用户 `PARKED`，2026-08-17"现在不是下决定的事"）

两种语义：①三臂各自从头优化，EV 数量成为内生结果；②共用事前冻结的车型归属，只比充电策略。

`FACT`（2026-08-18 新证据，改变了这条的信息状态）：本轮 `asap` 与 `cost_min` 两臂
**意外落在完全相同的路线上**——`distance_total` 925613.572427903、`distance_cv/ev`、
`fuel_liters` 15.077629、`electricity_kwh` 237.17704958953516、车队 2 CV/7 EV、
`route_time_hours` 16.122694 **全部逐位相同**；唯一差异为
`cost_elec` 180.189038→167.981349（**−6.78%**）与 `E_ev_indirect` 111.947748→104.954951（**−6.25%**）。
**这恰好就是语义②的形态，并且真出了效应**；而语义①下第三臂反向（见 L1）。

**为什么现在不做**：用户明示暂不决定，代理不得代选。
**解冻条件**：用户按键。已把新证据报给用户，等其决定是否提前定案。

### L5　混合车队的 EV 溢价扫描需要新的运行时车辆成本合同

`FACT`：单点 50 元/EV·日**已经生效**——主跑 7 辆 EV 对应 `cost_fix_ev_premium=350.0`，
`cost_plus_carbon` 4 辆 EV 对应 200.0。但混合车队入口无溢价参数，其 `DutyEvaluationContext`
未指定该合同；若只透传新溢价，完整评价器会因与车辆成本权威不一致而拒绝。

**为什么现在不做**：**扫描**属语义变更，须用户批准运行时成本合同。
**能跑通所需的只是单点已冻结的 50 元**，扫描不在"能跑通"范围内。
**解冻条件**：混合车队那张图要出正式数字时，单独报批。

### L6　P28 单干基准的正式版是 10 种子取最好

`FACT`：P28（2026-08-08 已定）要求每车场只服务自己的责任客户、用自己的车辆与设施，
分别运行最终算法 10 个种子，取最好利润冻结为基准。

**为什么现在不做**：本轮只需 1 种子证明"这条路能跑出数"。
**解冻条件**：算法定稿后的正式实验阶段（P79：算法定稿在前）。

---

## 三、性能与强度类

### L7　公开 28 题对冻结母体的胜负仍未知

`FACT`：本轮只跑 PR11A 与 PR17A、10 秒与 30 秒。自研两题两预算均胜，但优势
53.875→4.780（**−91.1%**）、81.196→1.005（**−98.8%**）；四次运行均 360/360 客户、
需求 4806/4806 与 4814/4814。方向在 30 秒内未翻转。

`DECISION`（Codex 提出、Claude 认可）：**否决"短墙钟方向可代表 20 分钟"这一假设**，
因此没有把 28 题压成无判定力的短跑。当前 28 题胜负 **`UNKNOWN`**。

**为什么现在不做**：公开侧本来就跑得通，这是"跑得快不快"不是"跑不跑得通"。
且今日全部修复之后从未在正式预算下重测过。
**解冻条件**：结构冻结、算法定稿之后，按正式预算一次性重测（P96 第 2 条硬要求）。

### L8　`--mechanism-off cross_depot` 不是"不合作"

`FACT`：伪关闭臂仍有 **24/50** 个跨场客户；该开关只禁止相对初始解继续改跨场归属，
不强制"每家企业只服务自己的责任客户"。主跑 `depot_collaboration` 提案 8460 次、进入评价 601 次。

`INFERENCE`：3014.853523 与 3506.007032 之差 **不得作为协同收益**。

**处置**：本条**不是挂账，是本轮要修的**——真正的关闭侧由 P28 单干子问题组成，
随单干入口一并解锁。列在此处只为标明旧读数作废。

---

## 四、工程卫生类

### L9　摸底包仍使用 `FORMAL_RUN_COMPLETE` 字样

`FACT`：子包验收字样为 `FORMAL_RUN_COMPLETE`，虽同时写有 `run_kind=probe` 与
`formal_candidate_result=false`。

`RISK`：假完成链 2026-08-17 才刚截断，"正式"二字出现在摸底包里是同类风险的复发面。

**处置**：已列入本轮顺手修（低成本），不挂账。

### L10　组件交互载具把初始化计入每臂墙钟

`FACT`：1 秒与 5 秒两组八臂 `completed_generations` 全为 0，
`route_local_search`／`charging_timing`／`vehicle_type_exchange` 提案与接受计数全为 0，
但每臂均通过五件套验收并服务 50/50、13264/13264 kg。
载具先生成共同初始解再逐臂计时，而初始化与设置已计入每臂墙钟。

`INFERENCE`：这**是机械缺陷**（安装时间记在跑步成绩上），不是纯预算问题。

**处置**：本轮要修的，不挂账。**同时暴露一个必须记住的教训：
"五件套包完整"与"机制真被执行过"是两回事，验收通过不等于机制跑到了。**

### L11　Semgrep 36 文件／77 条存量命中

`FACT`：2026-08-18 采用项目现有四条 Semgrep 规则、按当时 Git 受管 Python 文件集全量扫描，
在此前单独登记的 5 个真存量文件和承载 2 个误报的 public runner 之外，仍得到 **77 条规则
结果／36 个文件**：宽捕异常后返回默认值 29 条、模块级可变缓存 7 条、`id(...)` 对象地址缓存键
41 条，另有 1 个解析警告。重复规则结果不等于独立缺陷数。

**处置**：按用户止损令，Semgrep 暂降为非阻断报告模式；逐条清单作为
[分批还债队列](../../tools/quality/semgrep_legacy_registry.md) 保存，不在地基收官轮修生产语义。
队列与此前单独登记项全部清零、全量扫描确认无存量命中后，再恢复 `--error` 阻断。

---

## 五、算例数据口径类（2026-09-12 新增）

### L12　公斤需求沿用旧参照载重

`USER DECISION`：将以下问题作为未来挂账事项保留，不为当前文字修订重建算例：

> 这套公斤需求最初按1000 kg参照载重折算，而现稿燃油车和电动车载重已经是1735 kg和1700 kg。按现有数据，装满7.2 m³只对应约1000 kg，因此实际求解中通常是体积约束先起作用。模型和算法仍能正常计算，不构成代码冲突；正文不能再说它“保持当前车辆的载重占比”，也不应把公斤数写成实测货重。若重新按当前车型换算，就会改变算例和实验结果，需要全部重跑，没有必要为这一处文字这样做。

**为什么现在不做**：重新折算需求量会改变算例输入及全部实验结果，当前只修正文表述。

**解冻条件**：未来整体重建算例或决定重新生成并重跑全部实验时，再统一确定公斤需求与体积的换算基准。

### L13　逐小时碳强度与半小时计算网格

`FACT`：当前碳强度数据包含24个逐小时值，并在48个半小时计算槽中按小时重复；充电费用、间接排放和充电桩占用仍按半小时槽计算。逐小时值是数据分辨率，半小时槽是模型的积分与容量检查网格，两者作用不同。

`USER DECISION`：将小时制与半小时制的统一重构归为未来挂账事项。

**为什么现在不做**：改为单一时间粒度会触及数据装载、充电计算、容量检查和现有实验结果，超出当前论文文字与排版修订范围。

**解冻条件**：未来统一重构时间离散方式并准备重跑相关实验时处理；在此之前，正文只准确区分数据分辨率与计算网格。
