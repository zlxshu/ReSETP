# Homberger G1a 真正 SWAP* 顺序门合同

状态：`AUTHORIZED_AS_PHASE1_SEQUENTIAL_GATE`。

用户于 2026-07-18 要求阶段一内部并行推进，所有阶段一事项闭合后才进入阶段二；同时延续“最快、最短、最低成本验证，非必要不启动全量”的算法开发约束。本门用于决定是否有必要支付完整 G1 的 115,200 次评价，不授权 Solomon、中国81、E1--E7正式搜索或论文性能主张。

## 结果盲任务矩阵

四例固定为 `C1_2_1`、`C2_2_8`、`R1_2_8`、`RC2_2_8`，覆盖聚类/随机/混合空间结构以及 type-1/type-2 时间窗。种子固定为1--3；每个实例—种子使用同一确定性、BKS盲起点，比较默认基线与只增加真正SWAP*的候选。每臂400次完整评价，共24任务、9,600次评价，为完整G1的8.333%。

候选仍使用默认关闭的 `SETP_ALNS_CRUSH_TRUE_SWAPSTAR`；只在晚期连续停滞时触发，每次最多占4次统一完整评价，不扩大总预算。不读取BKS、参考路线或中国结果，不修改参数、角度阈值、top-3插入规则、选择器、接受准则或触发门。

## 一次性通过标准

- 24/24任务为OK，12/12配对共同起点哈希一致，评价数和候选评分数均严格等于400。
- 四个实例的最终解均通过独立VRPTW复算与正式检查器，零硬违规。
- 真正SWAP*至少在C、R、RC三种几何族各有一次完整评价；未满足时视为覆盖不足，不调触发器补救。
- 12对平均改善严格大于0，至少8/12不退化，最差退化不超过2%，平均计时开销不超过25%。

全部满足才判 `PASS_G1A_TRUE_SWAPSTAR_PROMOTE_TO_FULL_G1`。任一失败即判 `STOP_TRUE_SWAPSTAR_BEFORE_FULL_G1`，不得挑种子、追加实例或调参救援。通过只表示完整G1值得运行，不表示算法性能已证。

## 后续边界

G1a通过后，完整G1仍按12例×3种子×1600评价×2臂执行，并使用既有正式阈值；G1a结果不并入完整G1统计。G1a不通过则真正SWAP*退出当前阶段一，算法线以负结果闭合，阶段一其他并行事项继续。
