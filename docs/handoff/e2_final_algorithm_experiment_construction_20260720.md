# E2 终局算法实验施工图（陈雨蝶母版全模仿·跑完即关算法实验）

- 编号：E2-FINAL-CAMPAIGN-001
- 日期：2026-07-20
- 状态：`APPROVED_BY_USER_RUN_ONCE_AND_CLOSE`（用户 2026-07-20 令：完全模仿陈雨蝶设计、私有算例=China81、跑完即结束算法实验、不再中途询问）
- 执行者：Codex（本图自包含，冷启动可执行；Claude 额度耗尽也能独立跑完）
- 算法：`MV-HGS-SP`（H6 冻结结构：三视角 PyVRP 0.12.2 真 HGS + 完整模型裁判 + HiGHS 集合划分重组 + 单调保护），实现在
  `baselines/algorithm_prototypes/china81_mechanism_hybrid_20260720/`
- 母版：陈雨蝶等(2025)《双碳背景下复杂冷链物流模型及求解算法》，本地
  `/Users/zhouleixishu/Zotero/storage/AYDB6KXP/`；版式细节（三线表线宽/字体/图幅）沿用
  `docs/paper_submission_final/e2_e3_preview_20260714/SETP_VISUAL_CONTRACT.md` 实测参数（同刊，无冲突）

---

## 0. 铁律（先读）

1. 本战役是算法实验的**终局**：P0–P5 跑完，无论胜负，算法实验关闭，结果照实入表，不再有候选、不再救援、不再调参。
2. 中途**零用户决策**：所有分支由 §5 预注册主张阶梯自动决定措辞与后续，不停下来问。
3. 表头字段风格**逐字模仿陈雨蝶**（见 §1 映射）；三线表/字体/线宽/图例位置按视觉合同实测参数。
4. 保护文件零改（`cost.py`/`check.py`/`search/evaluation.py`/`prices.py` 默认值/主 TeX 语义段）；表格图由脚本从封存 CSV 生成（生成纪律照旧），禁止手工修图。
5. 每阶段产五件套（metadata/raw_runs/decision/artifact_hashes/report），哈希排除 AppleDouble/缓存；发现 `._*` 按既有规程清理重算。
6. 并发按 READ_ME_FIRST：8 逻辑核用 6 workers；监控低频事件驱动；实验 runner 非沙箱启动（沙箱多进程必崩的历史教训）。
7. 种子冻结 1–10；同臂同题同种子配对；公开与 China81 同机 M1 单线程每 worker。
8. 已裁决事项（不再讨论）：公开表"PyVRP"列由 **PyVRP 0.12.2 HGS（即母体）**同机填充——对应陈雨蝶表5 中母体 GA 占一列的先例，同时直接回答"不如直接用母体"；此为 v3 合同的登记修订（列名不变，填充版本变），修订理由=引擎身份纠正（0.13.4 是 ILS 非 HGS）。0.13.4 ILS 不进主表，可进附录证据。

## 1. 陈雨蝶母版逐表映射（表头字段逐字对齐）

### 1.1 表5 → 本文「公开标准算例结果表」（V13-MDVRPTW-28 全 28 题）

母版表头：`instances | Q | BKS | 对手1 Best. | 对手2 Best. | 对手3 Best. avg. | GA Best. avg. | CGA-VNS Best. avg.`，
表体=相对 BKS 的误差百分比，末行 `Avg`，本文列最后。

本文表头（字段风格一致，内容按 v3 对比合同的对手集）：

`instances | n | BKS | VCGP Best. | MDFIHA Best. | MDFIHA-ETGA Best. | PyVRP Best. avg. | MV-HGS-SP Best. avg.`

- `instances`=PR11A…PR24B；`Q`→`n`（客户数；母版 Q 为单一载重，MDVRPTW 各场异
  容不适用，登记字段替换说明）；`BKS`=可验 BKS（冻结快照）。
- VCGP/MDFIHA/MDFIHA-ETGA 用原论文逐题值算误差%（文献列只有 Best.，同母版对手1/2 只列 Best. 的处理）；
  PyVRP(0.12.2 HGS 同机) 与 MV-HGS-SP 列 Best.+avg.（10 种子）。
- 末行 `Avg`。加粗规则同母版：逐行最优加粗。
- 预期头条：文献三列误差全为正（可验 BKS 已 28/28 低于 MDFIHA-ETGA 发表值），本文列≈0 并有负值（新 BKS）。

### 1.2 表6 → 本文「不同算法对比表」（China81 代表题，10 次运行）

母版表头：`序号 | GA(值,CPU) | VNS(值,CPU) | CGA-VNS(值,CPU)`，行 1–10 + Min/Avg/Max，逐行最优加粗，CPU 单位 min。

本文表头：`序号 | PyVRP-HGS(值,CPU) | ReSETP-ALNS(值,CPU) | MV-HGS-SP(值,CPU)`

- 三臂=母体路由+公共完成器 / 独立机制 ALNS+同完成器 / 完整混合体（v3 §8 三臂原样；对应母版 GA/VNS/CGA-VNS 的"母体/独立组成/完整混合"角色，一一对应）。
- 与母版差异仅一处并在表注声明：三臂**等墙钟终止**（比母版各自终止更严），CPU 列报实际用时。
- 代表题按 v3 §8 零搜索规则预选：对 81 题算客户数、车场数、需求离散度、时窗紧度、EV 可达率五特征，取离全体中位向量最近者，并列按实例 ID 字典序。**开跑前登记，不得看结果换题。**

### 1.3 图4 → 本文「不同算法迭代图」（代表题）

横轴 时间(min)、纵轴 成本(元)，三条曲线（PyVRP-HGS/ReSETP-ALNS/MV-HGS-SP），彩色细线+异线型，无网格，四边框，图例图内右上；图幅/字号/线宽按视觉合同实测参数（4.30in×2.80in、图内 8pt、曲线 0.62pt 等）。

### 1.4 表4 → 本文「仿真实验最终路径表」（代表题最优解）

母版表头：`路径 | 距离(km) | 成本(元) | 时间(h) | 油耗(L) | 碳排放(kg) | num | 装载率(%)`，行=逐路线 + `均值` + 合计行。

本文表头：`路径 | 距离(km) | 成本(元) | 时间(h) | 油耗(L) | 电耗(kWh) | 碳排放(kg) | num | 装载率(%)`

- 仅新增 `电耗(kWh)` 一列（EV 车队必需，登记字段新增说明），其余逐字同母版；`num`=满足时间窗客户数（母版原义）。
- 数据来自代表题 MV-HGS-SP 十次中最优解，经独立检查器复算后由脚本生成。

### 1.5 China81 全量汇总表（表5 风格延伸，附录）

表头：`instances | n | BKS-v0 | PyVRP-HGS Best. avg. | ReSETP-ALNS Best. avg. | MV-HGS-SP Best. avg.`，81 行+Avg 行；
`BKS-v0` 按 v3 §9 协议：先封母体与独立 ALNS 两臂路线账为 `China81-BKS-v0`，完整体严格改善且过独立检查器者记"项目当前最好已知解"（论文措辞用此，不称公共 BKS）。

## 2. 战场、对手与数据假设

- 公开：`V13-MDVRPTW-28` 全 28 题（地基已冻结：`baselines/algorithm_foundation/mdvrptw_v13_comparison_20260719/`，28/28 原题+可验 BKS 双检通过；文献逐题值已抽取）。
- 私有：**China81 当前 V2-LOCATIONS 构建全 81 题**（H 系列同源）。登记假设：若阶段二后续重生成实例，按影响矩阵重跑，本批产物封存不回改。
- 完成器：三臂共用同一 ReSETP 完成器（车型/非线性充电/分时电价/碳/多车场责任），不许任何臂只看里程。
- 引擎：`build/python_envs/pyvrp-hgs-0.12.2/bin/python`（MIT，已锁）；SP 用 scipy.optimize.milp+HiGHS（公共接口）。

## 3. 战役分段（P0–P5，顺序执行，自动推进）

### P0｜接线与等算力保险丝（半天内）

- 复用 H6 三题（cn-jjj-25c-03 / cn-prd-75c-03 / cn-cy-150c-03）×种子 1–5×等墙钟
  T(25c)=60s、T(75c)=180s、T(150c)=360s，四臂：
  A=母体单视角全 T；B=多重启对照（单视角 3 种子×T/3 取最好）；C=MV-HGS-SP 全 T（SP 计时在内）；D=独立机制 ALNS 全 T。
- 记录跨视角归因日志（最终解逐路线视角来源；SP 选中路线视角分布）。
- **自动规则（不问用户）**：C 对 A/B/D 全部配对不劣 → 主张走 §5 A 级；C 劣于 B（重启红利）→ 主张自动降 §5 B 级并照常进 P1（战役不中断）；C 劣于 A 或 D（等算力真输母体/组成）→ 主张降 §5 D 级，正式表照跑照填，胜负照实。
- SP 三保险自此全程生效：池上限 600、SP 时间盒 ≤10%T、父解永在池（超时返回在职最优）。

### P1｜公开 V13-28 正式批（约 10–11 小时 @6 workers）

- 28 题 × {PyVRP-HGS, MV-HGS-SP} × 种子 1–10，T=max(120s, 0.6×n) 秒（360c≈216s、960c≈576s），单线程。
- MV-HGS-SP 机制字段缺失自然退化并记资格日志（不许人工关）；解按 V13 冻结计分器判读并独立复算。
- 产出：表5 数据 CSV（文献列由冻结地基的逐题值合入）+ 收敛轨迹存档。

### P2｜China81 代表题正式批（1 小时内）

- 代表题（按 §1.2 规则预选并登记）× 三臂 × 种子 1–10 × 等墙钟（按 §3-P0 公式套规模档 T=max(60s, 2.4×n)）。
- 产出：表6 数据、图4 曲线数据、表4 最优解路径明细（独立检查器复算后）。

### P3｜China81 全量批（约 13–14 小时 @6 workers）

- 81 题 × 三臂 × 种子 1–5 × 等墙钟 T=max(60s, 2.4×n)。
- 先封 A/D 两臂 `China81-BKS-v0` 路线账，再记完整体改善；产出 §1.5 汇总表 + 配对 Wilcoxon+Holm（对齐 MC-002 原则）。

### P4｜公开 BKS 冲刺（一夜，约 5 小时 @6 workers）

- 28 题 × MV-HGS-SP × 种子 11–12 × 30 分钟长预算；任何严格低于可验 BKS 的可行解→路线证书+独立复算+登记"新 BKS 候选"。命中数不承诺，有一个算一个。

### P5｜收官（半天）

1. 表4/表5/表6/图4/China81 汇总全部由单一脚本从封存 CSV 生成（矢量 PDF+300dpi PNG），版式过视觉合同核验；
2. SP 集合划分公式、单调接受规则、三视角参数写入算法手册+论文 TeX，小例 Python 穷举 vs milp 一致性验证；
3. 引用补全：Rochat & Taillard 1995（adaptive memory，最近祖先）、Wang 等 2025（路线池+SP，DOI 10.1016/j.trc.2024.104932）、Vidal 2012/2013/2014、Wouda 2024、SciPy/HiGHS 许可登记；novelty 句式=先引后异（机制轴视角/完整模型裁判/单调保护三点差异）；
4. 全部五件套+哈希+独立复算收口；HANDOFF 变更日志、memory、审批登记同步；
5. 写入终局标记：`ALGORITHM_EXPERIMENTS_CLOSED_20260720`——此后算法只许被引用，不许被改动；算法版本冻结触发 PRD §10 影响矩阵（E3–E7 依赖复核属论文阶段，不属本战役）。

## 4. 机时总账（M1，6 workers）

P0≈1.5h ｜ P1≈10.5h ｜ P2≈0.7h ｜ P3≈13.5h ｜ P4≈4.7h ｜ P5 人工半天。
合计约 31 小时计算 + 收官半天：**两个通宵批次 + 一个白天收官**。

## 5. 预注册主张阶梯（跑前锁死，杜绝事后包装）

| 级 | 触发条件 | 论文措辞（预注册） |
|---|---|---|
| A | P0 全过 + P1 本文列均值误差≤PyVRP 列且有严格胜/新 BKS + P3 完整体全量不劣且 75c 以上层显著胜 | "逐题压过全部已发表对手；与产生当前 BKS 的同族引擎同机等价并部分更优；China81 上显著优于母体与独立组成算法" |
| B | C 未胜多重启对照但 P3 胜利保持 | 同 A，但多视角表述降为"三视角池化重组"，重启红利在附录如实披露 |
| C | 公开仅等价无严格胜 | 公开主张=“误差 0.0x%、逐题压过文献对手、与 SOTA 引擎统计等价”；China81 主张不变 |
| D | 等算力下完整体输母体或独立 ALNS | 全表照实发布；论文算法列=China81 实测最优臂；MV-HGS-SP 降为结构探索如实入文 |

任何一级都构成可投稿的 E2 章：表5 的文献压制层（28/28 预期）不依赖任何未决胜负。

## 6. Codex 执行清单（照抄可跑）

1. 读本图 + `mv_hgs_sp_stage1_closeout_20260720.md` + v3 对比合同 + 视觉合同；
2. 预选并登记代表题（§1.2 规则，零搜索特征，写 decision 前置文件）；
3. 建战役目录 `baselines/e2_final_campaign_20260720/{p0_fuse,p1_public,p2_rep,p3_china81,p4_bks,p5_closeout}/`；
4. P0 起跑，按 §3 自动规则推进 P1→P5，每段五件套+报告；
5. 每段收口按八问格式汇报（我做了什么/成功了吗/败点/下一步/依据/顾问/执念/替代）；
6. 全部完成：写 `ALGORITHM_EXPERIMENTS_CLOSED_20260720` 决策文件，同步 HANDOFF/memory/登记，Git 提交。
