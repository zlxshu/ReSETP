# E2-JRC 精确小邻域 v2 执行授权

- 科学合同：`E2-JRC-EXACT-NH-001`
- 执行版本：v2
- 日期：2026-07-25
- 状态：`AUTHORIZED_ON_ABSOLUTE_HARNESS`

JRC v1 启动失败证据原样保留。共用绝对路径启动框架通过六进程零算例验收后，授权
仅新增 v2 执行外壳和输出目录。v2 必须逐项继承 v1 的科学机制、6客户等价证明、
三道锁定算例、两臂、起点、8客户邻域、每任务一次完整候选评分、30秒安全上限、
六个单线程 worker、效果阈值和停止规则。

顺序固定为：先通过6客户等价证明、静态检查、六进程身份、六起点只读复算和资源门；
全部通过后才运行三题乘两臂的六任务 G0。任一执行或科学门失败均最终停止，不修改
候选算法，不调参、不换题、不换起点、不延长时限、不重试。通过也只授权原合同指定
的下一匹配 A/B/A+B 门，不授权全量、BKS/SOTA、E3 或论文结论。
