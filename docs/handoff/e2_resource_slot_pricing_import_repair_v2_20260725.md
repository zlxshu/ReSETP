# E2 资源—时隙定价候选：v2 纯接线修复合同

- 日期：2026-07-25
- 编号：`E2-RESOURCE-SLOT-PRICING-IMPORT-REPAIR-V2-029`
- 权限：仅修复 v1 在进入 `main()` 前的 Python 同名模块解析冲突

## 边界

v1 的注册、源码、监控异常现场、STOP 判定和五件套全部原样保留。v2 不修改算法机制、
三个算例、五种子起点、`LOCAL/RESOURCE-SLOT` 两臂、六进程、图结构、标签逻辑、
支配规则、50000 次标签扩展、8 条候选路线、一次完整评分、60 秒安全上限、通过门槛
或停止条件。

唯一允许的变化是版本化入口通过绝对文件路径加载本候选的 `test_engineering.py`，
并在独立进程内显式绑定 v2 注册与输出目录，保证不再解析到已封存候选的同名模块。
v2 源码与注册哈希在正式工程门前重新冻结。

## 执行

先运行静态检查和零目标工程/六进程资源门。只有全部通过，才运行原预登记六任务 G0。
任一工程门或 G0 条件失败，立即 STOP，不修复、不重试、不调整任何算法或实验参数。
通过也只表示该最低成本 G0 通过，不授权 China81 全量、公开 BKS/SOTA、E3、论文
性能结论或 `1+1>2`。
