# E2 resource-slot pricing isolated final engineering contract (V4)

Status: `FROZEN_BEFORE_ZERO_OBJECTIVE_ENGINEERING`.

This is the final root-cause correction authorized after the V3 G0 pre-main
module collision. V1, V2, and V3 artifacts remain immutable. V4 creates the
unique importable package `rsp_final_isolated_20260725`; all worker entry points
use package-qualified imports. Frozen algorithm modules and dependencies are
loaded from registered exact files and fail closed on any module/file mismatch.

Before any real China81 instance is loaded, six spawned workers must each report
the exact worker module/file and frozen G0 runner module/file. The smoke gate
requires six returned rows, six distinct process IDs, and exact identity closure.
Only after that passes may the unchanged six zero-objective engineering tasks
run. The existing memory, source-drift, objective-count, extension, route-count,
and time gates remain unchanged.

If engineering passes, the already registered six-task G0 runs once with six
workers. Instances, starts, lanes, physical completion, exact scoring, one
complete evaluation per task, budgets, limits, thresholds, attribution rules,
and STOP conditions are inherited byte-for-byte from V3. No parameter rescue,
instance replacement, retuning, long run, China81 expansion, public benchmark,
BKS/SOTA claim, E3 release, or paper claim is authorized.

Any engineering failure is terminal. Any frozen G0 criterion failure yields
`STOP_RESOURCE_SLOT_PRICING_NO_LOW_COST_STRONG_HGS_HEADROOM` without rescue.
