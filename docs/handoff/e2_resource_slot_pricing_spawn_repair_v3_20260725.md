# E2 资源—时隙定价候选：v3 多进程入口修复合同

- 日期：2026-07-25
- 编号：`E2-RESOURCE-SLOT-PRICING-SPAWN-REPAIR-V3-031`
- 权限：仅修复 v2 的 spawned worker 不可导入问题

v1、v2 的注册、源码、监控现场、判定和五件套全部原样保留。v3 不修改资源—时隙
定价算法、三个锁定算例、五种子 HGS-M 起点、两条对照臂、图与标签语义、支配规则、
50000 次标签扩展、最多 8 条路线、每任务一次完整评分、60 秒安全上限、6 workers、
通过比例、改善阈值或墙钟比门槛。

v3 唯一变化是把多进程任务函数放入可由新 Python 进程按稳定模块名直接导入的
`worker_entry_v3.py`。正式加载真实算例前，必须先以 spawn 模式启动 6 个子进程，
每个子进程只反序列化并调用该模块的冒烟函数，返回模块名、绝对文件路径和 PID；
要求 6/6 成功、路径一致且 PID 两两不同。该测试不读取算例、不读取 witness、不解
主问题、不构造候选、不评价目标。

只有六进程冒烟、静态检查、冻结哈希和资源门全部通过，才允许运行不变的零目标真实
工程门；工程门通过后才运行原预登记六任务 G0。任一门失败即停止。G0 若未达到原
冻结性能标准，按原标准 STOP，不调整算法或参数。
