# E3–E7 实验与产物总设计（2026-07-12，用户批准版）

状态：用户已批准开工依据（2026-07-12 对话逐项拍板）。本文是 E2 之后所有机制实验（E3、E4/E5、E6、E7）与论文产物（表、图）的**设计权威源**。
与其他文档的关系：`project_experiment_master_plan_20260711.md` 的管理规则（看板状态、六步流程、四件套、每日汇报）继续有效；本文**细化并覆盖**其中 E3–E7 的实验设计与产物设计。`e1_e7_submission_contract_decision_20260711.md` 的 E2 部分不变，其"待用户拍板"部分已由本文第 0 节的决定闭合。
执行方式：Claude 只设计与判读；Codex 执行。**Codex 提示词骨架按执行顺序写在本文第 16 节**（用户 2026-07-12 指示：Codex 照单开工，不现场设计）；骨架中标注【待填】的少量参数由上一批产出决定，届时由 Claude 在对话中给出差量，其余以骨架为准。

---

## 0. 已拍板决定汇总（2026-07-12，用户逐项拍板，唯一有效口径）

1. **跨场服务费**：论文主结果 `c^tr = 0`（完全共享口径，Gansterer/Soriano/Fernández 类文献支持）；另做 `0/10/25/50/95 £/客户` 摩擦敏感性小实验，数值明确标注为**无现实标定的代理**，95 只是历史参考点。按车场间真实距离建模挂账为可选加强/未来工作。**不改模型公式**（目标函数 c^tr 项保留，只定取值）。E2 冻结数据与此口径一致，不动；E1 免费口径与此一致，不重跑；E3 须按此口径重跑。
2. **E4 换故事**：从"碳价 30 倍阈值触发油换电"（80 kWh 时代旧叙事，280 主场景下前提失效）换为"**碳价 × 充电时机**"。管理启示章按新故事重写。旧配额扫描轴删除。
3. **客户归属**：冻结现有"最近车场"1425 条归属映射（规则+文件+hash 锁死），论文明写为自有算例的**合成独立经营基线假设**。"归属不均衡"敏感场景计入手册为备选，7/31 前不做。
4. **碳配额**：实验保持配额=0 全额计价（E2 冻结数据不动）；论文参数表口径与实验对齐；删除"配额变化影响路线"的一切表述。配额的故事定为"**总量管得住，时间管不住**"（详见 E4 卡），用户认可。
5. **跨机数字规矩**：一张表一台机器（复用旧解的实验两臂同机重算）；跨表只谈相对百分比；每张表记录机器/commit/场景出身。**用户偏好：E3–E7 尽量统一在 M1**，x86 只作溢出备用；本文机时全部按 M1（8 逻辑核、8GB、6 workers 上限）估算。
6. **显著化要求**（用户：数据好看 / 展示好看 / 故事精彩，至少一头要亮）：已核验四个事实支撑——280 场景充电间接碳≈总碳一半（E3 M3 均值 487/993 kg）；同路线择时充电省**总碳约 11.3%**（1119.7→993.2 kg）；锚定日真实英国碳强度 49–199 gCO2/kWh、日内摆幅 4.1 倍；数据库含 30 个真实日、摆幅 1.3x–4.5x，可做全真实数据的"电网波动日"敏感轴。
7. **新文献映射**（用户 2026-07-11 新补 6 篇 OM 顶刊文献）：MS2025《Robbing Peter to Pay Paul》→ E4 时间盲区故事；POM2024《Benefits of Collaboration》→ E7 预注册假设（协同价值在需求波动时最大）；《Equity-Driven Workload Allocation》→ E6 公平代价曲线与度量讨论；POM2025《Supply Chain Contracts》与《Penalizing Lateness or Sharing Cost》→ 跨场费=0+利润分配的契约定位；《Service Oriented Considerate Routing》→ 公平度量边角引用。

---

## 1. 论文故事主线与展品总表

主线一句话：**多车场油电混合配送里，碳、协同、公平、动态不是四个孤立卖点——模型让它们真的互相作用，每个机制都有一张图直接看得见，算法在统一比赛中是九个方法里最强的。**

| 章 | 一句话主张 | 主打展品（读者 10 秒看懂什么） | 实验 | 状态 |
|---|---|---|---|---|
| 算法 | 九算法统一比赛我们第一，且最快 | T3 排名表 + F2 收敛曲线 | E2 | 收口中（M1 数据，冻结） |
| 模型 | 混合车队真的在干活、结构讲得通 | T4 仅油/仅电/混合三列对照 + F1 路线图 | E1 | 已完成，不重跑，只重生成表图 |
| 机制阶梯 | 每加一个机制，结果都有可解释的变化 | T5 阶梯表 + F-E3 阶梯图 + F-E3b 协同摩擦曲线 | E3 | 重跑（新口径） |
| 碳 | 碳价和电网碳强度决定"什么时候充电"；时间盲记账看不见这笔账 | F4 充电搬家图（主）+ T7/F5 碳价响应 + F-E4c 波动日 | E4/E5 | 新设计 |
| 公平 | 公平有代价，代价可量化 | F6 公平代价三区曲线 + T8 | E6 | 新设计 |
| 动态 | 动态订单来了，协同/低碳/公平一起接住 | F7 事件时间线案例图 + T9 四臂对照 | E7 | 新设计 |

设计三原则（用户"不直观"批评的解法，写死）：
- **一实验一主角**：每个实验恰好一件主打展品，其余进附录。
- **图讲机制、表存证据**：图必须直接显示"什么变了"（搬家、阶梯、拐点、时间线），表保存可审计的数字。
- **分母选对**：机制效果同时报"层内 %"和"总量 %"两个口径，杜绝"看起来很小"或"夸大"。

---

## 2. 通用实验合同（E3–E7 所有批次共用，写进每张实验卡）

- **场景**：280 kWh 主场景（80 kWh 只保留既有稳健性证据，不扩）；真实 48 槽碳强度、锚定日 2025-11-13；L-main v3 九档算例（论文按实际客户数 22/34/45/55/114/163/221/322/449 表述，不用 10..200 内部标签）。
- **算法**：当前公开名称为 `TVCI-ALNS: Time-Varying Carbon-Intensity-Guided ALNS`（中文：时变碳强度引导的自适应大邻域搜索）；兼容机器标识仍为 `staged_hybrid_carbon_aware`（tag `e2-submission-20260711`，commit `0124623e`）。4000 次完整评价，统一共同起点，`PYTHONHASHSEED=0`，金标准 Python 3.13 + NumPy 2.3.5。**机制实验只跑主算法**，不再拉九算法。
- **口径**（第 0 节决定的落实）：`c^tr=0` 主口径；客户归属=冻结的最近车场映射（hash 校验）；配额=0 全额计价；**公平默认关闭，只有 E6 和 E7 的公平交互臂从搜索阶段启用**。
- **机器**：默认全部 M1。任何批次溢出到 x86 必须整表迁移（该表全部数字 x86 重算），跨表只谈 %，每表记录机器/commit/场景。
- **统计**：5 seeds 起步；配对比较优先；报均值±标准差、胜平负、效应量；多算法/多档多重比较用 Holm 校正；p 值只作辅助不作唯一判据。
- **证据**：四件套（metadata/raw_runs/decision/artifact_hashes）+ report.md + 保存解；每个进论文的解经当前 checker 复算零违规；成本分项闭合；hash 排除 AppleDouble 污染。
- **诚实边界**：每张卡预注册"通过线"和"降级话术"；跑出什么写什么；不改 `cost.py`/`check.py`/`evaluation.py`；长跑启动后冻结代码。
- **数据契约**：每个进论文的数字可追溯到 CSV 行 → 算例 → 口径 → commit（07-03 审计的 DATA_CONTRACT 要求）。

---

## 3. WP0 开工前置（E2 封存后立即执行，约半天，Codex）

1. E2 收口链照旧（verify → F2 → 16000 预检 8 项 → 基线参数表 → 封存提交）。这是 E3 开工的硬前置。
2. **合同机器冻结**：`submission_contract` 由 `PROPOSED_PENDING_USER_CONFIRMATION` 改为 `FROZEN_BY_USER`，内容按本文第 0 节（c^tr=0、归属映射 hash、配额=0、公平只在 E6/E7 交互臂启用）。
3. **prices.py 四同步**：`c_tr` 默认值 95→0，注释保留"95=旧无来源代理，现仅存于摩擦敏感轴"痕迹；同步 TeX 参数表。按参数四同步铁律执行（代码默认值+代码注释+TeX 参数表/说明+HANDOFF）。
4. **TeX 同步欠账登记**（写作阶段执行，WP0 只登记清单）：参数表 B=80→280 主场景说明（80 保留为文献基准对照）；配额 CE 口径改 0 并解释；跨场费=0+敏感轴说明；客户归属合成假设句；删除"配额影响路线"表述。
5. E0 快速复核：九算例 manifest/hash/正式入口一致性（约 1 小时）。
6. 每批实验开工前照惯例过 200 评价接线短门（入口/预算/落盘/恢复正确性，不看输赢）。

---

## 4. E3 卡 — 机制阶梯（协同、碳、公平的累积消融）

**一句话主张**：从"各干各的"到完整模型，每加一层机制，成本、碳、结构都发生可解释的变化；协同在零摩擦下真实发生。

**为什么重跑**：旧 30 行是收费口径（c^tr=95），与 E2/E1 的免费口径矛盾，且 95 锁死协同（5 种子只 1 次跨场）。新口径下预期协同真实出现。

**跑什么**：
- 主梯：M0 独立经营 → M1 +协同 → M2 +电网碳核算（日均因子）→ M3 +时变碳&择时充电 → M4 +碳交易 → M5 +收益公平（θ=1 起）。200c 旗舰 × 5 seeds × 6 层 = 30 次。
- 摩擦轴：M4 口径（全机制、公平关）× `c^tr ∈ {0,10,25,50,95}` × 5 seeds = 25 次。
- 每次 4000 评价、冻结算法、共同起点；只变被测机制。

**机时（M1）**：hybrid 200c 单次实测约 2–5 分钟（以 E2 实测与短门校准为准），55 次串行约 2.5–5 小时，3 workers 约 1–2 小时。

**必须记录**：总成本分项、两层碳（油直接/充电间接）、跨场客户数、每场收益与最小公平比（profit_ratios_json）、EV/CV 路线与实体车、充电次数/电量、aware/naive 同路线对照（M3 层内）。

**主打展品**：
- **T5 阶梯表**（壳：陈婉茹表8/表9 风格）：行=M0..M5（标准术语，禁黑话），列=总成本 mean±std、相对上一层 Δ%、总碳、充电碳、跨场客户数、最小收益比、EV 路线数。脚注：M2/M3 碳尺不同，层间绝对排放不可直比（正式碳判据在 M3 层内 aware/naive 配对）。
- **F-E3 阶梯图**：分组柱（成本+总碳两组），逐层排布，层间标注 Δ。
- **F-E3b 协同摩擦曲线**：横轴跨场费 0→95，左轴跨场客户数、右轴总成本；95 处标注"旧代理值"。直接可视化"手续费涨、合作萎缩"，同时给跨场费决定一个证据性交代。

**通过线**：每层变化可解释；M1 在 fee=0 下多数种子出现真实跨场服务且成本不升；M3 择时降碳复现（层内配对 5/5 方向一致）；M5 公平可行。
**降级话术（预注册）**：某层几乎无影响→"该机制在当前场景作用弱"照写；协同仍不触发→审计车队池/负载根因，如实报告，协同卖点降级为"结构支持、成本持平"；不许改实验定义救结果。

---

## 5. E4/E5 卡 — 碳价 × 充电时机 + 时间盲区 + 波动日

**一句话主张**：碳的决策杠杆是"什么时候充电"：分时记账让挪充电有利可图（总碳 -11%），时间盲记账会低估真实排放（初步估算约 16%），电网波动越大这笔账越大；配额管总量收支、碳价管力度、时间分辨率管行为。

**三个部分**：

**Part A（零搜索，M1 复算，约半天内含在验收里）**：
- 复用/复算 45 组 aware/naive 同路线配对（已有 M1 证据，验收复算即可）。
- **时间盲账面 vs 真实排放**：对同一批保存解，分别用"日均因子"和"48 槽分时因子"记账（纯算术），报告账面低估幅度。这是配额故事（"总量管得住、时间管不住"）的正式数字，引 MS2025 加州碳交易实证作文献支撑。
- **碳账户小表**：配额 ∈ {0.8, 1.0, 1.2}×基准 ×（立即/择时）→ 企业碳账户净支出/净收益（纯算术）。基准排放定义待查 TeX 原文后锁死（自然候选：仅油车反事实排放，与 E1 衔接）。

**Part B（碳价扫描，重优化）**：碳价 ∈ {0.5, 1, 2}× 现值 × 5 seeds × 200c = 15 次（机时富余则加 100c 再 15 次）。每档报告：充电搬动电量与占比、目标时段分布、充电碳、总碳、总成本、EV/CV 边际分工。约 1–3 小时。

**Part C（波动日敏感轴，零搜索）**：取 3 个真实日——2025-11-07（最平且脏，169–222）、2025-11-13（锚定日，49–199，4.1x）、2025-11-17（最波动，48–215，4.5x）——替换碳强度曲线（数据级情景，明确标注），对固定路线做充电择时重调度复算。45 组解 × 3 曲线，分钟级。预期展示"波动越大、择时省得越多"的单调关系。

**主打展品**：
- **F4 充电搬家图（全章主打）**（壳：Cheng2022 Fig6 上下双子图 + Shi2025 Fig7）：上面板=锚定日 48 槽真实碳强度曲线（峰谷底纹）；下面板=立即充电 vs 择时充电两组充电负荷（同总量）；标注加权强度 138→124、总碳 -11%。
- **T6 三行链条表**（沿用样板契约）：仅油车重优化 → 混合+立即充电 → 混合+择时充电；列=油碳/充电碳/总碳/总成本。
- **T7 + F5 碳价响应**（壳：陈婉茹表10 / Qiu Fig5 双轴）：横轴碳价倍数，曲线=充电搬动占比、充电碳、总成本；副线 EV 份额。
- **F-E4c 波动日图**：横轴三个真实日（注明日期与摆幅），纵轴择时节省的充电碳 %（固定路线）；配一句"可再生占比越高、波动越大、机制越值钱"。
- **T-E4d 碳账户小表**：配额三档 × 两策略 → 净支出/收益（£）。

**通过线**：择时降碳在 Part A 复现；Part C 单调（平稳日≈0、波动日更大）。
**预注册双写法**：Part B 响应显著→"碳价驱动充电移峰"；响应偏平→"择时充电在宽碳价区间是近乎免费的减排手段，企业无需等碳价上涨"——两种都可写，跑出哪种写哪种。
**E5 处理**：并入 Part A（立即 vs 择时）；充电功率/曲线只做来源说明；桩容量/公共桩稀缺=未来工作。

---

## 6. E6 卡 — 公平的代价

**一句话主张**：协同省钱不以牺牲任一车场为代价；把公平要求拧紧，代价曲线清晰可见。

**为什么重设计**：旧 θ 网格整段落在"约束根本不绷紧"的区间，跑完每行同数（07-11 审计实测天然公平比 1.0000–1.0146）。新口径（fee=0）下协同放开、收益转移更大，绑定区预计移动，必须两段式设计。

**跑什么**：
- **Stage A（免费搭车）**：直接复用 E3 新主梯的 M4 行（全机制、公平关、5 seeds）测天然最小公平比 r0 分布。零额外机时。
- **Stage B（绑定聚焦网格）**：θ ∈ {0.90, 0.95, 1.00} ∪ {r0 附近 2–3 个点，含预期不可行的上探点} 共约 6 档 × 5 seeds × 200c = 30 次；**公平从搜索阶段启用**（合同要求，事后补算冒充无效）；独立基准收益复用 M0（独立经营）各场收益。约 1.5–3 小时。
- 网格具体取值在 Stage A 出数后按预注册规则定（贴着 r0 布点），不再机械扫全区间。

**必须记录**：每场收益比、最小公平比、可行/不可行、总成本、总碳、跨场客户数、约束是否绑定（搜索期激活证据）。

**主打展品**：
- **F6 公平代价三区曲线**（壳：陈婉茹图4a 响应曲线 + 样板已定的不可行底纹）：横轴 θ，纵轴总成本；三区=松弛区（平）/绑定区（上翘）/不可行区（底纹）；标出天然公平点 r0。副面板或第二轴：跨场客户数随 θ 收缩。
- **T8 公平表**：行=θ 档，列=可行率、总成本 mean±std、Δ% vs 公平关、实测最小收益比、跨场客户数、绑定标记。

**通过线**：θ 变化至少改变最小公平比、成本、跨场范围之一；能标出绑定边界。
**降级话术**：全区间不敏感→"当前场景公平约束天然满足，代价为零"如实写（这本身是协同分配友好的正面证据），配摩擦轴交叉解释；不许扫到"好看"为止。

---

## 7. E7 卡 — 动态需求压力测试（标题级承诺，最高叙事优先级）

**一句话主张**：临时订单不破坏已执行部分即可重排，且动态压力下协同吸收订单、充电仍择低碳、公平全程在线——动态是三机制的压力测试，不是孤立附录。

**前置**：动态实况合同已过（`M1_DYNAMIC_LIVE_STATE_CONTRACT_SUPPORTED`，commit `96cbcbf7`：不作弊、不瞬移、不偷用旧算法、收尾不重建）；开工前补过看板要求的极小状态门。

**预注册假设**（文献支撑：POM2024《Benefits of Collaboration》——协同价值在需求波动时最大）：**H：动态新增订单的跨场吸收率 > 静态方案中的跨场服务占比**，即波动放大协同价值。

**跑什么**：
- 实例：100c 档（221 实际客户，中档代表）。
- 事件流：5 条固定流（新增/取消/需求变化混合，预生成并 hash 冻结）；机时紧则先 3 条出机制判断，再补 2 条。
- 四臂（同事件流、同种子、同每阶段预算）：①静态不重规划（代价基线）②完整滚动（协同+择时充电+公平从搜索启用）③关协同 ④碳盲（重规划时立即充电）。
- 每阶段评价预算在短门实测后冻结（量级 800–1000），全臂一致。
- 任务量：4 臂 × 5 流 = 20 会话，估约 8–15 分钟/会话，串行 3–5 小时。

**必须记录（三交互门槛，缺一按降级话术处理）**：逐阶段新增/取消/变更数、冻结路线数、阶段成本与累计成本、累计碳、**动态订单被跨场吸收的数量与比例**（动态×协同）、**逐阶段各场收益与最小公平比**（动态×公平）、**充电动作与充电时段加权碳强度**（动态×碳）、最终未服务客户数（必须 0 或合法取消）。

**主打展品**：
- **F7 事件时间线案例图**（样板 F7 已承载）：选 1 条代表流，横轴时间；泳道显示事件到达标记、已冻结/重排路线、跨场吸收箭头、充电动作叠在碳强度背景上。一张图讲完"事件来了系统怎么接住"。
- **T9 四臂对照表**：行=四臂，列=总成本、未服务数、跨场吸收率、逐阶段最小公平比（min）、充电加权碳强度、平均每阶段重规划用时。

**通过线**：四臂全部零违规零丢客；②比①省（重规划有价值）；②比③省或跨场吸收显著（动态×协同）；②充电加权强度低于④（动态×碳）；公平比全程记录（若从未受威胁，如实写"动态压力未触及公平边界"）。
**降级话术（预注册）**：三交互做不齐→逐项收缩；最坏情况写"滚动接口与状态继承可行 + 已成立的交互项"，标题措辞由用户届时定夺。

---

## 8. 补充卡：E1 / E2b 四臂算法消融 / 外部验证

- **E1**：不重跑。免费口径与新合同一致；25 条证据（20 mixed + 5 CV-only）继续有效；EV-only 只写 NOT_FOUND 不写不可行。写作阶段重生成 T4（仅油/仅电/混合反事实分解，样板已转置）与 F1 路线图（壳：陈婉茹图3 双面板；坐标单位缩放、大算例可换小算例演示）。
- **E2b 四臂算法消融**（算法创新边界的验证账，合同 B4a 要求）：普通独立 ALNS / 直接接强重组 / 分阶段混合 / 分阶段混合+择时充电，×{100c, 200c} × 5 seeds = 40 次（机时紧则先 200c×5×4=20）。产物：算法消融附录表（均值±std、Δ%、时间）。支撑"组合式创新"口径，不得越界写"独创 ALNS"。约 2–4 小时。
- **16000 预检 8 项**：已授权，属 E2 收口链（Codex），不占本文排期主线。
- **Goeke 公开算例外部验证**：按既定合同 7/24 前只做可行性评估，做不完明确降级为投稿后补充；不占 M1 高峰机时。

---

## 9. 产物对账与最终规格（新旧图表设计逐张判定，2026-07-12 用户要求）

历史上有两轮图表设计资产：①**壳目录 v1**（2026-06-17，六篇范本逐张锁壳：陈婉茹 NKRC4JZU / 陈雨蝶冷链 6KCQCP66 / Soriano IF3WEZZJ / Shi IB9ZNJR3 / Qiu QLD6M6FA / Cheng TK9YAAPC，见 memory `figure-redesign-task`）；②**07-03 样板包**（`docs/paper_submission_final/design_templates/`：T1、T3–T9(+T9 附录) 的 tex 片段 + F1–F7 的 PDF/PNG + mock CSV + DATA_CONTRACT.md，全部过 XeLaTeX 编译）。

**底版规则（最终裁定）**：07-03 样板包 = 最终底版；壳目录 v1 = 壳来源权威；本文只做下表判定内的改动。凡样板与本文冲突而未列入下表者，以样板为准并停下来上报，不得自行发挥。壳复印铁律继续生效：壳定躯体、数据填血肉、Codex 只复印灌数、缺壳/缺字段即 HALT；标题去内部编号；机制名标准化；单位/轴步进合惯例。

### 9.1 逐张判定表

| 展品 | 旧设计 | 最终判定 | 改动说明 |
|---|---|---|---|
| T1 | 样板 t1 | **原样沿用** | 算例按实际客户数表述 |
| T3 | 样板 t3（含"相对已观测最优偏差%"列） | **沿用但删两列** | ⚠️删"相对已观测最优偏差%"列（用户 07-11 拍板：自家算例连组内 gap 都不报）；删"等墙钟成绩"占位列（最终合同=固定 4000 评价单账本），改报平均运行时间；显著性=Wilcoxon+Holm |
| F2 | 样板 f2（陈婉茹图2 壳，无箱线） | **原样沿用** | E2 收口链已批准生成 |
| T4/F1 | 样板 t4 转置反事实 / f1（陈婉茹表9/图3 壳） | **原样沿用** | 数据源=E1 冻结证据；F1 坐标缩放、大算例可换小算例演示 |
| T5 | 样板 t5（M 层×成本±std×相对完整模型Δ%×显著性×总排放×EV路线×跨场×最小公平比） | **沿用+两处扩展** | +1 列"相对上一层Δ%"（阶梯叙事）；总排放旁+“充电间接排放”列（两层碳特色）。M 层命名用标准术语 |
| F-E3 阶梯图 | 无（本文新增） | **新增，绑壳** | ←Shi2025 Fig5 分组柱壳；装不进即 NEEDS_REFERENCE_SHELL 停 |
| F-E3b 摩擦曲线 | 无（本文新增） | **新增，绑壳** | ←陈婉茹图4a 响应曲线壳（与 F6 同族）；95 处标"旧代理值" |
| T6 | 样板 t6（三行链条+加权碳强度+相对上一行Δ排放） | **原样沿用** | 与新故事完全一致，零改动 |
| F3 | 样板 f3 两层碳图 | **原样沿用（非主打）** | T6 的图形版 |
| F4 | 样板 f4（Cheng2022 Fig6 上下双子图壳） | **原样沿用，升为全章主打** | 旧渲染 bug 已由样板路径修复；标注 138→124 与总碳 -11% |
| T7 | 样板 t7（碳价档×成本/燃油/充电量/**充电时段重心**/碳交易成本/两层排放/EV路线） | **沿用+档位改** | 碳价档 0.8/1.0/1.2→**0.5/1.0/2.0**（拍板值）；可选+“移动充电量占比”列；"充电时段重心"列保留（时机故事的表格化，好设计） |
| F5 | 样板 f5（Qiu Fig5 双轴壳：碳价×总碳+EV路线柱） | **沿用壳，字段本地化** | 右轴序列"EV路线数"→"充电搬动占比"（EV 路线可作副线）；横轴档位同 T7。旧 F5b"30倍阈值"故事图**废弃**，其 Qiu 壳迁于此 |
| F-E4c 波动日 | 无（本文新增） | **新增，绑壳** | ←Shi2025 Fig7 分组柱壳；三个真实日标注日期与摆幅 |
| T-E4d 碳账户 | 无（本文新增） | **新增，绑壳** | ←陈婉茹表7 分组敏感表壳（配额行×策略组，结构天然契合） |
| T8 | 样板 t8（θ×各场Π_d/Π_d⁰×最小比×成本×排放×跨场×可行） | **沿用+两处改** | θ 行改两段式绑定聚焦网格；"可行"列扩为"松弛/绑定/不可行"三态 |
| F6 | 样板 f6（细 θ 网格+不可行底纹） | **沿用+一处加** | 加天然公平点 r0 竖线标注；三区读法写进图注 |
| T9 | 样板 t9（事件流×指标，含信息成本/守恒审计）+ t9 附录（逐阶段） | **主表改四臂，列族沿用** | 行=静态后见/完整滚动/关协同/碳盲四臂；列族保留样板的成本/信息成本/排放/跨场吸收/公平比最低/低碳充电占比/守恒审计（"信息成本"概念保留）；样板"事件流×指标"版与逐阶段版降为附录 |
| F7 | 样板 f7 动态时间线 | **原样沿用** | 选 1 条代表流做案例 |
| 附录表×4 | 部分已批准 | 基线参数来源表、环境声明（可选附录）、Wilcoxon+Holm 统计表沿用批准口径；**算法四臂消融表新增**（通用三线表，E2b 数据） | |
| （废弃清单） | F5 碳价热力图（06-17 已取消）；F2 箱线（已取消）；旧 F5b 阈值故事图（E4 换故事，作废）；T9 事件流主表（降附录） | **确认废弃/降级** | 防止旧壳复活 |

### 9.2 数据契约

样板 `mock_data/*.csv` 的字段结构 = 正式 CSV 的字段契约（`DATA_CONTRACT.md` 继续有效）；正式生成时**只换数据源、不改壳**。四张新增展品补齐同格式 mock 与契约行后才允许正式绘制。每个进论文的数字可追溯：数字→CSV 行→算例→口径→commit。

---

## 10. 排期（M1 单机，2026-07-13 起）

| 日期 | 内容 | 量级 |
|---|---|---|
| 7/13–14 | E2 收口链（Codex：verify→F2→16000 预检→参数表→封存）+ WP0（合同冻结、四同步、E0 复核） | 收口链为主 |
| 7/14–15 | E3：短门→主梯 30 + 摩擦轴 25 | 55 次，约 1 个工作日含验收 |
| 7/15–16 | E2b 四臂消融（可与 E3 验收穿插） | 20–40 次 |
| 7/16–18 | E4/E5：Part A 复算 + Part B 15–30 次 + Part C 复算 | 约 1.5 天含验收 |
| 7/18–20 | E6：Stage A（复用）→ Stage B 30 次 | 约 1 天含验收 |
| 7/20–23 | E7：状态门→事件流生成→四臂 20 会话 | 约 2 天含验收 |
| 7/24 | 只补验收缺口；Goeke 外部验证可行性评估交割 | 止损日 |
| 7/25–27 | 表图统一生成（第 9 节规格），逐图核对，F4 修渲染 | 表图冲刺 |
| 7/28–31 | 正文/管理启示/摘要重写→LaTeX→投稿 | 写作冲刺 |

机时结论：机制实验只跑冻结 hybrid（单次 2–5 分钟级），总任务量约 125–165 次搜索 + 若干零搜索复算，M1 富余；此前"排不下"的恐慌主要来自九算法矩阵，机制阶段不存在。若个别批次超时，按第 0 节规矩整表迁 x86。

---

## 11. Not-To-Do（本轮明确不做）

- 不重开 E2、不 rescue、不调参救结果；HGS/DR-ALNS 只写展望。
- 不做配额搜索轴；不做"配额改变路线"任何表述。
- 不按真实距离重建跨场费模型（挂账未来工作）。
- 不做归属不均衡场景（手册备选）。
- 不跑九算法机制实验；不做 E7 全因子排列。
- 不改 `cost.py`/`check.py`/`evaluation.py`；参数改动必须四同步。
- 不混 M1/x86 绝对数字；7/28 后不开新实验。

---

## 12. 变更与交接

- 本文变更须经用户确认并同步 HANDOFF 变更日志与 memory。
- Codex 提示词骨架集中在本文第 16 节，按执行顺序排列；【待填】参数由 Claude 在对话中给差量。
- 看板 `project_board_20260711.md` 在 WP0 时由 Codex 按本文重新基线化。

---

## 13. 2026-07-12 开工前审计后的补丁记录（用户逐条拍板）

审计总结论：模型无致命矛盾（公平比有基准>0守卫、收入随实际服务车场归集、配额分阶段口径明确、充电碳按时段重叠秒精确积分含跨午夜、M0为真独立子问题）。以下为用户已拍板的补丁：

- **P5 分箱稳健性复算（已拍板：做）**：E4 Part A 增加零搜索复查——把碳强度曲线聚合为 60 分钟一格重算择时节省额，数字方向不变即写入附录一句话，堵住"30分钟分箱假象"质疑。
- **P6 跨规模交叉验证 + 算例统一（已拍板：做，且 E3--E7 算例统一不顾此失彼）**：机制实验统一算例集 = {100c(221客户), 200c(449客户)}。E3 主梯 200c×M0--M5×5种子 + 缩减梯 100c×{M0,M1,M3,M5}×5种子（20次）；E4 Part B 主 200c、机时富余加 100c；E6 沿用 E3 的 200c（Stage A 复用 M4 行）；E7 用 100c。E4 Part A/C 为九档全量冻结解复算，覆盖面更广，不受此限。
- **P7 结构稳定性汇报（已拍板：可做）**：E3/E4/E6 验收增加跨种子结构稳定性行（EV 份额、跨场客户数、移动充电量的均值±标准差），零机时。**汇报纪律（Claude 执行边界）**：正文可按惯例放代表性案例，但全量数据必须进附录/数据包可查，正文定性结论必须与全量一致；不做"隐藏不好看数据"的选择性报告——若出现不稳定，如实报告并在限制段解释。
- **P8 事件流配比（已拍板：维持原配比）**：E7 正式 5 条事件流全部维持最初配比；"变配比稳健性"与"动态度(DOD)敏感性"两个变体挂账，7/24 前有富余时按行业惯例优先补动态度变体，否则不做。
- **P9 退化关系说明段（已拍板：做，判定为行业惯例）**：论文模型章补一小段——关闭协同/碳/公平/动态后模型退化为多车场带时间窗多趟VRP，再限单车场退化为VRPTW，由此继承NP-hard性质；E3 M0 与 E1 纯油/纯电臂为操作层退化检验。零实验成本，入 TeX 欠账登记。

待用户拍板项（讨论进行中）：①共享充电站容量的处置（原文 Wang 2023 调查已完成，见 HANDOFF 同日条目）；②车辆池治理故事写法；③E7 公平基准跨阶段规则（A 开局一次/B 终局重算检查/C 每阶段重算）；④"滚动重规划"措辞统一。

---

## 14. 每个实验的理想目标与图表判读标准（2026-07-12 用户要求写入手册）

用法：每批实验收口时，验收者对着主打图表按下面标准判读；达标写达标，不达标按各卡预注册的降级话术写，不许发明中间说法。

**E3 阶梯（T5 + 阶梯图）**
- 理想图景：成本阶梯自左向右不升，每层变化可解释；M1 层起跨场客户数>0（多数种子）；M3 层内配对 5/5 降碳；M5 公平可行。
- 一眼判读：阶梯图柱高逐层不升；T5"相对上一层Δ%"列无无法解释的正跳；跨场列自 M1 起非零。
- 降级信号：某层Δ≈0→写"该机制当前场景作用弱"；跨场=0→协同降级并审计根因。

**E3b 摩擦曲线**
- 理想图景：跨场客户数随费率单调下降、95 附近接近 0；总成本单调上升。
- 一眼判读：左轴曲线下行、右轴上行，交点清晰。
- 降级信号：不单调且波动大于效应→如实写"区间内不敏感"，不挑点。

**E4 充电搬家图（F4）**
- 理想图景：择时臂充电负荷集中在碳强度低谷，立即臂集中在回场时段；两臂总电量相等（图注声明）；加权强度标注下降（参考 138→124 量级）。
- 一眼判读：上下面板对照，两组负荷柱重心明显错位，低谷底纹内择时臂占比高。
- 降级信号：负荷几乎不动→检查可移动窗口是否被班次约束卡死，如实报告可动空间。

**E4 碳价响应（T7/F5）**
- 理想图景：充电搬动占比、充电碳随碳价单调；平坦也可写（预注册双写法：显著=碳价驱动移峰；平=择时近乎免费）。
- 一眼判读：曲线方向一致、误差棒不掩盖趋势。

**E4 波动日（F-E4c）**
- 理想图景：三个真实日的择时节省%随日内摆幅单调上升，平稳日≈0。
- 一眼判读：三点向上排列。

**E6 公平（F6/T8）**
- 理想图景：三区可见——低θ平坦区、r0 以上成本上翘区、更高处不可行底纹；至少一档真实绑定。
- 一眼判读：曲线拐点与底纹边界清晰；T8 绑定列有"是"。
- 降级信号：全区间平坦→"公平约束天然满足、协同代价为零"作为正面结论如实写。

**E7 动态（F7/T9）**
- 理想图景：四臂对照全部方向正确——完整滚动成本低于静态后见基线可解释（信息成本为正且合理）、低于关协同臂、充电加权碳强度低于碳盲臂、公平比全程≥θ、守恒审计全过；F7 时间线上可见跨场吸收箭头、充电动作落在低碳背景区。
- 一眼判读：T9 四行两两对照；任何客户丢失=直接不合格（不是降级，是重跑或 HALT）。
- 降级信号：某交互不成立→逐项收缩，最坏写"滚动接口+已成立交互"。

**E2b 四臂算法消融**
- 理想图景：普通独立 ALNS → 直接强重组 → 分阶段 → 分阶段+择时充电逐级改善；非单调也如实报告。
- 一眼判读：四臂均值列递降或标注例外。
- 用途边界：只支撑"组合式创新"口径，不支撑"独创 ALNS"。

**汇报纪律（对全部实验生效）**：正文放代表性案例（惯例允许），附录/数据包放全量（必须可查），正文定性结论必须与全量一致；方向都不一致的指标不得作为结论。

---

## 15. 2026-07-12 终局拍板汇总（与上文冲突处以本节为准）

1. **车场充电桩（拍板：A，不设卡）**：三路证据（冻结解审计：单场峰值同时充电中位4--7台/最高21台待核实、EV车队≤14；Froger等容量受限充电站论文：容量约束加在公共CS、车场明写"大量充电桩"、车场侧真实约束=电网功率且属另一支文献；行业实践：车场过夜充电为主干、桩车比1:1至1:3、同时充电分散系数0.5--0.7）一致支持现设置。处置：①正式零搜索桩数审计（核实21>14反常，输出各场隐含桩数需求/隐含峰值功率/占车队比例报表）；②论文加引用交代段（车场充电主干=行业+文献惯例，公共站按1桩从紧建模比Froger更严，隐含桩数落在行业区间）；③车场电网功率上限列为未来工作。E2不重跑。公共站问题的配套事实：80 kWh场景公共站真实使用（15/24解、45次）、280主场景2次——"电池容量决定公共充电依赖"写为场景洞察；严格趟级跨场充电=0、充电费按趟归集（归属权已考虑）；共享车辆池流转（280冻结3326车次跨趟换场）为协同新增硬证据，M0下应为0，列入E3对照指标。
2. **公平启用范围（拍板：A，覆盖第2节通用合同对应条款）**：凡代表"系统给出的方案"的实验——E3完整模型层（M5）、E3b摩擦轴、E4 Part B重优化、E6全部、E7四臂——公平约束从搜索阶段启用；机制隔离层（E3的M0--M4）按消融设计保持关闭并在论文明写；E2保持封存，论文明写其为松弛口径下的算法能力比赛。依据：合同审计实测费0下无公平约束的最优解θ=1仅1/5可行——松弛最优不可执行，用户哲学获数据支持。E3b摩擦轴由M4口径改为M5口径（含公平）。
3. **E4主打展品的数据源调整**：F4搬家图与T6三行链的正面主数据改用E3新跑的M5完整模型解（200c五种子+100c缩减梯M5），与公平启用范围一致；E2的45组aware/naive配对降为广度附录（标注松弛口径）。E4 Part A复算清单增补：公共站使用统计（80k vs 280）、车辆跨趟换场统计、车场桩数正式审计、分箱稳健复查（60分钟聚合重算）。
4. **E7公平基准（拍板：C）**：每次重排前用"当时已知客户集合"重算各场独立基准（因果、无前视）；基准子问题预算规则在E7实验卡预注册后冻结；终局按实际实现收益结算报告（对应现实"事后算钱"）。论文记号Π_d^{0,τ}本就带阶段上标，无模型公式改动。E7机时估算上调约一倍（约1个整天）。四臂全部公平启用，各臂只切换自己的被测机制。
5. **措辞与写作定稿**（并入TeX欠账登记）：全文统一"滚动重规划"，预判式决策入展望；车辆池治理段（公平约束+摩擦轴=防止一家被掏空的两道闸）按低成本接地气标准撰写；退化关系说明段；车场充电引用交代段；公共站场景依赖洞察句。
6. **WP0合同修订指令**：已冻结合同中"公平只在E6/E7启用"条款须按本节第2条修订并重新落盘hash，修订须在E3正式开跑前完成。

---

## 16. Codex 提示词骨架（按执行顺序，冷启动自包含）

**使用说明**：本节为**详版提示词，逐字可发**——每批开工时把对应骨架原文交给 Codex（或指明"执行第 16 节骨架 N"），Codex 照单执行，不需要现场设计。仅【待填】处由上一批产出决定，届时由 Claude 在对话中给差量。IWD 忠实度微门（另有独立提示词与用户待拍板范围）与本链条独立，可穿插执行，不阻塞骨架 0b--7。每个骨架默认包含以下**通用头**，不再逐条重复：

> 通用头：先按 docs/handoff/READ_ME_FIRST_FOR_AGENTS.md 清单读强制入口，再精读本文档第 0、2、13、14、15 节与 HANDOFF.md 变更日志 2026-07-12 全部条目。环境：M1、金标准 Python 3.13 + NumPy 2.3.5、PYTHONHASHSEED=0、最多 3 workers。算法：公开名称为 TVCI-ALNS（兼容机器标识 `staged_hybrid_carbon_aware`，tag e2-submission-20260711，commit 0124623e），每次严格 4000 次完整评价，统一共同起点。场景：280 kWh 主场景、L-main v3 算例。铁律：不改 cost.py/check.py/evaluation.py；不改场景/预算/算法身份；四件套+report+保存解+hash；任一行违规或未满预算立即 HALT 报告；拿不准停下问；完成后一屏汇报（完成数/失败数/关键数字/按第 14 节的判读结论）；结果绑 commit，同步 HANDOFF 变更日志。

### 骨架 0：WP0 合同冻结与地基（已发 Codex，此处存档备查）

前置：E2 已封存（tag e2-10seed-final-20260712）。任务：A. submission_contract 按第 0 节五项决定冻结为 FROZEN_BY_USER（c^tr=0 主口径+摩擦轴内存覆盖、归属映射 1425 条 hash 冻结、配额=0、280 主场景），跑 4 项合同测试；B. prices.py c_tr 95→0 四同步（代码默认+注释留痕+TeX 参数表+HANDOFF），回归测试，抽 2--3 个 E2 冻结解复算证明逐位一致；C. TeX 欠账登记文件（清单见骨架 7）；D. E0 复核（九算例 manifest/hash/入口/共享起点零违规）；E. 看板重基线化。验收：合同 FROZEN+hash 落盘、四同步同一提交、复算一致证明、E0 九项过、全部提交推送。

### 骨架 0b：合同公平条款修订（WP0 补丁，E3 开跑前必做，预计 ≤1 小时）

**前置及其验证**：骨架 0 已由 Codex 完成——验证方法：确认合同判决为 `E1_E7_CONTRACT_FROZEN_BY_USER`（产物在 `baselines/contract_audit/` 下 WP0 相关目录），且该冻结版仍写着旧条款"公平仅 E6/E7 搜索臂启用"。正是这句要改。

**背景**：用户 2026-07-12 拍板（本文第 15 节第 2 条）：公平启用范围扩大到全部"系统方案"实验。WP0 冻结在拍板之前，须打补丁。

**任务步骤**：
1. 定位 WP0 冻结的合同文件（`baselines/contract_audit/` 下状态为 FROZEN_BY_USER 的合同 JSON/MD）。
2. 修订公平条款为：公平约束（θ 下界，eq:fairness）在以下实验中**从搜索阶段启用**——E3 的 M5 层、E3b 摩擦轴全部行、E4 Part B 重优化、E6 全部行、E7 四臂全部会话；E3 的 M0--M4 为机制隔离层，按消融设计保持关闭并在论文明写；E2（810 行）保持封存，注明其为"松弛口径下的算法能力比赛"。
3. 若 `solver/src/setp_solver/search/submission_contract.py` 的机器闸门含"公平仅 E6/E7"的硬编码校验，同步修改该校验及其 4 项测试的期望值；**不改** cost.py / check.py / evaluation.py。
4. 重算合同 hash 并落盘（排除 AppleDouble）；跑合同 4 项测试及受影响回归。
5. 提交推送到 `codex/reporting-pipeline`；HANDOFF 变更日志追加一条，注明修订依据=总设计第 15 节第 2 条、用户 2026-07-12 拍板。

**验收**：新合同文本含上述五处启用点与两处例外；hash 更新；测试全过；提交号在案。
**边界**：只动合同文本、闸门校验、对应测试期望；不动 E2 封存件与 WP0 其他产物。
**汇报模板**：一屏——合同文件路径 / 新 hash / 测试结果 / 提交号。

### 骨架 1：E3 机制阶梯批次（主梯+缩减梯+摩擦轴+零搜索审计包，预计一个工作日含验收）

**前置及其验证**：骨架 0b 提交号存在；冻结合同文本含新公平条款且 hash 一致；E0 复核记录在 `baselines/contract_audit/wp0_e0_check_20260712/`。三者任缺即停。

**产物目录**：`baselines/e3_ablation/e3_v2_<开工日期YYYYMMDD>/`，下设 `preflight/`、`formal/`、`audits/`。并发安全规则照第 2 节：每 worker 只写 `.tasks/<run_id>.json`、`solutions/<run_id>.json`、`logs/<run_id>.log`，全部结束后单一汇总器生成 `raw_runs.csv` 与统计；禁止多进程写同一 CSV。断点续跑必须支持。

**任务一（runner 改造 + 200 评价接线短门）**：
1. 复制 `baselines/e3_ablation/m1_e3_cumulative_ablation_runner.py` 为 `e3_v2_runner.py`；旧 30 行证据目录 `baselines/e3_ablation/e3_submission_20260711/` 保持只读，不覆盖。
2. M0--M5 层语义与旧 runner **逐字一致**：M0=独立经营（按归属拆两个车场子算例、总预算均分、零 EV 电网碳、无碳交易、无公平）；M1=+协同；M2=+日均因子电网碳核算（mean_gamma）；M3=+真实 48 槽时变碳与择时充电（actual_gamma）；M4=+线性碳交易（quota=0）；M5=+θ=1 收益公平（搜索期启用）。
3. 只改三处：①跨场费全层取冻结合同值 0（prices.py 新默认已是 0，runner 内显式断言 合同值==prices 默认==0）；②归属映射读冻结 hash 的 `customer_owner_manifest.json`，metadata 记录该 hash；③M5 公平必须在搜索期真实生效——证据字段 `fairness_search_active_evidence`＝搜索中因公平被拒的候选计数或约束激活日志摘要，不得为空。
4. 新增两处：④100c 算例支持（`models/data_bundle/generated_instances/L-main/L-main-threeshift-100c-01/`，实际 221 客户）；⑤`--cross-site-fee` 参数，实现为 `dataclasses.replace(DEFAULT_PRICES, c_tr=x)` 内存覆盖，不改 prices.py。
5. 场景：280 kWh 任务内覆盖（`dataclasses.replace(..., B_battery_kwh=280.0)`；prices.py 默认 B=80 不动）；每行记录 `battery_kwh=280`、`scenario_role=main_280`。
6. 短门：200c seed1 上 M0 / M5 / 费率 95 覆盖各 1 次 × 200 评价。只验五件事：入口正确、EvalBudget 计数=200、四件套与解文件落盘、断点续跑可用、M5 公平证据非空且费率覆盖真实进入成本（发生跨场时 cost_transship>0）。判决写 `E3_V2_PREFLIGHT_READY` 或 `HALT_<原因>`；不过即停。

**任务二（主梯）**：`L-main-threeshift-200c-01`（449 客户）× M0--M5 × seeds 1--5 = 30 次，每次 4000 评价。
**任务三（缩减梯）**：100c × {M0, M1, M3, M5} × seeds 1--5 = 20 次。
**任务四（摩擦轴）**：200c × M5 口径（公平开）× `c_tr ∈ {10, 25, 50, 95}` × seeds 1--5 = 20 次；费率 0 直接复用任务二的 M5 行，汇总表中该 5 行标 `reused_from=<主梯run_id>`。

**任务五（零搜索审计包，`audits/` 下四份小报告，可与搜索并行占用第 4 个进程）**：
1. **车场桩数正式审计**：输入=`baselines/e2_alns/e2_submission_20260711/` 与 `baselines/e2_alns/e2_final_10seed_20260711/` 全部保存解 JSON。口径：充电动作按趟（vehicle_id 全称如 `EV5#T2`）归属其 route 的 home_depot_id；同一物理车前缀（`EV5`）的充电区间 `[charge_start_second, +occupancy_minutes×60]` 不得互相重叠——若发现重叠，即为快速审计"峰值 21 > 车队 14"反常的候选解释，逐案列出并说明是数据语义还是检查器盲区。输出：每实例×每车场的峰值同时充电台数、隐含峰值功率（台数×22 kW，注明功率假设）、峰值/EV 车队比；与行业分散系数 0.5--0.7 对照写一句判读。
2. **公共站使用统计**：`baselines/e2_alns/e2_80k_robustness_20260711/formal/checkpoints/`（24 解）对比上述 280 两目录全部解；station_id 不以 `D` 开头的充电动作的次数、能量、实例分布。
3. **车辆跨趟换场统计**：物理车前缀跨趟 home_depot 变化的车次数；280 冻结全量 + 本批 E3 各层同口径；预期 M0=0（独立经营对照），如实判读。
4. **分箱稳健复查**：输入=`baselines/e2_alns/e2_280_fleet_charging_audit_20260711/` 的 45 组 aware/naive 配对；把 48 槽曲线相邻两槽平均为 24 槽（60 分钟）重算两臂加权碳强度与节省额；输出"方向是否不变"判读。

**记录字段**：沿用旧 E3 的 39 列（清单见第 17 节词典）＋新增 `cross_site_fee_gbp`、`fairness_enabled`、`fairness_search_active_evidence`、`scenario_role`、`machine=M1`、`source_commit`。
**统计**：主梯逐层同 seed 配对，Wilcoxon 符号秩＋Holm 校正（实现参照 `baselines/e2_alns/e2_final_10seed_runner.py` 统计段，scipy 1.16.3）；同时报均值±std 与效果量。

**验收清单（逐项打勾）**：
- [ ] 70/70 行 status=OK、actual_evals=4000、violation_count=0；
- [ ] 保存解经当前 checker 复算，成本差 ≤1e-6；cost_component_error≈0（分项闭合）；
- [ ] M5 与摩擦轴各行 fairness 证据非空；
- [ ] 结构稳定性行进 report（EV 路线份额 / cross_site_customer_count / 移动充电量的跨种子均值±std）；
- [ ] T5 阶梯初稿（M0--M5 × 指标列，含"相对上一层Δ%"）；摩擦轴三列初稿（fee / 跨场数均值 / 总成本均值）；
- [ ] 审计包四份报告齐；
- [ ] 每次运行真实墙钟入 metadata（供 E4--E7 排期校准）。

**判读**：只用第 14 节 E3/E3b 标准与第 4 节预注册话术。
**汇报模板**（一屏）：完成 x/70、失败 y；主梯 200c 阶梯均值 M0→M5 及逐层Δ%；跨场客户数 M0/M1/M5；摩擦轴方向；缩减梯与主梯方向一致性；公平激活 x/25；审计四项各一句结论；第 14 节判读结果；异常与待用户决策。

### 骨架 2：E2b 四臂算法消融（算法创新边界验证账，预计 1.5--3.5 小时搜索+验收）

**前置**：骨架 1 收口（共享起点与冻结算法一致即可，可与骨架 3 穿插排队）。
**产物目录**：`baselines/e2_alns/e2b_four_arm_<开工日期YYYYMMDD>/`，并发与落盘规则同骨架 1。

**四臂精确定义**（每臂 metadata 写明实现方式，并留算子调用台账——destroy/repair 计数、阶段切换记录——证明臂身份真实）：
- A1 普通独立 ALNS：`setp_solver.algorithms.resetp_alns` 默认配置直跑，无分阶段、无强重组桥、无择时充电。
- A2 直接强重组：同 4000 预算内全程使用强重组内核（若无现成入口，用 staged 入口按 0+4000+0 预算比例实现，metadata 注明）。
- A3 分阶段混合：`run_staged_alns_lns_hybrid()`（400+3200+400），充电=立即（immediate）。
- A4 = A3 + 碳感知择时充电（正式 E2 算法身份）。

**矩阵**：{100c, 200c} × 4 臂 × seeds 1--5 = 40 次 × 4000 评价（机时紧则先跑 200c 的 20 次并声明）。**口径**：280 覆盖、c_tr=0、**公平关闭**——本批是算法能力消融，与 E2 封存比赛同松弛口径，metadata 与 report 明写此点及理由（与 E2 可比）。共同起点、同 seed 配对。

**统计**：A4 分别对 A1/A2/A3 做同 seed 配对 Wilcoxon＋Holm；输出各臂均值±std、相对 A4 的 Δ%、平均墙钟。

**验收**：40/40（或 20/20）满预算零违规复算一致；台账证明四臂身份互不混杂；算法消融附录表初稿落盘。
**判读边界（写作硬门）**：结果只能支撑"分阶段预算组织＋固定路线碳感知充电重调度这两件组合有效"的**组合式创新**口径；禁止写"独创 ALNS"；A1→A4 非单调时如实报告，不挑臂。
**汇报模板**：完成数；四臂两规模均值表；A4 对三臂的配对结论（胜平负+p 值）；判读一句话。

### 骨架 3：E4/E5 碳批次（碳价×充电时机+时间盲区+波动日，预计半天到一天含验收）

**前置及其验证**：骨架 1 收口（E3 v2 的 M5 保存解存在且复算通过——主打数据源）；骨架 0b 完成（Part B 公平开）。
**产物目录**：`baselines/e4_carbon/e4_v2_<开工日期YYYYMMDD>/{part_a,part_b,part_c}/`。

**Part A（全零搜索复算，约 1--2 小时）**：
1. **aware/naive 主口径**：输入=E3 v2 的 M5 保存解（200c×5 seeds + 100c×5 seeds = 10 组）。方法=固定客户分配、路线顺序、车辆类型、总充电电量，只把充电时刻在允许窗口内重排，两臂=立即充电 vs 碳感知择时（复用 `baselines/e2_alns/e2_280_fleet_charging_audit_20260711/` 的既有复算工具链）。每组输出：两臂加权碳强度（gCO2/kWh）、E_ev_indirect、移动充电次数/能量、成本差、可行性复核。**广度附录臂**：旧 45 组 E2 配对原样重引（标注 `relaxed_contract`）。
2. **时间盲账面 vs 真实**：同批解的立即充电臂，分别按"当日日均因子"（从 `data/Carbon/时变碳强度/Carbon_Intensity_Data.csv` 取 2025-11-13 的 48 槽实际值求均值，约 110.5 gCO2/kWh，程序现算勿写死）与"48 槽分时因子"记账；输出账面低估% = (分时真实−日均账面)/日均账面，正式化此前"约 16%"的初步估算。
3. **碳账户小表**：基准排放定义先查 `docs/paper_submission_final/paper_main.tex` 的 CE 定义并锁死（自然候选=E1 的仅油车反事实排放，若 TeX 无明确定义则停下报告待用户拍板）；配额 ∈ {0.8, 1.0, 1.2}×基准 × {立即, 择时} → 碳交易净支出/净收益矩阵（`p_car × (E − 配额)`，纯算术）。
4. 骨架 1 任务五若有未完成项并入本批。

**Part B（碳价扫描重优化，约 0.5--1.5 小时）**：`carbon_price ∈ {0.02517, 0.05034, 0.10068}`（即现值 0.05034 £/kg 的 0.5/1/2 倍，dataclasses.replace 内存覆盖）× 200c × **M5 口径（公平开）** × seeds 1--5 = 15 次 × 4000 评价（机时富余再加 100c 15 次）。记录字段=E3 列＋充电时段重心（对应 T7 样板列）、搬动电量占比、公共站充电次数、EV/CV 客户与距离份额。
**Part C（波动日，零搜索，分钟级）**：从同一 CSV 取 2025-11-07（最平且脏 169--222，1.3x）/ 11-13（锚定 49--199，4.1x）/ 11-17（最波动 48--215，4.5x）三日各自的 48 槽实际值构造情景曲线（对齐方式与锚定日相同，metadata 明确标注"数据级情景、真实历史日"）；对 Part A 主口径 10 组解 × 3 曲线做固定路线择时重调度；广度臂 45 组 × 3。输出：每日的择时节省%与该日摆幅的对应关系。

**验收清单**：
- [ ] Part A 主口径 10/10 两臂零违规、路线/车型/总电量逐位一致、择时臂降碳方向复现；
- [ ] 时间盲低估%正式数字落盘；碳账户矩阵落盘（或 HALT 待基准定义）；
- [ ] Part B 15/15 满 4000 零违规、公平激活证据非空；
- [ ] Part C 单调性判读（第 14 节 F-E4c 标准：平稳日≈0、摆幅越大节省越多）；
- [ ] F4 / T6 / T7 / F5 / F-E4c / T-E4d 六件展品的初稿数字齐（字段对齐第 9 节样板契约）。
**判读**：第 14 节 F4 / T7-F5 / F-E4c 标准；Part B 按预注册双写法出结论（响应显著=碳价驱动移峰；响应平=择时近乎免费的减排手段），不许第三种说法。
**汇报模板**：Part A 主口径两臂强度与降幅；账面低估%；碳账户翻正/翻负点；Part B 三档响应方向；Part C 三点排列；六展品初稿数字位置；判读结论；异常。

### 骨架 4：E6 公平批次（绑定聚焦，预计 1--2.5 小时搜索+验收）

**前置及其验证**：骨架 1 收口。Stage A 零成本取数：从 E3 v2 主梯 **M4 行**（全机制、公平关）的 `profit_ratios_json` 计算各 seed 的天然最小公平比 r0，报 r0 的中位/最小/最大；独立基准收益复用 E3 v2 **M0 行**各场收益（同 seed 配对使用，M0 基准必须满足 Π⁰>0，不满足的 seed 按论文规则将公平降为评价指标并如实记录）。
**产物目录**：`baselines/e6_fairness/e6_v2_<开工日期YYYYMMDD>/`。

**θ 网格构造规则（预注册，禁止事后加点）**：`{0.90, 0.95, 1.00} ∪ {round(r0_med, 3), round(r0_med+0.01, 3), round(r0_med+0.02, 3)}` 去重升序；若 r0_med ≤ 1.00，上探点取 {1.01, 1.02}。具体数值由 Claude 在对话中按此规则给差量【待填】。
**矩阵**：约 6 档 × 200c × seeds 1--5 ≈ 30 次 × 4000 评价；M5 口径，**公平从搜索阶段启用**并逐行留 `fairness_search_active_evidence`。

**记录字段**：θ、可行/不可行（不可行行保留 NOT_FOUND 原因与搜索证据，不得静默丢行）、每场收益比、min 比、总成本、总碳、跨场客户数、**绑定标记**（=最优解处公平约束等式近似成立，或搜索期因公平拒绝计数显著>0）、E3 通用列。
**统计**：相邻 θ 档同 seed 配对成本差；对"公平关（M4）"基线的 Δ%。

**验收清单**：
- [ ] 全部行满 4000（不可行行除外，其证据齐全）；
- [ ] 公平激活证据逐行非空；
- [ ] T8 初稿（θ 档 × 可行率/成本均值±std/Δ% vs 公平关/实测 min 比/跨场数/绑定三态）；
- [ ] F6 初稿数字（θ--成本曲线 + 不可行档 + r0 标注值）。
**判读**：第 14 节 F6/T8 标准——三区可见、至少一档真实绑定；全区间平坦时按预注册正面写法"公平约束天然满足、协同代价为零"，配 E3 摩擦轴交叉解释；**不许加密网格扫到"好看"为止**。
**汇报模板**：r0 分布；网格取值；各档可行率与成本均值；绑定出现在哪档；判读结论；异常。

### 骨架 5：E7 动态批次（四臂压力测试，预计一整天含验收）

**前置及其验证**：骨架 3、4 收口；骨架 0b 合同含 E7 四臂公平条款；动态实况合同已过（`M1_DYNAMIC_LIVE_STATE_CONTRACT_SUPPORTED`，代码提交 `96cbcbf7`，证据 `baselines/e7_dynamic/m1_dynamic_truth_gate_20260711/`）；看板要求的极小状态门（客户守恒/状态继承/冻结路径不变/最终全量检查四项）在本批短门内一并复验。
**产物目录**：`baselines/e7_dynamic/e7_v2_<开工日期YYYYMMDD>/{event_streams,preflight,formal}/`。会话级并行 ≤2（每会话内部含基准子求解）。

**任务一（事件流生成与冻结）**：100c 实例；5 条固定流，事件配比沿用事件生成器的**最初默认配比**（新增/取消/需求变化，配比数值原样写进 metadata，不引入新配比——用户拍板 P8）；每条流事件明细 JSON + hash 冻结；流与 seeds 1--5 一一对应。变配比/动态度变体不做（挂账）。

**任务二（短门与预算标定）**：流 s1 × 完整滚动臂 × 前 2 个重规划阶段试跑。标定并冻结两个预算：①每阶段评价预算（起点 800；若单阶段墙钟 >5 分钟降为 600；定死后全臂全流统一）【待填：短门实测值】；②公平基准子问题预算=每场每次主预算的 25%（两场合计 50% 附加，实测附加墙钟入 metadata）【待填：短门确认】。同时复验四项极小状态门。短门判决 `E7_V2_PREFLIGHT_READY` 或 HALT。

**任务三（正式，四臂 × 5 流 = 20 会话）**：
- B1 静态后见基线：终局已知全部事件后一次性静态求解；评价预算=阶段数×阶段预算（与滚动臂总预算对齐），用于计算信息成本。
- B2 完整滚动：正常滚动重规划——协同开、择时充电开、公平开。
- B3 关协同：同 B2，但客户只能由冻结归属映射的所属车场服务（M0 语义的动态版）。
- B4 碳盲：同 B2，但每次重规划后充电一律立即充（naive）。
- 全臂公平启用；**公平基准按第 15 节第 4 条**：每次重排前，用"当时已揭示的客户集合"按 M0 同法重算各场独立基准（因果、无前视）；终局报告实际实现收益与"终局揭示集合基准"的比值（事后结算口径）。
- 每臂每流同 seed、同事件流 hash、同阶段预算。

**记录字段（逐阶段 + 终局）**：阶段号、事件数（新增/取消/变更）、冻结路线数、阶段成本、累计成本、累计碳（两层）、动态新增订单被跨场吸收的数量与比例、各场收益与 min 公平比、公平基准值与激活证据、充电动作数与充电时段加权碳强度、未服务客户数、阶段墙钟；终局另记：信息成本=B2 成本−B1 成本（绝对与百分比）、守恒审计结果（全客户=已服务∪待服务∪合法取消）。

**验收清单**：
- [ ] 20/20 会话零违规、**零客户丢失**（丢失=HALT 非降级）、守恒审计全过；
- [ ] 各臂身份证据（B3 无跨场、B4 充电全立即）；
- [ ] 预注册假设 H 判定：B2 的动态订单跨场吸收率 vs E3 静态解的跨场占比；
- [ ] T9 四臂初稿（臂 × 成本/信息成本/排放/跨场吸收/公平比最低/低碳充电占比/守恒）；F7 代表流（默认 s1，若 s1 无跨场吸收案例则选最先出现者并声明）逐阶段数据；
- [ ] 三交互逐项判读（第 14 节 F7/T9 标准）。
**判读与降级**：交互不成立按第 7 节卡预注册话术逐项收缩；最坏写"滚动接口+已成立交互"，标题措辞交用户。
**汇报模板**：20 会话状态；四臂总成本与信息成本；H 判定；三交互各一句（协同=B2vsB3、碳=B2vsB4、公平=逐阶段 min 比曲线）；判读结论；异常。

### 骨架 6：表图统一生成批次（预计 2--3 天，7/25--27）

**前置及其验证**：E3--E7 全部批次进入 ACCEPTED 或 HALT（看板为准）；各批 formal 目录四件套齐。
**工具与流程**：以 `solver/src/setp_solver/reporting/design_templates.py` 与其 runner 为基础，把数据源从 `mock_data/` 切换到各批 formal CSV（数据源目录映射：T3/F2←E2 封存 formal；T4/F1←E1 冻结证据；T5/F-E3/F-E3b←`e3_v2_*`；F4/T6/T7/F5/F-E4c/T-E4d←`e4_v2_*`；T8/F6←`e6_v2_*`；T9/F7←`e7_v2_*`；算法消融附录←`e2b_four_arm_*`）；**壳的视觉参数一律不改**。先输出到草稿目录 `draft_<日期>/` 交用户逐图审，批准后才替换 `docs/paper_submission_final/generated_tables|generated_figures/`。

**逐项要求**：按第 9.1 节判定表逐张执行；T3 删"相对已观测最优偏差%"列与"等墙钟成绩"占位列；F4 渲染 bug 修复（旧 `figures.py` figure_f4 函数 97 行数据不渲染的问题）；新增四件（F-E3/F-E3b/F-E4c/T-E4d）先补 mock 契约行与 `DATA_CONTRACT.md` 条目再画；标题去内部编号（F3/F5b 等字样不得出现在图题）；机制名标准化（术语对照第 17 节词典）；单位/轴范围/步进按第 14 节"趋势显著"要求与既定施工细节（F1 坐标缩放、F2 步进、大算例可换小算例演示）；每个进论文的数字建 DATA_CONTRACT 溯源行（数字→CSV 行→算例→口径→commit）。

**验收清单**：逐张对照第 9 节判定 + 第 14 节判读；XeLaTeX 编译通过；旧数字零混入（每张图表的来源 commit 校验）；草稿包+用户批准记录+正式替换三步齐。
**汇报模板**：张数完成/待审；每张一句判读；DATA_CONTRACT 行数；编译结果。

### 骨架 7：写作欠账批次（非实验，7/28 起，预计 3 天）

**输入**：`docs/handoff/tex_sync_debt_20260712.md`（WP0 已建的登记文件）＋本文第 15 节第 5 条＋下方清单。**操作方式**：每条改动单独小提交，报改动前后位置；不动模型公式与实验数字。

**任务清单（均已拍板，逐条销账）**：
1. 参数表 B=280 主场景说明（80 保留为文献基准对照，说明证据链来源）；
2. 配额=0 口径统一＋删除全文"配额变化影响路线"表述＋配额三旋钮故事句（配额定收支、碳价定力度、时间分辨率定行为）；
3. 跨场费 0 主值＋敏感轴说明（95=无来源历史代理，仅摩擦敏感性档位）；
4. 客户归属合成独立经营基线假设句（1425 条映射规则+hash 引用）；
5. 全文"动态优化/前瞻"→"滚动重规划"，预判式决策入展望（与 DR-ALNS 并列）；
6. 车辆池治理段：公平约束＋摩擦敏感性=防止单车场被掏空的两道闸（引 E6/E3b 实测数字）；
7. 退化关系说明段：关机制→多车场带时间窗多趟 VRP→单车场 VRPTW，NP-hard 依据；E3 M0 与 E1 反事实为操作层退化检验；
8. 车场充电引用交代段：车场充电主干=行业实践+文献惯例（引 Froger 容量受限 CS 论文与行业分散系数 0.5--0.7），本文隐含桩数审计数字 X 台/占车队 Y%，公共站按每站 1 桩从紧建模；车场电网功率上限=未来工作；
9. 公共站场景依赖洞察句：80 kWh 场景公共站真实使用（15/24 解、45 次）vs 280 主场景车场主导——电池容量决定公共充电依赖；
10. 共享充电站容量措辞降级：只写"建模并核验"，不写"实验证明其作用"；
11. 4000 完整评价 vs 文献约 25000 迭代（多为增量评价）的汇率差声明；
12. 实验环境声明（M1/Python/NumPy/种子策略，可选附录，注明中文期刊不常见）；
13. 管理启示章按 E4 新故事重写（碳价杠杆=充电调度；时间盲区政策含义；波动日前瞻）；
14. 摘要与算法章按公开名 **TVCI-ALNS**（时变碳强度引导的自适应大邻域搜索）定稿，内涵="分阶段 ALNS--LNS 混合求解框架＋固定路线碳感知充电重调度"（强度视 E2b 结果，禁越 B4a 边界，禁"独创 ALNS"）；
15. 实例表述按实际客户数（22--449），不用 10..200 内部标签；D=2 车场在限制段明写；
16. 基线表述改口：IWD 判 `IWD_FIDELITY_GATE_FAIL`——全文改为"八个健康文献基线＋IWD（简化适配、标注失活）"，算法对比强主张限定在其余八基线，引 `docs/handoff/iwd_fidelity_closeout_20260712.md`。
**验收**：逐条对照第 0/13/15 节拍板记录销账，无一遗漏、无一越界；latexmk 编译过；引用完整。

---

## 17. 词典（冷启动一站查全：环境/锚点/数据/入口/目录/字段/判决词/术语）

### 17.1 环境与机器
M1 = 8 逻辑核、8GB 内存（当前主运行机，用户偏好 E3--E7 尽量全在 M1）。金标准解释器 `/opt/anaconda3/bin/python3.13`＋NumPy 2.3.5＋SciPy 1.16.3；一律 `PYTHONHASHSEED=0`。并行：搜索批 ≤3 workers；E7 会话级 ≤2（会话内含基准子求解）；审计/回放可占第 4 进程。x86（16 线程 16GB）只作溢出：一张表一台机器、跨表只谈%、每表记出身。

### 17.2 冻结锚（tag / commit）
- 正式算法：`staged_hybrid_carbon_aware`，tag `e2-submission-20260711`，commit `0124623e`（一切机制实验从此调算法）。
- E2 封存：tag `e2-10seed-final-20260712`，commit `1180abf3`，判决 `E2_10SEED_PRIMARY_LEAD_SUPPORTED`；16000 预检 `E2_16000_PREFLIGHT_SUPPORTED`（补充证据，不入主表）。
- 动态实况合同：commit `96cbcbf7`，判决 `M1_DYNAMIC_LIVE_STATE_CONTRACT_SUPPORTED`。
- 80k 充电窗修复：tag `e2-80k-charging-window-fix-20260711`，commit `40bd2883`。
- 投稿合同：`E1_E7_CONTRACT_FROZEN_BY_USER`（WP0 冻结；公平条款须经骨架 0b 修订）。

### 17.3 算例与数据
- 正式算例注册表：`models/data_bundle/generated_instances/L-main/resetp-l-main-main-benchmark.v3.json`（九档三班倒；标签 10..200，实际客户 22/34/45/55/114/163/221/322/449）。机制实验统一用 `L-main-threeshift-100c-01`（221 客户）与 `L-main-threeshift-200c-01`（449 客户）。
- 碳强度：`data/Carbon/时变碳强度/Carbon_Intensity_Data.csv`（英国国家级真实数据，2025-11-01..30，30 分钟 48 槽/日）；锚定日 2025-11-13（49--199 gCO2/kWh，4.1x）；波动日轴用 11-07（169--222，1.3x）/ 11-13 / 11-17（48--215，4.5x）。
- 客户归属：`customer_owner_manifest.json`，1425 条，规则 `nearest_depot_by_bundle_distance`，hash 已冻结；论文称"合成独立经营基线"。
- 场景：主场景 280 kWh 为**任务内覆盖**（`dataclasses.replace(DEFAULT_PRICES, B_battery_kwh=280.0)`）；`prices.py` 默认 B=80 **不改**；`c_tr` 默认已改 0（WP0 四同步）；`carbon_price=0.05034 £/kg`；`Q_capacity=3650 kg`；`v=25 m/s`；配额=0。

### 17.4 代码入口
- 正式算法：`run_staged_alns_lns_hybrid()`（400+3200+400）；独立包 `setp_solver.algorithms.resetp_alns`。
- 裁判三件（禁改语义）：`cost.py::evaluate()`、`check.py::check_solution()`、`search/evaluation.py::EvalBudget`。
- 共同起点：`make_shared_initial_solution(bundle)`。
- 充电碳：`cost.py::charging_slot_breakdown`（整段按槽重叠秒积分，跨午夜 cyclic）、`carbon_slot_index`、`is_overnight_depot_charge`。
- 收益：`profit.py::calculate_depot_profits`（收入随实际服务车场、成本按趟归集）、`infer_customer_home_depots`。
- 动态：`run_rolling_reoptimization`（滚动重规划，正常阶段走 resetp_alns）。
- 合同闸门：`solver/src/setp_solver/search/submission_contract.py`（4 项测试）。
- 统计实现参照：`baselines/e2_alns/e2_final_10seed_runner.py` 统计段（Wilcoxon+Holm+并列名次）。
- E3 母本：`baselines/e3_ablation/m1_e3_cumulative_ablation_runner.py`。
- 表图：`solver/src/setp_solver/reporting/design_templates.py`＋`docs/paper_submission_final/design_templates/`（样板包=底版）。

### 17.5 证据目录（只读区 vs 新建区）
只读封存：`baselines/e2_alns/e2_submission_20260711/`（270 行）、`e2_final_10seed_20260711/`（810 行）、`e2_16000_preflight_20260712/`、`e2_80k_robustness_20260711/`（80k 敏感性，公共站证据）、`e2_280_fleet_charging_audit_20260711/`（45 组 aware/naive）、`baselines/e3_ablation/e3_submission_20260711/`（旧 30 行，收费口径，作废只读）、`baselines/e7_dynamic/m1_dynamic_truth_gate_20260711/`、`baselines/contract_audit/wp0_*`。
新建区（本链条）：`e3_v2_<日期>/`、`e2b_four_arm_<日期>/`、`e4_v2_<日期>/`、`e6_v2_<日期>/`、`e7_v2_<日期>/`、表图 `draft_<日期>/`。

### 17.6 raw_runs 字段词典（E3 母本 39 列＋v2 新增）
`E_cv_direct` 油车直接排放 kg；`E_ev_indirect` 充电间接排放 kg；`E_total` 总排放；`actual_evals` 实际评价数（必须=eval_budget）；`battery_kwh` 电池覆盖值（280）；`carbon_quota_kg` 配额（0）；`charging_action_count` 充电动作数；`charging_actions_moved` 择时移动数；`cost_carbon` 碳交易成本；`cost_component_error` 分项闭合误差（≈0）；`cost_elec` 电费；`cost_fix` 固定派遣费（按趟）；`cost_fuel` 燃油费；`cost_km` 里程费；`cost_occ` 占桩费（车场=0）；`cost_transship` 跨场服务费；`cross_site_customer_count` 跨场客户数；`cv_route_count` 油车趟数；`distance_total` 总里程；`elapsed_seconds` 墙钟；`electricity_kwh` 总充电量；`ev_route_count` 电车趟数；`eval_budget` 预算（4000）；`head` 运行 commit；`instance` 算例 ID；`min_profit_ratio` 最小收益比；`naive_same_route_E_ev_indirect` 同路线立即充电对照排放；`naive_same_route_electricity_kwh` 对照电量（须与主臂相等）；`reason` 失败原因；`route_count` 总趟数；`route_structure_signature` 路线结构签名；`run_id` 行 ID；`seed` 种子；`solution_json` 解文件路径；`status` OK/HALT；`total_cost` 总成本；`variant` M 层；`violation_count` 违规数（必须 0）；`profit_ratios_json` 各场收益比。
v2 新增：`cross_site_fee_gbp`、`fairness_enabled`、`fairness_search_active_evidence`、`scenario_role`、`machine`、`source_commit`；E4 增 `charging_center_of_gravity_h`（充电时段重心）、`shifted_energy_share`（搬动电量占比）、`public_charging_action_count`；E6 增 `theta`、`binding_state`（松弛/绑定/不可行）；E7 逐阶段字段见骨架 5。

### 17.7 判决词与状态词
`OK`=行级正常；`HALT_<原因>`=停批不包装；`*_SUPPORTED / *_REJECTED / *_READY`=门级判决；`NOT_FOUND`=搜索无可行解（**不是**不可行证明）；`reused_from`=复用行标记；看板态 `ACCEPTED / HALT / ARCHIVE`（ARCHIVE 只可参考不可作新结论）；`relaxed_contract`=E2 松弛口径标签；禁止发明中间词（如"基本通过""接近显著"）。

### 17.8 术语标准化（表图与正文用词）
消融层：完整模型 / 无收益公平 / 无碳交易 / 无时变碳（日均核算）/ 无电网碳核算 / 独立经营基线（M5→M0 的标准称谓，禁内部代号）。方法词：滚动重规划（不用"动态优化/前瞻"）；合成独立经营基线归属；**算法公开名=TVCI-ALNS（Time-Varying Carbon-Intensity-Guided ALNS，时变碳强度引导的自适应大邻域搜索）**——对应"分阶段 ALNS--LNS 搜索内核＋固定路线充电时刻碳感知重调度"，机器标识 `staged_hybrid_carbon_aware` 只在代码/封存数据沿用，论文与新展示一律用 TVCI-ALNS；禁"独创 ALNS"、禁"全联合碳搜索"；松弛口径（E2/E2b 公平关）。基线表述：IWD 已判 `IWD_FIDELITY_GATE_FAIL`——论文写"八个健康文献基线＋IWD（简化适配、标注失活）"，不得写"九个忠实/健康基线"。图表编号 F/T 系内部标签，正式图题不得出现。

### 17.9 统计与汇报约定
同 seed 配对优先；Wilcoxon 符号秩（`scipy.stats.wilcoxon`）＋Holm 校正多重比较；报均值±标准差、胜平负、效果量、n；p 值只辅助。正文放代表例、附录放全量、结论以全量为准；方向不一致的指标不作结论。每批汇报一屏（模板见各骨架）。

### 17.10 已知未决与【待填】清单
- ~~IWD 忠实度微门~~ **已收口**（2026-07-12）：修复候选两轮 18/18 满预算零违规但 P3/P4/P5 未过，判 `IWD_FIDELITY_GATE_FAIL`；不重跑 IWD 90 行、不动 E2 封存；论文措辞改口（见 17.8 与骨架 7 第 16 条），报告 `docs/handoff/iwd_fidelity_closeout_20260712.md`。
- 算法公开名 TVCI-ALNS 已冻结（2026-07-12）：只影响展示字段与论文措辞，封存数据机器标识不变。
- Goeke 公开标准算例可行性评估：7/24 前交割，做不完明确降级（合同既定）。
- 碳账户"基准排放"定义：骨架 3 内置停止点，查 TeX 后锁死，无定义则报用户。
- 【待填】E6 θ 网格具体值（骨架 4 规则+E3 M4 的 r0）；【待填】E7 每阶段预算与基准子问题预算（骨架 5 短门实测）。
