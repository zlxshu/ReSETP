# FIX-INIT-CLOSURE 任务书：补初始种群结构闭包（发给 Codex，按你的方案 A）

先读 `docs/handoff/codex_standing_charter_20260817.md` 与你自己的
`solver/reports/type_closure_diag_20260817/report.md`。**本件就是执行你提的方案 A。**

## 授权判据

你已证：这是**纯接线**（runner 把 `type_exchange=false` 传给了主路线引擎/机制提案/集成搜索，
**唯独没传进 `build_initial_population()`**，而后者无条件生成并执行
`witness_perturbation_type_exchange`），**不改模型语义**；且探针实测过滤后
**初始种群仍 25/25、无车型/场站闭包违例、多趟保持启用**——第一堵墙不会复发。
故按 P85（有证据的接线修复可直接做）施工，不再报批。C 方案会改实验语义，本件不做。

## 要做的（严格按 A，不扩大）

1. 给 `initialization.py:555-568` 的 `build_initial_population()` 增加**结构闭包参数或回调**；
   在 `:690` 执行 move 之后、充电修复与入群之前，**按休眠项拒绝客户车型/场站变化**。
2. runner `:3249-3267` 传入现有 `mechanism_enabled`。
3. **通用闭包**：车型与跨场站两类都要覆盖（**不要只给 type-flip 打特例补丁**——
   你已证那样会留下跨场站泄漏）。
4. 补两类回归测试：初始化车型闭包、初始化跨场站闭包。

**明确不做**：B 的第二道全局硬检查（防未来泄漏，非本次必需，避免扩大战线）。

## 验收（逐级，任一不过即回滚并 HALT）

1. **闭包探针**：同算例 seed 11、多趟启用、`type_exchange` 与 `cross_depot` 休眠下，
   1 圈 closure probe 通过——**车型闭包、场站闭包、服务客户数、完成需求量四项全过**，
   且初始种群仍达 25/25。
2. **不启用休眠时行为不变**：不传休眠项（即全开）的路径须与当前**逐位一致**
   （200 圈同种子对照，除 wall 字段外全同）。
3. 回归 197 断言不动＋新增两类测试。
4. 五个冻结文件哈希不变（`cost.py` 现行 `13ae664b…`）；不改算例；SC3 默认关。

## 通过后立刻接着做（同一任务内）

闭包过了就**直接跑时变碳三臂**：`asap`／`cost_min`／`cost_plus_carbon`，
统一算例、seed 11、**各 200 圈**，产出每臂总成本、总排放、**充电时刻分布**、
充电电量加权碳强度、电费、碳成本、CV/EV 台数、服务量红线、可行性，
并给**相对 asap 的成本差与排放差**三臂并排表。
**须明写这是 200 圈技术读数、非正式结果**（正式按 P79 十种子）。
若闭包仍失败 → 停在新泄漏路径，**不许跑三臂**。

## 产物

`solver/reports/fix_init_closure_20260817/report.md`，首行 `INIT_CLOSURE_DONE`／`_HALT`、
末行 `INIT_CLOSURE_END`；含 diff、逐级验收表、三臂并排表（若跑到）、四件套、done.json、resume。
单进程串行，单次 ≤10 分钟。结尾按章程答"接下来该干什么"。
