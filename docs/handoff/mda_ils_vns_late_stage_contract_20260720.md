# MDA-ILS-VNS 后期分阶段路线精修合同

- 编号：`MDA-ILS-VNS-S2-001`
- 日期：2026-07-20
- 状态：`USER_AUTHORIZED_AUTONOMOUS_EXECUTION__PREREGISTERED_BEFORE_CODE`
- 前置：强母体基础门PASS；AILS-II强度反馈层STOP并保持冻结

## 1. 为什么只试这一刀

用户要强算法、清楚的机制故事和方便后续E1--E7的结构，不限制算法身份或参数，但要求
最低成本、不要无依据枚举。当前有三条直接证据指向同一刀：

1. 强母体在未见B组三题两种子中对原装PyVRP为5胜0平1负，唯一新增部件是刷新全局
   最好解时调用原生SwapStar。
2. 这六次候选共调用SwapStar 320次，形成84次严格路线级改善，总耗时约0.225秒；
   说明路线级精修有真实增益，且调用成本远低于扰动强度反馈。
3. 陈雨蝶等（2025）原文步骤7只在GA达到`Ps=60%--80%`后接入VNS，以前期广搜、
   后期精修平衡质量和时间。本文不照搬GA，而把同一分阶段思想接到已通过的ILS母体。

因此本门只研究：保留“刷新纪录即SwapStar”，并在搜索进度70%后按固定稀疏节拍额外
调用同一原生SwapStar。没有新档案、种群、接受器、跨语言核心或完整模型模块。

来源：陈雨蝶等（2025），DOI `10.12011/SETP2024-2027`；Vidal（2022）SwapStar，
DOI `10.1016/j.cor.2021.105643`；PyVRP，DOI `10.1287/ijoc.2023.0055`。

## 2. 冻结三臂

|配置|前70%|后30%|
|---|---|---|
|`foundation`|只在刷新最好解时SwapStar|同左|
|`late_vns_500`|同基础母体|除刷新纪录外，每500次常规搜索再SwapStar一次|
|`late_vns_250`|同基础母体|除刷新纪录外，每250次常规搜索再SwapStar一次|

70%取陈雨蝶原文60%--80%的中点。500/250不是无界参数网格：按基础门约1000次/秒和
SwapStar实测耗时估算，30秒开发臂后段只增加约18/36次调用，理论直接开销均低于
0.1秒。结果后不得改起点、频率、算子或增加第四臂。

## 3. 行为与工程门

代码只进入`baselines/algorithm_prototypes/mda_ils_vns_20260720/`，不被正式runner
导入。必须满足：

1. `foundation`在5/100/1000固定迭代下继续与已封存工作器逐位一致；
2. 1000固定迭代下，`late_vns_500`和`late_vns_250`的额外后期调用恰为1和2；
3. 后期调用只接收PyVRP原生路线局部搜索返回值，最终仍由原生ILS评分和接受；
4. 不改评价器、罚项、随机数、普通节点搜索、BKS、算例或保护文件；
5. Ruff、编译、定向测试和代码审计通过。

行为门只允许修实现错误，不允许改第2节算法语义。

## 4. A组有限选择

- PR11A、PR17A、PR21A；seed4；单线程；每臂30秒；
- 三臂同批运行；
- 只在两个后期臂中选择平均BKS gap最低者；
- 放行：相对`foundation`三题总目标严格更低，至少2胜，最多1负。

失败即`STOP_LATE_STAGE_VNS_KEEP_FOUNDATION`，不打开确认题，不救频率。

## 5. 未见确认

仅A组放行后运行此前未打开的PR16A、PR20A、PR24A，种子1--2，每臂60秒，基础母体
与冻结唯一候选同批配对。PASS须同时满足：

- 六对总目标更低，至少4胜、最多1负；
- 最大单对退步不超过0.25%；
- 平均BKS gap改善至少0.10个百分点；
- 完成迭代数候选/母体中位比至少0.98；
- 全部解完整、可行、独立验解，保护文件哈希一致。

任一不满足即STOP，不得在确认题第二发救援。

## 6. 输出与停止点

五件套及逐次JSON进入
`baselines/algorithm_foundation/mda_ils_vns_late_stage_20260720/`。

无论PASS或STOP，本门都不自动运行完整28题、China81、阶段二或修改论文结论。PASS后
先独立审计，再根据效果大小另立长预算BKS门或China81最小探针。
