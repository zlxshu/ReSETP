# China81 非线性充电动态继承 NL3a

日期：2026-07-20

判定：`PASS_NL3A_NONLINEAR_DYNAMIC_INHERITANCE`

动态精确车辆续排中的候选可达电量、最早发车时刻、趟链后向最小电量、动作持续时间、
证书验证和低碳择时已全部改为调用 NL0 唯一曲线内核。新动态动作冻结起止电量和曲线
身份；新动态证书冻结曲线 ID 与参数哈希。

零搜索夹具生成 2 个 NL90 动作，独立时长复算误差为 0；缩短一分钟或删除非线性元
数据均失败关闭。精确动态低碳择时把夹具间接排放从
`1.028968610541679 kg` 降到 `0.12105513065196208 kg`。在充电动作进行中切割时，
车辆只在已认证的充电结束时刻释放，并且整段电量只计一次。

旧动态/E7、静态多趟、NL2 成本检查共 22 项目标回归和 Ruff 通过；源码中原有
`energy/power`、`gap*power`、`battery+time*power` 与旧均匀碳择时调用均为 0。
L100 动作持续时间与历史公式误差为 0。

权威证据为
`baselines/model_verification/china81_nonlinear_dynamic_nl3a_20260720/`。
本门搜索评价为 0，不是算法性能结论。China81 仍需接入 CV/EV 两类车辆、每类
距离/时长/Σ(v²d) 三个有向矩阵；在 81/81 零搜索物理预检和最终失败关闭审计完成前，
`formal_search_allowed=false`。
