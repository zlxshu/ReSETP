# Duty-HGS 公开与私有试错后的 PDCA 重设计（2026-08-09）

- `USER DECISION`：用户要求在公开 28 算例和三地区私有试错效果极不理想后，按“定义范畴—识别问题—查找方案资料—综合统筹—针对性优化—形成新算法”的 PDCA 方式重设计；20 分钟只是充裕硬上限，私有 50—100 客户若仍不能及早稳定收敛，应优先视为性能或收敛设计问题。
- `USER DECISION`：Opus 只在关键疑难处作为顾问；默认 `high`，明显不可靠或疑难问题才可临时升 `extra`，仍不足再升 `max`，不得把有限额度浪费在一般苦力或无限审查。
- `FACT`：公开 28 例为自研 12 胜 16 负；双方均跑满 1200 秒的 13 个大例自研 0 胜 13 负。PR17A 十种子中混合 DCREX+SREX 对 SREX-only 为 5 胜 5 负，平均成本仅好 1.4，平均耗时 2.18 倍。
- `FACT`：成渝中止包 110525 行，第 5 轮 108784 行并接受 370 个充电动作；当前代码在每个接受动作后重新进入 `while True` 教育，并对跨趟站点/电量和跨会话时刻做两层笛卡尔积。主病是重复枚举，不是充电接口完全失效。
- `DECISION`：Codex 与 Opus 辩论后的候选为“快速 SREX+PyVRP 局部搜索的路线主层；公开路线—车场最小费用指派；私有整日任务—实体车辆匹配；电动车整日非线性充电标签 DP；共享桩协调；外层协作、公平和动态未来后缀”。原版 PyVRP 0.12.2 HGS 继续冻结为基线，自研文件独立。
- `DECISION`：DCREX 三个待选处置为 A 完全退出（双方推荐）、B 只用于初始多样化、C 只在原生重启（大规模 20 分钟内基本不触发，不推荐）。代理不得自行处置。
- `BOUNDARY`：完整流程图、数学模型、伪代码、依据和两项选择见 `docs/handoff/duty_hgs_pdca_redesign_for_user_approval_20260809.md`。未改算法、模型、受保护文件、实验或论文正文；施工等待用户批准整体架构和 DCREX 选项。
