# E2 算法排雷与机制展示算例（2026-07-24）

修正后 `cn-prd-50c-01` 十种子中，主方法相对 HGS-M 为 0胜10平。代码与结果审计
确认50客户层的整路线重组基本没有发挥空间，旧图又只保存少量代理目标刷新点，因此
“完全持平+稀疏阶梯”不能作为最终机制展示。

三条算法修复均在看结果前登记并保持每任务15000次HGS迭代、80次完整候选核算和
5秒限时路线池重组。档案中全部已完整核算的路线进入路线池通过开发门：六题中2题
严格改善，0题退步。两段式三视角完整解交换只得到1胜5平0负，未达到至少2胜门；
首次路线重组后回送短搜索为0胜6平0负。后二者均HALT，禁止在已看面板继续调参。

为分开“全量普遍性”和“机制可视化”职责，另立事后披露的100客户机制展示算例
`cn-cy-100c-01-V2-LOCATIONS`。它不是代表算例，也不替代81题统计。使用唯一通过
的档案路线复用版本重跑十种子，主方法相对HGS-F/HGS-E/HGS-M均为10胜0平0负；
平均成本分别为5178.417642、5287.829807、5184.956426、5185.435952元。十次均
严格消耗15000次HGS迭代和80次完整核算，独立复算零差异。

观察重放按各算法“最终成本最接近十次均值，种子并列取小者”的形状盲规则选种子，
得到HGS-F=1、HGS-E=6、HGS-M=10、MV-HGS-SP=10。四条曲线分别有13、16、27、
27个累计更优点，83行图源全部来自搜索中代理最优骨架的事后完整核算；终点逐位等于
十种子结果。图使用普通折线连接原始观测，不断轴、不平滑、不补点、不改坐标轴。

严格边界：该图只说明固定机制展示算例上的搜索过程。正式论文替换前，必须用档案
路线复用版完整重跑修正后China81 E2，并完成独立复算、S3--S5重建和论文文字同步。

## E2-STAGED-PORTFOLIO-BUDGET-RECHECK-001：v5 HALT 后 v6 计数闭合

首次启动因继承模块仍寻找 v2 登记文件而在任务初始化前退出，现场保留；v6 wrapper 已显式绑定 v3，未产生任何任务或成绩。

v5 正式复跑在第13个任务因小规模 cn-cy-10c-01-V2-LOCATIONS/seed1 只产生276次真实完整模型核查而触发280次硬门，HALT记录和已完成任务原样保留。该问题属于候选档案数量不足造成的运行器记账失败，不是可行性、评分或搜索质量结论。

用户批准 v6 机械修订：先记录每任务真实搜索核查数；若少于280，仅对固定的 HGS-F 已保存解重复调用完整模型评价器补足差额。重复核查不生成候选、不进入路线池、不消耗随机数、不改变解或成绩；真实数、补足数、合计数都写入 raw_runs.csv 和任务报告。v6 另立目录并在正式运行前冻结 formal_preregistration_v3.json，任何超过280、固定解不可行、评价异常或哈希漂移立即 HALT。该登记不改变算法或 E2 结论门；独立重放、强度门和 S3--S5 仍须在 v6 后重新执行。

## E2-STAGED-PORTFOLIO-SMALL-ARCHIVE-LEDGER-001：v7 验收修复

v6 retry 在同一 10 客户单元通过预算补足后，又被旧历史档案门停止。隔离复现确认六
阶段迭代、120 个快照、历史引用和四个完整解全部正常；燃油视角第二阶段只有 20 个
不同候选，且 20 个已全部进入档案。旧门仍要求额外差异候选，属于候选空间已穷尽时
无法满足的验收条件。

V7 保留 V6 搜索和预算，只把档案门改为“选入数等于 `min(24, 不同候选数)`”；候选
超过 24 时仍须有差异候选，不超过 24 时必须全部选入且允许差异余量为 0。V5/V6
HALT 不改判，源码安全点为 `cecf30c8`。正式 V7 不复用旧任务；三题预检通过后才可
从零运行 405 任务。

三题预检随后通过：10/100/200 客户任务均完成 280 次登记核查和 120 个历史快照，
四种方法返回的 12 个解均通过完整模型可行性检查。10 客户任务因一个阶段候选空间
穷尽，真实搜索核查为 276、固定解重复核算为 4；100/200 客户任务均为 280 次真实
核查、0 次补足。三题的档案选择均闭合，哈希清单复核无差异，定向回归 16 项通过。
判 `PASS_D6_E2_STAGED_V7_PREFLIGHT`，正式 405 任务现可按冻结 v4 登记从零启动；
预检结果不得复用为正式结果。

预检与冻结后续链已提交为 `9b80ca59`，开跑前标签为
`safety/e2-v7-preflight-pass-before-formal-20260724`。V7 正式批随后从空目录以
3 个并行单线程任务启动，监控状态为 RUNNING；运行中不得根据中间结果修改算法、
登记、论文或后续验收规则。

下游静态接线检查同时发现 S4 v2 会从只含成本和 CPU 的 S3 汇总行读取不存在的
`witness_sha256`。这不会影响正式 E2 搜索，但会让后续零搜索路线明细验收在开始前
报错。未读取任何正式结果行即冻结 S4 v3：固定解的选择规则不变，改为从被选中种子
的封存任务原始行读取解指纹，并先核对 S3 成本与任务成本一致。9 项源指纹、编译、
代码检查和最小模拟接线测试均通过；v2 保留，后续只执行 v3。

同样在未读取正式结果行时冻结新的 S5 零搜索生成器。它只在正式批、1620 解复算、
强度门、S3 和 S4 v3 全部通过后运行；生成全宽三线表、紧凑及 27 层完整汇总、图3
和图4。图4固定使用真实在线完整模型观测、连续坐标和全数据 3% 留白，不断轴、不
平滑、不插值，图例位置由固定最少遮挡规则决定。旧封存数据形状测试、中文字体测试、
编译和代码检查均通过，6 项源指纹闭合。

接力静态检查又修复了一个纯监控接线问题：S4 v3 与 S5 原先没有写完成标记，正常
退出仍会被误判异常。两个零搜索入口现各写 `done.json`，监控据此判完成；算法、
解、成绩、选择和图表规则均不变，登记指纹已同步。

已在不读取正式结果行的前提下冻结等待式自动接力。它不会重复启动正式批，只有正式
405 任务得到指定 PASS 后，才按独立复算、强度门、S3、S4 v3、S5 的固定顺序执行；
每步失败即停止且不救援。接力和各阶段监控、协议指纹已登记。

## 真混合算法共享充电桩容量结果前修复

新 A/B/A+B 算法在真实 G0 和 G1 均未执行时完成第三轮静态排雷。路线局部充电修复
得到的“公共站—日期—半小时时段”占用现在进入有限车队联合分配；同一站同一时段
占用路线数超过 bundle 桩数的组合，不再进入完整评价短名单。该变化不重排充电时刻，
不改成本、物理、完成器或最终检查器，只避免把已知共享桩冲突浪费为正式评价。

原 G1 v1 从未执行并保留。v2 仅登记上述容量预筛，三道开发题、种子1、三臂身份、
80 次完整评价、A 的 75000 次 HGS 迭代、A+B 的 60% HGS 比例、所有破坏修复参数、
接受规则和“至少两题严格优于两个单臂较优者”等硬门均不变。35 项轻量测试通过；
静态 G0 v12 为 14/14，20 项输入和 4 项产物哈希闭合；G1 v2 合同检查通过 31 项
源码和 55 个输入文件。旧 v7 与发布链接力仍运行，真实 bundle G0 尚未执行，因此
新算法搜索次数仍为 0，不支持可行性、性能、1+1>2、BKS 或 SOTA 结论。

为避免旧 E2 长任务释放资源后人工漏接，另冻结零判断的顺序接力。它只等待旧 E2
发布链精确 PASS；随后先跑真实 bundle 零搜索 G0，只有 G0 精确 PASS 才用 3
workers 运行 G1 v2。上游、G0 或 G1 停止都只写证据并终止，不做自动救援。接力
登记绑定 11 项源码/合同指纹，监控保护算法、预登记、成本、检查器、完成器、价格和
主 TeX，自动 AI 关闭。

## 2026-07-25：低成本优先与 G1 六进程结果盲资源门

用户要求优先做最低成本验证，只有通过小门且确有必要才扩大长跑；同时允许在安全
前提下尽量使用 6--8 workers。正在运行的旧 E2 v7 保持三进程合同不变。新算法在
任何真实 G0/G1 结果产生前冻结 v3：旧 E2 完整释放、真实 bundle 零搜索 G0 通过
后，先以六个并行零搜索任务加载三道开发题各两次，完成有限车队解码、完整验解和
PyVRP 投影往返，但不运行算法搜索、不保存目标值。

资源门固定判据为六项全部通过、最低系统空闲内存比例不低于 10%、六进程峰值内存
合计不超过 4096 MB。通过则 G1 用 6 workers，未通过则自动回退 3 workers；这不
改变任何单项的算法、种子、80 次完整评价、HGS 迭代、60/40 分配或性能门。静态
G0 v13 为 14/14，搜索次数 0；35 项轻量测试通过；G1 v3 合同验证 33 项源码和
55 个结果盲输入。

原 v1 等待链始终未越过“等待旧 E2”阶段。因结果前升级为 v3，先按监控规程暂停
v1，再保留其 `PROTECTED_FILE_DRIFT` 异常现场；不覆盖、不把它伪装为正常完成。
v2 监控确认无异常并进入只读等待后，v1 等待进程已安全终止；旧 E2 未停止或重启。
权威后续为 `run_genuine_hybrid_g0_g1_release_chain_v2.py`：顺序固定为旧 E2
发布 PASS → 真实 G0 → 六进程资源门 → 按 6/3 选择运行三题 G1。任一科学门失败
即写证据停止，不自动调参、换种子、换题或扩大长跑。

## 2026-07-25：E2 v7 机械放行与效果门解耦

现有完整发布链接在一起检查“数据是否正确”和“旧算法是否足够适合写进论文”。真混合
G0/G1 等待链又只认完整发布 PASS，因此旧算法若只在效果或展示层停止，会把替代算法
开发一并错误堵死。用户批准新增结果盲机械放行：正式 v7 必须 405/405 完成并返回
1620 份完整可行解，独立 witness 复算必须逐份重现成本且全部可行，两阶段哈希清单
必须闭合；原完整发布链还必须先形成终态，防止重复接力。

零搜索入口为 `build_e2_v7_mechanical_release_gate.py`，冻结登记为当前 v7 目录下
`mechanical_release_registration_v1.json`。机械 PASS 只授权低成本真混合开发，
不覆盖原发布链的效果、轨迹、路线明细、表图、BKS 或 SOTA 判定，也不自动启动第二条
G0/G1 链。当前仅完成合同、源码指纹、语法和默认只读校验，结果为
`CONTRACT_ONLY_PASS`；没有运行放行或新搜索。

应用内自动化 `e2-v7-30` 每 30 分钟只读检查正式任务的进程、完成数、终态、资源和
异常；正常不通知，异常只报告不自修。检测到正式终态后先暂停自身，再核对既有发布
链和真混合等待链的所有权，避免重复执行。

## 2026-07-25：正式批与独立复算通过、发布链监控恢复

E2 v7 正式 405/405 任务和 1620 个完整可行解已 PASS；独立复算 1620/1620 成本一致、
全部可行，也已 PASS。正式与复算哈希清单分别 2434 和 85 项闭合。

原发布接力在两个 PASS 之间被监控器误停：外层五分钟进度阈值不适合内层批处理复算，
内层又把已退出的僵尸子进程判为命令漂移。用户确认安全结束旧进程组并保留现场。
恢复 v2 只验证并复用两个 PASS，随后执行原冻结效果门、S3、S4 v3、S5；不重跑正式
E2 或复算，不改变算法、分数、阈值或论文结论。26 项登记指纹及 2434+85 项上游清单
已在执行前闭合。

恢复后的旧算法结果强度门判
`HOLD_E2_STAGED_PORTFOLIO_NOT_PAPER_STRONG`，因此旧发布链 HALT，S3--S5 不继续。
机械完整性门独立 PASS 后，旧真混合等待链因只认论文效果 PASS 而停止，且未执行 G0、
资源探针或 G1。机械接力 v1 只把 G1 的上游资格改为机械 PASS；v3 其余三题、种子、
算法、预算、算子、并发和性能门整体哈希锁定。接下来严格按 G0 → 零搜索六进程探针
→ 6/3 workers G1 执行，G1 STOP 不救援。

## 2026-07-25：真实 G0 三题共同停止

机械接力在真实 bundle 零搜索 G0 按合同停止：三道开发题均报“解码短名单没有完整
可行候选”，搜索次数为 0，资源探针和 G1 未运行。旧 E2 的正式 405/405、1620 解
独立复算和机械完整性 PASS 不受影响；旧算法论文效果 HOLD 也不因此改变。

只读审计定位到共同接线缺口。封存 witness 先通过了共同完成器、直接检查和精确评分；
三份 witness 还都是不超过登记车队上限的全燃油方案。新解码器随后尝试重新分配车场
和车型时，只根据路线的有限成本把候选当作局部可行，没有先检查时间窗、载重、充电
等硬条件；按该估价截取前 6 个整解后，完整模型发现 6 个都不可行。因 v1 没有保存
每个候选的具体违约明细，目前只能确认“局部筛选缺硬约束且没有保留可行原方案”，
不能诚实确认具体是哪一种硬约束触发。

在用户批准前，不修、不重跑。若获批，版本化修复只应补路线局部硬约束过滤、固定保留
已知可行当前分配、保存候选级违约明细和对应测试；不得改完整评价器、封存 E2 成绩、
G1 的三题/种子/预算/算子/门槛，也不得把保底候选免计预算。

## 2026-07-25：G1 v6 终态

结果前复核发现 v5 对局部覆盖违约的忽略过宽，用户确认终止；4 个部分结果未读取且
禁止复用。例外收窄后 38 项测试、G0 v3 三题和六进程资源门全部通过。G1 v6 九项
完整运行，每臂 80 次完整评价，全部可行、无重复、replay 零差异。

A+B 相对两个单臂较优者只在 `cn-cy-150c-03` 改善 0.894%，另两题与 A 完全持平；
三题直接协同改进证据均为 0。最终判 `STOP_G1_NO_HYBRID_GAIN`。这说明当前实现具有
无损择优和一题收益，但没有通过真协同 `1+1>2` 门。不得调参救援、扩大实验、启动
E3，或把本结果写成论文算法优势。

## 2026-07-25：首个独立深度搜索候选停止与下一架构

G1 失败后另立的 `TAILORED-DP-VNS` 首次行为门没有重跑 HGS，只尝试直接改善三份
封存强解。五类邻域都生成了结构，但 141 个结构在完整评价前因路线无可行车场/车型
分配被拒绝，三题严格改善为 0/3，故按合同停止并冻结。

零目标诊断随后发现 25 客户题在原距离排序更深位置存在路线级可行的重插、交换、
2-opt 和 2-opt*。失败不是完整检查器坏了，也不是强解附近绝对没有可行移动，而是
原实现先按纯距离取前 10 个，再检查容量、时间窗、车型和车场，系统性截掉了可行
候选。禁止把前 10 直接调成更大值后在原三题重跑。

下一候选另立为可行性引导的大邻域搜索：移动生成时就检查路线资源、车场—车型—
充电选项、有限车队和共享桩，并增加连续块弹出—逐客户束搜索重建。新开发题按实例
ID 哈希预先固定为京津冀50、成渝100、珠三角200三题；先过零完整目标 G0，再执行
每题最多120次完整评价的直接改善门，仍要求至少2/3改善和至少一次大邻域改善。未过
前不运行 A/B/A+B、E3、正式全量或论文优越性写入。

该候选随后以候选完整目标评价0次通过G0：四类局部邻域和连续块重建均能交付联合
可行结构，证明可行性接线问题已解决。正式直接改善门的算法计算完成后，runner 只在
CSV打包阶段因多字段中断；三份轨迹和最终witness已保存，按结果读取前登记的零搜索
恢复规则完成复算，没有重跑搜索。

恢复后的性能判定仍为STOP：50和100客户强解均无改善，200客户题仅由一次2-opt*
改善约0.000045%，连续块重建没有改善；总计1/3，低于至少2/3硬门。所有输出可行，
评价账闭合，最高重复比例约2.56%。第二候选、三题、512结构检查上限、束宽和120次
预算全部冻结，不做结果后调参。下一步只能基于历史失败与新增文献另立真正不同的大
尺度架构；没有清晰证据就停止，不把局部微改包装成真混合。

## 2026-07-25：联合路线—充电精确小邻域工程启动最终停止

新候选 `E2-JRC-EXACT-NH-001` 的目的，是在恰好8个客户上同时改变两条路线的客户
顺序、车场、车型和充电决定，并先以6客户独立穷举证明精确性，再用6 workers跑三题
两臂最低成本门。实现、合同和六任务注册均已冻结，三视角 HGS 仅作为不可修改起点。

首次受监督工程门未进入算法：监督器将相对工作目录解析为候选包目录，导致命令和
保护路径均无效；子进程在导入工程门前退出，stdout 为0。六客户证明、真实 bundle
加载、真实邻域搜索和G0均没有执行。因此没有任何算法性能结论。

合同明确规定工程阶段任何失败即最终停止且不得救援。当前终态为
`FINAL_STOP_JRC_EXACT_NH_ENGINEERING_LAUNCH_FAILURE`：不修路径、不建v2、不重试，
也不得换名复活。受保护 `MV-HGS-SP` 未改、未跑，继续保底。

## 2026-07-25：共用绝对路径启动基础设施通过

协调任务随后确认多次失败的共同部分是实验启动器，而不是候选算法，授权只修共用
基础设施，禁止做候选专用补丁或自动重跑JRC。JRC v1异常现场继续原样保存。

基础设施v1零算例测试启动了六个worker，但监督器把Python解析后的真实路径用于匹配
启动时的符号链接路径，同时系统进程命令文本截断导致末尾的并发字段不可见，故按规则
暂停。v2分别登记绝对启动路径和解析后真实路径，并把关键并发字段放到命令前部；所有
根目录、脚本、保护、结果和监督路径均改为已验证绝对路径。

v2受监督集成门判`COMPLETED`，异常0；6行来自6个不同spawn进程，全部回报同一仓库、
worker模块和Python，真实算例加载0次，结果哈希4/4一致。结论为
`PASS_EXPERIMENT_ABSOLUTE_HARNESS_V1`。该结论只授权把这套启动框架用于后续另批任务，
不改变JRC算法或协议，也不自动重跑JRC。

## 2026-07-25：JRC v2 工程通过、性能门前超时最终停止

协调任务授权JRC仅用通过验收的绝对路径框架做v2，不允许改变科学候选。v2注册逐项
继承v1的三题、两臂、起点、8客户邻域、每任务1次完整候选评分、30秒上限、6 workers
和效果阈值，v1异常目录与注册哈希保持不变。

工程门通过：6客户独立穷举和候选求解均得到规范最优值147；六个冻结起点全部精确
复算可行，均选出8客户；六个不同进程合计峰值330.71875 MiB，监督器无异常。随后
唯一一次六任务G0中，至少一项在冻结30秒内无法穷尽8客户邻域并证明最优，抛出
`ExactNeighborhoodTimeout`。因此没有完整六任务结果、独立复算或效果门结论。

终态为`FINAL_STOP_JRC_EXACT_NH_EXECUTION_FAILURE_V2`。候选最终冻结，不延长时限、
不改邻域/剪枝、不换题/起点、不重试，也不得用未证明最优的局部结果评价性能。该门
未开放下一A/B/A+B、全量、BKS/SOTA、E3或论文主张；`MV-HGS-SP`继续作为保底。
## 2026-07-25 RC-EV-Split 状态规模停止

前两版成品路线局部改善均未形成足够信号后，按既有
`algorithm_optimization_prd_20260718.md` 中未完成的 EV-aware Split 主线，实现了
给定客户顺序后联合切路线、分车场/车型并计入非线性充电、有限车队和共享桩时隙的
资源约束标签算法。四个核心测试与旧合同合计 42 项通过，代码审计 0 finding。

全新 15/75/150 客户 G0 的 v1 是片段异常接线错误，0 状态、0 候选评价；v2 只修复
“不可行片段应跳过”。v2 的 15 客户题完成（229 状态），75/150 客户均达到冻结的
500,000 总状态上限，判 `HALT_RC_EV_SPLIT_G0_STRUCTURE_OR_SCALE`。候选完整目标
评价仍为 0，不是性能负例。结论是共享充电时隙不能作为精确 Split 标签维度扩到中大
规模。后续不得增上限或加结果后束宽；只能审计并另立“路线列池 + 限时 MIP 全局资源
组装”架构。

## 2026-07-25 路线列—共享桩 MIP G0

旧 MV-HGS-SP 的 MIP 不含共享充电时隙约束，且只能选择父解已有整路。新原型从给定
客户顺序枚举路线级可行连续片段，并让 MIP 同时满足客户覆盖、分车场车型有限车队和
共享桩时隙容量。G0 在15/75/150客户题形成159/1576/2278列，MIP均证明最优且完整
复算闭合；最慢整题8.4秒，未再出现状态爆炸。三题目标均与输入持平，因此只判工程
PASS，不是效果PASS。已放行三道全新题、五封存顺序的旧整路池对新片段池直接门。

## 2026-07-25 单长链路线列直接门停止与无序路线对候选

直接效果门在三道冻结新题均完成、完整可行且MIP证明最优，但严格改善为0/3；新池
虽含307/2396/5573条父解外新边界，最终一条也未选择，判
`STOP_ROUTE_COLUMN_MIP_NO_DIRECT_HEADROOM`。200客户题的五父解整路池仍比最好
父解低0.459212，说明MIP组装有效，失败集中在新路线来源。

结构审计确认，旧生成器把无序路线集合按witness文件顺序压成一条客户长链，只允许
相邻路线跨界。25/100/200客户题实际覆盖全部父路线对的50.0%/19.2%/6.0%；中大题
绝大多数路线对从未进入重切。旧候选冻结，不通过延时、增列或换题救援。

新合同`E2-URP-MIP-001`只测试无序路线对精确重切：对任意两条路线形成两个拼接方向，
在扣除未动路线的车队和共享桩逐时隙占用后，用小型MIP重组该对客户并完整验解。
三道全新50/100/150客户题至少2/3直接改善且至少一次来自原本不相邻路线对，才允许
把内核扩成可独立运行的算法B和设计HGS双向接入；当前不授权E3或论文主张。

## 2026-07-25 固定次序重切停止与 HGS-ILS-XD 预登记

无序路线对直接门在三道冻结新题、每题六个固定起点上共求解2850个小型MIP，三题均
完整可行，但所有局部目标变化为0、没有候选进入完整评分、严格改善0/3，判
`STOP_URP_MIP_NO_DIRECT_HEADROOM`。这说明“覆盖全部路线对”仍不能弥补“路线内部
客户次序固定”的限制；不再通过延时、增列、换题或改起点救援。

系统对照已停的HGS+ALNS、ReMIX、MPILS-MVNS、两版VNS、穷举成段生成、资源约束
EV Split、单长链路线列和旧HGS+RR，并结合混合车队EVRP的分层路线评价、HGS专用
大邻域和跨问题空间搜索文献，只保留`HGS-ILS-XD`一个待证假设。A是真实HGS，
B是独立ILS；两者都必须可单独运行。按用户对陈式逻辑的澄清，基础A+B改为严格
`A→B`：A先完成并交出完整可行解，B再独立深化；实验任务之间可并行，但顺序任务
内部不得用中点交换替代交接。HGS+ILS和顺序交接本身不声称新颖。

第一门只跑三道未见题的B冷/热启动，共六个独立任务；资源允许用6 workers，每任务
128次ILS外层迭代、最多8次不同完整评价。热启动只取封存五种子中最强纯`HGS-M`，
不用`MV-HGS-SP`。除路线池外新路线和相邻关系变化外，还硬性要求至少2/3热启动
严格改善HGS起点、至少2/3热启动严格优于同题同种子冷启动，才允许严格同预算
A/B/`A→B`。失败即STOP，不调参重跑或追加陈文VNS。公开BKS/SOTA与私有China81
严格分开；任何小门都不授权论文、E3、全量或SOTA。

## 2026-07-25 HGS→官方 ILS 顺序交接 G0 最终停止

修订后的 G0 已按登记一次执行：3题各有ILS共同冷启动与最强纯HGS-M热启动，共6任务，
由6 workers完成。资源安全门通过，6个不同进程合计峰值393.0625 MiB、最低空闲内存
40%；任务异常0、预算违规0、源码漂移0。

结果未达到基础要求：完整可行新解2/6、冷启动可行1/3、池外路线2/3、热启动相邻关系
变化1/3、严格改善HGS-M起点0/3、热启动严格优于冷启动0/3。唯一明显改善发生在
珠三角50客户冷启动ILS，但2617.380393仍弱于同题HGS-M起点2512.932249；成渝100
客户热启动唯一新可行解还比交接起点差12.679465。零搜索独立复算6/6起点与2/2生成
witness全部闭合。

判`STOP_HGS_ILS_XD_G0_NO_FEASIBLE_COMPLEMENTARITY`，冻结该候选，不做G1、不追加
陈文VNS、不换题/seed/预算/完成规则。结论只限定为“官方ILS直接接手强HGS解未形成
可用深化”；下一候选必须以实质不同机制解释并跨过该失败，仍从低成本预检开始。
