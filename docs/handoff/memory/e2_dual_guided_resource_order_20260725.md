# 2026-07-25 强 HGS 深化失败后的唯一候选

- `HGS-ILS-XD` G0 按登记由 6 workers 完成并独立复算，热启动 0/3 改善强 HGS，
  冷启动虽能改善弱解但不能达到 HGS 起点，正式冻结且禁止调参/重跑。
- 历史证据共同排除普通局部动作、连续段穷举、固定内部次序重切、只重组已有路线、
  全题资源 Split 和通用 ILS 顺序交接。未解决的缺口是：低成本地产生能跨出 HGS
  局部最优、同时回应车队与共享充电稀缺性的全新客户次序。
- 唯一预登记候选是“对偶价格引导的资源耦合次序重构”：路线池线性松弛给出客户、
  车队和充电时隙的资源价格；只选两个最紧张路线对；用位移 `k=4` 的
  Balas--Simonetti 型动态规划同时改变内部次序和路线分界；最多 16 个候选交共同
  完成和精确评分。
- G0 冻结六道未用 50/100/150 客户题、六任务默认 6 workers、每题 16 次完整评价、
  100000 状态和 90 秒安全上限。至少 4/6 改善、2/6 改善不低于 0.05%、成本中位
  不超过对应 HGS-M CPU 的 25% 才通过；任一失败即 STOP，不救援。
- 权威合同：
  `docs/handoff/e2_dual_guided_resource_order_contract_20260725.md`。
- 当前只完成审查与预登记，未实现、未运行，不授权 E3、论文、BKS/SOTA 或
  `1+1>2`。

## 2026-07-25 执行终态

- 源码与六题输入已冻结，合成/静态检查 7/7 通过。监控启动 v1 的相对路径错误未
  触达实验；保留异常现场后，v2 仅用绝对路径启动同一冻结程序。
- 6 workers 零成绩工程门完成，候选目标评价为 0；内存门通过，但结构门仅 1/6 题
  产生有效候选（且只有 1 个），其余 5/6 为 0。
- 判定：`HALT_ZERO_OBJECTIVE_ENGINEERING_OR_RESOURCE_GATE`。G0 未启动；不得把
  本轮写成算法效果失败或成功，因为根本没有读取任何候选成绩。
- 禁止在原六题上改筛选顺序、`k`、路线对数、候选数、状态上限、墙钟或起点后重跑。
  任何“是否属于实现错误”的判断都必须另立版本化审查并由协调任务批准。

## 2026-07-25 最终裁决

- 用户决定不再开启实现语义审查；结构门本来就是用来淘汰候选生成能力不足的机制，
  因而 5/6 零候选和 1/6 仅一个候选足以终止本路线。
- 最终状态：
  `FINAL_STOP_DUAL_GUIDED_RESOURCE_ORDER_NO_RESCUE`。
- 原 HALT、注册文件、监控异常现场、工程五件套和所有源码原样保留；禁止修改或重跑。
- 未经新的“失败复核 + 一手文献 + 唯一候选 + 低成本预登记”授权，不启动下一候选。
