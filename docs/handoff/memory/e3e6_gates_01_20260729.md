# E3E6-GATES-01 前置门记忆（2026-07-29）

权威证据目录为
`baselines/china_e3_e7/e3e6_gates_01_20260729/`，根判决是
`HALT_AT_GATE_2_D3`。本任务单进程、零搜索，未启动 E3/E6 正式实验。

门一 D2-A 为 PASS。当前车型对已绑定为 CV 威铃 K7（额定载质量 1735 kg、
整备质量 2565 kg）和 EV 福田 ES1 快递版栏板（额定载质量 1700 kg、整备
质量 2600 kg、电池 77.28 kWh）。81 个实例共 144 个实例-车场行按
`(due_time, ready_time, customer_id)` 固定顺序执行确定性首次可行装箱。
81 个全燃油 witness 的完整模型复算均为 0 违约、0 跨场。旧
`china81_finite_fleet_authority_v1_20260723` 只读对比；`R_d` 与 rho=1.25
主情景上限变化均为 0/144，但新车型绑定和 witness 重放仍已重新闭合。

门二 D3 为 HALT。当前 `pyvrp_adapter.py`、`epochal_hgs.py`、
`route_pool_sp.py` 和 `formal_e3_runner.py` 哈希已重新登记。控制臂的搜索空间
多维容量硬锁、完整候选守卫、路线池初筛与 MIP 二次过滤、最终证书守卫均闭合。
45 个多车场实例在处理臂可构造跨场服务；36 个单车场实例不存在不同的第二车场，
即使 `hard_home_depot_lock=False` 且 `Exchange11` 启用也不能产生跨场服务。
因此“全部 81 个实例逐实例可产生”的强条件失败，不能用旧门的结构开放性替代。

门三 D4 因上游 HALT 未运行，没有读取或报告任何臂间成本。只读源码预检确认
E3/E6 当前路径为固定 80 次完整候选评价、`MaxIterations` 加墙钟安全上限，
不是 `NoImprovement` 收敛停机；未计算 `L/S`，未选择 80/56/32 或更高档。

保护文件 `solver/src/setp_solver/cost.py`、`check.py`、
`search/evaluation.py` 未修改。恢复路径需要用户明确裁定 D3 对单车场实例的
适用域或处理臂定义；在此之前不得运行 D4 pilot 或 E3/E6 正式实验。
