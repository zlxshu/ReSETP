# E4-CARBON-TIMING-01 记忆节点（2026-07-29）

## 状态与权威目录

- 任务：`E4-CARBON-TIMING-01`
- 状态：`PASS_COMPLETE_ZERO_SEARCH_REPLAY`
- 预注册结局：`POSITIVE`
- 权威目录：`baselines/china_e3_e7/e4_carbon_timing_20260729/`
- 输入：E2 统一批 `arm=MV` 的 405 个 `attempt0/k3000` 完成解
- 矩阵：405 解×28 电网日＝11340/11340 配对行
- 搜索候选数：0；没有调用元启发式

## 冻结对比

两臂共享客户服务、车辆指派、路线、充电地点、每次充电电量、非线性充电曲线和持续时间。`ASAP` 在同一合法窗口及半小时容量语义下取最早可行开始；`CARBON` 只在论文式(33)候选点中最小化预测充电排放，并用第二阶段开始时刻目标处理平局。两臂唯一变化字段是 `charge_start_second`。

## 主要结果

- ASAP 充电侧排放：475317.334540 kgCO2e
- CARBON 充电侧排放：214033.627569 kgCO2e
- 充电侧排放减幅：54.970371999%
- 系统总排放减幅：9.746262547%
- 充电电费变化：+134.879776719%（增加，不是节约）
- 可移动电量占比：98.338598297%
- 观测有效规模区间：`[10, 200]`

城市群充电侧减幅：京津冀 53.7214%，珠三角 65.4623%，成渝 40.7526%。九个客户规模分别为 59.7583%、57.4902%、58.8004%、58.4087%、56.9363%、53.7796%、50.2017%、56.1511%、54.7405%。逐日汇总范围 43.26%--71.35%，中位数 54.16%。

## 不可移动原因

不可移动电量占 1.6614%。其中 `ALREADY_LOWEST_CARBON` 为 14066.24 kWh，占不可移动电量 68.6%；`DEPOT_CAPACITY_CONSTRAINED` 为 6447.98 kWh，占 31.4%；`WINDOW_TOO_NARROW` 为 0；未分类为 0。

## 量级解释与写作边界

252 个城市日的日内碳强度相对振幅 `100×(max−min)/mean` 中位 144.80%，范围 37.94%--325.88%。44072 个会话日中 ASAP 有 33040 个从 0 点开始，CARBON 仅 332 个留在 0 点；窗口宽度中位 10.30 小时。这解释了较大的充电侧效应，但不能外推为一般规律。

论文必须同时写明电费增加 134.88%，不能把该结果包装成排放和成本同时改善。v3 装载器把同一碳因子用于预测和结算，因此这不是含预测误差的稳健性证据。

## 证据闭合

- 源解独立复算：405
- 两臂独立复算：22680
- ASAP 违约：0
- CARBON 违约：0
- 评价闭合最大残差：0
- 会话级回聚合最大残差：`5.68e-14`
- 受保护 `cost.py`、`check.py`、`search/evaluation.py` 前后哈希相同

`raw_runs.csv` 每行同时包含两臂排放、电费、完整模型成本和移动电量；`action_timing_audit.csv` 保存所有会话日的窗口、候选数、两臂开始时刻和不可移动原因；`day_boxplot.csv` 保存 28 日总体及分城市群箱线数据。

## v3/v4 装载边界

用户指定的 v3 日历数值完整，但缺 v4 后来追加的 `diesel_parameter_status` 和 `joint_key_status`，所以不能单独通过正式 bundle 门。执行前已逐行证明 v3 与 v4 的所有碳强度、电价、城市、日期、时段和映射字段相同。实际择时 profile 直接来自 v3；v4 bundle 只装载不随择时变化的实例、道路、车辆、车队、充电设施及检查器参数。不得把这写成 v3 已具有完整正式批准列。

## 并发与运行

进程列表在沙箱中不可读。程序请求 4 workers 后遇到 `PermissionError: [Errno 1] Operation not permitted`，按结果前冻结的机械回退规则使用 1 worker；墙钟 903.8 秒。这个回退没有改变输入、候选、算法、顺序、终点或分母。
