# E6-FAIRNESS-V3-20260731：线程冻结后的候选池回放与 I/U/F 终局

## 终态与权威目录

- 状态：`COMPLETE`。
- 权威目录：`baselines/china_e3_e7/e6_fairness_v3_20260731/`。
- 本轮仅重跑 JOINT 的 20 个单元，ZONE 搜索重跑 0 个；共同 cap=400，最多
  2 workers。
- 四个数值线程变量
  `OMP_NUM_THREADS/OPENBLAS_NUM_THREADS/MKL_NUM_THREADS/VECLIB_MAXIMUM_THREADS`
  在主进程与子进程中均固定为 1。此前 0/20 预检不能支持逐位不一致结论；v3 从
  0/20 独立执行并重新判定。

## 搜索不变与逐位认证

- 候选持久化发生在冻结搜索返回之后，只保存已经完成完整模型评价的可行候选本体；
  未改变搜索算子、随机流、接受规则、预算或路线池语义。
- 20/20 个 JOINT 单元的最终解哈希、完整目标浮点位、实际评价数、逐候选分类 trace
  与终止原因均与 E3 认证解一致。50c seed1 仍为解哈希
  `159dc8289888437ac1e1e3e7264d06641d933941b307ca488ee526e0dacf3985`，
  完整目标浮点位 `0x1.1e2a31f630958p+11`，实际完整评价数 331。
- `cost.py`、`check.py`、`search/evaluation.py`、`profit.py`、
  `route_pool_sp.py` 和 `epochal_hgs.py` 的冻结哈希全部匹配。旧 E3、E6-v1 和
  E6-v2 目录的既有清单只读复核分别为 167/167、10/10、82/82，缺失和哈希漂移均为
  0；本轮未覆盖或删除其中真实文件。

## 候选池与 F 定义

- 共保存 2869 个可行完整评价事件，形成 2764 个逐单元去重候选。每个候选保存路线
  与所属车场、客户序列、充电/跨场动作、完整目标浮点位与 breakdown、成员收益账本
  和来源事件。
- F 只在同一 U 运行的嵌套候选中选择：先筛选广州、深圳两名成员收益均不低于同
  seed 的 I，阈值 θ=1、容差 1e-9 元；再取系统成本最低者。若池内没有参与可行
  候选，F 才回退到 I。嵌套候选没有被当作独立样本。
- 独立 checker 逐候选重算解哈希、完整目标、成员账本、参与条件和 F 最小化选择，
  20/20 单元通过。U 自然帕累托为 0/20；2764 个唯一候选中满足参与条件的候选为
  0，因此 F 在 20/20 个单元均有证据地回退到 I，不是用 I 近似 binding 单元。

## 三态结果

- 50c 十种子均值：I/U/F 系统成本为
  2341.446790/2288.231338/2341.446790 元，广州收益为
  6917.233079/6042.162189/6917.233079 元，深圳收益为
  9075.820131/10004.106473/9075.820131 元，车辆数为 9.0/8.0/9.0。
  F 相对 U 的成本增量为 2.325615%，弱势成员收益平均改善 875.071 元。
- 100c 十种子均值：I/U/F 系统成本为
  4739.691422/4706.839925/4739.691422 元，广州收益为
  16215.690760/15763.241035/16215.690760 元，深圳收益为
  21031.117818/21516.419040/21031.117818 元，车辆数为 18.0/17.2/18.0。
  F 相对 U 的成本增量为 0.697952%，弱势成员收益平均改善 634.964 元。
- 两条算例行等权的公平成本为 1.511783666875632%；20 个单元合并后的算术均值为
  1.5124654575275558%，两者定义不同，报告主值采用预定的一行一算例等权口径。
  弱势成员收益改善的 20 单元总体均值为 755.018 元。
- F 墙钟复用生成候选池的同一 U 搜索墙钟，不是第三条搜索轨迹，不能与 U 时间
  相加。

## 解释边界与待裁决事项

- E3 的联合配送相对分区配送虽有有限降本并少用车，但 50c 的里程/碳排分别增加
  28.04%/4.04%，100c 分别增加 12.18%/1.18%。E6 进一步表明，本批所有 U 单元
  至少让一个车场收益下降；系统协同剩余没有自动形成成员帕累托改进。
- 结论限定为：在这两个预定算例、共同 seeds、cap=400 和已持久化的嵌套候选池内，
  不允许转移支付的账面参与保障使协同不可实施。不得外推为不存在任何其他公平方案
  或已证明全局最优。
- 转移支付没有实现。Shapley、MCRS、按贡献比例分摊或其他分配规则会改变科学定义，
  必须由用户另行裁决。

## 封装与终端证书

- 独立验收状态 `PASS`，`verification_id` 为
  `8b591b7f722d163bceb869225906069eaecb386cc31a33bf2dd838c5af24aad5`。
- 自动清单生成后，监控器只向 `experiment.stdout.log` 追加最终完成行；该监控日志
  因而成为旧清单唯一漂移。原日志保留并按监控运行态从重封清单排除，科学文件未变。
- 重封清单覆盖 75 个稳定文件，缺失、哈希漂移、未列稳定文件均为 0；v3 目录初批
  96 个 `._*` AppleDouble 及封装写入期间重现的伴生文件均被精确清理，最终扫描为
  0。`done.json` 在清单及全部纳入文件之后最后写入。
- 最终 `manifest_id`：
  `7dc048babcec150cd6b5531dee207c847a9f0ecea7d9ddc106d0b13ba5d45faa`；
  `decision_id`：
  `b808155258ff0035c92cd5f959ff654344628fabf936553381395fe9d48119dd`；
  `done_id`：
  `286e5fe854b3bd7fa80369ccd3d660c6b233ac4c24b7df123bb4f1a27fca9c49`。
