# MPILS-MVNS-C2 G1 B组首发

日期：2026-07-20。

用户在 G0 通过后批准 B 组首发：PR11B/PR17B/PR21B、seed1、每臂 8.5 秒、
同批新跑母体，过线为零负且至少一胜。G0 与 C2 源码先由提交 `2f434816` 冻结，
G1 合同和运行器由 `3fb6c925` 预登记；两个搜索前启动错误由 `d013d892` 只修外层
环境和 Git 血缘检查，算法、常数、实例和判据未动，且均未进入求解。

首发六臂全部完成并独立有效，墙钟、五机制、64/6/48/12 上限和保护文件门全过。
性能为 0 胜、0 平、3 负：PR11B -0.666%、PR17B -2.404%、PR21B -3.259%，判
`FAIL_G1_B_FIRST_FIRE__DIAGNOSIS_REQUIRED`。

首发暴露明确的活动开销病灶：候选只完成母体 50.7%、47.7%、39.3% 的迭代；
三题触发 39/38/28 次，而事件钩子与精英库本身只耗毫秒。PR21B 的 28 次动作全部
没有可用候选，仍丢掉约 61% 母体迭代。因此两发协议允许启用 A 组一次常数诊断，
只修 W/冷却期或升级顺序，不许加模块或扫参数。第二发仍失败则终止替换式扰动线。

证据：
`baselines/algorithm_foundation/mpils_mvns_c2_g1_b_first_fire_20260720/`。
判读：
`docs/handoff/mpils_mvns_c2_g1_b_first_fire_diagnosis_20260720.md`。
