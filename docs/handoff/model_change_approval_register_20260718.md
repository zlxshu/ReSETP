# 建模、单位与参数变更审批登记

## G1-NL-CORE-001：China81 非线性充电核心分批实施

状态：`NL2_PASS__NL3_DYNAMIC_AND_CHINA_PROFILE_ADAPTER_AUTHORIZED`
（2026-07-20）。

用户已授权以最终算法性能、China81 完整模型能力和后续 E1--E7 可用性为中心自主推进，
允许按证据调整实现和参数，并要求避免无效公开题微调。公开参数竞速和分阶段混合均已按
预登记止损，现转向 `G1-FREEZE-MERGE` 的真实物理阻断。

批准分批合同 `docs/handoff/china81_nonlinear_core_g1_contract_20260720.md`。第一批 NL0
只新增唯一纯数学曲线内核和回归，不修改 `cost.py`、`check.py`、`prices.py`、
`search/evaluation.py`、主 TeX 或正式搜索入口。NL0 全绿并封存后，才允许依次接解结构/
多趟排班、充电修复/成本/检查、动态继承和 China81 最终汇合；任一批失败即停在该批，
不得用旧恒功率结果冒充非线性正式结果。

NL0 执行终局：生产内核对冻结 264,600 行动作的持续时间和分时电量逐项零差异，
L100 退化、24 项测试、Ruff、保护文件零变化与 10 件登记制品哈希全部通过，判
`PASS_NL0_PRODUCTION_CHARGING_CURVE_CORE`。按分批合同只放行 NL1；正式搜索继续
关闭。

NL1 执行终局：`ChargingAction` 已向后兼容地携带起止电量与曲线 ID，多趟证书冻结
曲线参数哈希，排班中的可达电量、趟间补能和首趟预充均改由唯一曲线内核计算。同一两趟
夹具在 L100 下需要 1 辆、NL90 下需要 2 辆实体 EV；非线性动作的最大持续时间误差
约 `3.98e-13` 秒、电量误差 0，L100 最大漂移约 `3.13e-13` 秒，84 项相关回归与
Ruff 全过。全量回归仍诚实记录为 830 通过、12 个可在 NL1 前基线/旧契约复现的失败、
1 跳过，不冒充全量全绿。判
`PASS_NL1_CURVE_AWARE_MULTITRIP_SCHEDULE`，只放行 NL2；正式搜索继续关闭。

NL2 执行终局：正式静态充电修复、ALNS 充电修复、多趟碳择时、成本、间接排放、
场站利润和独立检查器均改为读取动作的起止电量与曲线身份，并按曲线分段功率精确分配
半小时槽电量。高 SOC 夹具中首槽真实电量为 `8.333333333333334 kWh`，旧均匀摊分
为 `5.555555555555555 kWh`，两槽仍闭合 `20 kWh`；缩短一分钟和缺元数据的动作均
双端拒绝。精确碳择时零搜索夹具从 `0.726524711670 kg` 降至
`0.121087451945 kg`，仅证明机制接线。L100 新旧动作逐位回归，目标回归与 Ruff
通过；全量仍诚实记录为 835 通过、12 个冻结提交可复现失败、1 跳过。判
`PASS_NL2_NONLINEAR_COST_CHECK_AND_TIMING`，只放行 NL3；China81 正式搜索继续
关闭。

## MPD-ILS-VNS-D1-001：多阶段双节奏整机

状态：`EXECUTED__STOP_MPD_ILS_VNS_PUBLIC_ARCHITECTURE__PIVOT_CHINA81`
（2026-07-20）。

用户授权以性能和后续实验便利为根本，自主决定算法母体、组合、结构与参数，并偏好有
机制化分工的陈雨蝶式混合结构。本项依据参数竞速未见块的直接互补证据，批准一次固定
双节奏整机门：基础快速搜索、冻结宽搜索，以及70/30和20/60/20两个分阶段组合。

全部比例、同墙钟计费、A组选择和未见B组双单体门冻结于
`docs/handoff/mpd_ils_vns_dual_regime_contract_20260720.md`。五个ReSETP机制只允许
常驻问询和预留正式接口；公开题缺少语义时自然不适用，不得伪造机制收益。失败后停止
公开架构开发并转China81，不得在B组救比例或打开完整28题。

执行结果：首次启动因监控目录未命中忽略规则被干净仓库门在求解前拒绝，事故留痕后
只修外壳并原样重启。A组三题seed7、60秒四臂中，`fast20_wide60_fast20`相对宽单体
3胜0负、相对基础母体2胜1负；但相对基础母体总目标为
`19,329,998>19,327,066`，多2932（约0.0152%），未过“双单体总和均更低”门。
判定如状态所示，B组未打开、不救比例。12个性能解、机制问询账、同墙钟和保护文件
独立闭合，新BKS为0。

## MDA-ILS-VNS-P1-001：强母体有限参数竞速

状态：`EXECUTED__STOP_PARAMETER_RACE_KEEP_FOUNDATION`
（2026-07-20）。

用户明确授权代理在有依据时调整参数，以算法变强和后续实验方便为根本；同时要求避免
执念、无用功和无边界枚举。本项据此批准一次有限、结果前冻结的逐轮参数竞速：16 个
LatinHypercube 分层配置先在 A 组 seed5 短筛，前四用新 seed6 复赛，唯一候选才进入
此前未打开的 PR16A/PR20A/PR24A 两种子确认。

完整空间、16 个配置、排序、三段预算和二值门冻结于
`docs/handoff/mda_ils_vns_parameter_race_contract_20260720.md`。确认失败后必须停止
公开题调参并转 China81；不得在确认题救参数、换题、扩网格或打开完整 28 题。

执行结果：第一轮保留`race_14/12/13/08`，换种子复赛选出`race_08`并以2胜1负、
总目标更低进入未见确认。PR16A/20A/24A两种子确认是4胜0平2负，总目标
`76,932,408→76,591,957`，平均BKS gap改善`0.441424478`个百分点；两处退步
`0.1281%/0.1559%`，均很小，但负例数2超过最多1，且迭代中位比仅
`0.68764153<0.95`。因此按预登记判`STOP_PARAMETER_RACE_KEEP_FOUNDATION`，
新BKS为0，不救`race_08`。78个性能解全部独立有效，保护文件与证据哈希闭合。

## MDA-ILS-VNS-S2-001：后期分阶段路线精修

状态：`STOP_LATE_STAGE_VNS_KEEP_FOUNDATION`
（2026-07-20；A组1胜2平0负但未过至少2胜门，确认块未打开）。

用户授权代理在明确最终目标后自主决定算法结构和参数，优先最低成本、结果导向，并特别
希望结构接近陈雨蝶“前期广搜—后期VNS精修”但不把形式置于性能之上。本单依据已通过
强母体的320次SwapStar调用、84次真实改善和约0.225秒总耗时，只批准
`mda_ils_vns_late_stage_contract_20260720.md`中的70%后500/250两档小门。它不恢复
已停止的AILS-II强度反馈层，不覆盖完整28题、China81、阶段二或论文优胜结论。

执行结果：基础臂固定迭代逐位一致，最佳`late_vns_500`在A组三题总目标只改善1009，
即约0.0052%，表现为1胜2平0负；额外精修19/20/27次但严格路线改善均为0，不能将
微胜归因于新部件。按结果前至少2胜门停止，不打开PR16A/PR20A/PR24A、不救频率。
权威证据位于`baselines/algorithm_foundation/mda_ils_vns_late_stage_20260720/`。

## MDA-ILS-VNS-A1-001：自适应扰动与接受控制

状态：`STOP_ADAPTIVE_LAYER_KEEP_FOUNDATION`
（2026-07-20；A组0胜0平3负，确认块未打开）。

授权来自用户2026-07-20的自主推进指示：算法母体、组合、结构和参数由代理按证据决定，
参数可调整，优先最低成本验证，成功再扩，失败及时换路；最终仍以算法强、故事清楚、
公开与China81两类算例表现好且便利后续E1--E7为目标。本单只覆盖
`docs/handoff/mda_ils_vns_adaptive_contract_20260720.md`中的隔离实现、A组有限选择
和未见确认小门，不覆盖完整28题、China81、阶段二或论文优胜结论。

方法只借AILS-II公开论文和MIT代码快照中的实际距离反馈、自适应扰动强度与逐步收紧
接受思想；本项目自行实现，不复制其Java代码。若A组没有自适应配置胜已通过的强母体，
立即保留强母体并STOP；若未见确认门失败，也不得第二发救援。

执行结果：基础臂5/100/1000固定迭代与原工作器逐位一致；A组最好臂
`adaptive_d20_10`对基础母体仍为0胜0平3负，三题退步0.284%/0.346%/0.696%，
只完成母体57.8%/56.2%/55.0%的迭代。按预登记在A组直接停止，PR16A/PR20A/PR24A
未打开，不改参数救援。权威证据在
`baselines/algorithm_foundation/mda_ils_vns_adaptive_20260720/`；基础门
`swapstar_best`继续保留。

## MDA-ILS-VNS-F0-001：强母体基础与选择性路线 VNS

状态：`PASS_MDA_ILS_VNS_FOUNDATION_VALIDATED_GAIN`
（2026-07-20；基础门已封存，完整28题、China81和阶段二仍未由本门放行）。

用户于2026-07-20明确授权代理自行决定算法、母体、结构和参数，以最低成本先验收，
证据向好时自主推进；并要求避免执念、及时变通，最终算法须有清楚故事、公开比较强、
China81胜母体且方便后续E1--E7讲出直接结果。该授权覆盖本合同中的隔离实现、有限
开发配置竞赛和三题两种子60秒验收，不覆盖完整28题、China81、阶段二、正式论文
结论或保护文件语义变化。

当前方法与边界详见
`docs/handoff/mda_ils_vns_foundation_contract_20260720.md`。固定停滞C2路线保持
`STOP`；本包只研究刷新最好解时的原生路线级精修与多车场时间窗题族配置。若未见
B组未过预登记门，本包立即停止，不得在B组救参数。

执行结果：结果前冻结的八种配置先在PR17A筛选，A组开发后唯一候选为只在刷新最好解
时调用PyVRP原生`SwapStar`的`swapstar_best`。B组PR11B/PR17B/PR21B、种子1--2、
每臂60秒对原装母体为5胜0平1负；六对总目标`29,002,981 < 29,116,626`，平均
BKS gap由`1.040976794%`降至`0.647618466%`，改善`0.393358328`个百分点；唯一
负例退步`0.093430403%`。12次B组解全部重新独立验解，33件哈希、胜负、总和、
gap和最大负例均独立复算通过。新BKS为0，因此本门只证明一个可继续开发的强母体，
不证明最终算法、公开一骑绝尘或China81碾压。

## 强制规则

从2026-07-18起，任何直接影响数学模型、物理模型、成本口径或实验含义的变更，以及任何新方法，必须完成“提出—溯源—独立核验—影响分析—用户明确批准—代码与合同同步—回归测试—证据封存”八步后才能进入正式链。

必须审批的事项包括：单位或币种转换；体积、重量、时间、距离、速度、能量、功率和排放之间的转换公式；把观测数据改造成代理变量；目标函数、约束、可行性定义和违约惩罚；默认参数及主/敏感性情景；车辆、车场、充电桩、时间窗和需求的语义；算例生成、抽样、配额、插补和筛选方法；道路矩阵、能耗和充电曲线构造方法；新算法机制、算子、阶段和参数自适应方法；统计单位、主要终点、检验方法、多重校正和显著性阈值。仅下载原始数据、计算不改变语义的描述统计、校验哈希和发现缺口，不视为模型或方法变更。

未经批准时，证据包可以写 `PASS_EVIDENCE_AUDIT`，方法探针可以写 `PROBE_COMPLETED`，但机器合同必须保持 `HALT_*_AWAITING_USER_APPROVAL` 或 `DRAFT_METHOD_AWAITING_USER_APPROVAL`，`formal_search_allowed=false`，代码默认值、正式方法入口和实验合同不得改变。

## G1-DEV-001：Homberger通用组件阶段一

状态：`STAGE1_COMPLETE_TRUE_SWAPSTAR_STOPPED_BEFORE_FORMAL_G1`（2026-07-18）。

授权链：用户明确“批准进行阶段一”，并要求算法开发必须基于论文或权威网络资料、针对当前瓶颈、先采用最快最短最低成本验证，非必要不启动全量算例。该授权覆盖Homberger开发集上的默认关闭通用组件实现、预算0/1/2/5行为门、少量实例/种子微门和接入冒烟；不自动覆盖115,200次评价的完整G1、Solomon、中国正式实例、模型机制变化或论文创新主张。

执行结果：SISR候选因R1三种子实际调用均为0判`KILL_COMPONENT_MECHANICAL_FAILURE`，关闭；真正SWAP*完成独立微门和内核接入，当前源码C1/B160接入门判`PASS_TRUE_SWAPSTAR_KERNEL_INTEGRATION_SMOKE`。为解除R1单种子未触发形成的歧义，按用户阶段一整体授权追加四实例×三共同种子×两臂×B400的G1a顺序门，共24任务、9,600次完整评价。24/24均OK、12/12共同起点、零硬违规；平均改善`0.003918%`、11/12不退化、最差`-0.000239%`、平均计时开销`+7.165%`均过对应数值门，但真正SWAP*只在C/R几何族活跃，RC三对均为零次，未满足预登记的C/R/RC三类活性覆盖门。按止损合同判`STOP_TRUE_SWAPSTAR_BEFORE_FULL_G1`，不启动115,200次评价的完整G1，不允许性能或论文主张。真正SWAP*继续默认关闭，本候选在本轮关闭。来源、数字、代码边界与下一步见`docs/handoff/e2_alns_g1_stage1_closeout_20260718.md`。

## 审批单 MC-001：公开中国订单的体积到公斤转换

状态：`APPROVED_ORDER_ATTRIBUTE_BASE_METHODS__FORMAL_SEARCH_BLOCKED`（2026-07-18；正式搜索仍受阶段二道路全矩阵等后续门阻断）。

原始事实：Figshare DOI `10.6084/m9.figshare.28113608.v1` 提供中国中部匿名零担城配企业10个高峰日、1222条订单。原始货量单位为立方米，只有1.0、1.5、2.0、2.5、3.0五档；数据没有实测公斤重量。论文案例给出源车辆容量7.2立方米；当前中国厢式电车合同的额定载重为1000公斤。

候选转换：`demand_kg = round(volume_m3 / 7.2m3 × 1000kg)`，得到139、208、278、347、417公斤。它保持每单占源车体积容量的比例，再映射成每单占目标车载重的同等比例。

性质：`CONSTRUCTED_CAPACITY_SHARE_SCENARIO_PROXY`。不得称为实测重量或中国货物平均密度；也不证明体积约束与重量约束等价。

可选决策：

1. 批准为正式基础情景。优点是完全可复现、与公开中国订单联合分布相连；风险是容量占比映射不是物理密度。
2. 仅批准为敏感性情景，正式主情景继续等待有重量字段的中国订单数据。科学最保守，但会继续阻断正式实例。
3. 否决，改用双容量模型（立方米和公斤同时约束）。物理含义更完整，但属于模型结构升级，需要车型货厢容积、货物重量或密度证据和新的算法改造。

关联证据：`data/ChinaInstances/china_order_attribute_calibration_v2_20260718/`。

当前机器处理：订单合同已同步为“容量占比构造代理获批、其余正式门仍阻断”；未改求解器、未生成正式实例、未运行算法搜索。

补充终裁（2026-07-18）：用户明确要求“计入手册，同步更新tex。如设计符号与公式，则需要跑python验证”。据此，MC-001的基础情景批准范围补齐为三项：1）上述容量占比需求代理；2）按来源论文案例参数`0.1 h/m3`计算服务时长，即`service_minutes = volume_m3 × 0.1 × 60`；3）从1222条交付订单中按冻结种子有放回抽取完整经验行，联合保留交付时间窗起点、宽度、体积映射需求与服务时长。服务时长不得称为实测停站时长，时间窗各字段不得拆开独立抽样。宽窗、紧窗仍只是未获批敏感性候选。

本补充终裁不改变模型目标、约束或求解器，不启动阶段二，不授权China81正式实例、道路全矩阵或算法搜索。新增论文公式必须通过独立Python数值与单位验证、现有数学审计和TeX编译后才算同步完成。

## 审批单 MC-002：E3–E7 机制暴露与统计显著性方案

状态：`P1_APP_05_METHOD_FROZEN_FULL_RUNS_NOT_AUTHORIZED`（2026-07-18；本状态取代下方较早的“部分批准”文字）。

2026-07-18后续批复 `P1-APP-05` 已冻结三图聚合、每图5个共同种子、只按结果盲残差/失败/耗时统一补到7、到7即停、E7先过75任务隐藏运行时门、六臂B1600只作阶段二上限、27格配对随机化主检验、敏感性检验和Holm。各实验族的“多大变化才值得”仍必须在该族解盲前分别写入运行合同；这是已批准的分终点预锁步骤，不是阶段一遗留审批，也不得事后按结果补阈值。

2026-07-18结果盲算力与功效包已完成，位于`baselines/statistics/mc002_compute_power_20260718/`，未运行正式搜索。中国矩阵核为27统计单元×3互斥图=81实例；五种子时E3/E5/E6合计2,835搜索臂，旧B4000仅作示例账为11,340,000次完整评价；E7第五臂增量为405任务而非30，五臂共2,025任务。27个独立单元在Holm最保守`alpha=0.01`下，80%/90%功效的标准化MDE为`d=0.644/0.733`。旧英国方差只能作结果盲方差代理，不能给出中国单一ETA或效应方向。包内16项哈希独立复算一致。

仍待用户逐项冻结：格内三图先图内种子均值再图间均值；每图先5共同种子并只按隐藏方向的残差方差统一补到7、7仍不足即HALT；E7第五臂先经过小/中/大三格的结果隐藏运行时门；算法六臂B1600机制门；各机制实质效应阈值；主要配对检验统计量。正式搜索仍禁止。

用户已批准目标：E3–E7 必须方向理想、效应量足够大且主要检验显著。尚未批准的是实现该目标的具体方法。

候选方法包括：以27个“地区×客户规模”为主要统计单位；每格三个互斥地图先聚合；每图至少五个共同种子；严格配对；五个主要检验族采用Holm校正；只按不含结果方向的方差规则扩样；为E3--E7分别设置责任错配、可移动充电量、非线性充电区、参与约束激活和动态多车场可行性的结果盲暴露门；E7增加“完整机制但碳盲”第五臂；算法采用六臂增量机制门。

其中27个“地区×客户规模”主要统计单元、严格配对、五个主要检验族采用Holm校正、结果盲暴露门已获批准。格内三图的聚合层级、每图最低种子数、E7第五臂、算法六臂机制门和效应阈值仍是待算力表与功效分析的提案；不得以部分批准启动正式试验、修改正式算法或宣布整份统计合同已冻结。

待用户裁决的核心问题是：是否批准上述统计单位、Holm校正、最低种子数、五类暴露门、E7第五臂和算法六臂机制门作为一个整体；若只批准部分，须逐项记录。

## 审批单 MC-003：2025年2月默认展示日

状态：`APPROVED_OPTION_C`（2026-07-18）。

完整正式面板固定保留2025-02-01至2025-02-28，不因展示日选择删日。代理推荐C=2025-02-12，因为它是三地区碳强度与电价档冲突最强且日内碳差仍较大的共同日期，适合展示成本—碳权衡；但用户此前明确要求默认日期选择权由用户保留，所以旧`selected_by=user_authorized_F4_decision`记载不成立，已纠正。

其他角色固定为A=2025-02-16（三地区共同日内碳差最大，作预注册敏感性）、B=2025-02-10（回场后差最大但北京偏重，作特例记录）、D=各地区分别选最大日（只作附录地区内图，不可跨区比较）。机器决策表已同步`selected_option=C`。

## 探索授权 EA-001：分机制算法创新候选池

状态：`EXPLORATION_COMPLETE_RANKINGS_FROZEN_PENDING_G0`。

用户授权代理自主搜索和尝试针对每个机制的高性能开源/文献算子，也授权设计精细的自主创新算法或算子。用户保留最终批准权。该授权允许文献检索、许可证核对、独立目录中的原型、功能/活性/速度探针和候选排序；不允许未经批准合入正式ALNS、修改正式实验合同、启动正式E1--E7或写成论文创新结论。

五个主题、两轮研究要求、候选字段和探针纪律见`docs/handoff/alns_mechanism_innovation_exploration_contract_20260718.md`。每个候选后续需要独立审批编号。

### EA-001 / 主题1：混合车队、车型选择、SOC与非线性补能联合机制

状态：`EXPLORATION_ONLY_AWAITING_USER_APPROVAL`。

2026-07-18已按用户指定的EA-001和`academic-deep-research`两轮方法完成研究。独立证据包位于`docs/handoff/algorithm_exploration_20260718/fleet_charging/`，含`report.md`、`source_evidence.csv`、`candidates.json`、`decision.json`和`artifact_hashes.json`。正式solver、`winner.py`、E7、模型、单位、物理合同和正式实验均未修改；正式搜索评价次数为0。

Cycle 2逐一复核后保留五个待审批候选：`FC-C01`为Hiermann等的车型感知`Resize/RelocateAndResize`及联合路线—车型插入；`FC-C02`为Froger等的固定路线非线性充电精确标签oracle及Apache-2.0实现`frvcpy`；`FC-C03`为MIT许可PyVRP的异构车队局部搜索、缓存和粒度邻域工程参照；`FC-C04`为Wang等HEVRP-NL的逐车型路线评价、非线性补能评估、ReplacePath、VND与路线池架构；`FC-C05`为自研候选“车型—模式后悔修复 + 分层非线性补能oracle（VMR-NL）”。其中自研候选的新颖性尚未证明，必须经过独立文献审查和消融后才可形成论文主张。

隔离功能探针只证明`frvcpy`能在论文示例路线插入充电站、PyVRP能在微型异构车队实例选择大车型，不证明性能优于当前ALNS。EA-001下可继续的隔离探索顺序为`FC-C02 → FC-C05 → FC-C01 → FC-C04`；`FC-C03`主要作实现和外部基线参照，这一顺序不构成正式实施批准。G0闭合前只允许预登记的预算0/1/2/5微探针，完整评价、cheap filter、oracle调用、缓存命中和路线池求解必须分账。任何车型语义、SOC安全边界、非线性曲线、充电站可达性、目标函数或接受规则变化仍须另行审批。

### EA-001 / 主题3：时变碳、分时电价、充电调度与非线性充电

状态：`EXPLORATION_COMPLETED_AWAITING_USER_APPROVAL`。证据包位于`docs/handoff/algorithm_exploration_20260718/carbon_nonlinear_charging/`，判定保持`EXPLORATION_ONLY_AWAITING_USER_APPROVAL`。

两轮研究已核验Montoya--Froger--Liang非线性固定路线标签法、Lin时变电价VNS/TS、Lu时间依赖IVNS、Cheng滚动碳感知调度、cspy、PathWyse和SAP车场共享容量排程。关键许可证结论是：`cspy`为MIT；PathWyse为GPL-3.0-or-later，实施前须单独裁决许可证边界；Cheng第一作者仓库可核但没有LICENSE，禁止复制；SAP仓库为Apache-2.0但已于2026-07-16归档。

本主题提出自研候选`DUAL_ORACLE_CARBON_PRICE_CONFLICT_CHARGING`，并将两类识别分开：机制层在同一路线、车辆、总电量、曲线、桩容量和oracle下比较`JOINT_COST_CARBON`与`COST_ONLY_BLIND`；算法层保持正式评价目标不变，只开关碳冲突算子。E7碳盲臂必须使用同一滚动机制和oracle，只隐藏未来碳信号，不能用简单插入对精确调度。

本记录不构成正式实施批准。EA-001已允许在独立目录创建候选原型，并运行不影响正式求解器的预算0/1/2/5功能探针；时间/SOC离散、非线性资源扩展、碳价/碳目标语义、GPL组件接入以及任何正式接入仍须另建审批编号。建议的正式审批编号已预留为`EA-001-C3-P1/P2/L1/R1`；当前全部未批准。

### EA-001-T2：多车场合作责任重划与参与公平

状态：`EXPLORATION_ONLY_AWAITING_USER_APPROVAL`。

用户明确指定第二个算法探索主题，并要求按EA-001和`academic-deep-research`完成两轮研究，只探索、不修改正式solver、`winner.py`或E7。2026-07-18已完成Cycle 1候选地图与Cycle 2原论文、官方代码、许可证、算子细节和E3/E6接口核验，交付5个外部文献/开源候选和1个精细自研候选。

关键事实是：当前E6公平约束已经进入完整候选评价、局部搜索和修复后的可行性检查，不是纯事后筛选；但当前评分只按每个违规成员增加一个`BIG_M`，没有按公平赤字大小引导，也缺少公平专用的多步跨场链和路线池重组。Soriano等（2023）原文证实其公平修正插入会在重构阶段偏向未达到利润目标的车场；PyVRP v0.13.4的MIT源码证实真实SWAP-star、SwapTails和Exchange可作为高性能实现参照；路线池集合划分可把参与下界放入组合主问题，但ReSETP非零碳配额下的成员碳成本分配不完全路线可加，必须先做列语义审计。

探索包位于`docs/handoff/algorithm_exploration_20260718/collaboration_fairness/`，机器判决固定为`EXPLORATION_ONLY_AWAITING_USER_APPROVAL`、`formal_search_allowed=false`。EA-001已允许创建独立隔离原型并运行预算0/1/2/5功能探针，无须为每次探索重复请示；未经新的用户明确批准，不得合入正式ALNS、启动E3/E6机制门、改变模型/单位/参数或写成论文创新。建议探索顺序是：公平修正插入隔离原型、MIT署名的真实SWAP-star/SwapTails适配、路线列可加性审计；G0闭合前不得据此宣称性能优越。

### EA-001-T4：E7动态滚动重规划

状态：`EXPLORATION_ONLY_AWAITING_USER_APPROVAL`。

用户明确指定第四个算法探索主题，要求按EA-001和`academic-deep-research`完成两轮研究，并禁止读取当前E7中间结果、修改E7保护文件、`winner.py`或正式solver。2026-07-18已完成Cycle 1动态/在线VRP、滚动时域、路线冻结、regret/ejection、稳定性目标和dynamic ALNS候选地图，以及Cycle 2原论文、官方代码、许可证、算子API、响应时间和E7公开事件语义适配核验。

探索包位于`docs/handoff/algorithm_exploration_20260718/dynamic_replanning/`，含5个文献/开源候选和1个自研候选。关键边界是：Wang等（2024）的`O(1)`只指预处理后单个插入位置的时间窗可行性更新；PyVRP的warm start不等于硬冻结；dvrpsim是事件状态框架而非求解器；OR-Tools可锁在线路由中已经行驶的部分，但不原生覆盖ReSETP完整的非线性充电、公平和碳语义；RoutingBlocks只有源文件级MIT声明，根目录缺LICENSE文件。

EA-001下正在按四个彼此隔离的微探针推进：当前状态伪车场序列化回读、冻结前缀保持、有限深度regret-ejection的活性与完整评价计数、路线稳定性指标独立复算。原型只能位于独立目录，不得读取本轮E7中间结果。未经用户新的明确批准，不得把依赖接入正式仓库、修改正式目标/接受规则/预算、增加正式E7实验臂或合入正式ALNS。机器判决固定为`EXPLORATION_ONLY_AWAITING_USER_APPROVAL`、`formal_search_allowed=false`。

## 审批单 MC-004：同路径有向道路三矩阵候选方法

状态：`APPROVED_METHOD_FULL_MATRIX_DEFERRED_TO_STAGE2`（2026-07-18；下方旧阻断说明保留为历史）。

用户明确批准MC-004，同时明确“暂时不进入阶段二”。批准范围仅为道路三矩阵生成方法：本地固定OSRM 26.7.3、冻结OSM、CV/EV隔离货运profile、逐有向点对请求，并从同一路线保存距离、时间和`Σ(v²d)`输入。此次批准不授权生成九城全矩阵、China81正式实例、任何求解器搜索或阶段二机制合入；`formal_search_allowed`继续保持`false`。

2026-07-18独立复核后的重提证据已经闭合：九个普通物流设施均形成明确标注为“情景道路接入点、非实测货车门”的WGS84点，并在五份冻结PBF上完成CV/EV 18/18最小路线；本地OSRM 26.7.3完整功能门18/18通过，覆盖高、宽、长、重、`hgv`、单行、不可达、方向性距离/时间、逐段注释和`Σ(v²d)`接口。证据分别为`data/ChinaInstances/phase1_nine_city_road_access_v1_20260718/`和`data/ChinaInstances/phase1_osrm_functional_gate_v3_20260718/`，哈希均可复算，正式搜索评价0。

现重提的MC-004只批准矩阵生成方法，不批准立即建全矩阵或启动试验：对每个有向点对使用本地固定OSRM Route API，同一返回路线同时保存道路距离、路线时间和运动学量`Σ(v²d)`；后者只是后续能耗模型输入，不冒充能耗本身。禁止反向复制、欧氏倍数和直线回退；不可达即在看算法结果前HALT该实例。完整China81矩阵仍放在阶段二输入生成门内。

2026-07-18阶段一前置包`data/ChinaInstances/china_mc004_mc006_preclosure_v4_20260718/`新增一个通过项：覆盖九城的五份2026-07-16固定OSM PBF已按Geofabrik大小、MD5和本地SHA-256完成5/5冻结，OSRM v5.27.1提交候选也已固定。阻断仍未解除：九个主场0/9货车入口闭合，本机无本地router runtime，默认car profile没有hgv访问层级且车辆尺寸/重量不匹配，纯电厢式车宽高字段为空，中国货运速度和缺失标签规则未批准。因此总判决保持`PASS_FIXED_OSM_INPUT_FREEZE__HALT_DEPOT_AND_FREIGHT_ROUTER_CLOSURE`，MC-004不重提批准。

候选方法对每个有向起终点单独请求OSRM Route API，从同一条返回路线的逐段distance/duration annotation同时得到道路距离、路线总时间和`Σ(v²d)`；不使用欧氏倍数、统一速度或反向复制。

北京三个冻结客户的最小探针对六个方向全部请求成功，距离、时间和`Σ(v²d)`三组节点对均显示方向差异；7项测试、Ruff和18项产物哈希通过。证据包为`data/ChinaInstances/china_road_matrix_probe_v2_20260718/`，候选实现为`baselines/china_instances/build_china_directed_road_matrices_v2_20260718.py`。

当前不建议批准为正式方法：九城货车入口仍为0/9，探针未含车场和充电站；公共OSRM端点没有暴露可冻结的路由器版本和地图SHA；普通`driving` profile未证明是中国货运配置。候选判决保持`EVIDENCE_OR_PROBE_READY_ROAD_METHOD_AWAITING_USER_APPROVAL`、`formal_search_allowed=false`。在本地冻结OSM提取、路由器版本、货车profile与正式入口闭合后，应重新提交审批，而不是直接沿用公共API探针。

## 审批单 MC-005：九城客户配额的GDP-PPS方法

状态：`CANDIDATE_A_APPLIED_POOL_AND_81_ASSIGNMENTS_PASS`（2026-07-18；正式搜索仍受阶段二物理核心与道路全矩阵等门阻断）。

候选A已按用户终裁写入机器合同。重庆池532、27/27格通过后，新的权威位置包`data/ChinaInstances/china81_customer_location_assignments_mc005_final_v2_20260718/`已重建81份、5805行；候选A配额逐份一致，27个单元内三个复本OSM身份零重叠，合同SHA与产物哈希复算一致，搜索评价0。旧位置包只保留历史，不再供后续构造读取。

2026-07-18前置已解除：固定外围20块新增227个与既有池互斥的OSM身份，重庆池由305增至532，超过成渝200格所需348；候选A 27/27格重新通过，独立回读`PASS_INDEPENDENT_READBACK`，52项哈希一致。证据为`data/ChinaInstances/china81_chongqing_mc005_pool_closure_20260718/`。边界不变：这些是可追溯POI身份，不是观测客户或订单；MC-004/MC-006和正式实例生成门仍未放行。

2024年现价GDP已实现九城同年、同单位、官方来源9/9闭合；同批快递业务量只有6/9精确数值，未被混拼。候选A保留“小规模单核心、中规模双城、大规模全城市群”的空间层级，按GDP排序激活城市，再在激活城市内使用PPS和Hamilton/Hare最大余数法形成整数配额。九个指定规模已检查无城市配额随总规模增大而下降，但最大余数法一般仍存在Alabama paradox风险。

证据和27组候选配额位于`data/ChinaInstances/china_city_pps_calibration_v2_20260718/`，判决=`EVIDENCE_READY_METHOD_AWAITING_USER_APPROVAL`。现有客户位置合同没有被修改。

需用户一起裁决：是否接受GDP作为配送活动代理；是否接受成渝小规模核心由成都改成重庆、珠三角中规模双城由深圳—东莞改成深圳—广州；是否统一使用九城同批初步公报，还是另做九城最终年鉴刷新；是否接受重庆全域GDP与中心城区客户池的空间口径差异。

若批准候选A，成渝200客户格需要三个复本共348个重庆身份，现池305，须先补抓至少43个并重过27格充足性门；不得为绕过短缺修改配额。

## 审批单 MC-006：九城车场继续闭合与备选切换

状态：`ACTIONS_1_AND_2_APPROVED_FOR_INVESTIGATION_ONLY`（2026-07-18；动作3逐场另批）。

当前主候选的园区存在和仓储/配送运营属性均有9/9公开证据，但精确WGS84货车入口、外部配送货车通行合同和完整货运充电硬参数均为0/9。事实包`data/ChinaInstances/china_depot_site_evidence_v2_20260718/`判决=`EVIDENCE_READY_SITE_SELECTION_OR_ASSUMPTION_AWAITING_USER_APPROVAL`，没有修改现有manifest。

候选动作一是保留九个当前普通商业园区，由代理在地图/浏览器中逐城定位货车入口，并在公开资料不足时请求用户按`proposed_manifest_patch.json`给出的最小步骤人工截图或联系园区。地图入口只能作为坐标证据，不能自动证明外部货车准入、时段和预约。

候选动作二是批准进一步调查深圳华南物流园和重庆京东亚洲一号。两者公开资料的货运充电证据强于当前主候选，但批准调查不等于批准替换；替换前还需比较普通商业属性、敏感性、入口、货车通行、充电硬参数、电价合同和与客户池的道路关系。

候选动作三是在实际现场数据持续不可得时，使用用户已允许讨论的“规划许可视为已投运”情景。该动作仍需逐场审批，并明确标注`ASSUMED_COMMISSIONED_SCENARIO_NOT_OBSERVED_OPERATION`；桩位不得自动等于桩数或枪数，功率仍须有独立产品/项目来源。

## 2026-07-18 批复记录（用户确认终裁；本节即批准记录）

授权链：用户 2026-07-18 在对话中明确确认以下终裁，并要求按该终裁同步机器合同和启动G0。对旧矩阵算力账、津石电价状态和重复微探针的三处纠正均被用户确认成立。

- **MC-001 = 条件批准选项 1**。容量占比构造代理进入正式基础情景；一切表述必须保持"容量占比构造代理"边界（非观测重量、非物理密度），论文与能耗模型永久携带该边界说明。
- **MC-002 = 拆分批准**。即刻批准原则层：27 个"地区×规模"统计单元、严格配对、五检验族 Holm 校正、结果盲暴露门。暂缓冻结：格内三图的聚合层级、种子数、E7 第五臂、算法六臂门、效应量阈值——须先产出正式算力表（事件流数、每任务评价预算、预计机器时）与功效分析，另行提交冻结。旧"整体批准+只多30项"说法作废（30 项系旧英国矩阵口径，不适用于中国合同）。
- **MC-003 = 选定 C（2025-02-12）**。选择主体：用户以"你来决定"将选择权转授 Claude，Claude 依结果盲决策面选 C（冲突相关 −0.682 最强、八槽碳差 378.183 次高）。A=预注册"碳择时上限日"敏感性；B=北京特例记录；D=附录橱窗、标不可跨区比较。机器决策表 `selected_option` 由 Codex 回填为 C；草案统计合同中提前写死的 `2025-02-12` 一致性问题就此合法化，但须补记"先草案后批准"的时序说明。
- **MC-004 = 当时暂不批准（历史裁决，现已被本文件MC-004顶部的后续批准覆盖）**。当时要求九城道路接入、本地冻结 OSM、路由器版本、货运 profile 闭合后按本地路由器重提；这些前置闭合后，用户已批准方法但未批准建全矩阵或进入阶段二。
- **MC-005 = 条件批准候选 A**。GDP 明确标注为配送活动代理（非订单量观测）；成渝小规模核心改重庆、珠三角中规模改深圳—广州写成规则推导结果；正式应用前先补抓重庆 ≥43 个客户身份并重过 27 格互斥充足性门。
- **MC-006 = 批准动作 1 + 动作 2（调查授权）；动作 3 仅逐场另批**。动作 1：保留九园区、代理逐城定位货车入口；动作 2：授权调查深圳华南物流园与重庆京东亚洲一号（调查≠替换承诺）；动作 3 "规划视为投运"每个采用车场单独登记 `ASSUMED_COMMISSIONED_SCENARIO_NOT_OBSERVED_OPERATION`，桩位不得写成投运枪数/功率。
- **EA-001 = 停止重复微探针**（四组隔离原型已有五记录面，边际价值低）；下一步冻结候选排序，G0 通过后按 FC/CF/CP/DR 四个主题包提交"进入机制门"审批；包内仍逐候选记录批准层级（继续原型/进入机制门/合入正式 ALNS），批处理只减少对话次数、不扩大批准范围。
- **阶段 2a/2b 拆分 = 采纳 Codex 精确版**：2a 通用搜索组件（SWAP*/SwapTails/route elimination/后悔插入/预算化局部搜索）G0 后在非 Solomon 开发集测试；2b 模型机制组件等非线性核心+正式分时信号接口后消融；并确认 Homberger 只证通用 VRPTW 能力，机制算子另需自有复杂模型开发集。
- **G0 = 立即执行**（用户"接替并继续未完成事项"授权 + E7 已释放共享内核 + 合同已冻结）。执行者：本Codex任务，合同=`e2_alns_budget_g0_patch_plan_20260717.md`。

G0执行终局（2026-07-18）：组合证据位于`baselines/e2_alns/e2_alns_budget_g0_20260718/`，判定=`PASS_ALNS_BUDGET_G0_COMPLETE`；静态阻断0、未分类0、行为与回归186/186。仅放行Homberger G1通用搜索组件开发；正式Solomon/中国胜负实验仍须后续门。MC-002延后项、EA-001四主题机制门和任何模型/单位/参数变化均未因G0自动获批。

## 审批单 MC-008：阶段二全部 G1 独立输入自主冻结

状态：`APPROVED_AND_IMPLEMENTED_AS_SCENARIO_INPUTS__FORMAL_G1_GATE_REMAINS`（2026-07-18）。

用户原话：`那你把不要G1的一次性全做掉。你自主解决，除非有需要我判断的。`

本授权只覆盖不会改变 G1 算法与非线性物理语义的静态输入、道路计算、结果盲统计阈值和
零搜索审计。已冻结中国成本情景、场充 22 kW×2 枪、公共充电 60 kW×1 枪及
0.40 CNY/kWh 构造服务费、2025-02 九城 48 槽电价—碳日历、E5/E7 实质效应阈值和
九城公共站身份。所有非观测值强制标为 `SCENARIO_PROXY`，不得写成全国统一价格或站点实测。

静态五记录包为 `data/ChinaInstances/china81_stage2_static_inputs_v1_20260718/`，共 81 实例、
5805 订单和 12096 条城市—日期—半小时记录，搜索评价为 0。道路执行最终采用 APFS 构建、
哈希验签后归档的区域稀疏连通图 v5，而不是保留失败的 exFAT v1--v3 或诊断 v4。
六张 CV/EV 图全部通过输入点吸附；v9 三矩阵物化 1,578,948 个有向点对，不可达 0，
不使用欧氏、反向复制、对称或直线回退。已分类的零时长子段、同吸附点、亚分辨率点和
8 个确定性有向绕点修复逐行进入独立诊断包。

最终汇合包 `data/ChinaInstances/china81_g1_independent_frozen_v2_20260718/` 判
`PASS_CHINA81_G1_INDEPENDENT_DATA_FROZEN__G1_PHYSICAL_SEARCH_ACCEPTANCE_HELD`，
81/81 通过、搜索评价 0。MC-008 的自主执行范围至此完成；唯一挂账是
`G1-FREEZE-MERGE`。G1 冻结前仍禁止非线性 SOC 正式验收、算法搜索可行性、
E1--E7 胜负和论文结论。

## 后续登记格式

每项新增审批必须记录：编号、原值/原语义、拟变更值/语义、来源链接与本地快照、转换公式与单位检查、影响的公式/代码/实验、替代方案、独立核验结果、用户原话或明确选择、批准日期、实施提交、回归测试和可回滚点。

## EA-HYBRID-003：结果优先的多引擎机制混合算法

状态：`PHASE1_EXECUTED_PUBLIC_HYBRID_STOPPED__CHINA81_HELD`（2026-07-19）。

用户原话：`我同意你的看法。你设计一套方案出来，然后执行。`

批准范围为阶段一隔离开发：冻结结果优先多引擎总合同；在同机单线程短墙钟下对
PyVRP HGS、PyVRP ILS 与项目 ALNS 做公开开发题核心筛选；建立新的有界
`SEG-GEN-02` 多车场连续段候选；运行单元/行为门和合同内的一次性低成本开发门；
强预警时才允许合同明确写明的三种子开发复核。算法身份不再限定为纯 ALNS，
但开源版本、许可证、论文引用、共同检查器、完整计算账和公平对手必须闭合。

未批准范围：阶段二、China81、E2--E7、正式 Solomon/Homberger 全量、正式统计、
论文优胜结论、正式 solver 合入，以及任何模型、单位、物理参数或目标语义改变。
旧 `SEG-GEN-01` 和旧 HGS--ALNS 失败证据保持只读，禁止在已见题上救援式调参。
实施合同为 `docs/handoff/outcome_first_multiengine_hybrid_contract_20260719.md`。

2026-07-19用户追加硬边界：`公开算例只考虑带BKS的原始开源算例，私有算例只考虑china81。`
据此取消尚未启动的donor03 100/150私有性能门（只生成输入，搜索评价0）；
SEG-GEN-02的旧英国夹具只保留单元/行为回归身份，不再进入性能结论。公开最小门改用
原始Solomon算例和SINTEF已独立复算BKS，BKS只在出解后关联；私有胜负必须等待
China81既有冻结与阶段二物理/搜索门合法开放，不得用其他自建英国题代替。

阶段一执行结果：`SEG-GEN-02` 的有界候选生成、零偷算和单车场不动作行为门通过；
第一组原始 Solomon+BKS 两秒核心侦察只形成 ALNS 快速压车辆、HGS 聚类型精修更强的
互补信号。随后两个预登记融合候选均未达到“总胜纯 HGS 和纯 ALNS”的硬门：
`ALNS-WARM-HGS-01` 对纯 HGS 为3胜2平1负、对项目ALNS为6胜0负，但因R111多一辆车
而汇总不敌HGS；`ALNS-DUAL-ELITE-HGS-02` 对HGS为2胜1平3负、对ALNS为5胜1平0负。
两者均时间公平、独立复算可行，但都已停止，不允许第三候选或在已见题上改比例。

China81 专用公平运行器已建成并在导入求解器前失败关闭：正式G1/China81冻结未开，
且没有公开混合强阳性，判`HALT_CHINA81_PRIVATE_GATE_NOT_ARMED`、搜索评价0。
移动硬盘`._*`旁车曾误入10份哈希清单；无重跑修复确认普通证据文件旧SHA-256
全部一致，只剔除旁车项。权威零搜索独立审计44/44通过；这只证明证据和停止线
自洽，不表示算法目标完成，不放行56题全量、China81、阶段二或论文优胜结论。

## EA-HYBRID-004：HGS 精英分歧驱动自适应 ALNS 最小门

状态：`EXECUTED_STOP_HGS_AEALNS_03_ANY_LOSS`（2026-07-19）。

用户原话：`我基本同意，允许你尝试。`

批准范围仅为候选 `HGS-AEALNS-03` 的隔离实现、单元测试、已见题行为冒烟，以及
一次结果盲的原始 Solomon+BKS 两臂小门。候选保留 PyVRP 0.12.2 HGS 的连续搜索
循环，在常规局部搜索后维护内部精英档案；连续 30 次未改善时，依据当前解与不同
精英解的邻接分歧移除连续客户段，或每第三次尝试清除一条容量下界以上的弱路线，
再用距离成本或精英邻接后悔修复并回到同一局部搜索。单次只接受共同惩罚成本严格
改善的子代，但等墙钟整机是否不退步仍由实验判定，不预先保证。

公开小门固定为此前两批融合门未用过的 `C106/C205/R109/R208/RC105/RC205`，
两臂为纯 PyVRP 0.12.2 HGS 与 `HGS-AEALNS-03`，共同 seed 1、共同确定性初解与
随机种群生成规则、单线程、每臂 5 秒；BKS 必须等 12 个求解任务全部结束后关联。
强阳性要求 0 负且至少 2 个严格胜、相对 BKS 的合计多车数不恶化、独立复算全过、
输入和保护面零漂移，并满足预注册时间容差。0 负仅 1 胜只记
`PROMISING_BUT_NOT_STRONG`；任一负局或 0 胜立即停止，不得换题、补种子、改参数
或在已见题上救援。

本批准不放行 Solomon 56 例、Homberger 全量、China81、阶段二、E2--E7、正式
solver 合入或论文优胜结论。完整合同为
`docs/handoff/hgs_adaptive_elite_alns_contract_20260719.md`。

执行终局：四项核心单测和1秒行为冒烟通过。冻结六题两臂小门对纯HGS为2胜2平2负；
候选在R109/R208严格胜、C106/C205平、RC105/RC205负，其中RC105为23路而纯HGS
为15路。候选相对BKS多车合计11，纯HGS为3；时间公平、共同初解、独立复算和零漂移
均通过，故按预登记硬门停止。独立零搜索审计60/60通过。源码与两个零搜索构造测试
确认修复代理缺少时间窗和新车固定费语义；这解释当前实现的碎路线风险，但不撤销
停止判决，也不自动授权修复后重跑。若继续必须新立`EA-HYBRID-005`，不得在已见六题
调参、补种子或救援。

## EA-HYBRID-005：HGS 安全记录型 LNS

状态：`STOP_SAFE_RECORD_LNS_INTEGRITY__EFFECT_NOT_ATTRIBUTABLE`（2026-07-19）。

授权依据：用户已要求自主完成强混合算法，公开原始BKS题必须不输纯HGS；持续目标要求
对上一候选的已证设计缺陷继续实质推进。本项只批准一个由死因直接推出的新候选，不授权
在已见题调参、扫参数或改变用户硬门。

相对`EA-HYBRID-004`只改安全语义：插入必须通过PyVRP同一时间窗/容量检查；新车固定费
和路线数进入插入排序；普通大改不得增路、删路大改必须少路；候选使用独立随机流；
只有完整可行且同时严格优于原子代和当前全局最好记录时才进入种群。`gamma=30`、
`dMax/dMin=30/15`、精英8、最长段12和其余HGS默认参数全部继承，不在旧结果上调整。

批准顺序为零搜索安全门，然后一次全新
`C105/C204/R108/R207/RC104/RC204`、seed1、两臂、单线程、每臂5秒门。只有0负且
至少2胜、BKS多车合计不恶化、时间和完整性全过，才自动追加seed2/3复核；否则立即停止。
完整合同=`docs/handoff/hgs_safe_record_lns_contract_20260719.md`。

未批准：已见六题救援、参数扫描、Solomon 56题、Homberger全量、China81、E2--E7、
阶段二、正式solver合入或论文优胜结论。

执行终局：冻结六题重算为对纯 HGS `1胜3平2负`，没有达到“0负且至少2胜”，
因此按预登记停止，未追加 seed2/3。更关键的是，R108、R207、RC104、RC204 四题
增强动作均未触发，但 R207 仍出现名义胜、RC104 仍出现负局，说明旧两脚本/计时路径
本身会改变结果。独立零搜索审计 62/62 通过并固定
`effect_attribution_valid=false`；权威目录为
`baselines/algorithm_prototypes/hgs_safe_record_lns_20260719/public_bks_seed1_gate_independent_audit/`。
该名义一胜不得归因给 LNS，不得救援，不得进入论文主表。

## EA-COMPARE-001：陈雨蝶式两类算例与两类对手重建

状态：`APPROVED_FOUNDATION_FREEZE__SEARCH_NOT_AUTHORIZED`（2026-07-19）。

用户明确要求拨乱反正：模仿陈雨蝶论文中公开标准题与自建复杂题的分工，以及外部
强手与母算法/组成算法的分工，不再临时挑六题和随意拼对手。终裁如下：

1. 公开算法主基准改为 CVRPLIB X-100；最终正式表必须覆盖全部100题。开发前只按
   原题序号和当前最优性状态冻结5题安全块、5题开发块、5题确认块、5题封存块，
   不按算法结果选择。
2. China81 是唯一完整模型算例，本轮不启动。英国自建题、临时私有题和其他
   China 子集不得替代 China81。
3. 公开主表对手按角色固定：BKS参照、母算法 HGS、大题独立强手 AILS-II、
   最接近的已发表混合体 HGSRR、组成方法 SISR、本文完整混合算法。异机文献值只作
   背景，同机强胜负主张必须由 HGS 与本文算法直接复算。
4. China81 后续主表只比较“同完成器的 HGS 路由核心、独立 ReSETP ALNS、完整
   混合算法”，避免拿原生不能表达完整模型的算法当稻草人。
5. 已证明最优题要求达到 BKS 且不输 HGS；未证明最优题保留“逐题严格胜最强对手”
   的研发期望。已证明最优值上不允许伪造严格改善。
6. 质量搜索前必须建立一个 `mode=hgs|lns|hybrid` 的同路径工作器；关闭增强时
   hybrid 与 HGS 在固定迭代下须逐位一致。

完整合同为 `docs/handoff/algorithm_comparison_foundation_20260719.md`。零搜索冻结
证据 `baselines/algorithm_foundation/x_cvrp_selection_20260719/` 已判
`PASS_X_CVRP_COMPARISON_FOUNDATION_ZERO_SEARCH`：官方目录100题、当前 Opt=yes
61题/Opt=no 39题；20/20 阶段题原始文件与固定 HGS 提交换行归一化后一致，20/20
BKS 路线独立复算通过；搜索评价0、China81未读未跑、阶段二未启动。

本批准只放行来源/BKS冻结、合同修正和同路径运行底座。不放行确认块、封存块、
X-100正式搜索、China81性能搜索、阶段二、模型公式变更或论文优胜结论。

## EA-COMPARE-002：对手先行的共同赛场重选与多重混合底座

状态：`PASS_FOUNDATION_MDVRPTW_V13_ZERO_SEARCH_AND_SAME_PATH__PERFORMANCE_NOT_STARTED`
（2026-07-19）。

用户进一步明确：公开算例不能因为方便而先定为 CVRP，必须先像陈雨蝶一样选择
有职责、有共同逐题结果的外部强手，再取它们共同使用且带 BKS 的原始开放算例；
China81 是唯一私有算例。算法身份、组成算法数量和融合方式不设人为上限，目标是
形成机制化多重混合算法并尽力产生新的公开和 China81 最好解，但当前先把比较基础
做对。该要求取代 `EA-COMPARE-001` 的 X-100 主场选择；X-100 零搜索包只保留为
被认真审计但未入选的候选证据，不得继续决定论文主表。

终裁与已完成事实如下：

1. 公开主场固定为 `V13-MDVRPTW-28`：`PR11A/B` 至 `PR24A/B` 共 28 道
   360--960 客户的大型多车场带时间窗题。它们同时覆盖多车场、容量、时间窗和
   车场车辆上限，与 China81 的基础结构相连。
2. 公开外部表按职责固定为：当前可验 BKS、经典 VCGP/HGSADC、当前可运行开源
   PyVRP 0.13.4、当前相近 CPU 混合算法 MDFIHA、GPU 混合算法
   MDFIHA-ETGA、本文完整算法。未公开代码的论文对手只比逐题目标，不伪造同机时间。
3. 公开血缘解释题事前固定为 `PR17A`，比较母体、独立破坏重建/精修、部分混合和
   完整混合；公开主表不铺满每个零件。
4. 开发块为`PR11A/B、PR17A/B、PR21A/B`，确认块为
   `PR16A/B、PR20A/B、PR24A/B`，其余 16 题封存。划分只看车场层级、规模和
   A/B 身份，不看算法结果。
5. China81 最终全 81 题只比同一 ReSETP 完成器下的母体、独立机制 ALNS 和完整
   混合体。先封存两组成算法形成 `China81-BKS-v0`，再打开完整混合体；实例、解和
   检查器未共同公开前，只能称“本研究当前最好可行解”。
6. 多重混合研发蓝图允许引入多群体、ALNS、VNS/局部精修、路线池精确重组和以后
   经证据支持的其他成熟组件，但必须共享解表示、评价器、精英库和贡献账；不贡献
   成绩的组件删除，不能仅为“看着高级”保留。

零搜索来源/BKS冻结位于
`baselines/algorithm_foundation/mdvrptw_v13_comparison_20260719/`，判
`PASS_MDVRPTW_V13_COMPARISON_FOUNDATION_ZERO_SEARCH`：28/28 原始与规范化题
语义一致，28/28 当前 BKS 经官方和独立检查双过，28/28 论文值抽取完成，搜索评价和
求解器调用均为0。当前可验 BKS 在28/28题上均优于2026论文的MDFIHA-ETGA值，
所以逐题目标以可验 BKS 为准。

同路径底座位于
`baselines/algorithm_foundation/remix_same_path_equivalence_gate_20260719/`，
判`PASS_REMIX_SAME_PATH_EQUIVALENCE`：母体、增强关闭、增强零预算三种模式在
`PR11A/seed1/5迭代`上产生相同解签名，组件调用0，输入、版本和独立验解一致。
两份证据清单另行复核均为0个哈希失败。

本条只确认比较基础和无性能接线已经完成。它不授权 China81、阶段二、确认块、
封存块、完整28题、正式优胜主张或在已见结果上换题救援。下一步必须先停下复盘；
若继续，只从`PR17A`单种子低成本行为门开始。

## 2026-07-18 阶段一三项阻断集中重提（等待用户批准）

集中审批单为 `docs/handoff/phase1_three_blockers_approval_sheet_20260718.md`，状态=`SAFE_PREPARATION_COMPLETE_AWAITING_USER_APPROVAL`。本轮只完成公开入口证据复核、固定路由器构建条件检查、福田 ES1 同行外形参数补证和 MC-002 六条统计规则压缩；未安装路由器、未换算正式入口、未修改车型机器合同、未生成道路矩阵、未启动正式搜索。

待批编号为：`P1-APP-01` 基础情景采用 ES1 厢式外形高 3.250 m（3.050 m 仅作敏感性）；`P1-APP-02` 原始坐标留存—固定换算—OSM复核—第二来源确认的入口转正流程；`P1-APP-03` 固定 OSRM v5.27.1 源码构建和单个裁剪小地图测试；`P1-APP-04` 车辆尺寸/重量及 OSM 货运和物理限行标签的隔离 profile 小测试；`P1-APP-05` MC-002 三图估计量、5→7结果盲补种子、E7 75任务隐藏运行时门、六臂B1600仅作阶段二上限、小效果阈值分终点预锁、27格配对随机化检验+敏感性检验+Holm。

以上任何一项只有用户明确批准后才实施；其中 `P1-APP-03/04` 即使批准也只放行最小功能测试，不放行五省全图、九城全矩阵、中国81正式实例或阶段二。

## 2026-07-18 P1-APP-01 至 05 用户全准与最小充分口径

用户原话：`P1-APP-01 至 05 全准。但是好像有很多是多余的吧，别的论文忽略的你也可以忽略。`

终裁解释：论文算例不再承担真实物流项目尽调。车场硬门缩为“普通物流设施身份有可信来源、道路接入点可复算、冻结路网上车辆可达”；门禁时段、预约证件、停车/月台和园区真实充电枪台账不建模、不声称、也不阻断阶段一。车场充电若使用，只能作为统一事前登记的基础设施情景，不能写成九园区现场实测。

`P1-APP-01`已将福田ES1基础路由外形锁为`5.995×2.200×3.250 m / 4495 kg`，`3.050 m`仅作敏感性。`P1-APP-02`已写入道路点换算、WGS84复核及“合理道路接入点可作合成算例节点但不得冒充已证实主货车门”的边界。`P1-APP-04`已生成CV/EV两份隔离货运profile并通过2/2静态核验；没有自创中国速度。`P1-APP-05`已冻结27格、三图聚合、5→7结果盲补种子、配对随机化主检验、敏感性检验、Holm和运行规模上限；全量仍未授权。

`P1-APP-03`执行遇到工具链阻断：源码和提交确认为v5.27.1；Homebrew依赖、CMake 3.31、Boost 1.85、Lua 5.4和TBB已准备，旧源码在当前AppleClang 16编译至约78%时因捆绑sol2模板与当前编译器不兼容失败。仅做了两个构建兼容调整：关闭“警告即错误”，以及将已删除的Boost convenience头替换为总filesystem头；未改路由逻辑。当前未安装成功、未跑小地图、未用26.7.3偷换。若改用Homebrew当前OSRM 26.7.3，须用户批准版本变更。

## 2026-07-18 OSRM 26.7.3版本变更批准与MC-004阶段一前置收口

用户随后明确批准版本变更，并要求除算法优化外尽快收口阶段一。已安装并钉住OSRM 26.7.3；二进制与基础`car.lua`均记录SHA-256。隔离CV/EV货运profile在人工小路网通过4/4规则断言：2.480 m柴油车使用3.0 m限高短路，3.250 m电动车绕行，两车均不使用`hgv=no`捷径。证据包为`data/ChinaInstances/phase1_osrm_smoke_v2_20260718/`，判`PASS_OSRM_2673_FREIGHT_RESTRICTION_SMOKE`。

终裁：MC-004的阶段一方法前置闭合，但这不是九城矩阵结果，也不授权正式搜索。五省建图、九城有向距离/时间矩阵、China81实例与全量实验均未运行，留待阶段二另行执行。旧v5.27.1构建失败证据保留，不改写。

## 2026-07-18 EA-HGS-001：官方HGS先B后A

用户原话：`那就搞官方HGS试试水。先走B，如果有强烈的向好预警，就果断走A。`

终裁状态：`APPROVED_ISOLATED_OFFICIAL_CVRP_ENGINEERING_ONLY`。B门在运行前固定为三个不属于CVRPLIB冻结六题的开发题`X-n110-k13/X-n157-k13/X-n190-k8`、三种子、每算法每题3秒配置；升级要求官方测试和18个共享任务全有效、HGS各题中位gap不超过1%、总体中位gap不超过0.5%、至少两题相对当前ALNS中位成本改善2%以上且无题退化超过0.5%。

实际判定=`GO_A_ENGINEERING_DISCOVERY`：18/18任务有效，三题HGS中位gap为0%、0.0178%、0.9187%，相对当前ALNS中位成本改善11.2198%、12.1617%、12.7406%，三题全部达到明确胜出。A随后完成固定提交本机构建、MIT许可记录、官方5项测试、原生C++ CLI的Python桥、二进制哈希验证和独立客户覆盖/容量/目标复算；单任务验收判定=`HGS_CPP_PYTHON_BRIDGE_READY`。

批准边界：不翻译C++源码，不替换现有Python环境，不修改正式ALNS或三份保护文件；官方HGS-CVRP只作为隔离CVRP引擎和结构参考。未批准Solomon/Homberger、China81、E2--E7重跑、正式solver替换、VRPTW/异质车队/SOC/充电/电价/碳/公平/多车场扩展或项目阶段二。证据为`baselines/e2_alns/official_hgs_b_gate_20260718/`与`baselines/e2_alns/official_hgs_a_bridge_smoke_20260718/`。

## 2026-07-18 MC-007：阶段二 G1 独立输入线

用户已批准在 G1 开发期间独立完成不依赖 G1 的输入、数据与治理工作，并要求立即执行。批准范围为本地 OSM/OSRM 图构建与有向道路矩阵准备、九城入口与基础设施证据、中国参数证据、MC-001 订单属性层、China81 数据层、零搜索数据检查，以及统计、运行器、监控和审计基础设施。

终裁状态：`APPROVED_STAGE2_G1_INDEPENDENT_INPUT_WORK__FORMAL_ACCEPTANCE_HELD`。执行合同为 `docs/handoff/china_stage2_g1_independent_execution_contract_20260718.md`。

边界不变：G1 冻结并与阶段一统一前，不得进行正式算法验收、正式 Solomon/Homberger/China81 胜负实验、非线性补能机制正式验收、强基线正式比较或论文结论。上述独立产物只能作为待汇合输入，不自动放行正式实验。

## 2026-07-18 EA-HGS-002：机制化 HGS--ALNS 隔离开发

用户原话包括：`把ALNS装进HGS。我批准`、`必须有效果，有故事，有机制。你去做吧。以最成本验证`，并追加硬要求：`HGS-ALNS我们自研后，必须要能干过纯粹的HGS和ALNS，绝对不能开倒车`。

终裁状态：`APPROVED_ISOLATED_MECHANISM_HGS_ALNS_DEVELOPMENT_ONLY`。允许在独立目录中实现“外层维护多个不同方案、内层调用现有ALNS精修”的最小组合，并接入四个已通过功能探针的机制候选：车型—补能联合修复、公平缺口定向修复、碳—电价冲突充电修复、冻结前段的动态有限连锁修复。允许补齐论文与手册来源，允许预算0/1/2/5行为门和少量非正式开发题三方比较。

强制过门：组合算法必须在相同开发题、配对种子、相同完整方案评价次数和同一独立复算器下，同时优于纯HGS与纯ALNS；同时报告墙钟时间和所有切分、充电精算、重组调用。只赢一方、靠额外算力取胜、机制动作没有实际触发、或收益只来自通用群体管理，均判设计未通过，不得进入正式试验或写成机制创新。

批准边界：不得修改`cost.py`、`check.py`、`search/evaluation.py`及冻结结果；不得启动Solomon/Homberger正式集、China81、E2--E7重跑或阶段二。最小开发门即使强阳性，也只形成“向用户申请正式试验”的资格，不自动放行正式实验。

执行结果：预算0/1/2/5功能与计数门通过。通用组合在25客户三结构、三种子、B100下仅6/9双赢，判`HOLD_REDESIGN_NOT_DOUBLE_WIN`。用户随后建议只在多车场场景开启；结果盲开关在25客户小门达到启用3/3双赢、禁用6/6精确退回ALNS，但多车场10/15/20/25/50客户放大门仅4/15双赢，20和50客户中位分别退步5.97%和6.22%，判`HOLD_MULTIDEPOT_SWITCH`。不得按已观测的25客户规模事后设开关。正式试验、正式合入、阶段二及论文创新主张继续不批准。

用户随后明确取消开关并要求继续优化。EA-HGS-002隔离范围内完成两次止损迭代：真正交错的“群体子代+短ALNS教育”在多车场五规模十五个配对任务仅1/15双赢；保留ALNS主体、固定追加跨车场自由重插责任移动和车型—补能收口后，多车场旧开发面仅5/15双赢且责任移动大多失活，不能作为多车场机制证据。转入已批准的280kWh诊断开发面后，25/50客户六任务6/6双赢；20客户B100为1负2平，B500时组合和纯ALNS三种子均精确为`621.2913242409613`。按现有“每个配对任务必须严格双赢、平局失败”合同，总判仍为`HOLD_REDESIGN_NOT_DOUBLE_WIN`。若用户希望把可独立复算的共同饱和值平局记为非退步通过，必须另行批准修改验收规则；在此之前不得申请正式试验。

用户进一步把“必须胜过原装开源算法”明确为 E2 的底线，并把具体设计选择交由本 Codex。为防止把无效拼接写成创新，追加了同预算拆件门。官方 HGS 的直接插入版只在 1/6 对严格优于去掉 HGS 的版本，HGS 先手只直接改善 1/6；后续把官方 HGS 原生路线放入路线池重组的两题 seed1 小探针也没有产生最终增益。两条 HGS 接入均止损，HGS 从候选内部组件降为强制外部对照，不再以“HGS--ALNS 成功融合”申请论文身份。

在不改模型、评价器和保护文件的前提下，隔离候选转为“机制优先 ALNS”：现有 ALNS 负责客户分组与顺序，新建车型—充电整套方案选择器为每条路线构造可行燃油车/电动车方案，再在路线之间一次性选完整组合，局部代理筛选另记账，最终只让最佳完整组合接受一次统一评分。固定 280 kWh、20/25/50 客户、三共同种子、B100 五方门的结果为：同时严格胜固定官方 Vidal HGS-CVRP 中性适配和原装 `alns 7.0.0` 中性适配 9/9；相对当前项目 ALNS 为 6 胜、3 平、0 负；相对拿掉联合选择的版本为 6 胜、3 平，其中 25/50 客户两项均为 6/6 严格胜出，20 客户三对仍到 `621.2913242409613`。九任务总墙钟为候选 7.231 秒、当前 ALNS 6.462 秒，即候选在本短门约慢 11.9%；两套原装开源对照分别为 19.874 秒和 10.553 秒，短门墙钟不作正式速度结论。因此“原装开源底线”在开发小门成立，但跑前完整合同仍未成立，机械判定=`HOLD_FULL_CONTRACT_SMALL_SATURATION_OR_COMPONENT_GAP`、`formal_search_allowed=false`。多车场责任、公平、碳—电价、动态订单仍需各自病灶门；本记录不授权改平局规则、正式合入、正式 E2、China81、E2--E7 重跑或阶段二。

继续执行没有修改平局规则。独立复算证明旧20客户共同值并非全局最优：固定路线、车型、电量和充电时长，只移动两个充电开始时刻即可把成本从`621.2913242409613`降到`621.0831141941019`且零违规。随后在EA-HGS-002既有隔离权限内完成五机制统一开发收口，证据=`baselines/algorithm_prototypes/mechanism_hgs_alns_20260718/mechanism_v7_stage1_closeout_gate/`，判定=`PASS_STAGE1_ALGORITHM_INFRASTRUCTURE_DEVELOPMENT_CLOSEOUT`。静态20/25/50客户×三种子×B100为9/9同时严格胜官方HGS中性适配、原装`alns 7.0.0`中性适配和当前项目ALNS；多车场18项中4项责任动作绑定并改善`1.2652%--5.6731%`、14项精确不倒退；固定100%独立收益底线在1个绑定种子完成修复、2个已达标种子零动作；冻结E7单一真实新增订单事件保持历史与资产合同并少1条未来路线、成本降`29.675379`。62行原始记录、六件主展品哈希和上游v6证据已独立重算，15项回归通过，三份保护文件零差异。

终裁边界不变：上述PASS只关闭EA-HGS-002的阶段一算法基础设施开发，不批准正式性能结论、正式solver合入、Solomon/Homberger正式benchmark、China81、E2--E7重跑或阶段二；证据文件继续固定`formal_search_allowed=false`、`formal_solver_integration_allowed=false`、`stage2_allowed=false`。分时电价和非线性充电仍依赖阶段二模型接口；公平仅1个绑定任务、动态仅1个真实事件、多车场三个强对照任务属于事后确认。进入下一阶段必须由用户另行批准并先冻结公开benchmark、统计样本、等预算/墙钟双轴和受影响实验范围。

## 2026-07-19 MC-008 失败关闭复核补充

MC-008 的授权范围不变。本次只修复其执行与审计入口中四类可能误写 PASS 的缺口，并
重新验签既有权威包；没有改变模型、代理、实例、路线或参数。复核包
`china81_fail_closed_review_v1_20260719` 对六个权威包逐文件复算哈希，并确认六批缓存
计数、1,578,948 个实例内有向对、81 个实例和 5805 条订单一致，判定
`PASS_CHINA81_FAIL_CLOSED_REVIEW__FORMAL_G1_GATE_REMAINS`。这不扩大 MC-008 为
正式验收授权，G1 汇合门继续关闭。

## EA-COMPARE-003：ReMIX V13 最低成本行为与六题开发门

状态：`EXECUTED__STOP_CURRENT_REMIX_V13_CANDIDATE_ALL_LOSS`（2026-07-19）。

用户在 `EA-COMPARE-002` 基础上批准设计并执行最低成本算法方案，同时明确：公开只用
带 BKS 的原始开放算例，私有只用 China81；当前先打好基础，最强算法逐题输给本文是
最终期望，不允许用平均数掩盖单题失败。

执行范围只包括：同路径关闭门、PR17A 行为门、HGS 路线供应桥和六道开发题
`PR11A/B、PR17A/B、PR21A/B` 的单种子十秒门。没有打开确认块、封存块、完整
28 题、China81 或阶段二。

同路径、行为和路线桥均通过，说明开关、调用账、跨版本路线重建和独立验解正确。
六题性能结果为 `0 胜、0 平、6 负`，新 BKS 为 0；六题较强可运行核心均为
PyVRP 0.13.4 ILS。全部 18 个最终解独立有效，全部端到端不超过十秒，三份保护源码
未变，证据哈希 34/34 复算通过。

运行器原判 `HOLD_MIXED` 与“有胜有负”的合同定义不符，原证据保持不可改写；
权威判定由
`docs/handoff/remix_v13_development_forensics_20260719.md`
更正为 `STOP_CURRENT_REMIX_V13_CANDIDATE_ALL_LOSS`。当前候选禁止扩题、延时、
补种子或调参救援。

若用户复盘后另批继续，候选方向限定为把多车场/时间窗 ALNS 大扰动放进 ILS 原有
主循环、替换一次原生扰动；不再削短 ILS 去并排运行 HGS 首解、固定五轮外置增强、
重复局部搜索或常开末尾路线会审。该方向尚未获执行批准。

## EA-MPILS-MVNS-001：多种群ILS—机制增强VNS最低成本门

状态：`EXECUTED__STOP_CANDIDATE_1_AFTER_ONE_WIN_TWO_LOSSES`
（2026-07-19）。

用户原话：`工作名模仿陈取算法名的方法，取一个好听的差不多类型的。跑公开算例，
机制可以无效或者跳过，但是不能关闭，不然这就是另一个算法了。我允许你按照规划去执行。`

据此批准工作名`MPILS-MVNS`，允许在PyVRP 0.13.4 ILS同一主循环内实现三类小种群、
停滞触发的连续段跨场重组/精英路线重组和五机制常驻问询。公开原题缺少某机制字段时
只能登记自然不适用，不得设置关闭开关或另建公开精简算法。

五项接线和行为门全部通过。三道已暴露工程题`PR11A/PR17A/PR21A`的一次性短门为
1胜0平2负：改善0.521%、退步0.356%、退步1.219%。全部六解独立有效、墙钟合规、
五机制常驻，多车场责任共产生152次候选，三份保护文件未变化。按“任一题退步即停”
合同判`STOP_ANY_LOSS_OR_INTEGRITY`；实际触发原因是性能退步，非完整性失败。

当前候选、阈值、种群容量和调度全部冻结；不得在已见三题上调参救援，不得打开
确认题、完整28题、China81、阶段二或论文优越性主张。若继续做结构不同的新候选，
须用户再次明确批准。权威合同与收口分别为
`docs/handoff/mpils_mvns_architecture_and_microgate_contract_20260719.md`和
`docs/handoff/mpils_mvns_candidate1_closeout_20260719.md`。

## EA-MPILS-MVNS-002：第二候选替换式扰动 G0

状态：`EXECUTED__STOP_REPLACEMENT_LINE_AFTER_SECOND_FIRE`
（2026-07-20）。

用户原话：`继续。但是claude的方案，除非是明确错误或累赘，否则可以尝试claude的安排。`

据此批准第二候选的修订合同与最低成本G0基础设施：保留强ILS母体、事件驱动三视角
记忆、替换式多车场连续段扰动、母体局部精修、独立随机流和五机制常驻；不自动批准
性能短门、确认题、完整28题、China81、阶段二或论文主张。

执行前核验纠正四项：PR11A单种子改善不能与少跑17%--22%迭代线性相加；固定
1/2/3段升级不是AILS-II原规则，只能标成本项目反馈规则；PyVRP 0.13.4没有现成的
接受事件/扰动替换回调；China81 `SEG-GEN-02`不能原样迁入V13公开题。修订合同为
`docs/handoff/mpils_mvns_c2_revised_contract_20260720.md`。

G0执行结果：上游和迁入ILS在PR11A固定5/1000/5000迭代下最终解、逐迭代记录、
母体随机数状态全部一致并独立有效；5000迭代×5交错重复中，常驻事件层相对上游
中位墙钟比1.008115，低于1.02门；人工夹具中专用动作真实替换本轮原生扰动并守住
64/每段6/48/12预算，五机制常驻且缺失语义自然不适用。

首次G0因本地MIT文本末尾多一个空行判FAIL，原失败证据保持不改写；零求解补正后
本地许可证与PyVRP 0.13.4上游逐字节相同，有效判定
`PASS_MPILS_MVNS_C2_G0_AFTER_LICENSE_REPAIR`。终审删除未使用导入及多余末尾空行；
首次源码卫生补正因少算一个字节差异和缓存旁文件而FAIL并保留，v2以旧SHA反推确认
两处均无执行语义，并重过Ruff、内存编译、5迭代等价和行为门。当前源码最终判
`PASS_MPILS_MVNS_C2_G0_CURRENT_SOURCE`。当前性能状态仍为UNKNOWN。
`PR11B/PR17B/PR21B`首次性能短门、确认题、扩种子、完整28题和China81须用户另批；
已暴露A题只作工程归因，不得用于C2性能调参。

用户随后明确批准G1 B组首发及两发协议。G0/源码提交`2f434816`先冻结，G1合同和
运行器提交`3fb6c925`后才允许搜索；两个搜索前启动错误由`d013d892`只修环境和Git
祖先检查，未进入求解。首发PR11B/PR17B/PR21B、seed1、每臂8.5秒，同批母体，
六解全部独立有效且完整性门全过，但性能0胜0平3负：
`-0.666%/-2.404%/-3.259%`，判
`FAIL_G1_B_FIRST_FIRE__DIAGNOSIS_REQUIRED`。

候选仅完成母体50.7%/47.7%/39.3%的迭代，28--39次段搜索触发是明确活动开销；
PR21B 28次全部无可用候选仍丢约61%迭代。因此满足用户预登记的常数死因条件，允许
只用A组三题做一次W/冷却期或升级顺序诊断，并只作一次常数修订。不得加模块、扫参数
或看确认题；重新冻结后B组只许第二发，再败即终止替换式扰动路线。

A组三次母体按预登记15%公式把W/冷却从128唯一修订到1666；未扫参数或改其他
结构。第二发PR11B/PR17B/PR21B为1胜0平2负：
`-0.430%/+0.000612%/-0.180%`，仍违反零负门。微胜题专用动作零触发，三题机制
动作刷新全局最好均为0。最终判
`STOP_MPILS_MVNS_C2_REPLACEMENT_LINE_AFTER_SECOND_FIRE`，不再修订；G2、完整
28题、扩种子、China81、阶段二和论文主张全部保持关闭。

## 2026-07-20 MC-NL-003：非线性动态继承 NL3a

状态：`EXECUTED__PASS_NL3A_NONLINEAR_DYNAMIC_INHERITANCE`。

依据用户对非线性物理闭合、后续 China81 最低成本验证和自主推进的明确授权，以及
`china81_nonlinear_core_g1_contract_20260720.md` 已登记的 NL3 授权，本门只迁移
动态续排中残留的恒功率计算，不改变曲线参数、目标函数、算法候选、实例或论文结论。

动态候选、趟链电池账、动作验证、低碳择时和滚动切割现在共用 NL0 曲线内核。NL90
动作带起止电量与曲线身份，缩短时长和缺元数据失败关闭；L100 与旧线性公式误差为 0。
22 项相关回归和 Ruff 通过，权威包为
`baselines/model_verification/china81_nonlinear_dynamic_nl3a_20260720/`。

本门搜索评价为 0，不授权算法性能结论。下一步授权仍限 NL3b：把已冻结的 CV/EV
两类车辆道路 profile 接入，每类恰有距离、时长和 Σ(v²d) 三矩阵；不得误建第三种
车辆，不得在 81/81 零搜索物理预检前启动 China81 胜负搜索。

## EA-MV-HGS-SP-001：多视角 HGS 与精确路线池重组最低成本门

状态：
`EXECUTED__PASS_PRIVATE_DIRECTIONAL_ARCHITECTURE_FREEZE__FORMAL_HELD`
（2026-07-20）。

授权依据是用户要求不限制算法母体和组成数量，以性能、机制故事和后续实验可用性为
中心自主推进，同时要求最低成本验证、证据向好才扩种子，并在跑出值得保留的结果后
停下复盘。本门仅在
`baselines/algorithm_prototypes/china81_mechanism_hybrid_20260720/` 隔离执行，
没有打开完整 China81、`V13-MDVRPTW-28`、阶段二或论文正式结论。

执行中先纠正算法身份：PyVRP 0.12.2 是 HGS，0.13.4 是 ILS。最终候选
`MV-HGS-SP` 使用三个真正 HGS 种群形成燃油、简化电动和机制电动路线视角，以完整
非线性 ReSETP 模型重排精英，再用 SciPy/HiGHS 集合划分精确重组路线；只有完整目标
严格改善时才采用重组，否则保留最好的完整 HGS 父方案。

H4 开发门 2 胜 1 平 0 负，H5 新题门 2 胜 1 平 0 负，H6 三题三种子
5 胜 4 平 0 负。75 客户层 3/3 改善，150 客户层 2/3 改善 1 平，
25 客户层全平；九解全部可行，34 项相关测试和 Ruff 通过，H6 十个登记文件哈希
复算一致。

本裁决只证明精确重组相对同批三个 HGS 视角中最好的完整父方案存在方向稳健增益。
候选额外使用精确重组算力，尚未完成等墙钟纯 HGS/独立 ALNS、China81 全 81 题或
公开 BKS 比较；公平和动态机制没有独立贡献，ALNS 后接探针无增益。H0 早期适配器
源码哈希因后续扩展而漂移，H6 当前源码和证据哈希闭合。按预登记停止加零件，
`formal_search_allowed=false`、`stage2_allowed=false`，等待用户复盘。

## E2-CHINA81-VIEW-ABLATION-001：China81 私有表对比协议 = 三视角子算法 + 收敛式

状态：`APPROVED_BY_USER_2026-07-22`。

用户 2026-07-22 明确拍板 E2 两张表的对手选择与目的，覆盖施工图 §1.2/§1.5 中
"PyVRP-HGS / ReSETP-ALNS / MV-HGS-SP" 的旧映射：

1. **对比协议变更（须登记）**：China81 私有表不再声称对比外部独立 ALNS。经核对，
   `paper_main.tex` 行958 的 "ReSETP-ALNS…\cite{ref:66}" 是撰写时编造的算法名
   （ref:66 实为 Ropke-Pisinger 2006 ALNS 开山论文），用户从未命名过 ReSETP-ALNS；
   用户实际持有第三方开源 ALNS 与自产加强版 ALNS 两者，**均不进任何对比表**（MV-HGS-SP
   内部为 HGS 非 ALNS）。China81 主表改为 **MV-HGS-SP 的三个视角子算法
   （`cv_only`/`naive_ev`/`mechanism_ev`）各自单独收敛 vs MV-HGS-SP 三视角+SP 融合体**，
   证 1+1+1>3。去 SP 消融（旧 ablation 臂）退出主表，降为附录组件消融证据。
   P0 的 D 臂自产 project_alns（15:0 输给 MV-HGS-SP）留在 P0 保险丝内当历史，不进主表。

2. **方法变更（须登记）**：私有表采用**收敛式**终止（各臂 NoImprovement 跑到自身收敛，
   报各自 CPU），偏离施工图 §P2 规定的"等墙钟 T"。理由：表6 需报 CPU 对比，等墙钟下
   各臂 CPU≈T 无对比意义；收敛式与已通过的 P3/G-CHINA-REP 及母版陈2025 表6（各算法
   收敛 CPU）一致。诚实边界：收敛式下完整体使用算力多于任一单视角，私有表判读为
   "质量结果 + CPU 全披露"（陈的框法），不得暗示等算力优越。

3. **保护边界不变**：`cost.py`/`check.py`/`search/evaluation.py`/`prices.py` 默认值、
   主 TeX 语义段零改；补跑仅换 `route_proxy_mode` 复用 P3 收敛式 runner，不新增算法机制。
   本变更只改"对比对象与终止准则"，不改目标函数、约束、算例或评价器。

执行路径见任务 S1–S7 与 `docs/handoff/memory/e2_final_campaign_p3_p4_complete_20260722.md`。
预注册停止条件：S1 preflight 若三视角不可行或塌成同值即 HALT 报用户，不得推全量；
任一阶段数据质量门失败照实 HALT，不得为救 1+1+1>3 故事改方向。

## S2-INFEASIBLE-UNIT-001：China81 单视角不可行单元的统计口径

状态：`APPROVED_BY_USER_DELEGATION_2026-07-22`（用户令 Claude 站在其角度按论文
写作与呈现决策；本条为统计口径登记，可被用户否决回滚）。

事实：S2 全量补跑 810 个新单元中恰 1 个失败——`cn-jjj-200c-01`、种子2、HGS-E
（`naive_ev`）：简化代理认为可行的路线在完整模型精确核算下客户 C099 迟到
0.388 秒（相对约 49305 秒时窗），完成器判无可行 all-CV 补全，确定性可复现。
同题其余 4 种子 HGS-E 全 OK；HGS-F 810 行 0 错；MV-HGS-SP 同单元可行
（9483.789543021865）。原 `HALT_S2_VIEW_INFEASIBLE_OR_ERROR` 决定文件原样留档。

口径（预登记，S3–S7 及论文一体适用）：
1. 该单元如实记 `INFEASIBLE`，禁止换种子重跑、放宽时窗、修改完成器/评价器
   （结果过滤红线；且失败为确定性，重跑亦不改变）。
2. 汇总统计中，该算例 HGS-E 的 Best/avg 按其余 4 个可行种子计，表注披露
   "HGS-E 在 1 个算例的 1 个种子上无完整模型可行解（简化代理时间窗误差）"；
   可行率如实报告：HGS-F 405/405、HGS-E 404/405、HGS-M 405/405（P3）、
   MV-HGS-SP 405/405（P3）。
3. 论文叙事允许（且建议）将其用作"完整模型裁判必要性"的实证依据写入算法章
   ——属合法设计放大器，不是结果过滤。
4. 流程：Codex 以 v2 修订判定收口 S2（原 HALT 证据不改写），链条放行 S3。

## S3-TRAJ-CURVE-DEF-001：图4 迭代曲线的观测口径

状态：`APPROVED_BY_USER_DELEGATION_2026-07-22`（用户令 Claude 代为判断，
目标=尽可能理想化并靠近陈2025图4；本条为观察结果出现后的协议变更，如实登记）。

事实：S3-TRAJ-V4 观测性重跑（快照+离线补全）中，40/40 单元最终成本与封存
S3 逐位一致、完整解零违约；但 `mechanism_ev`(HGS-M)/seed10 的一个历史快照
的完整评分为 `2364.589958517462`，低于该单元封存最终成本
`2365.8780971446868`（差约 0.054\%）。独立复核确认真实。原
`HALT_S3_TRAJ_OFFLINE_CURVE_GATE` 证据原样留档。

技术定性：单视角臂按协议在代理成本空间搜索、仅收敛后做完整评分；代理排序与
完整排序存在固有偏序不一致，故运行中途可能"路过"完整意义更优的骨架而未保留。
原硬门（快照评分不得低于封存最终值）等价于要求两种排序永远一致——与本文
"完整模型裁判"的存在动机直接矛盾，技术上不可满足，故废止。

新口径：
1. 图4 曲线定义 = 运行中历史种群最优骨架经完整模型离线评分的最好值随时间
   变化（best-so-far，单调不增）；观测性构造，不改变算法与任何封存成绩。
2. **表8 封存成绩零改动**：HGS-M/seed10 仍报 `2365.878...`（算法按协议实际
   返回的解）；禁止以离线观测值 `2364.589...` 替换任何表内成绩（结果救援红线）。
   注意差异方向为保守（报告值劣于观测值），不构成成绩夸大。
3. 强制披露：图4 注或正文写明——单视角算法按协议仅在收敛后完整评分，其最终
   采纳解与曲线终点可存在微小差异（本实验仅 HGS-M 的 1 个种子出现，约 0.05\%），
   该差异即代理评价与完整评价偏序不一致的实证，与 `S2-INFEASIBLE-UNIT-001`
   的不可行案例同为引入完整模型裁判的动机证据。
4. 曲线种子选取维持预注册规则（每算法取 Avg 最接近种子），不得看曲线形态换种子。

执行收口（2026-07-22）：按本条新口径完成离线物化，未重新运行求解器。40/40
个已有快照单元的封存最终成本逐位相等，完整解违约总数为 0，轨迹点全部通过
完整模型离线评分；仅 1 个单元（HGS-M/seed10）出现历史 best-so-far
`2364.589958517462` 低于封存表内最终成绩 `2365.8780971446868`，该点保留在
轨迹数据中且不进入任何表格。按预注册规则选出的图4种子为 HGS-F=3、HGS-E=9、
HGS-M=3、MV-HGS-SP=9。S3 v1/v2 HALT 证据原样保留；S5-REV-V4 只做呈现层
与已批准的观察口径物化，主 TeX、raw/witness、评价器和统计输入未改。

术语澄清（2026-07-23）：上述“历史种群最优骨架”专指各代理目标刷新时保存的
代理目标 incumbent 骨架。图4只对这些已保存骨架离线完整评分，并绘制已观察快照
集合中的累计最低复核成本；它既不覆盖搜索期间全部候选，也不等同于完整模型目标下
的逐时历史最优。此澄清不改变轨迹点、种子、表内成绩或既有审批口径。

## S3-TRAJ-OBSERVATION-001：S3 代表题全程轨迹的观察层重跑

状态：`APPROVED_BY_USER_2026-07-22__EXECUTION_PENDING`。

用户 2026-07-22 明确授权：为修复图4的观测分辨率，在已封存 S3 代表题
`cn-prd-50c-01-V2-LOCATIONS` 上对四臂（`cv_only`、`naive_ev`、`mechanism_ev`、
`MV-HGS-SP`）和种子 1--10 做一次观察性重跑。该重跑不得改变算法机制、评价器、
实例、种子、停止准则、SP 时限、随机流或任何封存 raw/witness；只在 PyVRP 0.12.2
HGS 代理全局最优改善时复制路线骨架和墙钟时间，完整补全与精确评分必须在该求解单元
结束后离线进行。

硬门：40 个单元的最终成本须与封存 S3 `representative_gate/raw_runs.csv` 逐位相等，
且完整解零违约；任一不等、不可行、保护文件漂移或观测逻辑不能证明为只读增量，立即
`HALT`，不换种子、不调参、不救援。通过后只授权生成图4轨迹数据，并按“每臂最终成本
最接近十种子均值，种子并列取小者”选择展示轨迹。该条是呈现/观测层变更，不授权新的
算法优越性或等算力主张。

执行目录：`baselines/e2_final_campaign_20260720/p2p3_threeview/representative_gate/s3_traj_v4/`；
执行前保留本条审批与 S3 v1/v2/v3 证据，新增产物须独立四件套与完成标记。

执行结果（2026-07-22）：本条观察重跑实际完成 40/40 个单元；四臂十种子的最终
`rerun_cost` 与封存 S3 `raw_runs.csv` 逐位相等，40/40 完整解可行、违约总数为 0，
封存 S3 SHA-256 仍为 `da7ebb85f29b166c0f386f30c354d3d844b2c9026e9fd9d69a3e05fc67891afa`。
首次 HALT 仅为 runner 未把临时 row 的 `snapshot_file` 写回 raw 的产物 bug，v1
`decision/report/metadata/hash/offline_recheck` 已分别归档为 `*_halt_v1`；修复后未
重新求解，仅做离线恢复。离线恢复在 `mechanism_ev`/seed 10 发现一个历史代理新优
最优骨架的完整评分为 `2364.589958517462`，低于该单元封存最终成本
`2365.8780971446868`；独立复核确认该差异真实存在。因此按本条预注册的“物化轨迹
最终成本须与封存 S3 逐位一致”硬门，最终判定为
`HALT_S3_TRAJ_OFFLINE_CURVE_GATE`，v2 现场归档为 `*_halt_v2`。这不是算法/评价器
或成绩变更，且不得删除该更好历史点或把曲线终点人为抬回；Figure 4 v4 与后续 S5
PASS 需待用户决定是否调整轨迹验收口径，当前不授权以该观察结果收官。

## E2-ALGORITHM-REPAIR-XVF-001：三视角完整解双向回馈开发门

状态：`EXECUTED__HALT_CROSS_VIEW_FEEDBACK_DEVELOPMENT_GATE`
（2026-07-24）。

用户明确拒绝把修正后仿真算例“主方法与完整电动估价十次全平”解释为可接受终态，
要求继续修复算法，使真实结果与迭代过程至少呈现陈雨蝶等（2025）同类的清晰改进
趋势。该授权不允许修改成绩或只做平滑制图；允许在固定总工作量下修复三视角之间
只在末端拼接、搜索中不交换完整模型好解的结构缺口。

一次性开发候选固定为两段搜索：每个视角的5000次迭代拆为2500+2500；第一段从
三视角共同选出8个不同的完整模型好解，送入三个视角的第二段。每段每视角完整核算
11个档案解，使总完整候选核算次数仍严格为80；最终仍使用同一个5秒限时路线池重组。
固定开发面板、种子、通过/停止规则见
`baselines/e2_final_campaign_20260720/algorithm_repair_diagnostic_20260724/cross_view_feedback_preregistration.json`。
门槛为六对全部可行、预算严格一致、相对当前档案路线复用版至少2胜且0负。失败后
不得在已见面板调整分段、精英数、种子或预算；通过也只放行另立冻结确认，不自动
改写正式E2或论文。

执行结果：固定六对得到1胜5平0负，预算与可行性检查全部通过，但未达到至少2胜的
预登记门槛，故本候选HALT。随后预登记的“首次路线重组后回送短搜索”候选得到
0胜6平0负，同样HALT。两项均不得进入正式E2、E3或论文方法描述。

## E2-ALGORITHM-REPAIR-ARCHIVE-ROUTE-001：已核算档案路线完整复用

状态：`USER_AUTHORIZED__PASS_DEVELOPMENT_GATE__FROZEN_FOR_FRESH_E2_RERUN`
（2026-07-24）。

用户明确判定修正后仿真算例“主方法与完整电动估价十次全平”属于算法未优化到位，
要求在不改成绩、不挑种子、不改坐标轴的前提下继续修复，并把陈雨蝶等（2025）同类
清晰改进趋势作为真实算法表现与展示目标。该授权只允许通过真实搜索与路线重组获得
改进，不允许把图形外观作为正式结果的通过门。

修复内容：每个视角原本已由完整模型核算24个档案候选，但旧实现只把其中最好的8个
候选所含路线送入最终路线组合。修复后，24个已经核算过的候选路线全部进入同一个
5秒限时路线组合；单视角输出仍取原来的8个完整模型优选候选，三个视角的随机流、
每视角5000次搜索迭代、每任务80次完整候选核算、初始解、种子、评价器、目标函数、
车辆/充电/价格/碳参数均不变。该修复不新增一次完整候选核算，只避免丢弃已经付出
核算成本得到的可用路线。

看结果前固定的开发面板为三城市群各1个50客户和75客户算例、统一种子1；通过门为
六题全部可行、单视角结果逐位不变、完整核算严格等于80次、路线池不缩小、主方法
不差于最佳单视角，并至少2题严格改善。结果为2题严格改善、4题持平、0题退步，
全部不变量通过，判
`PASS_ARCHIVE_ROUTE_REUSE_DEVELOPMENT_GATE`。证据为
`baselines/e2_final_campaign_20260720/algorithm_repair_diagnostic_20260724/`
中的 `archive_route_reuse_preregistration.json` 与
`archive_route_reuse_panel_results.json`。

正式边界：开发结果不是论文证据。自本条起冻结
`epochal_hgs.py`、`route_pool_sp.py`、正式runner、80次完整核算预算及全部输入权威，
必须在新目录完整重跑81算例×5种子并独立重算1620个解，旧E2结果原样保留。正式
全量结果无论改善、持平或退步均须照实报告；不得因结果不像陈图而换算例、换种子、
调参数或再次改算法。图4可另用明确标注的机制展示算例，但不得称其具有统计代表性，
全81题统计仍是普遍性结论的唯一依据。

## SHENZHEN-2025-02-TARIFF-001：深圳独立价区充电电价情景闭合

状态：`EXECUTED__PASS_SHENZHEN_2025_02_EV_CHARGING_SCENARIO_ROW_CLOSED`
（2026-07-23）。

本条依据用户“尝试寻找和闭合深圳独立价区档位”的授权执行。旧 F3 将全国一般
工商业单一制规则直接套入深圳，因找不到严格匹配的普通单一制行而保持 HALT。新增
官方证据 `粤发改价格〔2018〕313号` 明确：深圳独立计量的电动汽车充电设施按报装
容量执行相应的大量用电或高需求用电电价，且免收基本电费；深圳供电局 2026 年公开
答复交叉确认该规则仍按相同口径办理。

正式情景行因此登记为：独立计量 EV 充电设施、大量用电、101--3000 kVA（既有主
情景具体限定 >100 且 <315 kVA）、10 kV 高供高计、月用电量不超过
250 kWh/(kVA·月)，只计电能量价且基本费为 0。2025 年 2 月尖/峰/平/谷仍为
`1.43716875/1.15526875/0.75776875/0.25716875 元/kWh`。该数值行已由 2026-07-18
零搜索静态构造器在 E3--E7 正式搜索前选择，本次不改静态 CSV、算法、评价器、种子
或任何 E2 成绩。

本 PASS 只关闭论文和未来 E3--E7 的预注册构造情景。普洛斯深圳龙华魔方供应链
大厦的真实报装容量、供电电压、计量点、月利用小时和账单仍为
`UNKNOWN_NOT_CLAIMED`。证据包为
`baselines/e4_e5/shenzhen_2025_02_tariff_closure_20260723/`，机器覆盖登记为
`data/ChinaPrices/china_2025_02_tariff_register_v3.json`；原 v2 HALT 保留历史。
## PRE-E3-FULL-CHAIN-REVIEWER-AUDIT-001：E3 前全链路审计授权与停止门

状态：`EXECUTED__HOLD_E3_DECISIONS_AND_EXECUTION_BINDINGS_OPEN`
（用户于 2026-07-23 明确批准审计范围与执行边界）。

授权范围：允许因审计发现而重新打开 E2；审计代码、输入、证据和论文全链；可直接
修复确定性实现/映射缺陷，但参数、代理、模型语义、算法臂和统计/算力口径仍须审批；
禁止静默代理。坐标用于验证节点所属区域，电价和碳等关键参数必须由显式区域/合同
属性选择。论文只称“构造情景”，同时尽量采用真实运营参数。允许使用网络、Zotero、
GIS 和必要工具取证。

强制停止门：只有 P0/P1 为 0、81 个修正 bundle 全部通过、独立复算和对抗门通过，
才可另立 `GO_E3`。本次审计搜索评价数为 0，当前不得启动正式 E3。

执行结果：确定性地理、碳列、价区选择、公共充电站动态过滤和强证书缺陷已按新版本
权威修复；旧证据未覆盖。修正改变 54/81 个实例和 2221 个客户位置，成都碳列影响
15 个实例，日期对齐柴油候选若启用影响 81/81。正式实验仍等待 D1--D6：
柴油价、有限车队/车场桩、可执行 E3 控制臂、确定性预算、限时 MIP 重组命名/最优性
门、修正后 E2 重跑范围。权威记录见
`baselines/china_e3_e7/pre_e3_full_chain_audit_20260723/`。

## CHINA-E3-FORMAL-RELEASE-001：D1--D6 与时空联合映射实施裁决

状态：`APPROVED_BY_USER_2026-07-23__IMPLEMENTATION_IN_PROGRESS`。

用户明确批准：D1 启用登记的 2025-02-12 分城市柴油价；D2 按需求、时窗和修正路网
构造有限车队并预注册储备系数及敏感性，充电桩只称构造情景；D3 控制臂硬锁所属车场，
处理臂解除锁并启用双向跨场邻域；D4 以完整候选评价次数为主要预算、墙钟仅作安全上限；
D5 改称“限时 MIP 路线池重组”并披露 incumbent/bound/gap/status；D6 完整重跑修正后
China81 私有 E2 与 S3--S5，公开 P1 只做无搜索 witness 重放。

用户同时追加 P0 要求：时间、地域、碳强度、电价、油价等要素必须对应且统一。正式
运行必须绑定
`instance_id/node_id/city/price_area_id/carbon_source_column/diesel_zone/date/slot`
联合主键。坐标只验证所属城市；电价按价区、碳按省级/直辖市列、柴油按路线起点车场
城市结算，全部共用 2025-02-12 和同一半小时槽。任何缺列、错城、错日、错槽或隐式
城市群回退均失败关闭。

有限车队主规则冻结为：先以零搜索确定性首次可行装箱生成每场全燃油可行路线数
`R_d`；主情景 `num_cv=R_d`、`num_ev=max(1,ceil(0.25R_d))`，总储备系数 1.25；
敏感性为 1.10/1.25/1.50。车场充电主情景为构造的 `2 x 22 kW`，枪数敏感性
1/2/3、功率敏感性 11/22/44 kW。该规则保证保留可审计的全燃油后备 witness，
不把车队或充电桩冒充为真实园区观测值。

完整合同见
`docs/handoff/china_e3_formal_release_contract_20260723.md`。本条只批准实施；
在独立零搜索 release 判 `GO_E3` 前，`formal_search_allowed=false`，不得启动 E3
正式搜索。

## E2-STAGED-PORTFOLIO-FORMAL-001：两阶段多视角组合正式复跑

状态：`APPROVED_BY_USER_2026-07-24__FORMAL_PREREGISTRATION_FROZEN`。

用户明确否决将修正后 E2 中“主方法与完整电动估价大量持平”和阶梯式过程图解释为
可接受终态，要求修复真实算法，使正式成绩和真实改进过程至少达到陈雨蝶等（2025）
同类结果的清晰度；禁止只修图、换坐标、挑种子或把失败包装为事实如此。该后续授权
覆盖本登记中 `E2-ALGORITHM-REPAIR-ARCHIVE-ROUTE-001` 的旧 5000 次迭代、80 次
完整核算预算，但不改变修正后的 China81 输入、目标函数、约束、种子 1--5、完整
评价器或独立检查器。v4 全量和论文产物原样保留为历史，不覆盖。

排雷按预登记依次测试在线完整核算、两阶段加深、车型保持、限时 MIP 返回解保持及
历史质量--差异路线档案。车型热启动映射和“已验 MIP 解被再次贪心解码”两个确定性
实现缺陷已修复并有定向测试。稳定的“路线重组 1+1+1>3”假设在多批固定门中仅有
零星增益，已由
`STOP_ROUTE_FUSION_SUPERADDITIVITY_20260724.json` 正式停止，论文不得再作稳定
超加性主张。

最终保留的诚实主张为：三个独立视角分别深搜，完整模型核算并保留最优可行解；
限时 MIP 路线池重组为可能追加改善的辅助步骤，其单独增益频率和幅度只作描述。
冻结候选采用每视角 `5000+20000` 次 HGS 迭代、同视角 8 个完整精英热启动、
每阶段 20 个在线完整检查点、每阶段每视角 12 个质量档案加 12 个差异档案、
每任务严格 280 次完整候选核算、两次各 30 秒限时 MIP，并保留车型和已验 MIP
方案。它比任一单视角使用更多计算量，禁止等算力或等时间优越性表述。

新鲜确认面板为三城市群的 150/200 客户 `-03`、统一未见种子 18，共 6 题；
主方法对 HGS-F/HGS-E/HGS-M 分别 5胜1平0负、5胜1平0负、6胜0平0负，
平均降幅 0.766351%/0.148621%/0.383106%，6/6 真实过程轨迹通过，判
`PASS_MULTIVIEW_PORTFOLIO_CONFIRMATION`。该确认不是论文证据。

正式 v5 已在任何正式结果出现前冻结：
`corrected_china81_rerun_v5_staged_portfolio_20260724/formal_preregistration_v2.json`。
范围为 81 算例×种子 1--5=405 个任务、1620 个保存方案；必须完整独立重放。
论文强度门以 81 个五种子均值为主，主方法对三单视角均须零负，严格胜场至少
50/35/30，总体平均降幅至少 0.30%/0.08%/0.10%，三组单侧配对 Wilcoxon 经 Holm
校正后均不高于 0.05，且至少 200/405 任务严格改善受保护的一阶段结果。任一门失败
即保留证据并停止 S3--S5 和 E3，不得事后降门。

图4算例在结果前按输入规则固定为 `cn-prd-100c-01-V2-LOCATIONS`：只在九个
100 客户题内按五个输入特征距中位向量最近选择；仅称“迭代展示算例”，不称统计
代表。图只能连接运行中在线完整核算的真实点及返回终点，禁平滑、插值、断轴和
看形状换题换种子。

## E2-STAGED-PORTFOLIO-BUDGET-RECHECK-001：v5 HALT 后的机械计数修订

状态：APPROVED_BY_USER_2026-07-24__V6_PREREGISTRATION_FROZEN_BEFORE_RUN。

v5 正式批在第13个任务因小规模算例可产生的真实候选不足而 HALT：D6-E2-STAGED__cn-cy-10c-01-V2-LOCATIONS__seed1 的真实完整模型核查为276，而 v5 要求每任务恰好280。该 HALT、已完成任务和现场证据原样留档，不能改写为通过。

用户批准另立 v6，仅修“统一工作量的记账方式”，不调整算法机制。v6 不改变输入权威、目标函数、约束、完整评价器、种子、迭代数、MIP时限、随机流或任何搜索参数。每任务先记录真实搜索核查数；只有真实数小于280时，才对预注册固定的 HGS-F 已保存解重复调用完整模型评价器补足差额。该重复调用不生成候选、不进入路线池、不消耗随机数、不改变任何成绩，且单独记录为 budget_padding_rechecks；真实核查记为 search_complete_candidate_attempts，两者之和记为 complete_candidate_attempts=280。如果真实数超过280、固定解不可行、评价器返回异常、源/输入哈希不一致或任一机械门失败，立即 HALT；禁止换种子、调参、改评价器或删失败记录。

v6 预登记与运行器：baselines/e2_final_campaign_20260720/corrected_china81_rerun_v6_budget_recheck_20260724/formal_preregistration_v3.json、baselines/e2_final_campaign_20260720/run_corrected_china81_d6_staged_portfolio_v6_budget_recheck.py。v5 与 v6 不共享结果目录；v6 只有在完成405任务、1620方案、独立重放及继承的强度门后，才可放行 S3--S5 与 E3。

首次 v6 启动异常属于任务初始化前的登记文件名接线错误：继承的 v5 模块仍寻找 formal_preregistration_v2.json。异常现场保留且未产生任何任务或成绩；v6 wrapper 已显式绑定 v3 文件，源码哈希更新并重新做静态校验后，才允许 retry 监控启动。

## E2-STAGED-PORTFOLIO-SMALL-ARCHIVE-LEDGER-001：v6 HALT 后的小档案验收修复

状态：`USER_DIRECTED_REPAIR__V7_PREFLIGHT_PASS__FORMAL_RUNNING`
（2026-07-24）。

v6 retry 在 `cn-cy-10c-01-V2-LOCATIONS/seed1` 触发
`HALT_HISTORY_ARCHIVE_LEDGER`。该 HALT、已报告通过任务、监控日志和任务目录原样
保留；安全源码点为提交 `cecf30c8`、标签
`safety/e2-v6-halt-before-ledger-fix-20260724`。

用户随后要求按既有意见直接修复并保留回退点。隔离复现固定同一算例、种子、搜索、
迭代、档案上限、MIP 时限、评价器和线程环境，不写入 v5/v6 正式目录。六个阶段均
达到规定迭代和 20 个快照，历史候选引用均为正，四个返回解完整模型违约均为 0。
唯一触发项是 `cv_only` 第二阶段只有 20 个不同候选：旧选择器已将 20 个全部选入，
但旧验收仍要求 `archive_diversity_selected_count>0`。因此这是把“候选空间已穷尽”
误判为“档案没有使用”的验收错误，不是算法结果或解的错误。证据判
`CONFIRM_SMALL_ARCHIVE_LEGACY_GATE_BUG`。

v7 的唯一语义修订为：每阶段选入候选数必须等于
`min(archive_limit, unique_candidates)`；当 `unique_candidates>24` 时继续要求
12 个质量候选和正数差异候选（当前实现为 12）；当 `unique_candidates<=24` 时必须
选入全部可用候选，允许差异余量为 0。快照数、历史引用、质量候选正数、完整可行性、
280 次登记核查、搜索随机流、种子 1--5、5000+20000 次迭代、两次 30 秒限时 MIP、
输入、目标、约束和评价器均不变。

v7 不复用 v5/v6 任务；先在预登记的 10/100/200 客户三任务上通过机械预检，才允许
从零启动 405 个正式任务。任何预检或正式门失败均保留证据并 HALT，禁止降门、换种子、
调参数、删失败或以图形外观救援。合同与入口为
`corrected_china81_rerun_v7_small_archive_ledger_20260724/formal_preregistration_v4.json`
和 `run_corrected_china81_d6_staged_portfolio_v7_small_archive_ledger.py`。

三题机械预检已按预登记完成并判
`PASS_D6_E2_STAGED_V7_PREFLIGHT`：10/100/200 客户任务均为 PASS，四个返回解均
通过完整模型独立可行性检查，每任务总核查数均为 280、历史快照数均为 120、档案
选择均闭合。10 客户任务为 276 次真实搜索核查加 4 次已登记固定解重复核算，选入
140 个历史候选（质量 80、差异 60），其中 1 个阶段穷尽可用候选；100/200 客户任务
均为 280 次真实搜索核查、0 次补足，分别选入 144 个历史候选（质量 72、差异 72）。
四份哈希清单共 40 个条目已独立复核一致，30 个 AppleDouble 附属文件已按规程清理，
定向回归为 16 passed。该预检只授权从零启动 v7 的 405 个正式任务，不进入论文统计，
也不授权改变强度门、种子、参数或图形口径。

预检证据和后续冻结合同已提交为 `9b80ca59`，并建立开跑前回退标签
`safety/e2-v7-preflight-pass-before-formal-20260724`。随后以 3 个单线程任务并行、
每任务内部线程锁为 1，从空的 `full_gate/` 启动 405 个正式任务；启动后监控状态为
RUNNING，受保护文件指纹无变化。正式完成前不得据中间结果改代码、参数、论文结论或
后续合同。

## E2-S4-WITNESS-LOOKUP-001：S4 解指纹接线修复

状态：`REGISTERED_AFTER_FORMAL_START_BEFORE_ANY_RESULT_ROW_READ`（2026-07-24）。

在不读取正式结果行的下游静态接线检查中发现：S3 的论文汇总
`s3_trajectory_gate/raw_runs.csv`按设计只保存种子、四种方法成本和 CPU，不包含
`witness_sha256`；S4 v2 却试图直接从该汇总行读取此字段，因此将来必然在零搜索验收
开始前报字段缺失。该问题不影响正在运行的 405 个正式任务，也不涉及算法、评分、
算例、种子、选择规则或任何成绩。

S4 v2 登记和脚本保留不改。另立 v3，只把解指纹来源改为固定选择规则命中的那个
封存任务自身 `raw_runs.csv`，并先要求 S3 成本与任务成本在绝对容差 `1e-9` 内一致，
再核对任务记录中的 `witness_sha256`。迭代展示算例、十种子中最低主方法成本、同值
取较小种子、整解全局验解、完整评分、逐路线算术闭合和失败即停规则全部不变，搜索
次数仍为 0。登记为 `s4_route_detail_preregistration_v3.json`，入口为
`run_e2_staged_s4_route_detail_v3.py`；9 项源指纹一致，最小模拟接线测试、编译和
代码检查均通过。正式批和 S3 通过后只准执行 v3。

## E2-S5-SEALED-ARTIFACT-RUNNER-001：S5 零搜索表图生成器

状态：`FROZEN_AFTER_FORMAL_START_BEFORE_ANY_RESULT_ROW_READ`（2026-07-24）。

为避免正式结果完成后临时挑坐标、挑图例位置或手填表格，在未读取正式结果行时冻结
`run_e2_staged_s5_artifacts.py`。只有正式 E2、1620 解独立复算、论文强度门、S3
真实过程曲线和 S4 v3 路线明细全部通过后才允许运行；任一上游未通过、缺行、重复、
哈希漂移、曲线终点不一致或格式规则失败均 HALT。该入口搜索次数为 0。

输出同时保留 10 行城市群--规模带正文汇总和 27 个精确城市群--规模层加总体的完整
汇总，不隐藏层；表体统一为 `\linewidth` 中文三线表、两层表头和水平/垂直居中标识，
表注只通过出版社类文件的 `\tabnote` 宏嵌入。图3固定读取 2025-02-12 运行参数权威，
图例仅写京津冀/珠三角/成渝，左下角、无外框；图4只连接 S3 在线完整模型真实观测，
单一“时间(min)--成本(元)”坐标、全数据双侧 3% 留白、不断轴、不平滑、不插值，
图例角落按固定的最少数据点遮挡规则决定。生成器已用旧封存数据完成形状测试：公开表
29 行、迭代表 13 行、China81 81 算例/10 行紧凑汇总/28 行完整层汇总、图3 144 行，
中文缺字警告为 0；编译和代码检查通过。登记为
`s5_artifact_runner_registration_v1.json`，6 项源指纹闭合。

补充（2026-07-24，正式结果读取前）：接力检查确认 S4 v3 和 S5 监控原先均无
完成文件约定，程序正常退出后会被监控误判为异常。现仅在两个零搜索收口入口末尾
写入带决定文件和关键产物指纹的 `done.json`，并把对应监控的 `completion_files`
指向该文件。该修订不改变算法、解、成本、路线选择、统计或绘图规则；S4/S5 入口
指纹已同步更新。

## E2-STAGED-V7-RELEASE-CHAIN-001：E2 失败即停自动接力

状态：`FROZEN_AFTER_FORMAL_START_BEFORE_RESULT_ROW_READ`（2026-07-24）。

用户已授权在安全前提下持续推进至 E2 完成。现冻结
`run_e2_staged_release_chain.py`：它只等待既有正式批，不启动第二份正式批；正式
判定必须为 `PASS_D6_CORRECTED_CHINA81_E2_STAGED_V7_SMALL_ARCHIVE_LEDGER`，之后
固定按“1620 解独立复算 → 结果强度门 → S3 真实迭代数据 → S4 v3 路线明细 →
S5 零搜索表图”执行。每阶段只接受冻结的唯一 PASS，任何异常、缺文件、错误判定或
监控失败均写 HALT 并停止，不重采样、不改参数、不跨越失败门。S3 仍为 3 workers；
其余阶段不新增搜索并行。登记为 `release_chain_registration_v1.json`。

## E2-V7-PAPER-METHOD-CANDIDATE-001：结果前算法章与图示候选稿

状态：`PRE_RESULT_CANDIDATE_ONLY__MAIN_TEX_PROTECTED`（2026-07-24）。

用户要求按既有审稿意见直接修复论文展示，但 V7 正式 405 任务正在运行且主 TeX
属于监控保护文件。因此只允许在独立候选目录准备替换稿，不得改
`docs/paper_v2/paper_main.tex`，不得写入正式结果、预判成绩或改变正在运行的算法。

候选方法说明必须逐项对应冻结入口：三个视角共享同一可行初解和固定种子，但种群
独立；阶段1每视角 5000 次，阶段2每视角 20000 次且只接收本视角完整模型精英；
每阶段对历史候选执行完整模型检查和档案保留；两次路线池组合均为 30 s 限时 MIP；
最终由完整模型和独立检查器在受保护解、两阶段三视角完整解与可用 MIP 解中择优。
禁止恢复“精确路线重组”“组合必然改进”“三个评分直接相加”“等计算量优势”或
稳定超加性主张。

图1和图2只用透明黑白线框，不设伪代码、装饰底纹、难懂的无必要公式或结果性标记；
箭头不得穿字或被压缩，分支必须反映候选检查、不可行删除、可行路线入池、完整解直接
参与最后比较和 MIP 解重新完整复核。候选稿须独立编译并目检通过；只有正式 V7、
1620 解独立复算、预登记强度门、S3、S4 v3 和 S5 全部 PASS 后，才允许按封存事实
并入主稿。

## E2-V7-PAPER-TEXT-MATERIALIZATION-001：封存结果自动生成论文结果段

状态：`FROZEN_WHILE_FORMAL_RUNNING_BEFORE_RESULT_ROW_READ`（2026-07-24）。

主稿现存v4方法与结果，不能在V7完成后靠人工逐个替换数字。结果前冻结
`paper_text_preregistration_v1.json`、
`materialize_e2_v7_paper_text.py`和
`e2_experiment_protocol_v7_candidate.tex`。候选实验段只引用V7封存产物；任一
表图不存在即报错，不允许回退到v4。

文字物化入口搜索次数为0，只有正式405任务、1620解独立复算、结果强度门、S3真实
过程曲线、S4 v3和S5表图全部通过且各自哈希清单一致时才可运行。它从封存证据生成
十种子成本/CPU和胜平负、405任务/81算例/27层胜平负、总体平均降幅、Holm校正值、
相对阶段1保护解的改善次数和MIP单独产生严格增量改善的次数；不得手工改数字、配对
单位、展示算例、种子或统计口径。

固定结论边界：整体结果只支持“两阶段多视角搜索与最终择优”的组合效果；路线组合
单独改善次数必须如实披露，不得宣称稳定超加性、MIP必然改进或等计算量优势。生成
结果另立 `paper_text_gate/` 五件套，不修改S5封存目录及其哈希清单。

## E2-V7-PAPER-INTEGRATION-002：路线说明与整稿候选自动接入

状态：`FROZEN_WHILE_FORMAL_RUNNING_BEFORE_RESULT_ROW_READ`（2026-07-24）。

静态检查发现原结果文字物化只覆盖表8、图4和China81汇总，未覆盖表4旁的路线结构
说明；若直接换入S5新表，主稿可能保留旧版路线成本、车型数量和能耗文字。为消除该
错配，在未读取正式结果行时新增
`materialize_e2_v7_route_text.py`和
`route_text_preregistration_v1.json`。该入口只在S4、S5精确PASS且哈希清单闭合后
读取封存路线表，自动生成表4说明和表体引用；禁止手填数字、替换解、跨路线作车型
因果推断或执行搜索，输出另立`route_text_gate/`五件套。

同时冻结`build_v7_integrated_paper.py`与
`v7_paper_integration_registration.json`。该入口保护登记时的
`paper_main.tex`指纹，只能生成独立候选稿，永不覆盖主TeX；正式接入前必须同时通过
完整发布链、E2结果文字门和路线文字门，并逐项复核其哈希清单。合并范围固定为标题/
摘要、引言、模型结构、算法章、V7图表、路线说明和新增参考文献；旧v4结果路径、
5000次/80次/一次5秒口径、稳定超加性、等计算量优势和伪代码均为硬拒绝项。
当前只执行了不读取结果的结构检查，判定通过；正式结果候选尚未生成。

## E2-V7-PAPER-CANDIDATE-CHAIN-001：发布后论文候选自动接力

状态：`FROZEN_WHILE_FORMAL_RUNNING_BEFORE_RESULT_ROW_READ`（2026-07-24）。

为避免正式E2与S3--S5夜间完成后停在“有数据、未接论文”状态，新增等待式
`run_e2_v7_paper_candidate_chain.py`。它不启动第二份E2，也不启动E3；只等待既有
发布链精确判为`PASS_E2_STAGED_V7_RELEASE_CHAIN`并复核其完成文件和哈希清单，随后
依次运行E2结果文字物化、表4文字物化、独立整稿候选合并和XeLaTeX编译检查。

候选链禁止覆盖`paper_main.tex`，输出位于独立
`paper_candidate_chain/`。TeX错误、未定义引用、overfull/underfull、Type 3字体、
任一上游判定或哈希异常均立即HALT；即使PASS，也只表示封存证据候选稿编译通过，
仍须逐页视觉检查和显式主稿合并后才能放行E3。登记文件为
`paper_candidate_chain_preregistration_v1.json`，监控为
`monitor_paper_candidate_chain_v1.json`。

## E2-GENUINE-HYBRID-HGS-RR-001：面向模型的真混合算法开发

状态：`USER_APPROVED_DESIGN_DIRECTION__FORMAL_EXECUTION_NOT_YET_RELEASED`
（2026-07-24）。

用户明确否决把“三个 HGS 视角 + 路线池 MIP”继续包装成不同算法的混合，并追加两项
硬要求：不是为了加算法而加算法，必须在相同完整候选评价预算下实现可核验的
“1+1>2”；算法必须围绕本论文多车场、燃油/电动混合车队、有限车辆、充电、分时
电价、分时碳强度、分城市柴油价和动态状态继承定制，同时具备 BKS/SOTA 资格。

批准另立 `docs/handoff/e2_genuine_hybrid_hgs_rr_contract_20260724.md`。正在运行的
E2 v7 保持原样，降格为 `INTERIM_STRONG_HGS_FAMILY_BASELINE`，不停止、不改代码、
不读中间成绩选算法。新候选固定为三个主臂：A 为修正后的 HGS 家族搜索；B 为完全
独立、不得调用 PyVRP 种群/交叉/MIP 的机制感知破坏—修复搜索；A+B 为 HGS 精英进入
破坏—修复、其完整可行改进解再实际注入下一 HGS 阶段的双向协同搜索。

“1+1>2”不等于最终取 `min(A,B)`：组合臂必须在同一完整评价总数下同时优于 A 和 B，
并留下双向传递后继续改善的解谱系。开发门、确认门和正式门均已预置不可事后降低的
胜负、改进幅度和统计门；达不到即停止候选，不得靠调图、换种子或改名保留。

算法定制固定覆盖客户顺序、车场归属、跨场交换、油电车型、有限车队、充电与发车
时刻、城市—日期—半小时价格/碳参数、动态未执行尾段。须实现双层解表示、专用
车场—车型—充电—时间解码、支配剪枝与安全缓存，以及六类问题专用大邻域。任何退化
为通用 relocate/swap 的算子均不计作创新。

BKS/SOTA 只允许在有公开实例、公开 BKS 和可比最新文献结果的标准算例上认定。
China81 是构造情景，不得自封 SOTA。公开门要求相对封存 PyVRP-HGS 明显缩小误差、
不丢既有 BKS、至少新增一个 BKS，并达到经多重校正的统计优势；与检索截止日可比
最好方法核心指标持平或更好后才准写“SOTA”。

当前正式 E2 运行期间只允许独立目录内的合同、静态实现、单元测试和零搜索行为样例；
禁止启动新高负载搜索。G0--G2 全部通过且当前 v7 封存后，才可另立正式 E2 预注册。

执行进展（仍不授权搜索）：结果盲选开发题已冻结为
`cn-jjj-25c-02-V2-LOCATIONS`、`cn-prd-100c-03-V2-LOCATIONS`、
`cn-cy-150c-03-V2-LOCATIONS`；固定哈希复算通过。独立开发包已实现完整候选预算、
双向谱系、跨城市/日期/动态状态缓存键、六类专用破坏计划、下层状态支配前沿和预算
可见完成器，7 项轻量测试通过。静态审计确认旧完成器存在“一次外层调用含多次内部
完整评分”的口径差，故新三臂必须统一使用预算可见完成器，不能直接复用旧 280 次
外层调用口径。

补充（2026-07-24，仍未启动搜索）：用户进一步明确最终算法必须由论文模型与机制
反向定制，不能只是 HGS 换壳，并要求算法强度经 BKS/SOTA 和主消融检验。现冻结
A+B 完整评价分配为约 60% HGS、40% 破坏—修复：80 次门为
`1+16+16+16+15+16`，280 次门为 `1+56+56+56+55+56`，共同初解各计一次；
A+B 的 HGS 内部迭代数同步取 A 的 60%，不保留 A 全部 HGS 算力再额外叠加 B。

隔离实现已接通 A、B、A+B 三臂、有限车队车场/车型分配 DP、路线局部缓存、独立
regret 重构和双向阶段调度。算子验收不再以哈希变化代替：整路线须真换场、双向片段
须两个方向都交换、油电须真翻转、充电须真改变地点/能量/时刻，充电站不得当客户。
下一 HGS 阶段中未改变的热启动解和随机初始解不得记为协同后代。进一步静态审计确认
PyVRP 普通种群输出没有可核验的父母关系，故协同增益证据收紧为：每个阶段的三个
固定代理视角分别只对第一热启动解执行一次显式、可追踪的 PyVRP 局部搜索；A 与
A+B 均为每次运行固定 9 次；A+B 回送时第一解固定为 RR 解。只有该直接路径产生
不同骨架且完整成本继续降低才计数。普通种群偶然好解不计。
结构双向传递仍为单次运行硬门，增益数量只在预登记的 G1/G2/G3 汇总门判断，不能
删除无增益运行。该修正发生在任何真实新算法结果产生前，属于证据接线纠错，不是
结果后调参。

本轮同时修复单车场跨场操作崩溃、一个无可行动作导致整轮误停、时间窗操作固定重复
同一客户、跨场路线对无界枚举、无目标车型车位仍尝试翻转、充电重排重复原方案、
局部车型/充电操作误重建未选路线、重构时间窗被当前车型误拒等实现错误；时间窗压力
改按统一排程后的真实剩余时间，而非只按窗口宽度。缓存键继续闭合城市、价区、碳源、
柴油区、充电策略和碳权重，防止跨城、跨日或跨充电口径误用。

33 项轻量测试通过；静态 G0 权威 v10 子门 13/13 通过并判
`PASS_G0_STATIC_SEMANTICS__REAL_BUNDLE_PREFLIGHT_PENDING`。该判定明确不加载真实
China81、不运行搜索、不证明可行性、算法质量或 1+1>2。静态 v6 因新增验收脚本局部
变量错误形成 12/13 HALT，v9 因时间窗操作升级后验收夹具仍固定修改第一条路线形成
12/13 HALT，均原样保留；v10 的 19 项输入与 4 项产物哈希已复核一致。
由于现有 v7 正式批仍运行，
真实 bundle 的零搜索解码/完整裁判回放及 G1 继续禁止。真实 bundle G0 已另立
`g0_real_bundle_preregistration_v1.json` 和资源互斥执行器；合同模式确认三道开发题
及 55 个选定输入文件哈希一致，并在检测到 v7 运行时返回
`execute_allowed_now=false`，未运行任何新搜索或真实解码。

G1 公平入口也已在结果产生前冻结：登记文件为
`g1_micro_preregistration_v1.json`，执行器为 `run_g1_micro_gate.py`。固定任务为
三道结果盲开发题、种子 1、A/B/A+B 三臂共 9 次；每臂 80 次可见完整模型评价，从
同一补全后的 witness 起步。A 的 HGS 内部总迭代数固定为 75,000（三视角各累计
25,000），A+B 固定使用其 60%；其余档案、短名单、破坏范围、接受规则、墙钟安全
上限和最多 3 workers 均写入登记，结果后不得调整。入口已通过 31 项源码指纹和
55 个输入文件检查，34 项轻量测试通过；因 v7、发布链和真实 bundle G0 均未闭合，
当前只返回 `execute_allowed_now=false`，G1 搜索次数为 0。硬门仍为组合臂三题零负、
至少两题严格胜过两单臂较优者、至少一题有直接回送后续增益；不通过即
`STOP_G1_NO_HYBRID_GAIN`，不得在同三题救援。

## E2-GENUINE-HYBRID-CHARGER-CAPACITY-002：结果前共享桩容量预筛

状态：`FROZEN_BEFORE_REAL_G0_AND_G1_RESULTS`（2026-07-24）。

对真混合算法的下层解码进行结果前审计时发现：路线级充电修复能够给出每条路线的
充电站和半小时时段，但有限车队联合分配原先只约束车场和油电车辆数，没有在进入
完整候选短名单前约束同一公共充电站的桩数。最终 `check_solution` 仍会拒绝超额方案，
因此这不是旧成绩被错误接受，而是新算法会浪费有限完整评价预算并降低可行候选比例。

批准在不改变路线、充电时刻、成本公式、评价器或最终检查器的前提下，把每条路线
已经确定的充电时段占用带入联合分配；同站同半小时的路线数超过 bundle 中该站桩数
时，该组合不进入短名单。该修复只闭合固定充电计划之间的容量冲突，不是跨路线自由
改时的全局充电排班，论文不得称为最优联合充电调度。

该修复发生在真实 bundle G0 和 G1 均未执行、没有读取任何新算法成绩时。原
`g1_micro_preregistration_v1.json` 保留并标为从未执行；v2 仅增加该预筛，三题、
种子、A/B/A+B 身份、80 次完整评价、HGS 迭代数、60/40 分配、算子、接受规则、
墙钟上限和全部性能硬门不变。35 项轻量测试通过；权威静态 G0 v12 为 14/14 通过，
20 项输入和 4 项产物指纹闭合。G1 v2 合同检查通过 31 项源码和 55 个输入文件，
但仍被正在运行的 v7、未通过的发布链和未执行的真实 bundle G0 三重阻断，搜索次数
保持 0。

## E2-GENUINE-HYBRID-G0-G1-CHAIN-003：受保护发布后的顺序接力

状态：`FROZEN_BEFORE_RELEASE`（2026-07-24）。

旧 E2 v7 和其发布链预计仍需长时间运行。为避免资源释放后人工漏接，同时防止新算法
抢占当前任务资源，批准等待式
`run_genuine_hybrid_g0_g1_release_chain.py`。它不启动或重启 E2，只每 30 秒读取
上游发布判定和资源状态；只有判定精确等于
`PASS_E2_STAGED_V7_RELEASE_CHAIN` 且受保护进程全部退出，才先运行真实 bundle
零搜索 G0。G0 精确通过后，才按 v2 预登记以 3 workers 运行 G1。

若上游、G0 或 G1 停止，接力只写原判定和五件套并终止，不修代码、不调参数、不换
题/种子、不重采样、不重跑。G1 的 `STOP_G1_NO_HYBRID_GAIN` 是有效的研究停止结果，
不能被接力当成程序故障救援。接力绑定 11 项源码/合同指纹，并由
`monitor_g0_g1_release_chain_v1.json` 保护算法、预登记、成本、检查器、完成器、
价格和主 TeX；自动 AI 关闭。

## E2-G1-WORKER-PROBE-004：结果盲六进程安全门与低成本递进

状态：`USER_APPROVED__FROZEN_BEFORE_REAL_G0_AND_G1_RESULTS`
（2026-07-25）。

用户明确要求：优先使用最低成本验证，只有确有必要才进入扩大题数、种子或正式长跑；
并希望在安全前提下尽量使用 6--8 workers。当前正在运行的 E2 v7 已按三进程合同
开跑，禁止中途改变并行数、停止重启或重写登记。该要求只用于尚未执行的新算法 G1。

批准在旧 E2 发布链完整通过且相关进程退出、真实 bundle 零搜索 G0 精确通过后，
先运行一个结果盲资源门。资源门使用三道已登记开发题，每题两次，共六个并行的
真实输入加载、有限车队解码、完整模型验解和 PyVRP 投影往返任务；搜索迭代数为 0，
产物不保存目标值。六项全部通过、运行期间系统空闲内存比例不低于 10%、六个工作
进程峰值内存合计不超过 4096 MB 时，G1 使用 6 workers；否则不视为科学失败，
按预登记自动回退 3 workers。

并行数只改变九个相互独立 G1 任务的同时运行数量。每个任务仍为单线程，三道题、
种子 1、A/B/A+B 三臂、共同初解、每臂 80 次完整评价、HGS 迭代数、A+B 的 60%
HGS 比例、破坏修复算子、接受规则、墙钟安全上限和全部性能硬门均不改变。该门不
授权算法质量、1+1>2、正式 China81、BKS 或 SOTA 结论。

原 `g1_micro_preregistration_v1.json`、v2 及
`g0_g1_release_chain_registration_v1.json` 均保留。v1 等待链接近三小时仍只读取
旧 E2 进度，没有执行真实 G0、资源门或 G1；为修改其受保护配置，已按监控规程先
暂停，保留 `PROTECTED_FILE_DRIFT` 异常现场，不覆盖、不续跑。v2 监控确认正常
等待后，v1 等待进程已用监控器安全终止；旧 E2 正式进程和发布链未受影响。权威新登记为
`g1_micro_preregistration_v3.json`、
`g1_six_worker_resource_probe_registration_v1.json` 和
`g0_g1_release_chain_registration_v2.json`。

## E2-V7-MECHANICAL-RELEASE-005：机械完整性与论文效果门解耦

状态：`USER_APPROVED__FROZEN_BEFORE_TERMINAL_E2_RESULTS`
（2026-07-25）。

用户批准继续当前 E2 v7，并要求其结束后自动推进真混合算法。复核现有接力发现：
`run_e2_staged_release_chain.py` 只有在正式批、1620 份解的独立复算、论文效果门、
S3 轨迹、S4 路线明细和 S5 表图全部通过后，才输出完整发布 PASS；而真混合 G0/G1
等待链又只认该完整 PASS。这样，若旧算法仅在“论文效果不够强”或展示层停止，已经
机械正确的 E2 仍会错误阻断替代算法的低成本开发。

批准新增零搜索机械放行门
`build_e2_v7_mechanical_release_gate.py`，并在结果终态产生前冻结
`mechanical_release_registration_v1.json`。该门只在以下证据同时成立时通过：
正式 v7 为 405/405 任务、1620/1620 返回解且全部完整模型可行；独立 witness 复算为
405 个任务、1620 个解，成本复现且全部可行；两阶段哈希清单闭合；原完整发布链已经
形成终态，避免与既有接力争抢或重复启动。

机械 PASS 只授权低成本真混合 G0/G1 开发，不覆盖原发布链的 PASS/HALT，不证明旧
算法论文效果、迭代图、路线明细、BKS 或 SOTA，也不自动启动第二条 G0/G1 链。脚本
默认仅做合同校验，只有显式 `--execute` 才能在终态后封存五件套。当前
`CONTRACT_ONLY_PASS`，未读取正式成绩、未运行搜索、未创建放行结果目录。

用户同时批准应用内自动化 `e2-v7-30`：每 30 分钟只读检查正式进程、405 任务进度、
终态文件、资源和异常；正常时不通知，异常时只报告不自修；发现正式终态后先暂停
自身，再核对既有发布链和真混合等待链，避免重复任务。

## E2-V7-RELEASE-RECOVERY-006：嵌套监控误停后的版本化续接

状态：`USER_CONFIRMED__FROZEN_AFTER_REPLAY_BEFORE_STRENGTH`
（2026-07-25）。

E2 v7 正式批已判
`PASS_D6_CORRECTED_CHINA81_E2_STAGED_V7_SMALL_ARCHIVE_LEDGER`：
405/405 任务完成，1620/1620 返回解通过完整模型。随后独立复算已判
`PASS_D6_STAGED_FULL_WITNESS_REPLAY`：405 个任务、1620 个解，成本全部复现且全部
可行；正式批和复算哈希清单分别有 2434、85 项并逐项闭合。

原发布接力并非科学门失败。外层监控因 `release_chain/progress.json` 在内层复算期间
307 秒未更新而触发 `PROGRESS_STALLED`；恢复后，内层监控又把已经退出的复算子进程
识别为仍存活的僵尸进程，并因命令文字消失触发 `COMMAND_CONTRACT_MISMATCH`，导致
外层永久等待。复算本身已有完整 PASS 和完成标记。用户明确确认安全结束卡死的进程组
76777、保留异常现场，并从封存复算 PASS 后版本化恢复。

批准新增 `run_e2_staged_release_recovery_v2.py`、
`release_recovery_registration_v2.json` 和
`monitor_release_recovery_v2.json`。恢复顺序固定为：复核正式批及复算 PASS 与哈希，
不重跑二者；随后严格复用原登记的结果强度门、S3、S4 v3、S5。任一原门不通过即
原样写 canonical HALT，禁止救援、降门或跳过。全部通过才写 canonical
`PASS_E2_STAGED_V7_RELEASE_CHAIN`，由既有真混合等待链接手。

新监控不把批处理阶段的中间文件当心跳，`progress_files` 为空，避免再次用固定 300 秒
误判；仍保护恢复登记、四个冻结入口及登记、成本、检查、评价、价格和主 TeX。当前
静态结果为 `CONTRACT_ONLY_PASS`：26 项登记指纹闭合，2434+85 项上游清单复核通过，
监控 preflight 通过。尚未启动恢复阶段。

## E2-GENUINE-HYBRID-MECHANICAL-RELEASE-007：效果 HOLD 后放行 G0/G1

状态：`FROZEN_AFTER_MECHANICAL_PASS_BEFORE_G0_G1_RESULTS`
（2026-07-25）。

恢复链按原结果强度门得到
`HOLD_E2_STAGED_PORTFOLIO_NOT_PAPER_STRONG`，因此 canonical 发布链诚实写
`HALT_E2_STAGED_V7_RELEASE_CHAIN`，S3--S5 未继续。这不推翻正式 405/405 和
1620/1620 独立复算 PASS；零搜索机械门随后判
`PASS_E2_V7_MECHANICAL_INTEGRITY_RELEASE`。旧真混合 v2 等待链只认论文效果 PASS，
故按原合同写 `STOP_UPSTREAM_E2_V7_RELEASE`；真实 G0、资源探针、G1 搜索均未执行。

批准启用此前已登记的“机械正确即可开发替代算法”边界。新增
`g1_micro_preregistration_v4_mechanical.json` 与薄包装入口
`run_g1_micro_gate_v4_mechanical.py`。v4 只把上游要求从旧算法
`PASS_E2_STAGED_V7_RELEASE_CHAIN` 改为
`PASS_E2_V7_MECHANICAL_INTEGRITY_RELEASE`；v3 其余科学字段以整体哈希锁定：
三道开发题、种子 1、A/B/A+B 身份、共同初解、每臂 80 次完整评价、A 的 75000 次
HGS 迭代、A+B 的 60% HGS 比例、60/40 评价分配、全部算子、接受规则、墙钟上限、
六进程结果盲资源门和所有性能/谱系停止条件均不变。

新增机械接力 `run_genuine_hybrid_g0_g1_mechanical_release_chain.py`、登记与监控。
顺序仍为真实 bundle 零搜索 G0 → 六进程零搜索资源探针 → 按 6/3 选择执行九项 G1。
G1 只允许 `PASS_G1_COOPERATIVE_HYBRID_GAIN` 或
`STOP_G1_NO_HYBRID_GAIN`；STOP 不自动调参、换题、换种子、重跑或扩大。静态检查：
机械接力 24 项指纹 PASS，G1 v4 复核 v3 的 33 项源文件和 55 个结果盲输入，当前仅因
G0 尚未执行而阻断，符合顺序合同。

## E2-GENUINE-HYBRID-G0-REAL-008：真实 bundle G0 失败与修复授权边界

状态：`USER_APPROVED_AUTONOMOUS_REPAIR__V2_FROZEN_BEFORE_EXECUTION`
（2026-07-25）。

机械接力执行真实 bundle 零搜索 G0 后，预登记的小、中、大三道开发题均报
`decoder shortlist has no globally feasible candidate`，判
`HALT_G0_REAL_BUNDLE_WIRING`。三题均为搜索前失败，`search_iterations=0`；资源探针
和 G1 未运行。本条不授权把 G0 机械失败写成算法性能失败或成功。

只读证据表明，失败前的封存 witness 完整补全、直接检查和精确评分均已通过；否则
runner 会在进入短名单前以不同错误停止。三份 witness 均为全燃油方案，按车场统计
的路线数等于或低于冻结车队中的 `num_cv`。共同输入与旧 E2 witness 因此不是当前
已证实的失败源。

新解码器的路线局部候选构造把 `_single_route_cost` 返回有限值作为“可行”条件，但
该函数只计算成本，不执行硬约束检查。有限车队 DP 再按此局部成本截取前 6 个分配，
导致低估但完整模型不可行的候选能够占满短名单，已知可行的原车场/原车型分配没有
强制保留。v1 五件套未保存各候选违约类型，所以当前不得进一步声称具体是时间窗、
充电、容量或其他哪一类违约。

用户在收到上述根因和最小修复方案后明确授权“自主推进”。修复已另立结果盲 v2，
执行前冻结，且满足：保留 v1 HALT；路线局部
候选须用同一 checker 排除除“其他客户未出现在该单路线片段”之外的硬违约；已知完整
可行的当前分配固定进入短名单但仍占用可见完整评价预算；候选级违约类型和明细进入
审计产物；增加“便宜但不可行候选不能挤掉可行 incumbent”的测试；只重跑零搜索 G0，
通过后才允许资源探针和科学字段不变的 G1。v2 登记为
`g0_real_bundle_preregistration_v2.json`，修复前 37 项轻量测试全部通过。不得因 v2
结果再次改代码或改变 G1 题、种子、预算、算子与硬门。

## E2-GENUINE-HYBRID-G0-REAL-009：局部覆盖例外收窄与 G1 v6 最终 STOP

状态：`FINAL_STOP_G1_NO_HYBRID_GAIN__E3_NOT_AUTHORIZED`
（2026-07-25）。

G1 v5 运行期间、读取任何目标值前发现 v2 路线局部检查把全部
`CUSTOMER_COVERAGE` 违约忽略，范围超过登记的“只忽略片段天然缺少的其他客户”。
用户确认安全结束 v5；4 个已出现结果路径只计数不读内容，保存于
`g1_micro_v5_decoder_repair_abort/`，不得复用或解释。

批准且已执行的 v3 修复只收窄例外：仅当违约明细恰为 `customer not served` 且客户
不在当前片段时忽略；片段内重复服务继续判硬违约。未改完整检查器、成本、物理参数、
输入、搜索、三题、种子、每臂 80 次完整评价、75000 次 HGS、A+B 60% HGS、
60/40 分配、算子、接受规则或结果硬门。38 项测试通过，G0 v3 3/3 PASS，六进程
资源门 6/6 PASS。

G1 v6 最终 9/9 完成、每臂 80 次、全部可行且 replay 逐位一致。A+B 在三题中相对
`min(A,B)` 为 1 胜 2 平，唯一严格改善为 `cn-cy-150c-03` 的 0.894%；直接协同
改进证据为 0。预登记要求至少两题严格改善且至少一次直接回送后改进，因此判
`STOP_G1_NO_HYBRID_GAIN`。本条明确禁止结果后调参、重跑、换题、换种子、降门、
扩大 G2--G4、启动 E3，或把无损取优包装为 `1+1>2`。后续若要探索不同算法架构，
必须由用户另行批准新的问题诊断和结果盲合同，不能延续本次 G1 调参。

## E2-GENUINE-HYBRID-REDESIGN-010：失败归因后另立系统变邻域架构

状态：`USER_APPROVED__IMPLEMENTATION_AND_BEHAVIOR_GATE_ONLY`
（2026-07-25）。

用户明确否决把旧 v7 算法降格后直接作为论文最终方法；该路线只保留为真正走投无路
时的备选。当务之急改为查清真混合失败原因并实现真实成功，不得把“盘点旧材料并补
缺口”误当当前主线。

封存 G1 v6 的只读归因已确认：旧 B 只有五类随机问题动作，缺少完整路线优化能力；
六个 RR 阶段没有一次刷新 HGS 最好解；三题×三阶段×三视角共 27 个直接热启动局部
搜索后代全部因无可行车场/车型分配而无法完成联合解码；唯一严格胜出的 150 客户
结果来自不同随机流下的 HGS 档案，不能归因于合作。旧候选不能靠调比例或增加评价
次数救援。

批准另立
`docs/handoff/e2_genuine_hybrid_redesign_contract_20260725.md`。新候选进入独立目录
`baselines/algorithm_prototypes/tailored_dp_vns_20260725/`，先把第二算法做成完整的
系统变邻域搜索：单客户重插、客户交换、2-opt、2-opt* 和连续块联合重建；每个候选
均经车场—车型—有限车队—充电—时间的本题专用下层解码及完整模型评价。

首次只允许对 G1 v6 三道题的封存 A_HGS 最好解执行零 HGS 重跑的直接改善行为门，
每题最多 96 次完整评价。三题输出须全部可行，至少 2/3 严格改善，且至少一次改善
来自连续块联合重建或 2-opt*；五类邻域须在三题合计全部活跃，缺失车场/车型/充电
的候选不得进入完整评价，重复比例不得超过 20%。不通过即停止该新候选，不在同三题
调参。通过只授权另立新题盲测，不授权论文、E3、G2--G4 或全量长跑。

## E2-FEASIBILITY-GUIDED-VNS-011：首次 STOP 后另立可行性引导架构

状态：`USER_APPROVED__G0_AND_DIRECT_BEHAVIOR_GATE_ONLY`
（2026-07-25）。

`TAILORED-DP-VNS` 首次直接改善门已按登记停止：0/3 严格改善，141 个结构候选在
完整评价前因至少一条路线不存在可行车场/车型分配而被拒绝，只有 4 个候选进入完整
评价。五类邻域都已生成结构，输出解全部保持可行；原候选、三题和参数冻结，不扩大
短名单救援。

停止后的零目标结构诊断发现 25 客户题存在更靠后的路线级可行重插、交换、2-opt 和
2-opt*，说明失败根因是纯距离排序先截断、硬约束后过滤，而非强解附近绝对没有可行
移动。批准另立
`docs/handoff/e2_feasibility_guided_vns_contract_20260725.md` 和隔离目录
`baselines/algorithm_prototypes/feasibility_guided_ejection_vns_20260725/`。

新算法必须在生成阶段检查容量、时间窗、车场、车型、充电、有限车队和共享桩可行性，
并增加连续块弹出—逐客户束搜索重建；不得仅把原距离短名单从 10 调大。新开发题按
实例 ID 哈希规则预先固定为 `cn-jjj-50c-02`、`cn-cy-100c-01` 和
`cn-prd-200c-02`，起点使用题目冻结后 v7 五种子中最强的封存 `MV-HGS-SP`
witness。先做零完整目标 G0，再做每题最多 120 次完整评价的直接改善门；仍要求至少
2/3 严格改善且至少一次来自 2-opt* 或连续块重建。通过只授权另立未见题 A/B/A+B
小门；不授权论文、E3、全量、BKS 或 SOTA。

## E2-FGE-DIRECT-PACKAGING-012：行为门打包中断零搜索恢复

状态：`USER_APPROVED_AUTONOMOUS_RECOVERY__RESULTS_NOT_READ`
（2026-07-25）。

FGE-VNS 三道行为门算法计算全部结束并已保存三份轨迹与三份最终 witness 后，runner
在写 `raw_runs.csv` 时因结果字典含有表头外字段
`improvements_by_neighborhood` 抛出 `ValueError`，正式 decision 尚未生成。异常发生
前未向用户或代理显示三题目标结果；现场已整体保存为
`direct_improvement_gate_v1_abort_packaging/`。

批准绑定现场哈希后执行一次零搜索恢复：不得重跑搜索，不得修改已冻结算法、题、起点、
预算或性能门；只对封存起点和最终 witness 做直接检查与精确复算，并从轨迹恢复完整
评价账、重复、邻域访问和改善来源。原进程未持久化的每任务用时与峰值内存必须记为
不可恢复，不得估算或伪造；它们不是本行为门的 PASS 阈值，未来未见题 A/B/A+B 门
必须重新完整记录。恢复器搜索次数固定为 0，按原门生成版本化 v2 五件套。

## E2-RC-EV-SPLIT-013：回到既定 EV-aware Split 主线的工程门

状态：`USER_APPROVED__G0_ENGINEERING_GATE_ONLY`（2026-07-25）。

第二版可行性引导大邻域搜索已按冻结门停止：三个全新强起点仅 1/3 出现严格改善，
幅度 `4.55e-05%`，且连续块重建没有贡献。该结果说明可行性生成问题已经修复，但
在成品路线附近继续增加普通移动仍缺少足够互补性；禁止在相同三题扩大 512 上限、
束宽、候选数或完整评价预算。

仓库 `algorithm_optimization_prd_20260718.md`、旧 A11 短预算信号和新增 HGS/动态规划
文献共同支持重新启动此前未独立完成的 EV-aware Split：给定客户顺序，联合决定路线
边界、车场、车型、非线性充电、有限车队和共享充电桩占用。批准
`docs/handoff/e2_resource_constrained_split_contract_20260725.md` 及独立原型目录，
只执行单元测试和小中大三档零候选完整目标 G0。

G0 固定使用安全支配删除，不允许结果导向束宽；单位置 50,000 状态、全过程
500,000 状态和 4 GiB 峰值内存为硬停止线。三题必须能回放封存可行起点、生成非保底
片段、找到完整资源可行切分并通过完整验解。通过只授权另立新题直接效果门；不授权
HGS 接入、正式比较、论文、E3、全量、BKS、SOTA 或 `1+1>2`。

## E2-RC-EV-SPLIT-G0-WIRING-014：片段级不可行异常收窄

状态：`USER_APPROVED_AUTONOMOUS_REPAIR__V2_FROZEN_BEFORE_RESULTS`
（2026-07-25）。

RC-EV-Split G0 v1 在 0.6 秒内三题同类停止，0 行完成、0 个切分状态、0 个候选完整
目标评价。只读调用栈确认：连续片段方案生成器对“该片段没有任何车场/车型分配”
抛出 `NoFeasibleAssignmentError`，适配器没有按切分语义跳过该片段，而是错误终止
整道题。

批准 v2 只捕获这一种已知片段级异常并继续枚举更短/其他起点片段。v1 五件套和登记
原样保留；三道题、封存 witness、客户顺序、方案生成器、支配规则、50,000/500,000
状态上限、4 GiB 内存门、完整检查和所有 G0 判据不变。修复前未出现任何候选目标值，
不得借 v2 改参数或扩展性能主张。

## E2-RC-EV-SPLIT-G0-STOP-015：联合标签状态爆炸停止

状态：`FINAL_STOP_RC_EV_SPLIT_LABEL_STATE_EXPLOSION`（2026-07-25）。

G0 v2 在固定三题、固定顺序和固定资源上限下判
`HALT_RC_EV_SPLIT_G0_STRUCTURE_OR_SCALE`。15 客户题完成，生成 229 个状态、每位置
最多 4 个；75 和 150 客户题均触发 500,000 总状态硬上限。候选完整目标评价总数为
0，峰值内存未触发 4 GiB 门，输出不构成算法效果证据。

停止“有限车队 + 全部共享充电时隙进入 Split 标签”的精确联合状态表示。禁止扩大
状态上限、结果后束宽或同题救援。下一候选若继续，必须把路线局部方案生成与全局资源
组装分开：先形成带客户、车场、车型、充电时隙占用和路线成本的列，再用限时 MIP
统一满足客户覆盖、有限车队和共享桩容量，并完整披露 incumbent、bound、gap 和
status。该方向须先审计现有 MV-HGS-SP 与历史路线池失败，证明不是旧方案重命名。

## E2-ROUTE-COLUMN-MIP-016：共享桩约束路线列组装 G0

状态：`USER_APPROVED__G0_ONLY`（2026-07-25）。

旧 MV-HGS-SP 代码审计确认：列只来自 HGS 完整方案中的整条现成路线，MIP 含客户
覆盖和车队上限，但没有共享充电站半小时时隙容量；完整模型只在 MIP 后检查。历史
HGS+ALNS 路线池停止也来自父路线缺乏互补列，不能代表“从客户顺序生成新边界列”
无效。

批准 `docs/handoff/e2_route_column_mip_contract_20260725.md` 的独立 G0。新候选从
冻结客户顺序枚举所有路线级可行连续片段，形成带车场、车型、充电方案、成本和充电
时隙占用的路线列，再由限时 10 秒 MIP 同时满足客户覆盖、有限车队和共享桩容量。
只用既有三道工程题且不扰动顺序；结果只作结构/规模证据。通过不授权 HGS 接入、
论文、E3、正式、全量、BKS、SOTA 或 `1+1>2`。

## E2-ROUTE-COLUMN-MIP-G0-017：工程 PASS 后的新题直接效果门

状态：`USER_APPROVED__FRESH_DIRECT_HEADROOM_GATE_ONLY`（2026-07-25）。

路线列 MIP G0 三题全部通过：列数 159/1576/2278，MIP 均证明最优、gap=0、完整
约束与目标闭合，MIP 用时均低于 0.03 秒，整题最慢 8.4 秒，内存约 100--112 MiB。
三题目标与输入持平，G0 不形成效果证据。

批准按合同在三道全新结果盲题汇集 v7 五种子 MV-HGS-SP 客户顺序，固定比较最好父
解、只含父解整路的旧池、以及从全部顺序生成连续片段的新池。至少 2/3 新池严格超过
最好父解和旧池，且至少一次选入父解中不存在的新路线边界，才允许继续设计 HGS 接入
门。失败即停止，不换题、种子、顺序来源、MIP 时限或列上限。

## E2-ROUTE-COLUMN-MIP-DIRECT-STOP-018：单长链生成器直接门停止

状态：`FINAL_STOP_SINGLE_CHAIN_ROUTE_COLUMN_GENERATOR`（2026-07-25）。

三道结果盲新题全部完成，直接检查、精确评分、有限车队、共享桩时隙和MIP目标闭合；
HiGHS均证明最优且gap=0。新池含318/2444/5861列，但三题均未选择父解外新边界，
严格改善0/3，判`STOP_ROUTE_COLUMN_MIP_NO_DIRECT_HEADROOM`。禁止调整时限、
列上限、种子、题或顺序来源后重跑。

零目标结构审计确认根因是把无序路线集合按文件列表顺序压成单长链，只允许相邻路线
跨界；三题对全部父路线对的覆盖率为50.0%/19.2%/6.0%。该结论不修改封存v7、评价器
或旧门结果。

## E2-UNORDERED-ROUTE-PAIR-MIP-019：无序路线对精确重切最低成本门

状态：`USER_APPROVED_AUTONOMOUS__G0_AND_FRESH_DIRECT_GATE_ONLY`
（2026-07-25）。

批准`docs/handoff/e2_unordered_route_pair_resplit_contract_20260725.md`。新候选对
完整父解中全部无序路线对分别形成两个保持内部次序的拼接方向，只生成跨原边界路线；
扣除未动路线已占用的分车场车型车队和逐半小时共享桩容量后，用限时MIP重组该路线对，
并把候选放回完整解验收。

直接门使用结果前哈希冻结的三道全新50/100/150客户题，每题五个v7父解和五父整路池
组装解共六个固定起点。至少2/3题严格优于最好父解/旧整路池，且至少一次来自原文件
次序中不相邻路线对，才允许扩成独立迭代算法B并设计HGS双向接入。失败即停止，不改
方向数、路线对、2秒MIP时限、5000列上限、20分钟题级安全上限、起点或判据。PASS
仍不授权E3、正式全量、论文、BKS、SOTA或`1+1>2`。

## E2-UNORDERED-ROUTE-PAIR-MIP-STOP-020：固定内部次序路线对重切最终停止

状态：`FINAL_STOP_URP_FIXED_ORDER_RESCUE_FORBIDDEN`（2026-07-25）。

无序路线对 G0 通过后，三道结果前冻结的新题和每题六个固定起点全部完成。共求解
2850 个路线对 MIP，三题最终解均完整可行且与回退基线相同，但全部局部目标变化为 0，
进入完整评分的新候选为 0，严格改善 0/3，判
`STOP_URP_MIP_NO_DIRECT_HEADROOM`。

根因限定为：该方法覆盖了全部无序路线对，但仍固定两条路线内部的客户相对次序；
精确重切只能改变边界，不能产生新的客户排列。候选、三题、起点和参数冻结，禁止通过
增加方向、MIP 时限、列数、起点或换题救援，也不得扩为独立算法 B。旧 v7、评价器、
完成器、主 TeX、模型公式和所有失败证据保持不动。

## E2-HGS-ILS-XD-PREREG-021：文献审查后的顺序交接候选与两级低成本门

状态：`SUPERSEDED_BY_E2-HGS-ILS-XD-G0-STOP-023`
（2026-07-25）。

依据`docs/handoff/e2_order_changing_hybrid_literature_review_20260725.md`，排除已停的
普通 HGS+ALNS 教育、ReMIX、MPILS-MVNS、TAILORED-DP-VNS、
FEASIBILITY-GUIDED-EJECTION-VNS、穷举 SEG-GEN、资源约束 EV Split、单长链路线列、
固定次序路线对重切及旧 HGS+RR。不得通过改名、重新拼装或延长预算复活这些路径。

只批准候选`HGS-ILS-XD`进入 G0 工程开发：A 为 PyVRP 0.12.2 真实 HGS，B 为
PyVRP 0.13.4 独立 ILS。按用户对陈式逻辑的准确澄清，基础 A+B 必须先检验严格
`A→B`顺序交接：A 先完成并输出经共同完成、全局检查和精确评分的完整可行解，B
再以该解为起点独立深化。实验任务可并行，但一个`A→B`任务内部不得并行或用中点
双向交换替代顺序交接。顺序交接有既有文献先例，不声明新颖；只有该基础门通过后，
才允许另立合同检验一次受限的本题资源核验交换或路线池机制。

G0 固定三道未见题、B 冷/热启动共六任务、每任务128次 ILS 外层迭代和最多8次不同
完整评价；热启动固定使用五个封存 seed 中成本最低的纯`HGS-M` witness，不使用含
路线池的`MV-HGS-SP`冒充 HGS。资源门通过时用6 workers，因只有六个独立任务不得
复制到8。六任务须完整可行，至少2/3题产生 v7 五种子 HGS 路线池外的完整可行路线，
至少2/3热启动改变客户相邻关系并严格改善交接起点，且至少2/3题的热启动 ILS 严格
优于同题同种子的冷启动 ILS，才允许 G1；否则判
`STOP_HGS_ILS_XD_G0_NO_FEASIBLE_COMPLEMENTARITY`，禁止调参或重跑救援。

G1 仅在 G0 通过后开放，三道另选结果盲题固定
A24/B24/A-long48/B-long48/`A→B`48。`A→B`由A先用24次完整评价，B接手后再用
24次；G1不运行路线池或双向交换。主要比较`A→B`与同为48次完整评价的A-long、
B-long，并同时对照总评价数为48的`min(A24,B24)`独立并行取优。至少2/3严格胜、
三题零负、至少一题改善0.20%，且至少2/3可追踪到B接手后的新改善才通过；否则判
`STOP_HGS_ILS_XD_NO_MATCHED_BUDGET_SYNERGY`。通过也只授权多种子确认，不授权
China81全量、公开BKS/SOTA、E3或论文。

最终目标仍是公平证据下的真正 SOTA：公开 BKS 与私有 China81 严格分开；公开题须与
相关已发表强手和 BKS 在匹配实例、起点、种子、评分和计算量下比较；China81 须共同
完成、完整物理模型、精确评分和独立检查。任何小门、弱对手、有利子集或额外数倍计算
均不得写成 SOTA。

## E2-CHEN-SEQUENTIAL-HANDOFF-CORRECTION-022：陈式启发只取架构假设，不移植 VNS 动作

状态：`SOURCE_VERIFIED__CONTRACT_CORRECTED_BEFORE_G0`
（2026-07-25）。

已从陈雨蝶、干宏程等（2025）原文核验：其混合体由多种群 GA 先形成优良完整解，
后期再把该解单向交给 VNS 深化。用户借鉴的是这一“先完成、再交接”的架构逻辑，
不是要求把 insert、exchange、reverse、shift、swap、crossover 机械加入当前 ILS。

PyVRP 0.13.4 默认 ILS 已启用`Exchange10/20/11/21/22`、`SwapTails`和
`RelocateWithDepot`，源码另有但默认未启用的`Exchange30--33`。陈文多数搬移、
交换和跨路线尾段动作已被覆盖；显式三客户搬移、连续段反转又与本仓库已停止的
`SEG-GEN-01`、`TAILORED-DP-VNS`和`FEASIBILITY-GUIDED-EJECTION-VNS`重合，
此前已出现无改善、完整可行性拒绝或3.298--4.694倍用时。故本轮不追加陈文 VNS，
不把它作为第三算法组件，也不为失败 G0 做算子救援。

G0 在任何结果产生前已修订为纯`HGS-M`热启动与共同冷启动 ILS 的成对测试，并增加
“至少2/3热启动严格改善HGS起点、至少2/3热启动严格优于冷启动”两项硬门。该门只
验证顺序交接是否值得继续，不构成新颖性、性能、论文、E3、BKS或SOTA证据。

## E2-HGS-ILS-XD-G0-STOP-023：顺序交接基础门停止

状态：`FINAL_STOP_HGS_TO_OFFICIAL_ILS_HANDOFF_NO_RESCUE`
（2026-07-25）。

G0 按修订后登记一次执行，6个任务由6 workers完成；资源门记录6个不同进程、最低
空闲内存40%、合计峰值393.0625 MiB，任务异常0、源码漂移0、预算违规0。仅2/6任务
产生完整模型接受的新解，冷启动可行1/3；池外路线2/3，但热启动相邻关系变化1/3、
严格改善HGS-M起点0/3、严格优于同题冷启动0/3，判
`STOP_HGS_ILS_XD_G0_NO_FEASIBLE_COMPLEMENTARITY`。

零搜索独立复算重新核验6/6起点和2/2生成witness，完整评分、独立检查与记录目标闭合。
该结果冻结`HGS-ILS-XD`，不开放A24/B24/A-long48/B-long48/`A→B`48，不追加陈文
VNS，不调128次迭代、8次完整评价、题、seed或完成器救援。不得把“出现池外路线”
单独包装成互补性成功；基础门失败意味着后续性能、论文、E3、BKS和SOTA均未授权。

## E2-DUAL-GUIDED-RESOURCE-ORDER-024：强 HGS 深化根因复盘与唯一候选预登记

状态：`REVIEW_COMPLETE__PREREGISTERED_G0__NO_IMPLEMENTATION_OR_RUN`
（2026-07-25）。

冻结`HGS-ILS-XD`后，已把最新0/3热启动改善、普通局部邻域、连续段穷举、资源约束
Split、路线列MIP、固定内部次序路线对重切、ReMIX/MPILS/MDA和旧HGS+RR的失败证据
逐项对齐，并核验Zhao等、Dumez等、Desaulniers等、Froger等和Bruglieri等的一手文献。
不再提出通用ILS/VNS/ALNS、参数救援或机械堆叠。

唯一预登记候选为“对偶价格引导的资源耦合次序重构”：从客户覆盖、分车场分车型车队
和共享充电时隙线性松弛读取对偶价格，只选择两个高资源压力路线对；用`k=4`的
Balas--Simonetti型动态规划同时改变路线内部次序和路线分界；每题最多16个不同结构
进入共同完成、全局检查和精确评分。

G0按结果盲规则冻结六道未用于此前开发门的50/100/150客户题，六任务默认6 workers，
每题16次完整评价、100000状态、90秒安全上限。至少4/6严格改善、至少2/6改善不低于
0.05%、至少3个接受解产生旧池外客户相邻关系，且算子墙钟中位不超过对应HGS-M CPU
的25%才通过；任一失败即
`STOP_DUAL_GUIDED_RESOURCE_ORDER_NO_LOW_COST_HEADROOM`，禁止在已见题改参数、
换题或补跑救援。

权威合同为
`docs/handoff/e2_dual_guided_resource_order_contract_20260725.md`。当前只完成审查、
唯一候选合同和预登记；未实现、未运行，不授权E3、China81全量、公开BKS/SOTA、论文
图表或`1+1>2`。

## E2-DUAL-GUIDED-RESOURCE-ORDER-ENGINEERING-HALT-025：零成绩结构门停止

状态：`FINAL_HALT_BEFORE_G0__NO_RESCUE`（2026-07-25）。

按用户授权先实现零成绩工程验证；只有静态、接口、资源门全过才允许一次 G0。源码、
六题、五种子纯 `HGS-M` witness、完成器、评价器和参数已由
`g0_registration_v1.json` 冻结。合成/静态检查 7/7 通过。首个监控启动因工作目录
切换后解释器仍用仓库相对路径而在 0.12 秒退出，未加载算例、未计算；异常现场保留，
第二现场仅使用绝对启动路径，所有冻结哈希不变。

六任务由 6 workers 完成零成绩工程门，候选完整目标评价总数为 0。启动前可用内存
15.261%，预计六进程峰值合计 100777984 bytes，资源门无异常。结构门中五题有效候选
数为 0，仅 `cn-prd-150c-02-V2-LOCATIONS` 生成 1 个，因此判
`HALT_ZERO_OBJECTIVE_ENGINEERING_OR_RESOURCE_GATE`，G0 未运行。

不得在原六题上修改 `k=4`、两路线对、每方向四候选、100000 状态、90 秒安全上限、
筛选逻辑、起点或题后重跑，也不得把未运行的 G0 写成性能、BKS/SOTA、论文或 E3
证据。若认为零候选来自实现语义错误，必须保留本 HALT，另立版本化审查和用户/协调
任务批准；本条不自动授权修复。

## E2-DUAL-GUIDED-RESOURCE-ORDER-FINAL-STOP-026：用户裁决关闭候选

状态：`FINAL_STOP_DUAL_GUIDED_RESOURCE_ORDER_NO_RESCUE`
（2026-07-25）。

用户确认不开放 `E2-DUAL-GUIDED-RESOURCE-ORDER-ENGINEERING-HALT-025` 的实现语义
救援审查。冻结结构门的用途就是在读取候选成绩前淘汰无法在锁定算例上产生足量有效
候选的机制；5/6 题零候选、剩余 1/6 仅一个候选已经满足最终停止条件。原 HALT
判定、注册、监控现场、五件套和源码全部保留，不覆盖、不重写。

禁止修改代码、`k=4`、路线对数、候选数、100000 状态、90 秒上限、对偶排序、阈值、
算例、起点或完成/评价链；禁止重跑工程门、运行 G0、改名复活或把本候选并入其他
算法。该候选不授权 China81 全量、公开 BKS/SOTA、论文性能结论或 E3。

在用户另行明确指令前不启动下一候选；若未来启动，必须重新完成既有失败证据复核、
一手文献审查、唯一来源充分且可证伪的机制合同与最低成本预登记门。

## E2-RESOURCE-SLOT-PRICING-PREREG-027：资源—时隙定价路线生成候选

状态：`EVIDENCE_COMPLETE__ONE_CANDIDATE_PREREGISTERED__NO_IMPLEMENTATION_OR_RUN`
（2026-07-25）。

在最终冻结`HGS-ILS-XD`、普通局部动作、连续段穷举、`RC-EV-Split`、单长链路线列、
无序路线对重切及对偶引导有限位移路线对后，完成新一轮一手文献与失败证据复核。唯一
仍具备实质差异和最低成本可证伪性的候选，是在车场—车型分层稀疏时空图上运行资源
约束双向标签搜索：客户顺序形成时同步更新载重、时间窗、电量、非线性充电、充电时段
和地域—日期—时段成本，并让有限车队与共享充电时隙的主问题对偶价格直接进入扩展
约化成本。主问题只协调全局覆盖、车队与共享桩闭合，不把旧路线重组收益冒充新生成
机制。

最低成本门结果盲冻结`cn-cy-25c-02`、`cn-jjj-75c-01`和`cn-jjj-150c-02`，每题成对
比较`LOCAL`与`RESOURCE-SLOT`，共六个独立任务，资源门通过固定6 workers。每任务
50000次标签扩展、最多8条新路线、一次完整整解评分和60秒安全上限。必须至少2/3题
生成并选入旧池外新次序路线，至少2/3严格改善强`HGS-M`起点且一题不低于0.05%，并
在相同预算下至少2/3严格胜`LOCAL`、三题零负；中位/最大墙钟比不超过1.25/1.50。
任一失败即
`STOP_RESOURCE_SLOT_PRICING_NO_LOW_COST_STRONG_HGS_HEADROOM`，禁止同题调图、
调标签、换题、补种子或加全图兜底救援。

权威文件为
`docs/handoff/e2_resource_slot_pricing_candidate_review_20260725.md`。当前未实现、
未运行，不授权China81全量、公开BKS/SOTA、E3、论文性能或`1+1>2`。

## E2-RESOURCE-SLOT-PRICING-FINAL-STOP-028：零目标工程入口失败后不救援关闭

状态：`FINAL_STOP_RESOURCE_SLOT_PRICING_NO_RESCUE`
（2026-07-25）。

用户授权只运行冻结零目标工程门，且明确规定任一失败即 STOP、不修复、不重试。
`g0_registration_v1.json` 已冻结三题×两臂、六进程、输入 witness、源码和全部预算。
正式监控启动后，runner 在进入 `main()` 前因 Python 同名模块解析冲突退出：
`test_engineering` 被解析到已封存旧候选目录，缺少 `run_checks`。监控器记录
`FATAL_LOG_PATTERN` 和 `PROCESS_EXITED_WITHOUT_COMPLETION`。

候选完整目标评价数为0，真实算例任务和G0均未启动。本条不把工程接线失败误写成
算法无改进证据，但依用户冻结规则仍作程序性最终关闭：
`STOP_RESOURCE_SLOT_PRICING_NO_LOW_COST_STRONG_HGS_HEADROOM`。禁止修 import、
重启工程门、运行G0、修改源码/预算/题/起点、改名复活或写入China81全量、
公开BKS/SOTA、论文性能、E3及`1+1>2`主张。

## E2-RESOURCE-SLOT-PRICING-V2-FINAL-STOP-030：纯接线 v2 工程门失败关闭

状态：`FINAL_STOP_AFTER_V2_ENGINEERING_GATE__NO_G0__NO_RESCUE`
（2026-07-25）。

用户在 v1 未进入真实任务、候选完整目标评价为0的前提下，额外授权一次纯接线 v2：
只解决同名模块解析，不改变机制、题、起点、预算、标签、阈值、候选限制和停止条件。
v1 证据原样保留。v2 运行前精确模块检查6/6通过，v1/v2输入、任务及config完全相同，
37项源码与9项保护文件哈希闭合。

正式零目标工程门使用6 workers。六个子进程在反序列化任务函数时均报
`ModuleNotFoundError: resource_slot_pricing_v2_engineering_runner`，完成0/6，
候选完整目标评价0，判`HALT_ZERO_OBJECTIVE_ENGINEERING_OR_RESOURCE_GATE`；
G0未启动。

按授权的“v2任一门失败即STOP，不做算法或工程救援”，禁止修多进程可导入性、建立
v3、重启工程门或运行G0。该候选最终关闭；不授权China81全量、公开BKS/SOTA、E3、
论文性能或`1+1>2`。

## E2-RESOURCE-SLOT-PRICING-V3-G0-HALT-032：工程 PASS 后 G0 runner 解析错误

状态：`HALT_G0_WORKER_RESOLVED_ARCHIVED_RUNNER__NO_PERFORMANCE_EVIDENCE`
（2026-07-25）。

用户明确覆盖 v2 的工程不救援限制，只授权 v3 修复 spawned worker 可导入性，并要求
真实算例加载前先做六进程零目标冒烟；机制、题、起点、预算、标签、候选数、阈值和
停止条件不变。v1、v2证据原样保留。

v3冒烟6/6通过且为6个不同PID，冒烟通过前未加载真实算例；真实零目标工程任务6/6
PASS，候选完整目标评价0，工程/资源门判
`PASS_ZERO_OBJECTIVE_ENGINEERING_AND_SIX_WORKER_RESOURCE_GATE`。

随后冻结G0以6 workers启动。六任务均报`KeyError: 'k'`，完成0/6。只读复核证明
worker把裸`import run_g0`解析到已封存
`dual_guided_resource_order_20260725/run_g0.py`，而非本候选runner。候选完整目标
评价0，无witness，独立复算未启动，性能门未评价。

按用户指令，G0失败后只报告精确终态，不开启新工程救援。禁止建v4、重启G0或改变
算法/题/起点/预算/阈值；不授权China81全量、公开BKS/SOTA、E3、论文性能或
`1+1>2`。

## E2-RESOURCE-SLOT-PRICING-V4-FINAL-STOP-033：最终根因隔离与效果门关闭

状态：`FINAL_STOP_RESOURCE_SLOT_PRICING_NO_LOW_COST_STRONG_HGS_HEADROOM`
（2026-07-25）。

用户明确覆盖上一条“不建v4”的工程限制，并承担最终执行授权：只允许做一次根因级
导入隔离，禁止表面修改裸导入。v1--v3证据必须原样保留；新入口必须是唯一可导入包，
所有spawned worker须在加载真实算例前报告精确runner模块和文件身份。算法机制、三题、
起点、`LOCAL/RESOURCE-SLOT`两臂、6 workers、50000标签扩展、最多8条路线、每任务
一次完整评价、60秒上限及全部效果门不得改变。

V4六进程身份冒烟6/6通过且为6个不同PID，所有worker均闭合到
`rsp_final_isolated_20260725._frozen_g0_runner`及本候选冻结`run_g0.py`；真实算例
在冒烟通过前未加载。随后零目标工程任务6/6通过、候选完整目标评价0、资源门通过。

冻结G0六任务6/6完成，违约0，六个输出由完整评分与独立检查逐份重放闭合，每任务
恰好一次完整候选评价，全部预算和墙钟门满足。资源定价臂对强HGS起点改善0/3、达到
0.05%改善0/3、胜同预算LOCAL 0/3、负0/3；选入旧池外负约化成本新次序路线0/3，
资源价格改变选路归因0/3。墙钟比中位0.998893、最大1.004203。

因此按预登记规则判
`STOP_RESOURCE_SLOT_PRICING_NO_LOW_COST_STRONG_HGS_HEADROOM`，下一步
`FINAL_STOP_NO_RESCUE`。不得修改标签/弧/候选/状态/时间上限、阈值、题、起点或
选择逻辑后重跑；不得扩大China81、启动E3、公开BKS/SOTA或写入论文性能与
`1+1>2`。本条是已排除工程歧义后的低成本效果结论。
