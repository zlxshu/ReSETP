# MPILS-MVNS 第二候选 G1 B组首发合同

- 编号：`MPILS-MVNS-C2-G1-B-FIRST-FIRE`
- 日期：2026-07-20
- 状态：`USER_APPROVED__PREREGISTERED_BEFORE_SEARCH`
- G0 冻结提交：`2f434816`
- 当前源码：
  - `c2_core.py`：
    `78308a199b1cb9ff0a96ba74086ddc49ba476f821d16964f2f006a15672c3a9f`
  - `event_driven_ils.py`：
    `71590b0f493bdf8e6bc53330b2412a1c4646e98f88d9c40606a09ef59a456b18`

## 1. 本门只回答一个问题

G0 已证明第二候选没有接坏母体、闲时开销低于 1%、替换动作真实且预算受控。
G1 首发只检验：在第一候选没有见过的三道 B 题上，当前冻结候选能否对同批新跑的
PyVRP 0.13.4 ILS 母体做到零负，并至少严格胜一题。

G0 不构成性能证据；本门也不是完整公开算例、China81 或论文优越性实验。

## 2. 冻结输入与运行口径

- 实例：`PR11B`、`PR17B`、`PR21B`；
- 原题：V13-MDVRPTW 冻结包中的原始规范化题；
- 种子：每臂每题只用 `seed=1`；
- 母体：PyVRP `0.13.4` ILS；
- 候选：提交 `2f434816` 冻结的 MPILS-MVNS-C2；
- 每臂核心搜索墙钟：`8.5` 秒；
- 端到端保护上限：`10.0` 秒；
- 单线程；
- 每题两臂同批新跑，不复用任何旧基线数字；
- 固定运行顺序：
  - PR11B：母体 → 候选；
  - PR17B：候选 → 母体；
  - PR21B：母体 → 候选。

两臂使用同一原题、同一母体种子、同一初解构造、同一成本和可行性语义。候选的
专用扰动使用独立随机流，不消耗母体随机数。五类机制始终常驻问询；公开题缺少
车型充电、碳、电价、公平或动态字段时，只能自然记录不适用，不能人工关闭。

## 3. 冻结候选常数

- 停滞触发：`128` 次无最好解改善；
- 冷却期：`128` 次迭代；
- 目标路线边差异区间：`[0.15, 0.35]`；
- 精英库上限：`8`；
- 反馈强度：`1..3`；
- 每次触发的硬上限：源段 `64`、每源段位置 `6`、路线级检查 `48`、完整评价
  `12`。

本次首发不允许改动这些常数，不允许加入新模块，不允许用 A 组或确认题决定首发
参数。

## 4. 判定

每题按冻结计分器的精确整数目标值比较：

- 候选小于母体：严格胜；
- 候选等于母体：平；
- 候选大于母体：严格负。

通过条件同时满足：

1. 六个解均完整、可行且独立验解通过；
2. 六次端到端墙钟均不超过 10 秒；
3. 五类机制常驻问询；
4. 四项扰动限额全部守住；
5. 受保护文件哈希不变；
6. 三题严格负为 0；
7. 三题至少严格胜 1 题。

通过标签：`PASS_G1_B_FIRST_FIRE_ZERO_LOSS_AT_LEAST_ONE_WIN`。

未通过标签：`FAIL_G1_B_FIRST_FIRE__DIAGNOSIS_REQUIRED`。失败后不允许立即改代码、
换题或增加模块。

## 5. 两发协议

如果首发失败，先根据原始诊断账判断死因。只有死因明确指向本合同中的两个常数族
之一——停滞/冷却常数，或强度升级顺序——才允许使用已暴露的
`PR11A/PR17A/PR21A` 做一次诊断和一次常数修订。

修订后必须重新提交冻结；B 组只许再测一次。第二发仍失败则
`STOP_MPILS_MVNS_C2_REPLACEMENT_LINE`，不换题、不扫参数、不加模块。确认题
`PR16A/PR24B` 不能参与诊断或调参。

## 6. 通过后的边界

G1 首发通过只允许向用户申请 G2 确认门。G2 仍为 `PR16A + PR24B`，不得自行启动。
完整 28 题、扩种子、China81、阶段二和论文优越性主张均不在本合同授权范围内。
