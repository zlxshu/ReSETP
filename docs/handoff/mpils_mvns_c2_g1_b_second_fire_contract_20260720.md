# MPILS-MVNS-C2 G1 B组唯一第二发合同

- 编号：`MPILS-MVNS-C2-G1-B-SECOND-FIRE`
- 日期：2026-07-20
- 状态：`AUTHORIZED_BY_TWO_FIRE_PROTOCOL__PREREGISTERED`
- A组诊断证据提交：`8883d8a9`

## 1. 唯一修订

A组母体在 PR11A/PR17A/PR21A、seed1、8.5 秒内完成
`11109/10895/11849` 次迭代，中位数 `11109`。按运行前冻结公式
`round(0.15 × 11109)` 得：

- `trigger_after = 1666`
- `cooldown = 1666`

第一发的值均为 128。除此之外不改反馈强度、目标边差异区间、精英库、独立随机流、
五机制常驻、段动作、代理排序、母体精修或 64/6/48/12 限额。

## 2. 运行与判定

完全复用首发口径：

- PR11B、PR17B、PR21B；
- seed1；
- 每臂核心搜索 8.5 秒、端到端不超过 10 秒、单线程；
- 母体同批新跑；
- 固定顺序为母体→候选、候选→母体、母体→候选；
- 精确目标值判胜负；
- 六解独立验解；
- 过线为零严格负且至少一严格胜。

BKS 只用于报告差距，不进入搜索或胜负判定。

## 3. 终局

通过标签：
`PASS_G1_B_SECOND_FIRE_ZERO_LOSS_AT_LEAST_ONE_WIN`。通过也只允许向用户申请
PR16A/PR24B 的 G2，不得自行启动。

未通过标签：
`STOP_MPILS_MVNS_C2_REPLACEMENT_LINE_AFTER_SECOND_FIRE`。第二发失败后不再
改 W、不扫参数、不换题、不加模块，替换式扰动路线终结。

无论结果如何，完整28题、扩种子、China81、阶段二和论文优越性主张均未获授权。
