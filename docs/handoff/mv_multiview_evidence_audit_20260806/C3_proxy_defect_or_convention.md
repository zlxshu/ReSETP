# C3：集合划分模型少写约束，是正文建模的遗漏缺陷还是期刊文献的惯例

编号：`MV-AUDIT-C3-20260806`
执行：Codex（只读取证，codex 会话 `019fd5a6-513e-7d10-8ac8-762485bccf09`，任务 `task-msh3tvwf-se96fi`）
落盘：终端 Claude（codex 沙箱对本外置盘只读——该盘为 `exfat`，
连对目标文件 `touch` 都返回 `Operation not permitted`；Codex 拒绝改写到别处，只交回结论）
状态：**`PARTIAL`**

> **不要拿本文当完成品。**
> Codex 反复尝试写盘失败，最终只交回**判定与可行性核验那一半**，
> **报告正文、逐条行号、文献页码全部缺失**；
> 而且**用户真正问的那一半（文献惯例）没有回答**——见 §4「缺口」。
> 补查任务 C3b 已另行派出，回来后替换本文。

---

## 1. 判定（Codex 原话）

> 取证结论：**【两者兼有】**。简化代理是文献中真实存在的做法，
> 但历史 O 的 PASS 未覆盖完整实体车多趟约束；XA 后续正式包则有严格多趟证书，
> 因此"所有已报告解从未完整验解"不成立。

## 2. 已交回的实质发现（可行性核验链）

**这一节回答的是"已报告的解到底有没有按完整模型验过可行性"，是本轮最有价值的产出。**

`FACT`　**2026-07-27 O 臂那 405 个 PASS 是真的走过检查器的**——
逐解经过当时的 `check.py` / 精确评分器判定，不是空标签。

`FACT`　**但当时那条检查链不等于当前正文定义的"完整实体车多趟模型"。**
该历史源码：
- 只施加 **CV/EV 分车型上限**；
- **未施加**每车场**总实体车数**上限；
- **没有严格多趟物理化**。

`FACT`　**2026-08-02 的 XA 正式包另有完整证据**：
`strict_multitrip=true`、`multitrip_certificate.status=PASS`、`violation_count=0`。

`INFERENCE`（Codex）　因此**"所有已报告解从未按完整模型验过可行性"这个命题不成立**。
必须把两个命题严格分开，不能混成一个：
1. **"405 个 O 解未按完整实体车多趟模型验解"** —— 成立；
2. **"所有已报告解从未完整验解"** —— **不成立**（XA 包有严格多趟证书）。

`FACT`　**当前适配器工作树含有 2026-08-06 的未提交改动，不能直接当作正式实验依据。**
核对历史结论必须沿正式实验包的历史源码与产物哈希走，
不能拿当前 checkout 的行为反推过去的运行。

## 3. 本轮的方法学教训（值得保留）

Codex 明确记录：记忆索引只作检索线索，
**不得把历史屏幕观察直接当作本次事实**；
`complete_china81_route_skeleton`、`exact_china81_score`、`check.py`
与实验台账都必须在当前 checkout 中重新核实。
本轮正是靠这一条才发现"O 臂 PASS 是真的，但那把尺子不是现在这把"。

## 4. 缺口——**用户真正问的那一半没有回答**

下列内容 Codex **未交付**，C3b 补查中：

1. **集合划分模型逐条约束核对**（文件路径:行号），
   以及与论文正文对 SP 重组之声称的逐条对照。
2. **文献惯例问题（用户的原问题）**：
   在 VRP 文献里，集合划分/集合覆盖**主问题只写覆盖约束、
   把时间窗电池等可行性全部下放到列生成（路线生成）子问题**，
   是不是通行做法？须给论文页码/公式号。
   > 终端 Claude 的先验判断（`INFERENCE`，待证实或推翻）：
   > 经典列生成/分支定价框架（Desrochers-Desrosiers-Solomon 1992 的 VRPTW、
   > Baldacci 等的 set-partitioning based exact methods）里主问题**确实**只有覆盖约束，
   > 因为**列本身按构造就是可行路线**。
3. **真正的分野**：是否在于"文献的列按构造即可行，
   而我们把不同视角的路线混搭后整体不再可行"？
   若是，则病灶不是"主问题少写约束"，而是**"列的可行性定义与主问题不匹配"**——
   两种诊断导向完全不同的修法。
4. **混搭不同来源路线时，文献用什么手段保证整体可行**
   （资源约束放在子问题 / 主问题加 side constraints / restricted master + 修复）。
5. **84%—93% 的重组拒绝率在文献里属于什么水平**，有没有论文报告过这个量级。
6. **退回父方案的逻辑是否确实存在且生效**的代码级核实。

> Codex 交回的判定句里那半句"简化代理是文献中真实存在的做法"，
> 说的是 **PyVRP 搜索代理**，不是集合划分主问题——
> 这是首轮派活认错对象的残留，**不能当作对用户原问题的回答**。
