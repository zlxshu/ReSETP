# 夜间自主推进计划（2026-08-12 夜）

用户授权：「你自己今晚逐个自主推进吧。非必要不要骚扰我睡觉。」目标＝把实验推到发表标准所需的地基全部就位。

## 执行序列（严格串行于计时敏感段）

| 段 | 内容 | 前置 | 状态 |
|---|---|---|---|
| N1 | 预筛提速落地＋审代码 | 无 | 施工中（bmw6j2epu） |
| N2 | 车场对重建（cy/jjj 换近距真实园区对，P46 补充）＋审 | 无（数据侧，可与 N1 并行） | 施工中（bthiyyf5i） |
| N3 | **实战 v2**：①G1——把 `rebuilt_volume_capacity_enabled`/`rebuilt_shift_neighbours_only` 接进 combat（开关已存在默认 False）；②G2——机制级开关组（关一个机制＝关死它全部通路，含内核 DepotSplit/reload 旁路），并以轨迹证明闭合；③R6——按红队 Q2 复算结果做班次感知的代理电价（默认关，combat 开）。提示词已备 `$SP/codex_combatv2_prompt.txt` | N1 完成（同文件冲突） | 待派 |
| N4 | **收敛标定（P47）**：combat-v2 配置，prd 代表算例（50c-01×2 种子＋100c-01×1 种子），长预算记录在职最好解轨迹，产出收敛曲线数据。提示词已备 `$SP/codex_converge_prompt.txt` | N3 完成 | 待派 |
| N5 | 从收敛曲线定预算 → **总消融第一批**（机制开关组逐关，全效应矩阵 §执行顺序第 2 步） | N4 出曲线 | 视夜里进度 |
| N6 | 公开 28 题多种子 benchmark | 私有消融收口＋公开默认路径重验逐位一致 | **不在今夜强推**（与私有计时实验互斥，P43-K） |

## 夜间纪律

- 每份 Codex 交付必审代码（受保护文件哈希、默认关闭解哈希、增量评价、真值隔离）；
- 计时敏感实验严格串行、净机；发现无关重负载先清；
- 不推送打扰用户；仅在数据损失级事故时推送；
- Codex 额度若耗尽：停施工，转文档/分析，不越权改代码（角色铁律不因夜间授权失效）；
- 每段收口即落盘台账与事实源，防上下文压缩丢失。

## 已知风险与预案

- N3 的 G1 会**合法地改变搜索行为**（这是修复不是回归），默认配置必须逐位不变；combat 行为变化按"修复前后拒绝率对照"报告。
- N4 若曲线到 2.5 小时仍未平台：如实记录，预算取"最后改进点＋余量"并在早报中标注不确定性。
- N5 若通道闭合测试发现关不死的旁路：停在 N3 修复，不带病跑消融。
