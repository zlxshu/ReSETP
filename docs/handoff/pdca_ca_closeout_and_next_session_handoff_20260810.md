# 2026-08-10 PDCA 检查—改进收口与下一会话交接

状态：`CURRENT_SESSION_HANDOFF`

这份文件是 2026-08-10 本轮长对话的收口正文。新会话先读本文件，再查
`pending_decisions.md` 的具体已决与未决状态。它只整理当前有效目标、已经查实的证据、
尚未解决的问题和下一入口；不把代理建议升级成用户决定，也不授权自动进入下一阶段。

## 一、一句话总览

`USER DECISION`：项目唯一主线仍是形成一套运行时与开源 PyVRP HGS 完全分离、公开算例超过
冻结原生 HGS、私有算例能够支撑时变碳强度、混合车队和动态需求三大实验，并正确覆盖多车场、
多趟、非线性充电、协同配送和公平分配的最终自研算法。算法、算例和实验底座共同通过技术试跑后，
才进入正式实验。

`FACT`：截至本文件落盘，最终算法尚未完成，公开性能尚未过关，私有三大实验和尚未统一的五项
支撑因素也没有在同一套最终算法、同一代表算例上完成消融。当前不能放心进入正式实验。

## 二、用户已经确定、不得重开的边界

1. `USER DECISION`：公开算法超过冻结原生 HGS 是既定目标，不得再包装成“是否降低要求”的选择题。
2. `USER DECISION`：开源 HGS 与自研算法运行时完全分离、各成体系。成熟 HGS 组件可以按许可证复制、
   剥离、改名并独立编译；自研算法运行时不得调用 PyVRP。复制部分不算创新，真正的自研身份由经消融
   证明有效的问题部件承担。权威登记为 P31。
3. `USER DECISION`：公开与私有最终使用同一份 HGS 控制流程，通过普通路线与完整实体车整日任务两个
   适配器接入；能源、非线性充电、跨趟时间—电量衔接、完整成本和完整可行性必须进入同一套搜索、
   入群、生存和父代选择，不能继续由三套外层循环各自为政。
4. `USER DECISION`：DCREX 源码和历史证据保留，但不进入最终算法默认运行路径；不得自动恢复。权威登记为 P29。
5. `USER DECISION`：每个算法、每个算例最多运行 20 分钟；能提前收敛则提前停止。公开自研算法与冻结
   PyVRP 0.12.2 HGS 使用相同 20 分钟上限。权威登记为 P20。
6. `USER DECISION`：公开算例继承论文 v2 同款 28 例；私有算例从 China81 中选择能够让三大实验和五项
   支撑因素产生清楚效应、并尽量统一的代表算例。China81 可以重构，但必须先证明确属算例问题，提交
   调整申请和设计方案，经用户批准后才能修改。
7. `USER DECISION`：非线性充电是提高仿真可信度的支撑条件，不要求单独承担主要减排效应。车场采用
   Montoya 图 8 的 22 kW 常规曲线；公共站保留 60 kW 构造情景，并把同图 44 kW 快充曲线的归一化形状
   缩放到 60 kW，必须透明披露为文献形状迁移。权威登记为 P35。
8. `USER DECISION`：重大算法取舍、模型含义、正式参数、正式实验口径和算例修改仍由用户决定；普通机械
   实现和已经证实的接线错误可以自主处理。任何代理不得自设门槛、阈值、门禁或新的研究约束。

## 三、当前算法究竟是什么状态

`FACT`：当前仓库中已经存在一套不导入 PyVRP 的独立 HGS 代码，但“代码独立存在”不等于“最终算法已经
完成”。目前仍能看到三类历史形成的控制逻辑：复制剥离的成熟 HGS 底座、公开算例使用过的自写增强循环、
私有完整 Duty 问题使用的项目自写循环。三者尚未完全收敛成 P31 要求的唯一控制流程。

`FACT`：复制剥离的 HGS 底座在现有等价核对中与冻结 PyVRP 0.12.2 产生逐位相同的结果。它证明复制和
独立编译可以工作，但不能作为超过母体的证据，也不构成自研贡献。

`FACT`：旧 `algorithms/duty_hgs/` 代码仍在源码树中，其中历史文件仍有直接 PyVRP 调用。它们属于旧路径，
不能拿来证明当前独立算法已经完全闭合；后续清理不能抢在性能主线之前，也不能未经用户决定删除仍有价值
的历史材料。

## 四、公开算例已经查到什么

`FACT`：当前 fast-only 自写循环在 v2 同款 28 例、seed 11、每例最多 20 分钟、整数化距离口径下，对冻结
PyVRP 0.12.2 HGS 为 18 胜 10 负，平均配对成本低 0.2621055%，总运行时间为对手的 0.939339 倍；全部
服务量完整。原始包为 `solver/reports/problem_hgs_public_v2_28_fast_only_trial_20260809/decision.json`。

`FACT`：这批结果是单种子技术试错，且尚未完成与论文 v2 文献强对手一致的原始精度可行性闭合。因此
18 胜 10 负不能直接写成公开竞争力已获证明。

`FACT`：同一 28 例包中，不带 DCREX 的 fast-only 版本相对带 DCREX 的旧混合版为 17 胜 11 负，平均好
0.4333428%。这与 PR17A 十种子中 DCREX 只带来很小平均改善、但显著增加时间的证据方向一致，支持 P29
“代码保留、默认退出”的用户决定。

`FACT`：公开算例不含充电。私有充电候选生成方式不可能解释或修复公开复合部件的性能与耗时问题。

`UNKNOWN`：当前已有部件中，究竟哪一项产生了那约 0.26% 的质量差异，哪一项只是增加耗时；原始精度
下能否保持完整可行与优势；怎样只利用已有部件形成稳定超过冻结 HGS 的唯一独立算法。这是当前公开主病灶。

## 五、私有算例已经查到什么

### 5.1 混合车队

`FACT`：既有三地区技术试跑中，混合车队相对全燃油对照出现成本下降约 5.9%—6.9%、排放下降约
11.6%—20.9% 的信号；珠三角全燃油臂带有一项公平违规。证据汇总见
`docs/handoff/independent_algorithm_status_audit_20260809.md`。这说明混合车队有作用空间，但正式对照口径、
车型供给与公平基准仍未闭合。

### 5.2 时变碳强度

`FACT`：早期三地区技术对照的系统排放变化约为 -0.69% 至 -0.99%，电费基本不变，效应很弱。最新一次
珠三角、BOTH_TYPES_AVAILABLE、seed 11 技术对照中，时变碳相对日均碳强度的实际排放下降
3.6312896%，实际总成本下降 0.0199124%，服务量完整。原始包为
`technical_trials/carbon_both_types_current_20260810_prd50_seed11_i100_v1/decision.json`。

`UNKNOWN`：最新信号中有多少来自碳价、碳目标、车型供给、充电搜索或算例作用空间；成本折算、并列目标
和排放上限三种模型路线也没有完成同源技术对照。用户尚未掌握足够证据，不得要求用户现在拍板。

### 5.3 动态需求

`FACT`：现有技术试跑中，一个种子没有变化，两个种子出现较强改善信号；同时查出过在途电量截面、同车
多趟顺序和短视地耗尽车辆／待命电量等问题。局部接口问题不等于动态主实验已经完成。

`FACT（用户当前理解与倾向）`：用户更倾向邱莹莹式“有订单才接、分段滚动重算”的简化动态思想；只有目标期刊明显要求
更复杂方法，或者复杂版本已经正确完成且确有价值，才考虑保留复杂前瞻。不得为了仿真十全十美而 overdo。

`USER DECISION`：待命充电若进入最终算法，只能依据事先登记的潜在客户池和需求分布抽取内部情景，不得
读取本轮尚未揭示的真实订单。当前活动事件流不满足这一独立信息结构，A1 潜在池尚未施工，而且不得抢在
算法性能主线之前。

## 六、五项支撑因素的真实边界

- `FACT` 多车场：基础模型与完整评价存在，但历史客户同城／近场预归属可能压缩客户跨场服务、协同和公平
  的作用空间。是否改生成规则属于算例与模型含义调整，必须先证明并申请。
- `FACT` 多趟：实体车身份、趟序、连续 SOC、趟间充电和完整整日评价已经接通并有回归测试；这只是底座
  正确性，不等于最终算法已经能高效搜索。
- `FACT` 非线性充电：7A-1 曲线已接入并通过相关回归。它主要保证仿真可信，不承担公开性能，也不要求单独
  产生主要效应。
- `FACT` 协同配送：跨车场整趟动作和完整利润评价入口存在，但尚未在当前最终统一算法中证明稳定增量。
- `FACT` 公平分配：v2 论文中的利润、参与和分配素材可以复用；当前最终独立经营利润基准已定为各车场独立
  运行最终算法 10 个种子取最好利润。正式基准尚未生成，公平机制也没有在最终算法上完成消融。

`INFERENCE`：目前没有证据证明一个现成 China81 算例已经同时适合三大实验和五项支撑因素，也没有数学／
物理证据证明统一算例不可能。代表实例 P08/P22 继续待决。

## 七、本轮 PDCA 的检查与改进收口

### 检查得到的纠正

1. `CORRECTION`：公开超过原生 HGS 是既定目标，不是等待用户选择是否降低的事项。
2. `CORRECTION`：私有充电候选过慢与公开复合部件过慢是两个不同病灶，必须分别处理、分别归因。
3. `CORRECTION`：车型成本、三种碳模型、动态复杂度和深度充电搜索的价值证据没有备齐之前，不得让用户
   替代理猜测或拍板。

### 已执行的最小改进

`FACT`：经用户批准，只把私有 `problem_hgs/charging.py` 的充电备选由“先生成每趟全部备选再组合”改为
按需生成。候选集合、原笛卡尔积顺序、完整评价和模型未改变；相关私有、充电、多趟和动态回归 45/45 通过。

`FACT`：珠三角 PRD50、seed 11、BOTH_TYPES_AVAILABLE、关闭可选深度充电细化、最多 100 轮／1200 秒的
同口径复测，修复前后分别完成 21 与 22 轮，单位时间轮数提高 4.742644%。最终个体指纹、总成本
1793.348839580645、总排放 126.34916272649504 kg、2 辆燃油车／4 辆电动车、50/50 客户和
12223/12223 需求逐项相同。新包为
`technical_trials/fleet_supply_current_algorithm_20260810_prd50_BOTH_TYPES_AVAILABLE_seed11_i100_lazy_charge_v1/`。

`INFERENCE`：按需生成修复正确且没有改变结果，但单次只多完成 1 轮。它消除了局部无用计算，却不是私有
性能的主要瓶颈，更不能包办公开性能问题。

## 八、当前现场

1. `FACT`：没有正在运行的算法或实验进程。
2. `FACT`：本轮按需生成源码、回归测试、技术复测包和交接记录均已保存。
3. `FACT`：三个受保护文件在本轮没有改动，当前 SHA-256 分别为：
   `cost.py ad5b360d...ccd1`、`check.py 1cdb6236...b1072`、
   `search/evaluation.py c7215263...40d950`。
4. `FACT`：当前工作树包含本轮以及更早尚未统一提交的改动；本轮尚未提交和推送。新会话不得覆盖、清理或
   把全部脏工作树误算成本轮单独改动。
5. `BOUNDARY`：尚未启动正式实验，尚未修改正式论文正文，尚未选择代表算例，也没有增加新算子、新阈值、
   新门禁或新的调查支线。

## 九、下一会话的唯一入口

`USER DECISION`：下一步不是继续优化充电，也不是加强冻结原生 HGS，而是单独查清公开性能病灶。只能先
利用当前已有部件和既有产物回答：什么真正带来质量改善，什么只是增加耗时，原始精度是否保持可行，当前
限制与“必须超过原生 HGS”的目标冲突在哪里。

下一会话应当先做只读归因和必要的最小同源核验，交付一份大白话病灶说明；未经用户批准不得增加新算子、
降低目标、改变比较口径或自动进入下一项。历史上已获概念批准的 Vidal 式复合局部评价也不得借旧批准自动
施工；用户最新指示要求先把现有部件的真实病灶说明白。

公开病灶查清并经用户决定后，才轮到统一独立算法的最小施工；该阶段完成也不自动授权进入私有正式实验。
随后仍须用同一最终算法复核私有三大实验、五项支撑因素和统一算例。

## 十、Claude 与 Codex 的协作边界

- Codex 负责源码、产物取证、普通施工和验证，并承担整合责任。
- Claude Opus 是互补搭档和高级顾问，负责关键反例、根因辩论和最终独立复核；不做机械苦力，也不逐步签字。
- 双方形成的共识仍是代理意见；事实回到源码和原始产物，重大决定回到用户。
- Claude 必须核当前源码和当前产物，不能把旧 Duty-HGS 的 APPROVE 迁移给当前独立算法。
- 同层级待决事项一次性收齐，用大白话说明选项和后果；没有准备好证据的事项不提交用户猜测。

`FACT`：Claude 的全局入口 `/Users/zhouleixishu/.claude/CLAUDE.md` 已继续指向共享全局规范
`/Users/zhouleixishu/.codex/AGENTS.md`，并补充说明十六份通用技能的共享安装方式。六份
`global-ai-*`、九份 `global-academic-*` 和 `paper-formula-symbol-audit` 已安装到
`/Users/zhouleixishu/.claude/skills/`，以符号链接指向 Codex 的同一份技能正文；16/16 链接、目标和
`SKILL.md` 均已核验可读。Claude 原全局入口备份为
`/Users/zhouleixishu/.claude/backups/CLAUDE_before_shared_skills_20260810.md`。

`PROCESS`：前两次安装后核验脚本分别因 zsh 不自动拆分普通字符串、误用 zsh 保留变量 `path` 而在核验
脚本自身报错；没有修改或破坏技能。改用显式数组和任务专属变量后第三次完整核验 16/16 通过。

本轮 PDCA 的 C/A 到此闭合。下一会话从第九节开始，但本文件不是开始下一阶段的授权。
