# 阶段一并行执行状态与用户终裁门

日期：2026-07-18

状态：`PHASE1_NON_ALGORITHM_HANDOFF_READY__ALGORITHM_OPTIMIZATION_EXCLUDED__STAGE2_NOT_STARTED`

## 2026-07-18 订单属性方法补充批准与论文同步

用户补充批准把中国扩展算例的服务时长和交付时间窗方法计入手册并同步论文。基础方法现为：容量占比需求代理、来源论文`0.1 h/m3`案例服务时长、1222条交付订单完整经验行联合重采样。三项边界分别为非实测重量、非实测停站时长、不得拆分时间窗与订单属性独立抽样。该同步不改变目标函数、约束或求解器，也不启动阶段二；正式China81、道路全矩阵和算法搜索仍禁止。新增公式须通过独立Python数值/单位验证、现有数学审计和TeX编译。

## 2026-07-18 用户批准与最终交接审计

用户批准MC-004道路三矩阵生成方法，并明确“暂时不进入阶段二”。批准不包括生成九城全矩阵、China81正式实例、运行求解器搜索或合入阶段二机制；机器合同继续保持`formal_search_allowed=false`。

最终独立审计`data/ChinaInstances/phase1_nonalgorithm_handoff_audit_final_v1_20260718/`判`PASS_PHASE1_NONALGORITHM_HANDOFF_READY`：全部检查通过，待批项0，错误0，警告0，搜索评价0。阶段一非算法部分由此达到可移交状态；算法优化不在本次验收范围，阶段二未启动。

## 2026-07-18 修复执行结果

MC-005候选A已正式应用并重建81份客户位置，5805行、27格内三复本身份零重叠、合同SHA和产物哈希均通过。九个普通物流设施各固定一个WGS84情景道路接入点并登记对应OSM way，五份冻结PBF上的CV/EV最小路线18/18通过；点位不冒充实测货车门。OSRM 26.7.3完整功能门18/18通过，覆盖高、宽、长、重、`hgv`、单行、不可达、方向性距离/时间、逐段注释和`Σ(v²d)`接口。

独立交接预审`data/ChinaInstances/phase1_nonalgorithm_handoff_audit_preapproval_v1_20260718/`无错误，所有执行证据通过；当时唯一待办是用户对重新提交的MC-004道路三矩阵生成方法作最终批准。该待办现已批准并由上方最终审计闭合。未建全矩阵、未生成正式China81实例、未运行求解器搜索，算法优化不在本次范围。

## 2026-07-18 独立复核纠正

下方“非算法部分最终收口”已撤销。复核发现：MC-005候选A尚未写入机器合同并重建81份位置分配；九个普通设施没有逐城WGS84道路接入点和冻结路网可达证据；人工路网没有覆盖长度、宽度、重量、单行、不可达、方向性距离/时间和分段能耗接口；MC-004仍未重新审批。因此当前只可继续零搜索修复，不得移交阶段二。

## 2026-07-18 已撤销的非算法部分收口（历史记录）

用户批准把固定路由器从无法在当前AppleClang 16完成构建的旧OSRM v5.27.1改为当前OSRM 26.7.3，并要求除算法优化以外尽快收口阶段一。本地26.7.3已安装并记录二进制/profile哈希；最小人工路网验证4/4通过：2.480 m柴油车可走3.0 m限高短路，3.250 m电动车必须绕行，两车均避开`hgv=no`捷径。

据此，阶段一非算法工作正式关账：MC-002统计规则已冻结，重庆池532及27/27格已通过，九个普通物流设施按“可信身份+可复算WGS84道路接入点+冻结路网可达”闭合，备选调查完成且不替换，固定OSM输入与本地货运路由前置闭合。门禁时段、预约证件、停车/月台和现场真实充电枪不建模、不声称，不再追查。

本轮搜索评价为0；没有建立五省图、九城全矩阵、China81正式实例或启动正式算例。算法优化仍是唯一开放线，阶段二未启动。

## 2026-07-18 后续用户裁决（优先于下方旧“终裁门”）

用户批准 `P1-APP-01` 至 `05`，并明确允许忽略其他论文通常不建模的现场尽调项。车场入口线改用最小充分标准：普通物流设施身份可信、WGS84道路接入点可复算、冻结路网车辆可达。门禁时段、预约证件、停车/月台和现场真实充电枪台账不建模、不声称、不再阻断。

MC-002六项已全部冻结，ES1基础路由尺寸已锁，CV/EV两份隔离货运profile静态2/2通过。当前唯一尚未完成的阶段一基础设施执行项是本地路由运行时：固定OSRM v5.27.1源码在当前AppleClang 16被旧sol2模板兼容性阻断，未安装成功；改用当前可直接安装并重新钉住的OSRM 26.7.3仍须用户批准版本变更。下方“0/9现场完整闭合”和“六项统计终裁”是裁决前历史状态，不再作为当前硬门。

## 已完成

1. 算法线：真正SWAP*完成四实例、三共同种子、两臂、B400的G1a顺序门。24/24任务、12/12共同起点、零硬违规；平均改善`0.003918%`等数值门通过，但RC三对均零触发，未过C/R/RC三类活性覆盖门。判`STOP_TRUE_SWAPSTAR_BEFORE_FULL_G1`，115,200次评价的完整G1未启动。
2. MC-002证据线：算力表、历史耗时锚和功效分析完成，零正式搜索。E7第五臂增量纠正为405任务；五种子下E3/E5/E6为2,835搜索臂。
3. 重庆客户池：305增至532，候选A 27/27格通过；新增身份为可追溯OSM POI，不是观测客户。
4. 固定地图：覆盖九城的五份2026-07-16 PBF 5/5验签；OSRM v5.27.1候选提交已固定。
5. 两备选调查：深圳华南与重庆京东的公开证据边界已闭合到逐项缺口，不支持当前替换。

## 未完成且不能由继续网页检索冒充完成

1. 九个主场址0/9取得“可复算WGS84货车门+外部配送货车准入/时段/预约+同址投运充电硬参数”。11个主/备选点的最小人工输入见证据包`manual_closure_actions.csv`。
2. 本机没有已验签的本地路由运行时；货运profile、EV厢式车外廓宽高、中国货运速度及缺失标签规则均未冻结。MC-004仍不批准。
3. MC-002仍有六项用户终裁：三图估计量、5→7规则、E7第五臂运行时门、算法六臂B1600、实质效应阈值、主要配对检验。
4. 两备选车场是否“不替换”仍须用户明确决定；当前证据不支持替换。

## 建议终裁

1. 批准“三图先各自对共同种子取均值，再在格内对三图取均值，27格各贡献一个值”。
2. 批准每图先5个共同种子；只按不读取结果方向的成对残差方差、失败率和耗时统一补到7；7仍不足即HALT，不继续堆种子追显著性。
3. E7第五臂只条件批准设计，先做预锁小/中/大三格×一图×五种子×五臂的75任务结果隐藏运行时/失败/预算活性门；未过不得启动2,025任务。
4. 算法六臂B1600只批准为阶段二的最大机制门，不自动启动432,000次评价；每个新臂仍须先过0/1/2/5和小规模活性门。
5. 当前不批准拍脑袋的统一实质效应百分比。须先把各机制主要终点和运营上“值得”的最小变化绑定到中国成本/排放/服务合同，再结果盲冻结。
6. 主要配对推断建议采用27个格级差值的配对随机化检验，Wilcoxon符号秩和符号检验作敏感性；五族继续Holm。该项须在正式合同中写明零差、并列和缺失单元处理。
7. 两备选当前均不替换；保留重庆京东为人工闭合优先候选，深圳华南在宗地与持续投运闭合前降级。

## 阶段二硬门

在上述方法终裁、九城人工入口输入、本地货运路由合同和MC-004重新审批完成前，不启动China81正式实例生成、道路全矩阵、Solomon/China正式胜负、E1--E7中国正式搜索或阶段二机制合入。
