# ReSETP 项目看板（2026-07-12 WP0重基线）

总目标：2026-07-31形成可投稿稿件。E2已经封存，当前先完成WP0地基，再按E3→E4/E5→E6→E7推进；研究阶段不把不同实验混成一条线，单个实验批次内部才按资源受控并行。

## 当前统一口径

主场景为280 kWh；80 kWh只保留为稳健性对照。E2只回答“算法统一比赛谁更强”，不重跑810行。完整机制实验使用冻结的`TVCI-ALNS`（兼容机器标识`staged_hybrid_carbon_aware`），4000次完整评价、共同起点、固定种子规则、当前检查器复算。跨场费主值为0，10/25/50/95只是无现实标定的摩擦代理；客户采用冻结的最近车场映射，论文称为合成独立经营基线；配额默认0，只作会计项；公平默认关闭，只在E6/E7公平交互臂从搜索阶段启用。自造算例不报BKS/AVG/Gap%，公开Goeke标准算例另行挂账，使用未经改动的Goeke算法。

## 当前看板

| 工作包 | 状态 | 已知事实 | 下一步 |
|---|---|---|---|
| E2九算法正式比赛 | CLOSED / FROZEN | 810/810行，4000次完整评价，零违规，当前checker和hash复核通过；判决`E2_10SEED_PRIMARY_LEAD_SUPPORTED`，tag `e2-10seed-final-20260712`、提交`1180abf3`。 | 不重跑、不救援；只用于T3/F2和论文算法比较。 |
| E2长预算补充 | CLOSED / SUPPLEMENTARY | 8/8项16000评价通过，判决`E2_16000_PREFLIGHT_SUPPORTED`。 | 只作“4000次并非过短”的补充证据，不并入810行主表。 |
| WP0合同与E0地基 | PASSED / CLOSED | 合同判决`E1_E7_CONTRACT_FROZEN_BY_USER`；c_tr四同步、TeX欠账登记、E0九算例共享起点零违规、E2冻结解复算逐位一致均已留证。 | 不再改合同；进入E3短门。 |
| E3机制阶梯 | READY_AFTER_WP0 | 新口径为M0–M5主梯30次，加0/10/25/50/95摩擦轴25次；旧95收费口径证据不直接沿用。 | 先过200评价接线短门，再跑主梯和摩擦轴。 |
| E2b算法消融 | QUEUED_AFTER_E3 | 普通独立ALNS、强重组、分阶段混合、混合+择时充电四臂；用于界定“组合式创新”边界，不冒充独创ALNS。 | E3验收后按机时决定20或40次。 |
| E4/E5碳与充电 | BLOCKED_BY_E3 | 新故事是“碳价×充电时机”，不再做旧的配额触发油换电故事；固定路线择时复算已有机制证据。 | E3后做时间盲记账、碳价轴、真实波动日复算及必要短搜索。 |
| E6公平 | BLOCKED_BY_E3_E4 | 公平要画“成本—公平代价曲线”，公平从搜索阶段启用；不能沿用旧不绑定网格。 | 复用E3 M4天然公平分布，再围绕绑定区做小网格。 |
| E7动态 | BLOCKED_BY_E3_E4_E6 | 动态是协同、碳、 公平的压力测试；旧动态证据不能直接当新合同正式证据。 | 先过状态继承短门，再做固定事件流四臂。 |
| 表图与论文 | BACKLOG | 07-03样板包是底版，数字必须回溯到CSV、算例、口径、commit。 | 所有正式实验收口后统一生成T3–T9、F1–F7并改正文。 |
| Solomon公开标准算例 | ZERO_SEARCH_GATE_PASSED / WAIT_E7 | 主基准改为DIMACS口径的56个Solomon 100客户VRPTW算例；56/56本地—官方文件一致，BKS/最优标志、节点结构和距离口径零搜索审计通过。CVRPLIB六例作回退。 | E7释放共享内核后，先在非Solomon开发集冻结最终ALNS，再做56例×10种子正式测试与PyVRP/HGS强基线；不得用Solomon调参。 |
| HGS / DR-ALNS | FUTURE_WORK | 本轮投稿不实现、不对比，只在展望中并列说明。 | 不占当前关键路径。 |

## 本轮不做

不重开E2，不改E2冻结证据，不做配额搜索轴，不把95写成现实标定价格，不改cost.py/check.py/evaluation.py，不把E3–E7正式搜索塞进WP0，不做x86迁移。

## 状态规则

每个工作包必须有四件套（metadata、raw_runs、decision、artifact_hashes）和报告；进论文的解必须经当前checker零违规复算。任一验收项失败就停在该工作包，写清原因和最小修复，不带病进入下一个实验。
