# 2026-08-19 全仓深审：今天能否跑出八行图表数字

`STATUS_READ_ONLY_AUDIT_COMPLETE`

## 一、先给是非答案

**结论：今天之内不能证明八行全部跑出来；现有证据反而否定这个保证。**

最卡的不是某一行代码，而是两个上游原因叠加：第一，搜索预算从未按当前算法、当前算例、当前机器标定，昨夜 300 秒把不同单元随机截在 2–5 个周期；第二，八行中动态和公开两批的正式机时仍是 `UNKNOWN`。即使暂时不算动态与公开，只算已批准的碳价/混合车队批和协同/公平批，按每单元跑满 5 周期、安全双并发，也要 **10.0–31.7 小时理想墙钟**，而且尚未包括设计修正、失败重试、产物聚合和验证。

今天能诚实保证的是：把仓库里已有、当前口径仍成立的探索证据整理成 **4 行有数结果**——协同、实体车多趟、成本/参数、公平。它们不是八行最终正式成绩；如果要求“正式十种子、完整五件套、可直接进论文”，今天连这 4 行也不能在开跑前承诺全部完成。

---

## 二、审查口径与证据边界

1. 本轮只读；没有启动求解器、测试或性能探针，没有修改源码、测试、配置或既有实验产物。唯一写入为本报告。
2. 没有打开或读取正在运行的 `solver/reports/cycle_profile_20260819/`。当前版本的真实分阶段耗时，等该会话交付后才能更新。
3. 没有把 `solver/reports/formal_batch_20260818/` 里的结果重新取数；昨夜批次数字只采用本任务已确立事实和已写成的事故复盘。一次委派只读检索的排除规则写错，终端输出意外显示了该目录的少量文本匹配；没有直接打开文件，也没有采用其中任何新数字。这是本轮边界事故，不能隐去。
4. “今天能出数”分两层：
   - **有证据数字**：已有可复核探索产物，能把数字填进对应行并明确写探索性质；
   - **最终正式数字**：按最新用户决定、正式实验合同、完整服务量与五件套重新产出。两者不混写。
5. 本报告中的修复时间是代码阅读后的工程估算，不是用户批准的新门槛；无法从现有测量推出的，一律写 `UNKNOWN`。

---

## 三、按“是否卡住今天出数”排序的问题清单

| 顺序 | 问题 | 原因还是结果 | 证据 | 不处理时今天哪一步走不下去 | 修复/解除所需时间 |
|---:|---|---|---|---|---|
| 1 | 已批准的私有正式批本身已超过今天可保证的安全机时 | **原因** | 碳价批为 7 价点×3 策略×10 种子＝210 单元：`pending_decisions.md:1931-1945`；协同批为单干 20＋联合 10＝30 单元：`execution_blueprint_20260818.md:2271-2280`。按本任务已给实测 60–190 秒/周期，5 周期后两批共 20.0–63.3 机时，双并发为 10.0–31.7 小时 | 时变碳、混合车队、协同、公平四行不能在今天开始前获得“必定完成”证明 | 不是小修能消除；仅运行就需上述时间，且动态/公开尚未计入 |
| 2 | 协同与公平的最新决定和现行正式蓝图冲突 | **原因** | 最新决定：协同搜索必须关闭公平，公平只在协同最优解上事后分摊：`pending_decisions.md:2258-2275`。蓝图却给 joint 传 `--pi0-manifest` 并要求 `fairness_enabled=true`：`execution_blueprint_20260818.md:2397-2416`；源码收到 Pi0 后确实打开公平：`run_problem_hgs_private_technical.py:908-943` | 原样跑完得到的也不是最新口径的协同行和公平行 | 不改内核；修正编排、manifest 和验收并做定向测试，估计 2–4 小时 |
| 3 | 动态实验设计尚未获批，正式机时明确未知 | **原因** | 方案标记 `STATUS_PENDING_USER_APPROVAL`：`dynamic_experiment_redesign_20260819.md:1-4`；正式机时写 `UNKNOWN`：`:130-140`；用户决定要求批准后方可实施：`pending_decisions.md:2253-2255` | 动态行依法不能开跑，也不能给完成时间保证 | 用户批准所需时间不由代码决定；预算标定和正式运行时间 `UNKNOWN` |
| 4 | 公开表有 560 个正式单元，但各题各臂预算未标定 | **原因** | 28 题×10 种子×2 臂＝560：`execution_blueprint_20260818.md:2734-2745`；当前仅两题 10/30 秒摸底，正式胜负 `UNKNOWN`：`deferred_ledger_20260818.md:92-103` | 公开对比行没有可承诺的正式机时，也没有完整同批数字 | `UNKNOWN`；先要各题/各臂收敛标定，再谈 560 单元排产 |
| 5 | 每个候选先做充电修复；变更 EV 车辆日会逐趟修复并重建整份车辆日 | **已证技术原因** | 活跃链：`education.py:242-274` → `charging.py:1701-1818`。同调用链历史实测一圈 122.826 秒，其中充电修复 113.226 秒、增量评价 8.599 秒：`solver/reports/sc10_education_depth_20260816/report.md:7-11` | 私有批每周期 60–190 秒的数量级无法靠调外部超时消失；大量正式单元仍会烧在候选充电修复 | 结构优化加逐位等价验证至少 0.5–1 天；不能作为今天上午保证。当前版本精确占比待现行剖析，仍为 `UNKNOWN` |
| 6 | 候选层把宽泛的 `TypeError/ValueError` 转成普通拒绝，整批仍可能成功 | **原因/可信度风险** | `integrated_private.py:412-445` 捕获后返回 `None`；`education.py:97-134` 还按异常字符串分类，`:180-195` 对 frontier 异常直接继续。顶层异常才会写失败包并重新抛出：`run_problem_hgs_private_technical.py:4918-4929` | 一个实现错误可能表现为“候选不可行很多，但任务跑通”，正式结果的成功状态不够单独证明搜索链健康 | 最小窄化为明确的不可行异常并补定向测试约 2–4 小时；完整类型化改造约 0.5–1 天 |
| 7 | “增量评价”仍重建整解级结构并做整解检查 | **已证技术原因，但不是已证主瓶颈** | 候选合并全部切片并再跑 `prepare_multitrip_solution`、跨场标注：`evaluation.py:885-928`；之后重算全场利润并调用完整 `check_solution`：`:640-679` | 它持续浪费候选时间，但只修它不足以保证今天完成 | 真正把利润、检查和多趟准备增量化至少 1–2 天，且真值一致性风险高 |
| 8 | 当前正式五周期配置的单进程峰值内存没有实测 | **结果（观测缺失），不是慢速原因** | 当前登记明确内存字段缺失：`pending_decisions.md:2181-2188`。最近同实例深跑的峰值 RSS 为 1640.891 MiB、macOS footprint 为 2132.818 MiB：`fix_cost_cache_20260817/stage3_deep_probe/resource_samples.csv:1,13`、`fix_cost_cache_20260817/deep_summary.json:20-36` | 不能安全证明三并发；排产只能按最近实测保守取二并发 | 当前精确值要等现行剖析或独立资源采样，完成时间 `UNKNOWN` |
| 9 | 违规幅度解析失败时静默保留默认值 1.0 | **原因/质量风险** | `_measure_violations()` 先设 `magnitude=1.0`，时间窗、电池、车队等文本解析不到就沿用：`evaluation.py:1022-1078`；该幅度进入每次评价：`:752-770` | checker 文本一旦变化，罚分严重度可能被悄悄扭曲，最后看成搜索不稳或候选差 | 根治需结构化违规量，可能触碰受保护 checker，需另行授权；估计至少 0.5–1 天 |
| 10 | 距离矩阵权威目录缺失时静默换读包内备用目录 | **原因/溯源风险** | `run_problem_hgs_private_technical.py:1131-1140` | 来源路径失效可能被伪装成正常运行；是否在今日目标实例实际触发为 `UNKNOWN` | 改成失败关闭或验证两份矩阵哈希等价，估计 0.5–1 小时 |
| 11 | 昨夜 93 单元只有 30 成功、高碳价三点零成功 | **结果，不是原因** | `carbon_sweep_postmortem_20260819.md:12-25`；本任务已给按完成周期重分组的极差：2 周期 929 元、3 周期 432 元、4 周期 438 元、5 周期 67 元 | 现批不能填时变碳和混合车队最终行 | 不能只补 63 个失败单元；必须让全部所需单元达到统一、已标定的搜索量 |

### 已排除或降级的怀疑

- `depot_assignment_operator.evaluations` 不是完整评价次数，也不是已证主耗时；算术和应用数/评价数关系都已证伪，见 `carbon_sweep_postmortem_20260819.md:50-68`。
- 13.166 毫秒是一次完整评价的实测，不代表一圈实际做了 4500–14000 次完整评价。一个允许读取的同算例旧跑中，474 圈记录 3084 次完整评价和 9152 次增量评价：`calibration_20260818/excluded_fairness_off/seed_3/metadata.json:22030-22045`，合计约 **25.8 次评价调用/圈**。即使把两类全按 13.166 毫秒高估，总计也只有约 161 秒，占该跑 3604.6 秒约 **4.47%**；多出来的时间主要落在候选处理，已知历史主项是充电修复，而当前精确占比待现行剖析。
- 活跃 `problem_hgs` 路径未检出逐候选 `deepcopy`；距离矩阵在 bundle 载入时读一次：`solver/src/setp_solver/search/bundle.py:45-80`。没有证据支持“每候选重算距离矩阵”。
- 停机时和最终封包会各做一次完整冷评价：`education.py:474-484,741-759`、`runner.py:627-645`。这是少量结果性重复，不是 60–190 秒/圈的主因。
- 充电修复里的部分 `except ...: pass` 会继续尝试其他策略或最终重新抛出，并非所有捕获都会伪成功；真正危险的是上表所列候选层宽泛捕获。

---

## 四、八行图表逐行体检

| 行名 | 当前能不能出数 | 缺什么 | 技术还是设计问题 | 今天能否补上 |
|---|---|---|---|---|
| 1. 时变碳 | **不能出最终数**；已有一粒种子择时信号和残缺扫描 | 7 价点×3 策略×10 种子的统一满周期结果；高碳价点当前零成功 | 预算/性能；作用条件并非不存在 | **不能保证**。与混合车队共用 210 单元，单这一批双并发就要 8.75–27.71 小时理想墙钟 |
| 2. 混合车队 | **不能出最终数**；油电车型都能进入解，但同配置车队在 1CV/8EV 到 6CV/3EV 间乱跳 | 同一完整碳价批下稳定、可配对的车型构成 | 预算/性能，不是车型入口缺失 | **不能保证**；与第 1 行同批，不重复计机时 |
| 3. 多企业协同 | **已有探索数**：单干之和 4484.059549、联合 3540.092121、节约 943.967428（21.05%） | 正式口径要把联合搜索改为 fairness-off，再跑 10 种子 | **设计合同冲突＋正式批缺失** | 探索数今天可整理；正式数**不能预先保证** |
| 4. 实体车多趟 | **已有探索读数**，正式值需从获批正式解汇总 | 正式解中的实体车、趟数和衔接分布 | 无独立设计缺口；依赖上游批 | 探索数今天可整理；正式值随第 1/3 行产生，增量机时 0 |
| 5. 成本分项/参数/算例特征 | **能出探索/静态参数数值** | 若要最终结果分解，需要最终选定正式解 | 无独立设计缺口 | 今天可整理；正式结果分解随第 1/3 行产生，增量机时 0 |
| 6. 动态需求 | **不能出** | 用户批准三臂设计、每决策点预算标定、正式配对结果 | **设计未批准＋预算未知** | **不能保证**。事件流 10 个新增单全可直接服务，最紧仍有 3.44 小时裕度，效应只能来自合单、换车、改顺序、少开车：`dynamic_experiment_redesign_20260819.md:41-53` |
| 7. 公平分配 | **已有探索数**：事后分摊后两家各 +471.983714 | 必须对 fairness-off 的正式协同赢家事后分摊；旧 fairness-on 搜索不能继续当主线 | **现行正式编排与最新决定冲突** | 探索数今天可整理；正式数要随修正后的协同批产生，不能预先保证 |
| 8. 公开对比 | **不能出最终数**；仅 2/28 题短探针 | 28 题×10 种子×2 臂同批、同服务量、各题各臂已标定预算 | 规模/预算 | **今天不能承诺**；正式机时为 `UNKNOWN` |

### 作用条件与对照组复核

- **时变碳**：价格—碳强度错位存在；已有同一路线下充电时机改变电费与充电排放的证据。当前问题是搜索量不足，不是机制通道为零。
- **混合车队**：油车和电车都能进入解，作用载体存在；当前不能把未收敛导致的车型乱跳写成价格效应。
- **协同**：旧“关掉协同”只是不再继续改跨场归属，仍有 24/50 跨场客户，不能作为无协同对照：`deferred_ledger_20260818.md:105-110`。最新正确对照是两家真实单干成本之和与 fairness-off 联合最优。
- **公平**：现有无分摊时企业甲拒绝合作、分摊后两家均为正，作用条件存在：`CURRENT_PROJECT_CONTEXT.md:2790-2815`。但现行蓝图把公平约束放进联合搜索，会把协同效率和公平分配重新混在一起。
- **动态**：没有“紧急订单救不回来”的场景条件；若有效应，只能通过合单、换车、改顺序和少开车。不能预设动态必胜全信息静态；现有方案自己对 D 胜 M 把握为中、D 胜全信息 S 把握为低：`dynamic_experiment_redesign_20260819.md:151-160`。
- **非线性充电**：最高 SOC 45.531% 低于 85% 拐点，按最新用户决定，这是算法在拐点前停止充电的正面观察，不再判为“作用条件不存在”：`pending_decisions.md:2236-2248`。但当前直接证据只证明停在拐点前；“因为识别边际收益而主动这样选”的因果表述仍需反事实对照。

### 协同与公平在现有实现中的实际关系

- 普通联合效率上下文和单企业上下文都能明确关闭公平：`run_problem_hgs_private_technical.py:1507-1525,3382-3397`；完整 checker 也按 `fairness_enabled` 开关：`evaluation.py:668-679`。所以不需要改受保护评价器。
- 但现行蓝图显式传 Pi0，runner 因此把公平打开；旧公平探针还把 `fairness_enabled=true, theta=1` 当验收条件：`build_fairness_line_probe.py:330-367`。沿这条入口跑会得到错误实验。
- 事后两企业 Shapley 适配器已经存在并做总成本、利润闭合：`solver/scripts/coalition_accounting_adapter.py:80-152`。不过当前独立输出脚本仍自标 `run_kind=probe, formal_reuse_allowed=false`：`run_two_enterprise_allocation.py:32-67`；正式产物合同尚未按 P124 更新。
- 提案层仍有串味：一旦打开跨场，候选生成同时放行 `depot_collaboration` 和 `fairness_cross_depot`：`proposals.py:157-185`。在 fairness-off 时它不改变 checker 约束，但会留下额外候选工作和错误命名风险；当前没有证据证明它是主耗时。

---

## 五、机时排产的地基

### 5.1 单进程内存峰值与并行数

| 项目 | 数值 | 口径 |
|---|---:|---|
| 最近同实例深跑峰值 RSS | **1640.891 MiB** | `fix_cost_cache_20260817/stage3_deep_probe/resource_samples.csv:1,13` |
| 最近同实例深跑峰值 physical footprint | **2132.818 MiB** | `fix_cost_cache_20260817/deep_summary.json:20-36` |
| 当前五周期正式配置峰值 | **`UNKNOWN`** | 当前批未记录内存；不得把 8 月 17 日配置冒充今天配置 |
| 8 GB 机器建议并行 | **2 个求解进程** | 2×2132.818 MiB≈4.17 GiB；3×≈6.25 GiB，只余约 1.75 GiB 给 macOS、两个 AI 会话、文件缓存与聚合。三并发不能称为安全保证 |

并行数 2 是基于最近实测的资源算术，不是新造科学门槛。当前另一会话正在做剖析，在它结束前本轮不建议再启动求解进程。

### 5.2 按“每单元跑满 5 周期”的机时

本任务给定实测周期范围为 **60–190 秒**，因此每单元 5 周期为 **300–950 秒＝0.0833–0.2639 机时**。事故复盘中的一条具体轨迹为 130–190 秒/周期，但排产采用用户给出的完整 60–190 秒范围，不擅自抬高下界。

| 供应的图表行 | 正式求解单元 | 5 周期机器时间 | 安全双并发理想墙钟 | 可否用此数保证今天完成 |
|---|---:|---:|---:|---|
| 时变碳＋混合车队 | 210 | **17.50–55.42 h** | **8.75–27.71 h** | 否；上界已越过今天，且不含失败重试 |
| 协同＋公平 | 30 | **2.50–7.92 h** | **1.25–3.96 h** | 运行时间本身可能容纳，但先有 2–4 h 的合同修正和验证，仍不能保证 |
| 多趟＋成本分项 | 从前两批解汇总 | **增量 0** | **增量 0** | 依赖上游正式批合格 |
| 动态 | 三臂×10＝30 个最终结果，内部约 6 个决策批 | **`UNKNOWN`** | **`UNKNOWN`** | 否；不能机械套用静态 5 周期 |
| 公开表 | 560 | **`UNKNOWN`** | **`UNKNOWN`** | 否；各题各臂预算未标定 |

只算前两项，合计 **20.00–63.33 机时**，安全双并发理想墙钟 **10.00–31.67 小时**。这已经足以否定“今天八行一定完成”，无需靠乐观的平均值下注。

昨夜实际预注册 93 单元并不等于已批准的 210 单元合同。即使只把 93 单元全跑满 5 周期，也要 **7.75–24.54 机时**、双并发 **3.88–12.27 小时**；而已有 30 个成功单元也并非全都跑满 5 周期，所以不能只补 63 个失败单元冒充统一搜索量。

---

## 六、独立判断：今天到底该承诺什么

### 判断

**不可行：今天之内把八行最终正式数字全部跑出来，当前没有可证明的方案。**

证明链很短：

1. 已知部分的安全双并发上界已经是 31.67 小时；
2. 动态与公开两批机时仍为 `UNKNOWN`，其中动态还没有获批；
3. 协同正式编排违反最新“协同与公平分开”的决定，原样跑完也不能交付；
4. 当前版本的主要候选耗时占比与当前峰值内存仍在另一会话测量，不能在测量回来前许诺提速倍率或三并发；
5. 候选层宽泛异常捕获意味着“批次完成”还不能自动排除实现错误被记成普通拒绝。

### 今天最多能拿到哪几行

**能保证整理出有证据探索数字的上限是 4 行：协同、实体车多趟、成本/参数、公平。**

- 协同已有 21.05% 探索节约；
- 公平已有事后分摊后双方各 +471.983714；
- 多趟与成本/参数可从已有合格探索解和静态输入汇总；
- 这 4 行必须醒目标为探索/现有证据，不能改名为最终正式结果。

时变碳、混合车队、动态、公开四行今天都不能给“必成”承诺：前两行受 210 单元与满周期约束；动态未批且机时未知；公开有 560 单元且预算未知。

### 为换取确定性，应该砍什么

这是代理建议，不是替用户决定：

- **今天不再承诺八行齐全**；
- **不在今天启动动态正式批和公开 560 单元批**；
- **不做高风险的完整增量评价重写**，它至少 1–2 天且不是已证主瓶颈；
- **不把三并发写进排产**；
- 先把 4 行已有证据锁住。等现行剖析回来后，只对“碳价/混合车队共享批”与“修正后的协同/公平共享批”重新算可完成量，不再用 300 秒或只补失败单元的方式赌运气。

### 现在只需要用户决定一件事

- **A（建议）**：接受“今天保住 4 行有证据探索数字；动态和公开不承诺今日；现行剖析回来后再决定是否用剩余时间争取碳价/混合车队两行”。
- **B**：仍要求今天交八行最终正式数字。现有证据下必须同时接受：代理不能保证完成，而且动态未批、公开预算未知、已知批次上界超过今天。

---

## 七、没有查完或不能在本轮证明的内容

- 当前正在跑的单周期剖析结果：按铁边界未读，故现版本各函数精确耗时占比为 `UNKNOWN`。
- 当前正式五周期配置的单进程峰值内存：没有现成测量，故为 `UNKNOWN`。
- 动态正式机时、公开 560 单元正式机时：预算未标定，故为 `UNKNOWN`。
- 每个冻结文件内部剩余 10 条静态命中的逐条复核：本任务已确立它们都在冻结文件内，本轮没有越界重查或修改。
- 现有 4 行探索数能否直接升级为论文最终数：不能；当前事实源明确公平探索包 `formal_reuse_allowed=false`，正式批仍未完成。

本审查的停止点是回答“今天能不能保证八行出数”并给出可复核的原因链。答案已经由已知机时、两个未标定批次和一个正式设计冲突共同确定；继续扩大静态审计不会把这个“不保证”改成“保证”。
