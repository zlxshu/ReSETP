# 施工方案：从当前状态到正式填数（2026-08-18，v1）

`STATUS: SUPERSEDED_BY_BLUEPRINT`——用户 P101 令：施工方案须"乐高说明书级"（接口/函数/变量级），
只呈终稿。本骨架降级为参考；正式呈批对象＝`execution_blueprint_20260818.md`（制作中）。
本骨架的第一轮审查判 `PLAN_FAIL`（7 条，Claude 逐条自裁后全部采纳，其中图 5 需扫到 2.5 元/kg
与 vendor +351 行未归类两条已亲核）；7 条全部转为蓝图的强制审查项：
①适配件恢复逐件报批②W1.3 拆分四角色＋公共站归属须明写＋F2 严格顺序③三组件狩猎提前到
S5 前、通用搜索适配补项、vendor 差异先归类再建 manifest④F1 跑量改 UNKNOWN、
碳价格点按 P102 重定（文献＋新鲜测量推导，旧 2.27/1.223 降为待重测参考；S5 拍板冻结）、三臂语义列为用户按键点、
混合车队不等 P28⑤P11 移到正式批前⑥W1.2 验收改有限合同⑦"速度承诺"改"进度估算"。

**方案依据**：P100 四决（1A 规矩全批／2A 适配件准写／3B 双目标低优先级／4 公平先保守后上层）
＋公平适配令（不得调退役 ALNS）＋开源检索扩界（GitHub＋文献）＋速度令（最短出成果路径）。
**排程单位**：一轮＝一次 Codex 施工会话＋Claude 逐项复核。实测锚：walk_first 一轮 40 分钟
完成净删 947 行＋多件组装＋228 回归；带五类测试的适配件估 1–2 轮/件（估算）。

---

## 〇、总路径（一张图）

```
W0 闸门与出处（1–2 轮，不改算法语义）
   ├─ W1 四适配件（依赖序，3–5 轮）        ─┐
   └─ W2.1 效应算术账（纸面，并行）          ├→ W2.2 收敛标定（机器数小时）
                                            │       ↓
                                            │   S5 拍板（用户，带实测数字）
                                            │       ↓
                                            └→ W3 正式电池（10 种子，fail-closed）→ 填表
主线数据落袋后 → W4 第二波（公平上层／双目标附属 3B／三算法组件开源狩猎）
```

**速度承诺口径**：方案批准后 24–48 小时内到达 S5 拍板点（估算，前提是适配件不遇深坑）；
其后正式电池机时由 S5 标定结果决定（当前 UNKNOWN，见 W2.2）。"三天开始填数"是目标不是承诺。

---

## 一、W0　闸门与出处（Phase 0，不改任何算法语义）

### W0.1　机器闸门组装（1 轮）
- **内容**：按施工规矩§四组装九条闸门。工具全现成：pre-commit＋ruff（C90/PLR/SIM/BLE/TRY）＋
  import-linter＋vulture＋semgrep 规则（id() 键／模块级可变缓存／语义正则／宽捕异常降级）＋
  短 AST 检查（行数、复杂度）。入口一个脚本，前六条进每次提交。
- **验收**：①用仓库已记录的历史病样本造 fixture（id 键缓存、正则罚值、零消费者枚举、
  模块缓存），闸门必须全部标红；②对现仓跑一遍，产出"存量红名单"**只登记不立即修**
  （分批清理不挡实验）；③闸门自身代码也过闸门。
- **净行数预算**：闸门配置与脚本为新增（豁免区，属编排脚本角色）；生产代码净增 0。

### W0.2　third_party 出处清单＋三件搬出（1 轮，与 W0.1 可同轮）
- **内容**：`third_party/` 建逐文件 SHA-256 manifest；`IntegratedGeneticAlgorithm/
  ExternalPopulation/HGSControl.py` 三件移出 third_party 至 problem_hgs 侧，文件头登记
  「项目拥有、提交 15ea99190 首现、作者 UNKNOWN」；import 路径随改。
- **验收**：manifest 检查通过；回归 228 过；五冻结哈希不变；私有静态＋公开单题冒烟过
  （均须满服务）。
- **净行数预算**：净增 ≤0（只挪不写）。

### W0.3　探索包字样分离（半轮，顺手）
- **内容**：`run_kind=probe` 的包 verdict 不得再用 `FORMAL_RUN_COMPLETE` 字样（挂账 L9）。
  **只改标识，不改任何判据严格度**；fail-closed 行为不变。
- **验收**：故障注入测试证明失败仍 exit 2；probe/正式两类包字样可区分。

---

## 二、W1　四适配件（Phase 1，依赖序施工；每件受施工规矩硬规格约束）

> 本方案获批＝W1.1–W1.3 三件的规格预批（免逐件往返，兑现速度令）；
> 每件合并前仍产出工具自动生成的验收单供你查看。W1.4 因涉冻结文件单独请示。
> 每件的派工信带净行数预算与「现成件出处」节；查得到现成的照搬，查不到的部分
> 属项目真值薄层（这四件正是 walk_first 判定"无现成货、须适配件"的四件）。

### W1.1　动态在途未来充电分类（1 轮）
- **规格**：输入＝切换点证书＋路线附着的充电动作；输出＝切换时点后在途趟的未来充电处置
  与正确的初始电量基数。计算全部复用现有充电核，本件只做分类与接线（项目状态补充）。
- **验收**：性质测试（SOC 连续、服务量守恒、失败原样传播）；动态两臂冒烟满服务；
  与既有行为的差异逐项列明（改变了哪些包的哪些数）。

### W1.2　统一执行身份（1 轮）
- **规格**：单一不可变配置对象由 runner 持有，哈希＝实际生效的引擎／策略／开关全集；
  复用 copied SolveParams 的不可变模式。
- **验收**：性质测试"行为相关配置变 ⇔ 哈希变"；注入探针证明记录身份＝执行身份；回归过。

### W1.3　单干入口＋企业账＋参与约束接线（1–2 轮，最大件；兑现 P100 公平适配令）
- **规格**：**数据侧**构造两个单企业子算例——客户按已冻结企业归属取本企业、车队与车场取
  本企业、V3 合同全保留；**求解侧用当前 Problem-HGS 原样跑**（不得调退役 ALNS，旧
  `search/fairness.py` 入口标废）。产出每企业成本／利润账＋单干基准清单；随后把联合臂的
  公平开关接真实基准值（参与约束进搜索＝你已定的 P63-2 下层），废除中性占位 1.0。
- **无跨企业服务的保证方式**：按构造保证（子算例里只有本企业的车和客户），**不设数值门**
  （P98）；实际跨企业计数照报（应为 0，若非 0 即构造缺陷如实报）。
- **验收**：两企业各自满服务（各自客户数／需求量全量完成）；联合臂公平开启后跑通并报
  参与约束余量；真"无协同"对照＝两单干之和落盘，**旧伪关闭读数 3506.007032 登记作废**。
- **本件同时解锁**：公平主表数据源、真协同对照、混合车队入口所需的基准清单（挂账 L8 关闭）。

### W1.4　违规量化（gated，规格先行）
- **规格**：正确改法在冻结 `check.py` 的 Violation 增加结构化数值，evaluator 弃用英文正则。
  **动 check.py 前单独请你按键**；批准前正则路径原样保留（不影响 W3 开跑，
  只影响罚值精度的一个已知缺陷面）。

---

## 三、W2　实验准备（W2.1 与 W1 并行；W2.2 在 W0+W1 合并后）

### W2.1　三大实验效应算术账（纸面，半轮）
- 时变碳：在当前算例上重算充电量×强度极差×价的上限，并复核 1.472／1.223 两锚点的适用前提；
- 混合车队：溢价 50 元窗口 vs 当前算例路线长度分布；
- 动态：事件流的可行裕度分布。
- **碳价扫描格点提案（本方案获批即冻结）**：{0.075, 0.3, 0.6, 0.9, 1.223, 1.472, 1.5} 七点
  ——落在你已批的 0.075→1.5 情景区间（P44 第 3 款），两锚点显式在列。

### W2.2　收敛标定 → S5 提案（机器数小时＋分析半轮）
- 在合并后的最终代码上，统一算例少量种子长观测，按你定的判据（P79：曲线走出经典形态）
  读收敛点；产出 **S5 提案**交你拍板：真值边界默认开/关、每圈候选上限 ε、正式每跑预算、
  动态每决策点预算（W2.3 并入）。正式总机时在此后才可计算。

---

## 四、W3　正式电池（S5 拍板后开跑；全部 10 种子、fail-closed、五件套落 `solver/reports/`）

| 批次 | 内容 | 跑量公式 | 服务的图表行 |
|---|---|---|---|
| F1 | 时变碳×混合车队同批：asap／cost_min 各 10 种子（价无关）＋碳感知×7 价点×10 种子 | 20＋70＝90 跑 | 时变碳三尺度图＋混合车队 CV/EV-碳价图（同批两用） |
| F2 | 公平主表：单干 2 企业×10 种子（取最好冻结为基准）＋联合 10 种子 | 30 跑 | 公平表（无分摊行＝路线归属记账[你已定 P63-3]；有分摊行＝**固定分法提案："两方标准解"**——Shapley/核仁/τ/Gately 两方同值，报作"标准解"，本方案获批即冻结此选择）＋参与约束判定；协同对照同批出 |
| F3 | 动态配对：滚动 vs 机械在线 | 2×10＝20 跑 | 动态表 |
| F4 | 多趟／多车场／非线性／成本分项 | **0 跑**——从 F1/F2 正式解直接汇总 | 其余四行 |
| F5 | 公开正式批（最后、夜间连挂）：28 题×10 种子×本文臂＋母体同批回归 | 560 跑×2 臂预算 | 公开表＋"不输母体"内部回归 |
| F6 | 回填表格壳 | — | 全部 |

每包必报完成客户数与需求量；探索期读数一律不混入（包身份经 W0.3 分离）。

---

## 五、W4　第二波（主线数据落袋后，按需排入）

1. **公平上层**（你已定"跑通后加入"）：先按 P63-2 冻结退回阈值（届时给你选择题），
   再开上层与四车场分法选择实验。
2. **双目标附属分析**（3B，低优先级）：若故事或启示需要，先审计既有双目标实现的血统
   与正确性，再排入。
3. **三算法组件开源狩猎**（GitHub＋文献，P100 扩界）：第二父代车队遗传／共享充电桩调度／
   非负代理候选救回。每件产出＝检索报告＋可核人类来源或"未找到"证据；
   找到→按适配件规矩提案接入，找不到→保持缺件如实记录。

---

## 六、你的按键点（全部列明，无隐藏）

| # | 时点 | 内容 |
|---|---|---|
| K1 | 现在 | **批准本方案**（＝同时预批 W1.1–W1.3 规格、碳价七点、公平固定分法"标准解"） |
| K2 | W1.4 前 | 动冻结 `check.py` 的单独授权 |
| K3 | W2.2 后 | **S5 拍板**（真值边界开/关＋ε＋正式预算＋动态每决策点预算，带实测数字来） |
| K4 | W4.1 前 | 公平上层退回阈值（选择题） |
| K5 | 写论文结果前 | P21（集合划分去留）、P11（入文数值门槛） |

## 七、风险与诚实说明

- 适配件遇未知深坑→顺延并如实报告（不硬撑、不放宽判据）；
- 公开正式批机时未知，待 S5；若标定出的预算过大，F5 拆多夜连挂；
- W2.2 若曲线迟迟不出经典形态，说明算法仍有结构问题——停下报告，不假装收敛；
- 所有耗时除注明"实测锚"外均为估算；
- 本方案执行期间发现的新问题默认进挂账台账（除非挡数据），不开新战线。
