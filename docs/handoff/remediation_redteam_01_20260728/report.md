# REMEDIATION-REDTEAM-01 可执行整改表

任务编号：`REMEDIATION-REDTEAM-01`  
日期：2026-07-28  
红队来源：`docs/handoff/redteam_reviewer_01_20260728/report.md`  
执行边界：本轮只读核验并生成本文件；未修改论文、源码或实验产物，未运行 solver、benchmark、pytest、进程池、XeLaTeX 或图表生成，也未读取或改动 `baselines/e2_rerun_unified_01_20260727/`。

## 口径

红队报告正文列出 101 个细分编号，但只在末尾“按严重程度排序的问题清单”中对 28 个聚合问题明确给出“致命/大修/小修”判定。为避免自行发明严重级别，下面以这 28 个已评级问题为主键；括号中的红队细分编号用于追溯其覆盖范围。

状态只使用“未开始 / 进行中 / 已修复”。“进行中”不代表已取得方向性结果；本表中与 China81 洁净批有关的“进行中”仅依据本任务给定事实和当前 handoff 记录，未在本轮读取受保护运行目录。“已修复”只标记用户指定且已在当前稿源中逐项核实的三项。对于集合划分按路线条数计车的问题，本次按已明确接受的最低文字修复口径关闭“未披露近似”这一缺口；SP 等价性和偏差审计仍由 RR-05 阻断。

## 可执行整改表

| 问题编号与一句话描述 | 严重级别 | 最低修复动作 | 依赖关系 | 关闭条件 | 当前状态 |
|---|---|---|---|---|---|
| RR-01（1.9、7.2）：主稿曾接入明示作废的 China81 格式干跑表。 | 致命 | 保持 `docs/paper_v2/paper_main.tex` 的 `tab:china81-summary` 只引用可用的封存表；洁净批封存后，再由权威原始行生成新表替换。提交前增加主稿对 `*FORMAT_DRY_RUN_INVALID.tex` 的零引用检查。 | 恢复封存表无依赖；换成新五臂表必须等 E2 洁净批完成、独立审计和表源封存。 | `paper_main.tex` 中 `FORMAT_DRY_RUN_INVALID` 命中为 0，活动 `\input` 指向封存表或后来封存的新表；作废干跑文件即使保留归档也不能进入编译依赖。 | 已修复；当前 `paper_main.tex:1241` 已恢复引用 `corrected_china81_rerun_v4_20260724/artifacts/table8b_china81_summary.tex`，全文无 `FORMAT_DRY_RUN_INVALID` 引用。 |
| RR-02（1.10、4.4、7.12）：China81 车型、柴油价、迎风面积、最终解和表图仍属于旧模型。 | 致命 | E2 洁净批封存后，同步改 `paper_main.tex` 的“实验设计”车型/价格段、表 `tab:vehicle-parameters`、“最终解分析”、`tab:china81-summary` 及相邻结论；所有数值只从新批 `metadata.json`、`raw_runs.csv`、`decision.json`、`artifact_hashes.json`、`report.md` 和新路线证书生成，旧 140.41 kWh / 1000 kg / 3300 kg 结果仅能明确标作历史而不得混入当前结论。 | 等 E2 洁净五臂批达到终点、零违约独立复算、产物哈希闭合；在此之前不得回填方向性数字。 | 新批 2025 个计划单元全部具有终态且通过合同审计；论文活动参数与逐任务元数据一致；当前结果段、最终解表和图中不存在未标作历史的旧车型、旧柴油价或旧迎风面积数值。 | 进行中；用户已明确洁净批正在运行，本轮未读取其目录或中间结果。 |
| RR-03（1.3--1.7、6.13）：公开表把已被本地搜索突破的旧值继续称为“当前 BKS”。 | 致命 | 在公共基准证据包中建立 28 题逐题版本清单，至少分列“社区公开 BKS”“PyVRP 本地目标”“MV 固定协议复现”“新增本地可行候选”，记录来源、日期、证书、哈希和可公开身份；据冻结清单整体重算 `paper_main.tex` 的表 `tab:v13-results`、平均误差、胜题数、排序、摘要和结论，并把五题未复现的解释降为诊断性陈述，除非另有单因素反事实。 | 先冻结四类身份及认证规则；不依赖 China81 洁净批，但依赖公开候选证书和基准清单完成复核。 | 28/28 题均有唯一、版本化分母；表中全部 gap 可由清单逐项重算；正文不再同时出现“当前 BKS”与已突破却未回填的矛盾身份；13/18 和五个失败题在摘要、正文、结论口径一致。 | 未开始。 |
| RR-04（2.1）：电耗公式曾以 J 直接进入 kWh 电池状态约束。 | 致命 | 在 `paper_main.tex` 的车辆能耗模型中，对弧段电耗式和常速线性退化式显式加入 `3.6\times10^6` 换算，并写清输出单位。 | 无。 | 电耗式含 `1/(3.6\times10^6)`；线性系数同步含同一换算；正文明确 `e_{ij}`、`B`、`Y`、`\varepsilon` 均为 kWh。 | 已修复；证据为 `paper_main.tex:409-425`，式 `eq:electricity` 与 `eq:linear_energy` 均已显式换算并写明单位闭合。 |
| RR-05（2.15、2.16）：路线级成本不可加，当前 SP 又遗漏实体车、充电并发和参与约束，却被写成第二层等价显式形式。 | 致命 | 修改 `paper_main.tex` 算法步骤5及式 `eq:sp-recombination` 前后表述：在未证明列成本可加前，统一称“代理路线池重组启发式”，删除“完整成本”“第二层等价显式形式”等过强说法；另形成 `sp_proxy_bias_audit` 产物，逐方案比较代理目标、完整补全目标、被完整检查拒绝的原因及规模/车型/车场分层偏差。若坚持等价主问题，则必须改用实体车排班模式列并纳入趟链、共享桩、时序和参与约束。 | 必须先冻结新车型后的完整模型与洁净 witness；若需要改变求解语义，须先获用户批准并判定是否使在跑 E2 批失效。 | 二选一客观闭合：A）正文全程明确“代理”，偏差审计给出预登记样本、逐项误差与拒绝率，所有采用解通过完整检查；B）模式列主问题包含红队列出的关键耦合约束，并由小规模枚举证明与完整模型可行域/目标一致。 | 未开始。 |
| RR-06（2.17、2.18）：SP 按路线条数计车辆数的保守性此前缺少明确治理口径。 | 致命 | 在 `paper_main.tex` 式 `eq:sp-recombination` 后明确写出：约束按所选路线条数计数、实体车可多趟、该做法是可能排除实际可行组合的保守近似、只作为搜索范围限制，最终方案仍需完整补全复算与独立验解。更强的等价性和偏差量化归入 RR-05。 | 无。 | 正文同时出现上述四个客观要素，且不把该路线计数约束表述为实体车数的精确等价计数。 | 已修复；证据为 `paper_main.tex:756-760`，已明确“按路线条数计数”“实体车多趟”“保守近似”“可能排除部分实际可行组合”及最终完整验解。 |
| RR-07（1.1、1.2、1.5、1.14、7.1、7.3、7.13）：摘要、E3--E7、讨论和结论仍含空表、占位符及无结果的一般化主张。 | 致命 | 等五组机制实验各自封存后，仅由其权威 raw、独立复算证书和生成器替换 `paper_main.tex` 中文/英文摘要的 `DATA_PLACEHOLDER`、第4节 E3--E7 空表/占位图、讨论及结语；任何不成立的机制按预注册降级路径收缩，不补写“普遍规律”或“政策价值”。 | E2 洁净批释放算力；E3--E7 的方法审批、执行合同、五记录面、独立复算和表图源全部完成。 | 活动稿中 `DATA_PLACEHOLDER`、`待实验数据生成`、`待封存数据生成` 和结果表中的 `---` 占位计数均为 0；每个摘要/讨论/结论数字可反查唯一封存产物和哈希；五组实验缺一项即不关闭。 | 未开始。 |
| RR-08（1.11--1.13、3.10、6.7）：O 与旧四臂非同批、非同模型和非同停机合同，却被用于“总体优于”和机制阶梯。 | 致命 | 完成当前同批五臂洁净运行；从同一新车型/价格模型、同一完成器与评分器、同一基础种子派生规则和同一环境的原始行生成五臂表。预算按已冻结的收敛式合同执行并逐臂披露 CPU/墙钟，不再拼接旧 O 或旧 F/E/M/MV 行；`paper_main.tex` 相关表注、胜平负、阶梯和结论全部由新表自动生成。 | 等 E2 洁净批完成、饥饿规则处理结束、独立验解和哈希封存。 | 2025 个单元全部来自一个合同/环境谱系；逐行具有 start/seed/arm/CPU/wall/stop metadata；旧批行未进入表源；正文只作收敛式质量比较，不声称等计算量因果优势。 | 进行中；当前洁净五臂批正在运行，尚无可用终局结论。 |
| RR-09（2.2--2.9）：趟到车辆映射、油耗量纲、载重/电量守恒、合法充电位置、跨场转场和连续并发约束不闭合。 | 大修 | 在 `paper_main.tex`“模型建立”中补全 `r\mapsto k` 量词及车型参数域、`\beta_0^g/\beta_1^g` 单位、载重与电量传播的成对上下界或等式、允许充电节点/访问/停留/SOC 联立、异车场相邻趟的转场时间/距离/能耗、`\Omega_k` 的可验证生成条件，并说明半小时槽近似相对连续并发的误差方向；建立小规模精确模型或独立枚举证书与检查器逐项对照。 | 先判断是论文漏写还是正式模型语义改变；任何语义改变须用户批准，且不能在 E2 洁净批运行中静默切换。 | 每个式子的索引和单位有定义；人工小例的可行解集合、目标和违规类型在论文模型、实现及独立枚举间一致；槽离散误差方向和至少一个粒度复核被记录；若语义改变，受影响实验已统一重跑。 | 未开始。 |
| RR-10（2.12--2.14）：动态取消/改量、车辆当前状态和冻结前缀没有进入数学状态转移。 | 大修 | 改 `paper_main.tex`“动态需求处理”：定义 `W` 对新增、取消、改量的计量规则，给既有客户 `q_i`/服务时长/时间窗的更新方程，把 `o_k^\tau,t_k^\tau,\ell_k^\tau,\varepsilon_k^\tau` 接入下一阶段起点与资源约束，并形式化冻结前缀；E7 产出逐事件客户守恒、车辆连续性和前缀不可撤回证书。 | 动态方法需先获批准；E2 完成后才能进入 E7 正式执行；若方程改变现有动态实现，须冻结新版本后重跑 E7。 | 新增/取消/改量三类事件各有至少一个可复算证书；每阶段客户守恒、车辆位置/时间/载重/SOC 连续、已执行前缀不变，检查失败数为 0；论文方程与证书字段一一对应。 | 未开始。 |
| RR-11（2.10）：系统碳配额成本与成员收益没有统一的配额会计分摊。 | 大修 | 在 `paper_main.tex` 的 `F_1`、`eq:stage_profit` 和参与约束说明中冻结唯一配额分摊规则；同步登记模型变更，并生成系统账与成员账恒等式审计。若配额只作系统常数平移，也必须明确成员收益不据此作参与因果解释。 | 需要用户批准配额分摊/会计语义；E6 参与实验必须等该规则冻结。 | 对每个测试解，成员收益合计与系统收入减全部系统成本（含配额交易）在预设数值容差内一致；规则写入参数/方法登记；若改变参与可行域，E6/E7 受影响结果已重跑。 | 未开始。 |
| RR-12（2.11）：按配送趟计费的固定成本却被解释为折旧、保险和日工资。 | 大修 | 在 `paper_main.tex` `F_2` 段二选一：A）改称有来源的“每趟启动/派遣成本”，删除按实体车日发生的成本构成；B）改为按实体车/运营日计费，并同步模型、代码、审批和受影响实验。若管理结论依赖该口径，补按趟与按实体车/日的敏感性产物。 | 先由用户批准经济语义；选 B 会改变搜索目标，必须等待当前 E2 洁净批结束并重跑全部受影响证据。 | 正文成本名称、公式计费基数、代码账本和来源完全一致；敏感性表能复算且明确主要结论是否翻转；不得再出现“按趟计费的折旧/保险/日工资”组合。 | 未开始。 |
| RR-13（2.19、2.20、5.8）：逐会话充电择时只最小化碳且未协调共享桩容量。 | 大修 | 修改 `paper_main.tex` `eq:charging-rule` 及算法说明：若作为主算法补全，建立电费加碳价的联合目标并对同站会话做共享容量协调；若不实施，则把现规则限定为固定路线/车辆/电量后的零搜索排放情景，不声称总目标最优。形成带站点容量的微例最优性/可行性证书，并与 `frvcpy` 等最近邻方法说明边界。 | 先冻结该子问题的角色和审批；主算法语义改变会影响 E2/E4/E5/E7，须在当前洁净批之后统一处理。 | 主算法路径下，所有会话共同优化后电费+碳成本不劣于基线且站点并发零违规；或情景路径下，正文、摘要和结论均不含“总目标最优/算法补全”主张，并有同路线同电量复算证书。 | 未开始。 |
| RR-14（3.1、3.2、3.4、5.6）：公开与 China81 对照的计算预算和主张边界未统一。 | 大修 | 按当前 Claude 裁决，不执行红队提出的“必须等 CPU”硬门；采用收敛式预算，并在 `paper_main.tex` 公开表、China81 表及实验设置中逐算法并列 CPU、墙钟、停机规则和核心调用计数，删除任何“等算力优越”措辞。新 China81 五臂仍必须同批、同环境、同模型和同种子谱系。 | 等 E2 洁净批产出逐臂 CPU/墙钟/停机记录；公开表也需冻结其计时口径。 | 每个被比较算法均有可核 CPU、墙钟和停止 metadata；正文明确“收敛式质量比较而非等计算量比较”；不存在用迭代数冒充等计算量或用混批数据作优势判断的句子。 | 进行中；洁净批已按收敛式合同记录 CPU/墙钟，论文回填尚未开始。 |
| RR-15（3.7、3.8、3.11）：地区、规模、基础设施和种子相关性造成伪重复与主终点不清。 | 大修 | 在 `docs/handoff/model_change_approval_register_20260718.md` 先由用户批准统计单位；建议把 27 个地区--规模单元作为分层描述/推断单位，五种子作为重复测量，预先指定主要终点和多重校正规则；据此重算 `tab:china81-summary` 的区间、检验和表注。 | 等 `STAT-A` 获用户批准及洁净批完成；不得把未批准的 Claude 建议当冻结统计合同。 | 登记表出现用户批准的统计合同；分析脚本按该合同输出，种子不增加独立样本量；表注写明层级、主要终点和校正族；从 raw 到统计表可逐项重算。 | 未开始。 |
| RR-16（1.8、3.5、3.12）：PR14B 的单例极小改进和结果知情案例被用于 SP 机制归因。 | 大修 | 最低文字路径：把 `paper_main.tex` PR14B 段严格降为“开发后单一构造见证”，删除稳健/一般组件贡献含义；若保留组件贡献，则另做预注册的 SP-on/off、同预算多跑 HGS、相同路线池不同 MIP 时限配对面板并报告分布和代价。 | 文字降级无实验依赖；一般贡献路径依赖稳定模型、算力释放和预注册实验批准。 | 若走文字路径，全文 PR14B 只承担“该次解由 SP 产生”的定位，不外推期望收益；若走实验路径，面板有完整 raw、共同起点/种子/预算、置信区间和零选择性缺行。 | 未开始。 |
| RR-17（3.13、3.14、4.6、4.11--4.14）：车辆、阻力系数、速度、桩数/功率、车队储备、充电曲线和停机参数缺少敏感性。 | 大修 | 在新车型主模型冻结后预注册敏感性矩阵；分别生成车辆/设施情景表和停止轨迹表，覆盖阻力系数实际绑定、速度、车队储备、桩数/功率、充电曲线、`K`、外层轮数与连续无改进轮数；论文只回填预注册层级，逐项报告结论是否翻转。 | E2 洁净批结束并释放算力；D2/设施/统计等待用户批准；不得用中间结果选择敏感性层级。 | 每个预注册层级有完整终态、零选择性删除和哈希；主要结论在矩阵中的方向/翻转点明确；论文参数表与实际绑定快照一致。 | 未开始。 |
| RR-18（4.5）：EV 采用燃油车 2.48 m 对称高度，可能系统性降低阻力。 | 大修 | 对 2.48/3.05/3.25 m 三个已登记高度做同源敏感性；至少分开报告固定解复算的纯参数效应与重新搜索后的方案效应，在 `paper_main.tex` 车型表和结果段明确 2.48 m 是构造假设而非厂家值。 | 等 E2 洁净批完成；高度敏感性不得与正式批并行；运行前冻结复算/重优化层次和主终点。 | 三高度均有同一输入谱系、完整账本和独立复算；报告主要结果是否翻转；若厂家高度仍缺失，摘要/结论不把 2.48 m 单点结果写成现实车型结论。 | 未开始。 |
| RR-19（4.7、4.8、4.10）：柴油、电价和碳强度未统一到同一日期、地域和时槽主键。 | 大修 | 在 `paper_main.tex`“实验设计”价格与碳列段、参数表及逐任务 metadata 中统一保存 `city/date/tariff_zone/carbon_region/slot`；为每个价区给原始价表、档位和槽映射，对跨省代理做明确标注或敏感性。 | E2 新批已登记 2025-02-12 柴油价，但电价/碳列及跨省代理仍需来源和方法批准；E3--E7 执行前必须冻结联合主键。 | 每个正式任务都有完整联合主键和来源哈希；论文参数表可由 metadata 自动重建；不存在 2026 年柴油与 2025 年电价/碳列的未解释混用，跨省代理均有边界或替换敏感性。 | 进行中；新 E2 合同已统一柴油日期并要求逐任务记录，但整套经济/碳主键及论文回填尚未闭合。 |
| RR-20（4.9、6.12）：预测碳强度与实际结算序列身份、来源和生成过程不清。 | 大修 | 为预测序列和实际序列分别建立数据文件、来源、日期、版本、哈希和逐槽映射；在 `paper_main.tex` 充电模型、实验设计、图注中写清预测模型/误差构造/结算序列。若只有投影序列，则统一改称投影情景，不使用“实际结算”。 | 等碳数据来源与 E4/E7 方法合同获批准；不得从图形或单一投影数据臆造实际序列。 | 两条序列能独立下载/复算并在 manifest 中验签，或全文只保留“投影情景”单一身份；图、表、公式和结果段的日期、单位、序列身份完全一致。 | 未开始。 |
| RR-21（5.1--5.10）：与 Wang、Rochat--Taillard、Zhen、frvcpy 等最近邻工作的结构差异和新增价值未被证明。 | 大修 | 重写 `paper_main.tex` 引言文献比较表和算法差异段：逐项比较列/模式语义、非线性充电、车辆趟链、共享容量、参与约束、动态状态、路线池更新、主问题和预算；删除“同质多启动”等弱化标签、无来源企业宣传，并明确预印本身份。任何“三视角/完整复核/SP 新增价值”只引用 RR-14/RR-16 已闭合的证据。 | 先完成逐篇原文核验和最终算法/模型身份冻结；实证新增价值依赖洁净批与相应消融。 | 最近邻矩阵每一格有可核原文位置；正文没有仅靠打勾得出创新的表述；每个算法新增点都对应一个已封存消融或被降级为设计差异；企业和预印本边界有来源。 | 未开始。 |
| RR-22（4.1--4.3、6.1--6.11、6.14）：算法参数、硬件软件、数据生成、车型来源、环境、代码和检查器不足以外部复现。 | 大修 | 在 `paper_main.tex` 实验设置补完整参数/环境表；另发布机器可读复现清单，覆盖 `K`、外层终止、MIP 时限/线程/status、HGS 参数、Python/NumPy/PyVRP/HiGHS/OSRM 版本、CPU/内存/OS、代码 tag、随机源、PBF/profile 哈希、81 算例生成 manifest、全部模型参数、路线证书和检查器边界。 | 等最终代码、数据和全部正式实验版本冻结；不得用在跑批的暂态 commit 作为投稿 tag。 | 外部读者按单一 tag 和 manifest 能重建至少一个公开题和一个 China81 任务并由独立检查器得到相同可行性/目标；参数表对模型/算法中全部数值零缺项；所有文件哈希匹配。 | 未开始。 |
| RR-23（3.3、3.9）：China81 五种子和公开六题补充面板缺少重复数稳定性、功效和事前选择说明。 | 大修 | 公开六题逐题列身份、开发暴露状态和事前选择规则；China81 用洁净批五种子结果做结果盲重复数稳定性/区间分析，在运行前冻结何种条件才需要增加共同种子，不以方向性结果决定扩种子。 | 等洁净批全部五种子完成；若追加种子，需要额外算力批准且不得与当前批重叠。 | 六题选择规则可由未看结果的字段复现；五种子区间/稳定性报告齐全；是否追加种子由预先规则触发；论文不把五种子行当独立问题样本。 | 未开始。 |
| RR-24（3.6、7.8）：收敛图以时间为横轴却称迭代图，并为各算法事后挑不同种子。 | 小修 | 洁净批封存后重生成 `docs/paper_v2` 的收敛图：使用共同种子，或跨种子中位轨迹加区间；横轴名称与数据严格一致，图注披露预算和不确定性，不用不等时长留白制造视觉优势。 | 等同批轨迹、共同种子和 CPU/墙钟记录完整；需要可用 XeLaTeX/渲染通道做最终版面核验。 | 图源可从 raw 复算；所有算法使用同一选择规则；横轴、题名、图注一致；终点与结果表对应值匹配；版面无裁切或误导性空白。 | 未开始。 |
| RR-25（7.9）：碳强度图使用 g，而模型符号表使用 kg，且典型日身份不明。 | 小修 | 统一 `paper_main.tex` 符号表、碳强度图源、图注和正文单位；若保留 g/kWh 展示，明确与 kg/kWh 的 1000 倍换算，并标日期、预测/实际/投影身份及选择规则。 | 等 RR-20 的碳序列身份冻结和 E4/E7 图源封存。 | 图、表、公式单位可逐项换算且误差为 0；图注含日期和序列身份；“典型日”选择规则可由 manifest 重现。 | 未开始。 |
| RR-26（7.4--7.6）：正文大量使用“裁判、封存批、安全网、零回退”等内部研发术语。 | 小修 | 在证据闭合后统一重写 `paper_main.tex` 摘要、算法章和结果段：分别改为“统一完整目标复算/独立可行性检查”“历史实验批次”“保底接受规则”“未观察到劣化”等可定义术语；把问句式内部答辩改为中性研究问题。 | 依赖最终算法与证据身份冻结，否则术语替换可能掩盖尚未解决的版本差异。 | 预先列出的内部词在活动正文中命中为 0，或每个保留词都有数学定义；中英文术语一一对应，且不改变证据边界。 | 未开始。 |
| RR-27（7.10、7.11）：红队认为算法流程图与路线池图信息密度过高、字号偏小。 | 小修 | 按 Claude 裁决保留两图且不拆分；只做可读性验收：检查最终插入尺寸下最小字号、节点/箭线交叠、裁切、图注功能分工，并在正文明确主流程图回答“整体执行顺序”、路线池图回答“候选路线到限时 MIP 重组的关系”。若版面未过，只允许压缩标签、调整间距或图幅，不把关键关系移出正文。 | 等可用 XeLaTeX 通道恢复并完成最终两遍编译、逐页栅格化检查；算法结构先冻结。 | 两图均在最终 PDF 中无裁切/重叠，最小可读字号达到项目既定约 8 pt 门，正文不重复逐框复述，两图功能不重叠；无需满足红队提出的拆图/移补充材料条件。 | 未开始。 |
| RR-28（7.7、7.14、7.15）：交叉引用错误、失活 LEGACY 旧稿和模板化空话造成版本串线风险。 | 小修 | 清理 `paper_main.tex`：删除 `\iffalse` 中失活英国旧实验章，只保留 Git 历史；校正“第5节”等交叉引用；压缩政策口号和模板句，改为可证伪问题、最近邻差异和数据边界；运行标签/引用/占位审计并做最终人工通读。 | 最好在 E3--E7 权威结果回填后一次完成，避免重复移动章节；最终关闭依赖可用 TeX 编译与 PDF 检查。 | 活动源中无大段失活实验正文；所有 section/figure/table 引用解析到唯一目标；模板短语审计通过；编译日志无未定义引用、重复标签或结构错误，人工通读确认仅一套权威实验章节。 | 未开始。 |

## Claude 已明确不采纳的条目

下表记录的是“不采纳红队指定的修复手段”，不是忽略其底层风险。替代关闭条件已写回上表 RR-14 和 RR-27。

| 条目 | 不采纳的红队要求 | 理由 | 仍须遵守的边界 |
|---|---|---|---|
| NA-01（对应 RR-27、红队 7.10--7.11） | 因“信息密度过高”拆分算法主流程图与路线池图，或把关系矩阵/实现细节移到补充材料。 | 不采纳拆图。两图承担不同且都属于正文必要的方法关系：主流程图给整体执行顺序，路线池图给三视角路线、覆盖关系和限时 MIP 重组；当前论文写作合同要求关键算法关系留在正文，并按目标刊母版采用“总体流程图后接步骤说明”的结构。拆分不是本轮接受的修复方向。 | 保留不等于免检：最终 PDF 仍须通过约 8 pt 最小字号、无裁切/交叠、图注分工清楚和不重复逐框复述的客观可读性门；未过门时只调标签、间距和图幅。 |
| NA-02（对应 RR-14、红队 3.4/5.6） | China81 必须补做等 CPU 或等墙钟的单视角多启动对照，才能接受算法比较。 | 不采纳“必须等 CPU”硬门。当前冻结裁决采用目标刊可接受的收敛式预算，并要求逐算法披露 CPU 与墙钟；`docs/handoff/china81_vs_opensource_design_20260727.md:9-12,39-44` 已明确这一口径。等 CPU 不是本项目私有算例消融的强制验收条件。 | 仍必须同批、同模型、同环境、同基础种子谱系并完整披露 CPU/墙钟/停机规则；正文只能写收敛式质量比较，严禁改写为等计算量优越。旧 O 与四臂混批问题不因本裁决而豁免，仍由 RR-08 阻断。 |

## 当前总判定

三项已核实关闭：RR-01（作废干跑表已退出主稿）、RR-04（电耗 kWh 换算已显式补齐）、RR-06（路线条数计车的保守近似已披露）。RR-02、RR-08、RR-14 和 RR-19 仅因 China81 洁净统一批或其数据合同正在推进而标“进行中”，尚未形成可写入论文的终局证据。其余 21 项均保持“未开始”。本报告不授权修改论文、模型、实验合同或启动任何新计算。
