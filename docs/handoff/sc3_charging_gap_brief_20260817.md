# SC3 任务书：充电缺口捕获施工（发给 Codex，P81 已授权）

先读：`docs/handoff/codex_standing_charter_20260817.md`；
施工书 SC3 卡片 `docs/handoff/algorithm_build_spec_20260816.md:208-266`（**你写的，按它施工**）；
P81（`pending_decisions.md` 文末——用户原话"这是遗传算法跳出局部最优解的精髓！变废为宝"）；
你自己第九轮 Q5 对 SC3 的裁断（未被 SC8 吸收、BEST_EFFORT→A2 是真缺口）。

## 授权与边界（重要，先看这段）

- **P81 已批 SC3 为必做件**。施工点按你的卡片：`problem_hgs/charging.py` 失败路径
  （`:530-634`，`:625-629` 的 `ChargingRepairFailure` 包装）＋未受保护装配层
  （`contracts.py`、`problem_hgs/evaluation.py`）。
- **P78 冻结的是提速地界，不是整个文件**：`problem_hgs/charging.py` 内**只许动失败路径**，
  **不许碰**修复缓存/择时复用/剪枝类逻辑（那是 P78 待批的甲/丙）；
  `resetp_alns/support/charging.py` 整体不动。
- 三个受保护文件不动。**注意 `cost.py` 现行哈希已按 P87 更新为
  `13ae664bae0e9c8b5033780fbbc43cedcd43c626ac1eb23023a4a667bd2d1bbd`**（旧值作废）。
- 改动面超出上述文件即越界停工。

## 施工要点（按你的卡片，此处只列铁条）

1. `CHARGING_GAP = (缺电 kWh, 缺窗秒)` **两项分开存**，不许压成一个分数；
   三类返回齐全：可修复／`BEST_EFFORT`＋二元缺口／`REJECTED_*`＋原因（接口缺料不许伪造最好努力解）。
2. S1 统一时钟直接复用，不新算一套充电时间。
3. 装配层把两项映射为 A2 的独立违规/罚项记录；完整可行性仍由现有 `DutyFullEvaluator`
   与 protected checker 判定；**候选状态与完整可行性两个字段都保留**，账本记原始 gap。
4. 现有 `charging_rejection_reason` 失败证据不许被吞。
5. **开关默认关**（新通道纪律）：关闭＝与当前行为逐位一致；开启＝新行为。
   默认何时翻开属结构冻结时的 S5 决定，本任务不定。

## 验收（P88：全部快测，无长跑）

- **关闭态**：200 圈逐位＋800 圈指纹 `fc541b…d5970a3` 与成本 `2896.722268412695` 逐位（除 wall）。
- **开启态**（短技术跑即可，如 200–400 圈）：
  ①从一个已知充电修复失败快照**分别复算**能量缺口与窗口缺口，逐项核对；
  ②`BEST_EFFORT` 候选真实进入 A2（计数>0），其服务客户与实体车信息保留；
  ③**可行池纯净**：任何带缺口候选不得出现在可行池或最终 best；
  ④三类返回各覆盖至少一例；⑤服务量红线照报；⑥激活记录落 metadata（P82）。
- 回归 197 断言不动＋新单测；哈希表（cost 新值、其余四个不变）。
- **预期声明**：开启态的解与指纹**允许不同**（P81 批的就是语义变化），
  把开启态 800 圈新指纹如实登记为候选新基线，**不叫回归失败**。

## 产物

`solver/reports/sc3_charging_gap_20260817/`：`report.md` 首行 `SC3_DONE`／`_HALT`、
末行 `SC3_END`；diff、逐级验收表、四件套、done.json、resume。结尾答"接下来该干什么"。
