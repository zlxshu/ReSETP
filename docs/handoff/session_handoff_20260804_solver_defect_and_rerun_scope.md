# 会话交接 — 2026-08-04：求解器缺陷确诊后的重跑范围与已决事项封存

**状态**：`HANDOFF__NOTHING_STARTED__SOLVER_FIX_NOT_BEGUN`
**边界**：本会话全程只读。未跑实验、未改代码、未改 `paper_main.tex`、未动任何 `baselines/` 结果目录。
**用户当前指令**：停止推进，把状态打包进本交接文档。**接手方不得直接开工，先读完本文件。**

---

## 0. 一分钟版本

论文卡住的原因有两层，第二层比第一层严重：

1. **技术层**：求解器的搜索从来没有影响过最终答案。PyVRP 求解的代理问题不施加每车场实体车数上限，
   搜出的方案超编 1–5 辆，补全时被车队上限一票否决，异常被静默吞掉，
   最终解恒为"共同初始解"的补全。**种子无效、迭代预算无效、结果恒定。**
2. **范围层**：论文承诺的范围在冻结时就大于它能拿出的证据，且四个正式实验没有一个跑在
   已冻结的代表算例上。

用户已在 08-02/08-03 把主线故事、目录、代表算例、配角降级全部拍板。**当前没有待用户决定的事项。**
未开工的第一件事是**求解器修复**，修完按本文件 §5 的清单重跑约 226 次求解。

---

## 1. 用户已拍板、不得重开的事项（含原话与日期）

接手方**不得**把下列任何一条重新做成选择题交给用户。本代理已因此被纠正两次。

| 事项 | 决定 | 日期 |
|---|---|---|
| 主线故事 | **双碳背景下考虑动态需求的复杂混合车队物流模型及求解算法**；重点在双碳，落实为时变电网碳强度（目标期刊 2025 年发过双碳征稿）；混合车队是差异化热点；动态需求是小众难点。三者各自有人做过，**创新在于首次组合**。用户自述定位："把邱莹莹的毕业论文从简单仿真变成复杂仿真"。 | 08-02 04:48 |
| 配角降级 | 协同、公平分账、非线性充电、多车场**不是主线**，降为第 2 章模型组件，**引文献、不做正式实验、不进结论**。用户原话：这些"前人早就研究透了，我直接拿来用就好"。 | 08-02 04:58 |
| 算例数量 | **"不，就一个算例。"** 贯通全文的唯一私有算例，其余只做稳定性校验。依据两篇陈的实际做法。 | 08-02 17:01 |
| 代表算例 | 冻结为 **`cn-cy-100c-01-V2-LOCATIONS`**，冻结时刻 2026-08-03T01:10:38+08:00，规则先于候选身份写入。**此后不得因任何新实验的方向、幅度、显著性或图形效果更换算例、种子、阈值或口径。** | 08-03 01:10 |
| 新合同四条 | 多趟开启；固定成本**按实体车**计费 `c_fix=170`；车场充电**不设并发上限**；车队上限用 **fleet authority v3**（总量 943、逐场 T_d 2–22），废止 `num_ev=max(1,ceil(0.25R_d))`。 | 08-02 11:08 |
| 旧数据处置 | "以前的数据和设计几乎全部作废，以最新的为准"、"能用就用，不能用就拉倒重跑"。**不是删除授权**，旧目录一律原地保留。 | 08-02 11:08 / 04:58 |
| 论文本体 | **另起炉灶**写新论文，保留当前稿作素材。 | 08-03 04:23 |
| 顺序 | 先定问题 → 再定引言 → 再建模 → 做实验 → 写结论。 | 08-03 04:38 |
| 动态需求排期 | **排在最后**——"在前面的机制不成功的前提下，动态需求必然失败"。 | 08-03 05:37 |
| 中心论点 | "时变碳强度始终作用于决策，但其着力点会随车队电动化程度与动态扰动强度，在『充电时刻 → 车型指派 → 发车时机』三个层级之间转移；识别该转移规律，即可在给定条件下选出最有效的减排杠杆。" 用户否决了以"何时失灵"为主语的版本，理由："不能处处展现的是局限性……显得我这不是论文，是失败汇总。" | 08-02 06:45 |
| 目录 | 见 §4。第 5 章三节是**同一机制的三个层级**，不是三个并列敏感性。 | 08-02 06:45 后 |
| 推进顺序 | **先查清 → 再验证资产 → 最后改**。第一、二步已完成，第三步未开始。 | 08-03 |
| 并发 | 多拉 worker 加速，但留一个核心给系统。 | 08-03 10:02 |

**回读方法**（接手方必用）：HANDOFF 变更日志是二手转述，会漏掉用户当场说过但代理没记全的话。
要查用户原话，用 `mcp__ccd_session_mgmt__list_sessions` + `list_events`，
或直接读 `~/.claude/projects/-Volumes------512G--ReSETP/*.jsonl` 中 `type=user` 的消息。
上一段主要会话的文件是 `a0853293-13a8-4134-9c67-6d6d565ec73b.jsonl`。

---

## 2. 求解器根因（已确诊，`ROOT_CAUSE_CONFIRMED`）

权威文件：`docs/handoff/memory/solver_completion_reject_root_cause_20260803.md`（已列为
`READ_ME_FIRST_FOR_AGENTS.md` 第 0-A2 条，优先级高于 07-31 诊断主文档）。

**链条**：HGS 在代理问题上搜出好解（有效、可行、约 4000 倍改进）→ 取前 24 个候选去补全
→ **24 个全部因超出实体车队上限被拒**（`ValueError: China81 route skeleton cannot satisfy the
registered physical fleet caps at 'D_chengdu': overage=(1,0,1)`，overage 取值 (1,0,1) 至 (5,0,5)）
→ 异常在 `epochal_hgs.py:532-543` 被 `except (IndexError, KeyError, TypeError, ValueError)`
捕获后**不计数、不告警、静默丢弃** → `archive_completions` 只剩共同初始解
→ 集合划分拿到它的 18 条路线，无组合空间 → 输出恒定。

**要害**：搜索侧与检验侧用的不是同一套车队约束，中间没有反馈。

**证据三轮**（均 `COMPLETE`，产物在 `baselines/china_e3_e7/`）：
`seed_determinism_probe_20260803/`（6 单元逐位相同，预算 100/1000/25000 无差异）、
`seed_determinism_probe2_20260803/`（代理侧 18 行有 15 个不同取值＝搜索有效；完整侧只有 1 个取值）、
`candidate_pool_probe_20260803/`（`completion_attempted=24`、`completion_succeeded=0`）。

**规模**（`docs/handoff/asset_audit_20260803/`，898 个包，纯只读）：
全仓库 `archive_completion_attempts` 合计 184,854、成功 24,526，成功率 **13.3%**——**不是全局归零**。
但约束一收紧就塌到 0：公平约束（`e6_fairness_v2/v3`）与新合同固定车队总量（两个 formal 包）
均为 **0%**。即便最好情形也只有 13.4%，**86.6% 的候选一直在被丢弃**。
分类：`AFFECTED_CONFIRMED` 20、`AFFECTED_LIKELY` 76、`NOT_AFFECTED_PATH` 114、
`NO_SEARCH` 129、`UNDETERMINED` 559（其中 546 条缺少可区分求解路径的证据，属记录纪律缺口）。

**由此解释的既有现象**：五臂/三臂结果全平、100c 五臂全部 3621.569908、200c 三臂全部 7820.181472、
车队臂 75%/100% 两档 10/10 无解——此前都归因为迭代预算不足，实为补全全灭、各臂共用同一初始解。

**已作废的标定**：`V7_FIXED_25000_ITERATIONS_PER_VIEW` 对结果无作用，
"2000 次 vs 25000 次"的全部讨论无意义。教训："我们自己标定过"这句话本身也要有证据。

---

## 3. 四个正式实验没有一个跑在代表算例上（本会话新查出，与求解器缺陷独立）

| 实验 | 实际算例 | 代表算例 |
|---|---|---|
| 五算法比较 + 三臂消融 `formal_algorithm_20260802` | `cn-prd-100c-01` | `cn-cy-100c-01` |
| 车队电动化五档 `formal_fleet_levels_20260802` | `cn-prd-100c-01` | 同上 |
| 200 客户组件消融 `formal_ablation_200c_20260803` | `cn-prd-200c-01` | 同上 |
| 动态发车 `formal_dynamic_dispatch_20260802` | `cn-prd-100c-02` | 同上 |
| 碳择时重打分 `carbon_timing_rescore_20260802` | 81 题全网格 | 同上 |

成因是时间顺序（四项 08-02 启动，代表算例 08-03 01:10 才冻结），但后果与成因无关：
按"贯通全文的唯一算例"这条已定规则，**这五份结果没有一份能进正文**。

**其中两项跑在判别力已知为零的题上**：
`docs/handoff/representative_instance_selection_20260803/` 的判别力矩阵（覆盖全部 81 题）记录
`cn-prd-100c-01` 的算法部件维度实测 **0**（阈值 3），列在"仅差一个阈值"的落选表里。
五算法比较与车队五档都在这道题上。**该层与补全缺陷叠加，两个原因各自都足以让结果全平。**

---

## 4. 论文目录（08-02 定稿）

```
1 引言
2 问题描述与模型（2.1 问题描述 / 2.2 符号说明 / 2.3 能耗、充电与时变碳排放核算 /
  2.4 目标函数与约束——协同、跨场、分账、非线性充电在此引文献）
3 算法设计（3.1 算法框架 / 3.2 算法步骤）
4 数值实验设计与算法有效性（4.1 算例、参数与代表解 / 4.2 公开算例上的求解质量 /
  4.3 算法组件贡献分析）
5 时变碳强度下的减排杠杆转移（5.1 充电时刻层 / 5.2 车型指派层 / 5.3 发车时机层）
6 结论与管理启示
```

母体与替代母体（`docs/handoff/paper_restructure_20260802/`，R1/R2/W1/W2 + SYNTHESIS 施工图）：
- 第一母体陈雨蝶等（2025，师出同门）；第二参照陈婉茹等（2023）SETP 43(11):3320-3335。
- **判"不像、须另找母体"的四条**：组件归因学陈婉茹 p.3331 表8 的一因素移除式消融，不学陈雨蝶表6；
  题眼级时变碳强度学李进、张江华（2014）pp.1785-1786 的两级证据；
  车队电动化配置学陈婉茹 p.3331 表9；动态触发学姜广田等（2024）pp.2373-2377。
- **母体中不可照抄的部分**：陈雨蝶 p.22 §4.9 的逐因素控制变量结构是"因素流水账"的反例。
- 用户硬要求：不得机械模仿，要"模仿精髓、融会贯通、适配本文故事"；实验产物不得自己加戏。

---

## 5. 最小重跑清单（`docs/handoff/minimum_rerun_plan_20260803.md`，无待拍板项）

全部在代表算例 `cn-cy-100c-01` 上，各 10 种子。

**顺序：修 → 门一 → 门二 → 5.2 探路 → 4.3 → 5.1 → 5.2 → 5.3**

| 阶段 | 内容 | 次数 |
|---|---|---|
| 门一 判别力自检 | 换种子目标值必须不同；换预算必须不同；`completion_succeeded > 0`；失败候选**必须计数并写进产物** | 6 |
| 门二 预算标定 | 3 档预算 × 5 种子取拐点。**不过不准开正式批** | 15 |
| 5.2 探路 | 5 档 × 1 种子，见 §7 风险 | 5 |
| 4.3 | 5 算法 × 10 种子 + 3 臂消融 × 10 种子 | 80 |
| 5.1 | 基础解 10 + 联合三模式 30；28 日 × 2 臂重放零搜索、用全分母不挑日期 | 40 |
| 5.2 | 5 档 × 10 种子 | 50 |
| 5.3 | 3 策略 × 10 种子 | 30 |
| **合计** | | **约 226** |

- **4.1 代表解**从 4.3 完整臂里取，不单独跑。
- **4.2 公开算例不重跑**：`baselines/e2_final_campaign_20260720/mv_hgs_sp_final/run_p1_formal_public.py:43-55`
  直接调 PyVRP，不经 China81 补全环节；作废登记判 `UNAFFECTED_VALID_WITH_ORIGINAL_SCOPE`。
  但措辞必须改为"与母体同一水平"——等迭代对照 −0.100%、p=0.289。
- **门二为什么必须有**：五算法比较与消融实际用的 2000 迭代/150 无改善抄自陈婉茹 p.3328，
  属他人求解器的内部标定，与用户纪律冲突；而 `V7_FIXED_25000` 已被实测证伪。
  修复后迭代预算才第一次真正起作用，须用自己的数据重标。
- **226 不能按旧的每单元耗时估墙钟**：修复后补全真会成功，每单元要完成的候选从 1 个变成最多 24 个。
  排期前先用门一的 6 次实测新单耗。
- 对照：资产盘点给的"修完全部重跑"下界是 96 个包，真实数字更大。压到 226 靠的是
  **凡不进定稿目录、不在代表算例上的实验一律不重跑**。

---

## 6. 不需重跑但写作前必须处理的三件

1. **4 个无源参数**：公共充电服务费 0.40 元/kWh；迎风面积投影因子 0.85 与空气阻力系数 0.45；
   EV 初始 SOC=0；NL90 分段充电曲线的车型实测或页码来源。处置方案见
   `docs/handoff/risk_remediation_20260802/`（X1，19 项，零项需重跑）。
2. **12 处措辞强于来源**（D4）与全文"最优""优化得到"的表述——后者因求解器缺陷必须一律改口径。
3. **算例来源分栏**：真实实例与构造仿真分开说明、生成机制可追溯（陈婉茹等 2023 p.3328、p.3330）。

---

## 7. 风险清单

**5.2 可能修完也救不回来。** 它是中心论点第二层（着力点转移到车型指派）的唯一证据，
目前三档零差异、两档整档无解。三个互相独立的候选解释：补全全灭、算例判别力不足、
车型自由度本来就不够。**前两个靠修复与换算例排除，第三个排除不了。**
故门二之后先花 5 次探路；若在代表算例上仍是零差异，须在动 4.3/5.1 之前先决定第二层怎么办。

**5.3 的 14 个 `SEARCH_NOT_FOUND` 同理**：修完若仍找不到解，说明动态那节还有第二个病灶。

**已挂起、不在本轮范围**：非线性充电（两臂成本差 0.0，机制一次未触发，用户 08-03 裁定暂时挂起）；
车队臂 75%/100% 整档无解的成因（已由本次根因一并解释，不需单独一轮）。

---

## 8. 纪律（不因交接而放松）

- **角色**：Claude 只思考/判断/写 Codex 提示词/写文档；**禁止运行或修改代码、禁止开 workflow/ultracode**。Codex 执行。
- **与用户对话不得出现任何方案代号或状态标签**，实验按内容叫名字。用户已因此发火多次。
- **用户只做选择题、判断题**。一切待决事项须转成 2–4 个具体候选，写清选它意味着什么、代价、推荐项。
- **出选择题之前必须回读上一段会话的用户原话**（见 §1 末），确认不是已拍板事项。
- **Codex 容易 OVERDO，说的话不能全信**：它的审计取证准，但计划分类建议须逐条裁决；转述前先回原始文件核。
- **一任务一看门狗**，必须在被通知会话的进程树里；看门狗只判进程死活、日志停滞、产物有无，
  **不塞科学门槛**；多任务启动前做交付路径两两互异自检（bash 3.2 无关联数组）。
- **四件套 + report.md**：`metadata.json` / `raw_runs.csv` / `decision.json` / `artifact_hashes.json`；
  hash 必须排除 `._*`、`__pycache__`、`.pytest_cache`、临时 checkpoint。
- **受保护文件**（未获用户明确批准不得改）：`solver/src/setp_solver/cost.py`、`check.py`、
  `search/evaluation.py`。
- **不得新增无页码支持的科学门槛/阈值/统计单位/多重校正/预算合格线**。
- **不得把 `VALID_BUT_WEAK`、`HALT_*` 包装成胜利**；不得用 x86 绝对成本和 M1 正式表混比。
- **旧目录不删除、不移动、不覆盖。**
- Codex 配额已恢复（此前记录的"耗尽至 08-05 13:39"不再成立）。

---

## 9. 未开工的下一件事

**求解器修复任务书**（尚未写、尚未派）。要点：
1. 让代理问题携带每车场实体车数上限，使搜索侧与检验侧用同一套车队约束；
2. 补全失败必须计数、记录失败原因原文并写进产物，不得静默丢弃（现 `epochal_hgs.py:532-543`）；
3. 修复后先跑门一自检、再跑门二预算标定，两门都过才准开正式批。

修复完成后，受影响实验按 §5 清单重跑。

---

## 10. 文件索引

**必读入口**（顺序即优先级）：
- `docs/handoff/READ_ME_FIRST_FOR_AGENTS.md`（仓库级启动门）
- `docs/handoff/memory/user_operating_principles.md`（0-A，用户长期工作方法）
- `docs/handoff/memory/solver_completion_reject_root_cause_20260803.md`（0-A2，实验事实最高优先级）
- `docs/handoff/diagnosis_and_remediation_master_20260731.md`（0-B）
- `HANDOFF.md` 变更日志（最新几条在文末）
- `CLAUDE.md`

**本轮相关**：
- `docs/handoff/minimum_rerun_plan_20260803.md`（重跑清单，08-04 修订版）
- `docs/handoff/representative_instance_selection_20260803/`（代表算例冻结）
- `docs/handoff/asset_audit_20260803/`（898 个包逐包盘点）
- `docs/handoff/superseded_results_register_20260802.md`（旧结果引用资格）
- `docs/handoff/scout3_corrected_reading_20260803.md`（两条 SCOUT3 结论更正）
- `docs/handoff/diagnosis_20260802/`（D1 主线 / D2 同义反复 / D3 五七组根因 / D3B / D4 算例来源 /
  D5 敌意审稿 / D6 算法）
- `docs/handoff/paper_restructure_20260802/`（R1 母体逻辑 / R2 数据盘点 / W1 自我博弈 /
  W2 精确对照 / X3 章节设计 / SYNTHESIS 施工图）
- `docs/handoff/risk_remediation_20260802/`（X1 风险处置）
- `docs/handoff/RESUME_HERE_20260802_diagnosis.md`（08-02 彻查轮接续简报）

**权威链**（历史，仍有效的部分）：
`e1_e7_submission_contract_decision_20260711` → `e3_e7_experiment_product_design_20260712` →
`paper_north_star_20260713` → 07-17/20 China pivot → `e2_final_closeout_20260727` →
`contract_e5e7_blind_01_20260728` → `advisor_instance_selection_01_20260729`。
⚠️ `project_prd_execution_map_v2_20260702`、`project_planning_map_20260701`、
`MASTER_codex_takeover_plan` 的正文主体是**已退役的英国轨**，不得据其规划当前工作；
仍有效的是记录纪律、结论标签制、禁改语义、已关闭路线台账、不要做清单。
