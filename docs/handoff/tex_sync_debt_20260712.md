# TeX 同步欠账登记（2026-07-12）

本文件只登记欠账，不在 WP0 里扩大写作范围；写作冲刺时统一处理，并逐项回填证据来源。

1. 电池参数表：将 `B=280 kWh` 写成主场景，同时保留 `80 kWh` 作为文献基准稳健性对照说明。
2. 碳配额参数：正文和参数表改为默认配额 `0`（全额计价），删除全文“配额变化影响路线”的表述；配额只保留会计口径。
3. 跨场服务费：主值 `c^{tr}=0`，补充 `0/10/25/50/95` 的摩擦敏感性说明，明确非零值是无现实标定代理。
4. 客户归属：补充“按最近车场映射”的合成独立经营基线假设句，并标注冻结映射文件与哈希。

WP0 只登记以上四项，当前不启动 E3–E7 正式搜索，也不改 E2 冻结证据。
