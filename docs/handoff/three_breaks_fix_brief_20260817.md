# FIX-3BREAKS 任务书：修全流程三处断点（发给 Codex）

先读 `docs/handoff/codex_standing_charter_20260817.md` 与你自己的
`solver/reports/fullflow_smoke_20260817/report.md`（三处断点是你查出来的，本任务按你的建议顺序修）。

## 授权与判据（P85＋P90）

用户已给最高权限＋"别为收集而收集"；这三处经 Claude 判定**全属接线/入口层，无语义选择**，
故不再单独报批。**判据是 ROI**：每修一处必须能让对应那行图表从"填不了"变成"能跑"。
**修不动或发现它其实涉及语义选择 → 立刻停下报告，不许自行决定语义。**

## 三处，按依赖顺序，各自独立验收

### A｜公开 benchmark 环境（挡住第 ⑧ 行）

`FACT`（你查的）：`run_public_v2_28_clean_ruler.py:1059-1062` 的隔离 worker 删除 `PYTHONPATH`
→ `kernel_proposals.py:22` 从已安装内核导入 `DepotSplit` 失败。
**要的结果**：PR11A 与 PR17A 两题能真正跑完并产出本次成绩，且能与封存参照对上口径
（PR11A 已核 BKS 6655.548；PR17A 6292.594）。
**边界**：只修环境/导入接线；**不许改公开评价口径、不许改题目、不许为了跑通而放宽比较规则**。
**注意 P82**：公开侧自研组件应保持冻结，修完须在 metadata 里能看出自研件未激活。

### B｜时变碳三臂初始化（挡住第 ① 行，用户三大实验之首）

`FACT`：多趟按 P82 休眠时，`run_problem_hgs_private_technical.py:2616-2618` 抛
`single-trip initialization lacks same-type, same-depot idle assets`，第一臂 `asap` 即崩。
**要的结果**：三臂（asap／cost_min／cost_plus_carbon）各跑 200 圈，都产出成本、排放、
充电时刻分布。
**边界**：这处最可能藏语义选择——**若修法需要改变"哪些车可用/怎么算空闲资产"的语义，停下报告**；
只有当它确属"多趟休眠时初始化契约没接好"的接线问题才可自行修。不改算例。

### C｜混合车队溢价入口（挡住第 ② 行）

`FACT`：`run_mixed_fleet_experiment.py:1222-1247` 无 EV 溢价输入；实际溢价由
`run_problem_hgs_private_technical.py:1724-1729` 从固定合同读取。
**要的结果**：能显式传入两个溢价点各跑 200 圈，产出两点的 CV/EV 台数差异读数。
**边界**：**不改算例、不改合同里的既有默认值**（默认行为须与现在逐位一致）；只加显式入口。
P28 依赖如仍卡正式 MAIN-2，如实记录，不绕过。

## 通用铁条

- **每修一处只重跑对应那一段烟雾测试**（200 圈/120 秒级），**不放大预算**；单次 ≤10 分钟，总 ≤2 小时。
- 三个受保护文件与两个 charging 不动（`cost.py` 现行 `13ae664b…`）。SC3 保持默认关。
- 已通的四段（②③⑥⑦）行为**不得改变**：修完抽查私有静态 200 圈，与本次烟雾读数
  （成本 3230.068332、50/50、13264 kg）一致。
- 单进程串行求解（M1 8GB，另有两条只读任务在跑）。

## 产物

`solver/reports/fix_3breaks_20260817/report.md`，首行 `FIX_3BREAKS_DONE`／`_PARTIAL`／`_HALT`、
末行 `FIX_3BREAKS_END`。每处给：改了什么（diff）、验收读数、**对应图表行现在能不能填**、
修不动的如实写原因。结尾按章程答"接下来该干什么"。
