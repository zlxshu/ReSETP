# TIME-CARBON-3ARMS 任务书：时变碳三臂（发给 Codex，用户已拍板启用多趟）

先读 `docs/handoff/codex_standing_charter_20260817.md`。
**本件解除你在 `fix_3breaks_20260817` B 段的语义停工——用户已拍板。**

## 用户裁决（P82 补充，2026-08-17）

用户原话：「**我说的是"非必要"才休眠！这是个逻辑判断语句！很显然，事实已经证明了启用的必要性！
你别机械的僵化的理解我的话！我同意启用。**」

**判据澄清**：休眠的判据是"**非必要**"——组件若对**本实验的解是否成立**是必要的，
**就该启用**，哪怕它不是本实验的检验对象；只有既不被检验、解也不依赖它的才休眠。
**本例已定：时变碳三臂实验启用多趟。**

`FACT`（你自己算出的必要性证据）：见证路线在 `D_OSM_WAY_1071205721` 需 **15 趟**，
该场 CV 7＋EV 7＝**14 个物理车槽位**，油电全用仍差 1 辆——多趟休眠时数学上不可行。

## 要做的

跑时变碳**三臂**：`asap` ／ `cost_min` ／ `cost_plus_carbon`。

- **启用多趟**（本次唯一放开的组件）；其余组件维持你在全流程普查里对该段的计划口径；
- 统一算例 `cn-jjj-50c-01-DEPOTSEARCH-d996f755bd`、seed 11、**各 200 圈**（不放大预算）；
- **不改算例、不改车队上限、不改车型/车场归属、不碰五个冻结文件**
  （`cost.py` 现行 `13ae664b…`）；SC3 保持默认关。

## 要产出的读数（第 ① 行图表要的）

每臂给：**总成本、总排放、充电时刻分布**（各充电动作的开始时刻与电量）、
充电时段加权碳强度、电费、碳成本、CV/EV 台数、**服务客户数与完成需求量（红线必报）**、可行性。
三臂并排一张表，并给出**相对 `asap` 的成本差与排放差**。

`BOUNDARY`：这是 200 圈技术读数，**不是正式实验结果**（正式须按 P79 十种子）。
报告须明写这一句，不得把它当效应结论。

## 组件激活清单（P82）

如实记录本次激活/休眠的组件，**多趟须标 active 并注明"因解可行性必要而启用（P82 补充）"**。

## 产物

`solver/reports/time_carbon_3arms_20260817/report.md`，首行 `TC3_DONE`／`_HALT`、
末行 `TC3_END`；四件套＋done.json＋resume。单进程串行，单次 ≤10 分钟。
若仍跑不通：**如实 HALT 并写清新的阻塞点**，不许绕过判据或换口径救结果。
结尾按章程答"接下来该干什么"。
