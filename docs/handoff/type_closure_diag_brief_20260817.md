# TYPE-CLOSURE-DIAG 任务书：车型交换关着但归属仍变（发给 Codex）

先读 `docs/handoff/codex_standing_charter_20260817.md` 与你自己的
`solver/reports/time_carbon_3arms_20260817/report.md`（新阻塞点是你查出来的，本件接着查）。

## 已钉死的事实

`FACT`：多趟启用后初始化跑通，1 圈 `asap` 探针得到完整评价可行解、50/50 客户、13264/13264 kg、
9 CV/1 EV、两次充电。
`FACT`：`whole_duty_type_exchange` **提案次数为 0**（算子确实没被调用），
但车型归属哈希仍由 `fda01cb7…dd11` 变为 `e5bab0c3…59e6`，
涉及 **C003、C007、C010、C027、C029、C043** 六个客户；runner 因此判 `type_exchange` 休眠闭包失败（退出码 2）。

## 为什么必须查清（不是洁癖）

`INFERENCE`：三臂比较的前提是**臂间只差充电策略**。若车型归属还会自行变化，
三臂的成本差与排放差就混入车型变化，时变碳效应无法干净归因——那种数字不能进论文。

## 要回答的三问（**只诊断，先不修**）

1. **是谁改的**：车型归属的变化发生在哪条路径？候选（不限于此）——
   初始种群构造／解码器（hybrid decoder 的实体车槽位选择）／种群选择与生存／
   修复或充电重建的回退／见证适配。**给文件:行号级证据**，别猜。
2. **是接线问题还是语义问题**：
   - **接线**＝休眠开关没有覆盖到那条路径（本该覆盖却漏了）→ 说明补哪一处即可；
   - **语义**＝那条路径本来就有权改车型（例如槽位不足时必须换型才能物化候选）→
     说明它为什么必须改，以及不让它改会发生什么（是否退回"车不够用"）。
   **这两种的处置完全不同，必须明确判一个，不许两可。**
3. **最小干净方案**：要让三臂只差充电策略，最小的做法是什么？给 2–3 个候选，
   每个写清：改哪里、是否改语义、代价、以及**是否会让第一堵墙（车不够用）复发**。

## 边界

- **只诊断不施工**：本件不改任何源码。若你判定它是纯接线且修法零语义风险，
  **也只写方案，不动手**——因为它紧贴 P82 休眠口径，处置权在用户/Claude。
- 允许跑**最小探针**（1–5 圈级）取证；单次 ≤5 分钟，总 ≤30 分钟。
- 五个冻结文件不动（`cost.py` 现行 `13ae664b…`）；不改算例；SC3 默认关。

## 产物

`solver/reports/type_closure_diag_20260817/report.md`，首行 `TYPE_DIAG_DONE`／`_HALT`、
末行 `TYPE_DIAG_END`；四件套＋done.json。结论逐条标 `FACT`/`INFERENCE`/`UNKNOWN`。
结尾按章程答"接下来该干什么"。
