# 本次待投稿预览副本

- 来源：2026-08-28 18:19:36 时的 `docs/paper_v2/`
- 目标：按现有、已经确定的论文图表框架回填可追溯实验结果并生成预览 PDF
- 边界：不修改原始 `docs/paper_v2/paper_main.tex` 或 `paper_main.pdf`；不新增、不重设图表；不手工改实验数字

实验数据只从已有完整结果或本次新增的完整结果进入本副本。

## 当前回填状态

- 已回填：公开算例逐题对比、代表性仿真实验、车队动力配置、非线性充电函数、时变碳强度、
  配送模式、成本分摊和动态需求的既有图表及相应文字。
- 已补跑：代表性仿真实验种子 1、2、11；M1 串行，每个种子 600 秒，三次均完成全部客户与需求。
- 已回填代表性实验汇总行和三种子实际时间收敛图；种子 11 在约 583 秒时产生一次最好值改进，
  种子 1、2 在初始化后未继续刷新最好值。
- 动态需求表复用上次种子 1 的完整服务结果；三臂均完成 60/60 个客户和 16111/16111 kg 需求。
  旧包中的失败标记来自临时延后订单追回后状态未恢复，以及把跳过求解的延后批次误计为必须调用求解器；
  本次只删去这两项错误判定，没有改变实验字段、指标或求解方法。
- 本次另跑的随机种子 2396617133 中，滚动重优化只完成 57/60 个客户和 15209/16111 kg 需求，
  原始失败结果保留在补充实验目录，没有进入论文表格。
- 图形边界：沿用原有图题、坐标含义和图表位置，没有增减图表或指标。

## 预览文件

- `paper_main.pdf`：本次隔离回填后的预览稿。
- `paper_main.tex`：本次隔离回填源文件。
- `build_final/`：预览编译日志与中间产物。
