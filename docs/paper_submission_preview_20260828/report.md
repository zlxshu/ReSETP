# PAPER-CITATION-FORMAT-AUDIT-01 审计报告

审计对象：`docs/paper_v2/paper_main.tex` 当前工作树版本。审计日期：2026-07-28。执行边界：仅作串行文本检查、两张现用 PDF 图的低负载渲染检查，以及受限的 XeLaTeX 启动尝试；未启动实验、未使用多进程、未访问用户指定的受保护 E2 重跑目录。当前工作树在本任务开始前已有未提交修改，本任务保留这些修改。

本任务对论文源文件只实施了一项获准的机械修复：交换 `ref:88` 与 `ref:83` 两个完整参考文献条目的位置，使其与正文首次引用顺序一致。没有改动条目内容、正文措辞、论证、表图或数据。

## 1. 引用键、悬空引用、未引条目与参考文献顺序

机械解析共发现 76 次 `\cite` 调用中的 43 个唯一键，以及 43 个 `\bibitem` 条目。

- 悬空引用：0。全部 43 个唯一引用键均能解析到 `\bibitem`。
- 从未引用的参考文献条目：0。
- 修复前唯一错序：正文在第 986 行首次引用 `ref:88`，第 987 行首次引用 `ref:83`；修复前文后条目却是 `ref:83` 在第 1931 行、`ref:88` 在第 1934 行。
- 已实施的机械修复：交换上述两个完整条目。修复后 `ref:88` 位于第 1931--1932 行，`ref:83` 位于第 1934--1935 行；43 个文后条目的顺序与 43 个键的正文首次引用顺序逐项一致。

没有发现明确的拼写或键名错误，因此没有作键名修复。

## 2. `\ref` / `\label` 配对与编号连续性

将 `paper_main.tex` 与其第 672 行实际输入的 `generated_figures/algorithm_flow.tex` 一并解析后，未发现未定义的活动 `\ref`。正文第 655、664 行对 `fig:algorithm-flow` 的引用由该输入文件中的标签解析；仅孤立扫描主文件会产生假阳性，故不计为问题。未发现重复标签。

存在 9 个“已定义但正文未引用”的公式标签；它们不会产生未定义引用警告，但不满足严格的一一配对要求：

- 第 419 行：`eq:linear_energy`。
- 第 443 行：`eq:charge_window`。
- 第 482 行：`eq:F2`。
- 第 489 行：`eq:F3`。
- 第 496 行：`eq:F4`。
- 第 515 行：`eq:F6`。
- 第 745 行：`eq:sp-recombination`。
- 第 877 行：`eq:charging-candidates`。
- 第 878 行：`eq:charging-rule`。

仓库现有 `paper_main.aux` 快照显示活动编号连续：表 1--14、图 1--9、公式 1--34，均无跳号。第 1271--1566 行 `\iffalse` 块中的表图不参与活动编号。

## 3. 表格格式一致性

活动正文共有 14 张表。全部使用 `\linewidth` 宽度；其中第 1192--1199 行和第 1231--1238 行的 `tabular*` 位于各自 `\input` 文件内。全部活动表均有 `\toprule`、`\midrule`、`\bottomrule`；第 931--947 行表的 `\bottomrule` 位于第 944 行输入的表体文件末尾。

不符合项如下。

1. 缺少 `\tabnote`：`tab:literature-comparison`（第 290--310 行）和 `tab:symbols`（第 352--396 行）。其余 12 张活动表均有 `\tabnote`。
2. 非纯三线表：`tab:instance-detail`（第 931--947 行）的列格式在第 938 行显式加入 `\vrule` 竖线。虽然上下三条横线齐全，但不符合“无竖线的三线表”要求。
3. 表头未全部居中：`tab:literature-comparison`（第 295 行）、`tab:vehicle`（第 954 行）、`tab:tou`（第 995 行）和 `tab:final-solution`（第 1053 行）的首列使用 `l`，且表头未用 `\multicolumn{1}{c}{...}` 覆盖，因此首列表头左对齐。
4. 同列数字未右对齐：下列活动表的数值列均使用 `c` 或居中的 `Z` 列型，而不是右对齐列型：`tab:instance-detail`（第 931--947 行）、`tab:vehicle`（第 949--971 行）、`tab:tou`（第 990--1009 行）、`tab:final-solution`（第 1048--1071 行）、`tab:v13-results`（第 1101--1144 行）、`tab:algorithm-comparison`（第 1192--1199 行，列定义在第 1197 行输入文件内）、`tab:china81-summary`（第 1231--1238 行，列定义在第 1236 行输入文件内）、`tab:e3-summary`（第 1582--1598 行）、`tab:e4-summary`（第 1625--1641 行）、`tab:e5-summary`（第 1664--1678 行）、`tab:e6-summary`（第 1701--1715 行）和 `tab:e7-summary`（第 1740--1757 行）。`tab:literature-comparison` 与 `tab:symbols` 没有同类连续数值列，未列入本项。

旧英国情景禁用块内另有 10 张非活动表。它们都使用 `\linewidth` 和三线结构，但数值列同样使用居中 `c` 列型；其中 `tab:carbon-trading`（第 1281--1293 行）、`tab:dynamic-events`（第 1480--1492 行）和 `tab:dynamic-costs`（第 1499--1512 行）缺少 `\tabnote`。这些问题当前不会进入编译结果，但仍存在于源文件中。

## 4. 图形格式一致性

活动正文编号图共 9 张；另有第 672 行输入的算法主流程图。逐图结论如下。

- `fig:algorithm-flow`：由第 672 行输入；源码为无填充的黑白线条流程图，无图例，未发现底纹问题。
- `fig:route-pool`（第 774--862 行）：不符合“无底纹线条图”。第 782 行的 `chosen` 样式使用 `fill=black!15`，第 816--818 行又给三列矩阵填充 `black!15` 灰底。
- `fig:carbon-profile`（第 1026--1031 行）：对 `figure3_carbon_profile_v14.pdf` 的实际渲染检查显示无底纹；图例在轴内左下空白区、无外框、未遮挡曲线。
- `fig:convergence`（第 1201--1206 行）：对 `figure4_convergence_v14.pdf` 的实际渲染检查显示无底纹；图例在轴内右上空白区、无外框、未遮挡数据。
- `fig:e3-responsibility`（第 1600--1609 行）、`fig:e4-charging`（第 1643--1652 行）、`fig:e5-physics`（第 1680--1689 行）、`fig:e6-fairness`（第 1717--1726 行）和 `fig:e7-interaction`（第 1759--1768 行）：对应 PDF 文件均不存在，因此当前活动分支显示 `\fbox` 外框占位框，而不是线条图。它们不满足最终图形格式要求；由于没有实际图，也无法核验图例位置或遮挡。

未发现现有两张实际数据图使用轴外图例或带框图例。流程/示意图无图例，不适用图例位置判定。

## 5. 编译健康度

本任务未获得一份可声称为“XeLaTeX 连编两遍后”的新产物，因此本项正式结论为 `HALT_XELATEX_EXECUTION_UNAVAILABLE_WITHIN_TWO_ATTEMPT_BOUND`。

具体事实：首次使用低优先级包装器时，系统在 XeLaTeX 启动前以 `setpriority: Operation not permitted` 拒绝；随后直接启动的第一次 XeLaTeX 未产生 stdout、log、aux 或 PDF，串行的第二次命令未执行。为避免超过用户限定的至多两次启动边界，并避免存在未知阻塞时继续影响 China81 墙钟实验，本任务没有再重试。故不能把下列仓库既有产物冒充为本任务新双编译结果。

仅作参考，仓库现有 `paper_main.pdf/.log/.aux` 的时间戳快照可核验到：

- PDF 创建时间：2026-07-28 00:24:21 +08；23 页，A4。
- TeX 错误数：0（现有 log 中无 `!` 错误行）。
- 未定义引用数：0（现有 log 中无 undefined citation/reference 警告）。
- Overfull：0。
- Underfull：1 处。`paper_main.log` 第 1761 行报告 `Underfull \hbox (badness 1337) in paragraph at lines 1941--1942`，对应当前源文件第 1941--1942 行的 `ref:75` 条目和其后空行。TeX 对 underfull box 报告 badness，不报告“超出量”，因此可核验量为 badness 1337，超出量字段不适用。

上述既有快照早于本任务对 `ref:88` / `ref:83` 的机械重排，不能替代一次新的双编译验收。

## 6. `LEGACY_UK_MECHANISM_BLOCK` 位置

旧英国情景遗留块的标记包络从第 1268 行 `LEGACY_UK_MECHANISM_BLOCK_BEGIN` 到第 1567 行 `LEGACY_UK_MECHANISM_BLOCK_END`，共 300 行（首尾均计）。真正由 TeX 禁用条件包裹的范围从第 1271 行 `\iffalse` 到第 1566 行 `\fi`，共 296 行（首尾均计）。本任务未删除或修改该块。

## 7. 未回填的 `%% DATA_PLACEHOLDER` 标记

第 7 行是标记格式说明 `%% DATA_PLACEHOLDER: <说明>`，不是待回填数据位。其余未回填标记逐条如下。

### 活动正文与摘要

- 第 147 行：E3--E7 封存后补入不超过 30 字的中文主要结果句，使摘要保持 200--300 字。
- 第 164 行：补入上述封存结果句的英文对应句。
- 第 1266 行：按指定 `raw_runs.json` 重新生成 `tab:china81-summary` 并新增 O 列；当前仍为 v4 四臂表体。
- 第 1580 行：E3 结果段只从封存 `raw_runs` 与独立复算证书生成，并报告成本效应、跨场服务和责任结构边界。
- 第 1623 行：E4 从固定路线、车辆、电量的 28 日零搜索复算生成，报告改善范围、日间异质性和不可移动电量边界。
- 第 1662 行：E5 报告非线性可行率、不可行性减少和成本效应，不得只保留有利子集。
- 第 1699 行：E6 报告参与激活率、最低成员收益、系统节省及公平溢价，不能把嵌套候选当独立样本。
- 第 1738 行：E7 仅在事件流、状态继承、客户守恒、充电并发和独立证书齐全后填入。
- 第 1789 行：由适配器从五个 family 的统计汇总自动生成“机制作用—效应量—适用边界”讨论段。

### `\iffalse` 旧英国情景块内

- 第 1279 行：碳价/配额四点编号分析及政策边界总结。
- 第 1290 行：两种碳价乘六档配额的 12 行表体。
- 第 1302 行：四类决策目标对比分析及综合目标总结。
- 第 1331 行：填数后逐行加粗。
- 第 1344 行：排班方案数与成对单元数。
- 第 1365 行：充电窗口贡献分解、28 日分布与预测误差、三区域差异分析。
- 第 1367 行：充电择时机制的作用条件与边界结论句。
- 第 1377 行：配送模式四点编号分析及管理建议。
- 第 1406 行：填数后逐行加粗。
- 第 1415 行：用封存 CSV 生成的配送模式路径图替换 `fig:mode-routes` 占位框。
- 第 1423 行：责任交错程度与协同空间分析段。
- 第 1449 行：参与保障、系统成本、责任结构和跨场空间的分析段。
- 第 1478 行：动态触发参数与事件流构成说明。
- 第 1489 行：代表订单流逐事件行。
- 第 1497 行：逐次更新的触发、冻结、重派与成本变化叙事。
- 第 1509 行：各更新时刻表体。
- 第 1520 行：用封存 CSV 生成的动态路线图替换 `fig:dynamic-routes` 占位框。
- 第 1529 行：完整机制相对三类对照的经济、收益分布和可执行性分析段。

以上仅报告位置与机械事实；除第 1 节所述参考文献重排外，所有问题均未修复。
