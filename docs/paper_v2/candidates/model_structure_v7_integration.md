# 模型章节 V7 重排规则

正式主稿解除保护后，模型章节按以下顺序合并，公式内容和标签不改：

1. `问题描述与动态决策过程`：使用
   `model_problem_dynamic_v7_candidate.tex`，替换现有“问题描述”和末尾“动态需求处理”。
2. 符号说明表：保留现有表2，紧接问题与动态状态定义。
3. `车辆能耗模型`：保留现有内容。
4. `充电与分时碳排放模型`：保留现有内容。
5. `目标函数`：保留六类成本，并把总目标式
   `\min F=F_1+\cdots+F_6`移到该小节末尾。
6. `约束条件`：由现有第二个“模型建立”小节改名；删除其中已经前移的总目标式，
   其余配送趟、载重、时间窗、电量、跨趟衔接、客户覆盖、站点容量和参与约束保持原式。

验收时必须满足：动态触发、客户更新和累计状态三个公式只出现一次；所有公式标签与
正文引用闭合；总目标在任何约束之前出现；第二层模式选择说明保留；全文数学审计和
XeLaTeX编译通过。该重排只改变阅读顺序和过渡文字，不改变目标、约束、算法或数据。
