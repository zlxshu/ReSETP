# NOTICE — vendored Lera-Romero BPC code (`cpp/lera/`)

This directory vendors the branch-price-and-cut (BPC) solver for the TDVRPTW originally written by **Gonzalo Lera Romero** (Grupo de Optimización Combinatoria, Departamento de Computación, Universidad de Buenos Aires), together with its supporting `goc` library, as adapted in Florian Rascoussier's `TDVRPTW-solver` repository.

The algorithm is described in: Lera-Romero, Miranda-Bront, Soulignac, *Dynamic programming for the time-dependent traveling salesman problem with time windows* (and the companion TDVRPTW BPC line of work published from the same codebase, Networks 2019 / INFORMS JOC).

## License

The upstream code is distributed under the **MIT License**, Copyright (c) 2019 Gonzalo Lera Romero (see `TDVRPTW-solver/LICENSE`, reproduced by kayros' own MIT `LICENSE` at the repository root). This vendored copy retains that license.

## Provenance

Vendored on 2026-07-06 from `TDVRPTW-solver` git commit `35f1e3b` (local master):

- `goc/` — Lera-Romero's `goc` library (`code/goc`, sans its `CMakeLists.txt`: kayros owns the build). Includes Onyr's later updates (Digraph API renames, PWL fixes, NDCPWLF-era changes).
- `goc/include/goc/vrp/` and `goc/src/vrp/` (`Route`, `VRPSolution`) — restored from commit `8329844` (2019-08-13, Lera-Romero's last "all features working" state); these files had been deleted in the 2025 objective-function refactor while the BPC still depends on them.
- `nyr/` — Florian Rascoussier's `nyr` layer (`code/nyr`): TD `VRPInstance` (tau/pretau/dep/arr PWL matrices, LDT, unreachability), objective-templated routes, NDCPWLF, time utilities.
- `main/` — the BPC proper (`code/main`): `bcp/` (BCP, SPF, pricing problem), `labeling/` (bi/monodirectional labeling, lazy labels, PWL domination, ng-neighborhoods), `preprocess/` (instance preprocessing). Heuristics and standalone `main_*.cpp` entry points were not vendored (kayros has its own).
- `magic_enum/` — vendored header-only dependency (MIT, Daniil Goncharov), as carried by upstream.

## Local modifications (kayros)

Kept deliberately minimal; the reference for algorithmic behavior is Lera-Romero's `8329844` plus Onyr's half-port intent:

1. `goc/include/goc/goc.h`: re-added the two `goc/vrp/*` includes (match `8329844`).
2. `main/include/bcp/bcp.h` + `main/src/bcp/bcp.cpp`: the half-ported `nyr::VrpSolutionRecord&` parameter (a type that no longer exists) was replaced by (a) restoring Lera's original `Run(goc::VRPSolution*)` signature, and (b) a new optional `on_incumbent` callback member invoked on every new upper bound (anytime surfacing, plan 2 Stream 5 M5.0/M5.2).
3. Build: this subtree is compiled by kayros' own CMake (`KAYROS_WITH_LERA=ON`), never by upstream's CMakeLists.
4. **HiGHS backend (M5.1, kayros-added)**: `goc/{include,src}/goc/linear_programming/highs/` — `MatrixFormulation` (solver-agnostic in-memory `Formulation`) + `highs::solve_lp`/`solve_bc` mirroring the cplex pair, stateless materialization per solve (duals postsolved by HiGHS; no presolve weakening needed; single-threaded per kayros' one-run-one-thread convention). Compile-time switch `LERA_LP_BACKEND=cplex|highs` in `lp_solver.cpp`/`bc_solver.cpp` (`GOC_LP_BACKEND_HIGHS`); unused cplex includes dropped from `cg_solver.cpp`; `variable.h` gained `friend class MatrixFormulation`. Unsupported in the HiGHS `solve_bc` (fail loudly, unused by the BPC path): MIP starts, branch priorities, cut callbacks, lazy constraints. Known perf gap vs CPLEX: no simplex-basis warm start across CG iterations (rebuild per solve) — revisit at M5.2+.
5. `main/src/labeling/bidirectional_labeling.cpp`: `RouteDuration` → `goc::Route` conversion at the solution-pool exit; `nyr/include/nyr/vrp/delta.h`: explicit `SubpathHash` (GCC 15).
6. **Stopping / anytime compliance (M5.2, kayros-added)**: new `goc/include/goc/time/deadline.h` (`goc::Deadline`, absolute wall-clock deadline). `BCP` derives one deadline from `time_limit` at `Run()` and every component takes its residual budget from it instead of nested stopwatch re-derivations: CG (`ProcessNode`), strong-branching LPs (`EstimateBound`, which previously ran **unlimited** — `solve_colgen` reset the shared `LPSolver.time_limit` to `Duration::Max()`), the freeze heuristic (previously got the **full** TL as residual), and the root cut loop. New interruption points: `SeparateCuts` O(n³) enumeration, `IterativeMerge`/`LastArcMerge` in the bidirectional labeling, and the labeling's solution-pool repricing (post-deadline tail cut). Correctness guards: an aborted strong-branching probe or truncated pricing pass stamps `TimeLimitReached` so a deadline hit can never masquerade as an optimality proof (including `LastArcMerge` truncation no longer reporting `Finished`, and the `z_lb = z_ub` gap-close now conditional on a fully explored tree); the freeze heuristic accepts a TL-truncated MILP incumbent when one exists. `solve_colgen` now takes the CG optimum from the last in-loop LP solve instead of an unconditional re-solve (which wasted a solve and leaked `Duration::Max()` into later LPs).
7. **Warm start (M5.3, kayros-added)**: `BCP::SetInitialIncumbent(double)` — public setter for an initial UB known by the caller (the value of warm-start columns the bridge adds via the pre-existing `SPF::AddRoute` injection point). The bound must be in the master's own arithmetic (checker-exact since item 8; a bound tighter by dust than the master's costs could prune the true optimum) and no incumbent callback fires for it.
8. **Checker-exact column costs (M5.6 stage A, kayros-added, bridge-side)**: every route entering the SPF (seeds, warm-start columns, priced columns) is repriced by the kayros checker-exact fold (`cpp/core/route_eval` over the payload's raw MAMUT ATFs — Lera's preprocessing folds service/waiting into travel times, so the fold runs on the verbatim data), making all master objective coefficients, LP bounds, UBs and the reported value checker-exact; the final value is re-summed in the checker's canonical route order (bit-identical to `compute_solution_cost`). Lera's `BestDurationRoute` remains the *search* arithmetic (pricing reduced costs, labeling); safety: pricing emits only reduced costs < −1e-6 (goc epsilon) while the Lera-vs-checker cost dust is ~1e-12, so priced columns stay strictly improving under checker costs, and a checker-infeasible priced column fails loudly (certification integrity). **Pricing completeness is still modulo Lera's epsilon arithmetic** (a checker-optimal route could in principle be dominated away by dust inside labeling) — the stage-B labeling swap that would remove this residue was **deliberately skipped** (Onyr, 2026-07-06 night): values are checker-exact, the checker repricing referees every result, and the residue is a dust-probability with zero divergences observed across all gates. Certificates must keep wording it honestly.

9. **Stepwise-ATF (Rifki-style) support (M5.7, kayros-added)**: `main/src/labeling/bidirectional_labeling.cpp` gained `continuize_value_jumps` applied to each reverse arrival function in `reverse_instance`. Stepwise travel times produce interior *plateaus* in the forward arrival function (a tau down-step encoded as a slope −1 arrival segment); `dep = arr.Inverse()` turns each plateau into a value jump, the reflected reverse arrival inherits it, and inverting *that* leaves an interior domain gap where `DepartureTime`/`Value` fails (Lera's original padding only handles the boundary waiting plateau — IGP instances never produce interior ones). The helper bridges each upward value jump with a steep segment of width ≤ 1e-3 that stays *below* the following piece, so reverse arrivals are never over-estimated: bounds remain valid and no wrong optimum can be certified; the measure-delta under-approximation sliver is the same documented dust class as the epsilon-dominance residue (item 8). The forward-direction counterpart lives bridge-side in `kayros.lera._continuize_breakpoints` (duplicate-x breakpoints — tau up-steps — mollified before Lera ever sees them, matching the checker's left-continuous evaluation at the jump point exactly). Two follow-on dust guards in the labeling (same M5.7 scope): a label's `rw` and `dom(duration)` can disagree by mollifier dust (≤ 1e-3, beyond goc EPS), so boundary evaluations of label duration functions are clamped into the function's domain (`duration_at` in `bidirectional_labeling.cpp::Merge` and `monodirectional_labeling.cpp` alpha/beta) — Lera search arithmetic only, never certification arithmetic; and the bridge's stage-A guard skips Lera's empty-path INFTY pool placeholder (`AddSolution`'s `Route({}, 0, INFTY)`), which is not a column and pre-M5.6 entered the SPF as a harmless artificial-like column. **Refuted 2026-07-08:** the "no wrong optimum can be certified" claim in this item is false. Counterexamples: independent heuristic campaigns produced checker-valid solutions strictly below 43 of the 160 Rifki2020 certified optima (up to 1.46%), and certification is warm-start-dependent (TDVRPTW n=20 Rifki-16 certifies `Optimum` 8485 / 8376 / 8361 from stored-BKS / cold / counterexample warm starts, each in seconds, all three solutions feasible in the solver's own model). The pointwise under-approximation argument does not cover the labeling's *internal* arithmetic on the bridged functions: a 1e-3-wide bridge over a step of height h has slope ≈ 1000·h, so goc's 1e-6 epsilon comparisons and the `duration_at` domain clamps (this item) can misprice a merge by O(h) — enough for `Merge` to discard negative-reduced-cost routes, silently breaking pricing completeness. Do not stamp optimality certificates on stepwise-ATF (jump-carrying) instances until the labeling handles value jumps exactly; all Rifki2020 stamps were retracted from the MAMUT store on 2026-07-08. **Partially resolved 2026-07-09 (M5.9), by a different fix than "handle value jumps exactly"; see the 2026-07-10 amendment at the end of this item.** The root cause was not the mollifier's approximation. It was pricing incompleteness in goc's numerics: `PWLFunction::Inverse` was built by accumulating per-piece inverses through `Max`, which carried epsilon dust that, compounded over the mollified steep bridges, discarded negative-reduced-cost columns. `Inverse` is now an exact coordinate swap for non-decreasing functions (arrival and departure are non-decreasing), so the mollified continuous path prices completely, and `Value` is left-continuous to match the checker at jump abscissae. With this, the mollifier is retained and sound: the Rifki-16 n=20 reproducer now certifies `Optimum` 8361 cold and warm (was 8376 cold vs 8361 warm), and the fuzzer-discovered Rifki-22 subset reproducer certifies 2496 cold and warm (was 2504 vs 2496). An *exact* value-jump representation (true verticals through the labeling) was implemented and parked: it is currently less sound than the mollified path, because the labeling assumes uniform left-continuity while the time-reversal and composition of a left-continuous step function are not left-continuous. That representation stays as dormant, unit-tested scaffolding (`goc::LinearFunction::is_vertical`/`sweep_endpoints` and the step-capable `Value`/`Inverse`) for a future exact redesign; production never creates a true vertical. Soundness is validated by a randomized differential fuzzer against the ILS heuristic (`tests/test_prover_fuzz_soundness.py`, `tests/td_fuzz.py`): random subsampled instances (value-jump ATFs preserved) must certify no optimum above the ILS feasible cost and no warm-start-dependent value. Re-stamping Rifki2020 remains gated on a large fuzz sweep staying clean plus a nancy.g5k re-certification of Rifki2020 n≤50. **Amendment 2026-07-10: the g5k re-certification REFUSED re-stamping.** The two-arm campaign (300 instances, cold + warm in separate processes, TL 600) found 10 materially warm-start-dependent certifications on the gcc-13/Debian build (largest: TDVRPTW n=10 Rifki-25, cold `Optimum` 4820 vs true ≤ 4357, 10.6%), while the same instances are sound on the local NixOS build from **bit-identical solver payloads** (sha256-equal `to_lera_payload`). The exact `Inverse` fix is therefore necessary but not sufficient: the mollifier's steep bridges keep the labeling's epsilon comparisons on an FP knife-edge, and which side a comparison lands on is toolchain-dependent. Certification on stepwise ATFs stays blocked until the labeling handles value jumps exactly (with per-jump continuity handling; the dormant vertical scaffolding in goc is the starting point). Any future stepwise certification campaign must additionally gate on cross-platform agreement (two independent builds, same payload, equal certified values) besides warm-start independence. Campaign report: workspace `reports/experiments/2026-07-10-rifki-recert-g5k.md`. **Amendment 2 (2026-07-10, later the same day): the cross-platform divergence mechanism was found and fixed — uninitialized-memory UB, not FP codegen.** `BidirectionalLabeling::symmetric` was never initialized (upstream set it from the experiment JSON, e.g. `bpc_lera.json: false`; the vendoring dropped that wiring), so `t_m = symmetric ? T/2 : T` read per-binary-deterministic stack garbage: `false` on the local NixOS binary, `true` on the g5k gcc-13 binary. With the new `KAYROS_LBL_SYMMETRIC=1` toggle the local build reproduces the g5k campaign's wrong cold optima bit-for-bit (Rifki-25 n=10: 4820 vs true 4357; Rifki-17/19/6 n=20: 8423/7725/7084), and with `symmetric=false` all certify the true optima. The fix pins `symmetric = false` (the upstream Lera BPC configuration; asymmetric, `t_m = T`). Two facts remained at that point: the symmetric merge path appeared unsound on stepwise ATFs, and the asymmetric path had passed every gate so far (both statements are superseded by Amendment 3 below: the shared root cause was elsewhere and both paths now certify soundly). **Amendment 3 (2026-07-10, the root cause): the persistent unsound-certification mechanism was the PRICING LADDER's termination logic in the kayros bridge, not the PWL arithmetic.** `run_ladder` escalated (Heuristic-Cost -> Heuristic-Elementarity -> Exact) only while the pricing pool came back empty; the M5.6 fingerprint dedup lets a heuristic level return a non-empty pool consisting entirely of already-present columns (labeling-arithmetic reduced cost marginally negative where the checker-exact master cost is not), so zero columns reached the master and the BCP correctly read "no new columns" as node-optimal, closing the branch-and-price **without the Exact level ever running** (proven by pricing-level logs: certificates with 0 Exact iterations). Fixed by added-driven escalation (`run_one_level` + `run_ladder_and_add`): pricing is exhausted only when the EXACT level itself contributes no new column. After this fix every pinned reproducer in every mode (asymmetric, symmetric, exact/mollifier-off) certifies its checker-validated optimum cold == warm, all soundness gates are hard gates, and the full suite passes with zero expected failures. The earlier amendments' phenomenology (warm-start dependence, apparent regressions and cures from unrelated changes) is fully explained by where the heuristic-level stall happened to land on each trajectory. **Every certificate produced since the M5.6 dedup — including the standing Dabia2013/Ari2018/Vu2020 M5.7 stamps — was produced with the broken ladder and must be re-validated; the re-certification campaign covers ALL families n<=50 with the fixed ladder before any stamp is trusted or issued.** **Amendment 4 (2026-07-13, campaign complete; shipped in kayros 0.5.0):** the definitive re-certification ran over ALL 1352 instances of the four legacy families in both problem types (not only n<=50), four runs each, across a main wave and two extended-limit continuations. Final: 468 certified proven optimal (170 checker-valid strict improvements), 0 four-way disagreements remaining (the 13 the main wave surfaced were traced to a set-valued-duration defect at departure-choice plateaus and fixed with `PWLFunction::MinValueAt`, then closed by the repair plus longer time limits), 23 refused by the pricing-integrity guard, 4 unfinished for a full-horizon memory pathology. Every optimality stamp in the MAMUT store now carries audited-protocol provenance. **The production pricing keeps the mollifier**: the retained numerics plus the multi-gate protocol are the shipped, sound configuration, and the single-run stepwise stamp guard stays active (no stepwise certificate may be minted outside the campaign protocol). The exact value-jump labeling (dormant scaffolding above) remains the principled endgame that would retire the mollifier and the guard once its own residual incompleteness is closed; it is tracked as workspace Stream 13, not part of 0.5.0. **Amendment 5 (2026-07-18, closing; shipped in 1.1.0): the exact value-jump labeling is complete and is the production path on stepwise ATFs; the forward mollifier is retired and the stepwise stamp guard is lifted.** The dormant scaffolding was activated and its residual incompleteness was root-caused by checker-refereed witness-column tracing as three stacked defects, each fixed (M13.0): (a) the label-extension composite `(D + tau) ∘ dep` erases position-dependent mandatory waiting when a departure-function plateau meets a same-abscissa value jump; on step-carrying arcs the extension now uses the elapsed-time identity `(D − Id) ∘ dep + Id`, and fully-early extensions take the attained value at verticals (`MinValueAt`); (b) the solution pool deduplicated candidate routes by customer vertex set, so a checker-cheaper ordering of the same customers could stay shadowed behind a dust-carrying bound of another ordering; on the exact path the pool is keyed by the full path; (c) `PWLFunction::Compose`, `operator+` and `operator*` dropped or collapsed tagged verticals (stacked-vertical tails lost at operand exhaustion, choice verticals collapsed to a representative point, plateau composition at a vertical abscissa reading the unattained bound); all three now carry the jump/choice vertical tags with attained-endpoint pairing throughout. Validation ladder before promotion, all green: the three pinned reproducers certify their checker-validated optima cold == warm (largest prior error: TDVRPTW n=50 Rifki-17 certifying 21331 against true optimum 21319); the differential fuzzer is clean in both labeling modes; jump-free instances are bit-identical to the mollified build (the reverse-side `continuize_value_jumps` stays in place for them and is bypassed on the exact path, so the audited non-stepwise arithmetic is untouched; the `duration_at` clamps remain as inert safety guards); cross-platform gate 13/13 (NixOS vs gcc-13/Debian builds, identical payloads, equal certified values); a full-family Grid'5000 sweep (both Rifki2020 problem types, 778 runs) with zero unsound certificates, zero checker-infeasible priced columns and zero cross-run disagreements; and a 1444-run re-certification on the promoted build (Rifki2020 both problem types, four runs per instance: cold and warm starts crossed with the two labeling modes, plus the last pricing-integrity refusal, Vu2020 n=99), re-confirming 93 stored certificates at their exact stored values with zero checker-infeasible priced columns anywhere and only honest time-limit or memory verdicts elsewhere. Bridge consequences: stepwise ATFs are auto-detected per instance and routed to the exact path by `solve_duration` itself (`KAYROS_STEP_EXACT`), the forward-side mollifier `_continuize_breakpoints` is deleted, and single-run stepwise stamping is enabled (the refusal guarded the retired mollified path). The stepwise pricing-integrity refusals of the 2026-07-13 campaign (22 Rifki2020 TDVRPTW instances; the 23rd, Vu2020 n=99, is not stepwise and had already closed under the long-limit continuation, re-confirming cleanly on this build) are resolved structurally: the checker-infeasible pricing phenomenon behind them does not occur on the exact path, and those instances stay uncertified only where the time limit intervenes. **Amendment 6 (2026-08-05): the M13.0 gating premise was false, and Amendment 5's "jump-free instances are bit-identical to the mollified build" claim with it. From 1.1.0 through 1.5.0 the prover OVER-CERTIFIED jump-free instances.** M13.0 selected its exact arithmetic per operand, on `has_vertical` / `is_vertical()` / `domain.IsPoint()` predicates, on the stated ground that only step-carrying data produces verticals. It does not: `PWLFunction::Inverse` turns every departure-function plateau into a CHOICE vertical, and a label's duration is set-valued at departure choices, both ordinary on jump-free time-dependent instances (probe on TDVRPTW/Vu2020/n=59/Vu-A5-pA-d90-w40: 6560 step-arc extensions against 1387 legacy, 3307 selected by `dep` and 4958 by the label duration, and zero JUMP verticals anywhere in the solve). The exact path's deliberate over-estimation at unattained jump-gap abscissae is the sound side only against exact-path semantics; applied to a choice plateau of a continuous function it inflates label durations, and cold solves closed the tree at values strictly above checker-valid solutions: `Optimum` 2764.379834028286 against the true 2760.1098340282865 on that instance (+4.27), and +9.88 / +3.12 / +1.36 / +0.79 / +0.29 on five further Vu2020 instances (n=59 Vu-A5-pB-d90-w100, n=79 Vu-A1-pA-d98-w80, n=59 Vu-A2-pB-d98-w60, n=79 Vu-A2-pB-d90-w80, n=99 Vu-A1-pA-d90-w100). A bisect isolated two independently sufficient layers of the M13.0 change: the elapsed-time extension identity in `monodirectional_labeling.cpp`, and the `Compose` (CHOICE verticals carried through an increasing inner) plus `operator+` (sweep-order endpoint pairing, stacked-boundary hold-back) changes in `goc/src/math/pwl_function.cpp`; neutralizing both reproduces the pre-M13.0 build bit-exactly. **Fixed (M13.3)** by gating both layers on the SOLVE being step-carrying rather than on per-operand vertical detection: `goc::step_exact_arithmetic()` is a process-global flag that `kayros::lera::solve_duration_json` scopes from `KAYROS_STEP_EXACT` (the per-instance decision the Python bridge already makes from the ATFs), so jump-free solves take the audited kayros 1.0.0 arithmetic and step-carrying solves keep M13.0 unchanged. This restores the intent Amendment 5 documented; what was missing was a gate matching it. Consequence: **every jump-free optimality stamp minted by 1.1.0 through 1.5.0 must be re-validated on a build carrying this fix** (Amendment 5's own jump-free bit-identity gate could not have caught this, because it compared certified values only where the two paths happened to agree). Stamps minted before 1.1.0 and all step-carrying stamps are unaffected. Standing regression gate: `tests/test_lera_stepwise_soundness.py::test_jumpfree_vu2020_certificate_is_not_above_the_truth`. **Amendment 7 (2026-08-05): the REVERSE-side `continuize_value_jumps` was investigated for removal, found to be load-bearing on the jump-free path, and RETAINED.** With the forward mollifier gone since M13.0, the reverse-side helper looked like the last piece of mollifier residue, believed to be a structural no-op on jump-free data (it was documented as such in the call-site comment and in Amendment 5). It is not. An instrumented build shows it rewrites most reverse arrival functions on ordinary continuous instances: on TDVRPTW/Vu2020/n=59/Vu-A5-pA-d90-w40, 229 of 314 reverse arcs change, 158 CHOICE verticals are flattened and 128 steep bridges are inserted; on TDVRP/Dabia2013/n=25/C101, 590 of 650 arcs change with 566 bridges and no vertical present at all; zero JUMP verticals appear on either. Two ordinary features of jump-free data reach it, and the helper distinguishes neither from a genuine value jump: (a) CHOICE verticals, which `PWLFunction::Inverse` creates from every departure-function plateau and which `FlipTime`/`FlipValue` carry into the reflected reverse arrival (a vertical's `Value` returns its attained endpoint at both domain endpoints, so it contributes one breakpoint and its other endpoint reappears as a duplicate-x against the next piece); and (b) coincident abscissae between ordinary pieces, since pieces store slope/intercept and `Value(domain.left)` need not round-trip bit-exactly to the y that constructed the piece. Flattening the verticals is precisely the invariant the audited kayros 1.0.0 reverse arithmetic assumes: a jump-free solve runs with `goc::step_exact_arithmetic() == false` (M13.3), so goc applies its legacy vertical handling, and verticals surviving into the reverse `arr`/`tau`/`dep`/`pretau` make it over-certify. Measured on the removal build, cold/asym TL 600, one process per solve: TDVRPTW/Vu2020/n=59/Vu-A2-pB-d98-w60 certified `Optimum` 1278.2616212247312 against a stored checker-valid 1275.8316212247312 (+2.43) and Vu-A2-pB-d98-w100 certified 1852.9437142855365 against 1846.8070188618972 (+6.14), both with the tree closed and both reproducing bit-for-bit on re-run; the other ten instances of a twelve-instance panel (four jump-free, six stepwise) were bit-identical, and the stepwise half is inert by construction because the call site is already skipped under `KAYROS_STEP_EXACT`. This is Amendment 6's failure mode mirrored onto the reverse side, and it is the reason Amendment 5's "the reverse-side `continuize_value_jumps` stays in place for them" must be read as a requirement rather than an accident. Removing it for real means making the reverse instance vertical-safe under exact arithmetic (extending the M13.3 gate to the reverse construction, or replacing the 1e-3 bridging with an exact vertical-flattening pass); that is its own project with its own validation ladder, deferred. The `duration_at` domain clamps were assessed in the same pass and also retained: 47552 instrumented calls across six jump-free and stepwise solves never went out of domain, but nothing enforces `rw` inside `dom(duration)` and the alternative to clamping is a thrown exception, not a better value. Standing regression gates for the reverse side: `tests/test_lera_stepwise_soundness.py::test_jumpfree_reverse_side_certificate_is_not_above_the_truth` (Vu-A2-pB-d98-w60 and Vu-A2-pB-d98-w100), which the pre-existing Vu-A5-pA-d90-w40 gate does not cover: it passes on the removal build.

10. **Memory self-guard (M13.2, kayros-added; shipped in 1.0.0)**: new `goc/include/goc/base/memory_monitor.h` (`goc::MemoryMonitor`, process-global RSS watermark, throttled `/proc/self/statm` poll, sticky once tripped). Polled at the same loop heads as the M5.2 deadline (the monodirectional and bidirectional labeling loops, the BCP node loop, the bridge's pricing ladder and column adds), so a solve whose pricing would outgrow the machine (the full-horizon TDVRP label accumulation behind Amendment 4's "4 unfinished for a full-horizon memory pathology") unwinds cleanly with the new `MLBStatus`/`BLBStatus::MemoryLimitReached` enumerators, mapped through the pre-existing `CGStatus`/`BCStatus::MemoryLimitReached` plumbing, instead of being OS-OOM-killed with no verdict. The same M5.2 correctness rule applies: a memory-truncated pricing pass or strong-branching probe stamps the status so it can never masquerade as node-optimality, and the final labeling status resolution gives `MemoryLimitReached` precedence over `Finished`/`SolutionLimitReached`. The guard observes only (no numeric state): an armed, untripped watermark is bit-identical to no watermark. The limit arrives via `SolveParams::memory_limit_mb` (Python default: machine-derived auto limit).

Behavioral changes inherited from the half-port (upstream `TDVRPTW-solver`, not introduced here): subset-row cut violation threshold raised from `> 0` to `> 0.1` in `BCP::SeparateCuts`.

## Conventions

This subtree is **vendored code**: kayros' plain-old-data / style conventions do not apply here. Keep diffs against upstream minimal and documented in this NOTICE.
